# Fresh install walkthrough -- 4060, 2026-09-25

Purpose: find friction and bugs in the first-run path. Every step is logged,
not only the failures. Machine: RTX 4060 Laptop, 8 GB VRAM. HEAD at start:
c292539cd475d29bf9315849195ae008152f6180. All actions performed through the
Comfy Desktop app UI (mouse/keyboard), not a headless launcher, unless a step
says otherwise.

Legend: FRICTION = a first-time user would likely be confused, stuck, or need
outside knowledge. BUG = something that is actually broken.

## Log

1. C:\Users\jeffr\AppData\Local\Comfy-Desktop\ComfyUI-Installs\ was confirmed
   empty (operator uninstalled Comfy Desktop by hand, then reinstalled it) at
   2026-09-25T15:08Z. Starting state: no ComfyUI-Installs, no custom_nodes,
   no C:\ComfyUI-Models (operator wiped all downloaded weights beforehand at
   the same fresh-user request).

2. 2026-09-25T15:30Z. Took control of the desktop (computer-use). Comfy
   Desktop was already open, landing on its instance picker: "Personal"
   workspace, INSTANCES 1 (this count appears to include the Comfy Cloud
   tile, not a local instance -- only two tiles show: "+ New Instance / Set
   up a fresh ComfyUI environment" and "Comfy Cloud (TRY FREE)"). No local
   instance card exists. Signed in as jbrick2070@gmail.com already (this is
   the operator's own account on the operator's own machine, not a brand-new
   signup -- the account/login step itself is out of scope for this walk;
   what is in scope starts at "New Instance"). Not FRICTION or BUG, just
   starting state. Clicking "+ New Instance" next.

3. 2026-09-25T15:31Z. Clicked "New Instance". "Create a New Instance" form
   appeared: Name (placeholder "ComfyUI"), build type tabs Public
   Builds/Managed Builds/Remote Connection (Public Builds selected by
   default), Release dropdown "Stable (Recommended)", ComfyUI version
   dropdown "v0.37.2 (Recommended)" -- note this is newer than the v0.36.0
   the prior (now-deleted) headless install ran; a fresh user gets a
   different ComfyUI core version than the one this session tested Part B
   on before the wipe. Hardware picker auto-selected "NVIDIA RECOMMENDED"
   (correctly detected the 4060; good default, no friction). Install
   location defaulted to
   C:\Users\jeffr\AppData\Local\Comfy-Desktop\ComfyUI-Inst... (truncated in
   UI), 546.56 GB free, ~4.96 GB required. All defaults look sane for a
   first-time user; accepting every default and clicking Continue next.

4. 2026-09-25T15:31Z. Clicked Continue. Landed on "Choose a starter
   workflow" (Video/Image/3D/Audio tabs; Video selected by default) offering
   MiniMax H3 (~53 GB, marked Recommended), Seedance 2.5 (REQUIRES CREDITS),
   Wan 2.1 (~11 GB), Wan 2.1 Fun Cam (~11 GB). None of these are OTR --
   they are Comfy's own generic starter workflows, unrelated to the pack
   this walk is installing. FRICTION: a user who came here specifically for
   OTR has no cue on this screen that these are optional/unrelated to their
   goal, and the visually loudest option is "Recommended" MiniMax H3 at
   ~53 GB -- a user could easily click Install here and download 53 GB of
   an unrelated model before ever reaching the OTR pack. Choosing
   "Skip & Install" (bottom left of the two buttons) instead, since none of
   these apply.

5. 2026-09-25T15:32Z. Install progress screen appeared: step list
   "Downloading ComfyUI" (done) -> "Unpacking the install" (in progress,
   0% extracting) -> "Set up environment" (pending). Overall progress bar
   28% at first read. Waiting for this to finish before continuing.

6. 2026-09-25T15:35Z. Install finished end to end (Downloading ComfyUI ->
   Unpacking -> Set up environment -> Clean up environment -> Update to
   latest stable -> Starting ComfyUI -> Running security scan -> Loading
   libraries -> Initializing GPU -> Loading custom nodes -> Starting server
   -> 100%), about 4 minutes total wall time from clicking Skip & Install.
   No prompts, no errors, no manual input needed anywhere in this sequence
   -- good, low-friction base install. Landed directly in the ComfyUI web
   UI with a "Templates" browser overlay open showing generic starter
   graphs (MiniMax H3, LTX-2.3, Z-Image-Turbo, Krea-2, Wan 2.2, Qwen, etc.)
   -- none are OTR. Closing this template browser and going to the
   Extensions/Manager entry point next, to install OTR the way a fresh
   user browsing the node registry would (per operator instruction: use
   the registry/extensions search, not manual file placement).

7. 2026-09-25T15:36Z. The top toolbar "Extensions" button opens a combined
   Templates/Nodes Manager overlay; first click landed on the Templates tab
   (workflow graphs, not node packages) -- mildly confusing, since
   "Extensions" reads as "browse installable node packs" but opened
   workflow templates first. Typed "old time radio" -> no template results
   (expected: OTR is not a bundled template). Navigated to "Nodes Manager"
   ("All Extensions" in the left rail of the same overlay) and searched
   "old time" there instead. Two results: ComfyUI-PixydustQuantizer
   (unrelated, 6851 downloads) and ComfyUI-OldTimeRadio (29 downloads,
   version 2.3.2, published by "fluxus", dated Sep 21 2026, description
   "Press Queue once: ComfyUI writes or adapts a radio play, casts and
   performs it with neural voices... composes the score, and outs a...").
   OTR is findable by name search, ranks second by downloads under a
   generic query -- fine, not a real friction point. NOTE (not a bug, but
   material to what this test can prove): the registry serves 2.3.2, not
   the unreleased main HEAD this session pulled directly (c292539c /
   e7a4806a). 2.3.3 was still Pending as of this project's last handoff
   note. A real fresh user installing through Manager right now gets
   whatever shipped in 2.3.2 -- which predates the AnimateDiff
   auto-download fix (PR #6) and the asset_cleanup widget entirely, both
   merged to main after 2.3.2 published. This run will surface exactly
   what a real user hits today, which is older and less capable than what
   this session was regression-testing on main. Clicking Install on
   ComfyUI-OldTimeRadio.

8. 2026-09-25T15:37Z. FRICTION/BUG candidate, in the Comfy Desktop Manager
   UI itself, not in OTR's own files: clicking Install opened a "Select
   Version" dropdown defaulting to "Latest (2.3.2)" (checked), with 2.3.3,
   2.3.1, 2.3.0, 2.1.6 listed below it, unchecked. At the same time the
   "Node Pack Info" side panel for the same pack (same downloads count 29,
   same publisher fluxus) reads: Last Updated Sep 24 2026, Status Active,
   Version 2.3.3. So the version picker's "Latest" checkmark (2.3.2) and
   the info panel's own "Version" field (2.3.3) disagree about which
   version is actually latest/active, in the same view, at the same time.
   A first-time user has no way to resolve this discrepancy themselves --
   picking the checked default (2.3.2) looks reasonable but silently skips
   a numerically newer, Active version sitting one row below it. Operator
   said to use 2.3.3; selecting it explicitly from the dropdown rather than
   accepting the "Latest" default. This is real, live-observed evidence
   (not a fixture) so it is admissible; whether it is caused by OTR's own
   publish metadata or by Comfy Desktop's manager cache is not yet known
   -- flagging to the 5080 rather than guessing.

9. 2026-09-25T15:37Z. Selected "2.3.3" explicitly in the dropdown (operator
   said use 2.3.3) and clicked Install. UI now shows "Installing" spinner
   on the action button and a toast at bottom "Installing
   comfyui-old-time-radio 0 of 0". Waiting for it to finish.

10. 2026-09-25T15:38Z. Install finished, about 15-20s (fast -- node pack
    itself is small, no model weights bundled, matches CLAUDE.md's design:
    weights fetch later at queue time, not at pack-install time). A green
    toast appeared: "To apply changes, please restart ComfyUI" with an
    "Apply Changes" button. The card in the list still shows a plain
    "Install" button rather than an "Installed"/"Update" state -- checking
    "All installed" in the left rail to confirm it actually took, before
    trusting the card's own button label.

11. 2026-09-25T15:38Z. "All installed" (with the "old time" filter still
    active) showed "No results found matching your search." Cleared the
    search filter: "All installed" then read "No Extensions Installed --
    You haven't installed any extensions yet." So the install is staged
    only -- it has not taken effect in the running Python process, and the
    Manager's own "installed" list does not reflect it until restart. Not
    a bug (ComfyUI cannot hot-load a new custom node's Python import
    without restarting), but worth recording: a first-time user checking
    "did it work?" right after Install, before restarting, sees "you
    haven't installed any extensions" -- which reads as the install
    having silently failed, when it actually just needs the restart the
    toast already told them to do. Clicking "Apply Changes" to restart
    ComfyUI now.

12. 2026-09-25T15:39Z. FRICTION, in Comfy Desktop itself: "Apply Changes"
    showed a "Restarting backend to apply changes... 1 of 1" toast that
    sat unchanged for 35+ seconds. Opened Logs (in-app "Console" tab):
    "Unable to load logs, please ensure you have updated your ComfyUI
    backend" -- unhelpful/alarming wording while a restart is legitimately
    in progress. Opened the separate Instance Manager's Terminal tab
    instead: shell sitting at an IDLE prompt
    (.venv) PS ...ComfyUI-Installs\ComfyUI\ComfyUI>, no server process
    visible, no boot output -- the "restart" toast in the main window had
    stopped meaning anything and the backend was simply not running. A
    first-time user has no way to know this from the toast alone. Had to
    explicitly click "Restart" in the Instance Manager (confirmed a
    "Restarting will stop the running session, unsaved work will be lost"
    warning) to actually bring the server back up. Filing as BUG candidate
    against Comfy Desktop's own apply-changes flow (not an OTR file), for
    the 5080/operator to judge -- OTR has no code in this path.

13. 2026-09-25T15:40Z. Restart is progressing for real this time: 15%,
    "Initializing GPU" (Configuring GPU memory), "Loading custom nodes"
    next. Waiting for full boot to check whether OTR's nodes actually
    register.

14. 2026-09-25T15:42Z. BUG, confirmed three independent ways. After boot
    finished, searched the Nodes panel for "OTR" (fuzzy-matched unrelated
    built-ins only) and for "Ledger" (exact): "No nodes match 'Ledger'".
    Checked the boot log's "Import times for custom nodes:" section
    directly: it lists exactly one line,
    "0.0 seconds: ...\ComfyUI\custom_nodes\websocket_image_save.py" -- no
    comfyui-old-time-radio entry, not even a Skipped or error line, as if
    the folder never existed for this boot. Checked disk directly:
    C:\Users\jeffr\AppData\Local\Comfy-Desktop\ComfyUI-Installs\ComfyUI\
    ComfyUI\custom_nodes\ contains only example_node.py.example and
    websocket_image_save.py -- no OTR files were ever written. Checked
    ComfyUI-Manager's own persistent log (user\comfyui.log) for
    "old.time.radio" or "fluxus": zero matches -- the Manager's install
    action left no trace anywhere that it was ever invoked. Reopened the
    Nodes Manager just now: the ComfyUI-OldTimeRadio card is back under
    "Not Installed" with a plain "Install" button, as if the Install click
    from step 9 never happened.
    BUG: clicking "Install" (having explicitly picked version 2.3.3 from
    the version dropdown) showed a full, convincing success sequence --
    spinner, "Installing comfyui-old-time-radio 0 of 0" toast, then a
    green "To apply changes, please restart ComfyUI" toast -- and silently
    did nothing. No files written, no log line, no error surfaced to the
    user anywhere. A first-time user has zero signal that this failed;
    every visible cue said it worked. This is the single most serious
    finding of this walk: the low-friction registry install path is
    currently broken end to end for this pack on this box. Filed to the
    5080 for a fix; retrying the Install click now to see if it is
    reproducible or transient.

15. 2026-09-25T15:49Z. Retried Install once more, watching closely, no
    other interaction this time. Same shape: near-instant "Installing"
    spinner, then straight to "Restarting backend to apply changes...
    1 of 1" which hung again -- confirmed via PowerShell process
    inspection: the two python.exe processes serving :8188
    (C:\...\ComfyUI-Installs\ComfyUI\ComfyUI\.venv\Scripts\python.exe,
    confirmed as the real per-instance venv, not a shared standalone-env)
    were gone/cycling and port 8188 had no listener mid-"restart". This is
    reproducible, not a one-off. 5080 confirmed independently: this is
    Comfy Desktop's own Manager/registry-fetch mechanism, not anything in
    OTR's nodes/, workflows/, or __init__.py -- nothing here to fix in the
    repo. No newer registry version exists to try instead (2.3.3 is
    correctly Active; 2.3.4 with asset_cleanup is planned but not
    published). Per the 5080's direction: stop spending time on the
    Manager-install path here, fall back to the already-proven
    `git clone` checkout of main to actually exercise Part B's render
    legs, and record this as its own PROD_BUG_LOG.md entry rather than
    letting it block the regression wave. Continuing there next.

16. 2026-09-25T15:53Z. Operator overrode the CLI-fallback plan: no
    headless launcher, keep everything inside Comfy Desktop's own GUI, and
    the operator rebooted Comfy Desktop again by hand. This time it took:
    Nodes panel search for "Ledger" now finds
    OldTimeRadio > LPL Script Writer (v2.0), and under v2 >
    LFC Ledger Freeze Cascade, and under v2/audio > Cast Lock (v2
    ledger...). The pack is genuinely loaded. Whatever was wrong in steps
    12-15 self-resolved on a later reboot rather than being fixed by any
    action taken here -- consistent with the earlier finding being real
    and intermittent, not a one-off misread. Proceeding to load a Part B
    variant workflow into the canvas and run it through the GUI Run
    button, per operator instruction, rather than the API runner script
    or a headless launcher.

17. 2026-09-25T16:00Z-ish. Loaded otr_canonical via Templates -> EXTENSIONS
    -> comfyui-old-time-radio to answer the operator's worry about missing
    variant entries: confirmed "Showing 1 of 1 templates" -- only
    otr_canonical is exposed there, matching README wording exactly
    ("There is exactly one entry, otr_canonical"). The 24 profile variants
    are not meant to appear in this gallery at all; they are
    generated files for the CLI runner / drag-onto-canvas path, not
    per-profile gallery entries. Separately confirmed the two-category
    Add-Node menu ("OldTimeRadio" vs "OTR") is a real, tiny inconsistency:
    every node's CATEGORY is "OldTimeRadio" or "OldTimeRadio/..." except
    nodes/vram_context_test.py, which uses "OTR/v2/Diagnostics". Cosmetic
    (diagnostics-only node, does not affect the shipped lanes), logged
    here rather than as a PBUG since it was found by source reading, not
    a live run.

18. 2026-09-25T16:10Z-ish. Operator had all OTR content deleted from this
    box (D: repo checkout and the installed pack's custom_nodes folder)
    to reset for the next test. Both fully recoverable (repo pushed at
    6fd23d61 with a clean tree; pack re-installable). Re-cloned the repo
    (HEAD c739d081) to keep logging.

19. 2026-09-25T16:26Z. 5080 relay: registry 2.3.4 is published
    (NodeVersionStatusPending -- Comfy-Org's scan has not promoted it, so
    Manager's "Latest" default will not pick it up on its own). Operator
    instruction: relaunch and reinstall picking 2.3.4 EXPLICITLY from the
    version picker, not a plain Install click, to test whether a Pending
    version can be hand-selected. Relaunched Comfy Desktop (the app had
    fully closed, not just the server -- 0 python.exe processes running).
    otr_canonical reopened automatically with "2 errors found" (expected:
    the pack's node types are unresolved since custom_nodes is empty
    again after step 18). Opened Manager, searched "old time" (session
    remembered the query), opened the version picker: "Latest (2.3.3)"
    now correctly matches the info panel's own Active/2.3.3 read (the
    step 8 mismatch is gone -- Active version moved forward and the two
    readouts now agree). 2.3.4 is listed with a distinct warning-triangle
    icon instead of the blue-shield icon every scanned version gets --
    accurate, useful signaling for "unscanned/Pending", not a bug.
    Selected 2.3.4 explicitly, clicked Install.

20. 2026-09-25T16:27Z. Install this time visibly took: the card flipped
    from an "Install" button to an Enabled ON/OFF toggle, the info panel
    now reads Uninstall / Enabled: true / Version: 2.3.4 / Status: Active
    / Downloads: 30, and the usual "To apply changes, please restart
    ComfyUI" toast appeared. This directly answers the 5080's question
    about PBUG-20260925-01: it is NOT a permanent no-op. Across four
    attempts this session the shape was install-succeeds-but-does-not-
    load (steps 8-11), install-silently-no-ops twice more (steps 12-15),
    pack-loads-after-an-unrelated-later-reboot (step 16), and now
    install-visibly-registers-immediately (this step, different version,
    different session state). The common thread is NOT "which version" --
    2.3.3 both failed (steps 8-15) and is what the "Latest" default still
    resolves to now; the variable is something about the RESTART/reload
    cycle, not the picked version. Updating the PBUG entry to reflect
    intermittent-not-permanent once this install is verified end to end
    (node search, boot log, disk). Restarting now.

21. 2026-09-25T16:28Z. Restart completed with NO "2 errors found" banner
    (present before this restart, from the empty custom_nodes state).
    Nodes panel search "Ledger" immediately finds OldTimeRadio >
    LPL Script Writer (v2.0) / v2 > LFC Ledger Freeze Cascade / v2/audio >
    Cast Lock -- loaded on the FIRST restart after this Install, unlike
    every 2.3.3 attempt earlier today. Templates -> EXTENSIONS ->
    comfyui-old-time-radio now reads "Showing 25 of 25 templates" (was 1
    of 1 before the wipe) -- confirms the 5080's report: otr_canonical
    plus all 24 machine variants (otr_16gb_*, otr_8gb_*, otr_8gb_ltx25_
    native_*, otr_24gb_native_foley, otr_amd_still, otr_cloud_*,
    otr_mac16_*) are individually browsable now that they live at
    workflows/ top level instead of workflows/variants/. Opened
    otr_8gb_low from the gallery: loads cleanly, no 404, new tab named
    "otr_8gb_low". Confirmed asset_cleanup default by reading the actual
    file loaded (workflows/otr_8gb_low.json): OTR_LedgerScriptWriter
    widgets_values is 36 wide, last value "off (keep everything)" --
    matches the 5080's item 2, correctly off by default.

22. PBUG-20260925-01 update, answering the 5080's question directly: this
    is INTERMITTENT, not a deterministic "always needs a second restart"
    and not a permanent no-op either. Timeline this session: attempt 1
    (2.3.3, explicit version pick) -- convincing success UI, zero trace
    on disk/log/Manager-log (steps 8-11). Attempt 2, immediate retry, same
    version -- identical no-op, reproduced (steps 12-15). Some time later,
    after the operator's own unrelated app reboot (no new Install click
    initiated by this session in between) -- the pack was found loaded
    (step 16); whether that reboot alone flipped a half-applied install
    live, or a fresh Install happened outside this session's view, is not
    established either way. Attempt 3 (today, this entry): custom_nodes
    wiped clean first, 2.3.4 explicitly picked, single Install + single
    restart -- worked immediately, first try, no retry needed. Net: the
    same Install action has now been observed to no-op twice in a row and
    then succeed cleanly twice (once eventually, once immediately) under
    different-but-overlapping conditions (different version, different
    starting custom_nodes state, different restart path). Not enough
    signal to name the exact trigger; recording as CONFIRMED INTERMITTENT
    rather than deterministic, and leaving PBUG-20260925-01 as filed
    (still a real, live-verified Comfy Desktop defect) with this
    additional evidence appended rather than reclassified.

## Part B, leg B2 (otr_8gb_still) -- 4060, on 2.3.4

23. 2026-09-25T16:56Z. 5080 direction: continue Part B from the GUI as a
    person would, starting with otr_8gb_still (fastest proof), act_count
    1, asset_cleanup off, press Queue, log every step. Closed the stale
    otr_8gb_low tab (had an unsaved-changes dot, unclear provenance) and
    reopened otr_8gb_still fresh via Templates search (typing directly
    into the gallery's search box worked this time -- "Showing 1 of 597
    templates", opened cleanly, new tab, no error banner). Confirmed
    workflows/otr_8gb_still.json's own Preflight - Workflow Validator node
    reads workflow_json_path = "workflows/otr_8gb_still.json" and
    profile_id = otr_8gb_still -- correct file, correct profile, matches
    what Templates loaded.

24. Panned/zoomed to the writer node (OTR_LedgerScriptWriter, 36 widgets --
    zooming via the bottom-right percentage field and click-dragging to
    pan, since scroll-wheel zoom moves in tiny increments and node text is
    unreadable below roughly 50%). act_count already read 1 -- the
    operator helped by setting it directly in the GUI while I was
    navigating, so no widget edit was needed from me. Did not visually
    re-confirm asset_cleanup in the live canvas (that node is tall and a
    pan attempt hit a text-area widget instead of the canvas); relying on
    the file-level confirmation already logged (workflows/otr_8gb_still.json,
    last of 36 widgets, "off (keep everything)") since nothing has edited
    that file or that specific widget. Writer's creative/technical model
    both show "Qwen/Qwen3.5-4B (8.7 GB download, mac16-tight nv8-nf4
    nv16 nv24)" -- first real friction/cost signal: this leg needs an
    8.7 GB LLM download on top of whatever else queues, unless a Kokoro
    voice or other 8gb_still-specific weight is already cached from
    C:\ComfyUI-Models being wiped earlier today (still empty). Pressing
    Run (top-right button, this UI's Queue) next.

25. 2026-09-25T16:57Z. Pressed Run. Queued immediately: progress panel
    shows "Total: 0%, Current node: Preflight - Workflow Validator, 0%,
    1 running, 0 queued". Preflight node outlined green (executing) within
    ~1s of the click -- no dialog, no confirmation prompt, no download
    prompt yet. Watching for the writer's model download next (8.7 GB
    Qwen3.5-4B expected per the widget values).

26. 2026-09-25T16:58Z. AUTO-DOWNLOAD CONFIRMED LIVE, on a genuinely wiped
    C:\ComfyUI-Models. Console (LOGS tab) shows real-time lines:
    [OTR.assets] DOWNLOAD z_image_turbo_bf16.safetensors bytes=0/12309866400 elapsed_s=0.2
    ... climbing steadily every ~5s (182266942, 339903577, 424224592,
    694131344, 950585063, 1214636279 ... 3353939601/12309866400 at
    elapsed_s=76.2). No dialog, no confirmation prompt asked anything --
    it just started fetching the ~12.3 GB z_image_turbo checkpoint the
    moment the graph needed it, exactly the low-friction behavior this
    wave exists to prove. Roughly 27% at 76s implies roughly 4-5 minutes
    for this one file alone; still queued behind it: the Qwen3.5-4B
    writer models (8.7 GB) and whatever else otr_8gb_still needs (Kokoro
    voices, Stable Audio 3). Continuing to watch.

27. 2026-09-25T17:01Z. z_image_turbo_bf16.safetensors finished cleanly:
    "[OTR.assets] DOWNLOADED z_image_turbo_bf16.safetensors
    bytes_verified=12309866400 elapsed_s=233.0
    native=C:\Users\jeffr\AppData\Local\Comfy-Desktop\ComfyUI-Shared\
    models\unet\z_image_turbo_bf16.safetensors" -- 12.3 GB in 233 s
    (~52.8 MB/s), byte-verified, landed at the expected models path.
    FRICTION, not fatal: right before/around this, the console printed a
    huggingface_hub warning about Windows symlink support -- "To support
    symlinks on Windows, you either need to activate Developer Mode or to
    run Python as administrator" with a link to HF's cache-limitations
    docs. Did not block or slow the download (it completed and verified
    right after), but it is exactly the kind of scary-looking console
    text a first-time non-technical user would not know to ignore -- CLAUDE.md's own
    "red text in a working boot is not a failure" lesson, applied to a
    library warning instead of a boot log this time. Immediately after,
    the writer model started: qwen_3_4b.safetensors (8,044,982,048 bytes,
    ~8.0 GB) DOWNLOAD begun at bytes=0, now at 1,568,257,436 (~19.5%) at
    elapsed_s=30.6. No dialog either time; auto-download continues to
    just work.

28. 2026-09-25T17:03Z. qwen_3_4b.safetensors finished (8,044,982,048/
    8,044,982,048 bytes) around elapsed_s=132. Writer now actually
    running: "[OTR_LedgerScriptWriter] skeleton ledger saved up front",
    a BUG-LOCAL-290 pending-dir sweep (routine cleanup of stale empty
    dirs from a prior run, not new), "[OTR] Downloading Qwen/Qwen3.5-4B
    -- 8.7 GB -> C:\...\ComfyUI\models\huggingface" (this is a SEPARATE
    HuggingFace-cache download from the qwen_3_4b.safetensors file
    fetched a moment ago -- the writer pulls its own copy through
    transformers/hub caching). Same Windows-symlink UserWarning repeated
    here, plus a new line: "[Selector] proceeding with caution:
    ctx_cap=UNKNOWN@8192, vram_fit=WARN@4.3 GB" -- a WARN-level VRAM-fit
    flag on this 8 GB card, which is exactly the condition this whole
    profile exists to exercise. Not clearly a bug (looks like a
    designed defensive warning, not a failure), but flagging since
    "WARN" plus unknown ctx_cap is worth watching for a later failure.
    'build_shakespeare_briefs' attempt 1/3 also logged, temperature=0.35
    -- the writer LLM call is underway. Continuing to watch.

29. 2026-09-25T17:07Z. Writer generation genuinely running and producing
    real content: "peak_accelerator_gb=2.97 physical_vram_gb=8.00
    cpu_overflow=False disk_offload=False" -- comfortably inside the
    8 GB card, the earlier vram_fit=WARN reads as a conservative safety
    margin, not an actual near-OOM condition (ctx_cap since moved from
    UNKNOWN to PASS@262144). Heartbeat lines show token generation
    climbing (64 -> 403 tok at ~5.5 tok/s, ~71s), with visible story
    content -- a Hamlet-derived scene ("MARCELLUS: Nervous, observant.
    OTHELLO (Ghost): Deep, resonant, spectral whisper", key_terms
    ["Ghost","Fortinbras","Elsinore","Watch"], schema
    OTR_SCHEMA_CONTRACT_V1, model_id shakespeare-otr). First LLM call
    ended cleanly (ended_with_eos=True, 403 tokens). news_interpreter
    OK (6 key_terms), cast RNG seeded (379162559, OS entropy, randomized
    per episode per BUG-LOCAL-269), now on to
    llm_write_description:HORATIO (character description pass, attempt
    1/3). No red, no dialog, no stall. Continuing to watch.

30. 2026-09-25T17:11Z-17:28Z. Operator asked mid-run why "Lumina2" was
    loading when the profile selects z_image_turbo. Checked
    workflows/otr_canonical.json directly: zero occurrences of "lumina"
    or "auraflow" anywhere in the graph -- no separate node requests it.
    Conclusion, confirmed a few minutes later by the actual log order:
    "Model Lumina2 prepared for dynamic VRAM loading" was immediately
    followed by "[OTR.image.z_image_turbo] minted still 1472x832
    seed=849290168 ..." -- "Lumina2" is ComfyUI's own internal
    architecture-class name for the flow-matching DiT implementation
    z_image_turbo's checkpoint runs through (the two share a code path
    in ComfyUI's model backend; Comfy's own stock Z-Image-Turbo template,
    seen earlier in this walk, uses a ModelSamplingAuraFlow node for the
    same reason). Not a second model, not a bug -- one weight file, one
    load event, ComfyUI's internal name for the class it instantiated.

31. 2026-09-25T17:30Z. LEG B2 (otr_8gb_still) PASSES. Full receipt:
    - row: otr_8gb_still, loaded via Templates -> EXTENSIONS ->
      comfyui-old-time-radio -> otr_8gb_still (GUI path, not the CLI
      runner), act_count=1, asset_cleanup=off
    - repo HEAD at leg start: c739d081 (fetched/rebased through the
      session; no code changes landed during the leg)
    - episode: "The Unpaid Half-Share" / dir
      signal_lost_the_unpaid_halfshare_20260925_101511
    - obs file: C:\Users\jeffr\AppData\Local\Comfy-Desktop\ComfyUI-Shared\
      output\otr\obs\unpaid_half_share_20260925_101511__rfrc__stmo__zimg__
      koko__sspr__q354b__sa3_final.mp4 -- 72,557,760 bytes, present,
      timestamped 2026-09-25 10:28 local
    - wall time: "[INFO] Prompt executed in 00:32:09" (console, verbatim)
      -- 32 minutes 9 seconds, GUI Run-click to obs_publish
    - peak VRAM observed: 6,867 MiB / 8,188 MiB (84%) during the
      z_image_turbo stage (spot-checked via nvidia-smi mid-run, not
      continuously sampled, so this is a floor on the true peak, not a
      guaranteed max)
    - exit/result: no CLI exit code in this GUI path; console's own
      terminal signal is [OTR_MasterAudioMux] "duration_check
      v=121.960s a=98.779s tail_budget=23.2s (declared) OK",
      "audio_byte_identical OK (110d3daca844)", "obs_publish OK ->
      ...\otr\obs\unpaid_half_share_...final.mp4" -- all present, no
      red anywhere in the run
    - [OTR.assets] fetch lines: z_image_turbo_bf16.safetensors (12.3 GB,
      verified) and qwen_3_4b.safetensors (8.0 GB, verified) both fetched
      fresh from an empty C:\ComfyUI-Models; the writer's own
      Qwen/Qwen3.5-4B HF-cache copy (8.7 GB) fetched fresh from an empty
      HF_HOME, with the Windows symlink warning noted in entry 27
    - notable non-fatal warnings along the way: the HF symlink/Developer
      Mode warning (entry 27), vram_fit=WARN@4.3 GB on the 8 GB card
      that never materialized into a real problem (entry 28-29), and two
      [OTR_LedgerScriptWriter]/[LFC] "frozen_with_warns" /
      post_warns=2 markers whose content was not individually inspected
      -- worth a follow-up read of the ledger if anyone wants to know
      exactly what those two warnings said.
    This is the first Part B leg run entirely through the Comfy Desktop
    GUI, on the 2.3.4 install, on a genuinely wiped models root and a
    genuinely wiped repo checkout -- as close to a real first-time user's
    experience as this wave gets. Reporting to the 5080 and pushing this
    entry next.

32. 2026-09-25T17:35Z. Per the 5080's ask, read the two B2
    "frozen_with_warns" / post_warns=2 texts verbatim off the episode
    ledger (audio\signal_lost_the_unpaid_halfshare_20260925_101511_
    ledger.json). Both are classified by the ledger's own
    freeze_warn_taxonomy as "cosmetic_warning" (explicitly not
    structural_error, not story_accuracy_warning), and both self-resolve
    later in the same pipeline:
    - "cast[1] (char_id='c02') has no voice_preset yet (assigned by
      OTR_CastLock post-freeze)"
    - "cast[2] (char_id='c03') has no voice_preset yet (assigned by
      OTR_CastLock post-freeze)"
    Same two lines appear under gap_audit_pre, gap_audit_post, and
    freeze_disposition -- consistent, not contradictory readings. A
    first-time user watching the console would see these as warnings, but
    they are provably benign: the pipeline itself labels them cosmetic
    and assigns the missing voice_preset a few steps later
    (OTR_CastLock), which the episode's own successful completion
    confirms. Nothing else in the ledger's video_readiness/audio_readiness
    warnings blocked anything either (num2words not installed -- numeric
    expansion skipped; cast rows without portrait -- still_motion doesn't
    need one). Confirmed benign as predicted; no further action.

## Part B, leg B3 (otr_8gb_animatediff)

33. 2026-09-25T17:49Z. This is the leg that matters most for this whole
    wave -- it directly proves the original PR #6 fix (AnimateDiff
    auto-download at queue time) on a genuinely empty C:\ComfyUI-Models.
    Operator opened otr_8gb_animatediff themselves (GUI focus was
    intermittently blocked for several minutes by a Textinputhost
    surface staying foreground, presumably tied to the operator actively
    typing elsewhere; waited it out rather than fighting for focus).
    Operator explicitly left act_count at 3 (the profile's own shipped
    default) rather than overriding to 1 -- confirmed in the loaded
    graph's writer node, along with asset_cleanup="off (keep
    everything)" at the bottom of the same node. Running with 3 acts per
    operator's explicit instruction; this will take proportionally
    longer than B2's 1-act leg. Clicking Run now.

34. 2026-09-25T17:50Z. PR #6 PROVEN LIVE. Queued cleanly ("Job queued"),
    Preflight - Workflow Validator running. Console:
    - [OTR.assets] EXISTING checkpoints/stable_audio_3_small_music.safetensors
      and text_encoders/t5gemma_b_b_ul2.safetensors -- preserved from B2
      (shared across profiles, correctly not re-fetched)
    - [OTR.assets] PLAN Comfy-Org/stable-diffusion-v1-5-archive/
      v1-5-pruned-emaonly-fp16.safetensors, bytes=2,132,696,762
    - [OTR.assets] PLAN guoyww/animatediff/v3_sd15_mm.ckpt,
      bytes=1,673,262,583 -- THE motion module PR #6 exists to fetch
    - [OTR.assets] PLAN guoyww/animatediff/v3_sd15_adapter.ckpt,
      bytes=102,134,097 -- the matching v3 adapter
    - [OTR.assets] missing files=3 total_download_bytes=3,908,093,442; no
      packs, no substitution; resume/retry via huggingface_hub
    - DOWNLOAD v1-5-pruned-emaonly-fp16.safetensors bytes=0/2132696762
      elapsed_s=0.0 -- fetch actually started
    No PREFLIGHT FAIL, no dialog, no manual step. This is the exact
    behavior TEST_WAVE.md's B3 row asks to prove ("SD 1.5 checkpoint, v3
    motion module and v3 adapter all download at queue time; no
    PREFLIGHT FAIL"), now proven through the GUI on a genuinely empty
    models root -- the strongest possible evidence for the original
    PR #6 fix. Continuing to watch the downloads.

35. 2026-09-25T18:08Z. BUG, real and reproducible: after all three
    weight downloads finished (fast -- SD1.5, both AnimateDiff v3 files,
    all byte-verified per the same [OTR.assets] pattern as B2), story
    writing completed cleanly (3 acts, ~420+ words, voices assigned via
    Kokoro), and the base video encode ran to completion (4664 frames,
    186.6s audio), node "9d - Video Render" failed with a red banner:
    "This node threw an error during execution." Full error:
    RenderError: shot shot_music_opening_001 engine
    'animatediff15_v3_haunted_video' failed to render; fallbacks are
    disabled (FailureKind.DEPENDENCY_MISSING) -- fix the engine or its
    inputs: video engine 'animatediff15_v3_haunted_video' is not usable
    for role 'text_to_video': missing required ComfyUI node class(es):
    ADE_StandardStaticContextOptions, ADE_AnimateDiffLoaderGen1 -- Ghost
    Signal needs ComfyUI-AnimateDiff-Evolved installed (pinned to the
    recorded 2026-08-22 commit) and a server restart
    Wall time to this failure: "Prompt executed in 00:18:22". Confirmed
    on disk: this fresh Comfy Desktop instance's custom_nodes\ has only
    comfyui-old-time-radio and the two stock example files --
    ComfyUI-AnimateDiff-Evolved was never installed, because nothing in
    the OTR registry pack installs it (apple/DEPENDENCIES.md documents
    it as a separate manual install, matching the README's own
    dependencies table).
    IMPORTANT DISTINCTION: this does NOT undo entry 34. PR #6's actual
    claim -- that the AnimateDiff v3 motion module and adapter WEIGHTS
    auto-download at queue time with no PREFLIGHT FAIL -- is still fully
    proven; that already happened, byte-verified, before this failure.
    What failed is a SEPARATE, pre-existing, documented prerequisite (the
    AnimateDiff-Evolved NODE PACK, which OTR has never claimed to
    auto-install) that this fresh Comfy Desktop instance never had
    installed. For a genuine first-time user this is still real friction
    worth recording: the error message is technically dense
    (FailureKind.DEPENDENCY_MISSING, ADE_* class names, "pinned to the
    recorded 2026-08-22 commit") for someone who does not know what
    ComfyUI-AnimateDiff-Evolved is or that they needed it, and OTR's own
    Manager-based install does not offer to pull in this dependency
    automatically. The episode did NOT reach obs -- per TEST_WAVE.md
    Rule 1, this leg is a FAIL on completion (even though the specific
    thing B3 exists to test -- weight auto-download -- passed). Filing
    to the 5080 now with the choice: install ComfyUI-AnimateDiff-Evolved
    via Manager (matching the documented manual step) and retry to prove
    the rest of the pipeline, or accept the weight-download proof as
    sufficient and move on to B4.

## Fresh restart: registry 2.3.5, wiped OTR install + models root

36. 2026-09-25T20:00Z-ish. Operator decision, relayed by the 5080: retest
    from the REGISTRY (2.3.5), not a git checkout -- the git-clone-at-
    3437d021 install I had staged was reverted per instruction before
    any test ran against it. Wiped, confirmed clean: custom_nodes\ back
    to stock (example_node.py.example + websocket_image_save.py only),
    ComfyUI-Shared\models\ emptied to bare category folders (this is the
    REAL models root this GUI path uses -- confirmed by file paths in
    every earlier [OTR.assets] "native=" line; C:\ComfyUI-Models, the
    CLAUDE.md-documented root, was never touched by any GUI leg and is
    a headless-launcher-only path this session abandoned), and the HF
    hub cache directory removed outright.

37. 2026-09-25T20:08Z. Installed via Manager exactly as a fresh user
    would: Extensions -> search "old time" -> ComfyUI-OldTimeRadio card
    -> Version "2.3.3 >" -> version picker -> explicitly picked "2.3.5"
    (warning-triangle icon, Pending, not the default -- 2.3.3 is Active/
    default now, 2.3.4 is Flagged) -> Install. FRICTION, minor: the
    Install click did not register on the first two attempts (no toast,
    no state change, confirmed nothing written to disk after each) --
    the third click, made without any intervening click elsewhere,
    worked (card flipped to Enabled toggle + Uninstall button, "To apply
    changes, please restart ComfyUI" toast). Clicking anywhere outside
    the version dropdown -- even on what looks like an unrelated part of
    the same panel -- silently closes it via what is likely a full-
    screen invisible backdrop, and the very next click (even on the
    real Install button) can be swallowed by that backdrop's own close
    handler rather than reaching Install. Not logged as a PBUG: low
    stakes (a slow click gets there eventually, no data loss, no wrong
    version installed), but worth knowing if this recurs.
    Restarted. On the FIRST restart after this install: Nodes panel's
    EXTENSIONS section shows "OldTimeRadio" -- loaded clean, first try,
    no retry needed. That is a THIRD data point for PBUG-20260925-01's
    intermittency (2 no-ops, 2 first-try successes now) -- still not
    enough signal to call a rate, but leaning toward "usually works,
    occasionally doesn't" rather than 50/50.

38. 2026-09-25T20:18Z. Gallery investigation (Templates showed "1 of 1"
    again despite 25/25 files on disk): captured the live
    /workflow_templates response from the still-running pre-restart
    server before the operator's next wipe -- it correctly listed all 25
    names under "comfyui-old-time-radio". Backend/packaging fully
    correct; the undercounted gallery was the Desktop frontend's own
    stale template list, not a file or version problem. 5080 confirmed:
    resolved, no code fix needed on the pack's side, fix-for-next-time
    is curl /workflow_templates first, then restart + hard refresh
    (never reinstall) if the backend already has everything.

39. 2026-09-25T20:20Z-ish. PBUG-20260925-02 LIVE-VERIFIED FIXED. Operator
    queued otr_8gb_animatediff on a fresh 2.3.5 install with
    ComfyUI-AnimateDiff-Evolved deliberately NOT installed. Full console
    (pasted verbatim by the operator):
    - [OldTimeRadio] OK - All 24 nodes loaded successfully
    - three [WARNING] [OTR.assets] lines: announcer/music/character
      image_model=z_image_turbo not downloaded ("this role's video lane
      animatediff15_v3_haunted_video mints no still (accepts_still=False),
      so its image weights are provably unused") -- correct, not an error
    - [ERROR] !!! Exception during processing !!! Install
      ComfyUI-AnimateDiff-Evolved (Kosinkadink) from ComfyUI Manager,
      then restart ComfyUI (apple/DEPENDENCIES.md names the pinned
      commit) -- the video engine 'animatediff15_v3_haunted_video' needs
      node classes that are not registered on this server:
      ADE_StandardStaticContextOptions, ADE_AnimateDiffLoaderGen1.
      Nothing was downloaded or rendered; fix this and press Queue again.
    - [INFO] Prompt executed in 0.86 seconds
    This is exactly the fix's design goal: refusal in under a second (not
    18:22 like the pre-fix run), the actionable fix as the first
    sentence, zero [OTR.assets] DOWNLOAD lines anywhere in the log (only
    the three "not downloaded, provably unused" skip-notices), zero
    writer/cast/audio/video work started. PBUG-20260925-02 is closed by
    this evidence.

40. 2026-09-25T20:22Z. NEW BUG found in the same log, unrelated to the
    gate fix: "[INFO] [OldTimeRadio] writer LLM folder:
    C:\ComfyUI-Models\LLM" -- wrong for this instance. Every other
    category in the same boot resolves under ComfyUI-Shared\models\
    (this instance's real, configured extra_model_paths root, per the
    same log's "Adding extra search path ..." lines). Read
    nodes/_otr_models_root.py: `_models_root()` checks env vars (unset),
    then `Path(r"C:\ComfyUI-Models").is_dir()` -- returns it
    UNCONDITIONALLY on bare existence, before ever consulting ComfyUI's
    own folder_paths.models_dir (which would give the right answer
    here). C:\ComfyUI-Models exists as an EMPTY directory on this box --
    a leftover from this session's earlier headless-launcher setup;
    every "nuke" this session emptied its contents but never rmdir'd the
    folder itself. Consequence: the new writer-LLM-folder feature
    silently writes into a directory disconnected from this Comfy
    Desktop instance's actual model tree, no error, downloads still
    "succeed" -- just to the wrong place. This is a real design gap
    (existence is not the same claim as "this is genuinely the
    configured root") that could bite any box with a stray empty
    C:\ComfyUI-Models folder, not just this one. Reported to the 5080 in
    full with the code citation; not yet fixed. Operator's plan: full
    Comfy Desktop uninstall (app + settings) for the next fresh start,
    which removes this specific artifact but not the underlying logic
    gap.
