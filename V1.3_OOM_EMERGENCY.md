# v1.3 Full Workflow OOM — Emergency Brief

**Status:** BLOCKER. v1.4 hardware regression is paused until this is root-caused and fixed.
**Scope:** This document is for a fresh AI session tasked with *only* fixing the v1.3 OOM. No v1.4 work. No new features. No refactors. No scope creep.

---

## Rules of engagement — read this first

**Before touching anything, read the locked standing rules file:**
`C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\CLAUDE_v1.3_LOCKED.md`

That is a pinned snapshot of `CLAUDE.md` as of the start of this OOM hunt. Do not read `CLAUDE.md` — read the locked file. This prevents drift if the live file gets edited mid-investigation.

The locked file covers, verbatim and non-negotiable:
- Clean code, clean logs, meaningful names, SFW content, no curse words
- Regression discipline: AST parse, widget-count audits, Bug Bible at https://github.com/jbrick2070/comfyui-custom-node-survival-guide/blob/main/BUG_BIBLE.yaml
- Git push protocol: PowerShell blocks with absolute `cd` as line 1, plain ASCII, copy-paste ready, one attempt max
- Lockstep verification after every push: local HEAD vs GitHub HEAD, 0-byte scan, BOM scan, AST parse, NODE_CLASS_MAPPINGS grep
- Encoding: UTF-8 no BOM on Windows, no `Set-Content`/`Out-File` for Python source, verify with `Get-Content | Select-String`
- Platform reality: RTX 5080 Laptop, 16 GB VRAM, Blackwell sm_120, torch 2.10.0, CUDA 13.0, SageAttention + SDPA, **Flash Attention 2 is NOT available — do not chase it**
- Scope discipline: 100% local, open source, offline-first, no cloud, no API keys, 14.5 GB VRAM real-world ceiling, sequential execution only
- Autonomy: act like a senior pair-programmer, but escalate on decisions or destructive ops

Confirm you have read `CLAUDE_v1.3_LOCKED.md` before the first tool call.

---

## The bug

- **Workflow:** `workflows/old_time_radio_scifi_full.json`
- **Symptom:** OOM on the RTX 5080 Laptop mid-run
- **Last clean tag:** `v1.3` at commit `a0b9ccd`
- **Last v1.3 docs commit:** `3656508`
- **Branch where the OOM surfaced:** `v1.4-voice-arc-infra` (local working tree has v1.4 code — Kokoro Announcer, MusicGen Theme, SceneSequencer resample fix)

**Critical framing:** the v1.3 full workflow does NOT touch any v1.4 node. If the OOM repros on the clean `v1.3` tag, it is a latent v1.3 bug that hid in prior testing. If it only repros on the v1.4 branch, something in the v1.4 branch is leaking VRAM even when the v1.4 nodes are not executed.

---

## Prime suspects (in order)

1. **Gemma → Bark VRAM handoff in `nodes/gemma4_orchestrator.py` and `nodes/batch_bark_generator.py`.** Gemma 4 is the heaviest single model in the pipeline. If `del model`, `gc.collect()`, and `torch.cuda.empty_cache()` are not called in the right order (or are skipped on certain code paths), Bark will load on top of a dirty allocator and blow the 14.5 GB ceiling.
2. **Arc Enhancer Phase A/B/C re-entry into Gemma.** Phase B bookend rewrite may be loading Gemma a second time without fully unloading it from Phase A. Check `_arc_check_and_rewrite_bookends` for nested load paths.
3. **SageAttention + SDPA fallback.** On some prompts the attention backend can silently fall back to a higher-memory path. Check the startup log for backend reports.
4. **OpenClose evaluator.** Gated off in v1.3 (`ENABLE_3_OUTLINE_EVALUATOR = False`) — confirm it really is off in the failing run.
5. **Allocator fragmentation.** Even with correct `empty_cache()`, the torch allocator can fragment across a long multi-node run. `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` is the standard mitigation on recent torch.

---

## Investigation plan (in order, do not skip steps)

### Step 1 — Confirm it repros on clean v1.3
Do not debug on the v1.4 branch. Get to a clean v1.3 first.

Hand the user a PowerShell block that:
1. `cd` to absolute repo path on line 1
2. `git stash push -u -m "v1.4 wip before OOM hunt"`
3. `git fetch --tags`
4. `git checkout v1.3`
5. `git log --oneline -1` to confirm HEAD
6. `git status` to confirm clean

Wait for the user to run the full workflow and report: OOM or no OOM.

### Step 2a — If OOM repros on clean v1.3
It is a latent v1.3 bug. Bisect with VRAM telemetry:
- Add lightweight `torch.cuda.memory_allocated() / 1e9` logging between major nodes (non-destructive, behind a debug flag)
- Identify the exact node that crosses 14.5 GB
- Root-cause whichever node it is (prime suspect: Gemma unload path)

### Step 2b — If OOM does NOT repro on clean v1.3
It is a v1.4 branch side-effect even though the v1.4 nodes aren't in the failing graph. Check:
- Top-level imports in `__init__.py` — does importing `musicgen_theme.py` or `kokoro_announcer.py` pull `transformers` / `kokoro` into VRAM at registration time?
- Any module-level model instantiation
- Any `@torch.no_grad()` or persistent state on module import

### Step 3 — Minimal fix on a dedicated branch
- Branch from tag `v1.3`: `git checkout -b v1.3.1-oom-fix v1.3`
- Apply the smallest possible fix
- AST parse, widget audit, NODE_CLASS_MAPPINGS grep, BOM scan
- Hand the user a PowerShell push block with absolute `cd` on line 1

### Step 4 — Lockstep verify after push
- Local HEAD vs `origin/v1.3.1-oom-fix` HEAD
- 0-byte scan
- BOM scan
- AST parse of every changed .py
- Wait for user confirmation

### Step 5 — Sign-off
- User runs full workflow on `v1.3.1-oom-fix` on the 5080
- VRAM peak logged and confirmed ≤ 14.5 GB
- Only then: merge strategy discussion (cherry-pick to main, rebase v1.4 branch onto v1.3.1, etc.)

---

## Do NOT do any of this during the OOM hunt

- Do not touch any v1.4 file (`kokoro_announcer.py`, `musicgen_theme.py`, any v1.4 edits to `scene_sequencer.py`, `batch_bark_generator.py`, `gemma4_orchestrator.py`, or the workflow JSONs)
- Do not attempt Flash Attention 2 install (not available on this platform, documented)
- Do not chase audiocraft, AudioGen, Fish Speech, XTTS, RVC, or any other out-of-scope library
- Do not refactor for "cleanliness" while in the fix
- Do not push without the user pasting the PowerShell block
- Do not amend commits; always create a new commit
- Do not skip lockstep verification

---

## Files you will likely need to read

- `CLAUDE_v1.3_LOCKED.md` — standing rules (read first)
- `nodes/gemma4_orchestrator.py` — prime suspect
- `nodes/batch_bark_generator.py` — prime suspect
- `nodes/scene_sequencer.py` — arc enhancer phase orchestration
- `workflows/old_time_radio_scifi_full.json` — the failing workflow
- `__init__.py` — registration side-effects

---

## Deliverable

One minimal commit on `v1.3.1-oom-fix` off tag `v1.3` that:
- Fixes the OOM
- Passes full workflow on the 5080 with VRAM peak ≤ 14.5 GB
- Does not introduce any new features, nodes, or dependencies
- Passes AST parse, widget audit, BOM scan, and NODE_CLASS_MAPPINGS grep
