# OTR Bug Log — 2026-06 epoch (ACTIVE)

**Repo:** `ComfyUI-OldTimeRadio` @ `v2.0-alpha`
**Owner:** Jeffrey A. Brick
**Started:** 2026-06-14 | **Archive:** [`BUG_LOG.md`](BUG_LOG.md) (reference only)

---

## What this file is

Fresh, lean, append-only bug log for the **current code epoch** (post Wan-2.2 /
video-platform, June 2026 forward). The code has changed enough that old open
items are **not** being chased here — this file tracks **new** bugs found from
2026-06-14 on.

For historical bugs and how past issues were fixed, **consult the archive:**
[`BUG_LOG.md`](BUG_LOG.md) (~440 KB, `BUG-LOCAL-001..~305`, through 2026-06-12).
Do **not** port old entries into this file — link to the archive when a new bug
relates to a past one.

Per CLAUDE.md: log every bug the moment it's found — no batching, no waiting.

---

## Baseline at start — 2026-06-14, HEAD `961d8fc`

Objective triage at clean checkout (HEAD == origin/v2.0-alpha, working tree clean):

- **Full suite:** 4249 passed / 28 skipped (collected 4277) — **0 fail / 0 error**.
- **Bug Bible:** 16 passed / 7 skipped / 3 xfailed.
- **git:** `961d8fc` == `origin/v2.0-alpha`, tree clean, everything pushed.

=> **Clean green baseline. No open runtime bugs logged here yet.**

---

## Numbering

New epoch range **`BUG-LOCAL-400+`** — the archive's voice-path-cleanbreak era
reached the ~300s; starting at 400 marks the June-2026 video-platform epoch and
avoids collision (the archive itself uses intentional range cuts between epochs:
`001-129` legacy, `200+` voice-path-cleanbreak). Monotonic, no reuse. Before
assigning a number, glance at the archive for the current max.

---

## Entry schema (per CLAUDE.md)

```markdown
### BUG-LOCAL-NNN: Title  [FIXED <sha> YYYY-MM-DD]
- **Date:** YYYY-MM-DD | **Phase:** 0-6 | **Bible candidate:** yes/no
- **Symptom:** exact error / console output
- **Cause:** root cause (or "pending — awaiting investigation")
- **Fix:** what resolved it (or "pending")
- **Verify:** how to confirm (test / render / grep)
- **Tags:** vram, widget-drift, ffmpeg, subprocess, parse-fatal, json-wiring, etc.
```

Mark `[FIXED <commit-sha> YYYY-MM-DD]` after the title when resolved — do not
delete entries. When `Bible candidate: yes` and the fix is verified, promote to
the survival-guide repo per CLAUDE.md "Bug Log Pipeline".

---

## Entries (newest first)

### BUG-LOCAL-410: end-of-episode ROLLING CREDITS / Telemetry HUD post-roll GONE — black tail, only the static title card (REGRESSION vs 6/5)  [FIXED + OPERATOR-VERIFIED 2026-06-14 — CLOSED]
- **OPERATOR-VERIFIED 2026-06-14:** a fresh render with **flux_still** shows the rolling credits working great (scroll over the held last clip, to the end). HuMo not yet tested (low risk — the credits path is engine-agnostic; the backdrop is whatever the last media row is). Marked CLOSED and removed from `GO_FORWARD_PLAN.md`.
- **RESOLUTION (2026-06-14):** FIXED via a 4-model roundtable-converged Option A (allow an intentional SILENT credits post-roll). Three surgical edits, NO JSON wiring change, audio spine untouched: (1) `otr_silent_composite.py` — extend the assembled length to the floor's FULL video frame count (`floor_frames`) so the ~20s rolling-credits scroll survives instead of capping at the master mix; (2) `otr_post_upscale_procgen_blend.py` — black-pad the §4D scopes input (`tpad=stop_mode=add:color=black`) so the `shortest=1` lighten-blend no longer re-clamps the deliverable to the shorter scopes; (3) `otr_master_audio_mux.py` — relax the v≤a gate to permit a bounded silent credits tail (`v ≤ a + OTR_MAX_CREDITS_TAIL_S`, default 45s) while still failing LOUD on gross drift. Master audio stays BYTE-IDENTICAL (`-c:a copy`; the output audio stream is unchanged, only the container is longer — the SHA check still passes). **Backdrop follow-on (`229cade`, operator 2026-06-14):** the credits now ride over the HELD LAST drama clip (the 6/5 "credits over the scene" look) instead of the dark CRT telemetry card — the composite tail holds the temporally-last real media row (clip OR still, adapts to whatever is last; the §4D floor layer lighten-blends the green credits over it), falling back to the procgen end-slice/black when there is no clip. AUDIO is SILENT during the scroll (the closing theme ends with the master; music-under-credits would need a longer master — future, ties to BUG-408). Regressions: `test_assemble_extends_to_floor_for_credits_tail_bug410` (composite extends to floor past a shorter sibling master.wav), updated `test_master_audio_mux_no_shortest_and_credits_tail_guard` (moderate tail allowed, gross drift fails), updated `test_build_cmd_3input_scopes_no_double_format_gbrp_bug402` (scopes pad present). Suite **4257 pass / 28 skip / 0 fail**, Bug Bible **16/7/3**, targeted blend+mux+scopes green. Roundtable: `docs/2026-06-14-credits-tail-fix/roundtable/` (4 models, ~$0.22). **NEXT = operator look-QA a fresh render: the closing now scrolls the `[ CLASSIFIED TRANSCRIPT ]` + CAST credits to the end (silent tail after the closing theme), like 6/5.**
- **Date:** 2026-06-14 | **Phase:** 5 (§4C/§4D procgen floor / closing post-roll composite) | **Bible candidate:** yes (a feature that renders fine in an older build but vanishes after a floor/blend rework is the classic "unwired/orphaned after refactor" regression — verify the post-roll renderer is still CALLED and composited, not just defined).
- **Symptom:** at the END of the current episode there are **NO closing credits at all** — no rolling transcript, no CAST & VOICES, no looping background video/still. Just BLACK with the static green header `=== SIGNAL LOST === 00:38` + `"Whispers in the Static"`. Render: `output\otr\obs\...signal_lost_whispers_in_the_static_20260614_211154_silent_procgen_blended_final.mp4` (FRESH, 21:11, post-402/404/406 fixes).
- **6/5 REFERENCE (what it SHOULD look like — operator-supplied):** `signal_lost_booms_shadow_20260605_235158_procgen_blended` shows the FULL closing treatment — a `[ CLASSIFIED TRANSCRIPT ]` panel scrolling the scene arc + full script on the right, with `METADATA / NEWS SEED / SYSTEM TELEMETRY / CAST & VOICES` down the left, **all composited OVER the looping cockpit background video** (the still/video loops while the credits roll), ending on `[ END OF CLASSIFIED TRANSCRIPT ]`. Operator: "the still or video should loop while credits roll, now we have no credits."
- **Cause (ROOT FOUND via 6/5-vs-HEAD diff, 2026-06-14):** The HUD post-roll renderer is ALIVE and STILL APPENDED at HEAD — `nodes/video_engine.py` builds the floor mp4 with `total_encode_frames = total_frames + _hud_frames` (L2001) and the `_frame_gen` yields the `_TelemetryHUDRenderer` frames AFTER the audio-length main frames (L2023-2030, "Post-roll Telemetry HUD - plays after audio ends"). So the FLOOR video keeps the credits tail (it is the LONGEST stream, ~59s when master audio is ~39.7s). The regression is in the COMPOSITE/BLEND that builds the deliverable:
  - **6/5 (`c9af198`) used `nodes/video_composite.py`.** `nodes/otr_silent_composite.py` did NOT exist at 6/5 — it is NEW (the §4C/§4D rework, `336fb41`/`39aa6c9`, 2026-06-13). The current deliverable is `_silent_procgen_blended_final.mp4` (silent composite -> §4D blend), vs 6/5's `_procgen_blended.mp4`.
  - **The §4D blend clamps to the SHORTEST input.** `nodes/otr_post_upscale_procgen_blend.py` blends `[source/composite][procgen floor][scopes]` with **`shortest=1`** (L442-443 the 3-input scopes branch; L481 the 2-input branch). The silent composite is conformed to the MASTER-AUDIO length (~39.7s) and the scopes track is now ALSO master-audio length (BUG-406 fix extended scopes to master, but only to master — NOT master+HUD). The floor (~59s, with the appended HUD) is the only long input, so `shortest=1` clamps the blended output to ~39.7s and the floor's HUD/credits post-roll (frames ~40-59s) is CUT. Net: black/static tail, no rolling credits.
  - This is the SAME family as BUG-406: 406 extended scopes to the master length so the body would not clamp; it did NOT account for the floor's appended HUD post-roll that lives BEYOND the master audio, so the credits region is still clamped off by `shortest=1`.
- **DECISIVE RUNTIME PROBE (2026-06-14, render 211154):** floor `#12` SignalLostVideo = **65.72s**; master audio = **~45.7s**; `_silent.mp4` (composite) = `_silent_procgen_blended.mp4` (§4D) = `_..._final.mp4` (mux) = **45.68s v / 45.706s a**. => The floor renders the FULL credits post-roll out to 65.72s (master 45.7 + ~20s scroll). The deliverable is LOCKED to the master-audio length (45.7s) and the ~20s scrolling transcript (45.7→65.7s) is CUT — only the first few static-header seconds survive (the "SIGNAL LOST / title" card the operator sees). **The §4D `shortest=1` was NOT the operative clamp here** (composite/blend/final all agree at 45.68s); the real cap is the **mux video≤audio gate**: `otr_master_audio_mux.py` (invariant V-1) copies the FROZEN master with `-c:a copy` (byte-identical), FORBIDS `-shortest`, and ASSERTS composite-duration == master-audio (within 1/fps) BEFORE muxing — so the composite is built to the master-audio budget and cannot carry the post-roll. `otr_silent_composite.py` (L420-440) already restores credits up to the longest `*_master.wav`, but that master is only 45.7s, so there is no room for the ~20s scroll.
- **Operator sizing directive (2026-06-14):** "it needs just enough seconds to scroll the video credits." => the deliverable's closing tail must be extended by EXACTLY the credits-scroll length (the HUD frame count `_hud_renderer.hud_frames()`, already computed in the floor node), not arbitrarily — long enough to fully scroll the `[ CLASSIFIED TRANSCRIPT ]` + CAST roll once, with the closing background looping underneath. Don't over-pad (no long black/dead tail), don't under-pad (no cut-off scroll).
- **Fix (pending — coder; code, likely no JSON):** make the deliverable span **master-audio + HUD-scroll** so the floor's post-roll survives the blend. Options to weigh: (a) extend the silent composite AND the scopes track to `master_frames + hud_frames` (pad the composite tail by looping the closing background still/video — 6/5 behavior: "still/video loops while credits roll" — and pad scopes with suppressed/black as 406 does), so all three blend inputs reach the floor length and `shortest=1` no longer clamps; OR (b) drive the blend output length from the FLOOR (longest) and hold/loop the shorter inputs. Either way the closing must show the rolling `[ CLASSIFIED TRANSCRIPT ]` + CAST & VOICES + telemetry composited over a LOOPING closing background. Verify the HUD frame count (`_hud_renderer.hud_frames()`) is discoverable by the composite (it is produced in the floor node — may need to surface `_hud_frames`/duration to the composite via the ledger or a probe of the floor mp4 length vs master). Keep the AUDIO spine frozen (`test_audio_byte_identical` green — the post-roll is silent video beyond the audio; do NOT add `-shortest`, do NOT alter the master mux). Regression: assert the blended final's duration ≈ floor length (master + HUD), the tail is non-black, and the transcript/CAST text is present in the closing frames.
- **Verify:** re-render; the closing plays the rolling `[ CLASSIFIED TRANSCRIPT ]` + CAST & VOICES + telemetry over a looping background, like `signal_lost_booms_shadow_20260605_235158`, not a black card.
- **Tags:** credits, telemetry-hud, post-roll, transcript, §4C, §4D, floor, composite, regression, orphaned-after-refactor, video_engine

### BUG-LOCAL-409: hero title card scrambles for the WHOLE music window — resolved title only shows for the last ~1-2s  [FIXED 2026-06-14]
- **RESOLUTION (2026-06-14):** FIXED. New pure helper `_title_reveal_progress(fi, w0, me, in_dock, reveal_frac)` in `nodes/video_engine.py` completes the decode/reveal in the FIRST `reveal_frac` (default 0.4, env `OTR_TITLE_REVEAL_FRACTION`) of the `[w0, me)` window, then HOLDS the solid title (p==1.0) until the POP/dock — `_draw_title_card` calls it instead of stretching `reveal_span = me - w0` across the whole window. Applies to BOTH the open and close cards (same draw path). **Credits safety (operator caution 2026-06-14):** the close title card is rendered ONLY in `[0, total_frames)` by the `_CRTRenderer` (clamped `c1 = min(total, c1)`); the rolling credits are a SEPARATE `_TelemetryHUDRenderer` appended AFTER `total_frames` — non-overlapping frame ranges, so the close card cannot run over the credits, and this change only alters reveal TIMING within the card's own window, not its extent. Regression: `test_title_reveal_resolves_early_and_holds_bug409`. Suite 4259/0, Bug Bible 16/7/3. NEXT = operator look-QA (title resolves by ~40% of the head-gap and holds solid).
- **Date:** 2026-06-14 | **Phase:** 5 (§4C procgen hero title card) | **Bible candidate:** yes (a decode/reveal animation whose completion point is pinned to the END of its window leaves the resolved content visible for only the final frame(s) — the reveal span must be a FRACTION of the window so the payload holds).
- **Symptom:** operator look-QA (screenshot, episode "Whispers in the Static"): the #1 hero title card does its cool decode/scramble animation for essentially the entire opening-music duration; the FULLY-RESOLVED title only appears in the last couple seconds. At playhead 0:09 the title still reads `WHISPERS IN THE STATIF[]` (mid-scramble, trailing block cursor) when it should already be fully resolved. Operator: the title "should resolve by here" (well before the end of the window).
- **Cause (GROUNDED — nodes/video_engine.py::SignalLostVideoRenderer._draw_title_card, L677-723):** the reveal progress `p = (fi - w0) / reveal_span` with `reveal_span = max(1, me - w0)` — i.e. the decode/scramble is linearly stretched across the WHOLE window `[w0=start_f, me=music_end_f)`. `p` (and therefore `revealed = int(p * total_chars)` for the hero lines AND `reveal_n = int(p * len(carrier))` for the "=== SIGNAL LOST ===" carrier line) only reaches 1.0 at `fi == me`, the very last reveal frame. POP fires only at `1 <= (me - fi) <= 2` (last 1-2 frames), then dock `[me, end)`. So by construction the solid resolved title is on screen for ~1-2 frames before POP/dock — exactly the operator's "only the last couple seconds." (The same full-window stretch likely applies to the S5 close bookend at L413, "reveal + POP, no dock"; check it too.)
- **Fix (pending — code-only, §4C visual; LOW risk, NO audio-spine touch):** decouple the reveal-COMPLETE point from `music_end`. Make the reveal finish in the FIRST fraction of the window, then HOLD the solid resolved title for the remainder, then POP→dock at `music_end`. Concretely: `reveal_span = max(1, int((me - w0) * REVEAL_FRACTION))` (env-overridable, e.g. `OTR_TITLE_REVEAL_FRACTION`, default ~0.35-0.45 so a ~9.5s window resolves by ~3.5-4s and holds solid ~5s before POP); keep `p = min(1.0, (fi - w0)/reveal_span)` so it clamps to 1.0 after the reveal span; keep the block cursor `if not in_dock and p < 1.0` so it stops blinking once solid; keep POP at `me-1..me` and dock `[me, end)` unchanged. Apply the SAME fraction to the carrier line and (if present) the close bookend so they stay in sync. Verify-at-build: confirm `card["start_f"]/music_end_f/dock_frames` come from `_resolve_title_timing` (post BUG-404 they span the head-gap `[0, first_dialogue)` ~9.5s — that fix is correct; THIS bug is the within-window pacing, independent of 404). Regression: a unit test asserting the title is fully resolved (`p>=1.0` / `revealed==total_chars`) at `fi = w0 + reveal_span` and held solid through `me-2`.
- **Verify:** on a re-render the hero title resolves to solid well before the music ends (≈ first ~40% of the window) and stays readable until the POP→dock; the scramble no longer eats the whole duration.
- **Tags:** title-card, §4C, procgen, decode-reveal, animation-pacing, reveal-span, video_engine

### BUG-LOCAL-408: default MUSIC sounds bad / "doesn't sound like music at all" — music prompt/inputs produce non-musical output  [IMPLEMENTED 2026-06-14 — pending operator A/B + golden re-baseline]
- **IMPLEMENTATION (2026-06-14):** Path B landed in `nodes/_otr_audio_engines/eng_stable_audio_3.py` (SA3-only; `compose_music_prompt` contract untouched, so no caller changes). (A) `generate_clip` now sets `ConditioningStableAudio` `seconds_total` to a structural CONTEXT (`OTR_SA3_CONTEXT_S`, default 30s) with a per-cue `seconds_start` via the pure `_sa3_clip_window` (intro→head, outro→tail, else→middle; cue detected from the prompt's intro/outro/transition markers, so the signature stays `(prompt, dur, seed)`); the LATENT stays exactly `dur` → clip length + seed determinism unchanged. (B) `_sa3_augment_prompt` prepends an SA3-shaped genre/instrument/production anchor (deterministic era→genre map, length-capped, no fabricated key) and a real negative prompt replaces the empty `""` (`OTR_SA3_NEG_PROMPT`, targeted — no blanket "dissonant/noisy"). (C) steps/cfg/sampler/scheduler are env-overridable constants (`OTR_SA3_STEPS/CFG/SAMPLER/SCHEDULER`) + a render_log trace (window/seed/settings/prompt-hash) for A/B. Tests: `tests/test_sa3_music_prompt_bug408.py` (pure helpers). **SETTINGS BAKED IN (6-model roundtable 2026-06-14, ~$0.47, `docs/2026-06-14-sa3-settings/`):** `CONTEXT_S 30→12` (context = the longest cue so short cues are coherent slices of one tight phrase, not aimless 30s fragments — the key fix), `CFG 6.0→7.0` (SA3 native), kept `dpmpp_3m_sde_gpu/exponential` @ 100 steps (Stable Audio reference; golden already passes so determinism holds — rejected the ODE-swap), refined negative (ban vocals + "modern pristine mix" push; dropped harsh/muddy/out-of-tune/low-quality that fight the eerie/tape texture), +opening/closing cue detection, +"instrumental" anchors, safe env parsing + `OTR_SA3_DENOISE` + context<dur warning + stale-docstring fix. Suite 4261/0, Bug Bible 16/7/3. **OPERATOR-GATED NEXT:** (1) RESTART ComfyUI Desktop (module cache), (2) render + A/B listen vs the old SA3 (tune `OTR_SA3_CFG`/`STEPS`/`CONTEXT_S` if needed), (3) when approved, RE-BASELINE the `test_audio_byte_identical` golden (this change INTENTIONALLY alters music bytes — expect that test RED until re-captured). Plan: `docs/2026-06-14-sa3-music-improvement/roundtable/pass01_plan.md`.
- **Date:** 2026-06-14 | **Phase:** 3 (music generation / music-prompt) | **Bible candidate:** TBD
- **Date:** 2026-06-14 | **Phase:** 3 (music generation / music-prompt) | **Bible candidate:** TBD
- **Symptom:** operator look-QA — the DEFAULT music bed (opening/closing theme + inter-beat music) renders as bad / non-musical audio: "the prompting or inputs for the default music is pretty bad, doesn't sound like music at all." Heard on default-lane renders.
- **Cause:** LIKELY an ENGINE-SWAP REGRESSION, not a prompt defect. **Operator (2026-06-14): the OLD musicgen output was GOOD; Stable Audio (SA3) was adopted as the default expecting better quality but is WORSE ("doesn't sound like music at all").** So the regression tracks to making `eng_stable_audio_3` (SA3-native) the default music engine over `musicgen` (the clean-break 1c music lane: `ad0ac50` music-prompt-via-Meta-brief + `9259336` musicgen->clip). SA3 was chosen as the default because it is the only conflict-free new music default (no extra pip dep) — but conflict-free != better-sounding. Secondary checks before any change: confirm WHICH engine the default lane actually resolves to for the music slot, and whether the SAME Meta-brief music prompt feeds both (so we know musicgen's good output wasn't prompt-dependent). The music prompt builder is `nodes/_otr_music_prompt.py`.
- **GROUNDING 2026-06-14 (read-only git/code, planner window):** Two facts confirmed against the real code, which DISPROVE the "prompts changed since 6/5" hypothesis and point at the engine swap + prompt-model-mismatch:
  - (1) **The music prompt builder did NOT change since 6/5.** `nodes/_otr_music_prompt.py` last commit = `ad0ac50` (2026-06-03 "route theme-music prompt through the Meta brief protocol"); NO commits to it (or any `*music*` node) after 6/5. Same prompt code that produced the "good" 6/5 music.
  - (2) **The music DEFAULT engine flipped to SA3 on 2026-06-03.** `nodes/_otr_engine_profiles.py` → `"music": ("stable_audio_3", "musicgen", "stable_audio_music")` (index 0 = default); `eng_musicgen.py` is tagged `default_roles = ()  # DEMOTED 2026-06-03: SA3 is now the music default; musicgen stays selectable`.
  - (3) **Two music paths may diverge** — `nodes/stable_audio_theme.py` (`OTR_StableAudioTheme`, opening/closing THEME cues) still names `musicgen` its "legacy byte-identical default" (engine order musicgen > stable_audio_music), while the engine-profile `music` slot defaults to SA3. So the operator's GOOD 6/5 theme music may have been musicgen while the current BAD music is SA3 on the inter-beat/music slot. CONFIRM which path renders the bad audio on a real default-lane render before tuning.
  => **Conclusion:** prompts are UNCHANGED and were tuned FOR musicgen; SA3 is a different model that does not respond to musicgen-shaped prompts the same way. Root = engine swap + prompt-model mismatch, NOT a prompt regression.
- **DECISION (operator 2026-06-14): Path B — keep SA3, improve its prompts/inputs/seed/conditioning.** A 4-model roundtable (GPT-5.5 + Gemini-3.1-pro + Grok-4.3 + DeepSeek-v4-pro, ~$0.17) CONVERGED a grounded, buildable plan: `docs/2026-06-14-sa3-music-improvement/roundtable/pass01_plan.md` (+ `pass01_judgment.md`; problem statement `PROBLEM_STATEMENT.md`). Headline fixes: (A) in `eng_stable_audio_3.generate_clip` set `ConditioningStableAudio.seconds_total` to a ~30s structural CONTEXT with a per-cue `seconds_start` (opening=0 / interstitial=mid / closing=tail) while keeping `EmptyLatentAudio` at exactly the cue `dur` (structure without trimming or breaking determinism/length); (B) keep `compose_music_prompt(meta,cue)->(str,int)` signature, add SA3-shaped genre/instrument/production text + a real negative prompt INSIDE `generate_clip` (deterministic period→genre map, length-capped, targeted negatives — not blanket "dissonant/noisy"); (C) sampler steps/cfg/sampler/scheduler as env-overridable constants + a name-exists guard + log the settings/prompt-hash in render_log. GROUNDED: JSON node 83 `OTR_StableAudioTheme=stable_audio_3` (SA3 IS live), and `test_audio_byte_identical` sha256s the FULL episode audio vs a golden → this work intentionally changes music bytes, so it REQUIRES a **deliberate operator-gated golden RE-BASELINE** (expect that test RED until the operator approves the new golden). CUT: render-longer-then-trim, best-of-N seeds, larger-model migration. **Path A (revert to musicgen) was the fallback — NOT taken.** NEXT = coder window implements A→B→C, operator A/B-listens, then re-baselines the golden. NOTE — the audio SPINE (byte-identical master + mux-LAST) is FROZEN; any change here is UPSTREAM music-generation only, must keep `test_audio_byte_identical` GREEN, and touches only the music-engine default and/or the music prompt builder (broader-audio otherwise PARKED, but this is an operator-flagged default-experience defect).
- **Verify:** on a re-render the default music bed sounds like coherent music (melodic/rhythmic, on-genre for OTR sci-fi), not noise; A/B against a known-good earlier render if one exists.
- **Tags:** music, musicgen, stable-audio, music-prompt, meta-brief, default-experience, audio-upstream

### BUG-LOCAL-407: flux_still (and the still/floor families) rendered the HuMo PORTRAIT canvas, not full 16:9  [FIXED 2026-06-14]
- **Date:** 2026-06-14 | **Phase:** 5 (render_driver canvas) | **Bible candidate:** yes (only the talking-head face family needs the portrait pillarbox; every other engine must FILL the composite landscape canvas).
- **Symptom:** operator look-QA -- a flux_still scene/opener still rendered as a skinny PORTRAIT pillarboxed in the 16:9 frame, the same shape as a HuMo talking-head, instead of filling the frame.
- **Root cause:** `build_request_from_shot` (nodes/_otr_video_engines/render_driver.py) gave the full-frame landscape canvas (`OTR_VIDEO_LANDSCAPE_CANVAS`, 1472x832) ONLY to `ltx_video`/`wan_i2v` (the 2026-06-10 fix). Every other engine inherited `build_request`'s default **480x832 PORTRAIT** (the accepted HuMo talking-head pillarbox) -- so flux_still, station_card, still_kenburns, visualizer all came out skinny-portrait. Portrait is a HuMo (`audio_driven_face`) REQUIREMENT, not a default for stills/b-roll.
- **Fix:** FIXED 2026-06-14. The landscape canvas now applies to EVERY engine whose family is NOT a face family: only `audio_driven_face` (HuMo, live), `lipsync_overlay` (latentsync, parked) and `character_3d` (deferred) keep the portrait canvas; flux_still + static_image_gen/static_motion/abstract/text_to_video/image_to_video all fill 1472x832. Env-overridable. Regression: `test_build_request_from_shot_flux_still_fills_landscape_canvas` + `..._humo_keeps_portrait_canvas`.
- **Verify:** suite 4257 / Bug Bible 16/7/3 green; on re-render flux_still beats fill the 16:9 frame and HuMo beats keep the portrait pillarbox.
- **Tags:** canvas, aspect, portrait, landscape, flux_still, cheap-families, humo, render_driver

### BUG-LOCAL-406: closing FREEZE under the closing theme — scopes track shorter than the master CLAMPS the §4D blend  [FIXED 2026-06-14]
- **Date:** 2026-06-14 | **Phase:** 5 (§4D scene-aware scopes / 3-input blend) | **Bible candidate:** yes (an OVERLAY input to a `blend=...:shortest=1` filtergraph must be >= the base length, or it silently clamps the whole output below the deliverable and the mux clone-holds the tail). *(Numbered 406 — 405 is the per-role image entry; operator called it "405".)*
- **Symptom:** the published episode FREEZES on the last drama frame while the closing theme plays; the rolling audio-reactive scopes ("HUD treatment") + credits never appear. Worked 100% on 2026-06-12; broke after the §4D scopes node landed 2026-06-13.
- **Root cause (PROVEN by render durations, not assumed):** `OTR_SceneAwareScopes.render_scopes` (nodes/otr_scene_aware_scopes.py) rendered only `manifest['total_target_frames']` (the BEATS-ONLY budget) via `plan_scope_frames` (L268) — it did NOT extend to the MASTER-audio length the way `OTR_SilentComposite` does (the composite credits-restore extends `target_total` to the master WAV). When the master exceeds the beats (longer closing theme / head-tail gap), the scopes mp4 is shorter than the master. The §4D `OTR_PostUpscaleProcgenBlend` 3-input filtergraph blends `[source][procgen][scopes]` with `blend=...:shortest=1`, so the SHORT scopes input clamps the whole blended video below the master; `OTR_MasterAudioMux` then clone-holds the last frame over the remaining closing-theme audio = the freeze. MEASURED on `signal_lost_plunging_depths_20260614_185229`: master 39.705 s, composite `_silent.mp4` 39.68 s, floor 59.72 s, but `_silent_procgen_blended.mp4` = **32.24 s** (= the beats-only scopes length) → ~7.5 s frozen tail. 06-12 (pre-scopes, 2-input blend) finals matched their masters (no clamp). Regression introduced by the scopes node `39aa6c9` (2026-06-13), content-triggered (master >> beats); the operator's "worked 2 days ago" = pre-regression.
- **Fix:** FIXED 2026-06-14. `render_scopes` now extends the scopes plan to the master-audio length (the node already receives the master `audio` input): `master_frames = ceil(len(a)/sr*fps)`; pad the `plan` tail with `None` (suppressed — a black frame the lighten-blend ignores, matching the credits/post-roll region) and analyze/encode to `master_frames`. The scopes track now spans the master, so `shortest=1` no longer clamps below the deliverable. NO change to the composite, the §4D blend, the mux, or the AUDIO spine (frozen master untouched — visual/metadata only). Regression guard `test_scopes_span_master_audio_no_blend_clamp_bug406`. The diagnostic `[BUG-406 instr]` composite logging (added during triage, commit `9b1f5a1`) was REMOVED once the root was found.
- **Verify:** scopes unit suite 10/10 green; full suite + Bug Bible + `test_audio_byte_identical` green. On a fresh render `_silent_procgen_blended.mp4` ≈ master length (no ~7.5 s freeze) and the scopes/HUD treatment runs to the end of the closing theme.
- **Tags:** scopes, §4D, blend, shortest, frame-budget, closing-theme, freeze, mux-clone, regression

### ROUNDTABLE-CONVERGED FIX PLAN 2026-06-14 (pass01: Gemini+Grok+DeepSeek+GPT, ~$0.41) — FOR THE CODER WINDOW
Hardened plan + judgment: `docs/2026-06-14-opener-still-imagemodel/roundtable/pass01_plan.md` (+ `pass01_judgment.md`). **STATUS: IMPLEMENTED 2026-06-14** — FIX1 `3ef0098`, FIX3 `9b028c8`, FIX2 `601c13b`; `[BUG-403/404 instr]` logging removed `5899ec7`. Suite **4259 pass**, Bug Bible **16/7/3**, `test_audio_byte_identical` GREEN, HEAD==origin on `v2.0-alpha`. **AWAITS operator VISUAL look-QA** of a fresh real-episode render (opener b000 still over the head-gap; hero title card spanning [0, first_dialogue); green audio-reactive scopes; burned SDH captions; the per-role image dispatch trace in the server log). Build order was **FIX1 → FIX3 → FIX2**. Run full suite + Bug Bible after each; commit+push per green chunk; `test_audio_byte_identical` stays GREEN; any JSON change goes in `workflows/otr_scifi_16gb_full.json` + re-validate.
- **FIX 1 (BUG-403 remainder — opener centre BLACK):** code-only in `nodes/_otr_video_engines/render_driver.py::build_request_from_shot`. `flux_still.family="static_image_gen"` is NOT in `_SCENE_INIT_FAMILIES` (`{image_to_video,static_motion}`, L413) and `!= "ltx_video"`, so the opener (operator picked flux_still for music) never conditions on its scene still → `init_image=""` → black. FIX: make flux_still condition on the beat scene still via `_still_index(led).get(beat_id)`, LOUD if absent (mirror the LTX-I2V missing-still trace). **Do NOT patch `build_clip_manifest`** — its `init_image` field is DEAD CODE (the composite reads clip path/start_s/tfc, not init_image). Verify-at-build: the opener still is in `_still_index` (kind `scene_*`, key `b000_music_open`; broaden narrowly for `object_id=="still_"+beat_id` if needed). Add a `build_request_from_shot` regression test (opener → `asset_refs.init_image` non-empty, `init_source=="scene_still"`).
- **FIX 3 (BUG-404 — title window collapses to ~1s):** in `nodes/video_engine.py::SignalLostVideoRenderer.render_video`, right after `led = _OTRLC.load_ledger(script_json)`, call `otr_shot_lock.overlay_audio_timing(led)` BEFORE `_resolve_title_timing` so it sees `start_s` → window `[0, first_dialogue)`. Remove/guard the `_envelope_intro_end` 1s fallback. Verify-at-build: the `OTR_SignalLostVideo` `script_json` wiring in the JSON (if it is the pre-audio ledger → JSON wiring change, NOT code-only); post-audio `speaker_role` values (extend `_SPEECH_ROLES_VIDEO` if `char_voice`/`dialogue`).
- **FIX 2 (BUG-405 — per-role image model ignored):** NOT a dispatcher/fallback bug (dispatcher SKIPS on `assert_usable` fail; `_inprocess_gen_fn` dispatches by `engine_id`). Add a LOUD diagnostic in `otr_image_gen_dispatcher.dispatch_images` (`object_id/role/slot/resolved engine_id`), re-render, CAPTURE the real `image_policy_json`. If it carries `flux_gen1` → fix the SOURCE (OTR_ImageDirector widgets in `otr_scifi_16gb_full.json` + confirm `direct()` emits the live picks). If it carries lumina/qwen/hidream but flux minted → verify those engines' `render_image` + weights on disk; fail LOUD on missing. NO dispatcher rewrite.
- **CUTS (consensus):** no 2nd `music_open` line (breaks `derive_opening_music_beat`); no dispatcher rewrite; no composite-floor title card. DEFENSIVE (non-blocking): make the `plan_timeline_segments` `all()`-positioned flip LOUD in the composite report.
- **`[BUG-403/404 instr]` logging is LIVE** (commits `91e9eff`/`8a517b1`, logging-only) — keep for FIX 2's capture, REMOVE once all three land + regressions pass.

### INSTRUMENTED FINDINGS 2026-06-14 (coder window, HEAD `8a517b1`) — these SUPERSEDE the BUG-403/404 converged roundtable plan
A live 30-word smoke with the `[BUG-403/404 instr]` logging (commits `91e9eff` + `8a517b1`, logging-only) GROUNDED the opener bug and **overturns the roundtable's converged direction** (which had misread the code). Verbatim instr from render `signal_lost_echoes_of_the_restricted_20260614_171037`:
- `build_clip_manifest opener: bid=b000_music_open line_hit=False shot.start_s=0.0 -> row.start_s=0.0 tfc=238 exists=True`
- `plan_timeline_segments: positioned=True ... seg order=0 source=clip shot=shot_b000_music_open n=238`
- `_resolve_title_timing: music_open_line=None first_dialogue_f=None resolved_open=False -> {music_open_start_f:0, music_open_end_f:25}`

**BUG-403 REFRAMED — placement was never the bug.** `build_execution_plan` already stamps the synthetic opener shot `start_s=0.0`; `build_clip_manifest` resolves the row to `start_s=0.0` (shot.start_s fallback); `plan_timeline_segments` runs **positioned** and places b000 at `[0,238)=[0,9.5s)`. The original all-black opener was a **BUG-402 casualty** (blend fell back to source-copy); with 402 FIXED the blend SUCCEEDS and the opener now shows scopes + title (operator look-QA of the 17:10 obs final confirms). => the converged "synthesize a music_open line in EpisodeAssembler" is **NOT the fix** (redundant; a `start_s=0` line would ALSO break `derive_opening_music_beat`, which the roundtable missed, as it missed the pre-existing BUG-130 music-mirror).

**BUG-403 REMAINING (new, real) — opener CENTER STILL missing.** 17:10 obs final: title + green scopes present, but the b000 center image is BLACK. ROOT: `build_clip_manifest` sets `"init_image": _portrait_index(led).get(row_char, "")`; the opener beat has `char_id=""`, and the minted opener still (`still_b000_music_open`, a NON-cast image recorded in `ledger['images']`) is NOT in the char-id-keyed `_portrait_index` -> b000 `init_image=""` -> flux_still renders an empty center. Dialogue beats show because they carry real char_ids. FIX direction (harden in /roundtable): resolve the opener beat's init_image from the opener still, not the empty char-id key.

**BUG-404 UPDATE.** The legacy `[Video]` render's `_resolve_title_timing` gets `first_dialogue_f=None` (it runs on pre-audio-timing lines without `start_s`; the role set `("announcer","character")` DOES match, so it's a missing-`start_s` issue, not a role mismatch), so the title window collapses to the envelope fallback `[0,25)` (~1s). The static "SIGNAL LOST / <title>" header DOES show; the proper `[0, first_dialogue)` window does not. FIX direction (harden in /roundtable): feed the audio-timed lines so the card spans the head-gap.

### BUG-LOCAL-405: per-role IMAGE-model selection ignored — flux_gen1 used despite operator-set lumina/qwen/hidream  [RESOLVED `601c13b` 2026-06-14]
- **Date:** 2026-06-14 | **Phase:** 5 (image dispatcher / OTR_ImageDirector) | **Bible candidate:** no (config + unbuilt-engine reality, not a code defect)
- **Symptom:** OTR_ImageDirector set BEFORE the render (operator-confirmed) to `announcer_image_model=lumina_image`, `music_image_model=qwen_image`, `other_beats_image_model=hidream_i1`, but the 17:10 render minted EVERY still with `flux_gen1` (`[OTR.image.flux_gen1] minted portrait ...` x6). The per-role image-model picks are not honored.
- **Cause (GROUNDED — NOT a code defect):** TWO facts, both confirmed against the real code/JSON. (1) The dispatcher (node 91) reads `image_policy_json` from **OTR_ImageDirector (node 88)**, whose **SAVED `widgets_values` are `['flux_gen1','flux_gen1','flux_gen1',...]`** — all three image slots are flux_gen1 in `otr_scifi_16gb_full.json`, so the policy correctly carried flux_gen1 and the dispatcher faithfully minted it. `direct()` emits per-role picks VERBATIM (no flux default; an incompatible registered pick RAISES, an unregistered pick passes through) and the dispatcher SKIPS an unusable pick (it never silently substitutes flux). (2) The operator's intended engines are **not usable on this box**: `lumina_image`/`qwen_image`/`hidream_i1` (and chroma_hd/flux2_klein/sd35_large/z_image_turbo) are **default-OFF opt-in stubs** — `requires_flag` OFF, **`render_image` raises `NotImplementedError`**, and no weights on disk (`assert_usable` -> `EngineUnusable`). **`flux_gen1` is the ONLY working image engine** (always-on, real `render_image`, usable). So even if those picks HAD reached the policy, the dispatcher would have SKIPPED them (no image), never minted flux in their place.
- **Fix:** `601c13b`. Added a permanent LOUD `dispatch_images` resolution trace (`object/kind/role/slot/engine`) so an unexpected engine is visible in the server log, and made the unusable-engine skip message explicit ("SKIPPED -- no image minted, NOT silently substituted"). **No workflow-JSON change**: writing the stub engines into the shipped graph would skip/crash the render (they cannot mint). The saved `flux_gen1` defaults are correct/working. To actually use lumina/qwen/hidream they must be BUILT (real `render_image`), weighted on disk, and enabled (`OTR_ENABLE_*=1`) — a separate opt-in GPU track. Regression: `test_image_director_emits_distinct_per_role_picks_bug405` proves `direct()` emits distinct per-slot picks.
- **Verify:** suite 4259 pass + Bug Bible 16/7/3 @ `601c13b`; on the operator's re-render the server log shows `[OTR_ImageGenDispatcher] resolve: object=... -> slot=... engine=flux_gen1` per still (and a LOUD skip naming any unusable selected engine).
- **Tags:** image-director, dispatcher, model-selection, fail-loud, opt-in-stub, config

---

### BUG-LOCAL-403: ~9.5s all-black opening — opening music beat carries no timed line, so the opener still is never composited over the head-gap  [FIXED `3ef0098` 2026-06-14 (remainder)]
- **RESOLUTION (2026-06-14):** REFRAMED then FIXED. The all-black opener was a **BUG-402 casualty** (the blend fell back to source-copy; fixed `99320ae`) — placement was never the bug (instrumented: positioned, b000 at `[0,9.5s)`). The real REMAINDER = the opener **centre still** was black because **FIX1**: `flux_still` (family `static_image_gen`) was in neither `_SCENE_INIT_FAMILIES` nor the `ltx_video` branch in `render_driver.build_request_from_shot`, so the music-slot opener kept `init_image=""` and the cheap family drew its dark floor. FIX1 `3ef0098` conditions `flux_still` on the beat scene still (`_still_index(beat_id)`, LOUD if absent). The original "synthesize a timed music_open line" plan was REJECTED (redundant; would break `derive_opening_music_beat`). Verify on the operator render: opener shows the b000 still, not black.
- **Date:** 2026-06-14 | **Phase:** 4 (scene sequencer / composite timing) | **Bible candidate:** yes (an opening/closing music beat that drives a VISUAL must carry `start_s`/`dur_s`, or the composite has nothing to place over its window and downstream visual effects can't anchor to it)
- **Symptom:** the published episode opens on **~9.5 s of BLACK** while the opening theme music plays — no opener still, no title card, no reactive CRT. (`signal_lost_across_the_room_20260614_153812`; operator screenshots: black 0:00→~0:09.5, then the first character beat appears.)
- **Evidence:** `…153812_ledger.json` `lines[]` has **only 5 lines, all dialogue** — `b001` announcer `start_s=9.5`/`dur_s=8.83`, `b002–b004` character, `b005` announcer; **ZERO `music_open`/`music_visual` lines**. The first spoken line `b001` starts at `start_s=9.5` → a **9.5 s un-anchored lead-in**. A `b000_music_open` STILL was generated (`stills/still_b000_music_open_*.png` + `stills_manifest.json`) but has **no timed line/shot** to place it.
- **Cause (root):** the opening theme music (`OTR_StableAudioTheme.opening_theme_audio`, ~9.5 s) is **prepended as AUDIO by `OTR_EpisodeAssembler`** but is **never represented as a timed LINE**. `nodes/scene_sequencer.py` (~L688 + L708-712) does a **PASSTHROUGH** on `music_open`/`music_close`/`music_inter` and explicitly does NOT stamp timing — comment: *"Sequencer never handled music timing … music lines retain … `start_s=None, dur_s=None`. EpisodeAssembler / MusicGen handle music timing downstream."* So nothing carries the `[0, ~9.5 s)` opening-music window with `start_s`/`dur_s`. `OTR_SilentComposite.plan_timeline_segments` places beats by line `start_s`; with no timed music_open beat at `[0, 9.5)`, the head-gap is floor/black gap-fill and the existing `b000` still is never placed over it → **black opener**. (Note: this ledger had no music_open line at all — also confirm whether the writer emits one; even if it did, the Sequencer passthrough leaves it untimed.)
- **Fix (pending — coder window; ROUNDTABLE-CONVERGED 2026-06-14, GPT-5.5 + Gemini-3.1-pro + DeepSeek-v4, ~$0.33, grounded — see `docs/2026-06-14-opener-timing-bug/roundtable/pass01_judgment.md`):** stamp the opening music beat as a TIMED `music_open` line **in `OTR_EpisodeAssembler`, NOT the SceneSequencer**. GROUNDED WHY: EpisodeAssembler is the only node that prepends `opening_theme_audio` (scene_sequencer.py L1059) AND computes the master-timeline shift `_shift_s` (BUG-LOCAL-106, L1239-1276 = `sum(segments before scene) - xfade*scene_idx`); the SceneSequencer writes `start_s` in **scene-audio space** (L923 `start_s_space="scene_audio"`), the wrong timeline. **Correction to the original note:** there is NO existing music-mirror to "un-skip" — music lines are explicit passthrough (L706-713), the SFX mirror was deleted as a no-op (L945), and EpisodeAssembler only SHIFTS existing lines, never CREATES the opening-music one. So: (1) in EpisodeAssembler, when `opening_theme_audio is not None`, SYNTHESIZE a `music_open` line `start_s=0.0, dur_s=_shift_s` (derive from the real prepended-theme length, never hardcode 9.5). (2) ALSO emit a clip-manifest ROW for it — `plan_timeline_segments` reads `manifest["clips"]` (tfc>0) ONLY, never stills on disk, so the existing `b000_music_open` still is inert without a row: `start_s=0, target_frame_count=round(_shift_s*fps)`, shot_id/path -> the b000 still (`OTR_VideoRenderBatch`/`build_clip_manifest`). (3) A/V-sync guard: assert `total_target_frames == round(master_audio_dur*fps)` so the added beat does not double-count length (audio spine FROZEN — visual/metadata only). DEFENSIVE SHOULD-FIX (latent, not this episode's root): `plan_timeline_segments` `positioned = all(start_s is not None)` → ANY future untimed row flips the WHOLE composite to sequential; change to `any()` + filter untimed rows.
- **Verify:** re-render; the opener shows the `b000` still (or the title card) over the opening music — not black. Assert a `music_open` beat exists with `start_s≈0` / `dur_s≈first_dialogue.start_s`, and `plan_timeline_segments` emits a non-black segment for `[0, 9.5 s)`.
- **Tags:** scene-sequencer, composite, timing, start_s, music_open, head-gap, procgen-opener, av-sync

---

### BUG-LOCAL-404: §4C episode-title card never drawn on the opener (coupled to BUG-403)  [FIXED `9b028c8` 2026-06-14]
- **RESOLUTION (2026-06-14):** FIX3 `9b028c8`. Root: `video_engine.SignalLostVideoRenderer.render_video` loaded the **PRE-audio** `script_json` (lines carry no `start_s`), so `_resolve_title_timing` saw `first_dialogue_f=None` and collapsed the card window to the ~1s volume-envelope fallback (`[0,25)`). Fix: call `otr_shot_lock.overlay_audio_timing(led)` right after `load_ledger` (same on-disk-ledger contract ShotLock uses; `OTR_TEST_MODE`-skipped, fail-soft) so per-line `start_s` is present -> the card spans `[0, first_dialogue)` (~9.5s). Made the envelope fallback LOUD so a missing overlay can never silently recur. Code-only (no JSON rewire: `OTR_SignalLostVideo.script_json` stays as-is; the overlay bridges the timing). Regression: `test_resolve_title_timing_spans_head_gap_not_1s_bug404` + `test_overlay_audio_timing_stamps_start_s_from_disk_bug404`. `speaker_role` values were already `announcer`/`character` (in `_SPEECH_ROLES_VIDEO`), no role-set extension needed.
- **Date:** 2026-06-14 | **Phase:** 5 (procgen floor / §4C title card) | **Bible candidate:** no (likely resolved by 403; trace-only)
- **Symptom:** the big-bold decode→reveal→POP→dock episode-title card never appears at the open (operator screenshots: "no title / no title card / no reactive crt graphics").
- **Cause (partial — coupled to BUG-403):** `_resolve_title_timing` (`nodes/video_engine.py`) gets the card window PRIMARILY from a `music_open` line's `start_s`/`dur_s` — absent here (BUG-403). It has a FALLBACK (first-dialogue onset → window `[0, round(first_dialogue.start_s*fps)) ≈ [0, 238)` @25fps) that SHOULD fire from `b001.start_s=9.5` and draw the card on the floor's first ~238 frames. Since the card is still not visible, the coder must confirm WHICH link breaks: (1) does the fallback actually fire — does `render_video` receive `led["lines"]` with the 5 timed dialogue lines so `first_dialogue` resolves? (2) does the COMPOSITE actually show the floor's head-gap frames `[0,238)` — or does `plan_timeline_segments` drop to **SEQUENTIAL mode** (ANY shot row with `start_s=None` forces sequential — and an untimed `b000` music shot would do exactly that), so there is no positioned floor head-gap to carry the card? (3) BUG-402 made the post-upscale blend fall back to source-copy, dropping the floor procgen's SECOND (screen) pass — but the floor is also the composite BASE, so the head-gap floor SHOULD still show; re-verify on a clean post-402 render.
- **Fix (pending — coder):** primarily falls out of BUG-403 (a timed `music_open` beat → the §4C primary path resolves the card window). Independently, verify the fallback path end-to-end + that the composited head-gap actually shows the floor title-card frames. **ROUNDTABLE clarification (2026-06-14, grounded):** for THIS episode the ledger had ZERO music rows, so `plan_timeline_segments` was in POSITIONED mode (all 5 dialogue rows timed) and the head-gap `[0, _shift_s)` WAS floor — i.e. this is most likely a pure floor-title-card RENDER miss, not a composite-mode bug. Instrument to localize: log `_resolve_title_timing()` output + the first 5 `plan_timeline_segments` segments (`order/source/shot_id/src_start_frame/n_frames`) on the failing ledger → distinguishes "black because floor base missing" vs "floor present but card not drawn" vs "b000 clip hides floor." Also audit `_envelope_intro_end` (may return 0 on a faint opening → fallback disabled).
- **Verify:** re-render post-(402+403); the opener shows the decode→reveal→dock title card with the real episode title.
- **Tags:** procgen, title-card, §4C, timing, fallback, composite-headgap, coupled

> **Look-QA verify checklist for the next CLEAN render (post-402 fix, HEAD ≥ `99320ae`)** — the `153812` screenshots are from a render made BEFORE the BUG-402 fix (15:38 vs fix 15:52), so re-confirm on a fresh render: (a) burned SDH subtitles return; (b) audio-reactive green scopes return in the portrait gutters + the gap centres; (c) the **end-of-episode Telemetry HUD treatment** (scrolling cast/script post-roll) returns — it was *likely a BUG-402 casualty* (the blend's source-copy fallback dropped the floor procgen's HUD post-roll screen-pass; the composite truncates to audio length, so the HUD tail rides only on the blend's floor screen-pass). If the HUD is STILL missing after the 402 fix, open a new entry (suspected BUG-405).

---

### BUG-LOCAL-402: §4D procgen blend emitted `format=gbrpformat` → scopes + burned captions silently lost every render  [FIXED `99320ae` 2026-06-14]
- **Date:** 2026-06-14 | **Phase:** 5 (post-upscale procgen blend / ffmpeg) | **Bible candidate:** yes (concatenated ffmpeg filter steps need an explicit `,`/`;` separator; a step var that ALREADY ends with `,format=gbrp` + a second literal `format=gbrp` collapses into the bogus `format=gbrpformat`)
- **Symptom:** Live look-QA of a published episode showed NO burned SDH subtitles AND NO audio-reactive CRT scopes (the pillarbox side-areas stay black). Server log: `[PostUpscaleProcgenBlend] ffmpeg blend failed -- Error applying option 'gbrpformat' to filter 'format': Option not found ; falling back to source-copy so the pipeline still produces a deliverable`. The episode still publishes (obs_publish OK) but visually degraded.
- **Cause:** `nodes/otr_post_upscale_procgen_blend.py` 3-input branch (§4D scopes). When `green_only_overlay` is ON (the default), `green_only_step` ALREADY ends with `,format=gbrp`; the `[1:v]` (pgn) chain then appended ANOTHER literal `format=gbrp` with no separator → `...,format=gbrpformat=gbrp[pgn]`. ffmpeg parses `gbrpformat` as a bogus option on the `format` filter → the whole `filter_complex` is rejected → the node's try/except falls back to a plain source-copy, which carries NEITHER the scope overlay NOR the `ass=` caption burn (both live only in the failed graph). Default-on overlay = every episode degraded. Suite missed it: no test built the 3-input + green-overlay filter and checked it was well-formed.
- **Fix:** FIXED `99320ae` (2026-06-14). `pgn_gbrp = "" if green_only_step else ",format=gbrp"` so the gbrp pin is added EXACTLY once with the proper comma (green_only_step supplies it when ON; pgn_gbrp when OFF) → one `format=gbrp[pgn]`, no `gbrpformat`. Regression guard `test_build_cmd_3input_scopes_no_double_format_gbrp_bug402` asserts no `gbrpformat`, exactly 3 gbrp pins, and the caption ass-burn survives in both overlay states.
- **Verify:** full suite **4254 passed / 28 skipped**, Bug Bible 16/7/3 green @ `99320ae`. Restart ComfyUI Desktop + re-render → the final should show burned subtitles + audio-reactive scopes in the side areas.
- **Tags:** ffmpeg, filter-complex, procgen-blend, captions, scopes, source-copy-fallback

---

### BUG-LOCAL-401: flux_still rejected for the music slot — missing role tag, not a real incompatibility  [FIXED `9465dbd` 2026-06-14]
- **Date:** 2026-06-14 | **Phase:** 5 (video director / role-compat) | **Bible candidate:** yes (an engine's `roles` tuple must list every role its `required_inputs` actually allow, or the full-registry per-slot dropdown offers a pick that fail-closes at execute)
- **Symptom:** GUI Run (after BUG-400 fixed, the pipeline now reaches video) → `OTR_VideoDirector` (node 87): `ValueError: engine 'flux_still' does not fit any role for slot 'music_video_model' ('music_visual',) ... Pick a compatible model -- no silent swap.` Operator set all three video slots to flux_still (fastest path); announcer + other_beats validated, music did not.
- **Cause:** `flux_still` (`nodes/_otr_video_engines/cheap_families.py`) declared `roles = (announcer_visual, scene_broll, character_video)` — missing `music_visual` (and `background_abstract`). `engine_fits_role` requires the role to be in the engine's `roles` AND `required_inputs ⊆ role's available inputs`. flux_still needs only `text_prompt`, which `music_visual` DOES supply — so there was NO real input incompatibility, just a missing role tag. The director lists the full registry per slot (V-6) and fail-closes an incompatible pick at execute, so the missing tag surfaced as a hard stop. Suite missed it: the check only fires for registry-known engines on the live box (different in OTR_TEST_MODE).
- **Fix:** FIXED `9465dbd` (2026-06-14). Expanded `flux_still.roles` to all five roles (`announcer_visual, music_visual, character_video, scene_broll, background_abstract`) — a plain still is the fast universal pick and is valid wherever only `text_prompt` is needed (every role). No dropdown filtering (operator wants flux_still selectable everywhere) and no workflow-JSON change (shipped JSON uses ltx_video/humo_1.7B; the operator's flux_still picks now validate). Regression guard `test_flux_still_fits_all_roles_bug401`.
- **Verify:** full suite **4253 passed / 28 skipped**, Bug Bible 16/7/3 green @ `9465dbd`. Restart ComfyUI Desktop to load, then Run with flux_still in any slot → OTR_VideoDirector validates.
- **Tags:** role-compat, video-director, engine-roles, fail-closed, comfyui

---

### BUG-LOCAL-400: Enabled cloud writer slots block the whole graph — sentinel `(enable …)` not in COMBO list  [FIXED `d967c6b` 2026-06-14]
- **Date:** 2026-06-14 | **Phase:** 0 (script writer / INPUT_TYPES) | **Bible candidate:** yes (ComfyUI COMBO contract: a saved widget value must be a member of the INPUT_TYPES list — state-dependent dynamic dropdowns must keep the saved/“off” value valid in every state)
- **Symptom:** GUI **Run** → node 1 `OTR_LedgerScriptWriter` turns red, run produces nothing. Server log:
  ```
  Failed to validate prompt for output 12:
  * OTR_LedgerScriptWriter 1:
    - Value not in list: comfy_slot_a_model: '(enable Comfy Credits)' not in (list of length 21)
    - Value not in list: openrouter_slot_a_model: '(enable OpenRouter)' not in (list of length 344)
    - Value not in list: openrouter_slot_b_model: '(enable OpenRouter)' not in (list of length 344)
    - Value not in list: comfy_slot_b_model: '(enable Comfy Credits)' not in (list of length 21)
  Output will be ignored        # also outputs 92, 93, 85, 94, 7 → whole episode aborts
  ```
- **Cause:** `openrouter_catalog_dropdown_choices()` / `comfy_catalog_dropdown_choices()` (`nodes/_otr_model_catalog.py`) include the enable-sentinel **only when the lane is DISABLED**; when **ENABLED** they return the catalog (344 OpenRouter / 21 Comfy) with **no sentinel**. The saved workflow `workflows/otr_scifi_16gb_full.json` (widgets_values[19–22]) stores the sentinel as each slot's value. With `OPENROUTER_API_KEY`+`OTR_ENABLE_OPENROUTER=1` and `OTR_ENABLE_COMFY_CREDITS=1` set, `openrouter_enabled()`/`comfy_credits_enabled()` return True → sentinel dropped from the lists → ComfyUI's pre-exec COMBO validator rejects the saved sentinel → all node-1 outputs ignored. The S3 resolver `_clean_slot_value()` ALREADY treats a parenthesized sentinel as “unset” (→ local/env fallback), so **only the validation layer is wrong**, not the resolver. Suite stays green because the slot-widget tests exercise the disabled (sentinel-only) branch; nothing loads the real JSON through COMBO validation with the lanes enabled.
- **Fix:** FIXED `d967c6b` (2026-06-14). New `_lead_with_sentinel()` helper in `nodes/_otr_model_catalog.py`; both `openrouter_catalog_dropdown_choices()` and `comfy_catalog_dropdown_choices()` now lead EVERY branch (disabled, enabled+cache, enabled+empty, import-fail) with the enable-sentinel, so it is always `choices[0]` — the slot's valid 'off' value AND its INPUT_TYPES default. This keeps 'off' the default even when a lane is enabled (no silent default to a billable model). No workflow-JSON change (the stored sentinel was already correct). This is the SYMMETRIC fix to the 2026-06-10 sentinel restore (`test_workflow_json_guardrails` L694-700), which only covered lanes-OFF and left lanes-ON broken. The S3 resolver `_clean_slot_value()` already maps the sentinel to 'unset → local/env fallback', so no resolver change was needed. **Workarounds (no longer needed):** pick a real slug per slot, OR unset `OTR_ENABLE_OPENROUTER` / `OTR_ENABLE_COMFY_CREDITS`.
- **Verify:** DONE — full suite **4252 passed / 28 skipped** (4249 baseline + 3 new guards), Bug Bible **16 / 7 / 3**, all green @ `d967c6b`. New guards: `test_enable_sentinel_leads_when_enabled_bug400` (openrouter + comfy) + `test_saved_slot_sentinels_validate_with_lanes_enabled_bug400` (INPUT_TYPES end-to-end with both lanes ON). **Operator action to see it live:** the running ComfyUI loaded the old module at boot (14:21) — **restart ComfyUI Desktop**, then re-Run with lanes enabled; node 1 should validate and outputs should no longer be ignored. Sibling audit: only the cloud slot pickers had the state-dependent omission; `creative_writing_model`/`technical_model` are curated-always-listed (the live log showed only the 4 cloud slots failing), so no other dropdown is affected.
- **Tags:** comfyui-combo, input-types, json-wiring, openrouter, comfy-credits, validation, dynamic-dropdown

---

_(baseline was clean at 2026-06-14 HEAD `961d8fc`; first bug of the epoch logged 2026-06-14.)_
