# Video stack pinned dependencies — Day 2 lock
# =============================================
# Matches the running ComfyUI .venv as of 2026-04-17 on Jeffrey's RTX 5080
# Laptop (Blackwell sm_120, CUDA 13.0, Python 3.12.11).  Locked so the
# 14-day sprint stack is reproducible; deviate from these only if a Day-2+
# kill criterion trips and the fallback table in ROADMAP.md P0 demands it.
#
# NOT a full ComfyUI requirements file — only the deltas needed for the
# otr_v2.visual.backends video stack (FLUX, PuLID, LTX, Wan2.1,
# Florence-2, SDXL Inpainting).
#
# Explicitly excluded: flash_attn (NOT BUILDABLE on sm_120 — do not chase).
#   Rely on torch.nn.functional.scaled_dot_product_attention + SageAttention.

# Core runtime (from existing venv — pin-to-installed, do not upgrade blind)
torch==2.10.0+cu130
diffusers==0.37.0
transformers==5.5.0
accelerate==1.13.0

# FLUX text encoder tokenizer
sentencepiece>=0.2.0

# Image IO (Pillow already required by ComfyUI; pin floor for safety)
Pillow>=10.0.0

# HuggingFace model loading — OFFLINE-FIRST; HF_HUB_OFFLINE=1 in prod
huggingface_hub>=0.24.0

# Optional: FLUX FP8 GGUF fallback (Day 2 kill-criterion escape hatch only)
#   Uncomment and install locally only if diffusers FP8 peaks > 14 GB on
#   1024² and the city96/ComfyUI-GGUF logic needs to be ported.
# gguf==0.10.0
