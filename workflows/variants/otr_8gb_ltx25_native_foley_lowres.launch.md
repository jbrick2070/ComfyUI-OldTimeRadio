# Launch recipe: otr_8gb_ltx25_native_foley_lowres

GENERATED by scripts/build_variants.py -- do not hand-edit (regenerate instead). Paired variant: `workflows/variants/otr_8gb_ltx25_native_foley_lowres.json`.

- profile: `otr_8gb_ltx25_native_foley_lowres` (8 GB NVIDIA (Ada or newer) -- THE FAST, LOW-RESOLUTION FOLEY LANE. Identical to otr_8gb_ltx25_native_foley in every way that makes an episode -- same mix4x8 transformer, same Gemma-4 encoder, same 97-frame clips, same self-generated foley mixed under the voices -- except that it decodes the picture at the model's native 832x480 instead of running the x2 latent upscaler and a second refinement sampler to reach 1664x960. PICK THIS ONE IF YOU DO NOT WANT TO WAIT. Stage two samples the whole joint latent again at four times the pixel count and then decodes that, which is where much of a small card's render time goes; dropping it costs real detail and buys back the time. Pick otr_8gb_ltx25_native_foley instead when you want the sharper picture and will wait for it. It also needs ONE FEWER WEIGHT: the x2 spatial upscaler never loads here, so it is not in this lane's download list. THE HOST-RAM REQUIREMENT IS UNCHANGED AND IS STILL THE THING THAT BITES -- 8 GB VRAM plus about 10 GB of FREE SYSTEM RAM, because the 12.86 GB transformer exceeds 8 GB and streams, and the Gemma-4 12B encoder is pinned to the CPU on this tier so its 9.88 GB lands in host RAM. Lowering the output resolution does not change either of those; a box with 16 GB of RAM and a browser open will still page. NOT YET PROVEN ON A LEG: the lane is registered, wired and suite-green, and the time it saves is arithmetic about which nodes run rather than a measurement.)
- status: shipping
- platform/backend/vendor: any/cuda/nvidia
- master_hash: `3963f4679268dcc86e96ae4827dc19eaf2e4180b51a0150583a48f1fd6b9dea3`

## ComfyUI launch

- args: `(none)`
- sage_attention: False

## Environment

- Windows hosts: set `PYTHONUTF8=1` (cp1252 consoles crash the prestartup banner otherwise).
- Models root override: `OTR_COMFYUI_MODELS_ROOT` (the `C:\ComfyUI-Models` default in _otr_hf_env/_otr_gguf_backend is a Windows-only convenience); GGUF path escape hatch: `GEMMA4_12B_GGUF_PATH`.

## Music (per bank; only My Story takes its own)

- `media_archive`: small-group jazz quartet, relaxed swing (fixed)
- `original`: salsa conjunto at 100 BPM, clave-driven (fixed)
- `public_domain`: Chicago house at 122 BPM, soulful and steady (fixed)
- `scifi_news_pro`: Detroit techno at 128 BPM, hypnotic machine funk (fixed)
- `shakespeare`: chosen by the source's year; its own default is Elizabethan consort music
- `my_story`: the `music_style` widget on `OTR_StableAudioTheme` (blank = the house radio orchestra). Every other bank keeps its genre whatever is typed.

## Required key NAMES (values are NEVER stored here)

- (none)

## Install pointers

- torch cu128+ build (the nv baseline is 2.10/cu130)
- llama-cpp-python CUDA wheel
- ffmpeg on PATH (mac: ensure libx264 + aac encoders are in the build).

## Preflight models

- LTX25-distilled-DiT-comfy-mix4x8-13.8GB.safetensors
- gemma4-12b-ltx25-comfy-w4a8.safetensors
- ltx-2.5-video-vae-bf16.safetensors
- ltx-2.5-audio-vae-bf16.safetensors
- stable_audio_3_small_music.safetensors
- t5gemma_b_b_ul2.safetensors
