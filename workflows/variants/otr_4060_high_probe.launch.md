# Launch recipe: otr_4060_high_probe

GENERATED by scripts/build_variants.py -- do not hand-edit (regenerate instead). Paired variant: `workflows/variants/otr_4060_high_probe.json`.

- profile: `otr_4060_high_probe` (PROOF LEG: haunted lane + Qwen3-4B-Instruct-2507 GGUF writer -- one variable changed from otr_nvidia_8gb_haunted (the writer). Exists to move that row's vram_fit_tier off UNKNOWN with a published episode.)
- status: draft
- platform/backend/vendor: any/cuda/nvidia
- master_hash: `cfcb7ec015d37f818c92718c677790bec581d7b89d7c50415ffe5a2093c1caac`

## ComfyUI launch

- args: `(none)`
- sage_attention: False

## Environment

- Windows hosts: set `PYTHONUTF8=1` (cp1252 consoles crash the prestartup banner otherwise).
- Models root override: `OTR_COMFYUI_MODELS_ROOT` (the `C:\ComfyUI-Models` default in _otr_hf_env/_otr_gguf_backend is a Windows-only convenience); GGUF path escape hatch: `GEMMA4_12B_GGUF_PATH`.

## Required key NAMES (values are NEVER stored here)

- (none)

## Install pointers

- torch cu128+ build (the nv baseline is 2.10/cu130)
- llama-cpp-python CUDA wheel
- ffmpeg on PATH (mac: ensure libx264 + aac encoders are in the build).
- minimal Linux: libcairo for viz_mandala.

## Preflight models

- unsloth/Qwen3-4B-Instruct-2507-GGUF
- v1-5-pruned-emaonly-fp16.safetensors
- v3_sd15_mm.ckpt
- v3_sd15_adapter.ckpt
