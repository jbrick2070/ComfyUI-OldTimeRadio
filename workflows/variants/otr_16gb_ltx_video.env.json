{
  "_comment": "Paired recipe-knob env for the otr_16gb_ltx_video preset (video-tiers). ltx23_16gb_video is the FINAL display alias of the EXISTING ltx_video / eng_ltx_video two-stage HQ engine (unchanged; its internal x2 latent upscaler stays active); OTR_ENABLE_LTX_VIDEO is that engine's PRE-EXISTING enable flag (NOT a new gate). A launcher CLEARS the prior-tier key set, exports these, then boots + loads workflows/otr_canonical.json + applies the profile in memory (never this preset as the workflow). Silent output; OTR_MasterAudioMux muxes the OTR master audio afterward.",
  "variant": "workflows/variants/otr_16gb_ltx_video.json",
  "master_hash": "6f2b99f9e7782180dc6e60306435b6b6cc38d6b31564c57b1af774537966926e",
  "engine": "ltx_video",
  "env": {
    "OTR_ENABLE_LTX_VIDEO": "1"
  }
}
