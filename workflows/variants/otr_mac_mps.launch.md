# Launch recipe: otr_mac_mps

GENERATED by scripts/build_variants.py -- do not hand-edit (regenerate instead). Paired variant: `workflows/variants/otr_mac_mps.json`.

- profile: `otr_mac_mps` (Apple Silicon MPS (draft, UNVERIFIED on hardware))
- status: draft
- platform/backend/vendor: mac/mps/apple
- master_hash: `13d9b55d8f4767dfb21458f73d7fcb5e40ad38354cf7aee1b70042cf4ac873e7`

## ComfyUI launch

- args: `(none)`
- sage_attention: False

## Environment

- Windows hosts: set `PYTHONUTF8=1` (cp1252 consoles crash the prestartup banner otherwise).
- Models root override: `OTR_COMFYUI_MODELS_ROOT` (the `C:\ComfyUI-Models` default in _otr_hf_env/_otr_gguf_backend is a Windows-only convenience); GGUF path escape hatch: `GEMMA4_12B_GGUF_PATH`.

## Required key NAMES (values are NEVER stored here)

- `OTR_GOOGLE_API_KEY`
- Google key aliases accepted by existing clients: `GEMINI_API_KEY`, `GOOGLE_API_KEY` (preferred name: `OTR_GOOGLE_API_KEY`).

## Install pointers

- default PyPI torch wheels (Metal/MPS included)
- llama-cpp-python Metal wheel
- ffmpeg on PATH (mac: ensure libx264 + aac encoders are in the build).
- minimal Linux: libcairo for viz_mandala.

## Preflight models

- (registry-driven; see the engine rows for the selected lanes)
