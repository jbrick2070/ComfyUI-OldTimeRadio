{
  "_comment": "Paired recipe-knob env for the otr_8gb_wan preset (video-tiers). Recipe knobs the director/render widgets do NOT own (steps / decode-tiling / low-VRAM frame and canvas caps); the profile remains the source of truth. A launcher CLEARS the prior-tier key set, exports these, then boots + loads workflows/otr_canonical.json + applies the profile in memory (never this preset as the workflow). wan_8gb is flag-free (requires_flag=None; selection does NOT depend on OTR_ENABLE_WAN_TI2V).",
  "variant": "workflows/variants/otr_8gb_wan.json",
  "master_hash": "383c734b618b19bca9e3719f6ea709d858fa697b6ee5c27a781d417803e8956a",
  "engine": "wan_ti2v",
  "env": {
    "OTR_WAN_TI2V_STEPS": "30",
    "OTR_WAN_TI2V_CFG": "5.0",
    "OTR_WAN_TI2V_SAMPLER": "euler",
    "OTR_WAN_TI2V_SCHEDULER": "simple",
    "OTR_WAN_TI2V_TILED_VAE": "1",
    "OTR_VIDEO_LANDSCAPE_CANVAS": "832x480",
    "OTR_WAN_TI2V_MAX_FRAMES": "17"
  }
}
