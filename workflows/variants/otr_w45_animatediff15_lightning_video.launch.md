# Launch recipe: otr_w45_animatediff15_lightning_video

GENERATED by scripts/build_variants.py -- do not hand-edit (regenerate instead). Paired variant: `workflows/variants/otr_w45_animatediff15_lightning_video.json`.

- profile: `otr_w45_animatediff15_lightning_video` (45-word run: animatediff15_lightning_video (CUDA campaign row -- NOT the Mac proof path))
- status: shipping
- platform/backend/vendor: any/cuda/nvidia
- master_hash: `24e7cbc45995c0ec4dfdc9b7fe9320fbea18bea5d229125d63b1892b68900d62`

## ComfyUI launch

- args: `(none)`
- sage_attention: True

## Environment

- Windows hosts: set `PYTHONUTF8=1` (cp1252 consoles crash the prestartup banner otherwise).
- Models root override: `OTR_COMFYUI_MODELS_ROOT` (the `C:\ComfyUI-Models` default in _otr_hf_env/_otr_gguf_backend is a Windows-only convenience); GGUF path escape hatch: `GEMMA4_12B_GGUF_PATH`.

## Required key NAMES (values are NEVER stored here)

- (none)

## Install pointers

- torch cu128+ build (the nv baseline is 2.10/cu130)
- llama-cpp-python CUDA wheel
- ffmpeg on PATH (mac: ensure libx264 + aac encoders are in the build).
- minimal Linux: libcairo for viz_mandala.

## Preflight models

- v1-5-pruned-emaonly-fp16.safetensors
- animatediff_lightning_8step_comfyui.safetensors
- vae-ft-mse-840000-ema-pruned.safetensors
