# Launch recipe: otr_w45_humo

GENERATED by scripts/build_variants.py -- do not hand-edit (regenerate instead). Paired variant: `workflows/variants/otr_w45_humo.json`.

- profile: `otr_w45_humo` (45-word run: humo)
- status: shipping
- platform/backend/vendor: any/cuda/nvidia
- master_hash: `a9733f2f6acaae642b288c3e1704b02312ea63f1b72eb36ac1350dd35f5cbd08`

## ComfyUI launch

- args: `(none)`
- sage_attention: True

## Environment

- Windows hosts: set `PYTHONUTF8=1` (cp1252 consoles crash the prestartup banner otherwise).
- Models root override: `OTR_COMFYUI_MODELS_ROOT` (the `C:\ComfyUI-Models` default in _otr_hf_env/_otr_gguf_backend is a Windows-only convenience); GGUF path escape hatch: `GEMMA4_12B_GGUF_PATH`.
- `OTR_HEADLESS_DISABLE_PINNED=1`
- `OTR_HEADLESS_RESERVE_VRAM_GB=2.921`

## Required key NAMES (values are NEVER stored here)

- (none)

## Install pointers

- torch cu128+ build (the nv baseline is 2.10/cu130)
- llama-cpp-python CUDA wheel
- ffmpeg on PATH (mac: ensure libx264 + aac encoders are in the build).
- minimal Linux: libcairo for viz_mandala.

## Preflight models

- (registry-driven; see the engine rows for the selected lanes)
