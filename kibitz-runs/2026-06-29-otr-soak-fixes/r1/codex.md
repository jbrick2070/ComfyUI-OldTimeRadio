VERDICT: no. The arc is plausible, but S-F and S-E rest on stale or contradictory architecture, and BUG-411 is partly already implemented.

MUST-FIX BEFORE BUILD:
1. [S-F] The proposed seam cites `scripts/_otr_soak_phase0.py` / `_otr_soak_phase1.py`, but those files are not present; the current coverage runner is `scripts/otr_coverage_sweep.py`, which says it runs a 30-word FULL episode through writer/audio/video (`scripts/otr_coverage_sweep.py:4-12`). Concrete fix: rewrite S-F around the actual harness (`scripts/otr_coverage_sweep.py` + `scripts/otr_api.py`) or create the missing fixture harness explicitly. Do not describe it as “mirror of existing phase0/phase1” unless those files are restored. verify: exact nonexistence of the two named scripts in `scripts\`.

2. [S-F] “Test harness ONLY -- no production-path change” conflicts with the real graph shape: node 91 consumes `script_json`, `image_policy_json`, `image_prompts_json`, `audio_done`, and `episode_id`; node 92 consumes `patched_ledger_json`, `master_audio_path`, and `image_done` from the normal upstream chain (`workflows/otr_scifi_16gb_full.json`, nodes 89-92). Concrete fix: define the fixture as an API/workflow patch that feeds the existing node inputs and invalidates cache, or add an explicit fixture-loader node and wire it into the production JSON per repo rules. Do not leave “where exactly to inject” open in a wire-ready plan.

3. [S-E] The no-fallback vision contradicts existing policy/UI and stale fallback architecture. `OTR_VideoDirector` still exposes and serializes `allow_auto_fallback` (`nodes/otr_video_director.py:216`, `nodes/otr_video_director.py:342`), schema default still allows it (`nodes/_otr_video_engines/schemas.py:126-129`), and `render_driver` still carries `FLOOR_NAMES`, `UNIVERSAL_FLOOR`, and `SYNTH_FALLBACKS` (`nodes/_otr_video_engines/render_driver.py:42-58`, `nodes/_otr_video_engines/render_driver.py:142-160`) despite `render_shot` now failing loud (`nodes/_otr_video_engines/render_driver.py:1526-1553`). Concrete fix: make S-E a migration checklist: remove/deprecate the widget and schema field, delete fallback constants and tests, then remove engine metadata references.

4. [S-A] Clip-fill ownership is incoherent. HuMo comments say capped 14B renders are exact-fit/mirror-extended before returning (`nodes/_otr_video_engines/eng_humo.py:102-107`), but the compositor still warns then encodes any short clip with `tpad=clone`, i.e. a last-frame hold (`nodes/otr_silent_composite.py:243-265`, `nodes/otr_silent_composite.py:395-411`). Concrete fix: set one contract: either every engine must return `frame_count == target_frame_count`, or `OTR_SilentComposite` must loop/ping-pong any short real clip. Acceptance should assert the manifest’s delivered frame count, not just absence of warnings.

SHOULD-FIX:
1. [S-B] The plan asks to “confirm node-87 ltx canvas widget,” but node 87 only has global `canvas_w/canvas_h` set to 832x480 in the workflow; `ltx_audio_in` is forcibly clamped later to 512x288 in `render_driver` (`workflows/otr_scifi_16gb_full.json`, node 87 widgets 9-10; `nodes/_otr_video_engines/render_driver.py:1165-1179`). Concrete fix: state that LTX-AV canvas is a render-driver override, not a node-87 widget, or add a real per-engine canvas control.

2. [S-E] Dropdown-label scope is underspecified. Current labels are only `engine_id` plus aspect suffix (`nodes/otr_video_director.py:61-63`, `nodes/otr_video_director.py:105-118`); registry capability rows have VRAM estimates and model requirements but no display label/recipe field (`nodes/_otr_video_engines/registry.py:127-232`). Concrete fix: add a registry/display metadata field and generate labels from it, or cut the rich-label work from this sprint.

3. [S-E] Retiring `still_motion`, `station_card`, and `abstract` needs dependency sequencing. They are registered engines (`nodes/_otr_video_engines/cheap_families.py:165-191`), capability rows (`nodes/_otr_video_engines/registry.py:127-139`), and floor names (`nodes/_otr_video_engines/render_driver.py:42-47`). Concrete fix: first remove fallback metadata/tests that still name them, then unregister and remove workflow dropdown values.

4. [S-C] `audio_motion_profile` is a broad new subsystem, not a medium bolt-on. It needs a ledger schema, producer location, per-engine consumption contract, cache key/`IS_CHANGED` behavior, and proof that conditioning WAVs never replace master audio. Concrete fix: split S-C into “profile extraction + ledger stamp” and later per-engine consumers. [ASSUMPTION] The profile does not currently exist as a shared ledger field; verify before build.

OPTIONAL / NICE-TO-HAVE:
- [S-A] Face-presence phase 2 is correctly deferred; keep it out of this sprint unless the legibility guard produces false greens.
- [S-F] Add a fixture hash to every leg report so visual comparisons prove they used the same baked ledger/audio.

CUT THESE (scope / over-engineering):
1. [BUG-411] “Restore FluxGuidance” is stale: `flux_gen1.py` already has `OTR_FLUX_GUIDANCE` default 3.5 and wires a `FluxGuidance` node into its in-process graph (`nodes/_otr_image_engines/flux_gen1.py:86-93`, `nodes/_otr_image_engines/flux_gen1.py:130-149`). Safe cut: remove that item and keep only unverified look levers. verify: cinematic suffix, distress suffix, bookend seed, portrait style line.

2. [BUG-411] “Wire the FluxGuidance node into the JSON” does not match the current architecture; image generation is behind `OTR_ImageGenDispatcher`, while Flux builds its own internal graph (`workflows/otr_scifi_16gb_full.json`, node 91; `nodes/_otr_image_engines/flux_gen1.py:119-154`). Safe cut: do not add a workflow node unless image generation is intentionally moved out of the dispatcher.

3. [S-E] `visualizer_rainbow` should stay out of this correctness sprint. There is already a real `visualizer` engine with audio-reactive CRT scope behavior (`nodes/_otr_video_engines/eng_visualizer.py:1-19`), and the plan itself says rainbow needs its own roundtable. Safe cut: defer until after S-A/S-B/S-D/S-E core are green.