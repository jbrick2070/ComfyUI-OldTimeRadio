VERDICT: no. The plan still has an unresolved route and the existing code cannot make "14B on talking-face beats only" work without new routing/schema changes.

MUST-FIX BEFORE BUILD:
1. [GATING DECISION / MUST-FIX 1] "14B on talking-FACE beats only" is not implementable through the current `other_beats_video_model` slot. `OTR_VideoDirector` validates `other_beats_video_model` with `fits_any`, not all roles, so `humo_14B_169` passes because `character_video` fits even though `scene_broll` / `background_abstract` do not (`nodes/otr_video_director.py:77-84`, `nodes/otr_video_director.py:345-353`; role inputs at `nodes/_otr_shared/role_compat.py:55-72`). `OTR_ShotLock` then stamps that same engine onto all three other-beat roles (`nodes/otr_shot_lock.py:709-780`). Concrete fix: add an explicit per-sub-role routing data shape before coding, e.g. `video_models.character_video_model`, `scene_broll_video_model`, `background_abstract_video_model`, or a deterministic ShotLock rewrite that maps only `character_video` to `humo_14B_169` and keeps scene/background on a compatible configured engine. Update profile JSON, widget mapping, workflow JSON, and tests in the same change.

2. [GATING DECISION / MUST-FIX 1] The plan says "confirm ... per-beat fallback" but current render fallback is disabled, so incompatible face-less beats will abort rather than fall back. `render_shot` raises `RenderError` on any engine failure and documents "NO FALLBACKS" (`nodes/_otr_video_engines/render_driver.py:1468-1495`). `run_real_episode` passes a fallback chain, but `render_shot` ignores it (`nodes/_otr_video_engines/render_driver.py:1634-1680`, `nodes/_otr_video_engines/render_driver.py:1470-1475`). Concrete fix: remove "confirm fallback" as an option; either prevent incompatible assignments before render or explicitly reintroduce a scoped, tested fallback policy.

3. [MUST-FIX 3] Beat-length cap is still not specified as code. HuMo currently quantizes/clamps inside `render_clip` to `_HUMO_MAX_FRAMES=177` (`nodes/_otr_video_engines/eng_humo.py:53-54`, `nodes/_otr_video_engines/eng_humo.py:339-341`) and returns the encoded frame count from the shorter render (`nodes/_otr_video_engines/eng_humo.py:352-367`). The manifest still carries the original `target_frame_count` from ShotLock (`nodes/_otr_video_engines/render_driver.py:1921-1950`), and `OTR_SilentComposite` warns that short motion clips will hold the last frame unless loop/ping-pong extended (`nodes/otr_silent_composite.py:237-262`). Concrete fix: define a constant such as `HUMO_14B_SAFE_RENDER_FRAMES`, render at that cap, then call the existing `wrapper_bridge.extend_frames_to_target` before encoding so `frame_count == target_frame_count` for the manifest (`nodes/_otr_video_engines/wrapper_bridge.py:438-466`).

4. [Build order] "profile+workflow edit ... via `_otr_workflow_apply.py`" is underspecified for the current exact-match invariant. The checked-in profile says `other_beats_visual = "humo_1.7B"` (`config/profiles/16gb_full.json`), while the live workflow widget currently stores `"visualizer (16:9)"` for `OTR_VideoDirector.other_beats_video_model` (`workflows/otr_scifi_16gb_full.json`, node 87). Tests require profile and saved workflow widget values to match exactly (`tests/test_capability_profiles.py:176-205`, `tests/test_workflow_apply.py:111-117`). Concrete fix: state the exact saved widget representation to commit, including aspect suffix or bare id, and update `config/profiles/16gb_full.json` plus `workflows/otr_scifi_16gb_full.json` together.

SHOULD-FIX:
1. [MUST-FIX 2 / Build order] Acceptance says prove "ledger rows / render manifest used `humo_14B_169`", but the current soak assertions only count `humo`, not `humo_14B_169` (`nodes/_otr_video_engines/render_driver.py:2030-2047`, `nodes/_otr_video_engines/render_driver.py:2104-2107`). Concrete fix: add a production acceptance helper that asserts manifest `engine_histogram["humo_14B_169"] > 0` and that all such rows are `role == "character_video"`.

2. [SHOULD-FIX] Preflight should check the exact 14B landscape engine, not generic HuMo. `HuMo14BLandscapeEngine` inherits the flag/checkpoint path but has its own engine id and cfg hook (`nodes/_otr_video_engines/eng_humo.py:541-563`). Concrete fix: instantiate/get `humo_14B_169`, call `assert_usable`, and verify `OTR_ENABLE_HUMO=1` plus the 14B checkpoint path resolved by `_ckpt_path()`.

OPTIONAL / NICE-TO-HAVE:
- Add a small CPU test proving `OTR_VideoDirector` no longer accepts an aggregate other-beats engine unless every role it will actually stamp can satisfy required inputs, or proving the new per-sub-role policy stamps compatible engines.

CUT THESE (over-engineering):
1. [CUT / MUST-FIX 4] Keep "two-stage evict" cut. The current HuMo path already does post-decode `reclaim_idle_models` (`nodes/_otr_video_engines/eng_humo.py:355-361`) and the render loop has pre-render/inter-beat residue frees (`nodes/_otr_video_engines/render_driver.py:1520-1572`); adding another eviction design before routing/cap fixes increases failure surface without addressing the current build blockers.