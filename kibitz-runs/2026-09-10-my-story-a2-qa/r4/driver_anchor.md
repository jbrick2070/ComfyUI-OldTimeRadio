# A2 finished-diff anchor, before CLI dispatch

Driver Codex, sole editor, af9ccb09 base. Eight production files and nine test
files currently changed. Full R1-R4 design already converged under
my-story-cross-machine/r4; this is the required single CLI finished-code review.

Root read the catalog, load/request/cache owners, all four native generation
paths and scheduler/structured-call fit preparation. Actual config and explicit
setting are now separate. Known nested decoder context wins; snapshot, AutoConfig
and loaded config never get replaced by an estimate when later metadata is sparse.
Pin and canonical hub root are captured before native discovery and retained
through load/cache publication. GGUF and all three remote transports route early.
Epoch ownership and weights-only HF admission remain in place.

CPU preparation preserves exact normalization/template/tokenization. Generation
fits before device transfer; inspector returns primitives without generation
accounting. structured inspection enriches the schema exactly as the ladder does.
Default output minimum is one token; explicit atomic contracts remain. Google
uses this shared default too; OpenRouter/Comfy retain their explicit floors.

Independent internal audits: Einstein found the sparse-AutoConfig downgrade;
root fixed it and added actual loader-boundary coverage. Euler found no production
issue and suggested torch-availability skip in the new fixture, now applied.
No live/model/GPU run, no Mac kill diagnosis or complete source qualification.
