# R4 driver anchor - Codex

VERDICT: ready for bounded implementation, pending residual-defect review.
GO_FORWARD alone owns current work. R1-R3 judgments grounded real Windows files.
Prior final exact bytes are this input. Codex is panelist and sole judge.

CONFIRMED closure: A1 optional metadata/count/monologue gates removed, provider
capacity wrapper+None reused, ordered actual topology normalized before seal,
frame credits and actual cues assembled consistently, shared ledger repair and
freeze reused. Root added shared speakable-text predicate and late title parity.
No new graph control: canonical23nodes/63links/37writer widgets/link291 unchanged.
B fresh per-call primitive receipts, actual fallback identity after success,
scheduler helper classification and Original-only pre-serialization credit.
C full measured wrapping in same scratch/paint function, no title truncation.

A2 deliberately bounded to known native HF capacity and explicit settings.
Unknown/remote estimate transport is a disclosed separate follow-up: None breaks
arithmetic/defaults20 locally and changes remote reservation. No false global
unlimited claim, new estimator or weakened explicit spend/VRAM settings. Actual
loaded snapshot owns capacity (newest config-only cache may disagree). Preserve
allocated GGUF n_ctx. A real OOM is acceptable; predictive story-length gates are
not. No named-field blame. This boundary prevents speculative shared rewrites.

Please identify NEW residual defects in the plan, with real source evidence.
Do not call planned changes missing because pre-build code still has the old
behavior. R3 reviews repeatedly did that. Credits/video cited gp bindings do NOT
read act_count; do not invent consumers. Known local executed request IDs remain
useful even with reported=False; only truly missing identities map to unknown.

Read-only Windows repo. Write only your review artifact. No model/GPU/testing or
other edits. Profiles disabled (stale overlay). Full canonical component proof
requires durable freeze verdict, then regressions and one finished-diff CLI QA.
No App/full publication claim or prior accepted-asset mutation. Root owns code.
