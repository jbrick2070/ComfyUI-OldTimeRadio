VERDICT: yes-with-fixes -- A/B/C match a real live refusal and two real credits defects, but the same document still points builders at D0, leaves act-count unbounded against the 1..6 size law, and names two contradictory success definitions for "finish My Story."

MUST-FIX BEFORE BUILD:
1. [WHERE TO PICK UP vs §0 THE ATTACK ORDER] The pickup block says D1 is done and the next coding work is Sprint A. Section 0 still says the next concrete step is "sprint 3: Codex My Story D0 from HEAD `e562e146`" and ranks "My Story (sprints 3-5 above)." Those are different programmes. A builder who obeys §0 (the file's own "READ THIS BEFORE PICKING A ROW" header) restarts shipped work. Fix: rewrite §0's next-step line to Sprint A; rename or map old "sprints 3-5" so they cannot be read as A/B/C.

2. [Sprint A.2 + Operator contract + D1 §4/P1] GO_FORWARD now treats requested act count as a preference and says "Preserve all authored acts." `docs/2026-09-10-my-story-d1-design.md` is still live law: P1 post-validator `len(acts) == act_count`; §4 "Story size is `act_count` 1..6 only"; P3 `music_inter` "exactly act_count-1." `nodes/_otr_episode_budget.py` `MAX_ACT_COUNT = 6`. `nodes/_otr_my_story.py` `StoryTreatment.acts` is `Field(min_length=1)` with no max, and P2 loops `treatment.acts`. Unbounded preserve-all plus one call per act is a runaway topology, not a preference. Fix (smallest): accept requested≠actual only when actual N is sequential 1..N and `MIN_ACT_COUNT <= N <= MAX_ACT_COUNT`; rewrite D1 P1/P3/§4 in the same change as the validator; do not code against two specs.

3. [Sprint A.2 numbering] `_make_treatment_validator` does two requested-N checks, not one: `len(model.acts) != act_count` and `act.n == i` with error `"acts must be numbered 1..%d in order" % act_count` (`nodes/_otr_my_story.py` 555-560). Dropping only the length test still fails a valid 2-act list when requested=1 if numbering is judged against 1. Live proof: `docs/2026-09-10-my-story-live/qwen4b_1_act_failed_log.txt` died on "acts has 2 entries; 1 were requested" after typed repair repeated 2. Fix: sequential check uses `len(model.acts)`, not the widget.

4. [Sprint A.2 interstitials] `_pass_frame` still computes `inter_wanted = (act_count - 1) if (include_act_breaks and act_count > 1) else 0` from the widget (`nodes/_otr_my_story.py` 1117-1121); `_make_frame_validator` then requires `len(music_inter) == inter_wanted` (700-702); `_assemble` walks actual `acts` and consumes `frame.music_inter` in order (868-899). Requested=1/actual=2 with breaks on yields 0 cues for 2 scenes; requested=6/actual=2 yields 5 cues for 1 gap. The plan names this but does not close D1 P3 (`include_act_breaks and act_count > 1`). Fix: `inter_wanted` from `(include_act_breaks and actual_acts > 1)` using accepted treatment length; D1 P3 follows that formula.

5. [Operator contract vs existing capacity gate] "No duration, word-count or story-length rejection" vs `_call` → `MyStoryInputTooLongError` ("Shorten it -- '%s' is the longest field", `nodes/_otr_my_story.py` 420-437, 121) and D1 retry table (prompt no-room is terminal). That is a truthful context-fit failure, not the act-count preference gate. An implementer who deletes every length refusal will also delete the only honest no-room path and then hit raw token arithmetic or OOM. Fix: keep no-room/OOM/cancel as truthful provider failures; the banned class is preference gates (requested act/word/duration mismatch on an otherwise usable ledger).

6. [WHERE TO PICK UP Sprint C vs later Mac sentence] Operator contract: fix credits hero crossing column 2; Mac acceptance does not prohibit it. Same section later: "Preserve the accepted title and credits; do not reopen their appearance during the hardware handoff" (echo of `docs/2026-09-10-mac-final/README.md`). Those instructions cannot both be executed. Fix: fence the Mac line as handoff-only; Sprint C is an explicit new layout defect (`otr_credits_roll._flow_col1` autoshrinks then `d.text` one line, `_PT_HERO_MIN = 48`; meta uses `_fit_text`, hero does not).

7. [Sprint B stamp site] Bank default `"an original story generated by machine for this broadcast"` is stamped in `OTR_LedgerScriptWriter.py` 3472-3483 before the lane. `_SlotScheduler._account_and_get_entry` can read a live `cache_entry` (`model_id`, provider dispatch, Google `cache_entry["google_model"]` in `nodes/_otr_google_api/llm.py`). OpenRouter concrete resolution is snapshotted in `_otr_writer_tail.py` 1568-1591 AFTER `script_json = json.dumps(...)` at 1541, so it misses the writer output wire (link 291 is writer node 1 slot `script_json` → mux 85, `workflows/otr_canonical.json`). Stamping only early misses alias resolution; stamping only at the resolved_models site misses `script_json`. Fix: one story -- identity for the printed Original line is taken from cache-entry when that is the executed model, and from the OpenRouter snapshot when that is; `script_json` is serialized after that stamp (or re-dumped). Do not invent identity. [ASSUMPTION] Comfy Credits "slug" field name: verify on the credits cache_entry, not only the widget.

SHOULD-FIX:
1. [PRIORITY #1 vs Sprint A dry-run vs D1 §11] Headline is "finish the My Story creator flow." `docs/2026-09-10-my-story-app-scope.md` §1: success is a published episode. D1 §11 Sprint 5: 1/3/6 canonical runs with the file in `otr/obs/` plus App/browser proof. This campaign's Sprint A 1/3/6 matrix is freeze-62 component-only and says it is not publication. Same numbers, weaker evidence. Fix: this arc ships "My Story ledger policy + two credits defects"; D1 §11 publication/App remains a later row under its own name. Do not retire D1 §11 by running freeze-62.

2. [Sprint A.1 interpret gates] `_make_interpret_validator` still rejects `cast_plan.planned != expected` where expected is derived from the numeric request (`nodes/_otr_my_story.py` 458-477). That is the same class as requested-act equality: a shape preference, not a broken ledger. Voice-capacity overflow (`MyStoryCastError` when expected > pool) is a usable-voices gate and should stay. Audit must say which of those two stays.

3. [Sprint A.3 vs D1 §8] Plan reuses `_otr_clean_transaction` + `run_ledger_clean` (creative rewrite) + `run_ledger_cleanup`. D1 already folded that the tail clean window runs on every lane, including My Story. `FIDELITY_BANKS` is only `shakespeare` and `public_domain` (`nodes/_otr_spoken_text_policy.py`); `my_story` gets language-kind rewrites of spoken lines. That does not destroy listener typed fields in `source_meta.story_input`. State that explicitly so "preserve the listener's full material" is not read as "skip clean" or "freeze-readonly means no tail rewrite." Freeze `content_owned_readonly` only skips inline safety cleanup (`nodes/_otr_freeze_cascade.py` 199-204), not the tail transaction.

4. [Sprint A HUD/telemetry] `_otr_writer_tail.py` 852-865 stamps `meta.gen_params_initial.act_count` from `resolved["act_count"]` (widget). After A.2, HUD/forensics will still say 1 while the ledger has 2 scenes unless requested and actual are both recorded on `meta.my_story` and that is what consumers read. Plan says record both; name this stamp as a consumer that must not remain the topology authority.

5. [Sprint C vertical budget] Multi-line hero increases `_flow_col1` height; footer fit already walks `_GAP_TIERS` then `_LEDGER_DROP_ORDER` abridgment (`otr_credits_roll.py` 844-850, 857-862). Wrapping Lantern can drop FRAMES/VRAM/REV rows that currently fit. State that as accepted, or cap hero lines, before visual QA treats abridgment as a regression.

6. [D1 header fingerprint] D1 still claims 62 links, `last_link_id` 290, 33 writer widgets. Live `workflows/otr_canonical.json`: `last_link_id` 291, writer `widgets_values` length 37, 23 nodes (ids 63,1,3,4,7,12,62,80-95). Plan's 37/291 match the file. Updating D1 without re-fingerprinting will reintroduce stale wiring claims. verify: exact `links[]` cardinality (plan says 63).

7. [Sprint A.4 LMFE cap] `nodes/_otr_structured_call.py` has no `constrained_generate` / `JsonSchemaParser` import; binder lives in `nodes/_otr_constrained_generate.py`. Keeping binder off this sprint is right. The "silently caps unbounded arrays at 20" claim is not in this repo. verify: installed `lmformatenforcer.JsonSchemaParser` default for arrays without `maxItems` before anyone treats 20 as a measured ceiling.

8. [Sprint A dry-run 3 and 6] The demonstrated bug is requested-1/actual-2. 6-act on Qwen3.5-4B is interpret+treatment+6 act calls+frame plus tail clean (extra LLM). [ASSUMPTION] that completes inside a useful window without OOM. Do not treat a 6-act timeout as a policy regression. Lead with the exact failed prompt `e11e5d86-2dda-41fa-b942-bb927a754b83`; 3/6 are topology extras.

OPTIONAL / NICE-TO-HAVE:
- Disclose requested vs actual act count in announcer/credits when they differ; Python-authored, not a new model call.
- Cap credits hero wrap lines so abridgment stays rare.
- Replace "delivery link 291" with `script_json` (writer 1 slot 1 → mux 85 slot 10); the claim is otherwise right.

CUT THESE:
1. This campaign's implementation of D1 §11 App + 1/3/6 publication / row 5 in the pickup table. Operator already deferred it; keeping it in the "next sprints" table recollides with A. Safe because graph path is the initial view and freeze-62 is defined as non-publication.
2. Registry / strip-down / alpha.21 packet (§0 leftover and the stale registry block). Pickup already says older queue is outside; leaving it in the same input as A/B/C is how D0 got re-queued.
3. Speaker-placement follow-on. Explicitly not P1; no ledger/credits dependency.
4. Replay/old-bundle work. Operator cut; My Story already admits typed input on a fresh canonical run.
5. Turning on the local schema binder / any grammar-cardinality project. Sprint A.4 already cuts it; keep it cut even if act-count tests stay red.
6. Re-running the D1 design arc or D0. The failure is a policy bug in shipped `_make_treatment_validator`, not a missing bank.
7. New nodes/widgets/canonical controls for A/B/C. Writer already has 37 widgets; no new graph surface is required for these three defects.

[ASSUMPTION] 2,165 / 210 Original ledger scan and "no spoken Jeffrey author in Original" -- only restated in GO_FORWARD, no scan artifact found in-repo; do not let that number drive a code path.
[ASSUMPTION] Qwen3.5-4B will emit freeze-clean sequential acts once the count equality gate is lifted; live run never reached assembly (`docs/2026-09-10-my-story-live/qwen4b_1_act_receipt.json`). Tests may use fixtures; the dry-run must not be declared passed from `/history` success alone (`OTR_LedgerFreezeCascade` / `meta.freeze_verdict` can be `needs_full_rerun`).
