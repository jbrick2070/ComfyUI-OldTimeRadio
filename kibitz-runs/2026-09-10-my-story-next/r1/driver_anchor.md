# R1 driver anchor - Codex

VERDICT: YES WITH FIXES. Review only the priority A/B/C sprints in GO_FORWARD;
retain the older numbered queue without redesign. GO_FORWARD is authoritative.

## MUST-FIX BEFORE BUILD

- CONFIRMED: Latest operator rejects artificial story-length/timing gates and
  accepts real OOM. `_otr_my_story.py` treatment validator rejects a valid two-act
  plan for a one-act request before any shared ledger repair. Actual prompt
  e11e5d86-2dda-41fa-b942-bb927a754b83 failed twice on that count. Requested counts
  must be preferences where ledger topology can remain valid; preserve all acts.
- CONFIRMED: frame interstitial count currently uses requested act_count while
  act generation loops treatment.acts. A count policy change must derive framing
  and receipts from actual accepted acts and audit downstream count consumers.
- CONFIRMED: `_otr_writer_tail.py` already owns clean + cleanup in one protected
  transaction. Reuse that; no parallel repair subsystem and no forged reseal.
- CONFIRMED: `_flow_col1` draws one line at font floor; live Lantern screenshot
  and current measured 1144px title inside 598px column prove horizontal overflow.
  Full title wrapping needs identical scratch measurement and visible drawing.
- CONFIRMED: actual model identity is available in scheduler cache/provider
  response metadata. Stamp Original credit before script_json serialization;
  virtual slot handles and late-only resolved_models metadata are insufficient.

## SHOULD-FIX / VERIFICATION

- CONFIRMED: MY Story has other early refusals (input budget, cast assumptions,
  optional fields). Audit each against usable-ledger necessity under new ruling.
  Don't hide infrastructure failures or discard user input to meet a target.
- CONFIRMED: freeze may finish execution with needs-rerun; require its durable
  semantic verdict, receipts and real ledger content for the component proof.
- UNVERIFIABLE until live rerun: Qwen 4B will produce an accepted ledger after
  removal of the demonstrated refusal. Plan must not claim it already passed.
- CONFIRMED: Existing LMFE default array cap makes broad schema binding a scope
  and policy hazard. Do not replace the rejected count gate with another ceiling.

## REVIEW BOUNDARY

Real Windows repository and venv only. Read source, never the lagging Linux mount.
No production/test/GO_FORWARD edits by reviewers. Only write the review artifact.
Driver is sole coder/judge. Runtime ComfyUI 0.35.1/frontend1.51.10, canonical23
nodes/63links, component target62 on full canonical prompt, saved mps -> cuda via
existing validated applier. Stale optional local profile is deliberately disabled.
No fresh generation, GPU run or paid auxiliary reviewers from review lanes.
