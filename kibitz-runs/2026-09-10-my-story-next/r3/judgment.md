# R3 grounded judgment - Codex

Verdict: integration plan ready for final convergence. Both external calls rc0.
Root read the cited Windows owners; source remains pre-build, so still-present
code is evidence for the planned change, not a newly missing plan requirement.

CONFIRMED / already specified in R2 input:
- AgY1/CursorM1: actual treatment count must drive frame and assembly. Breaks
  flag is explicit, missing proposals use existing composer, extras disclosed.
  AgY out-of-bounds claim MISREAD: current length guard avoids that exception.
- AgY3/CursorM2: normalize ordered treatment slots immediately after acceptance,
  record originals, then normalize accepted act replies before assembly/seal.
  No shot collision after that sequence. AgY says validator raises ValueError:
  MISREAD, it returns a diagnostic to the structured repair helper.
- AgY4/CursorM3-M4: prune planning/count/binary-gender/monologue gates, change
  missing gender default, preserve unique identity/reference requirements.
  Real exhausted allocation remains distinct from predicted request count.
- AgY2/CursorM5: append deterministic attribution before frame snapshot and
  seal; optional blanks create no spoken rows. AgY claims stamp_receipt reads
  story['frame']: MISREAD, _assemble946 reads frame.model_dump directly. Common
  accepted frame must still be used consistently. Existing clean transaction
  opens and reseals content-owned mutations; no detached generic repair.
- CursorM6: optional title override None invokes shared title generation.
- AgY5/CursorM7-M8: primitive journal at call success, before unload. Fresh
  invocation sidecar passes actual fallback slug, published after runaway check.
  Both reviewers mislocate model-gone fallback in _post_with_retries; it is in
  generate. Implement lifecycle at actual source boundary. Scheduler records
  only after base returns, never failed calls or retained cache/model objects.
- AgY6/CursorS4-S5: actual scheduler helper names, Original-only overwrite,
  per-call local/request identity and remote reported identity, stamp before
  serialization; aggregate telemetry remains separate. Do not classify every
  support helper as unknown: plan explicitly records known support separately.
- AgY7/CursorS6: one measured flow handles full hero lines, split long tokens,
  per-line height and shared scratch/paint; existing marked row abridgment.
- CursorS1: capacity blame removal belongs A1. Preserve original capacity error
  facts; no instruction to shorten the longest user field. A2 is separate.
- CursorS2: ProviderCapacityMessages AND None, inherited repairs. No broad
  schema binder; installed LMFE default20 is configurable, AgY 'unconfigurable'
  MISREAD. Output-limit semantics are existing shared provider behavior.
- Cursor optional: budget_mode receipt, real full canonical target62 and actual
  durable freeze verdict. These were already required, not optional shortcuts.
- AgY S2: remove model architecture mutation to estimate; exact loaded native
  configuration owns known HF capacity, not newest config-only snapshot.

MISREAD / rejected:
- CursorS3: cited credits299/video1558 only bind gen_params_initial. Neither file
  reads act_count; exhaustive search confirms no requested-count topology bug.
  Preserve those provenance displays; no unrelated consumer edits.
- AgY S1 proposed new direct HF_HUB_CACHE env read bypasses existing canonical
  cache-root repair. Reuse ensure_hf_home before discovery instead.
- AgY S3 would label every reported=False local executed ID unknown. Local IDs
  honestly identify executed requests, even without server-version reporting.
  Use explicit unknown only when identity itself is missing; retain reported flag.
- Optional fallback log adds nothing required; existing telemetry is sufficient.
- Cast identity/fidelity deviations are recorded, not falsely claimed fulfilled.
  Do not turn every interpretation discrepancy into another rejection gate.

NEW root-grounded additions folded into GO_FORWARD:
- Use public clean_spoken_text in both act and cumulative speaker coverage,
  matching _otr_cast_voice_coverage._sayable. Stage directions alone otherwise
  bypass last-act repair and fail later. Keep existing repair and predicate.
- Shared cleanup can fill a title after J.5. Synchronize final ledger title to
  canon/file before consistency and news headline; no new LLM stage/title gate.
- A2 bounded to known local HF/native capacity. Unknown/remote estimate transport
  changes remain explicit pending follow-up in GO_FORWARD. None is not unlimited:
  current local arithmetic/default20 and remote reservation/accounting require
  separate work. No claim all global capacity policies are fixed. Explicit user
  limits stay, native maximum is not a static allocation, GGUF n_ctx stays real.

Cuts confirmed: no replay, font-resolver, generic repair/binder, new controls,
GGUF reuse rewrite or VRAM-estimator rewrite. Actual live failed history retained;
no ledger/media pass claimed. GO_FORWARD is the only active plan.


## Operator correction during R4; R3 snapshot amended before renewed R4

The operator clarified selected act and character counts are binding, variable
per run. This supersedes our proposed acceptance of count drift. R4 calls were
interrupted; neither counts as a completed review. Prior final bytes preserved
in final_before_count_correction.md. Main plan now retains count validation,
uses selected numeric controls instead of guessed cast_plan, and supplies the
FULL failed treatment to the existing LLM repair seam. Root and read-only audit
verified generic400-character echo loses acts and a PostValidationError takes
one typed repair directly (two total calls is intended, not a missing rung).
Original messages + failed artifact + exact count instruction are sufficient
context; no global repair rewrite or outer retry loop. Existing count helpers
exclude announcer. No silent Python merge or drop. A3 records independently
verified older SciFi/advisory and legacy six-vs-ten gaps for separately reviewed
alignment; do not claim those banks compliant after MyStory-only code.

R4 now reviews this user-corrected snapshot; B/C and prior source grounding remain
valid. This is an explicit operator-driven input amendment, not an uninterrupted
original hash chain or a completed original R4.
