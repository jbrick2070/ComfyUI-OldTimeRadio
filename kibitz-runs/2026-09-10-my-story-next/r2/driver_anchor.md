# R2 driver anchor - Codex

VERDICT: YES WITH FIXES / concrete implementation decisions to verify.

CONFIRMED operator priority: no artificial gates, accept real OOM, preserve
listener content; GO_FORWARD is authoritative. Do NOT restore actual-act caps,
truncate titles, merge stories or archive pending backlog. R1 rejected those.

MUST-FIX before build:
- Make optional planning metadata advisory; preserve source and explicit credit.
  Keep true ledger identity/reference repair, actual voice allocation and saves.
- Actual act topology owns frame boundaries; requested/planned/actual metadata
  separate. Missing cue prompts have existing StableAudioTheme producer.
- Shared context A2 must inspect nested text_config, canonical cache root,
  pre-load estimated VRAM veto, native config mutation and resident capacity key.
  Preserve actual GGUF n_ctx and explicit operator settings. Avoid bank bypass.
- OpenRouter success identity must be response-local through fallback; aggregate
  snapshot is stale on missing model. Journal only primitives, no model retention.
- Stamp Original credit before finalizer/serialization; distinguish generation
  from cleanup helpers and returned calls from accepted authorship.
- Hero wrap must preserve all title glyphs, use same scratch/draw height and
  existing marked ledger-row abridgment. Do not introduce new truncation.

SHOULD-FIX: name exact test seams and smallest file changes; keep schemas,
prompts, fixtures and repair instructions aligned. Do not let a component freeze
PASS retire full-media qualification. No further rounds beyond requested R4.

Sources grounded: _otr_my_story validators/assembler, shared writer tail,
model catalog/loader/budget, OpenRouter backend, installed LMFE default20,
actual Lantern credits PNG and persisted live failed MyStory request/history.
Real Windows files/venv only. Review-only, no edits except your review artifact.
No GPU/generation calls. Root is sole editor and judge. Profiles disabled because
local overlay is stale; full canonical23nodes/63links, target62, no new widgets.
