VERDICT: no -- the production path will run, but the harness still cannot prove which recipe an arm measured, so the A/B this diff exists to run can still print a false number.

MUST-FIX BEFORE BUILD:
1. [§5 / §r3.2 / receipt_matches CONTEXT_RATIO exception] Interface contract is one-way. Engine (`nodes/_otr_audio_engines/eng_stable_audio_3.py` `_sa3_clip_window` + `generate_clip` receipt) records `seconds_total`/`duration_s` but never the ratio it used. Harness (`scripts/otr_music_ab.py` `receipt_matches`, the `OTR_SA3_CONTEXT_RATIO` branch) only checks `seconds_total >= duration_s * ratio`. A default 3x opening (`duration_s=12`, `seconds_total=36`) therefore satisfies a 1.0 control arm (`12*1.0=12`; 36>=12). That is the inert-arm class this binder was added to kill, on the one knob change 4 exists to A/B. Locked in by `tests/test_music_ab_binds_its_own_episode.py` (`seconds_total=12` matches ratio 1.0; no case that 36 must fail 1.0). Fix: write the clamped ratio into the engine receipt (`params["context_ratio"]` next to `seconds_total`), add `OTR_SA3_CONTEXT_RATIO -> context_ratio` to `_RECEIPT_FIELDS`, compare with `_same_setting`. Drop the floor special-case.

2. [§5 / episode_from_log] `run_arm` prefers `episode_from_log` then `newest_episode`. `episode_from_log` only matches `episodes/<slug>_<YYYYMMDD>_<HHMMSS>` in the runner log. `scripts/otr_canonical_api_run.py` never prints that path (QUEUED/RESULT/prompt_id/`otr/obs` only; verify: no `episodes\` episode-id echo in that file). The preferred binder is a no-op against the process it actually spawns; `tests/test_music_ab_binds_its_own_episode.py` feeds a synthetic log, which does not prove wiring. Live attribution is `newest_episode`. One foreign episode with music mtime after `started` and this arm producing none (timeout, crash, or still-running after the watcher exits) is measured as this arm. Empty-env / shipped arms then pass `receipt_matches` with no field checks. Fix (smallest): stop falling through. If the log does not name a fresh episode, refuse. Then make the runner print the episode dir it actually produced (history/output path, or the ledger path) so the regex has a producer.

3. [§5 / run_arm boot vs next arm] Hidden order dependency. `boot_server` runs only when `env_overrides and needs_its_own_server(env_overrides)`. A later arm with `{}` (shipped) does not reboot and `receipt_matches(episode, {})` returns True immediately. Sequence `control:OTR_SA3_CONTEXT_RATIO=1.0` then `shipped` (or any recipe arm then shipped) measures the mutated server as defaults. Docstring example happens to put shipped first; that is an unenforced convention, not a contract. Fix: if this process has already booted a server-side arm, a following arm with no server-side keys must boot a clean server (no overrides) or refuse. Do not rely on argv order.

SHOULD-FIX:
1. [parse_arm vs OTR_SA3_NEG_PROMPT] `parse_arm` splits settings on comma. `_RECEIPT_FIELDS` binds `OTR_SA3_NEG_PROMPT`, and `NEGATIVE_PROMPT_DEFAULT` / any real negative is comma lists. A control arm for the loop-negative cannot be expressed; it SystemExits or truncates. Fix: different pair delimiter, or read the negative from a file/env already in the parent, not from the comma list.

2. [EPISODES hardcode] `scripts/otr_music_ab.py` pins `C:\Users\jeffr\Documents\ComfyUI\output\otr\episodes`. Production writers use `otr_episodes_root()` / `OTR_OUTPUT_DIR` (`nodes/_otr_paths.py`). Launcher sets `OTR_OUTPUT_DIR` to that same tree on this box; another machine or a moved output root makes every binder miss. Fix: `Path(os.environ.get("OTR_OUTPUT_DIR") or <that default>) / "otr" / "episodes"`.

3. [receipt_of glob] First `audio/*_ledger.json` that parses wins, including an orphan `pending_*_ledger.json` (`nodes/production_ledger.py` still describes those leftovers). A stale ledger without the new params fails closed (lost number, not a lie). Fix: prefer `<episode_dir.name>_ledger.json`.

4. [test isolation / env] `test_a_cue_is_never_a_self_contained_piece` and `test_sa3_clip_window_*` assert 36.0/24.0 without pinning `OTR_SA3_CONTEXT_RATIO`. `_env_float` reads live (`nodes/_otr_shared/env.py`). An A/B shell that exported 1.0 turns those tests red or, worse, green on the defect. Fix: monkeypatch the default in those tests.

OPTIONAL / NICE-TO-HAVE:
- `tests/test_music_palette.py` still says a pace tie "falls to the earliest row of the mood table"; code uses `_PACE_TIE_ORDER` (`nodes/_otr_music_palette.py`). Comment only; slow still wins the cases the test names.
- Two names for the negative: ledger `render_receipt.negative_prompt` is the composer's (`nodes/stable_audio_theme.py` `_render_receipt`); engine text is `params.negative_prompt`. Harness compares params. Readers of the top-level field will be wrong when `OTR_SA3_NEG_PROMPT` is set.

CUT THESE:
1. `newest_episode` as silent fallback once must-fix 2 has a real log identity -- it is the heuristic this change said it replaced; keeping it reopens the pending-dir / wrong-episode path. Safe because the log (once it names the episode) plus `cues_written_after` plus equality `receipt_matches` are the proof.
2. Floor-only `OTR_SA3_CONTEXT_RATIO` branch once the ratio is in `params` -- redundant and weaker than equality.
3. Unused `index` in `votes.append((pace, index))` -- `_winning_pace` never reads it; `_PACE_TIE_ORDER` is the tie-break. Dead data, not a second order.

Grounded non-issues (so they are not re-litigated): `cue_spec_sha256` hashes only `generation_prompt`/`target_duration_s`/`placement`/`anchor_line_id` (`nodes/production_ledger.py` `_MUSIC_CUE_SPEC_FIELDS`); new receipt keys do not retouch identity or the merge gate. `%` vs arg counts on every `log.*` in `eng_stable_audio_3.py` match. `cues_written_after` empty list is False (`bool(cues) and all(...)`). `mood_devices` cannot return `[]` (`_device_limit` floor 1; unmatched -> `DEFAULT_DEVICE`). [ASSUMPTION] `Start-Process` inherits the PowerShell env that `boot_server` set; launcher `.cmd` does not `set OTR_SA3_*`. Not live-booted here.
