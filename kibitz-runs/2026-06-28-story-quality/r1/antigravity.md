VERDICT: build-ready as-is? no. Critical regressions of byte-identity for non-v2 runs, missing budget propagation to targeted rerolls, and ungrounded assumptions about register detection will break builds/tests.

MUST-FIX BEFORE BUILD:
1. [S2] Coda Segue Flag-Gating Defect: The plan modifies `NEWS_CODA_POOL` and increases attempts globally in `nodes/_otr_line_composer.py#compose_news_coda`. However, this function is called under `_style_grammar_on` in `nodes/OTR_LedgerScriptWriter.py:L4770` and is not aware of `story_quality_v2_enabled`. Modifying this function globally will violate the "default-OFF / byte-identical-when-able" invariant, altering outputs of existing tests/episodes where `story_quality_v2` is disabled but `_style_grammar_on` is enabled.
   - Concrete Fix: Add `story_quality_v2_enabled: bool = False` to `compose_news_coda` signature, pass it from `nodes/OTR_LedgerScriptWriter.py`, and gate all S2 prompt/attempts/fallback changes behind it.
2. [G1 / Reroll Reconstruction] Missing Budget Range in Ledger Meta: G1 scales `max_words` with the budget's `words_per_beat_range` `eff_hi` inside `compose_line`. However, `words_per_beat_range` is not currently saved in the ledger's `meta` dictionary or carried in `LineRequest`. As a result, the reconstructed `LineRequest` in `nodes/_otr_reroll.py:L366` will not be able to retrieve the budget-scaled word limit during targeted rerolls, causing them to fall back to static limits (e.g., 28 words) and violating both first-pass/reroll consistency and seed-determinism. [ASSUMPTION]
   - Concrete Fix: Save the budget's `words_per_beat_range` into `meta` inside `nodes/OTR_LedgerScriptWriter.py:L3024` (e.g., `meta["words_per_beat_range"] = list(episode_budget.words_per_beat_range)`), add `words_per_beat_range` to `LineRequest` in `nodes/_otr_line_composer.py:L735`, and retrieve/populate it during reconstruction in `nodes/_otr_reroll.py:L366`.
3. [S4] Broken Cliche Dropping Concept: S4 proposes a "deterministic de-cliche rewrite that drops the phrase" if the second reroll attempt still fails. Dropping a cliché phrase (e.g., "over my dead body", "not on my watch") from a sentence without syntax-aware editing will produce ungrammatical fragments (e.g., "Over my dead body, Lemmy." becomes ", Lemmy."). [ASSUMPTION]
   - Concrete Fix: Avoid naive phrase dropping. If a cliché survives the second attempt, fallback to a pre-defined mapping of safe replacements (e.g., "not on my watch" -> "not while I am here"), replace with a simple universally safe fallback, or simply accept the second-best draft and raise a validation warning instead of corrupting the sentence structure.
4. [S5] Non-Existent Deterministic Register-Divergence Detector: S5 proposes a "deterministic register-divergence reroll" to prevent interchangeable voices. The codebase has no NLP register/style detector for generated text (the existing `speech_signature_overlap` in `nodes/_otr_casting.py:L33` only checks the cast description strings at casting time). Attempting to implement a deterministic text-similarity register checker without an LLM critic call is an ungrounded assumption that will over-constrain dialogue or fail to compile. [ASSUMPTION]
   - Concrete Fix: Cut the line-level "register-divergence reroll" gate from S5. Keep only the prompt-side directive by threading the `speech_signature` into the per-line prompt.

SHOULD-FIX:
1. [S1] Over-Constrained Off-Premise Drift Detector: S1 proposes an off-premise-drift detector that checks if a line references *any* seed key_term/anchor. Since drama naturally requires short conversational lines (e.g., "Yes.", "I don't know.", "What?"), enforcing this at the line level will cause massive false-positive flags, exhausting the reroll budget and forcing the model to cram key terms unnaturally into every single sentence. [ASSUMPTION]
   - Concrete Fix: Define the off-premise-drift detector at the beat sequence / sliding window level (e.g., checking if the last N character lines contain zero seed anchors) rather than per individual line, or limit the check to high-tension/objective-critical beats.
2. [S3] Lack of Defect Comparison Metric for body_gate_reroll: S3 proposes to "keep the cleaner of the two drafts" for the writer's `body_gate_reroll` path in `nodes/OTR_LedgerScriptWriter.py:L4516`, but the plan does not define a scoring metric to compare drafts when both are imperfect (e.g., one has a minor leak and the other fails grounding). [ASSUMPTION]
   - Concrete Fix: Implement a simple defect count scoring system (summing grammar failures, punctuation issues, and leak-floor flags) in `nodes/OTR_LedgerScriptWriter.py` to deterministically choose the better draft.

OPTIONAL / NICE-TO-HAVE:
1. [G1] UI-Level Word-Ceiling Validation: Since high target word counts (>1363 words at 3 acts) raise an `InvalidEpisodeBudgetError` before generating, it would be nice to have widget-level validation or a helper tip in the ComfyUI node documentation indicating the relationship between act counts and maximum target words to prevent user confusion.

CUT THESE (scope / over-engineering):
1. [S6] Acronym-based Phantom Name Reroll: S6 proposes rerolling on `phantom_name` flags for bare acronyms like "SAT" or "Q-SAT". Since acronyms are common in sci-fi setups, and the phantom-name gate is currently detect-and-flag only, turning this into a reroll loop will add latency, invite false positives, and over-engineer a low-impact edge case. It is safe to cut.
