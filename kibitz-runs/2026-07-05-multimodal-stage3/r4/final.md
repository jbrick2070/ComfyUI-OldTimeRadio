# r4 judgment -- Stage 3 arc CONVERGED (BUILD-READY)

Verdict: CONVERGED. r4 raised only contract-precision fixes (era_tail
resolution ownership, empty-era_tail semantics, producer-side hash tests,
API naming, lint matching rule) -- no new seams, no architecture change.
All folded into STAGE3_SUBPLAN.md v5 FINAL ("R4 CONTRACT PINS").

Arc summary:
- r1 (codex): style-vs-content boundary -- forbidden_terms scrub CUT (could
  delete diegetic content); law narrowed to "JSON owns visual-style DELTAS";
  v1 slice = tails + allow_radio_tails only; geometry anchors stay Python;
  3B packs dormant until 3C; ledger_directives cut.
- r2 (codex): de-swallow the composer seams (except-Exception around
  finish/grade = hidden fallback); compose_still_word_prompt added to
  routing; forbidden-term warn state cut (load-time lint only); schema
  hygiene (version pin, id regex, frozen dataclass).
- r3 (codex + SONNET 3-LENS FAN-OUT, operator-directed): all 4 reviewers
  independently found _compose_mesh_fodder_prompt (:1234-1243) -- sweep
  complete; constant-ownership decision (constants stay as the extraction
  fixture, AST guard bans production reads -- zero 3A tail-test rewrites);
  helper signature shape; complete 3C pin list (incl. the TWO
  test_source_bank_widget_2c pins + the api-companions mock schema);
  cache/hash + audio-spine CONFIRMED SAFE.
- r4 (codex): contract pins above. Verify-at-build checklist adopted
  (r4/codex.md items 1-5).

Agent calls: 4 codex + 3 Sonnet subagents (r3) + 1 Explore (site map).
Antigravity: not attempted this arc (credit-bugged earlier today).
Plan of record: docs/multimodal-story-schema/STAGE3_SUBPLAN.md (v5 FINAL).
