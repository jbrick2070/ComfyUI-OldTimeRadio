# r1 judgment -- Stage 3 visual_style

Panel = codex (gpt-5.5); Claude anchor+judge. All 6 MUST-FIX grounded CONFIRMED.

Accepted into v2 (rewritten in docs/multimodal-story-schema/STAGE3_SUBPLAN.md):
- M1 + CUT-2: post-assembly forbidden_terms SCRUB is CUT (it could delete
  diegetic story content -- violates "ledger content untouched"). v1:
  forbidden_terms = load-time lint on the pack's OWN fields + compose-time
  WARN-ONLY disposition log (observability, zero mutation).
- M2: law narrowed -- "JSON owns visual-style DELTAS (tails + declared
  overrides); Python owns geometry contracts + core prompt assembly."
- M3 + CUT-3: v1 slice = TAILS + allow_radio_tails ONLY. Subject/motion/
  anchor-style overrides -> Stage-3 checklist (later optional chunk, gated on
  authoring all packs' fields + geometry-split design).
- M4: pack era_tail REPLACES only the ERA_TAIL_DEFAULT string; get_era_tail's
  profile logic stays Python, unchanged.
- M5: STYLE_ANCHOR family stays Python (geometry/lip-sync contracts locked).
- M6: 3B packs are ADDRESSABLE/DORMANT until 3C stamps meta["visual_style"]
  (2B pattern); "fully live" claim removed.
- S1 + CUT-1: ledger_directives dropped from the v1 schema (unknown key =
  hard error); archival adaptation strips it like the lab's
  outline_rules_extra in 2B.
- S2 -> verify-at-build: image cache/hash keys include the composed prompt
  (style change => hash change); confirm at 3A build.
- S3: CONFIRMED fine -- the 2C signature-filtered refine capture picks up any
  new positional-default run() param by construction.
Rejected: none.
