VERDICT: yes-with-fixes -- live failure and files are real, but several steps as written collide with existing topology, title, credit, and context APIs and will not build cleanly.

MUST-FIX BEFORE BUILD:
1. [A.2 / A.6] After dropping the exact-count gate, `_pass_frame` still sizes cues from the REQUEST, not accepted acts. `nodes/_otr_my_story.py:1118` is `inter_wanted = (act_count - 1) if (include_act_breaks and act_count > 1) else 0` while `_assemble` at `:897` uses `act is not acts[-1] and inter_seq < len(frame.music_inter)`. Requested-1 / actual-2 (the live failure) would then demand 0 interstitials for 2 acts; requested-3 / actual-2 would demand 2 cues for 1 slot. Fix: `inter_wanted = (len(treatment.acts) - 1) if (include_act_breaks and len(treatment.acts) > 1) else 0`. Record requested/planned/actual separately in `meta.my_story`; leave `meta.gen_params_initial.act_count` as request-only (`nodes/_otr_writer_tail.py:852-864`).

2. [A.2] Act numbering vs shot ids. `_assemble` uses raw `act.n` for `scene_id`/`shot_id` (`:871-872`) and postamble `shot_%03d % (len(acts) + 1)` (`:911`). Unnormalized `[1,2,4]` or `[5,6]` collides with or disorders `shot_003`. Plan says normalize 1..N before acceptance. Concrete: rewrite `ActPlan.n` to `1..len(acts)` (keep originals in `meta.my_story`), drive `_pass_act` / `_make_act_validator` / `_assemble` from that sequence, never from widget 1..6. Do not call `compute_episode_budget` on actual count (`nodes/_otr_episode_budget.py:238-242` raises outside 1..6); My Story does not go through that outline table.

3. [A.1] Interpret still rejects on predicted counts before allocation. `_make_interpret_validator` raises `MyStoryCastError` when `expected > voice_capacity` (`nodes/_otr_my_story.py:463-470`) and repairs `cast_plan.planned != expected` (`:471-477`). `_make_treatment_validator` then requires `len(names) == planned` (`:536-538`). Removing only the act-count check leaves the same class of gate. Fix: keep unique nonempty names as identity rules; drop planned-count and pre-allocation stock math; let `_assign_voices` (`:766-772`) fail only when the live pool is empty. Do not consolidate people or reuse voices.

4. [A.1 / tests] `_make_act_validator` still requires two speakers unless solo (`:615-616`). That blocks the stated monologue case. Fix: require nonempty lines and in-cast speakers only; last-act unheard-speaker rule can stay (`:617-623`).

5. [A.6 vs A.1] "Pass an absent title override as None" contradicts the live type and the tail. `MyStoryTailParts.final_title_override` is `str` (`nodes/_otr_my_story.py:312,1171`); the tail treats any non-None override as the authored title and skips regen (`nodes/_otr_writer_tail.py:735-740`). `StoryTreatment.title` is `min_length=1` (`nodes/_otr_my_story.py:254`). Passing `None` when a treatment title exists runs `_generate_title_from_script` and discards the accepted title. Predicate: pass `treatment.title` whenever the treatment was accepted; pass `None` only if that field is truly absent. Do not pass `""`.

6. [A.6] Attribution insert vs authorship seal. `_make_frame_validator` currently fails if the sentence is missing (`:695-699`). Deterministic insert is `attribution_sentence` in `nodes/_otr_story_input.py:416-426`. `stamp_receipt` runs at the end of `_assemble` (`nodes/_otr_my_story.py:946-954`). Insert into intro/outro BEFORE `stamp_receipt`. Insert after it and `content_owned_readonly` freeze (`nodes/_otr_freeze_cascade.py:199-204`, pack `nodes/story_packs/my_story/my_story.json:22`) will treat the extra words as mutation.

7. [A.1 / C gender] Binary gender is still a treatment hard gate. `CastMember.gender` defaults to `"female"` (`nodes/_otr_my_story.py:211`) and the post-validator demands `male|female` (`:547-550`). `EnsembleSlot` already allows `other` (`nodes/_otr_casting.py:149,547-554`); `python_assign_voice_preset` already draws `other` from the full pool (`:935-945`). Fix: drop the binary post-validator; default unspecified to `""`/`other`, never `"female"`; keep honouring a stated male/female. Prompt in `my_story_treatment_system` still says `"gender": "male" or "female"` (`my_story.json:10`) -- change it in the same chunk or the ladder will keep "repairing" unknown to binary.

8. [A.5] A2 has no data shape for three different numbers. `_read_advertised_context` reads only top-level `max_position_embeddings|n_positions|n_ctx` (`nodes/_otr_model_catalog.py:689-707`), not `text_config`. `resolve_context_cap` then `min(parsed, HARD_VRAM_CONTEXT_LIMIT)` (`:1918-1923`); curated Qwen is pinned 8192 (`:1828`). `request_slot` prices VRAM with that cap before load (`nodes/_otr_model_loader.py:1908-1914,2018`) and GGUF KV scales with `context_cap` (`nodes/_otr_model_catalog.py:2034-2035`). Feeding 262144 into `resolve_context_cap.value` is the pre-load veto the plan forbids. Name three fields: `advertised_context` (nested config, message only), `allocated_n_ctx` / `context_cap` (what load_llm/GGUF actually get; keep operator `gguf_n_ctx` / `OTR_HARD_VRAM_CONTEXT_LIMIT`), `vram_priced_ctx` (= allocated, never advertised). Put allocated_n_ctx in GGUF reuse identity; do not put advertised 262144 there. verify: on-disk Qwen `config.json` / `text_config.max_position_embeddings` is 262144 as claimed.

9. [A.5] `_call` still wraps every prompt-no-room as `MyStoryInputTooLongError` blaming `_longest_field(bundle)` (`nodes/_otr_my_story.py:409-437`). Treatment prompts dump the whole interpretation JSON (`:577-578`), so the listener field may not be the overflow. Shared owners are `resolve_context_cap`, `_otr_model_loader`, `_otr_generation_budget`. Fix the shared message to report measured prompt tokens, `allocated_n_ctx`, and failure phase (`prompt_no_room` vs native OOM). Keep `MyStoryInputTooLongError` only as a type alias if tests need it; stop naming a field. `tests/test_my_story_runner.py:551-558` pins the old blame string -- rewrite it in the same chunk.

10. [B] Original credit cannot be appended beside telemetry without overwriting the early bank default. `nodes/story_packs/banks.json:53` already sets `credits_source_line` to the generic machine line; writer stamps it unconditionally at `OTR_LedgerScriptWriter.py:3478-3483` before any Original call. Late write that only fills an empty key will never run. Original is inline `original_multi_pass` (`nodes/_otr_lane_specs.py:90`), not a dispatched runner, so `tail_finalizer` is None. Fix: after `_stamp_final_slot_telemetry` (`nodes/_otr_writer_tail.py:1527-1534`) and before `json.dumps` (`:1541`), if `source_bank == original_radio` (or equivalent bank id), overwrite `meta["credits_source_line"]` with `Story generation models used: <identities>`. Do not touch My Story's later override (`:3493-3498`) or provenance-filled adaptation banks.

11. [B] OpenRouter executed identity is not on `cache_entry`. `_record_resolved` runs in `_extract_text` before the no-choices check (`nodes/_otr_openrouter_backend.py:1572-1578`) against process-global `_resolved_models` (`:669-698`). Snapshot is copied to `meta["resolved_models"]` at terminal save (`nodes/_otr_writer_tail.py:1584-1591`). That matches the plan's "missing model preserves a prior identity / extract before validation / fallback changes slug" defects. `for_slot` also increments `calls_by_slot` before `base()` (`OTR_LedgerScriptWriter.py:720-733,635`). Fix: per successful return only, append JSON primitives `{helper, slot, provider, requested, executed, reported: bool}` onto an episode-local list on the scheduler or ledger; OpenRouter must stash the response `model` on that call (contextvar or return sidecar), not `resolved_models_snapshot()`. On exception, do not append. Fallback: record the successful fallback slug, not the gone one. Local: `cache_entry["model_id"]`. Comfy Credits: `cache_entry["slug"]` (`nodes/_otr_comfy_backend.py:457-459`). Google: `cache_entry["google_model"]` (`nodes/_otr_google_api/llm.py:178`). Dropdown `label` in `nodes/_otr_model_catalog.py:906` is a truthful display label; use it only after `_strip_label_suffix` / identity match, else the concrete id. Unknown: `model identity unreported`.

12. [B] Creative vs finishing allowlist is unnamed. Original still runs outline + line composer on the inline path, then shared `ledger_clean` / `ledger_cleanup` / optional `repair_zero_coverage_cast` (`nodes/_otr_writer_tail.py:1388-1420`). Printing every successful helper will credit cleanup. Fix an allowlist: Original front `original_concept|original_select|original_brief` plus outline/cast/composer/title helpers; exclude `ledger_clean*`, `ledger_cleanup`, `cast_coverage_repair`. Finishing identities go in a separate meta record, not `credits_source_line`.

SHOULD-FIX:
1. [A.2] `_make_treatment_validator` still requires `act.n == 1..requested` (`nodes/_otr_my_story.py:555-560`) and D1 still specifies `len(acts) == act_count` and "Story size is act_count 1..6 only" (`docs/2026-09-10-my-story-d1-design.md:410,560`). Same-commit: validator + `my_story_treatment_system` ("Produce exactly the number of acts requested") + D1 fingerprint. Leaving the prompt in place makes the 3-attempt ladder fight the new policy.

2. [A.6] Blank music: `_make_frame_validator` rejects any blank cue (`:690-693`). `StableAudioTheme._resolve_cue_specs` already composes when `generation_prompt`/`description` is empty (`nodes/stable_audio_theme.py:319-331`). Allow blank strings; still emit `music[]` rows; record unused proposals + disposition on `meta.my_story`. Freeze stores null-able prompts (`production_ledger.py:1639-1640`). Component freeze-62 will not execute StableAudioTheme; do not claim theme composition in that receipt.

3. [A / freeze] Dry-run success is not `/history` ok. `phase_10_gap_audit_post_and_freeze` raises `FreezeAssertionError` on errors after stamping `needs_full_rerun` (`nodes/_otr_ledger_freeze.py:1045-1048`). `OTR_LedgerFreezeCascade.py:242-265` can return `needs_full_rerun` without a node exception. Proof must read `meta.freeze_verdict` in `{frozen_clean, frozen_with_warns}` plus nonempty tables. Do not treat `needs_full_rerun` as a ledger.

4. [C] Hero wrap must not call `_fit_text` (`nodes/otr_credits_roll.py:660-682`, ellipsis) or `_wrap` unchanged (`:697-708`, `or not cur` keeps one overflowing token). `_flow_col1` currently autoshrinks then `d.text` one line and `y += _fh(fh)` (`:943-948`). `_fh` is `int(font.size * 1.28)` (`:685-686`). After wrap: same `_flow_col1` for scratch and paint; `y += n_lines * _fh(fh)` (or identical multiline metric); never `ImageDraw.multiline_text` unless measurement uses it too. Column width is `int(_COL1_W * sx)` with `_COL1_W=598`, `_REF_H=1080` (`:86-88`). Token split: glyph loop via existing `_fw`/`textlength`. Abridgment `_LEDGER_DROP_ORDER = ("FRAMES:", "VRAM:", "REV:")` already preserves SEED/COMMIT (`:827-862`); taller hero should fall out of `_draw_col1` if `_flow_col1` height is honest.

5. [B] Do not reuse `meta["resolved_models"]` or `video_engine.py:1617` snapshot for the spoken/printed Original line. Leaving the snapshot for OpenRouter cost telemetry is fine; credit must be the new journal.

6. [A.4] `invoke_structured_slot` never calls `_otr_bind_schema` (`nodes/_otr_structured_call.py:596-645`). The binder exists only on local scheduler closures (`OTR_LedgerScriptWriter.py:737-742`). Keep it off. verify: installed `lmformatenforcer.JsonSchemaParser` default max array length is 20; that number is not in this repo.

OPTIONAL / NICE-TO-HAVE:
- Character-split helper later reused by `video_engine._wrap_words` (`nodes/video_engine.py:952-954`); out of Sprint C.
- Treatment `max_new_tokens=1800` already bounds wild act lists; no extra max required.
- verify: `partial_execution_targets: ["62"]` against the ComfyUI version on this box (plan's live prompt id is evidence it worked once).

CUT THESE:
1. Turning on `_otr_bind_schema` / LMFE cardinality -- already forbidden; `structured_call` is parse-then-validate.
2. A second My Story repair ladder -- shared tail already opens `_otr_clean_transaction`, `run_ledger_clean`, `run_ledger_cleanup` (`nodes/_otr_writer_tail.py:1383-1404`). My Story pack has no `line_composer_system`, so freeze is `content_owned_readonly` and skips generic cast-coverage repair (`:1417-1418`). Do not add another unsealed pass.
3. Calling `compute_episode_budget` / `ACT_COUNT_CONFIG` to "support" extra acts -- that table is the other banks' 4-beats-per-act outline; My Story emits one scene per act (`_assemble:868-878`).
4. Invented model-name maps -- catalog `label` already exists.
5. Two-line ellipsis / title rewrite -- forbidden; `_fit_text` would be that policy by accident.

[ASSUMPTION] Canonical 23 nodes / last_link_id 291 / writer `widgets_values` length 37 / `llm_device` `mps` at node 1 / link 291 on writer `script_json` to mux 85 match `workflows/otr_canonical.json` (node ids 63,1,3,4,7,12,62,80-95; widgets_values `:541-578`).
