# R3 driver anchor - Codex

VERDICT: YES WITH SPECIFIC WIRING/BOUNDARY VERIFICATION.

CONFIRMED latest operator bans artificial story rejection, accepts real OOM,
GO_FORWARD main plan. Root rejects reinstated caps or truncation. Prior rounds
already confirmed actual act boundaries and same-call identity; focus on NEW
wiring/implementation defects, not restating existing missing pre-build code.

MUST CLOSE:
1. Shared capacity unknown/remote-estimate behavior must be explicit before A2
   code. Native HF context is not an allocation, GGUF n_ctx is. Keep explicit
   runtime limits; no new guessed8192 or minimum-model-window rejection. Current
   HF estimator is weights-only (catalog2049), so raw native262144 does NOT itself
   add KV as reviewers assumed; don't rewrite that subsystem. Audit all factories.
2. MyStory reuses ProviderCapacityMessages+None (SciFi existing path); schema
   injection and typed repair preserve markers in structured_call510-578/1090.
   No hidden fixed output caps or broad local schema binder. No new widgets.
3. A1 must normalize bookkeeping before content seal, preserve raw receipt
   numbers, use actual boundary cue mapping and keep request metadata separate.
4. B keeps str return and invocation-local primitive sidecar through successful
   OpenRouter fallback, journal after cloud runaway check; no retained model refs.
   Exact scheduler helper sets and bank id original are now in main plan.
5. C uses one measured _flow_col1 for scratch and paint; actual whole title
   remains. Existing marked row abridgment is allowed; no title truncation.

SHOULD VERIFY: no canonical shape change needed; 23nodes/63links, writer37widgets,
link291 writer1 script_json to mux85input10, component target62 full prompt.
Do not add native App/media qualification to this coding scope or archive pending
older work. Main plan remains GO_FORWARD; review final is only a snapshot.

Read real Windows files, no code edits except review artifact; no model/GPU
runs. Root sole coder/judge. Profiles disabled because stale overlay. Identify
exact evidence if any plan claim cannot be verified rather than inventing it.
