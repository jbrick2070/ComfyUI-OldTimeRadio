VERDICT: yes-with-fixes. Architecture (no new nodes; reuse Sci-Fi ProviderCapacityMessages + shared tail/freeze) is sound, but A1's 1-request/2-act path still has a wrong producer-consumer order on act topology, several still-live My Story gates, and B's credit stamp sits after model unload unless the journal is primitive at call time.

MUST-FIX BEFORE BUILD:

1. [A.2 / A1 P1->P3->P5] Sequence defect: `inter_wanted` is computed from requested widget `act_count`, not accepted treatment topology. In `nodes/_otr_my_story.py` the treatment validator still rejects `len(model.acts) != act_count` (line 555-557); even after that gate is removed, P3 still does `inter_wanted = (act_count - 1) if (include_act_breaks and act_count > 1) else 0` (line 1118) using `resolved["act_count"]` (line 1003). Frame validator then requires `len(model.music_inter) == inter_wanted` (700-702). `_assemble` only emits interstitials when `inter_seq < len(frame.music_inter)` (897). Requested=1 / actual=2 / breaks on therefore accepts two acts, asks the frame for 0 cues, and emits 0 boundaries. That is the live e11e5d86 failure mode after the count gate is lifted, and it fails the plan's "actual cue topology" proof. Fix: after treatment acceptance, set `actual_acts = len(treatment.acts)`, derive `inter_wanted` from that, then call `_pass_frame`. Pass `include_act_breaks` into `_assemble` and emit one uniquely anchored cue per actual boundary (empty prompt if the ordered frame list is short). Record extras in `music_cue_disposition`. Do not keep `frame.music_inter` length as topology authority.

2. [A.2 act-number normalize vs assemble] The plan says both "normalize before acceptance" and "normalize ... act replies before sealing". Those are different clocks. `_assemble` uses `scene_id = "s%02d" % act.n` and `shot_id = "shot_%03d" % act.n` (871-872); postamble is `shot_%03d % (len(acts)+1)` (911). If original numbers are 1 and 3, shot_003 collides with the postamble. `_make_act_validator` also keys on `plan.n` (604-605). Fix: immediately after treatment acceptance, record originals in `meta.my_story.act_number_normalization`, rewrite `treatment.acts[].n` to 1..N, then generate acts and assemble against the normalized numbers. Do not wait until freeze/seal.

3. [A.1 remaining gates still fire before assembly] Removing only the act-count check does not reach assembly. Still live, and the plan already names them as non-requirements: (a) `_make_act_validator` rejects non-solo acts with `< 2` speakers (615-616) while the plan allows monologues; (b) `_make_treatment_validator` requires `gender in ("male","female")` (547-550); (c) `_make_interpret_validator` raises `MyStoryCastError` when `expected > voice_capacity` (463-470) before P4 `_assign_voices` (767-772), which is the actual empty-stock failure. Fix in the same A1 diff: keep last-act unheard repair (617-623, 1105-1114); delete the two-speaker rule; drop predicted pool-size refusal; let P4 raise only when `open_voice_pool` is actually empty. Do not cycle voices.

4. [A.1 schema defaults vs "never female by default"] `CastMember.gender = "female"` (`nodes/_otr_my_story.py` 211). Pydantic fills a missing JSON key before the post-validator. Relaxing the binary check without changing this default silently casts unspecified people as female. `canonical_bank_gender` already passes empty through (`nodes/_otr_roster_gender.py` 200-219). `python_assign_voice_preset` already treats non male/female as the full pool (932-945). Fix: default gender to `""` (or `"other"`), keep empty through validators, and update `nodes/story_packs/my_story/my_story.json` `my_story_treatment_system` which still says `"gender": "male" or "female"`.

5. [A.1 frame seal vs authorship receipt] Plan: drop verbatim-credit veto, append `attribution_sentence` to outro when absent, then seal. `_make_frame_validator` currently requires the sentence in intro/outro (694-699) and nonempty intro/outro/coda/music (690-693). `StoryFrame` has `min_length=1` on intro, outro, coda, music_open, music_close (286-291). `_assemble` then `stamp_receipt(..., accepted_artifacts={"frame": frame.model_dump(...)})` (946-954). If you append to ledger lines after `stamp_receipt`, freeze `content_owned_readonly` (`nodes/_otr_freeze_cascade.py` 199-204) will see text not in the artifact. If you append to `frame` after stamp, same miss. Fix: (1) allow blank optional frame strings at schema+validator; (2) mutate `frame` (append attribution to outro if missing) BEFORE `_assemble`; (3) skip blank spoken rows in `_assemble`; (4) `stamp_receipt` on that adjusted frame. Shared tail still runs `run_ledger_clean` / `run_ledger_cleanup` (`nodes/_otr_writer_tail.py` 1383-1404). `my_story` is not in `FIDELITY_BANKS` (`nodes/_otr_spoken_text_policy.py` 83), so clean may rewrite the outro. My Story already `stamp_receipt`s (943-954); `open_transaction` reseals via that meta receipt when `tail_finalizer` is None (`nodes/_otr_clean_transaction.py` 31-36). Do not add a second generic end-repair. Verify: clean policy does not drop the appended attribution without reseal.

6. [A.1 `final_title_override` None vs `""`] `MyStoryTailParts.final_title_override: str` (312) always set to `treatment.title` (1171). Tail uses `elif ctx.final_title_override is not None` (`nodes/_otr_writer_tail.py` 735-739), so `""` skips the shared title producer. Fix: type it `str | None`; pass `None` only when blank; keep a nonblank accepted title. Writer already handles `None` as the regen path.

7. [B stamp clock vs unload] Plan: stamp `credits_source_line` beside final slot telemetry, after calls, before lane finalizer / `script_json`. Real order in `_run_writer_tail`: last LLM (`cast_coverage_repair`) -> `unload_writer_llm_after_script` (1432-1456) -> `_stamp_final_slot_telemetry` (1527-1534) -> `tail_finalizer.before_save` (1538-1539) -> `json.dumps` (1541). Cache/model objects are already gone at telemetry. `_SlotScheduler.for_slot` builds a fresh `base = _build_truncating_generate_fn(...)` per call and `return base(...)` (`OTR_LedgerScriptWriter.py` 715-733) with no retained receipt. Fix: on every successful `base(...)` return, append a primitive dict to a per-run `successful_model_calls` using `scheduler._current_helper` (not `structured_call.helper_name`). Stamp from that list at `_stamp_final_slot_telemetry`, overwrite `meta["credits_source_line"]` only when `meta.source_bank == "original"` (bank id in `nodes/story_packs/banks.json` 37 is `original`, not `original_radio`). Do not read `cache_entry` or `resolved_models_snapshot()` (`nodes/_otr_openrouter_backend.py` 694) at stamp time.

8. [B OpenRouter receipt vs fallback slug] `OpenRouterBackend.generate` retries/fallback inside `_post_with_retries`; model-gone swaps `payload["model"] = fb_clean` and posts again (1379-1404) then `return text` (1408). `_extract_text` runs on the successful 200 (1486-1489). `make_openrouter_generate_fn` then runs `assert_no_verbatim_cycle` (1696-1707). If `receipt_out` is filled from the outer `slug` / `cache_entry` at extract of the first attempt, executed identity is the gone model. Fix: write `receipt_out` only after extract AND runaway check succeed, with executed = the slug actually posted on the successful attempt (`fb_clean` after fallback). Scheduler copies primitives off that closure immediately; do not keep the dict as a process-global. Preserve `generate_fn(...) -> str`.

SHOULD-FIX:

1. [A.5 / `_call` vs A2 split] A1 still wraps `prompt_no_room` as `MyStoryInputTooLongError` and names `_longest_field(bundle)` (`nodes/_otr_my_story.py` 409-437, 420-437). Operator contract forbids predictive story-size rejection; this is a My Story message, not shared catalog honesty. A2 owns `_read_advertised_context` (catalog 689-704: top-level `max_position_embeddings` only, no nested `text_config`) and loader mutation `model_config.max_position_embeddings = _cap` (`nodes/_otr_model_loader.py` 1227-1229) plus default `HARD_VRAM_CONTEXT_LIMIT` 8192 (catalog 1802-1812). Do not leave A1 shipping "shorten '%s'" while claiming A2 will fix blame later. Smallest A1 change: re-raise the shared `PromptContextOverflowError` (or wrap without naming a field / "shorten it"). Keep A2 as the catalog/n_ctx/reuse_key chunk; do not let advertised 262144 become a pre-load VRAM veto (`_assert_policy_admits_vram` still prices the request at 2018-2019).

2. [A.1 ProviderCapacityMessages + `max_new_tokens=None`] Copy Sci-Fi exactly: wrap the list in `ProviderCapacityMessages` and pass `max_new_tokens=None` (`nodes/_otr_scifi_news_pro.py` 1915-1926). Do not pass the old 1200/1800/1600/700 with the wrapper; local transformers uses `context_cap if reserve_remaining else max(1, int(max_new_tokens))` (`OTR_LedgerScriptWriter.py` 883-885) so None without the marker TypeErrors. Repair already rewraps via `_inherit_generation_contract` (`nodes/_otr_structured_call.py` 545-555, 1090-1091). GGUF already does `int(max_new_tokens or cap)` (`nodes/_otr_gguf_backend.py` 1381) and honors `_otr_fail_on_output_limit` (1369-1617). Output-limit is not a structured-call recoverable error; that is existing Sci-Fi behavior, not a My Story repair hole to fill.

3. [A.1 / tail `gen_params_initial.act_count`] `_stamp` in the tail overwrites `meta["gen_params_initial"]["act_count"]` from `resolved["act_count"]` (`nodes/_otr_writer_tail.py` 852-865). Plan: that field stays request evidence, never delivered topology. Freeze/video/credits consumers that still treat it as delivered act count will disagree with ledger scenes. Audit readers of `gen_params_initial.act_count` (writer_tail 852, credits 299, video_engine 1558) and point topology at actual scene/shot rows / `meta.my_story.counts.actual_acts`. My Story already skips `compute_episode_budget` (that call is on the inline path at `OTR_LedgerScriptWriter.py` 4282, after dispatched return at 3679). Do not start calling it for My Story.

4. [B helper names] Original concept/select run under `helper_context("build_news_briefs")` (`OTR_LedgerScriptWriter.py` 1684, 3744) while `structured_call(..., helper_name="original_concept"|"original_select")` (`nodes/_otr_original_radio.py` 283-300). Journal helper must be `_current_helper`. `generate_title` is in the tail (writer_tail 807), after the inline compose loop (`compose_announcer_intro` 5325, `announcer_intro_rewrite` 5547). Collect through the last generation helper before classifying finishing (`ledger_clean` 1388, `ledger_cleanup` 1396, `cast_coverage_repair`). Do not credit `story_brief_reflection` / `produced_story_summary` as story creation.

5. [B local/Comfy/Google identity] Plan only adds `receipt_out` on OpenRouter. Local `model_id`, Comfy Credits slug, Google `google_model` must be copied from `cache_entry` in the same `for_slot` success path before unload. `openrouter_meta_for` / `openrouter_run_meta` (writer_tail 1282-1292) stay aggregate telemetry, not `generation_models`.

6. [C `_flow_col1` vs `_wrap`] Hero is `layout["hero"] = title.upper()` (otr_credits_roll.py 579), autoshrink then single `d.text` (943-948). `_wrap` keeps an overlong first token (`or not cur`, 697-708). `_draw_col1` / `_draw_col1_abridged` already measure and paint through `_flow_col1` (846-912), so wrapping inside `_flow_col1` with `y += _fh(fh)` per line is the right single contract. Do not reuse `_wrap` or `_fit_text` (truncation). Split overlong tokens at glyph width `int(_COL1_W * sx)` (`_COL1_W = 598`, line 88). Keep abridgment of FRAMES/VRAM/REV only (`_LEDGER_DROP_ORDER` 862).

OPTIONAL / NICE-TO-HAVE:

- Record `budget_mode=provider_capacity` on My Story pass receipts (currently `max_new_tokens` integers at 1017-1021, 1054-1074).
- A2: parse nested `text_config.max_position_embeddings` in `_read_advertised_context`; store `advertised_context` vs `vram_priced_ctx` vs allocated GGUF `n_ctx` as three fields. Keep `OTR_HARD_VRAM_CONTEXT_LIMIT` as an explicit user pin only.
- Confirm freeze node 62 / writer 1 / mux 85 / link 291 (`workflows/otr_canonical.json` last_link_id 291, ids 63/1/62/85). Dry-run `partial_execution_targets: ["62"]` is the right target; `/history` success is not `meta.freeze_verdict == "frozen_clean"` (`nodes/_otr_freeze_cascade.py` 777, 1110-1127).

CUT THESE (over-engineering):

1. Turning on local `_otr_bind_schema` / LMFE for My Story. Scheduler documents that binder is local-transformers only (`OTR_LedgerScriptWriter.py` 696-742); structured_call does not invoke it. Plan already forbids it (undeclared array cap). Safe to leave off.

2. A new My Story repair ladder or extra pass after P2. Last-act typed repair + existing `stamp_receipt` + clean transaction already cover ledger-shape defects. A bolt-on unsealed repair is what the plan forbids.

3. Reusing video title-card wrap or credits `_wrap` for the hero. Different overflow policy; copying either reintroduces the Lantern overflow.

4. New widgets/nodes/wiring. Canonical already has the writer->freeze path. A1-C are runner/catalog/credits-draw changes.

5. Rewriting GGUF `reuse_key` (already includes allocated `n_ctx`, loader 2027-2033). Plan says do not rewrite it. HF key change is A2 only.

[ASSUMPTION] Live prompt `e11e5d86-...` treatment was schema-valid except act count; other P1 gates were not the stop. If that artifact also violated two-speaker/gender/cast-plan.planned, those gates would still block A1 after only the count check is removed -- verify against `2026-09-10-my-story-live/` before treating #3 as optional.

[ASSUMPTION] LMFE silent `maxItems=20` on unbounded arrays was not found in-repo; treat as an installed-package behavior. Still do not enable the binder.
