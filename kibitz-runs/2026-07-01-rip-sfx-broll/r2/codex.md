VERDICT: no. The plan contradicts itself on `sfx_cue`, splits coupled removals across phases, and under-specifies the replacement data model after deleting two video roles.

MUST-FIX BEFORE BUILD:
1. [Codex's "keep sfx_cue" -- OVERRULED / Resolved decisions Q1 / Invariants] `sfx_cue` is both ordered removed and ordered kept. The removal list names `nodes/_otr_outline.py:120`, `nodes/_otr_line_composer.py:848`, `nodes/production_ledger.py:806`, etc.; later Q1 says "DO NOT remove `sfx_cue`", and Invariants says prove `SOUND IN THE ROOM` still renders. Current tests also expect it: `tests/test_phase1_composer_prompt.py:367`, `tests/test_phase2b_progressive_ledger.py:135`, `tests/test_s1_music_suppression.py:138`. Concrete fix: choose one contract. If keeping, delete the whole "RIP `sfx_cue` OUT TOO" removal list and keep allowed grep/tests. If removing, delete the keep invariant and explicitly update all `sfx_cue` tests and ledger/text behavior.

2. [Phase order P1/P2] P1 removes `SPEAKER_TO_VIDEO_ROLE["sfx"]` before P2 removes `sfx` as a valid speaker role. That leaves a phase where `sfx` is still valid in `nodes/_otr_speaker_role.py:57-66` and `_otr_outline.py:551`, but cannot route in `nodes/otr_shot_lock.py:55-83` once fail-loud is added. Concrete fix: make speaker-role removal and its video-route removal one atomic phase/commit, or keep the `sfx` route until the writer/outline/parser contract rejects it.

3. [Resolved decisions Q2 / Phase order P1] "Fail-loud fallback" is not implementable as written because the tolerant fallbacks are not enumerated. Current code silently maps unknowns in `nodes/_otr_speaker_role.py:104-127`, mutates invalid roles to character in `nodes/_otr_speaker_role.py:174-185`, maps unknown video roles to `LEGACY_OTHER_BEATS_SLOT` in `nodes/_otr_shared/role_slots.py:91-98`, and maps unknown speaker roles to `_DEFAULT_VIDEO_ROLE` in `nodes/otr_shot_lock.py:81-83` and `nodes/otr_meta_brief_image_prompt.py:483-485`. Concrete fix: specify exact new behavior and touched functions: `resolve_speaker_role`, `stamp_default_role`, `_video_role_for_line`, `slot_for_role`, `engine_id_for_role`, and `derive_scene_still_targets` must raise or explicitly legacy-normalize only at a named migration boundary.

4. [Final scope #2/#3] Removing `scene_broll` and `background_abstract` deletes two role tokens but does not define the new role model. Current shared role schema is five-role based in `nodes/_otr_shared/role_compat.py:34-72`, slot routing is five/legacy-role based in `nodes/_otr_shared/role_slots.py:33-80`, soak/profile builders are five-role based in `nodes/_otr_shared/slot_matrix.py:26-35`, and director aspects emit five role keys in `nodes/otr_video_director.py:423-429`. Concrete fix: define the exact remaining `Role` enum, `ROLE_AVAILABLE_INPUTS`, `ROLE_TO_VIDEO_SLOT`, `VIDEO_SLOT_ROLES`, policy `aspects` keys, and profile keys after removal.

5. [Final scope #2] The engine registry surface is under-scoped. The plan names `cheap_families.py` but not other registered adapters and render/soak helpers that still carry the deleted roles: `nodes/_otr_video_engines/eng_ltx_video.py:274-275`, `eng_still_parallax.py:178`, `eng_viz_mandala.py:54`, `eng_viz_rainbow.py:43`, `eng_wan_i2v.py:85`, and `nodes/_otr_video_engines/render_driver.py:92-100`. Concrete fix: add every adapter/helper role tuple and any related tests to the removal list, or state that engine `roles` metadata may retain historical strings and adjust forbidden grep accordingly.

6. [Final scope #3 / Workflow JSON] Removing `other_beats_clip_mode` and `other_beats_n` is a mid-list widget deletion, not a tail truncation. They are required widgets at `nodes/otr_video_director.py:232-247`, before `fps/canvas/seed`, and in workflow node 87 widget values at positions 6-7 in `workflows/otr_scifi_16gb_full.json`. The workflow section only mentions dropping tail widgets 17/18. Concrete fix: add an explicit positional workflow rewrite for node 87 inputs and `widgets_values` removing indices 6-7, then re-audit every later widget by name.

7. [Final scope #3] Other-beats pooling removal misses policy consumers. `OTR_VideoDirector` emits `policy["other_beats"]` at `nodes/otr_video_director.py:382`; `OTR_ImageDirector` forwards it at `nodes/otr_image_director.py:381-386`; `otr_meta_brief_image_prompt.py` parses it at `309-317` and passes it at `1301-1307`; `otr_shot_lock.py` reports budget fields at `956-967`. Concrete fix: either keep the policy key as a no-op for backward compatibility, or remove it from every producer/consumer/report/test in the same phase.

SHOULD-FIX:
1. [P4 tests] "delete targeted test fns" is too small. There are broad tests hard-coding `scene_broll`/`background_abstract`, including `tests/test_route_a_14b_promotion.py:33-75`, `tests/test_slot_matrix_soak.py:45-54`, `tests/test_video_role_compat_additive.py:48-69`, and `tests/test_still_spine_helpers.py:330-338`. Concrete fix: add a grep-driven test inventory step and classify each as delete, rewrite to remaining roles, or keep as historical parser migration.

2. [CONFIG] "rename to a character fallback or drop" is ambiguous for `other_beats_image`. `config/profiles/widget_mapping.json:37-39` still targets `OTR_VideoDirector.other_beats_image_model`, and `nodes/otr_image_gen_dispatcher.py:151-173` defaults non-announcer/music image roles to that slot. Concrete fix: define whether this slot remains as the character image slot, is renamed, or is removed; then update mapping, workflow, dispatcher, and profiles consistently.

3. [Residual verify-at-build] "Confirm no engine's roles/default_roles becomes EMPTY" needs to be an implementation step, not residual. `nodes/_otr_video_engines/cheap_families.py:180-181` has `StillMotionFamily.default_roles = ("scene_broll",)`, so deleting the role without changing the default creates stale default metadata. Concrete fix: specify the new `default_roles` value or empty it intentionally with a registry test.

OPTIONAL / NICE-TO-HAVE:
- [Verification] Add a migration fixture with an old ledger containing `speaker_role:"sfx"` and assert the chosen boundary rejects it loudly, because current `resolve_speaker_role` defaults invalid values to character.

CUT THESE (over-engineering):
1. [Phase order] Cut the four separate commits for P1/P2/P3 where the data model is coupled. The speaker role, video role map, workflow widgets, and policy schema need one atomic buildable change; splitting creates intermediate red states.
2. [Invariants] Cut "audio byte-identical" unless `sfx_cue` is kept and the outline prompt is unchanged. If the schema/prompt removes `sfx_cue` or `sfx`, fresh LLM output is not byte-identical by contract; use fixed no-sfx ledger audio regression only.