VERDICT: yes-with-fixes. Core code path is implementable, but the plan misses profile/applier surfaces and does not make the removed fallback fail loud enough.

MUST-FIX BEFORE BUILD:
1. [Code changes #4 / Tests to update] Profile mapping still targets the removed widget, so profile application/cross-validation will break after `other_beats_video_model` disappears. `config/profiles/widget_mapping.json:13-19` maps both `role_overrides.other_beats_visual -> OTR_VideoDirector.other_beats_video_model` and `role_overrides.character_visual -> character_video_model`; committed profiles still set `other_beats_visual` in `config/profiles/16gb_full.json:14`, `8gb_lite.json:14`, `cpu_floor.json:14`. Concrete fix: delete the `role_overrides.other_beats_visual` mapping and remove that key from all profiles; keep `role_overrides.character_visual -> character_video_model`. Update `tests/test_capability_profiles.py:280`, `355`, `360` and any profile cross-validation expectations.

2. [Code changes #1 / #2] Removing the fallback makes empty character resolution possible but not loud. `nodes/_otr_shared/role_slots.py:118-123` currently returns `""` after the dedicated slot is empty; `nodes/otr_shot_lock.py:713-720` stamps that directly into execution groups, and `nodes/_otr_shared/resolver.py:49-66` validates ids/deps but not `engine_id`. Concrete fix: after `OTRVideoDirector.direct()` resolves `video_models`, validate that every role in `ROLE_TO_VIDEO_SLOT` resolves to a non-empty engine id; raise `ValueError` naming the empty slot. Do not rely on ShotLock/resolver to catch it.

3. [Tests to update] The test list is incomplete. A repo search shows more fixtures still asserting/feeding the removed video slot: `tests/test_3d_image_streams.py:21,36,46,49`, `tests/test_image_platform_c1.py:270,288,334,355,373,383,484-497,547,587,613`, `tests/test_talking_portrait_s4b.py:55-56,73-74`, plus the listed direct-call tests. Concrete fix: update all character-lane video policy fixtures to `character_video_model`; keep `other_beats_image_model` only for image policy tests.

4. [OPEN #1] The image rename question is not build-ready as written. The image surface is not low-risk: `nodes/otr_image_director.py:148-151` maps `other_beats_image_model` to `character_video`, `nodes/otr_image_director.py:332-351` reads/emits it, and `nodes/otr_image_gen_dispatcher.py:151-173` hard-codes fallback behavior to that slot. Concrete fix: explicitly defer image-slot rename in this pass. If not deferred, add a full plan covering ImageDirector, dispatcher, workflow JSON, widget mapping, profiles, and tests.

5. [Code changes #4] The plan calls `nodes/_otr_workflow_apply.py:140` a “widget-order list”, but it is a `frozenset` admissibility whitelist, not serialization order. Actual order comes from `INPUT_TYPES` via `serialized_slot_names()` in `nodes/_otr_workflow_apply.py:185-207`. Concrete fix: change this instruction to “remove `other_beats_video_model` from `_VIDEO_DIRECTOR_WIDGETS`; `character_video_model` is already present.” Do not imply this edits widget order.

SHOULD-FIX:
1. [Code changes #2] Preserve the stated character default in code, not only in node 87’s saved JSON. `nodes/otr_video_director.py:209-220` required video combos currently have no default metadata; the old optional character combo default is the sentinel at `nodes/otr_video_director.py:259-260`. Concrete fix: give the new required `character_video_model` combo a default matching the full combo label for `humo_14B_169`, or add a helper that returns the correct labelled default.

2. [Code changes #2] Update stale UI/help text that will direct users to the removed key. `nodes/otr_video_director.py:243-250` still gives `{"other_beats_video_model": "my_engine"}` as the custom-model example. Concrete fix: change the example to `character_video_model`.

3. [Tests to update / Invariants] Smoke and soak helpers still patch/log the removed slot. `scripts/run_otr_30word_smoke.py:65-74` includes `other_beats_video_model`; `scripts/run_otr_30word_smoke.py:433-439` logs it; `scripts/_otr_cloud_desktop_probe.py:41-43` includes it; `scripts/_otr_nightly_anthology_soak.py:214-216` uses it as a fallback schema source. Concrete fix: remove the removed video slot and use `character_video_model` where the character lane is intended.

OPTIONAL / NICE-TO-HAVE:
- Update comments/docstrings that still describe the legacy migration lane after code removal: `nodes/otr_video_director.py:132-142`, `nodes/otr_video_director.py:252-258`, `nodes/_otr_shared/role_slots.py:20-24`.

CUT THESE (over-engineering):
1. [OPEN #1] Cut same-pass image-slot rename. It is safe to cut because the stated target UI keeps image trio separate, and current image code intentionally uses `other_beats_image_model` for character stills in `nodes/otr_image_director.py:148-151` and `nodes/otr_image_gen_dispatcher.py:162-173`.