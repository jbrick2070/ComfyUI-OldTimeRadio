VERDICT: no -- the document treats "stills exist before video" as an answer to a continuity-contract objection, and option B as written would pin a future beat's still onto every H3 clip, colliding with intra-beat chain and fear cape.

MUST-FIX BEFORE BUILD:
1. [goal + §6.B + §4] Goal/method diverge. The operator asked for beat-to-beat continuity ("end where the next begins"). Option B implements intra-clip FL2V interpolation between two independently minted stills. Those stills are one-per-beat scene images with different object ids/prompts/seeds (`otr_image_gen_dispatcher.py:800-812`; `coverage_plan.py:517-522`: "the image phase mints exactly ONE still per beat"). They were never composed as a matched pair. H3 will morph/warp from still N to still N+1 during beat N, which is not cinematic continuation and is exactly the capability `frame_contract.py:16-17` already refuses to call chaining ("Veo's lastFrame is first/last INTERPOLATION inside ONE clip, not chaining"). Concrete fix: pick one contract in writing -- (i) FL2V morph toward the next beat's opening still, last segment only, or (ii) true cross-beat chain via `extract_terminal_frame` (`render_driver.py:4870-4872`), which does not use `last_frame` at all. Do not keep both under the word "continuity."

2. [§6.B + §7.B + `render_beat_coverage`] Option B has no segment granularity. Intra-beat CHAIN successors begin on the predecessor's extracted terminal and own no still (`render_driver.py:1166-1178`, `coverage_plan.py:517-518`). Naive `last_frame = next beat still` on every H3 render does this: segment 0 locks its never-denoised last keyframe to beat N+1's still (`nodes_minimax_h3.py:5,146-148`); the extracted terminal is that lock; later segments then open on it. If they also get the same `last_frame`, first and last are the same image -- the held-frame case `render_driver.py:1216-1217` already forbids. The coverage planner will not see a failure: `can_chain` / `join_mode_for` never inspect `last_frame` (`frame_contract.py:320-327`, `coverage_plan.py:195-197`). Concrete fix: last_frame is legal only on the last segment of a beat (and on JOIN_SINGLE). Intermediate CHAIN segments stay first-frame-only, as today.

3. [§3 vs §2] Section 3 does not defeat the recorded objection. The objection is that last_frame is "a frame the coverage planner never chose" (`eng_minimax_h3.py:1060-1063`). Graph order 91-before-92 is true: node 91 `OTR_ImageGenDispatcher` `order` 15, node 92 `OTR_VideoRenderBatch` `order` 16, wired on `patched_ledger_json` link 260 and `image_done` link 267 (`workflows/otr_canonical.json`). That proves stills exist before video. It does not prove `coverage_plan.py` chose an interpolation endpoint for clip N. Coverage plans lengths and joins, not images. ShotLock + ImageGenDispatcher choose stills as beat N+1's opener, not as beat N's end. Concrete fix: reassign the owner in the objection -- ShotLock/request-builder stamps `asset_refs.last_frame` from the successor shot's scene still -- and stop citing node order as the answer.

4. [missing + §8] Fear cape is absent and collides with last-segment last_frame. On a long CHAIN beat, the frame fed to the final segment is inverted (`render_driver.py:4873-4891`). Pinning last_frame to the next still on that same segment interpolates inverted-current -> next-beat still. Concrete fix: last_frame is off whenever fear cape will fire on that beat, or last_frame is JOIN_SINGLE-only until that interaction is designed.

5. [§1 "one line" + §6.B] Wiring cost is not one line. A second keyframe needs a second staged image (`_conditioner_nodes` today only `LoadImage` + `first_frame`, `eng_minimax_h3.py:1067-1074`), planner-owned successor still selection, `accepts_last_frame` (only Veo sets it today: `eng_google_veo_video.py:544`; H3 does not), last-beat/engine/canvas gates, and a rewrite of `tests/test_minimax_h3_video.py:294-301` which asserts `"last_frame" not in h3_node`. `_prune` defaults missing `accepts_last_frame` to True (`render_driver.py:2137-2148,2167-2169`), so a planner that stuffs the ref without an H3 flag will not prune it. Concrete fix: name the owner (ShotLock or `render_beat_coverage` request builder, not the adapter), set `accepts_last_frame` explicitly on `MiniMaxH3VideoEngine`, stage last_frame through the same `_materialize_init_image` path as init (`wan_shared.py:568-606`).

6. [§7.F] False dichotomy. `h3_low_video` is the public id of `minimax_h3_video` (`public_engines.py:128`; `eng_minimax_h3.py:6-7`). One class. Lane 20 is `MiniMaxH3AudioInEngine` / `MiniMaxH3ReferenceToVideo`, no `first_frame`, `CONTINUITY_SOFT_REFERENCE` (`eng_minimax_h3.py:1137-1152`). Concrete fix: this change is that one FL2VA class only; say lane 20 stays untouched.

7. [§7.E] Last beat / successor engine / canvas cannot remain open if B is the build. VideoDirector picks per role (`otr_video_director.py:376-396`); consecutive shots can be different engines. H3 `required_inputs = ("init_image",)` (`eng_minimax_h3.py:1011`); a viz/soft successor may mint no scene still. Concrete fix: last_frame only when successor shot exists, same internal engine id, same canvas, and a materialized scene still; otherwise omit it (honest jump at the beat boundary). Last beat: omit.

SHOULD-FIX:
1. [§5] Asymmetry is real on the node (`nodes_minimax_h3.py:141-148`) and a no-op if both images are already canvas WxH via `_materialize_init_image` (default policy `pad`, `wan_shared.py:530-541`). It becomes a join bug only if last_frame is raw-loaded. Fix: mandate materialize on both sockets; drop "canvas-aspect precondition" as a separate gate unless materialize is skipped.

2. [§7.D] Do not add `strict_both_endpoints`. `can_chain` is intra-beat first-frame lock (`frame_contract.py:46-48,320-327`). Cross-beat last_frame is outside that planner. A new mode forces `coverage_plan.py` + every adapter for a join the partitioner does not model. Keep `CONTINUITY_STRICT_FIRST_FRAME` on lane 19; document last_frame as an optional last-segment conditioner, not a continuity mode.

3. [intro VRAM + §8] The 7.21/6.79 GiB ladder is first-frame-only (`docs/4060_DRILL_LOG.md:46`). last_frame adds a second never-denoised keyframe (`nodes_minimax_h3.py:5`). [ASSUMPTION] extra VRAM is small. Measure FL2V at the same 864x480 rung before calling the 4060 fit unchanged. Do not trade the recipe for memory if it does not fit.

4. [§1 install path] This box's node is at `C:\Users\jeffr\ComfyUI-Installs\ComfyUI\ComfyUI\comfy_extras\nodes_minimax_h3.py` (verified). The 4060 log names a different tree (`docs/4060_DRILL_LOG.md:13-14`). Feasibility is this install, not both boxes. State which box's ComfyUI was read.

5. [§8 recipe] Wiring last_frame changes the frozen graph (`tests/test_minimax_h3_video.py:307-314`, `_RECIPE_RECEIPT`). That is a capability change, not a speed/memory trade, but IS_CHANGED hashes the whole ledger (`otr_video_render_batch.py:411-433`) and will not see last_frame if it is derived only inside the adapter. Stamp the successor still path on the shot so same-seed replay is ledger-identical.

OPTIONAL / NICE-TO-HAVE:
- Live SSIM/PSNR at the beat join with last_frame on vs off (lane 19 already cites intra-beat SSIM 0.900 at `eng_minimax_h3.py:1034-1036`; that number is not a cross-beat FL2V measurement).
- Skip last_frame when consecutive still hashes are already near-identical (bookend clones).

CUT THESE:
1. [§6.C] MiniMaxH3AddGuide. It consumes an existing H3 AV latent + conditioning (`nodes_minimax_h3.py:162-192`), can take clip/audio, and is not in this lane's `_EXTRA_NODE_CANDIDATES` (`eng_minimax_h3.py:1013-1016`). For two endpoints, `last_frame` on `MiniMaxH3ImageToVideo` is the native socket. AddGuide is a recipe expansion, not a cheaper last_frame.
2. [§6.D] Setting/shot-family gating. No FL2V family classifier exists; `scene_id` on the ledger is not a motion-join signal. Building a "same setting" oracle is a new product. The engine/canvas/still gate in must-fix 7 is enough; a hard scene cut with last_frame omitted is an honest jump (`frame_contract.py:31-32`).
3. [§7.D] A new continuity mode. Safe to cut because intra-beat chain does not need it if last_frame is last-segment-only.
4. [§6.A as a "plan"] "Do nothing" is a valid operator call, not an engineering option to carry into r2. If the panel keeps B, drop A from the build spec.
