VERDICT: yes-with-fixes. Runtime graph wiring is mostly coherent, but the landed `[SYSTEM]` scroll block drops fields from the existing SYSTEM contract.

MUST-FIX BEFORE BUILD:
1. [F3 SYSTEM in the scroll] The scroll `[SYSTEM]` block does not carry “all the same details” as the repo’s existing SYSTEM contract. `nodes/video_engine.py:1150-1168` documents/emits Host, OS, CPU + cores, RAM + peak, GPU + VRAM, CUDA, torch, Python. The new `sys_grid` in `nodes/otr_credits_roll.py:280-288` emits Host+OS, CPU, RAM, GPU, CUDA/torch/Python, but drops `cpu_cores`, `ram_peak`, and `vram` from `[SYSTEM]`. Concrete fix: expand `sys_grid` to include CPU cores, RAM peak, and VRAM total, e.g. CPU value includes `sysd["cpu_cores"]`, RAM value includes `sysd["ram_peak"]`, GPU value includes `sysd["vram"]`. Add a test that monkeypatches `cr._sys_specs()` with sentinel `hostname`, `vram`, `cpu_cores`, and `ram_peak` and asserts those sentinels appear in `dict(lay["col3_flow"])["system"]`.

SHOULD-FIX:
1. [Panel-caught bugs fixed] The test suite does not actually lock the fixed `hostname` / `vram` key wiring. Code uses `sysd.get("hostname")` at `nodes/otr_credits_roll.py:281` and `sysd.get("vram")` at `nodes/otr_credits_roll.py:264`; the collector emits those keys at `nodes/_otr_sys_specs.py:185-191`. But `tests/test_credits_roll_spec.py:227-229` only asserts CPU/GPU labels, and `tests/test_credits_roll_spec.py:157-166` asserts render peak VRAM formatting, not total system VRAM or hostname. Concrete fix: add sentinel-key assertions as above so a regression back to `host` / `gpu_vram` fails.
2. [Configuration and environment propagation] `OTR_CreditsRoll` hard-resolves ffmpeg/ffprobe from PATH only in `nodes/otr_credits_roll.py:841-851`, while surrounding workflow nodes expose an `ffmpeg` widget/config path in `workflows/otr_scifi_16gb_full.json:1` for nodes 84, 85, 86, 93, and 94. This creates a hidden config dependency: credits can fail even when the rest of the video chain is configured. Concrete fix: smallest no-JSON change is to honor `OTR_FFMPEG` and `OTR_FFPROBE` env vars before `shutil.which`; fuller fix is optional node inputs plus same-change workflow JSON wiring.
3. [F3 SYSTEM in the scroll] Contract comments are stale and point future wiring work the wrong way. `nodes/otr_credits_roll.py:10-15` and `tests/test_credits_roll_spec.py:4-8` still say `[SYSTEM]` is in static columns 1+2; `nodes/otr_credits_roll.py:674-676` still describes the scroll as starting at STORY SPINE. Concrete fix: update those docstrings to `SYSTEM -> STORY SPINE -> TRANSCRIPT -> INTERCEPT -> DIAGNOSTIC`.

OPTIONAL / NICE-TO-HAVE:
1. [Invariants held] Workflow linkage checks out: node 95 receives node 93 video and node 92 manifest, then feeds node 85 video and declared tail via links 274/276 in `workflows/otr_scifi_16gb_full.json:1`. No additional gate input appears necessary.

CUT THESE (over-engineering):
1. None. The remaining fixes are small contract/config corrections, not new architecture.