# R4 driver anchor - Codex, latest operator count correction

VERDICT: updated A1 ready for residual review; latest operator says ALL stories
must obey selected act and character counts, variable per run. This supersedes
R1-R3's proposed count-drift acceptance. Do not recommend that rejected approach.
Two prior R4 calls were interrupted; new input is amended R3 final, old bytes
preserved. Root grounded this user-driven amendment before fresh fanout.

CONFIRMED: live requested1/model2 failed after generic typed repair. Existing
repair sees400 characters of failed treatment, before acts. No missing retry:
postvalidation intentionally skips same-prompt reroll and takes typed repair.
A1 should use its existing RepairPromptFactory seam with original messages +
complete failed treatment + binding selected counts/reorganization instruction.
Keep bounded shared ladder and postvalidation, no Python story deletion/merge,
no extra repair subsystem. Selected numeric character count overrides model's
cast_plan guess; historical named-count overrides are superseded. Count excludes
announcer. Full story material remains authority; disclose unresolved conflicts.

Other grounded A1 work still stands: provider-capacity wrapper+None, optional
metadata/gender defaults, monologue acts, shared clean_spoken_text coverage,
frame attribution before seal, actual accepted boundary cues, title parity,
shared clean transaction/freeze. No new widgets or duration/word gates.
A2 scoped known HF capacity, native config not allocation, explicit limits stay;
unknown remote accounting follow-up disclosed. A3 records actual cross-bank
count exceptions (SciFi ignores n_max, legacyclamps6 despitewidget10) as pending
separate reviewed alignment, not already solved by A1. Do not expand A1 blindly.
B response-local model identity journal and Original-only credit; C full measured
hero wrap; neither changed with latest count ruling. GO_FORWARD sole main plan.

Read real Windows files, write only review artifact, no code/GPU/model work.
Root sole judge/editor. Identify NEW implementability problems, not that planned
changes are absent from pre-build code. Check binding count repair specifically.
Do not demand a generic binder, more review rounds, or claim all banks qualified.
