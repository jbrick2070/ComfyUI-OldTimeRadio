VERDICT: no. The plan still leaves core branch contracts undecided and would preserve fallback/dual-draw paths in the current code.

MUST-FIX BEFORE BUILD:
1. [1, 7.2, 9.1] The new catalog API is not specified tightly enough to code. Current `build_story_contract(cast_seed, script_brief, news_seed, meta)` always calls `select_style()` and has no forced path (`C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\nodes\_otr_style_catalog.py:754-772`). Concrete fix: add one public entry point with exact semantics, e.g. `build_story_contract(cast_seed, script_brief, news_seed, meta, *, forced_slug: str | None = None, label_override: str | None = None) -> StoryContract`; when `forced_slug` is set, resolve it through `get_style()` and raise on unknown, never hash-draw. If the UI uses labels instead of slugs, map label -> slug explicitly; Comfy combo values are the actual string delivered to `_resolve_inputs` (`OTR_LedgerScriptWriter.py:1919-1934`).

2. [1, 5, 9.2] `resolved["style"]` cannot become a free-text label. Freeze validates `meta.gen_params_initial.style` as lowercase snake_case (`C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\nodes\_otr_ledger_freeze.py:594-613`), while writer stamps `resolved["style"]` into `gen_params_initial.style`, `meta.visual_plan.style`, and `meta.style` (`OTR_LedgerScriptWriter.py:5496-5508`, `5631-5636`). Concrete fix: keep `resolved["style"]` as the canonical catalog slug; add `resolved["style_label"]` / `meta.style_label` only if human prose is needed. If `style_custom` survives, it must be `label_override`, not the slug field.

3. [1, 7.3, 9.3] The style decision timing is underspecified and currently impossible as written for RSS fetch. `_resolve_inputs` fetches the article before the story contract exists and passes `style_slug=rss_style_slug` (`OTR_LedgerScriptWriter.py:1377-1408`); the only auto-path value is `_LLM_STYLE_FALLBACK`, which section 2 deletes (`OTR_LedgerScriptWriter.py:281-289`). `_fetch_rss_seed_or_die` still maps style into a legacy 10-style rerank slug and falls back to `mission_control_procedural` for unknown catalog slugs (`OTR_LedgerScriptWriter.py:1138-1178`). Concrete fix: either remove style from the fetcher contract/reranker entirely, or define a catalog-to-fetch-rerank mapping before deleting `_LLM_STYLE_FALLBACK`. Do not pretend the post-news `StoryContract` can drive pre-news RSS selection.

4. [0, 1a, 7.3] `story_scaffold` is still an unresolved architectural fork, not an implementation step. The code uses it to mutate `OTR_ENABLE_STYLE_GRAMMAR` (`OTR_LedgerScriptWriter.py:1710-1729`, `2638-2655`), and that flag gates contract creation plus multiple downstream branches (`OTR_LedgerScriptWriter.py:2813-2819`, `3337-3362`, `3568-3600`). Concrete fix: choose before build. For one-engine cleanbreak, delete the widget, run parameter, env mutator, `_style_grammar_on` gate, and all `contract is None` runtime paths; make the catalog engine unconditional.

5. [0, 1, 7.3] The existing writer has fallback paths that directly violate “no fallback.” Contract build exceptions are swallowed and execution continues with `contract = None` (`OTR_LedgerScriptWriter.py:3337-3362`); later the climax path performs a second `select_style()` draw if the contract is missing (`OTR_LedgerScriptWriter.py:3577-3599`). Concrete fix: remove these broad `except Exception` fallbacks for the style engine path; invalid catalog/style state must fail loud.

6. [2, 3] The “delete outright” sweep is incomplete unless it includes story-pack schema/content, not just picker modules/tests. Runtime-adjacent pack code still allowlists style-pick seams (`C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\nodes\_otr_story_pack.py:27-44`), and JSON pack files reference `style_pick_*` (`nodes\story_packs\banks.json`, `nodes\story_packs\pipelines.json`, `nodes\story_packs\science_news\science_news_default.json`). Concrete fix: remove these seams from the allowlist, banks/pipelines JSON, and pack fixtures in the same deletion sweep, or explicitly classify them as retained runtime schema.

7. [4, 1a] Workflow widget handling is not concrete enough for removing `story_scaffold`. The real workflow has `story_scaffold` at `widgets_values[24]`, followed by `source_bank` at `[25]` and `visual_style` at `[26]` (`workflows\otr_scifi_16gb_full.json:1`; confirmed by `OTR_LedgerScriptWriter` node values). Tests also pin slot 24 (`tests\test_openrouter_slot_widgets_s2.py:62`, `tests\test_workflow_json_guardrails.py:730-733`). Concrete fix: if deleting the widget, update the workflow values and every positional pin so `source_bank`/`visual_style` do not silently shift into the wrong parameter.

SHOULD-FIX:
1. [7.5] “Rewrite tests - positive pins only” is too vague for implementability. Add exact positive pins: explicit combo slug builds the same `StoryContract.slug`; sentinel hash-draw is deterministic; `style_custom` label override does not change slug; `meta.gen_params_initial.style == meta.story_contract.slug`; no `meta.style_pick`; workflow slot audit passes.

2. [7.7] The plan says full regression + Bug Bible after the whole chunk, but repo rules say run regression suite + Bug Bible after every code change / green chunk. Concrete fix: phrase this as “after the atomic cleanbreak edit before commit” only if the implementation is truly one edit chunk; otherwise test per green chunk.

3. [2] Delete/grep list should exclude `__pycache__` explicitly. Current repo contains many stale binary hits under `nodes\__pycache__` and `tests\__pycache__`; source sweeps should target tracked `.py`/`.json`/workflow files, then separately clean generated caches if desired.

OPTIONAL / NICE-TO-HAVE:
- [1] Add a small helper like `catalog_choices() -> list[str]` so the UI dropdown source is test-pinned against `STYLE_CATALOG` without duplicating list construction in the writer.

CUT THESE (over-engineering):
1. [1] Cut the option to keep `style_custom` as a free-text primary path. It is the main source of slug/label ambiguity and conflicts with the single-engine rule. Safe cut: retain only catalog slug selection plus optional label override, or remove `style_custom` entirely.