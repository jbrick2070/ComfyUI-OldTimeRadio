# Sonnet final QA after remaining campaign coding

The operator explicitly asks for Sonnet QA after coding, again after revisions.
Review the finished F2/B/C/Mac/O2 diff against bdeca482, plus the new tests in
snapshot.json. Read actual Windows files. Read-only: write only your review;
do not edit code, execute tests, call models, or start/coordinate hardware.

The existing R1-R4 architecture converged earlier. This is scoped finished-code
R4 QA under the operator's later checker+rewriter amendment. No new full arc.

F2: My Story character scenes receive full raw fields and structural scene
context. Existing technical owner returns a corrected prompt within at most
two calls including repairs, capped by max_reseed + 1. No report-only checker,
recursive review, or extra publication veto. Unusable correction retains the
composed initial prompt unresolved; real provider/OOM/cancel propagates. Failed
visual operations are logged before rethrow, not falsely claimed persisted.
Corrected prompts reach dispatch, with final transformation/hash receipts and
current source/context cache identity on base and derived jump stills. Neutral
portraits/music/announcer remain separate. No semantic or pixel PASS asserted.
Independent review found setting omission and lost jump-source identity; both
are now fixed and covered through the real merge/cache path.

B: Original credits successful creative generation calls, with finishing and
support separate. Per-call journal is primitive and episode-local, recording
returned generation, not accepted authorship. Local/GGUF/Google/Comfy identity
is labeled request_identity. OpenRouter gets fresh response-local identity
through model-gone fallback, published only after validation/runaway checks.
Unknown response identity stays unreported. Original-only credits_source_line
is stamped before finalizer/wire/save; My Story and source authors preserved.

C: hero-only real textbbox wrapping after existing autoshrink reaches its
font floor. Short-title position/size preserved when already fitting. Break
overlong tokens without ellipsis, retaining marks/joined symbols; compensate
negative bearings and account for ink height. Scratch and paint share flow;
existing footer/abridgment policy stays. Exact long Lantern title passed real
font column/footer tests at 480/720/1080/2160 and CPU PNG inspection at 480/1080.
Finite tiny canvases still have the existing disclosed overflow policy.

Mac: passive RSS/nullable MPS snapshots at actual generation return before
decode in writer/constrained/base/polish, and before retirement/after CPU move/
after existing allocator flush. No new allocation policy, release claim or
Mac OS-kill diagnosis. Counters/imports are lazy, independent and non-raising.
Existing epoch, timeout, ownership and CUDA/MPS teardown stay unchanged.

O2: manifest copies frozen video.roles_effective, never earlier picked roles
or mutated actual shot intent. Health distinguishes intended LTX with actual
existing LTX artifact, intentional non-LTX, absent/unmapped intent, degradation
and sanctions. Empty offenders is not health proof. Optional report_out is
persisted by builder; default check remains a pure manifest read. Six exact
LTX IDs and strict-mode/sanction controls remain.

Driver evidence: 287 focused passed, one live-Metal skip, two existing SWIG
warnings. Actual canonical validator passes (23 nodes, 63 links), no interfaces
or widgets changed. Full suite running against F1 baseline 14,283 passed /
51 existing failures /183 skipped /one xfailed. Bible check still pending.
Do not independently attest those counts; review them as driver receipts.

Identify concrete remaining must-fix bugs with file/line and execution path.
Otherwise give a clear no-must-fix verdict. Hardware qualification is separate
and waits until code QA/checks finish; Mac/4060 remain held by the operator.
