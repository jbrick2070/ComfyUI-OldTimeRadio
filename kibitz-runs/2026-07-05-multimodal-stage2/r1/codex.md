VERDICT: no. The stage goal is coherent, but the identifier model and runnable/non-runnable story are contradictory enough to make the first build encode the wrong contract.

MUST-FIX BEFORE BUILD:
1. [1/2A, 1/2B] Pack coordinates contradict each other. 2A requires `story_packs/<bank>/<model>.json`, but 2B puts `simple_4_prompt_experimental` under `nodes/story_packs/experimental/`; the lab pack header says `source_bank_id: custom_source_bank` and `story_model_id: simple_4_prompt_experimental` (`docs/multimodal-story-schema/schema-examples/story_packs/experimental/simple_4_prompt_experimental.json:2-4`). The example bank also points `custom_source_bank -> simple_4_prompt_experimental` (`docs/multimodal-story-schema/schema-examples/banks.json:104-112`). Fix: choose one canonical coordinate, preferably `nodes/story_packs/custom_source_bank/simple_4_prompt_experimental.json`, or rename the bank to `simple_4_prompt_experimental` everywhere. Do not keep `experimental` as a path segment if path means bank id.

2. [0 Scope, 1/2A, 3 Acceptance] Runnable state is internally inconsistent. The plan says new lanes do not execute and selected `runnable=false` banks fail loud (`input.md:24-28`), but 2A says adapt example banks, where `media_archive` and `public_domain_story` are `runnable: true` (`docs/multimodal-story-schema/schema-examples/banks.json:66`, `:100`) despite missing interpreters/fetchers. Fix: set every non-science bank `runnable: false` until its interpreter/fetcher or pass runner exists, and make the resolver fail before it can enter the science RSS path.

3. [1/2A, 1/2B] Seam vocabulary is split between lab names and production names. Current production allowlist is `outline_macro_system`, `outline_phase_system`, `outline_beat_system`, `line_composer_system`, `style_pick_*_system/user` (`nodes/_otr_story_pack.py:27-40`; pinned by `tests/test_story_pack_stage1.py:23-29`). The example banks/pipelines use `outline_system`, `line_grounding`, `style_pick_inventor`, `style_pick_chooser_user_template`, etc. (`docs/multimodal-story-schema/schema-examples/banks.json:24-31`, `:55-65`; `pipelines.json:11-20`). Fix: define the canonical seam ids in 2A and normalize `required_seams` and `pipeline.seam_refs`, not only pack `prompt_stages`.

4. [1/2B] The proposed global allowlist expansion breaks the meaning of `PRODUCTION_SEAM_ALLOWLIST`. The current code explicitly treats it as the exact production seam set, “not a superset” (`nodes/_otr_story_pack.py:25-27`), and tests assert exact equality (`tests/test_story_pack_stage1.py:59-64`). Adding inert `pass_1..pass_4` seams there because `pipelines.json` references them (`input.md:74-76`) makes non-production lab pass names look production-consumable. Fix: keep production seams separate from pipeline-pass seams, e.g. `PIPELINE_SEAM_ALLOWLIST` or per-pipeline declared seams.

SHOULD-FIX:
1. [0 Scope, 1/2A, 2C] The plan claims fail-loud resolution for `source_bank`, `story_model`, and `story_pipeline` (`input.md:15-17`), but the user-facing chunk only adds a `source_bank` widget (`input.md:89-93`) and the proposed API only has `story_model_id=None` with no pipeline override (`input.md:52`). Fix: state explicitly that Stage 2 only exposes bank selection and resolves model/pipeline from bank defaults, or add non-widget API coverage for explicit `story_model` and `story_pipeline`.

2. [1/2C] The widget append point must be named. The writer’s current optional tail is `story_scaffold` (`nodes/OTR_LedgerScriptWriter.py:2191-2212`), and the canonical workflow already has its saved `"auto"` value at the writer tail (`workflows/otr_scifi_16gb_full.json:1`). Fix: say `source_bank` appends after `story_scaffold`, then add tests for `INPUT_TYPES`, `run()` signature order, and the workflow widget count.

OPTIONAL / NICE-TO-HAVE:
- [1/2A] Add a registry manifest test that reports the resolved coordinate table: bank id, model id, pipeline id, pack path, runnable, executable. This will make failures readable instead of just “cross-ref mismatch.”

CUT THESE (scope / over-engineering):
1. [1/2A] Cut `default_visual_style` validation from Stage 2, or keep it as inert unvalidated metadata only. Stage 0 says “No visual_style anything (Stage 3)” (`input.md:22-23`); validating it now pulls Stage 3 concepts into the routing chunk.

2. [1/2A] Cut `custom_source_bank` from the Stage 2 dropdown unless it is the canonical home of `simple_4_prompt_experimental`. As written, it is both a user “Add Your Own” affordance and the experimental simple-4 lane, which muddies the story-path model before any execution exists.
