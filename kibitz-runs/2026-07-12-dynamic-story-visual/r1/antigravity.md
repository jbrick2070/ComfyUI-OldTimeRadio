VERDICT: yes-with-fixes. The plan has strong structural alignment but contains fatal deadlocks around ComfyUI caching, writer-validation gates, and post-freeze timing/preset mutations that will break the story hash invariants.

MUST-FIX BEFORE BUILD:
1. [Section 6.1] STALE STORY HASH DEADLOCK: The plan proposes verifying `story_binding.lines_sha256`, `story_binding.cast_sha256`, and `story_binding.beats_sha256` by computing raw canonical digests of `ledger.lines`, `ledger.cast`, and `ledger.beats` respectively. However, downstream nodes mutate these lists post-freeze (e.g. `OTR_EpisodeAssembler` sets timing fields `start_s` and `dur_s` on `lines` / `beats`, and `OTR_CastLock` assigns voice presets on `cast` fields). A raw canonical digest comparison at the reader stage (ShotLock, MetaBrief) will fail due to these expected, non-story mutations.
   - FIX: Filter the dictionaries before hashing. For `lines`, only digest `["line_id", "text", "char_id", "speaker_role", "boundary", "skip"]`. For `cast`, only digest `["char_id", "name", "gender", "character_description"]`. For `beats`, only digest `["beat_id", "speaker", "char_id", "line_ids"]`.
2. [Section 5 & 6.1] PARAMETER CONTEXT MISMATCH IN STYLE RESOLUTION: The plan states that the story hash and validation matrix will be enforced in the `_resolve_style` branch. However, `_resolve_style` (nodes/_otr_story_brief_helpers.py:330) and `get_visual_style` (nodes/_otr_visual_styles.py:378) only accept the `meta` dict parameter, and do not receive the parent `ledger` or `lines`/`cast`/`beats` lists, making content hashing impossible at that seam.
   - FIX: Retrieve the active ledger dynamically inside `get_visual_style` from the thread-safe global ledger singleton using `nodes.production_ledger.peek_ledger()`.
3. [Section 5] DROPDOWN/GATE DEADLOCK FOR SENTINEL: The script writer gate calls `_otr_visual_styles.resolve_visual_style(visual_style)` (nodes/OTR_LedgerScriptWriter.py:3339) to validate the visual style before any story generation or freeze. If the user selects `dynamic_story`, it will raise `UnknownVisualStyleError` because no `dynamic_story.json` pack exists on disk. This prevents the story from ever running and reaching the FreezeCascade stage where the dynamic pack is generated.
   - FIX: Teach the script writer gate in `OTR_LedgerScriptWriter.py:3339` to exempt `"dynamic_story"` from `_otr_visual_styles.resolve_visual_style()` validation, or teach `resolve_visual_style()` to return a dummy/skeleton `VisualStyle` when queried for `"dynamic_story"`.
4. [Section 7.1] RE-RUN CACHING DEADLOCK: In ComfyUI, a node is cached and will not re-execute if its inputs are unchanged [ASSUMPTION]. The proposed `OTR_DynamicStoryDirection` node takes only `script_json` and `gate_in` as inputs. If a user wants to re-roll/regenerate the visual style for the same frozen story, the inputs will be identical and ComfyUI will return the cached output, making re-rolls impossible.
   - FIX: Add a mutating widget parameter (e.g. `seed: INT` or `force_reroll: INT`) to the `OTR_DynamicStoryDirection` inputs to allow the operator to force a cache miss and trigger a re-run.

SHOULD-FIX:
1. [Section 4] VRAM LEAK / LLM RESIDENCY: The `OTR_DynamicStoryDirection` node makes LLM calls by requesting the `technical` slot, which loads the LLM into GPU memory. If the node does not unload the model before exiting, it will leave the LLM resident in VRAM, risking Out-Of-Memory (OOM) errors when subsequent heavy image/video generators (Flux, LTX) run.
   - FIX: Implement a `finally` block in `OTR_DynamicStoryDirection`'s run method to unload the LLM using `_otr_model_loader.unload_llm_if_local_resident()`, matching the behavior of `OTR_LedgerFreezeCascade` (nodes/OTR_LedgerFreezeCascade.py:462).
2. [Section 7] IMPORT ISOLATION VIOLATIONS: The proposed new files `nodes/otr_dynamic_story_direction.py` and `nodes/_otr_visual_direction.py` might import heavy libraries at the top level, slowing down ComfyUI startup or crashing it if dependencies are missing.
   - FIX: Mandate that all heavy model loading or LLM invocation utilities (e.g. `_otr_model_loader`) are imported lazily inside the node's methods rather than at the module top level.
3. [Section 6.4] CROSS-PROMPT CACHE LEAKAGE: The static visual styles are cached globally in `nodes/_otr_visual_styles.py` under the `_STYLES` dictionary. If `get_visual_style` dynamically populates this dictionary with the `dynamic_story` pack, subsequent runs in the same ComfyUI session will serve stale cached data.
   - FIX: Ensure that when `dynamic_story` is resolved, it bypasses the static `_STYLES` cache dictionary entirely and is rebuilt/validated on every call.

OPTIONAL / NICE-TO-HAVE:
1. [Section 9 D3] Keep per-shot notes in `meta.visual_direction` and let visual composers read them directly instead of duplicating them into `ledger['video'].shots[].creative`. This keeps the schema clean and reduces ledger serialization size.

CUT THESE (scope / over-engineering):
1. [Section 9 D7] Wardrobe lock overlap: Keep `continuity.characters` orthogonal to `OTR_OUTFIT_LOCK` instead of folding wardrobe lock into the direction node. Let `OTR_OUTFIT_LOCK` continue managing character wardrobe presets dynamically as it does today (nodes/otr_shot_lock.py:143-153) to avoid over-engineering the visual direction node.
