VERDICT: no — full-story provenance, bounded generation, exact target consumption, strict artifact shape, canonical wiring, and a meaningful live proof still have build-blocking defects.

MUST-FIX BEFORE BUILD:

1. [§2.5, §4.3–4.4; Lesson 5] P-A may head-truncate accepted story text, contradicting both “reads the final accepted/frozen story” and “NEVER truncates.” Remove truncation: P-A receives the complete line spine or preflight aborts before generation. `kibitz-runs/2026-07-12-dynamic-story-visual/r4/input.md:14-17,439-448,601-608`; `docs/PRODUCTION_SPRINT_LESSONS.md:66-75`.

2. [§4.3–4.4, §10/I2–I3; Lessons 4–5] Budgeting is not implementable. Most authored strings, list cardinalities, evidence counts, and `mood` are unbounded; the repair equation double-counts input/output, while the real repair helper echoes only 400 failed-output characters. Define every bound, requested sampling value, and deterministic source-order batch rule. Preflight `max(base_call_tokens, actual_repair_call_tokens) <= configured_safe_cap`; separately record advertised/native and configured-safe caps, exact versus conservative counting, and fail if cap/count is unknown. Close constrained-generation behavior for both P-A and P-B per provider. `r4/input.md:625-665,1258-1266`; `nodes/_otr_repair_prompts.py:111-152`; `nodes/_otr_model_catalog.py:1200-1269`.

3. [§2.5, §3.2, §7.1; Lessons 1, 5] `DirectionSourceV1` has no unique ledger projection. Production stores `story_brief` as a string, flat palette/key-object fields, and nested `story_brief_terms={setting,lighting,atmosphere}`; the plan instead specifies both `lighting` and an untyped `story_brief_terms`. It also leaves `cast[].traits` string-versus-list normalization ambiguous and misstates `episode_id` as coming from meta instead of the top-level ledger. Define exact source paths, closed types, and normalization; use one canonical representation with no duplicated lighting. `r4/input.md:417-435,502-516`; `nodes/_otr_story_brief.py:841-860`; `nodes/_otr_ledger_freeze.py:454-465`; `nodes/production_ledger.py:551-571`.

4. [§2.2, §4.1, §4.5, §9.1.3; Lessons 2–4, 14] The wrong-depth defense still regressed. P-A’s ownership prose omits top-level `era_cues`, and the “per-pass” test places `era_tail_2` in P-B although P-B has no era field. Create one exact parent/key table for every P-A/P-B object and collection, shared by schema, base prompt, fixture, validator, and repair. Test authoritative-top-level plus nested extras, absent/empty top-level, identical versus conflicting candidates, wrong types, unknown keys, duplicate IDs, full post-validation, and zero LLM calls after successful deterministic repair. `r4/input.md:548-557,697-708,1077-1082`; `docs/PROD_BUG_LOG.md:469-487`.

5. [§2.2–2.6, §7.6; Lessons 11, 13] Executable fields can evade semantic grounding by selecting `kind="rationale"`; inline motif/clue/shot evidence has no `kind` at all, and `{id,quote}` cannot identify which cast/brief list leaf was cited. Require at least one factual positive anchor for every executable `look.*` field and still-direction row, with closed `field/path`, index where needed, `start`, `end`, and exact-slice `quote`. Persist the normalized source DTO or bounded per-path source receipt so a later mismatch identifies first drift rather than only reporting a whole-object hash. `r4/input.md:297-306,520-527,954-965`; `docs/PROD_BUG_LOG.md:489-495`; `docs/PRODUCTION_SPRINT_LESSONS.md:142-153,167-177`.

6. [§2.3–2.4, §5.4, §8.6/8.9] Persisted `style_pack.is_dynamic=true` would fail `_validate_row`, which rejects every non-v2 key. Remove it from the stored payload; add runtime-only `VisualStyle.is_dynamic=False`, set true only after validating a `dynamic_story` payload against the exact v2 schema and path-stem rule. Also pin the safety base key-for-key or identify a hashed authoritative source—its current values are unspecified, allowing incompatible mouth, motion, typography, and subject behavior. `r4/input.md:343-347,393-410,806-808`; `nodes/_otr_visual_styles.py:138-166,184-226`; `nodes/visual_styles/sci_fi_radio.json:1`.

7. [§2.3, §3.2–3.3, §5.2/5.5; Lessons 1, 14] Stored provenance objects remain open placeholders: `runtime_policy`, `effective_sampling`, `token_budget.pb_batches`, journal rows, and failure/teardown receipts lack exact shapes and ownership; the journal later adds `effective_config` absent from its example. `policy_from_meta` returns a dataclass with a tuple-valued allowlist, not the promised strict JSON receipt. Define closed JSON DTOs and owners, or cut redundant fields. A failed Direction teardown occurs before `meta.visual_direction` exists, and MetaBrief has no ledger output, so arbitrary `*_unload_ok` persistence is not implementable. `r4/input.md:316-330,710-716,751-759,818-823`; `nodes/_otr_shared/llm_policy.py:65-80,131-154`; `nodes/otr_meta_brief_image_prompt.py:2099-2106`.

8. [§2.6, §7.4, §9.1.14] The still-direction universe and consumption law contradict production. `derive_scene_still_targets` deliberately emits `b000_music_open`, yet vd-1 excludes it. Policy-dependent mesh roles replace the ordinary still with mesh fodder plus a plate, while the matrix consumes neither authored field; `still_word` discards `subject_note`. Define a named line-backed projection that filters only the synthetic opening, and either replace `subject_note` with a per-kind `visual_note` consumed by every branch or add policy input plus exact wiring. Specify exact clause templates, punctuation, and insertion positions so prompts and hashes are deterministic. `r4/input.md:455-463,901-922`; `nodes/otr_meta_brief_image_prompt.py:1026-1118,1937-2045`.

9. [§2.4, §5.4, §7.4, §8.13–14] The talking-safety claim is false: dynamic LLM-authored `positive_tail` currently enters IA2V talking prompts, and ShotLock appends the full authored visual tail. Pin dynamic talking registers to the vetted talking look, or explicitly define a constrained dynamic cue and revise D6. Separately, preserve `get_story_brief_ltx` as semantic scene content; only its look/palette authority is suppressed. Pass the resolved dynamic style to the finisher instead of deleting the narrative core. `nodes/_otr_video_engines/render_driver.py:619-641,2052-2089`; `nodes/otr_shot_lock.py:618-636`; `nodes/_otr_story_brief_helpers.py:149-170`.

10. [§8.3; Lesson 7] The claimed literal workflow delta is not literal: `order` remains a placeholder and output slots 1/2 lack indexes. Specify a valid complete node record—node 96 at order 4, with current orders ≥4 shifted—and reconcile array placement, links, output fan-outs, and widgets. Canonical node 63 currently has no active profile/master stamp, contradicting “no widget change” followed by “master hash stamp”; leave canonical stamps empty, compute its receipt hash, then regenerate and verify all committed stamped variants. `r4/input.md:998-1026`; `workflows/otr_canonical.json:1`; `nodes/_otr_workflow_validator.py:394-408`; `scripts/build_variants.py:129-157,316-338`.

11. [§8.2, §9.1.20; ComfyUI node contract] AST presence in `_NODE_MODULES` does not prove registration: root loading catches import failure and silently omits both mappings. Add a cold package-import test asserting the concrete class and label in `NODE_CLASS_MAPPINGS` and `NODE_DISPLAY_NAME_MAPPINGS`, then pin `INPUT_TYPES`, `FUNCTION`, `CATEGORY`, return names/types, and actual tuple arity. `__init__.py:351-367`; `tests/test_workflow_contract_validation.py:41-98`.

12. [§9.1.8, §9.3; Lessons 7–8] The live control cannot byte-match a committed pre-feature prompt: Writer always re-executes and local generation samples without a seed. Keep byte-identity proof in captured-ledger/injected tests; live control should prove named-pack pass-through, no dynamic receipts, and successful publication. Canonical defaults also select visualizers that ignore stills, so a dynamic smoke can publish without exercising story-directed pixels. Run control/dynamic qualification with the same explicit still-consuming configuration, assert a generated still and artifact-authored prompt clauses, then verify final episode/OBS assets. `r4/input.md:1095-1099,1183-1193`; `nodes/OTR_LedgerScriptWriter.py:3034-3038`; `nodes/_otr_model_loader.py:1122-1129`; `nodes/otr_image_gen_dispatcher.py:398-425,561-566`; `nodes/_otr_video_engines/cheap_families.py:222-240`.

13. [§3, §9.4; Lessons 1, 6, 10] The sprint receipt omits Dispatcher and ShotLock as authoritative durable provenance writers and does not define when `PASS` is legal or whether 720w is required versus `NOT RUN`. Add mechanical PASS criteria and an exact build sequence: capture/commit/push baselines first; land code, consumers, registration, tests, canonical JSON, and variants as one activation chunk; run all gates and push; qualify 30w then 120w; record 720w status explicitly; verify `HEAD==origin`. `r4/input.md:492-516,1203-1225`; `AGENTS.md:73-81,134-142`.

SHOULD-FIX:

1. [§9.2] Record each qualification leg as explicit creative-slot and technical-slot assignments, with concrete provider/model IDs, prompt hashes, constrained mode, cap/count source, and repair counts. The cloud lane must be declared before harness implementation, not merely before execution. `r4/input.md:1165-1179`.

2. [§9.3; Lessons 8–9] Make the operational gate explicit: selective reset, empty port, baseline VRAM, UTF-8 canonical launcher, server and leg logs, watchdog, queue/history/runner verdicts, and one-variable comparisons. A reusable live failure must update both `PROD_BUG_LOG.md` and the relevant preflight/planning guide. `docs/PRODUCTION_SPRINT_LESSONS.md:110-129`.

OPTIONAL / NICE-TO-HAVE:

1. [§9.1.21] Add a small perceptual-difference metric to the operator look rubric after functional gates; it must not replace prompt/provenance and human visual review.

CUT THESE:

1. [§8.1] Cut unwired `direction_report`; the same report is logged and the durable artifact is the audit surface.

2. [§8.1, §10/I4] Cut unwired `done` in v1. Re-add it with the ordering edge only if the documented live OOM trigger fires.

3. [§5.3] Cut `ResolvedDirection.artifact_meta`; no listed consumer uses it.

4. [§2.3, §5.2/5.5] Cut persisted `source_recheck` and partial failure/success stamps. A stored artifact can only contain `source_recheck="clean"`; failures refuse the stamp. Preserve named exceptions, structured logs, and live residency evidence instead.

VERIFY-AT-BUILD checklist:

- [ ] [§2.5, §4.4] Confirm the full untruncated source DTO, every field/cardinality bound, P-A/P-B reserve, repair maximum, deterministic batch partition, and artifact-size maximum.
- [ ] [§4.3] Record advertised/native and configured-safe caps, token-count mode, provider output floor, constrained mode, and actual repair-message size for Mistral, Gemma E4B, and the declared cloud lane.
- [ ] [§4.1/4.5] Run the complete wrong-depth/unknown-key/duplicate/conflict battery for both passes; prove deterministic repair runs the complete validator without another LLM call.
- [ ] [§2.2, §7.6] Verify every executable field and still-direction row resolves to an exact stored source span, and a one-field source mutation identifies its precise coordinate.
- [ ] [§8.2–8.3] Cold-import node 96; validate both mappings and the full node contract; confirm canonical has 24 nodes, 58 links, IDs 96/284, exact `62→96→{89,90}` topology, zero widget slots, reconciled fan-outs, JSON round-trip, computed hash, and regenerated variant stamps.
- [ ] [§6–7] Re-run named/absent-style byte baselines and immutable-input cache tests; verify named packs resolve `is_dynamic=False`, dynamic resolves true only after strict v2 validation, and `_STYLES` is unchanged.
- [ ] [§7.4] Exercise scene-character, scene-beat, still-word, mesh-fodder, and talking paths; prove every authored executable field is consumed exactly as specified and hostile dynamic tails cannot contaminate talking safety.
- [ ] [§9.2–9.3] Run canonical 30w then 120w qualification on two local families plus the declared cloud lane, using a still-consuming visual configuration; verify persisted artifact/ledger, generated stills, authored prompt clauses, episode asset, `obs_publish OK`, and final `otr\obs\` file.
- [ ] [ASSUMPTION] [§5.5, §10/I4] Measure actual scheduler order, peak VRAM, unload result, model-cache residency at Dispatcher entry, and any direction/audio co-residency; only a reproduced OOM reopens link 285.
- [ ] [§9.3–9.4] Verify reset/launcher/watchdog/queue/history/log receipts, full Windows suite, Bug Bible, AST/JSON/no-BOM/no-zero-byte checks, every live-failure PBUG entry, pushed green commits, and `HEAD==origin`.
