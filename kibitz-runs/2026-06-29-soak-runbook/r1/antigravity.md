VERDICT: build-ready as-is? no. Multiple critical process-termination, act-budget limits, and engine enablement conflicts will crash the run or fail the soak silently.

MUST-FIX BEFORE BUILD:
1. [Section 7] Blanket process termination kills parent MCP session.
   - Defect: Step 1 of the checklist dictates "selective kill, never a blanket Stop-Process -Name python" to avoid severing MCP connections. However, the canonical leg launcher [scripts/otr_run_leg.ps1](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/scripts/otr_run_leg.ps1#L44-L45) directly executes `Stop-Process -Name python, pythonw -Force` as its first step. Running this will crash the agent session.
   - Concrete Fix: Modify [scripts/otr_run_leg.ps1](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/scripts/otr_run_leg.ps1#L44-L45) to use selective process killing, matching the pattern in [scripts/otr_overnight_sweep_launch.ps1](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/scripts/otr_overnight_sweep_launch.ps1#L42) (`Get-Process python | Where-Object { $_.Path -like '*Documents\ComfyUI\.venv*' } | Stop-Process -Force`) or invoking `scripts/kill_otr_zombies.ps1 -Force`.
2. [Section 1] Invalid act count recommendation.
   - Defect: The table states that target words 1500 - 3120 can use "up to ~10" acts. However, the budget validator in [nodes/_otr_episode_budget.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_episode_budget.py#L103) caps act counts strictly at 7 (minimum 1, maximum 7) because the static `ACT_COUNT_CONFIG` only maps act counts 1 through 7, and `compute_episode_budget` (line 249) raises `InvalidEpisodeBudgetError` if `act_count` is not within `[1, 7]`.
   - Concrete Fix: Correct the table to cap the maximum acts at 7, and clarify that any act count above 7 is structurally invalid and will be rejected.
3. [Section 2] Inverted engine default-enablement logic.
   - Defect: Section 2 claims that `ltx_video` and `visualizer` are "Default-ON (no flag needed)" and `OTR_ENABLE_LTX_AV` is "Default-OFF (set flag)". This is false:
     - In [nodes/_otr_video_engines/eng_ltx_video.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/eng_ltx_video.py#L280) and [nodes/_otr_video_engines/eng_visualizer.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/eng_visualizer.py#L41), `default_roles = ()`. The registry base [nodes/_otr_shared/engine_registry_base.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_shared/engine_registry_base.py#L223) rejects any non-default engine selection if its `requires_flag` env var is not set to "1". Since `_otr_soak_server_launch.cmd` does not export `OTR_ENABLE_LTX_VIDEO=1` or `OTR_ENABLE_VISUALIZER=1` in default mode, these engines are Default-OFF/opt-in.
     - In contrast, the `ltx_audio_in` engine subclass of [nodes/_otr_video_engines/eng_ltx_av.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/eng_ltx_av.py#L830) defines `default_roles = ("music_visual", "announcer_visual")`. It bypasses the registry flag check, and its flag `OTR_ENABLE_LTX_AV` defaults to "1" (opt-out) in usability checks (line 386). Thus, LTX-AV is Default-ON.
   - Concrete Fix: Reclassify `visualizer` and `ltx_video` under Default-OFF (requiring `OTR_ENABLE_VISUALIZER=1` and `OTR_ENABLE_LTX_VIDEO=1` at launch), and reclassify `ltx_audio_in` (governed by `OTR_ENABLE_LTX_AV`) under Default-ON.
4. [Section 0 / Section 5 / Introduction] Goal vs. Method Divergence on Loop Error Handling.
   - Defect: Stated goal is to "surface engine/config failures LOUDLY and EARLY instead of churning them silently all night." However, the loop harness in [scripts/_otr_soak_marathon.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/scripts/_otr_soak_marathon.py#L242) catches all exceptions (`except Exception as exc:`) inside the leg loop, logs them, and moves to the next leg. If a config error (like the episode budget mismatch) occurs, the harness will silently fail every single leg all night, directly violating the early-surface goal. Relying on a manual operator verification gate (Section 0) is error-prone.
   - Concrete Fix: Update the loop harness [scripts/_otr_soak_marathon.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/scripts/_otr_soak_marathon.py#L242) to track consecutive failures (e.g., exit loop if the first leg or 3 consecutive legs fail) to ensure unattended runs fail fast.

SHOULD-FIX:
1. [Section 2] Redundant environment variable recommendation.
   - Defect: The runbook notes that `wan_ti2v` requires setting `OTR_WAN_TI2V_VAE_NAME`. However, [nodes/_otr_video_engines/eng_wan_ti2v.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/eng_wan_ti2v.py#L263) defaults VAE resolution to "wan2.2_vae.safetensors" if unset, making the recommendation redundant.
   - Concrete Fix: Remove `OTR_WAN_TI2V_VAE_NAME` from the required setup variables in Section 2, stating it is only needed as an override.
2. [Section 6] Stale morning report monitoring context.
   - Defect: Section 6 says "A scheduled morning task should read..." but does not name or reference the actual script [scripts/otr_overnight_sweep_launch.ps1](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/scripts/otr_overnight_sweep_launch.ps1#L7) or the scheduled Windows Task (`otr-sweep-monitor`).
   - Concrete Fix: Explicitly name the scheduled task (`otr-sweep-monitor`) and reference [scripts/otr_overnight_sweep_launch.ps1](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/scripts/otr_overnight_sweep_launch.ps1) as the script that schedules/drives this report.

OPTIONAL / NICE-TO-HAVE:
- [Section 2 / Section 7] Detached shell environment inheritance issue [ASSUMPTION]. Environment variables set in the parent shell via `setx` may not propagate immediately to detached processes launched via `Start-Process` unless the parent shell is restarted. Adding a brief reminder to restart PowerShell or verify the env block prevents silent option mismatches.

CUT THESE (scope / over-engineering):
1. [Section 1] Redundant act_count calculation heuristic.
   - Why it is safe to cut: The runbook recommends "When in doubt, set `act_count = max(1, target_words // 60)`". This is redundant and confusing because the budget package in [nodes/_otr_episode_budget.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_episode_budget.py#L331) implements `auto_act_count(target_words)` which automatically and deterministically calculates the ideal act count directly in the node workflow when `act_count="auto"` is set. The manual formula should be cut in favor of telling the operator to set `act_count="auto"`.
