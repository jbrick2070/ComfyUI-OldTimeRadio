VERDICT: no. The plan targets the right leak, but it misstates the failed run scope and proposes an intermediate-deletion lifecycle that can break the wired scopes overlay.

MUST-FIX BEFORE BUILD:
1. [What happened / ACCEPTANCE] False scope claim: the run was not just "11 failed but rendered"; `scripts/_otr_overnight_combos.log:399` and `:708` show non-hygiene `OTR_VideoRenderBatch` execution failures for `cov_fill_mesh_stage` and `cov_fill_wan_i2v`, while many other `SOAK_FAIL` entries are scope-temp leaks (`scripts/_otr_overnight_combos.log:89`, `:164`, `:257`, `:311`, `:359`, `:443`, `:484`, `:521`, `:565`, `:629`, `:671`, `:746`, `:784`, `:822`, `:860`, `:898`). Fix: explicitly narrow this plan to "system-temp hygiene failures only" and move mesh/wan_i2v render failures out of acceptance for this change.
2. [FIX #1] "delete the intermediate after the final scopes mp4 is produced" is conceptually wrong if implemented in `OTR_SceneAwareScopes`: that node returns the path at `nodes/otr_scene_aware_scopes.py:537`, and the real workflow wires node 94 output link `273` into node 93 `scopes_mp4_path` in `workflows/otr_scifi_16gb_full.json:1`. `OTR_PostUpscaleProcgenBlend` resolves and consumes that file at `nodes/otr_post_upscale_procgen_blend.py:903-912` and does not delete it. Fix: say either "producer writes to `otr_shared_tmp_dir()` and janitor owns cleanup" or "consumer unlinks `scopes_mp4_path` only after successful blend/fallback decision"; do not delete in the producer before downstream consumption.
3. [FIX #2 / ACCEPTANCE] The prevention story is missing the actual regression guard. Existing `tests/test_engine_tmp_in_tree.py:111-129` only scans `nodes/_otr_video_engines/*.py`, so it would not catch `nodes/otr_scene_aware_scopes.py:537`, which is the leak. Fix: add/require a top-level node hygiene test that catches `tempfile.gettempdir()` joined with `otr_*` in workflow-reachable nodes, plus a focused `SceneAwareScopes` path-allocation test.

SHOULD-FIX:
1. [FIX #2] The peer-audit wording overstates "persistent" for `nodes/video_engine.py`: it writes `otr_video_audio.wav` in system temp at `nodes/video_engine.py:2201` but removes it after encode at `nodes/video_engine.py:2404`; the risk is failure-before-cleanup and shared fixed-name collision, not normal successful persistence. Fix: reword the audit criterion to "system-temp `otr_*` creation that can persist on failure or collide across runs."
2. [FIX #2] The audit set is incomplete if the goal is "OTHER persistent `otr_*` system-temp writers": `nodes/_otr_shared/gpu_residency.py:51` defaults `otr_gpu_residency.lockdir` under system temp, and `nodes/_otr_openrouter_backend.py:1424` creates `otr_orcat_` under system temp in its `__main__` self-test. Fix: classify these as intentional non-render lock/test paths or add them to the audit decision table; do not leave them unstated.
3. [FIX #1] The plan says to mirror `otr_engine_tmp_mp4`'s loud behavior, but `otr_shared_tmp_dir()` itself returns a path and the helper creates the directory at `nodes/_otr_video_engines/_tmp.py:23-24`. Fix: require `mkdir(parents=True, exist_ok=True)` at the scopes allocation point, not just using the path authority.

OPTIONAL / NICE-TO-HAVE:
- [ACCEPTANCE] Add "scopes overlay still appears in the blended output" or at least "PostUpscaleProcgenBlend reports `scopes: 3-input blend`" so the hygiene fix cannot silently drop the visual layer.

CUT THESE (scope / over-engineering):
1. [FIX #2] Cut broad rewrites of cleaned `mkdtemp(prefix="otr_*")` callers for this build. `nodes/otr_silent_composite.py:751` is removed in `finally` at `nodes/otr_silent_composite.py:780`, and `nodes/rtx_upscale.py:653` cleans `silent`/`tmp_dir` at `nodes/rtx_upscale.py:699-706`; auditing is enough unless the audit finds a real persistence path.
2. [OPEN QUESTIONS #3] Cut making the hygiene gate tolerant. `scripts/_otr_soak_capstone.py:225-276` is correctly detecting new `otr*` entries under `%LOCALAPPDATA%\Temp`; weakening it would hide exactly this class of leak.