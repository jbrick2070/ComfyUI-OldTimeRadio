VERDICT: no. The document ranks a 14-arm campaign before choosing which of three mutually fighting goals the first build is allowed to serve (haunted look vs visual-style obedience vs still-carried identity), and it makes an unproven 8 GB Klein+AnimateDiff combination a hard success gate on every still arm.

MUST-FIX BEFORE BUILD:

1. [§6 + §5B + §5C + §7 E0/E10/E11] Goal and method diverge. §6 asks the new lane to be haunted, unique/alive, and style-obedient. The shipped machinery cannot do all three at once: `GhostSignalV3HauntedEngine` freezes `ADAPTER_V3_STRENGTH = 1.0` as the "full video-dataset domain" (`nodes/_otr_video_engines/eng_ghost_signal_official.py:103-113, 154-156`); `compose_ghost_prompt_v2` gives the pack only `compact_style_cue` (`nodes/_otr_visual_styles.py:632-653`) plus `effective_negative`; the anime pack negative bans "photograph, photorealistic, ... film grain" (`nodes/visual_styles/anime.json`) while the adapter at 1.0 is photographic grime. Stills-in (E1/E2/E7/E10) cannot "carry the medium" through that adapter. Concrete fix: name one first campaign and sequence the rest. Smallest: freeze E0 per-style (at least `anime` vs `storybook_engraving` vs `sci_fi_radio`) before any still arm; do not treat a single frozen 1.0 (or a single lowered number) as "the current recipe" for style tests.

2. [§7 prerequisite + §8 + §9] The still prerequisite is a shipping-contract replacement, not "one shared code change." Today `GhostSignalEngine.accepts_still = False`, `still_plan = ()`, `required_inputs = ("text_prompt",)`, `family = "text_to_video"`, `subject_ownership = "prompt"` (`nodes/_otr_video_engines/eng_ghost_signal.py:294, 334, 350-352`). Flipping `accepts_still`/`init_image` hits: `_NO_STILL_VIDEO_ENGINES` (`scripts/otr_provision.py:1546-1551`); `_engine_scene_init_required` then `DeferredImageGapError` on a missing still (`nodes/_otr_video_engines/render_driver.py:2238-2271`); ImageGenDispatcher MODEL REFUSAL that continues without a still (`nodes/otr_image_gen_dispatcher.py:1746-1760`); preflight that requires `subject_ownership == "prompt"` on this lane (`tests/test_ghost_signal_lane.py`; `tests/test_lane_preflight_matrix.py`); ten haunted profiles including `otr_5080_haunted_12b_overnight` whose display_name is "No image model" (`config/profiles/otr_5080_haunted_12b_overnight.json:2-16`). §9 then requires the 8 GB class to publish Klein-then-AnimateDiff, which §2 itself says has never happened (`config/machine_classes.json:163-169`). Concrete fix: keep the shipping haunted engine prompt-owned. Run still-in as a named lab peer (new engine id + new recipe receipt), on the 5080 first, with Klein+AD on the 4060 as a later measured leg -- never as the gate that blocks E1.

3. [§7 E1/E2/E7/E10 vs §2 mode cycle vs §3 Tectal excerpt] Still-in identity and Ghost identity are different mechanisms and they collide. Recurrence today is the motif string plus `schedule_ghost_modes` on `GHOST_CHARACTER_CYCLE = ("figure", "object", "figure", "signal")` (`nodes/_otr_video_engines/ghost_signal_author.py:101, 472-521`). The Tectal excerpt in §3 is Zhang figure / Boyden object (amber key) / Zhang figure. A `scene_character` still is a person; object/signal laws demand "the object fills the frame" / "the object lit against a dark room" (`nodes/_otr_video_engines/ghost_signal_prompt.py:125-146`). Img2img/injection/SparseCtrl from a face-still into an object-mode prompt will not hold identity; from a key-still it will not hold the face. `frame_contract.continuity = CONTINUITY_NONE` (`eng_ghost_signal.py:386-393`) also means no last-frame carry across the "eleven unrelated" clips §6 complains about. Concrete fix: before any still arm, decide per mode: figure beats may consume `scene_character`; object/signal beats either stay text-only, consume a prop still, or the cycle is frozen to figure for the still-in peer. Do not claim E1/E7 "identity persists" while the cycle still removes the person from half the character beats.

4. [§7 E9 ranked 9 / §5B door 5 / §9 step 2] E9 is a precondition, not an experiment. ShotLock writes `led["video"]` and returns `patched_ledger_json` without saving (`nodes/otr_shot_lock.py:3060-3076`). `Ledger.data` has no `video` key (`nodes/production_ledger.py:712-731`). `_merge_with_disk` `TOP_PRESERVE` is only `schema_version, audio, audio_gates, transitions, radio_bookend_path` (`production_ledger.py:1592-1595`). Seed comes from `render_request_hash` over brief/cast/beat/char only (`otr_shot_lock.py:1316-1317, 1475-1477`; `render_driver.py:3748-3767`). Without durable prompt+seed, the §9 A/A protocol cannot be cited by hash. Concrete fix: land E9 first, as one chosen shape (either add `"video"` to `Ledger.data` + `TOP_PRESERVE`, or a `render_trace` stamped by `OTR_VideoRenderBatch`). Do not start visual arms until a published episode shows `ghost_prompt` + final `text_prompt` + `request_seed` on disk.

5. [§7 prerequisite list] E5 is falsely bundled with the still prerequisite. Context extras (`ADE_ContextExtras_ContextRef` / `_NaiveReuse`) plug into the existing Gen1 `context_options` chain and do not need `init_image`. Concrete fix: drop E5 from that list. E5 remains a separate VRAM-risk arm against the 13.3-14.2 GB overnight peaks in §2.

6. [§7 E1 vs §8] "Experiments add, they do not replace" is hollow for E1. E1 replaces `EmptyLatentImage` and `GHOST_DENOISE = 1.0` (`eng_ghost_signal.py:99, 157-158`) with VAE-encoded init + denoise < 1.0. Those are proven recipe cells. Concrete fix: declare E1 a named peer recipe (`..._img2img_v1` or similar), A/A against the empty-latent haunted receipt, and never stamp `animatediff_sd15_v3_haunted_static16_512x288_v1` on a denoise-not-1.0 clip.

SHOULD-FIX:

1. [§7 E3 vs Prompt v2] E3 re-opens the fields v2 was built to exclude. `compose_ghost_prompt_v2` "never reads raw beat_intent, raw traits, raw arc_phase, a pack motion register" (`ghost_signal_prompt.py:836-838`). Putting those on `ADE_PromptScheduling` keyframes is a second authoring path, not "deterministic Python beside v2." If a beat-shape arm is wanted, schedule only already-composed v2 slots (motif / leaf / law) across windows. Leave `traits`/`beat_intent`/`arc_phase` alone; they are null on `scifi_news_pro` anyway (§3).

2. [§7 E1 cost row] "VRAM unchanged" is unproven. Empty latent already allocates U frames; VAEEncode + RepeatLatentBatch of a 1472x832 still resized to 512x288 is probably small, but §2 headroom is 300 MB-1.2 GB against 14.5 GB. [ASSUMPTION] peaks stay inside that band. Fix: measure clip `vram_peak_mb` on the 5080 before calling E1 cheap, and do not claim 8 GB is free.

3. [§9 A/A protocol vs §7 intro] The plan admits a fixed seed is "not bit-stable across context windows," then asks the operator to judge one experimental clip against one noisy A/A pair. A 95-frame beat is 8 static windows (`ComfyUI-AnimateDiff-Evolved/animatediff/context.py:312-328`, stride 12). Fix: publish two A/A nulls, not one, so the noise floor is a range.

4. [§4 still size vs engine canvas] Scene stills are `_landscape_still_dims()` default 1472x832 (`nodes/otr_meta_brief_image_prompt.py:475-480`); portraits on wide lanes are 832x480 (`nodes/_otr_shared/aspect.py:31, 54-60`); Ghost canvas is 512x288 (`eng_ghost_signal.py:82-83`). ADE `resize_image=True` exists (`ComfyUI-AnimateDiff-Evolved/animatediff/nodes_sample.py:631-657`) but the fit/crop/letterbox policy is unnamed. Fix: name one resize policy (letterbox vs crop) in the E1/E2 graph and stamp it on the receipt.

5. [§7 prerequisite vs `family`] Do not flip `family` to `image_to_video`. `FAMILY_REQUIRED_INPUTS["text_to_video"]` is `("text_prompt",)`; `image_to_video` is `("init_image",)` only (`nodes/_otr_video_engines/schemas.py:55-60`). `_required_inputs_for_engine` already unions family + engine `required_inputs` (`render_driver.py:205-221`). A family flip would make `text_prompt` optional at schema level on a prompt-owned lane. Fix: if a still peer is built, keep `family = "text_to_video"` and add `init_image` only on that peer's `required_inputs`.

6. [§2 + `otr_5080_haunted_12b_overnight.json`] Overnight loops are 28-37 clips. Enabling stills on that profile mints one Z-Image-Turbo still per beat before video. Wall-clock and unload are not budgeted. `free_otr_pipeline_residue` is LLM+Bark then surgical ComfyUI detach, and it never calls `unload_all_models` (`nodes/_otr_vram_levers.py:17, 128-226`). [ASSUMPTION] Klein GGUF / Z-Image actually leave the GPU before AnimateDiff loads. Fix: verify residue free after the image phase on both engines, or the 14.5 GB video budget is fiction.

7. [docs/GO_FORWARD_PLAN.md ~1019-1021] Still-in is PARKED ("not started; ship-readiness first"). The plan treats E1/E2/E7 as the first 1-3 arms. Fix: the still peer stays a lab engine until that park is lifted; E0+E9+E12 do not need that lift.

OPTIONAL / NICE-TO-HAVE:

- Stamp `adapter_strength` on the per-clip receipt (already has `domain_adapter_strength` per §2; confirm it records the env override, not only the constant).
- One compact still for this canvas (512x288 or 832x480) instead of downscaling 1472x832, if a still peer ships.
- Persist `audio_motion_profiles` pre-render only if E4 is actually scheduled; otherwise leave the post-render stamp alone.

CUT THESE:

1. E6 FreeInit. Multiplies sampling time; does not serve style, identity, or setting. Flicker is real, but this is a cost arm on a lane that already takes ~29 min/episode (§2). Safe to cut until a still peer is judged too flickery by eye.
2. E8 CameraCtrl / v2 motion LoRAs. README line 50: original motion LoRAs "really only work on v2-based" modules; lines 66-67: CameraCtrl is "specifically trained for v3" and "Gen2 only" (`ComfyUI-AnimateDiff-Evolved/README.md`). Gen2 replaces `ADE_AnimateDiffLoaderGen1` (`eng_ghost_signal.py:157`). §8 already forbids silent Gen2. Safe to cut from the first campaign.
3. E11 per-style SD1.5 checkpoints. Pack schema has no checkpoint/LoRA fields (`nodes/_otr_visual_styles.py`; `nodes/visual_styles/*.json`). This is a recipe fork plus ~2 GB per pack, against "one haunted lane" (§8) and "no checkpoint in the style schema" (§5B). Safe to cut until E0+still-peer fail on `anime`.
4. E13 style-aware roll. Hides packs the lane cannot draw; does not make the picture obey style. Contradicts the operator line in §5B. Safe to cut.
5. E7 SparseCtrl as a first-1-3 arm. New pinned pack, missing encoder, extra VRAM, 8 GB unknown. Operator-named, but E1 (stock VAEEncode, no new pack) answers "stills in" first. Keep SparseCtrl as a later named arm after E1 is judged.
6. E3 ledger prompt-scheduling as a first-wave arm. Crosses the v2 boundary (§7 E3 vs `ghost_signal_prompt.py:836-838`) and spends the 77-token window the 2026-08-22 finding already lost. Safe to cut until stills+adapter are judged.
7. §5C research questions 1-6 as a parallel campaign. That is a prompt-craft study. Operator rule: story quality is done; damping-word / medium-token findings already fence it. Keep E12 as one budgeted same-seed pair per style after E0, not a six-question research program.
8. IP-Adapter / PIA / Lightning / AnimateLCM -- already "not recommended" in §7. Keep them out.

[ASSUMPTION] ADE Gen1 `sample_settings` injection (E2) behaves as documented under `v3_sd15_adapter.ckpt` at strength 1.0 -- not established in this repo.
[ASSUMPTION] Klein 4B Q4 GGUF is released by `free_otr_pipeline_residue` before the haunted KSampler; verify against `nodes/_otr_vram_levers.py` + `nodes/_otr_image_engines/flux2_klein.py`.
[ASSUMPTION] No new OTR node class is required; ADE nodes are already registered by AnimateDiff-Evolved. Do not add sweep widgets to `OTR_VideoRenderBatch` / `otr_canonical.json` -- env/engine constants only, or positional widget drift (BUG-LOCAL-097).

ComfyUI contract: the haunted engine is already `@register`ed as `animatediff15_v3_haunted_video` (`eng_ghost_signal_official.py:119-152`). Graph mutations belong in `GhostSignalEngine`'s internal graph builder (`eng_ghost_signal.py` `GHOST_NODE_CANDIDATES` / omitted ADE sockets), not as new entries in OTR `NODE_CLASS_MAPPINGS`. A shipping `accepts_still` flip is the change that would leak into every queued haunted prompt via dispatcher + `IS_CHANGED` still-file dependency; a lab peer engine does not.
