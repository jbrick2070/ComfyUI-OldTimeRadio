VERDICT: build-ready as-is? no. R3/R8 would break stored v2 prompts, and R9 has no implementable prompt-derivation or seed-preservation path.

MUST-FIX BEFORE BUILD:

1. [R3, R8] Deleting v2 constants while “not touching” frozen v2 paths is impossible. `compose_ghost_prompt_v2` requires `GHOST_MODE_LAWS_V2` and the current exact object schema/version (`nodes/_otr_video_engines/ghost_signal_prompt.py:827-896`; `nodes/_otr_video_engines/ghost_signal_author.py:1106-1185`). A global `GHOST_AUTHOR_VERSION` bump makes every stored v2 object fail validation, and replay returns before reauthoring (`nodes/otr_shot_lock.py:3089-3124`). Keep all v2 symbols immutable. Add separate `GHOST_AUTHOR_VERSION_V3`, v3 hash keys, v3 object fields/schema, and version-dispatched validators/finalizers. Render dispatch must be: absent object -> v1 compatibility; schema 1 -> v2; schema 2/prompt_version v3 -> v3.

2. [R9] `--derive-prompt-version` has no code path. The bundle tool only implements `--derive-engine`, copying ledger bytes unchanged (`scripts/otr_freeze_replay_bundle.py:179-249`); import stamps only `replay_engine_override` (`nodes/production_ledger.py:681-687`); ShotLock then returns the frozen plan without authoring (`nodes/otr_shot_lock.py:3089-3124`). Add a validated manifest field such as `prompt_override`, import it as `replay_prompt_override`, and handle it before replay’s early return by rebuilding only each Ghost shot’s v3 prompt object. Preserve route, coverage, timing, and `render_request_hash`.

3. [R9] The stated seed-copy mechanism is wrong. Planned shots are initialized with `request_seed: 0` (`nodes/otr_shot_lock.py:2798-2823`), while the actual render seed is recomputed from `render_request_hash + shot_id` and ignores that field (`nodes/_otr_video_engines/render_driver.py:3749-3772`). Preserve the frozen `render_request_hash`; rebuild only the Ghost author hash and prompt-dependent actual-request hash. Verify resulting seeds against `video.manifest.clips[*].request_seed`, which is where the completed render records them (`nodes/_otr_video_engines/render_driver.py:6148-6153`).

4. [R2, R3] The metadata resolver is partial. Both `key_objects` and `story_brief_terms.setting` are legitimately empty on brief failure (`nodes/_otr_story_brief.py:419-457`), so indexing `[0]` cannot support R4’s promised empty-`key_objects` fallback. Define one total `resolve_crux_kernel(meta, measure)` contract: bounded `crux_subject`; else first non-empty key object plus setting; else bounded story brief/setting; else a named pre-LLM refusal. When `key_objects` is empty, use the resolved kernel as the beat subject. Replace undefined “location word” detection with normalized exact setting-phrase containment.

5. [R2] `crux_subject` lacks a producer and safety contract. `StoryBriefModel` does not declare it, the two JSON prompts do not request it, and only `story_brief` is validated nonblank (`nodes/_otr_story_brief.py:120-159`, `:185-253`). Add it with an empty default to both story schemas/prompts, `_success_delta`, and `_failure_sentinel`; bump `_PROMPT_VERSION`; bound it at the consumer to the promised 15 installed tokens. The existing prompt explicitly permits context names (`nodes/_otr_story_brief.py:201-212`), so run cast-name, lettering, and non-drawable checks over the kernel and beat subject, not only over the LLM leaf.

6. [R3, R4, R8] The v3 spec and hash identity are undefined and incomplete. Current specs require `motif_cue` and hash thirteen v2 fields (`nodes/_otr_video_engines/ghost_signal_author.py:697-776`). Hashing only “crux and beat nouns” omits other model inputs named by R4: setting, palette, key objects, selected subject, vantage, and lighting. Define a v3 spec with normalized scalar/list types and hash every causal input plus model/template/validator versions and style identity. Store the five final component strings in the durable v3 object so render does not reconstruct a different prompt later.

7. [R3, R4] Ownership of the beat subject contradicts itself. R3 deterministically selects one object, but R4 gives the model all objects and merely says it “may” name the selected subject; the existing validator checks neither subject coverage nor visible motion (`nodes/_otr_video_engines/ghost_signal_author.py:902-945`). Pass only the selected subject and require the returned clause to begin with that exact normalized phrase, or make the model return only a motion predicate and prepend the subject in Python. Validate the resulting complete clause before storage.

8. [R4, R5] The new mode model is not defined and conflicts with human validation. The current scheduler has separate character and bookend domains and bookend motifs keyed by `object/signal` (`nodes/_otr_video_engines/ghost_signal_author.py:472-521`, `:572-685`). “world / thing / world / hand” cannot reuse it unchanged, and applying `_HUMAN_WORDS` to every vantage would reject R5’s permitted crowd/crew cases (`nodes/_otr_video_engines/ghost_signal_author.py:346-355`, `:940-941`). Add v3-only mode constants and `schedule_ghost_modes_v3`; specify bookend modes/motif keys; add an explicit `allow_people` boolean derived from trusted beat terms; keep full-person words forbidden for `thing/hand` and conditionally allowed for `world`.

9. [R7] Fitting ownership and drop rules contradict the module boundaries. The pure composer cannot run the installed tokenizer, while `finalize_ghost_prompt_v2` explicitly never trims (`nodes/_otr_video_engines/ghost_signal_author.py:1295-1308`). R7 also drops a “second light term” although R1/R5 provide one, and dropping a subject’s second word can turn “data logs” into “data.” Make `compose_ghost_prompt_v3` assembly-only. Put token-aware fitting in a v3 author function that drops only whole optional units—light, optional vantage qualifier, then tail—and stores the fitted components. Never word-slice the kernel or subject; use a shorter validated motion candidate or fail before generation.

10. [R7] The banana route is missing from the v3 plan. The existing v2 finalizer applies it to both prompt and protected components, measures the transformed text, and prevents a second pass (`nodes/_otr_video_engines/ghost_signal_author.py:1317-1383`; `nodes/_otr_video_engines/render_driver.py:2871-2877`, `:2946-2950`). Give v3 an equivalent single finalizer and generalize `_ghost_v2_finalized` to a version-neutral flag. Test banana on/off, component survival, 69-token author admission, and 77-token render refusal.

11. [R1, R8] The style-slot budget does not match the actual API. `compact_style_cue` returns empty for `sci_fi_radio` and otherwise only 2-4 words, not the stated 4-9 tokens (`nodes/_otr_visual_styles.py:632-653`). No 3-5-word tail helper exists beside it. Define a pure `compact_style_tail` with exact source precedence and whole-word/whole-phrase behavior. Treat the default cue as an absent slot or explicitly change its contract; then measure all packs with the real tokenizer instead of budgeting a nonexistent non-empty cue.

12. [R8] `observability.prompt_slots` cannot become token counts. It is currently a list of slot names and is copied through the render trace under that contract (`nodes/_otr_video_engines/render_driver.py:2922-2945`, `:4981-5006`). Keep it as a list; add `prompt_slot_tokens: {slot: int}` and `prompt_dropped: [slot]`, then add both to the trace allowlist. Document that independently measured BPE slot counts are diagnostic and do not sum exactly to the joined-prompt count.

13. [R9, test matrix] The listed tests omit the failure boundaries introduced above. Add coverage for ordinary frozen v2 replay, derived v3 replay, absent/empty brief fields, every v3 bookend mode, conditional people allowance, exact hash invalidation for every model input, banana single-application, prompt-slot trace projection, unchanged `render_request_hash`, and actual same-seed equality. Existing replay tests currently assert no LLM resolution and byte-identical request hashes (`tests/test_canonical_replay.py:197-312`), so the derived-prompt case must be an explicit opt-in sibling, not a weakening of ordinary replay.

SHOULD-FIX:

1. [R4] Put episode-wide crux, setting, palette, and key-object context once in the batch header; per-row data should contain only id, selected subject, vantage, light, and trusted beat terms. Repeating unbounded arrays for every shot creates avoidable context growth; the producer currently places no list-size bounds on these fields (`nodes/_otr_story_brief.py:142-151`).

2. [R10] “The plate gains the crux in one line” ignores its explicit protected head and ordered drop algorithm (`nodes/_otr_video_engines/ghost_plate_prompt.py:38-160`). Specify whether `crux_kernel` is protected, where it enters `PLATE_DROP_ORDER`, and how its survival is receipted. A protected full style tail plus protected crux may exceed the existing 69-token target.

3. [R9] If pHash distinctness remains, name the exact algorithm, frame-selection rule, dependency, normalization, and missing-frame failure. The existing report computes MAD and first-frame triptychs only; it has no middle-frame, pHash, or VLM implementation (`scripts/otr_stillin_probe_report.py:88-109`, `:143-196`).

4. [R10] State that no ComfyUI node signature changes are intended, then prove the canonical workflow remains valid with JSON round-trip, validator, widget/input/link audits, and live `/object_info`. Run acceptance only through `workflows/otr_canonical.json`.

OPTIONAL / NICE-TO-HAVE:

Add a derived-bundle comparison report containing v2/v3 prompt text, five component values, dropped slots, final token count, `prompt_sha8`, frozen `render_request_hash`, and actual seed per shot.

CUT THESE (over-engineering):

1. [R9] Cut the local VLM subject-hit score unless a specific installed model, callable, output schema, synonym policy, and unload path already exist. The repository’s current probe has none, and the operator’s eye is explicitly the verdict; prompt receipts plus deterministic frame sampling remain sufficient evidence.

2. [R2] Cut the proposed generic “location word” classifier. Exact normalized containment of the selected setting phrase is deterministic, testable, and avoids an unbounded vocabulary subsystem.
