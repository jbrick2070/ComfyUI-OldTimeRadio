VERDICT: yes-with-fixes -- v2.1 already runs, but several stated control-flow invariants are false in the functions, and two remaining word-list collisions will drop whole episodes the same way the clock-hand did.

MUST-FIX BEFORE BUILD:
1. [Q2 / nodes/otr_shot_lock.py:1818-1856] The "one fresh whole-batch retry" cannot work as described. Attempt 2 calls `gen()` on the identical `build_batch_prompt(specs)` at `GHOST_BATCH_TEMPERATURE = 0.1` with no rejection text. A systematic validator false-positive therefore fails twice and always falls to `deterministic_fallback`. That is exactly the clock-hand incident (docs/2026-08-22-ghost-prompt-v2-publish-receipt.md:277-282). Fix: on attempt 2, append the attempt-1 parse/validate errors to the user message (or raise temperature). Do not code a second call that cannot differ.

2. [Q1 / nodes/_otr_video_engines/ghost_signal_author.py:306-310 + 872-873] `_HUMAN_WORDS` still contains `face`/`faces`. Whole-word match will reject a radio "dial face" / "clock face" the same way `hand` rejected a clock. This show's bookend vocabulary is dials (GHOST_BOOKEND_MOTIFS at :517-522; v1 framing even said "dial face centered" in ghost_signal_prompt.py:146). Fix: drop bare `face`/`faces`, or only reject them when not preceded by `dial`/`clock`/`watch`. Leave `portrait` if you want a face-request guard.

3. [Q5 / ghost_signal_author.py:276-281 vs 490-498] `_LETTERING_WORDS` contains `letter`/`letters` while `MOTIF_PROP_WORDS` contains `letter`. A legal motif is `a charcoal letter`; a leaf `the letter slides across the desk` is then rejected as "asks for lettering" and kills the batch. Fix: remove `letter`/`letters` from `_LETTERING_WORDS` (keep `lettering`, `caption`, `text`, `typography`). Pin a test that a prop-letter leaf passes in object/signal.

4. [Q2 / otr_shot_lock.py:1788-1815 + 1854-1856 + ghost_signal_author.py:896-910] Whole-batch reject plus unvalidated fallback is a pixel regression path, not a safe default. `_ghost_validate_batch` is not run on `deterministic_batch`. Object fallbacks are pronoun clauses (`"it tilts on the table..."`) with no concrete noun in the leaf. Receipt 4A measured v2 deterministic at 0/4, and the v2.1 6/8 arm was `writer_llm=8` with zero fallback (receipt:289-291). One bad leaf still throws away seven accepted LLM leaves. Fix (smallest): salvage valid rows, re-ask only the failed ids; per-beat `ghost_prompt` already stores independent `source` (GHOST_PROMPT_FIELDS / GHOST_AUTHOR_SOURCES). The "prompts must not depend on try count" rationale is already false: attempt 2 regenerates the good rows too.

5. [Q3 / ghost_signal_author.py:74-86, 414-458] Docstring claims "half of them show a person" and "cannot produce a run of three". Run-of-three is true by construction (cycle has no equal adjacent entries; only a bookend is flipped, and only to break a third). Half is false: `GHOST_CHARACTER_CYCLE` is period 4, so figure count is `ceil`/`floor` of n/2 depending on `char_offset`. For n%4 in {1,3} a seed starves below half; n=1 can be 0 figures; n=3 can be 1/3. The wiring fixture uses three character beats (tests/test_ghost_prompt_v2_lane.py:61-79). The unit test encodes the hole: `figures * 2 >= len(chars) - 1` (test_ghost_signal_author.py:197-205). Fix: either change the test+doc to "at least floor(n/2) except n%4 in {1,3}" or schedule so `figure` count is always `ceil(n/2)` (e.g. pin leftover slots to figure). Do not ship "half" as an invariant.

SHOULD-FIX:
1. [Q4 / ghost_signal_author.py:320-324 vs receipt:238] The reject list does not match the measured mush words. Receipt named `emblem`, `signal`, `field`, `waveform`; only `waveform` is banned. A leaf can still say `a brass emblem turns` or `an energy field pulses`. Add `emblem`/`emblems` and `field`/`fields` if the diagnosis is the spec. Do not add `signal` as a bare token until you check bookend copy ("broadcast signal") [ASSUMPTION: model may echo mode names].

2. [Q4 / grain] Rejecting leaf `grain` does not strip archival look. Pack cue is `compact_style_cue` of `positive_tail` (nodes/_otr_visual_styles.py:632-653); archival cue is the first two words "archival documentary", not "grain"/"texture" (nodes/visual_styles/archival_documentary.json:4). False positive: `wood grain on the desk`, `grain spills from a torn sack`. If archival grain matters, it already lives in the pack tail, not the leaf. Narrow `grain` to `film grain` / `grain overlay`, or drop it.

3. [Q1 / ghost_signal_author.py:192-217 vs 789-797] Batch rules still say object/signal show "no person". The validator and `test_a_clock_hand_is_not_a_person` accept `a shoulder passes through the light` and `a hand adjusts the knob`. At 512x288, `shoulder` is a person request. That is not "too permissive for synonyms"; it is a spec/code split. Pick one: (a) keep body parts, change rule 7 to "no full figure"; or (b) ban `shoulder`/`shoulders` only (does not re-break clock hand). Do not put `hand` back.

4. [Q5 / request hash] `GHOST_REQUEST_HASH_KEYS` (ghost_signal_author.py:686-690) omits style_id / pack cue. Replay compares only those 13 keys (otr_shot_lock.py:1979-1990) and does not re-run `candidate_fits`. A pack switch reuses a leaf that was admitted under a shorter cue; render then raises `GhostBudgetError` in `finalize_ghost_prompt_v2` (1229-1248). Put `style_id` (or the actual pack-cue string) in the hashed set, or re-fit on replay.

5. [Q5 / assert_shell_fits] ghost_signal_author.py:1298-1303 does not use the longest real motif. Hardcoded `charcoal overcoat` + `briefcase`; `MOTIF_GARMENT_WORDS` has `trenchcoat`/`greatcoat`, `MOTIF_PROP_WORDS` has `microphone`. A live row can exceed the "proven" shell. Enumerate max(len(colour)+len(garment)+len(silhouette)+len(prop)) or the actual longest formatted string.

6. [Q5 / deterministic_leaf] ghost_signal_author.py:956-960 returns `pool[start]` when every clause is in `used` -- a silent duplicate. LLM path forbids duplicates (otr_shot_lock.py:1804-1808); fallback does not. Raise, or add a second pool, rather than colliding.

7. [diagnosis / compose_ghost_prompt_v2:868-869 + GHOST_CFG=8.0] The "concrete nouns and nothing else" claim is not isolated. v2 positives are shorter (driver: 164-203 vs 208-317); negative path is unchanged (`compose_ghost_negative`, live at cfg 8.0 in eng_ghost_signal.py:91-96, 818). Relative uncond weight rose. `GHOST_PROMPT_TARGET_LOW/HIGH` (260-280) are unused by the v2 composer. A length-matched control is missing from any next coding step. video_art's leading cue is `video-art feedback style` (compact_style_cue on video_art.json:4) -- texture-first, CLIP-first. anime's cue is `anime style` (anime.json:4), which is not abstract-by-design; anime 0/8 is evidence for the noun/law regression, not a pack alibi.

OPTIONAL / NICE-TO-HAVE:
- Bump `GHOST_PROMPT_VERSION_V2` / `GHOST_AUTHOR_VERSION` so receipts partition v2 vs v2.1. Both still say v2 / `ghost_drawable_beat_v1` (ghost_signal_prompt.py:64, ghost_signal_author.py:67). Template hash already moved if rules/examples changed, so replay of v2 leaves is probably already broken; version stamps are for humans.
- Ban `he`/`she`/`body`/`human`/`lady`/`gentleman` only if you keep a word list; current set is closed-world.
- Put `GHOST_MODE_LAWS_V2` bytes into `_template_identity` so a longer law invalidates stored leaves before render blows the 77-token gate.

CUT THESE:
1. Per-row conversational salvage with a new temperature schedule and a second writer call per failed id -- one informed whole-batch retry, or salvage+one subset re-ask, is enough. Mixing both is extra VRAM on the 16 GB card before `_ghost_unload_writer`.
2. Expanding `_ABSTRACT_SUBJECT_WORDS` with `geometry`/`scanlines` taste extras before fixing `face`/`letter`. Those extras are not what 4A measured; the next dropped batch will be a radio dial face.
3. Re-deriving a figure quota test on only the 4-character TIMELINE (test_ghost_signal_author.py:140-144). That timeline cannot starve; it cannot catch Q3.

verify: `comfy.sd1_clip.SD1Tokenizer.tokenize_with_weights(..., return_word_ids=True)["l"]` entry layout. Not in this repo; `C:\Users\jeffr\Documents\ComfyUI\comfy\sd1_clip.py` was not readable here. tests/test_ghost_signal_author.py:679-687 pins the 75/76 -> (77,1)/(80,2) contract when that import exists. Do not change `entry[2] != 0` without re-running that test on the installed encoder.

[ASSUMPTION] Standard live Ghost episode is 8 beats with 4 character lines (TIMELINE + cadence docs). On that shape, figure count is exactly 2 and Q3 starvation does not fire. The wiring test's 3-character ledger is the shape that starves.
