VERDICT: no. The plan depends on cloud image/audio adapters and several partner-node inputs that do not exist in the pinned schemas.

MUST-FIX BEFORE BUILD:
1. [Global policies / adapter build scope] Cloud stills and cloud audio cannot be built from this plan as-is: image registry has only local image engines, no `cloud_recraft`, `cloud_ideogram_v4`, `cloud_seedream_2`, `cloud_nano_banana_2`, or `cloud_flux_pro` CAPABILITIES rows (`nodes/_otr_image_engines/registry.py:107`); audio registry has only local engines, no ElevenLabs/Sonilo/Stability rows (`nodes/_otr_audio_engines/registry.py:185`). `canonicalize_audio` and `canonicalize_image` still raise `NotImplementedError` (`nodes/_otr_shared/cloud_media_canonical.py:99`, `nodes/_otr_shared/cloud_media_canonical.py:106`). Concrete fix: add real cloud audio/image adapters, register/import them, add CAPABILITIES rows, and implement audio/image canonicalizers before treating these profiles as buildable.

2. [Video / cloud_wan_i2v] The plan and current adapter use the wrong Wan input contract. The pinned row requires `first_frame`, `model`, `prompt_extend`, `seed`, `watermark`, with optional `audio` and `last_frame` (`nodes/_otr_shared/partner_nodes.yaml:415`-`420`), but the adapter sends `image`, `prompt`, and `seed` only (`nodes/_otr_video_engines/eng_cloud_video.py:303`-`307`). Concrete fix: either re-pin the intended Wan node that actually accepts `prompt`, or map to the pinned names and supply `model`, `prompt_extend`, and `watermark`.

3. [Video / cloud_seedance_2] The profile says Seedance takes reference image, audio-ref, and a motion prompt, but the pinned schema exposes only `model`, `seed`, and `watermark` (`nodes/_otr_shared/partner_nodes.yaml:297`-`301`). The adapter currently invents `image` and `audio` kwargs (`nodes/_otr_video_engines/eng_cloud_video.py:267`-`279`). Concrete fix: re-pin a Seedance node/schema that exposes image/audio/prompt inputs, or mark this row unsupported until the live schema proves those names.

4. [Stills / cloud_nano_banana_2] The “feed canonical portrait as reference” instruction is not implementable against the pinned row. `cloud_nano_banana_2` has required `model`, `prompt`, `response_modalities`, `seed` and optional `system_prompt`, `temperature`, `top_p`; no image/reference input exists (`nodes/_otr_shared/partner_nodes.yaml:231`-`239`). Concrete fix: use/re-pin a Gemini image-edit node with an IMAGE input, or remove the reference-image identity guarantee from this row.

5. [Global policies / SEED] “true for all image + video rows” is false. `cloud_kling_lipsync` is a video row with `seed_supported: false` (`nodes/_otr_shared/partner_nodes.yaml:186`-`214`). Concrete fix: explicitly exclude seed for lipsync in profile, cache-key construction, and ledger stamping.

6. [Global policies / NEGATIVE/AVOID] The plan assumes profile-level negative terms are universally supported, but most pinned rows do not expose `negative_prompt`. Recraft does (`nodes/_otr_shared/partner_nodes.yaml:265`-`268`); Ideogram V4, Seedream, Nano Banana, Flux2 Pro, and the video rows generally do not (`nodes/_otr_shared/partner_nodes.yaml:126`-`147`, `310`-`330`, `216`-`239`, `95`-`117`). Concrete fix: per row, either map to a real negative input or inline avoid terms into `prompt`; include that final prompt in the cache key.

7. [Global policies / RequestCacheKey + ledger] The plan says seeds land in `RequestCacheKey` and the ledger, but the invoke path does neither. `invoke_partner_node` reserves, calls, bills, and logs only node/provider/estimate/path/content_type (`nodes/_otr_shared/cloud_media_invoke.py:562`-`603`). `RequestCacheKey`, `cache_lookup`, and `cache_store` exist but are not used by adapters (`nodes/_otr_shared/cloud_media_cache.py:67`, `nodes/_otr_shared/cloud_media_cache.py:179`, `nodes/_otr_shared/cloud_media_cache.py:202`). Concrete fix: each adapter must build the request key pre-submit, cache-hit before invoke, cache-store after canonicalization, copy into episode, and ledger `seed`, `cache_key`, params, and input hashes.

8. [Video / cloud_kling_lipsync] “resolution: 720p ALWAYS” is not an input on the pinned lipsync row; it only accepts `video`, `audio`, `voice_language` (`nodes/_otr_shared/partner_nodes.yaml:201`-`205`). Concrete fix: make 720p a base-clip/canonicalization requirement before lipsync, not a lipsync partner parameter.

SHOULD-FIX:
1. [Global policies / RESOLUTION] “request the ROLE CANVAS exactly” is underspecified for COMBO-based nodes. Recraft uses `size: COMBO` (`nodes/_otr_shared/partner_nodes.yaml:269`-`273`) and Ideogram uses `resolution: COMBO` (`nodes/_otr_shared/partner_nodes.yaml:142`-`146`), while combo options are excluded from the pin (`nodes/_otr_shared/partner_nodes.yaml:2`). verify: allowed combo values for every role canvas; add a quantize/pad/canonicalize rule when exact canvas is unavailable.

2. [Stills / cloud_flux_pro] The profile says reuse local FLUX prompting unchanged, but the pinned node also requires `prompt_upsampling: BOOLEAN`, `width`, and `height` (`nodes/_otr_shared/partner_nodes.yaml:112`-`117`). Concrete fix: specify `prompt_upsampling` policy, probably `false` if the writer prompt must remain the content source, and include dimensions/upsampling in cache params.

3. [Voice / cloud_elevenlabs_tts] The model/output format/text-normalization values are dynamic combos, and the pin excludes options (`nodes/_otr_shared/partner_nodes.yaml:57`-`65`, `nodes/_otr_shared/partner_nodes.yaml:2`). verify: exact accepted values for `eleven_flash_v2_5`, multilingual v2/v3, `OFF`, and 44.1k PCM before coding constants.

4. [Music / cloud_stability_audio] “prefer for longer beds” depends on provider duration limits and selected model behavior, but `duration` is optional and model is required (`nodes/_otr_shared/partner_nodes.yaml:382`-`388`). verify: max duration, model id, and whether long beds are accepted before making the >80s router rule automatic.

OPTIONAL / NICE-TO-HAVE:
Add a small schema conformance test that walks every profile row and asserts every emitted partner kwarg is declared in `partner_nodes.yaml`; it would catch the current Wan/Seedance mismatches immediately.

CUT THESE (over-engineering):
1. [Voice] Cut separate profile logic for `_flash` until the adapter exists. Both ElevenLabs rows pin the same class and same input shape (`nodes/_otr_shared/partner_nodes.yaml:8`-`72`); one adapter with a row/model selector is enough for the first build.