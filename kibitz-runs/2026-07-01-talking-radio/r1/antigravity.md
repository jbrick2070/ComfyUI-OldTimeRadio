VERDICT: no. The upsampler lacks downscaling for the base pass (causing VRAM OOM) and lacks a clear re-concatenation strategy for the audio/video latents in the refine stage.

MUST-FIX BEFORE BUILD:
1. [SUB-PLAN A] The plan assumes the base pass is 512x288, but `nodes/_otr_video_engines/eng_ltx_av.py#L771-L777` runs at the full requested canvas size (default 832x480, up to 1472x832). Upscaling a full-res base pass will exceed the 14.5 GB ceiling [ASSUMPTION] and crash with OOM. Fix: When `OTR_LTX_AV_UPSCALE=1`, divide the base pass spatial dimensions (`width` and `height`) by 2 in `_build_graph` before feeding them to `emptylatent` or `i2v`, so the base pass is small (e.g. 416x240) and the upscale/refine stage brings it back to the target resolution.
2. [SUB-PLAN A] LTX-AV UNET expects a concatenated AV latent via `LTXVConcatAVLatent`. The plan separates the video latent using `LTXVSeparateAVLatent` before running the upsampler but does not specify how the upscaled video latent is re-conditioned with the audio latent for the second refine sample. Fix: After `LTXVLatentUpsampler` runs on the separated/upscaled video latent, run `LTXVConcatAVLatent` again with the upscaled video latent and the original audio latent before passing the result to the refine `SamplerCustomAdvanced`.
3. [SUB-PLAN A] The plan loader requires `LatentUpscaleModelLoader` but does not define which upscale model file to load, how it is configured via the environment, or its file size validation floor. Fix: Define `OTR_LTX_AV_UPSCALE_MODEL` in the env variables (with a default upscale model path), add it to `_weight_paths` with a sanity floor (e.g., 50MB) [ASSUMPTION], and map it in `_node_candidates`.
4. [SUB-PLAN A / Invariants] The plan says `OFF` must be byte-identical to today. However, it also suggests wiring `LTXVCropGuides` even in the single-pass path if it changes framing/sync. This would break byte-identity and cause regression tests to fail. Fix: Restrict `LTXVCropGuides` strictly to the upscale path (`OTR_LTX_AV_UPSCALE=1`), or if it is correctness-relevant, update the regression tests and accept the change to the baseline single-pass trace.

SHOULD-FIX:
1. [SUB-PLAN B] The plan refers to "the 'no human' / 'no baby' negatives" for the console face. But in `nodes/otr_meta_brief_image_prompt.py#L170-L172`, the `console_face` negative (`RADIO_CONSOLE_NEG`) has "human, person..." but no "baby", while `radio_head_person` (`RADIO_HEAD_PERSON_NEG`) has "baby". Fix: Explicitly specify that the "no baby" negatives are for the announcer `radio_head_person` style, while `console_face` uses `RADIO_CONSOLE_NEG` to keep humans completely out.
2. [SUB-PLAN B / Ask for the panel #2] The plan asks if changing `build_radio_host_prompt` risks breaking the mesh/faceless-radio invariant elsewhere. Fix: Confirm in the doc that it is safe: `nodes/otr_meta_brief_image_prompt.py#L706-L732` shows `_mesh_fodder_subject` uses `radio_form_from_meta` which generates a faceless prompt (e.g. "a vintage walnut table radio"), so the face-related terms from `build_radio_host_prompt` never reach the 3D mesher.
3. [Ask for the panel #4] The plan asks why lip-sync would fail on a non-face radio still. Fix: Point out that since LTX-2.3 `LTXVImgToVideo` has no explicit landmark/face detector [ASSUMPTION], it relies entirely on the UNET's learned representation of mouth areas. If the anthropomorphic radio lacks a clear mouth-like region (grille or dial), the UNET will only produce general motion/drift rather than synchronized mouth movement.

OPTIONAL / NICE-TO-HAVE:
1. [SUB-PLAN C] Provide a script/command to quickly run the eyeball test under `OTR_FORCE_ENGINE_MAP` to ensure developers don't have to manually edit dispatch rules to route bookends to `ltx_audio_in`.

CUT THESE (scope / over-engineering):
1. [SUB-PLAN A] `ResizeImagesByLongerEdge` from the candidates. The engine already accepts exact, pre-snapped canvas dimensions (`_render_dims` in `nodes/_otr_video_engines/eng_ltx_av.py#L771-L777`) and the input image is already sized, making dynamic image resizing redundant.
2. [SUB-PLAN A] `LTXVPreprocess` from candidates unless it is proven that the upscale model cannot function without it. ComfyUI's standard `LTXVImgToVideo` handles VAE encoding directly, and adding preprocessing adds unnecessary graph complexity and VRAM overhead.
