VERDICT: no. The plan’s main arc misses the likely producer-side gate: still_pan is selected as a still consumer, but the image dispatcher will not mint stills for it because StillPanFamily lacks accepts_still=True.

MUST-FIX BEFORE BUILD:
1. [MECHANISM] Defect: the chain starts too late at “dispatcher mint -> ledger row”; it omits the earlier coverage gate that decides whether the dispatcher mints anything. In nodes/otr_image_gen_dispatcher.py:287-339, _still_needed_for_role only mints if engine_consumes_still is true; engine_consumes_still falls back to required_inputs containing init_image. In nodes/_otr_video_engines/cheap_families.py:204-221, StillPanFamily uses_still=True but has required_inputs=("text_prompt",) and no accepts_still=True; StillFlatFamily has accepts_still=True at nodes/_otr_video_engines/cheap_families.py:225-241. Concrete fix: make the review trace start with role -> selected video engine -> engine_consumes_still -> dispatch skip/mint before ledger lookup. The minimal likely code fix is StillPanFamily.accepts_still = True, plus a test that _still_needed_for_role(... still_pan ...) is True.
2. [REGRESSION WINDOW #1] Defect: the “did old flux_still SELF-GENERATE a Flux image?” premise is contradicted by code. git show c1132196 for nodes/_otr_video_engines/cheap_families.py shows FluxStillFamily inherited _CheapFamilyBase, uses_still=True, and render_clip only animated asset_refs or fell back to lavfi floor; it did not self-generate Flux. Concrete fix: replace that branch with “did the rename preserve the old missing accepts_still capability, leaving dispatcher coverage false for still_pan?”
3. [WHAT TO DETERMINE A] Defect: the key-space trace is incomplete because it does not include “no row was intentionally skipped” as a first-class outcome. nodes/otr_meta_brief_image_prompt.py:249-360 and :850-940 can emit scene targets/objects, but nodes/otr_image_gen_dispatcher.py:431-441 can skip them if the selected video engine ignores init_image. Concrete fix: require the diagnosis to report, for the failed beat, whether the object existed in image_prompts_json and whether dispatch skipped it due to _still_needed_for_role.
4. [SYMPTOM] Defect: the failed episode ledger/trace is not listed as an input to the investigation, and I cannot verify it in the repo tree. verify: exact failed ledger JSON, video_policy_json, image_policy_json, image_prompts_json, dispatcher report, and render trace for pending_20260630_054646. Concrete fix: add those artifacts as required evidence before diff-hunting commits.
5. [WHAT TO DETERMINE D] Defect: “ALL still-carrier legs” is too broad and mixes engines with different coverage declarations. ltx_audio_in explicitly declares accepts_still=True in nodes/_otr_video_engines/eng_ltx_av.py:851-858; still_flat does too in cheap_families.py:232-241; still_pan does not at cheap_families.py:204-221. Concrete fix: split the diagnosis by engine: still_pan, still_flat, ltx_audio_in, ltx_video, and visualizer/procedural lanes.

SHOULD-FIX:
1. [REGRESSION WINDOW #3] Defect: the C3/C4 unregister/menu-filter suspects are currently bloat for this symptom. The grounded mismatch is a capability contract gap between StillPanFamily and ImageGenDispatcher, not a registry visibility issue. Concrete fix: demote C3/C4 unless the failed policy proves a formerly hidden engine became selected.
2. [MECHANISM] Defect: “missing_scene_still -> cheap family synthesizes dark floor” is accurate but framed as the root. render_driver.py:928-958 only consumes the ledger; cheap_families.py:128-140 then draws the dark floor when no still exists. Concrete fix: state that render_driver is the visible failure point, while dispatcher coverage is the likely root locus.
3. [INVARIANTS] Defect: the plan says workflow JSON is an invariant but does not tie the failed leg back to the saved workflow. workflows/otr_scifi_16gb_full.json node 87 currently shows visualizer-oriented saved widgets, so a still_pan failed leg likely came from a sweep/override path. [ASSUMPTION] Concrete fix: require recording the runtime override source that selected still_pan.

OPTIONAL / NICE-TO-HAVE:
- Add a small contract table in the plan: engine_id, uses_still, accepts_still, required_inputs, dispatcher mints?, render_driver lookup key.
- Add a regression assertion mirroring tests/test_video_cheap_render.py:90-99 for still_pan, not only still_flat.

CUT THESE (scope / over-engineering):
1. [REGRESSION WINDOW #3] Cut C3/C4 scaffold/menu-filter investigation until a policy artifact implicates engine selection. It is safe because the code path already shows still_pan can be selected yet not receive a minted still.
2. [WHAT TO DETERMINE D] Cut the broad “all still-carrier legs” sweep from round 1. It is safe because still_flat and ltx_audio_in already declare accepts_still=True; start with still_pan and only widen if evidence remains unexplained.
3. [REGRESSION WINDOW #4] Cut materialization-path speculation as the primary arc until dispatcher skip is ruled out. If _still_needed_for_role returns false, there is no row or episode copy to materialize.