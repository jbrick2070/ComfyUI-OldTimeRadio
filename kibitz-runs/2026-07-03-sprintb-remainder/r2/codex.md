VERDICT: yes-with-fixes. Core seams exist, but the plan leaves render-driver routing, policy shape, and deleted pooling behavior underspecified.

MUST-FIX BEFORE BUILD:
1. [Build steps 1/2] `still_word` will not receive the minted scene still if only registered as a `still_flat` sibling. `render_driver.build_request_from_shot()` special-cases only `"still_pan", "still_flat", "ltx_audio_in"` for scene-still init routing (`nodes/_otr_video_engines/render_driver.py:1044`); new `static_image_gen` engines otherwise keep the default portrait/empty init (`nodes/_otr_video_engines/render_driver.py:956-958`). Fix: add `still_word` to that branch or replace the hard-coded name check with a capability-based `static_image_gen + accepts_still` path, then test request `asset_refs.init_image`.
2. [Build steps 1 / No fallbacks] Copying `StillFlatFamily` behavior directly can silently render the dark lavfi floor when the still path is absent or missing on disk (`nodes/_otr_video_engines/cheap_families.py:129-140`). That masks exactly the wiring failure this feature is meant to expose. Fix: `StillWordFamily.render_clip()` must fail loud when `asset_refs` has no existing still, or the request builder must guarantee/validate the still before render.
3. [Build steps 2 / Open question] The data shape for “per-role VIDEO engine selection” is not specified. `OTRMetaBriefImagePromptGen.generate()` currently accepts only `image_policy_json` and extracts `aspects`, `mesh_fodder_roles`, and `talking` (`nodes/otr_meta_brief_image_prompt.py:1384-1391`); the usable video selection is forwarded inside `image_policy["video_models"]` by `OTR_ImageDirector` (`nodes/otr_image_director.py:375-382`). Fix: define a pure helper that parses `image_policy_json["video_models"]` and resolves roles via `role_slots.engine_id_for_role()` (`nodes/_otr_shared/role_slots.py:104-123`), then pass a `still_word_roles`/resolver into `derive_image_prompts()`.
4. [Build steps 2] Do not reuse `_compose_char_scene_prompt()` as the word-driven helper. It calls the writer LLM when available and falls back to a character template (`nodes/otr_meta_brief_image_prompt.py:686-733`), which contradicts “PURE + deterministic” and “missing beat line fails LOUD.” Fix: add a separate deterministic `compose_still_word_prompt(meta, role, beat_line)` that raises on blank text/title and computes `prompt_hash` after final no-text/grade clauses.
5. [Build steps 2/5] “character beats in POOL mode” and “pool-mood SET” are not buildable against the current live model. Pooling was removed; code comments state every beat is per-beat (`nodes/otr_image_director.py:383-384`, `nodes/otr_shot_lock.py:723-725`, `nodes/_otr_video_engines/schemas.py:112-119`). Fix: delete pool-mood from v1 and mark it deferred unless a new pool-mode schema is explicitly reintroduced.

SHOULD-FIX:
1. [Build steps 1] Registration needs all three places, not just `cheap_families.py`: class with `@register`, `__all__`/package import coverage via `nodes/_otr_video_engines/__init__.py`, and a `CAPABILITIES["still_word"]` row. Tests enforce registry/capabilities equality (`tests/test_capability_profiles.py:215`).
2. [Verify-at-build] Add focused tests before GPU proof: combo exposure (`tests/test_tested_only_dropdown_gate.py` pattern), cheap render static-command parity (`tests/test_video_cheap_render.py:67-99`), dispatcher still consumption (`tests/test_image_platform_c1.py:527-536`), and render-driver init routing for `still_word`.
3. [Workflow JSON] [ASSUMPTION] If v1 is intended to run in the real production workflow, node 87 must be changed from current saved values (`viz_green`, `humo_14B_169`) to `still_word`; otherwise the code is only selectable, not active. Current node 87 is in `workflows/otr_scifi_16gb_full.json`.

OPTIONAL / NICE-TO-HAVE:
- Add report provenance like `source=still_word_line` / `still_word_title` so output diffs show the branch taken.
- Add a max-length/cleanup pass for spoken lines before prompt assembly.

CUT THESE (over-engineering):
1. [Build steps 4] Cut any registered `word_razzle` stub. A registered non-rendering engine becomes selectable because the registry is the menu (`nodes/otr_video_director.py:146-159`). Keep only a name constant or doc note until the animated path is actually implemented.
2. [Build steps 2/5] Cut pool-mood SET from v1. The schema and runtime no longer support pool mode, and rebuilding it just to defer it increases blast radius.