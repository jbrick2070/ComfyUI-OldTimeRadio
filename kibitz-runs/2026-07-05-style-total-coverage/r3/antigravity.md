VERDICT: build-ready as-is? no. Eager loader directory sweep breaks build order between Chunks A1 and B.

MUST-FIX BEFORE BUILD:
1. [Section 2] loader eager-sweep directory crash: Chunk A1 implements the schema v2 validation which fails v1 packs, but leaves the 4 non-default packs in v1 format until Chunk B. Because `_load_all()` in `nodes/_otr_visual_styles.py` eagerly sweeps and validates all JSON files in the `nodes/visual_styles/` directory, the test suite or any style resolution in Chunk A1 will crash on the v1 packs. Concrete fix: Upgrade all 4 non-default JSON packs to the v2 schema with empty/default values in Chunk A1, or merge Chunk B's JSON pack upgrades into Chunk A1's commit.
2. [Section 1a, 1b & 2] Chunk C fields schema validation loop: Chunk C introduces 3 new fields (`still_word_typography`, `still_word_backdrop`, `still_word_title_mood_style`) to the visual-style packs, but Chunk B authors the non-default packs with only 11 fields. If the v2 schema in Chunk A1/B does not require the Chunk C fields, then Chunk C will break validation for all 4 packs from Chunk B unless they are updated again. Concrete fix: Define the final v2 schema to include all 14 fields (11 from A1/A2 + 3 from C) from the start of Chunk A1, and have `sci_fi_radio.json` and the non-default packs include them in Chunk A1 and B respectively.
3. [Section 1b & 5] motion selector reference to retired constant: In `nodes/_otr_video_engines/render_driver.py` line 614, key selection in `_ltx_motion_role_key` checks if `_open_key` is in the `_LTX_MOTION_PROMPT_BY_ROLE` dictionary. If `_LTX_MOTION_PROMPT_BY_ROLE` is retired (per Section 1a), `_ltx_motion_role_key` will cause a NameError or reference an obsolete constant. Concrete fix: Update `_ltx_motion_role_key` to check against a static Python set of expected motion keys `{"music_open", "music_close", "music_inter", "announcer"}` instead of `_LTX_MOTION_PROMPT_BY_ROLE`.

SHOULD-FIX:
1. [Section 1a & 1b] ambiguity of style-look insertion for `_build_char_scene_request`: [ASSUMPTION] The subplan states `portrait_instruction_look` lives ONLY in `_build_char_prompt_request` but that `_build_char_scene_request` has a style-look insertion specified separately at build. The subplan fails to define which `VisualStyle` field represents this scene-specific look insertion or how it preserves byte-identity. Concrete fix: Clarify whether `_build_char_scene_request` uses `portrait_instruction_look` or if an additional schema field (e.g. `scene_instruction_look`) needs to be defined.
2. [Section 1b & 5] missing parameter and double-resolution of style in fallback image prompt: The function `compose_image_prompt_fallback` is a caller of `_style_anchor_for_aspect` which now takes the resolved style, but `compose_image_prompt_fallback` does not take or resolve a style object. Concrete fix: Add `style` parameter (default `None`) to `compose_image_prompt_fallback` and call `_resolve_style(meta, style)` inside it to pass `style=_style` to `_style_anchor_for_aspect`.
3. [Section 1b & 5] unspecified `prompt_field_source` mapping values: The subplan introduces a new provenance key `prompt_field_source` but does not define what values this string should take for the different prompt paths (e.g., `"motion_registers"`, `"open_subjects"`, etc.) in the video lane. Concrete fix: Specify the exact string mapping values for `prompt_field_source` for each of the dispatch arms in `build_request_from_shot`.

OPTIONAL / NICE-TO-HAVE:
- [Section 1a] Immutable mapping optimization: Storing `open_subjects` and `motion_registers` as `types.MappingProxyType` instead of raw `dict` in the frozen dataclass `VisualStyle` to enforce true immutability at the type level.

CUT THESE (over-engineering):
- None.
