# Clean audio R1 - Astra judgment

Both yes-with-fixes. Each claim checked against current Windows code. No clean-audio runtime edits made.

- AgY must 1/2 (launcher count/seed removal; skip silent correlation): already explicitly required in input, not missing. Final now reduces redundant silent cases to one and gives exact three-case receipt fields and complete lag-dependent assertion bypass.
- Both C1 not enumerated: confirmed omission from this fresh standalone brief (present in parent plan). Added all seven exact stores, not merely the two named by AgY. No unrelated hunt.
- AgY noise import/step numbering: already specified; keep exact whole-block removal. Variant check added explicitly; driver's actual current check reports no committed variants, so reject implied multi-variant validation.
- AgY zero width always dual-mono: MISREAD for stereo inputs. `_mono_to_stereo` preserves first two channels; width=0 skips decorrelation. Correct tooltip/helper prose.
- Cursor narrow vs dry: accepted need for explicit sound scope. Latest operator says clean audio, beyond the preceding random-noise wording. Final clearly chooses dry defaults (all coloration off), retains optional corrective LPF without inventing artifact-free upstream voices. No implied operator acceptance of every Bark artifact.
- Cursor zero-master/min100-sample branch: confirmed at scene:289-304,380-381,1757 and by actual driver's zero-input probe. Preserve trimming; silent input duration is not padded input duration. One silence+opening case suffices, with two chirps retaining timing proof. Reject 1.50-s silent sample-delta requirement: trim/crossfade clamp means it is not the chirp offset.
- Cursor surgical JSON change/count: confirmed; last_link_id is not a count. Only six widgets plus one current description phrase change, no reserialization.
- Cursor audio fans: confirmed links15->node12 SignalLostVideo and272->node94 SceneAwareScopes. Drier audio can alter visual content; qualification is CPU audio only. Foley/replay/master/scheduler scope explicitly recorded.
- Cursor header/canonical prose: adopt tiny descriptive corrections in already-read __init__.py and canonical extra.info.description, no import/mapping changes. Do not sweep other old prose.
- Cursor A/B listening: no synthetic chirp listening presented as episode acceptance. Real current zero-input diagnostic is stronger for additive-noise causality; actual fresh production/listening qualification remains outside this no-server/GPU review.
- Cursor remove LOC estimates: declined; operator explicitly asked estimated removable runtime lines. Estimates remain separate from net diff and test-code growth; they do not drive removal decisions.
- Cursor do not touch room_tone gate test / no DSP framework: agreed. This test covers supplied quiet audio, not automatic roomtone generation.
- Cursor assumed no remaining RNG: targeted source scan of the three route implementations locates noise only in the two retired functions; post-edit independent processes still must prove deterministic output. Cache/scheduler internals and full foley route remain unreviewed. KEEP override refers to earlier review-plan hold, not a newly invented standing operator ruling.

Driver additions: actual pre-change diagnostic confirms nonzero scene and subtle-tape output from zero inputs; all-off enhancement preserves zero. Replay uses frozen master copy/hash, so existing masters remain unchanged and a future normal fresh assembly is required. No cache/artifact purge.
