# Clean audio R2 - Astra judgment

Both yes-with-fixes. Runtime edit map confirmed. Distinct claims grounded:

- AgY tape-loop ledger pollution: CONFIRMED at AudioEnhance:489-516 and _otr_ledger.in_flight_ledger_path:538+. Move once into main before any ledger creation, require actual missing path, keep test-mode protection; no metadata mocks or active fixture mutation.
- AgY count/seed/stub removal/IDs/tooltips/helper prose: already required in input. Add explicit episode names; same-chunk launcher and sequencer-ledger patch removals remain mandatory. Optional second silent case already removed. Variant soft-skip was measured by driver, not guessed.
- Cursor clone Scene not factory: input explicitly says clone Scene output, so MISREAD as a missing behavior. Lock the actual tensor expression to eliminate ambiguity. Both rates must be 48000 before equality.
- Cursor silence rank/duration: clarify the existing factory's unchanged 3-D nonempty shape and voice/cue lengths. Keep min100 trim; no padded-duration golden.
- Cursor receipt/IDs/seed: already specified, explicit shared keys now given. Cursor hiss keys+block, surgical JSON, no last_link_id count, header stores: all already mandatory, not additional defects.
- Cursor decode mean can hide anti-phase: CONFIRMED implementation uses channel mean for correlation. Silent proof must inspect raw PCM across all channels first. Adopt.
- Cursor loop once: adopt alongside AgY stronger pre-ledger placement. Actual methods still run; one four-mode loop per child.
- Cursor validator skip and semantic round-trip: confirmed real validator. Parent plan already gives exact True/True/no-prompt call; add it to standalone C4 brief. No serialized-file rewrite or byte-equal round-trip was proposed.
- Cursor timeout180: UNVERIFIED need; retain120 per child. Driver measured complete existing two-process test at12.11s. Increase only on observed timing, not speculative fixture count.
- Cursor optional public display-name rename: decline scope/compatibility churn; node still provides optional spatial effects. Fix descriptive defaults, retain identity and registration.
- Both no second harness, no snapshot migration/cache purge/second silent timing: agreed; already prohibited. Old supplied-quiet-audio gate test stays.

External private imports, foley/video appearance, Comfy execution cache and GPU execution remain unqualified. R3 focuses only wiring/receipt integration and the current finished preparation files, not another candidate hunt.
