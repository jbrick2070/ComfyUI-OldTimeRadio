# Clean audio R3 - Astra judgment

Both yes-with-fixes, primarily repeating requirements already present. Current source and the cited test seams grounded; no runtime cleanup edits.

- Both top-level public_tape_checks location/format: reasonable explicit contract; fix dict schema and launcher expectations. Not a production defect.
- Both tensor clone / same-chunk helper patch deletion / count3 and seed removal / hiss import / default tooltips / zero-width prose / environment log removal: ALREADY REQUIRED. No extra findings or dead code retained. Add exact tensor clone wording, not a second implementation.
- Cursor silent window empty and all lag uses: CONFIRMED consequence of min100 trim; input already requires bypassing correlation and every lag-derived assertion. Emphasize branching BEFORE slicing the empty template, not merely skipping the final assertion. Keep repeated timing tuple comparison, which has no lag dependency.
- Cursor direct tape call and pre-ledger order: CONFIRMED, already described as a public-method probe. Make actual FUNCTION invocation explicit so route.run cannot inject saved off widgets. Under test mode and no singleton, _otr_ledger returns None rather than walking production files; assert it, no mock.
- Cursor mono chirp cannot detect width: CONFIRMED at _stereo_decorrelate:129-136, Side is zero. Add one asymmetric stereo direct-default call in the pre-ledger scope, plus narrow schema/canonical clean-map assertions. This also closes the driver's independently identified omitted-default proof gap. Do not duplicate the all-node signature sweep.
- Cursor only surgical JSON/two hunks and same code+defaults chunk: already required, preserve. No schema mutation beyond named defaults/minimum. No use of last_link_id as count.
- Cursor min100/singleton/restart/replay facts: confirmed code. Preserve trim to avoid empty-bus shortfall; no promise existing masters or resident code become clean. Future normal production needs fresh process, outside this bounded review.
- Cursor stale replay comment: adopt tiny prose fix in owned scene file. Public display-name/node_list rename declined: optional spatial functionality still exists and identity is out of scope.
- Cursor global sweep/round-trip in child: MISREAD if claimed required inside the audio child; these are separate existing qualification gates. Final explicitly keeps them separate. Existing real validator probe was quick and passed; no in-child speed claim or server is needed.
- AgY suggested monkeypatch cut: agreed, input already forbids metadata mocks. No such code exists in the preparation.

`scoped_final.md` is the accepted C4 specification. Final round input integrates it with the already-reviewed parent C1-C3 plan; the merged bytes are saved as BOTH parent r3/final.md and this r3/final.md. One shared R4 with the same two lanes reviews that integration. The parent and C4 each have their real R1-R3 reviews; neither inherits fictional prior coverage. Fourteen total Kibitz lane calls are expected after the two shared R4 calls complete.
