VERDICT: build-ready as-is? no. Multiple implementability blocks exist, including missing StoryBlueprint integration in the frozen OutlineRequest, undefined OTR_VisualStyleDirector node inputs/outputs, lack of a source bank selection widget, and prompt contradictions from hardcoded "science-fiction" system prompts.

MUST-FIX BEFORE BUILD:
1. [V1 Contract Shape / First Coding Plan Shape] Missing StoryBlueprint integration in OutlineRequest: The plan states that public-domain adaptation will generate a StoryBlueprint and "feed [the] existing outline path." However, the outline planning authority OutlineRequest (defined in nodes/_otr_outline.py:L283-L320) is a frozen dataclass with no input field or parsing logic for a blueprint. Similarly, generate_outline does not accept a blueprint parameter. Fix: Modify OutlineRequest in nodes/_otr_outline.py to add an optional `blueprint: Optional[StoryBlueprint] = None` field, and update `_build_user_prompt` to append the blueprint's premise, setting, characters, and required beats to the LLM user prompt if present.
2. [First Coding Plan Shape] Undefined OTR_VisualStyleDirector node interface: Step 6 of the Coding Plan introduces OTR_VisualStyleDirector as a new registered ComfyUI node, but the plan completely omits its class signature, INPUT_TYPES, and output sockets. Fix: Define the node structure: INPUT_TYPES must expose a `style` combo widget containing ["cinematic_35mm", "archival_mono", "anime"], returning a single ("STRING",) matching RETURN_TYPES = ("STRING",) and RETURN_NAMES = ("visual_style_policy_json",).
3. [First Coding Plan Shape / V1 Contract Shape] Hardcoded "science-fiction" genre constraints in outline and grading system prompts: While step 3 of the Coding Plan introduces source-label plumbing to replace the fallback wording "Science story" in outline prompts, it fails to address the system prompts in nodes/_otr_outline.py (L539: "You are a story editor for short science-fiction audio dramas grounded in real science."), nodes/_otr_pitch_room.py (L185), and nodes/_otr_story_select.py (L165). [ASSUMPTION] For non-sci-fi source modes (like public-domain historical or gothic adaptation), these hardcoded instructions will conflict with the input text and force the LLM to hallucinate sci-fi tropes. Fix: Dynamically parameterize the system prompts in these modules to substitute the genre and grounding constraints depending on the incoming packet's `source_kind` or `source_label`.
4. [First Coding Plan Shape] Lack of source bank selection interface: The plan deferred migrating widgets to a new story director (Section "Rejected or Deferred Suggestions" #1) to avoid workflow breaking changes, but fails to define how the new media_archive and public_domain_story source banks are activated. Without a new source selection control on OTR_LedgerScriptWriter, the writer cannot determine whether the operator wants to run RSS news, a media archive feed, or a public-domain text adaptation. Fix: Append an optional `source_bank` combo widget at the end of the optional inputs in OTR_LedgerScriptWriter.INPUT_TYPES (with values ["science_news", "media_archive", "public_domain_story"], defaulting to "science_news"). Because it is appended, it maintains backwards compatibility with existing workflow widget indices.
5. [First Coding Plan Shape / Accepted Architecture Decisions] Prompt composition helper bypasses visual style policy: In the video rendering lane, nodes/_otr_video_engines/render_driver.py composes B-roll prompts on-the-fly via the shared helper finish_visual_prompt (defined in nodes/_otr_story_brief_helpers.py:L458-L503). This helper currently hardcodes the cinematic style tail STYLE_TAIL_DEFAULT (35mm film grain look). Since the plan does not update finish_visual_prompt to inspect the visual style policy stamped on the ledger, non-cinematic modes (like anime or archival_mono) will still append the cinematic 35mm tail to their B-roll video clips. Fix: Update finish_visual_prompt to read `meta.get("visual_style")` and dynamically apply the tail keep/replace/suppress or forbidden-term scrub rules.
6. [First Coding Plan Shape / V1 Contract Shape] Undefined verification mechanism for StoryBlueprint fidelity: Step 8 of the Coding Plan introduces fidelity tracing, warnings, and failures for the public-domain adaptation, but the plan does not define what component verifies these requirements or how they are checked. Fix: Explicitly specify that the OTR_LedgerFreezeCascade (or an inline validator in OTR_LedgerScriptWriter during outline verification) will perform an LLM-based verification pass comparing the generated outline beats to the blueprint's protagonist, antagonist, and ending, throwing a hard validation error if a "must" requirement fails.

SHOULD-FIX:
1. [V1 Contract Shape / First Coding Plan Shape] Undefined field mapping from StoryInputPacket to news_used: Step 2 of the Coding Plan specifies that the writer will "Mirror to meta.news and news_used" to support legacy nodes. However, news_used is a 1-element JSON array of dictionaries containing specific legacy fields (e.g., headline, summary, source, link). The mapping between the new StoryInputPacket fields and this legacy format is undefined. Fix: Explicitly define the mapping: `headline` maps to `source_title`, `summary` maps to `source_summary`, `full_text` maps to `source_text_ref` (or `script_brief`), `source` maps to `source_label`, and `link` maps to `source_url`.
2. [V1 Contract Shape] Missing default profiles definition for VisualStylePolicy: The plan declares a three-way style_id choice (cinematic_35mm, archival_mono, anime) but does not specify what default properties (positive tail, negative tail, forbidden terms, color language) each of these profiles contains. Fix: Define the default profiles in nodes/_otr_visual_style_policy.py:
   - cinematic_35mm: positive_tail = "cinematic, 35mm film look, subtle film grain", base_tail_strategy = "keep"
   - archival_mono: positive_tail = "black and white, archival photograph, vintage monochrome", base_tail_strategy = "replace", forbidden_terms = ["color", "vibrant", "35mm"]
   - anime: positive_tail = "anime aesthetic, hand-drawn illustration, cel shaded", base_tail_strategy = "replace", forbidden_terms = ["photorealistic", "35mm", "film grain", "photo"]

OPTIONAL / NICE-TO-HAVE:
1. [First Coding Plan Shape] Performance: Pre-parsing and caching of media archive RSS feeds: [ASSUMPTION] To prevent network latency from blocking the writer's main thread when fetching the LOC/NFPF feeds, consider pre-fetching and caching feed articles in a local sqlite database or JSON cache.

CUT THESE (over-engineering):
1. [Rejected Or Deferred Suggestions] Automatic or arbitrary Gutenberg search: Safe to cut because the plan already deferred this to focus on operator-supplied text. Keeping it deferred is correct as it avoids dealing with book parsing, formatting, and complex search APIs.

[ASSUMPTION] Pydantic is already present in the workspace environment, as it is imported by nodes/news_interpreter.py and nodes/_otr_story_select.py but not explicitly pinned in requirements.txt.
[ASSUMPTION] ComfyUI handles required input sockets via keyword arguments during execution.
