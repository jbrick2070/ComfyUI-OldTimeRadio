VERDICT: no. The two-axis architecture is right, but the scaffold is not build-ready until it chooses concrete compatibility seams for `meta.news`/`news_used`, outline source labeling, and visual-style policy insertion.

MUST-FIX BEFORE BUILD:
1. [StoryInputPacket Contract / Proposed Node-Level Shape] [CONFIRMED] The plan says "Do not keep overloading everything as `meta.news`" but does not define the compatibility bridge for the current graph. `OTR_LedgerScriptWriter` still emits `news_used` and calls `_build_news_payload(outline, resolved["news_seed"], resolved["seed_source"])`; the canonical workflow wires the writer's `news_used` output through `OTR_LedgerFreezeCascade` into `OTR_SignalLostVideo`. Concrete fix: define v1 migration as `meta.source` canonical plus a legacy `meta.news` mirror and keep the existing `news_used` output name/payload shape until a separate workflow-breaking migration is planned.
2. [Source Bank Kinds / media_archive] [CONFIRMED] "Feed swap" alone is too optimistic because `_otr_outline` still has raw fallback wording of `Science story` / `extrapolates from the science story` when no `script_brief` exists, and the macro prompt has the same science fallback. Concrete fix: either make archive interpretation fail-closed until a `script_brief` exists, or add source-kind/source-label fields to `OutlineRequest` before enabling `media_archive`.
3. [public_domain_story / pd_adaptation_engine] [CONFIRMED] The scaffold leaves the highest-risk choice open: direct `Outline / Beat[]` output vs a stricter `StoryBlueprint` adapter. That ambiguity will produce incompatible implementations. Concrete fix: first build `StoryBlueprint -> existing Outline` rather than bypassing the existing outline validators, budget checks, speaker stamping, and ledger skeleton path.
4. [Axis B: Visual Style and Visual Model Fork] [CONFIRMED] The proposed flow says `VisualStyleDirector -> ... -> OTR_VideoDirector model policy`, but the current `OTR_VideoDirector` is an independent policy node and does not consume ledger/canon. Current prompt surfaces are `OTR_MetaBriefImagePromptGen(image_policy_json)`, `OTR_ShotLock(video_policy_json)`, `_otr_story_brief_helpers`, and the render driver. Concrete fix: choose one explicit v1 route: add `visual_style_policy_json` as a separate optional input to prompt-producing nodes, or embed it under an existing policy JSON. Do not leave both viable.
5. [Implementation Order] [CONFIRMED] The plan says contracts first, but not where those contracts live or how they fail. This repo already uses typed schemas with `extra="forbid"` for video requests. Concrete fix: put source/style contracts in small pure modules with Pydantic validation and no ComfyUI/node imports; tests should validate unknown-key rejection, default behavior, and JSON round-trip.
6. [Acceptance Gates] [CONFIRMED] The workflow rule is present but too generic for this build. The local profile says the canonical workflow has 22 nodes / 51 links / 139 widget slots and that widget order is positional. Concrete fix: each implementation chunk must list the exact nodes/links/widgets touched in `workflows/otr_scifi_16gb_full.json`, and any new optional widget must be appended, never inserted.

SHOULD-FIX:
1. [StoryInputPacket Contract] [CONFIRMED] The packet has `source_hash` and `source_text_ref` but no source selection/cache semantics. Network feeds and public-domain files are external state in ComfyUI caching terms. Fix: define `source_hash` as the cache key and add a verify-at-build step for `IS_CHANGED` or equivalent cache invalidation wherever source fetch/load is moved into a node.
2. [VisualStyleProfile Contract] [CONFIRMED] The style profile lists positive/negative tails but does not say how to remove existing cinematic tails. Current image prompts call `finish_visual_prompt` and append `IMAGE_GRADE_TAIL`; anime would inherit cinematic film terms unless explicitly scrubbed. Fix: define style transforms as `base_tail_strategy = keep|replace|suppress` plus forbidden-term scrub.
3. [Source Bank Kinds] [ASSUMPTION] Public-domain text acquisition may be too large/noisy for the first build. Fix: start with operator-supplied local text or curated URL list, not arbitrary Gutenberg search, then expand once the adapter is proven.
4. [Acceptance Gates] [CONFIRMED] Add no-production-match tests for dormant architecture terms until implemented, then flip them into positive wiring tests: `archive_seed`, `pd_adapt`, `visual_style_policy_json`.

OPTIONAL / NICE-TO-HAVE:
1. [Visual Fork Examples] Keep `archival_mono` as the first non-default visual profile because it exercises the media-archive path without requiring anime-specific model assumptions.
2. [Story Engines] Add `science_explainer_engine` later as a clean engine example, but do not let it block `media_archive`.

CUT THESE (scope / over-engineering):
1. [Visual Fork Examples] Cut `graphic_novel` and `custom` from the first coding sprint. They prove no new architecture beyond `archival_mono`/`anime` and expand the QA matrix.
2. [Story Engines] Cut `science_explainer_engine` from the source-bank MVP unless the goal shifts away from archive/public-domain sources. It is useful later but not required to prove the source bank abstraction.
3. [Proposed Node-Level Shape] Cut a separate `OTR_StoryDirector` node from the first chunk. Use a pure `StoryDirector` module called from `OTR_LedgerScriptWriter` first, then promote to a node when more than one wired source path exists.

