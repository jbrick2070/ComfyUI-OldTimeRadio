VERDICT: yes-with-fixes. The plan introduces a solid separation of story and visual concerns, but contains critical gaps in visual policy propagation, caching, and missing LLM configuration inputs for the new nodes.

MUST-FIX BEFORE BUILD:
1. [Proposed Node-Level Shape / Axis B] The plan states that the visual-style fork happens after the ledger exists and does not rewrite the story ledger, yet it fails to define a mechanism for the downstream video renderer [OTRVideoRenderBatch](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/otr_video_render_batch.py) to consume the visual style policy JSON. Fix: Update `OTRVideoRenderBatch` (and its underlying [render_driver.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/render_driver.py)) to accept `visual_style_policy_json` as an optional input, or stamp the visual style policy into the ledger metadata before rendering.
2. [Proposed Node-Level Shape / Axis A] The proposed `OTR_SourceBankDirector` and `OTR_StorySourceInterpreter` lack any input widgets or sockets to accept user-specified premise texts or custom public-domain text files. Fix: Add a `custom_premise_path` or `custom_source_text` input widget/socket to `OTR_SourceBankDirector` to allow operator overrides for the story sources.
3. [Proposed Node-Level Shape / Axis A] The proposed `OTR_StoryDirector` does not define widgets/inputs for LLM configuration, length target, or creativity. Since outline and script generation are driven by LLMs, these must be explicitly exposed. Fix: Migrate the core widgets from [OTR_LedgerScriptWriter](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/OTR_LedgerScriptWriter.py) (`creative_writing_model`, `technical_model`, `target_words`, `num_characters`, `creativity`, `min_p`, `repetition_penalty`) to `OTR_StoryDirector`.
4. [Proposed Node-Level Shape / Axis A & B] The plan introduces new nodes (`OTR_SourceBankDirector`, `OTR_StorySourceInterpreter`, `OTR_StoryDirector`, `OTR_VisualStyleDirector`) but does not address ComfyUI caching. Without proper cache invalidation, these nodes will either fetch RSS feeds too frequently or serve stale cached scripts. Fix: Implement classmethod `IS_CHANGED` for these nodes, returning `time.time()` for generators/interpreters, or a hash of input parameters for the directors to trigger re-runs when inputs change.
5. [Proposed Node-Level Shape / Axis B] Wiring the new `visual_style_policy_json` into [OTRMetaBriefImagePromptGen](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/otr_meta_brief_image_prompt.py) and [OTRShotLock](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/otr_shot_lock.py) as a positional parameter will break existing saved workflows due to LiteGraph positional widget mapping. Fix: Append `visual_style_policy_json` as an optional input at the end of the `optional` dictionary in `INPUT_TYPES` for both nodes to ensure backward compatibility.
6. [__init__.py / Proposed Node-Level Shape] The four proposed nodes are not registered in the package entry point. Fix: Register `OTR_SourceBankDirector`, `OTR_StorySourceInterpreter`, `OTR_StoryDirector`, and `OTR_VisualStyleDirector` in the `_NODE_MODULES` dictionary inside [__init__.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/__init__.py).

SHOULD-FIX:
1. [Axis B / VisualStyleProfile Contract] The `allow_story_rewrite: false` field in the visual style profile contract is redundant and contradicts the rule that visual styling must never modify dialogue. Fix: Remove `allow_story_rewrite` from the schema to preserve a clean separation of concerns.
2. [Axis A / Source Bank Kinds / media_archive] The plan specifies that `media_archive` "must remove 'science story' assumptions from prompts," but does not detail where this prompt cleanup will live. Fix: Define specific templates or prompt modifiers in [_otr_story_brief_helpers.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_story_brief_helpers.py) that strip the science-news prompts.

OPTIONAL / NICE-TO-HAVE:
- Consolidation of Directors: [ASSUMPTION] Instead of adding `OTR_VisualStyleDirector` as a standalone node (which increases graph clutter with spaghetti connections), it could be integrated directly into [OTRVideoDirector](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/otr_video_director.py) to output both video model policy and style policy from a single node.

CUT THESE:
1. [Axis A / Story Engines] `science_explainer_engine`: Safe to cut because a non-drama explainer violates the core product identity of a sci-fi radio drama generator and requires a different narrative structure and audio mixing flow.
2. [VisualStyleProfile Contract] `allow_story_rewrite` field: Safe to cut as it introduces unnecessary logic gates for a behaviour that is architecturally prohibited.
