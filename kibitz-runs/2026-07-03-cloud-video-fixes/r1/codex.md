VERDICT: yes-with-fixes — Bug A’s direction is plausible, but Bug B’s arc contradicts the saved workflow, and the cloud-video goal includes adapters that current code says are not invocable.

MUST-FIX BEFORE BUILD:
1. [BUG B / Hypotheses + Fix] Defect: the plan frames the final path as either legacy-only or still-base, but the real workflow already routes `OTR_VideoRenderBatch` into the final chain: node 92 `clip_manifest_json` -> node 84 `OTR_SilentComposite`, then node 84 -> node 86 -> node 93 -> node 85 mux, while node 12 procgen is also wired as floor/overlay (`workflows/otr_scifi_16gb_full.json`, links 246, 247, 250, 261, 265, 266). `OTR_SilentComposite` explicitly uses real clips and only gap-fills from floor/black (`nodes/otr_silent_composite.py:665`, `:670`, `:747`, `:799`). Concrete fix: rewrite Bug B as a visibility/coverage/blend investigation: manifest coverage, segment selection, and node 93 procgen overlay dominance, not “wire motion into final.”
2. [BUG A / Fix] Defect: adding hidden inputs to the OTR nodes is not sufficient unless the hidden values reach session creation. Current bridge calls `get_or_create_session(current_prompt_id())` with no hidden args (`nodes/_otr_shared/cloud_media_invoke.py:578`), while `resolve_auth` only sees hidden credentials if `get_or_create_session` passes them (`nodes/_otr_shared/cloud_media_backend.py:135`, `:421`). Concrete fix: define one auth-binding seam end-to-end: OTR node hidden inputs -> scoped context/binder -> `invoke_partner_node` -> `get_or_create_session(..., hidden_api_key=..., hidden_auth_token=...)`, with reset after execution.
3. [BUG A / Open questions] Defect: the plan both chooses “contextvar” and leaves “contextvar vs threading through the session table” unresolved. That is not build-ready. Concrete fix: pick the seam before coding and remove the open question; tests should exercise the chosen path through `invoke_partner_node`, not only `resolve_auth`.
4. [BUG A / Goal] Defect: “cloud engines ... seedance video actually authenticate + render” overclaims. `CloudSeedance2Engine` intentionally raises “row not yet invocable” because DYNAMICCOMBO_V3 expansion is missing (`nodes/_otr_video_engines/eng_cloud_video.py:273`, `:289`-`:292`). `CloudWanI2VEngine` also requires `OTR_CLOUD_WAN_MODEL` and says dynamic prompt work lands later (`nodes/_otr_video_engines/eng_cloud_video.py:295`, `:305`-`:313`). Concrete fix: either add V3 expansion/model-resolution to scope, or remove Seedance/Wan from the auth-fix acceptance list.
5. [BUG B / Fix] Defect: the acceptance target is “motion reaches the shipped final,” but the plan stops at motion clips and base-layer routing. The shipped artifact passes through node 93 and `OTR_MasterAudioMux`, which publishes to OBS (`nodes/otr_master_audio_mux.py:340`, `:348`-`:349`). Concrete fix: require final OBS/final MP4 visual verification after mux, not just `Test-Path` for per-beat clips.

SHOULD-FIX:
1. [BUG A / Tests] Add a session-cache test: existing sessions are keyed by prompt id (`nodes/_otr_shared/cloud_media_backend.py:421`-`:434`), so auth-binding tests must cover first cloud call, repeated cloud calls in one prompt, and env-over-hidden precedence.
2. [BUG B / Evidence] Record node 93’s current saved widgets in the plan because they are likely central to “motion hidden by procgen”: `screen`, opacity `1`, `bypass=False`, `green_only_overlay=True`, `burn_captions=True` in `workflows/otr_scifi_16gb_full.json` node 93.
3. [BUG A / Assumption surface] [ASSUMPTION] ComfyUI hidden injection by `API_KEY_COMFY_ORG` / `AUTH_TOKEN_COMFY_ORG` type-name is not verified in this repo. Partner pins show those hidden names (`nodes/_otr_shared/partner_nodes.yaml:10`-`:20`, `:156`-`:168`), but the actual ComfyUI injector behavior must be verified against the running ComfyUI version.

OPTIONAL / NICE-TO-HAVE:
- Add a tiny final-video pixel/motion audit artifact beside the run log so “clips exist” and “clips are visible in final” are not conflated.

CUT THESE (scope / over-engineering):
1. [BUG A / Fix 1] Cut “`unique_id` / `prompt` if the bridge needs the prompt id” from Round 1. The bridge already has `current_prompt_id()` / `bind_prompt_id()` (`nodes/_otr_shared/cloud_media_invoke.py:91`, `:101`); decide extra hidden fields only if R2 grounding proves they are required.
2. [BUG B / Fix] Cut the blanket “make procgen optional overlay” rewrite until the trace proves overlay dominance. Current architecture already separates source motion from procgen overlay via `OTR_PostUpscaleProcgenBlend` (`nodes/otr_post_upscale_procgen_blend.py:14`-`:15`, `:890`-`:897`); the smaller fix may be widget/default/blend behavior.
3. [BUG A / Goal] Cut Seedance/Wan from the immediate “auth fixed means render works” claim unless V3 expansion/model env handling is added in the same build.