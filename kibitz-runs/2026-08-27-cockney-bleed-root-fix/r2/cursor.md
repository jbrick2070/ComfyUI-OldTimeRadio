VERDICT: yes-with-fixes -- the two call-site edits and the keyword-only helper are real and grounded, but P2.1's type guard does not match P3.1.6, the new policy text drops the live `\n\n` separator, P3.1 will fail collection of the tests it says to keep, and P5.3 can miss the defect path because the canonical `source_bank` is a roll.

MUST-FIX BEFORE BUILD:
1. [P2.1.3 + P3.1.6] Type guard as written is not the API the tests pin. P2.1.3 only rejects top-level `str`/`bytes` then iterates; a Mapping is not a `Sequence` but is iterable by keys. `append_dialogue_policy(system, active_speakers={"name": "LEMMY"})` would iterate `"name"`, raise nothing, and append nothing -- the old silent miss, not the `TypeError` P3.1.6 requires. `_CastShim` as the container is not iterable (`nodes/_otr_compose_exchange.py:898-905`), so that case TypeErrors by accident. Fix: after excluding `str`/`bytes`, require `collections.abc.Sequence`; then `TypeError` if any element is not `str`. That rejects dict/`_CastShim`/sets/generators as the container and list-of-dicts as elements, which is what P3.1.5-6 actually test. Do not `str()`-coerce elements (P4.2).
2. [P2.1 exact policy block] The live constant starts with a blank-line separator:`nodes/_otr_dialogue_policy.py:6-9` is `"\n\nConvey the Cockney accent..."`. The P2.1 "exact policy meaning" block has no leading `\n\n`. Concatenating it as written fuses the Lemmy rule onto the last system-prompt sentence (`compose_line_draft` a`nodes/_otr_line_composer.py:1049-1054`; `build_exchange_prompt` a`nodes/_otr_compose_exchange.py:389-393` already does `system = base + f"  - {grounding_clause}\n"` then appends). Fix: keep a leading `\n\n` on `_COCKNEY_ORTHOGRAPHY_RULE` (or prepend it in `append_dialogue_policy`) and rewrite only the sentences after that separator. One module-level constant, one text.
3. [P3.1] "Replace only `test_roster_has_lemmy` and `test_append_dialogue_policy`" while leaving later tests intact is not buildable as written. `tests/test_otr_dialogue_policy.py:4-7` imports `roster_has_lemmy` at module top. Deleting `roster_has_lemmy` (`nodes/_otr_dialogue_policy.py:13-26`; only other production use is `append_dialogue_policy` at :34) without dropping that import fails collection of the whole file, including the BUG-12.86 tests P3.1 orders kept. Fix: in the same edit, remove `roster_has_lemmy` from that import; keep `_COCKNEY_ORTHOGRAPHY_RULE` if the new tests still assert against it. P4.1 `rg` on`nodes tests` is the lock.
4. [P5.3.2-P5.3.3] Live qualification can miss both affected callers. Canonical writer `widgets_values` slot 22 is `"roll (any eligible bank)"` (`workflows/otr_canonical.json:531`; slot map in `tests/test_workflow_json_guardrails.py:608`). `lemmy_cameo=always include` is legal (`nodes/_otr_writer_inputs.py:85-89`) and whitelisted (`scripts/otr_api.py:854-858`), but a bank roll can land `scifi_news_pro` (dispatched before the helper,`nodes/OTR_LedgerScriptWriter.py:3376-3431` +`nodes/_otr_lane_specs.py:76-80`) or `public_domain`/`shakespeare` (Lemmy excluded,`nodes/_otr_casting.py:1178-1189`). Fix: the sanctioned command must also `--set` `OTR_LedgerScriptWriter.source_bank=media_archive` (whitelist at `scripts/otr_api.py:842-844`; dedicated `--source-bank` exists on `scripts/otr_canonical_api_run.py:101-102,344`). `original` is the other inline bank (`nodes/_otr_lane_specs.py:83-87`); do not leave bank on roll.

SHOULD-FIX:
1. [P3.1.5 vs P3.1.7] "scalar string/bytes container" is not the same call as a positional second argument. `append_dialogue_policy(system, "LEMMY")` is already TypeError from the keyword-only `*` in P2.1.2. Pin P3.1.5 as `active_speakers="LEMMY"` and `active_speakers=b"LEMMY"`.
2. [P3.3.4] `_CountingGen` (`tests/test_compose_exchange.py:107-123`) records messages but needs pre-scripted slot lines; `_fake_gen_valid` (`:467-475`) emits parseable `d###|...` lines from the format block and records nothing. `run_exchange_prepass` grouping is not known until runtime. Fix: wrap `_fake_gen_valid` (or equivalent) with message capture; assert on `messages[0]` where `role=="system"`. Do not search the joined user blob -- user roster is names plus persona (`nodes/_otr_compose_exchange.py:341-347`).
3. [P5.3.6] "reconstruct accepted groups from ordered ledger beats/lines, the audit's accepted `beat_ids`, and `group_voiced_beats(min_size=2, max_size=3)`" is underspecified. `run_exchange_prepass` groups the full ordered run, with announcer/empty-speaker breaks (`nodes/_otr_compose_exchange.py:973-1001,1028-1036`); `meta["exchange_prepass_audit"]` only stores composed ids (`nodes/OTR_LedgerScriptWriter.py:4932-4935`). Regrouping only accepted ids merges across a failed/singleton hole and can invent a mixed LEMMY group that never ran. Fix: build VoicedSlots from the full ordered beat list (same `_groupable` rules), run `group_voiced_beats`, then keep groups whose beat_ids are a subset of the audit set.
4. [P5.3.2 vs P5.3.6] `-Acts 1` vs "at least one accepted mixed LEMMY group". Canonical `act_count` is already `"3"` (`workflows/otr_canonical.json:514`; slot 6). One-act plus`num_characters=2` makes a mixed consecutive 2-3 voiced run easy to miss; the plan then says rerun. Fix: keep 3 acts for the mixed-exchange live proof, or drop the one-act constraint. Also quote the cameo value: `-Set "OTR_LedgerScriptWriter.lemmy_cameo=always include"` -- `_parse_value` keeps the string (`scripts/otr_canonical_api_run.py:46-51`) but an unquoted space splits the PowerShell `-Set` array (`scripts/otr_headless_canonical.ps1:25,208-209`).
5. [P3.2 vs user prompt] P3.2 correctly inspects `role="system"` via `_recording_creative` (`tests/test_phase1_composer_prompt.py:136-148`). `compose_line` returns `LineResult` (`nodes/_otr_line_composer.py:1109-1161`); do not treat its return as the prompt. Do not search the user message: `all_voice_cards` still carries Lemmy's `speaks:` / description with the word `Cockney` (`nodes/_otr_line_composer.py:492-522,724-730`; `config/cast_pools.py:382-384`). That is labeled CAST context, not the defect P0.2 names. P5.3.10 already defers it; keep tests on system content only so they do not fail a correct helper patch.
6. [P2.1+P2.2+P2.3] Keyword-only `append_dialogue_policy` must land in the same change as both callers. Current sites are positional:`nodes/_otr_line_composer.py:1051` and`nodes/_otr_compose_exchange.py:393`. Split application TypeErrors every compose. `active_speakers=(req.speaker,)` and `tuple(slot.speaker for slot in beat_group)` match `LineRequest.speaker: str` (`nodes/_otr_line_composer.py:248`) and `VoicedSlot.speaker: str` (`nodes/_otr_compose_exchange.py:109`); production prepass already stringifies speakers (`nodes/_otr_compose_exchange.py:970-971`).
7. [P5.3.3] Canonical creative widget is `"google/gemma-4-12b-it (11.9 GB)"` (`workflows/otr_canonical.json:510`), not Gemma 4 E2B. If E2B is required for ledger comparison, pin the live combo string via `--set`/`--creative-model`; otherwise record 12B as the replacement as the plan already allows. verify: exact catalog label for Gemma 4 E2B before putting it in the command.

OPTIONAL / NICE-TO-HAVE:
- Inline the "private active-speaker predicate" (P2.1.1) into `append_dialogue_policy`; P3.1 never tests it as a public name.
- Parametrize P3.1.1-4. Keep 5-7 as distinct TypeError pins.
- P3.3.1-2: assert `messages[0]["role"]=="system"` explicitly, same as P3.2.

CUT THESE:
1. A second Lemmy-only vs mixed policy string / branch -- P2.1 already shares one block; extra branching reintroduces the "global orthography" sketch this plan supersedes (`docs/GO_FORWARD_PLAN.md:167-168`).
2. Post-generation dialect scrub / vocabulary blacklist -- already a non-goal (P2.4); would violate the no-shim rule and P5.3.4.
3. A `use_exchange=False` live workflow -- already forbidden (P5.3.9); per-line is covered by P3.2 captured-prompt tests; fallback remains`nodes/OTR_LedgerScriptWriter.py:4881-4969`.
4. Applying this helper to `scifi_news_pro` -- that lane never imports it (`nodes/_otr_scifi_news_pro.py`); register is pinned per shape (`:1049-1074`, `:2219-2229`). P3.4 already says do not add a noise test there.

ComfyUI contract: no new node, no `INPUT_TYPES` / widget / link / `otr_canonical.json` edit (P0.9, P2.4). `_otr_dialogue_policy.py` is a helper, not in `NODE_CLASS_MAPPINGS`. No tensor, VRAM, `IS_CHANGED`, or new top-level heavy import. Repair rebuilds via `_run_once` -> `build_exchange_prompt` with the same `beat_group` (`nodes/_otr_compose_exchange.py:653-706`); `failure_reasons` go to the user message (`:446-452`), so P3.3.3 system identity is implementable. Line retry copies `messages` then appends assistant/user turns (`nodes/_otr_line_composer.py:1101-1104`); P3.2.3 is implementable. `use_exchange` is not on `CREATIVE_WHITELIST` (`scripts/otr_api.py:831-859`).
