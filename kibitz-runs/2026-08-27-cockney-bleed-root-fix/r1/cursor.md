VERDICT: yes-with-fixes -- the dual-caller active-speaker scope plus unscoped-imperative rewrite is the right story; P5.3 as written can publish a false pass, and GO_FORWARD's orthography sketch would violate P0.7 if the implementer follows it instead of P2.1.

MUST-FIX BEFORE BUILD:
1. [P5.3.4 / P0.5] Defect: live qualification requires only "cast contains Lemmy plus at least one other character" and that `exchange_prepass_audit` shows some grouped composition. Canonical `use_exchange` is True (`tests/test_workflow_json_guardrails.py` slot 13). Groups without Lemmy, or Lemmy-only singletons falling back to `compose_line`, never exercise P0.5. `meta["exchange_prepass_audit"]` is stamped only inside the successful prepass try a`nodes/OTR_LedgerScriptWriter.py:4932-4935` and records beat ids, not speaker sets. A 1-act `media_archive` run can therefore publish with `obs_publish OK` and never contain a LEMMY+other group -- the operator symptom in `docs/GO_FORWARD_PLAN.md` ("when lemmy is in the scene"). Fix: P5.3.4 must fail unless the saved ledger shows at least one accepted exchange group whose speaker set includes `LEMMY` and one other name; if the 1-act topology does not produce that, rerun, do not waive.
2. [P2.1 / P0.2 / P0.7 vs `docs/GO_FORWARD_PLAN.md`] Defect: GFP's standing sketch ("make LEMMY the grammatical subject of the accent sentence and leave the orthography sentence global") contradicts this plan's exact blob, which gates BOTH sentences on active-speaker Lemmy. An implementer who "helps" by always appending the orthography sentence will either change no-Lemmy prompts (violates P0.7) or keep a Cockney-adjacent policy on non-Lemmy calls (violates P0.2). Current helper appends one unsplittable constant (`nodes/_otr_dialogue_policy.py:6-10,29-36`). Fix: P2.1 must state that the GFP global-orthography sketch is rejected; non-Lemmy active-speaker assembly returns `system_prompt or ""` with zero policy bytes.

SHOULD-FIX:
1. [P3.1] `tests/test_otr_dialogue_policy.py` is not a policy-only file: `test_roster_has_lemmy` / `test_append_dialogue_policy` sit beside Lemmy profile and qualification-receipt tests (BUG-12.86). "Replace the roster-oriented tests" can be misread as rewriting the module. Fix: name the two functions to replace and forbid deleting the receipt tests.
2. [P3.3.5] False lock. `tests/test_exchange_seam_lane2.py` `TestAssemblyIdentity._messages` uses MARLOW/VERA and `cast=[]`; it only asserts `startswith(EXCHANGE_SYSTEM_PROMPT)` plus grounding, not "Cockney" absence and not full system equality after `append_dialogue_policy`. Those tests stay green if Cockney is appended to a no-Lemmy group. Fix: drop the byte-identical claim for that file; P3.1.1 and P3.3.1 are the actual locks (or add an explicit Cockney-absence assert there).
3. [P5.3.3 / P5.3.4] "Prefer the model/bank pairing from an operator-observed bleed artifact" names no episode. `docs/GO_FORWARD_PLAN.md` / `docs/HANDOFF_LOG.md` record the 2026-08-26 report, not an id. [ASSUMPTION] a comparable artifact is still on disk. Fix: pin the episode id/path, or say the pairing is unpinned and default `media_archive` + `lemmy_cameo=always include` is the qual.
4. [P5.3.4 remaining channel] Per-line user prompts still dump full-cast cards with Lemmy's `speaks:` signature (`nodes/_otr_line_composer.py:514-522,724-730`; writer builds `all_voice_cards` from every row at `OTR_LedgerScriptWriter.py:4544-4578`). That is labeled per character, unlike the subjectless system command -- not the same defect. If live still bleeds after P2, diagnose this channel (and rolling `prior` / `last_lines`) before widening the patch. Do not fold CAST-card rewriting into this build.
5. [P2.1 TypeError vs PD1] Fail-loud on non-strings is the right API fence. `compose_exchange` duck-types slots (`nodes/_otr_compose_exchange.py:100-106`). A non-str `speaker` would TypeError inside `build_exchange_prompt`, and the writer's prepass `except` at `OTR_LedgerScriptWriter.py:4941-4948` would empty `_ex_lines_by_beat_id` for the whole episode. Fix: keep helper TypeError; P2.2/P2.3 pass `str(req.speaker)` / `str(slot.speaker)` so a dirty slot cannot disable grouping.

OPTIONAL / NICE-TO-HAVE:
- P3.2: one ANNOUNCER capture. Mid-episode announcer uses the same `compose_line` + full-cast `allowed_people` (`OTR_LedgerScriptWriter.py:5148-5174,4567-4572,4828-4841`). Same mechanism as ALICE; not a third helper.
- P5.3.4: three human-checkable idiom examples for the ledger read. Not a generation blacklist.
- P3.3.2: assert the isolation clause is absent from a Lemmy-only per-line system message if you want P2.1's "negative clause is for mixed exchanges" to be literal. Current helper appends one blob whenever any active speaker is Lemmy; extra isolation on Lemmy-only lines is harmless.

CUT THESE (scope / over-engineering):
1. Live `/object_info` in P5.2.4 when `git diff` shows `workflows/otr_canonical.json` unchanged. No node, widget, or link. JSON round-trip + `OTR_WorkflowValidator` still run. Safe because P0.9 / P2.4 already forbid a graph edit.
2. Changing `_normalize_cast` / `_CastShim` (`nodes/_otr_compose_exchange.py:898-920`) to carry `speech_signature`. Already a P2.4 non-goal. Production shims drop persona to `persona`/`voice`, so mixed-group Cockney today is the system rule, not the roster line. Touching it confounds this repair.
3. Post-generation dialect scrubber / vocabulary gate. Already a P2.4 non-goal; would be a shim and can damage legitimate registers.
4. A live `use_exchange=False` canonical run. `scripts/otr_api.py:831-859` does not whitelist `use_exchange`. Per-line is the fallback; P3.2 is the right proof. P5.3.7 already says this.
5. New `tests/test_scifi_news_pro_lemmy_cameo.py` cases, and a `scifi_news_pro` live leg, unless the diff touches `_otr_scifi_news_pro.py`. P1.4 / P3.4 are correct: that runner is dispatched before the inline composers (`OTR_LedgerScriptWriter.py:3376-3431`;`nodes/_otr_lane_specs.py:75-80,113-124`).
6. Merging this obs run with the owed H3 video-proof row (`docs/GO_FORWARD_PLAN.md`, commit `e923a9f3`). Different defect, different widget (`character_video_model`). Combining them turns a prompt-scope receipt into a video-engine receipt.

[ASSUMPTION] Production beat/slot speaker strings are canonical `LEMMY` after strip+upper. Plan P2.1.4 does not use `speaker_identity_key` (`nodes/_otr_scifi_news_pro_markup.py:265-270`), which also folds internal whitespace. Casting pins the name `LEMMY` (`nodes/_otr_casting.py:1524`). Verify at implementation: `LineRequest.speaker` and `VoicedSlot.speaker` on a forced-cameo ledger row.

[ASSUMPTION] Selective reset actually frees port 8000 and GPU to desktop baseline before P5.3. GFP still describes a resident foley server as a competing occupant of that port.

ComfyUI domain: no new node class, no `INPUT_TYPES` / `widgets_values` / link change, no tensor/VRAM/`IS_CHANGED` surface. Callers already lazy-import the helper (`nodes/_otr_line_composer.py:1049`,`nodes/_otr_compose_exchange.py:391`). `_otr_dialogue_policy.py` is typing-only at module top. If any of that slips into the diff, stop -- it is out of contract.
