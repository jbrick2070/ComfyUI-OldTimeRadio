VERDICT: yes-with-fixes — helper + two callers match the real contracts if landed atomically, but P5.3.6 will invent mixed groups that never ran, and the two test fakes do not share a capture shape.

MUST-FIX BEFORE BUILD:
1. [P2.1 + P2.2 + P2.3 + P3.1] Keyword-only `append_dialogue_policy(system_prompt, *, active_speakers=...)` and deletion of `roster_has_lemmy` cannot land before both production call sites and the test import. Today`nodes/_otr_line_composer.py:1049-1051` and`nodes/_otr_compose_exchange.py:391-393` still call `append_dialogue_policy(system, roster)` positionally; `tests/test_otr_dialogue_policy.py:4-7` still imports `roster_has_lemmy`. Split order = `TypeError` on every `compose_line` / `compose_exchange` and `ImportError` on the policy file. One working tree: helper signature, both callers, replace the two tests, drop the old import. Then P5.1.

2. [P5.3.6] Mixed-group proof must copy `run_exchange_prepass`'s two-stage grouping, not `group_voiced_beats` on a pre-filtered list. Production: `_groupable` requires nonempty speaker AND `_SLOT_ID_RE` (`^d\d{3}$`) AND not reserved (`nodes/_otr_compose_exchange.py:89-89, 968-979`); a failed `_groupable` flushes the run (`1028-1035`); only then `_compose_run` builds `VoicedSlot`s and calls `group_voiced_beats` (`984-1001`). `group_voiced_beats` itself does not check `d###` (`792-794`). Filtering to valid slots then grouping, or grouping accepted `meta.exchange_prepass_audit.beat_ids` (`OTR_LedgerScriptWriter.py:4932-4935`), bridges announcer / invalid-slot / failed-group holes and can invent a LEMMY+other group that never called `compose_exchange`. Fix: walk the full ordered outline/ledger with the same `_groupable` breaks, chunk each run with `group_voiced_beats(min_size=2, max_size=3, reserved_speakers=("ANNOUNCER",))` — writer does not override those defaults (`4924-4931`) — then keep groups whose beat-id set is a subset of `beat_ids`. If none mix LEMMY with another speaker, rerun; do not qualify.

SHOULD-FIX:
1. [P3.2] Pin the exact capture path on `_recording_creative`. It stores `state["calls"]` as a list of message-lists (`tests/test_phase1_composer_prompt.py:136-145`). `calls[0]` has no `role`. Required: `msgs = creative.state["calls"][0]; sys_msg = msgs[0]; assert sys_msg["role"] == "system"` then assert on `sys_msg["content"]`. Existing retry test already unpacks calls this way (`162-167`). Do not scan the user message: production `LineRequest.all_voice_cards` dumps Lemmy's card (`OTR_LedgerScriptWriter.py:4575-4578, 4848`; `_otr_line_composer.py:724-730`) which contains the word Cockney (`config/cast_pools.py:382-384`). P3.2 fixtures leave `all_voice_cards=""`, so a joined-blob `Cockney` check is accidentally green and will not survive a more production-shaped fixture.

2. [P3.3] `_CountingGen` is a different shape: `gen.calls[i]["messages"][0]["content"]` (`tests/test_compose_exchange.py:107-123`). Copying the P3.2 path onto it fails. Repair (item 3): compare system contents only. `failure_reasons` are appended to the USER message (`_otr_compose_exchange.py:446-457`); full-message equality is a false red. Reuse the stateful Tier-A pattern in `test_repair_triggers_once_then_succeeds` (`220-238`). Do not feed `_raw_for` to a LEMMY/MARLOW group: it hard-codes MARLOW/REESE text (`85-92`). Prompt capture still happens before parse, but any `status=="ok"` assertion will fail.

3. [P3.3.4] Recorder wrapper around `_fake_gen_valid` must keep `(messages, *, temperature, max_new_tokens)` and still parse `dNNN|SPEAKER: <line>` (`467-475`). A messages-only wrapper TypeErrors inside `_run_once` (`573-577`). Assert `role=="system"` only; the user Speakers block is slot-only (`341-348`) and will not contain Lemmy on a non-Lemmy group even when cast dicts do.

4. [P7 vs P3.2] Stop condition "system prompt still contains `Cockney`" is weaker than the policy constant and is the wrong check if applied to joined messages. Gate on `_COCKNEY_ORTHOGRAPHY_RULE` in the system entry, matching P3.1/P3.2. Packs do not contain `Cockney` (verify:`nodes/_otr_creative_prompt_router.py`,`nodes/_otr_story_pack.py` grepped clean).

5. [P5.3.1 + wrapper] The listed command omits `-Port`. `scripts/otr_headless_canonical.ps1:31,173-189` default `Port=0` binds an ephemeral port, sets `OTR_HEADLESS_PORT` / `COMFYUI_URL`, and `_otr_soak_server_launch.cmd:99-205` honors that. Do not treat profile port 8000 as the listen port unless `-Port 8000` is passed. After `Stop-OtrPython`, treat nvidia-smi near desktop baseline as a hard gate; the wrapper only logs VRAM (`162-170`). A resident GUI on :8000 plus a second headless boot will not fit the 14.5 GiB review gate.

6. [P1.3 / P5.10 / user channel] Exchange USER roster is per-slot (`341-348`); non-Lemmy groups already omit Lemmy's persona. Per-line USER still ships full-cast `all_voice_cards` on every fallback/singleton beat (`4881-4969` fallback into `5005-5014`). That channel is unchanged by P2. Keep P5.10 as the live diagnostic order; do not widen this patch. If P3.2 system tests pass and live fallback lines still bleed, that is this residual, not a missed helper caller (`rg` shows only those two production imports).

OPTIONAL / NICE-TO-HAVE:
- Collapse P3.1 TypeError cases 5-7 into one parametrize.
- Confirm `LEMMY_PROFILE["name"] == "LEMMY"` (`config/cast_pools.py:376`) rather than importing `cast_pools` into the stdlib-only helper.
- P5.1 can stay as listed: no other test file imports `roster_has_lemmy` / `append_dialogue_policy`.

CUT THESE:
1. Do not add a third production owner for Cockney (scrubber, polish, announcer). `compose_announcer_intro` / `outro` never call the helper (`_otr_line_composer.py:1410+`). `scifi_news_pro` returns at `OTR_LedgerScriptWriter.py:3376-3431` before the inline composers. Extra owners would violate P2.4 / P0.8.
2. Do not thread `cast` / `_CastShim` / `allowed_people` back into the helper "just in case." Production exchange already normalizes dicts to `_CastShim` (`898-920`), which current `roster_has_lemmy` ignores (`_otr_dialogue_policy.py:17-26`) — that is why live grouped trigger is `VoicedSlot.speaker` (P1.3, grounded). Re-accepting roster objects reopens the category bug P2.1 exists to kill.
3. Do not edit `workflows/otr_canonical.json`. Writer slot 13 is already `true` (`workflows/otr_canonical.json:507-521`; index 13). `use_exchange` is not on `CREATIVE_WHITELIST` (`scripts/otr_api.py:831-859` and`nodes/_otr_workflow_apply.py:681-728`). No node class, INPUT_TYPES, widget, IS_CHANGED, or VRAM change is in this patch.

Grounded contracts (no mismatch if P2 is followed):
- `LineRequest.speaker: str` (`_otr_line_composer.py:248`); production sets it from `beat.speaker` (`OTR_LedgerScriptWriter.py:4736, 4828-4829`). `(req.speaker,)` matches P2.2.
- `VoicedSlot.speaker: str` (`_otr_compose_exchange.py:109`); prepass assigns `_speaker(b)` which is `str(...)` (`970-971, 988-993`). P2.3 tuple of slot speakers matches. Repair rebuilds via `_run_once` -> `build_exchange_prompt` with the same `beat_group` (`653-705, 562-570`); system policy is re-derived, not mutated by repair.
- `lemmy_cameo="always include"` is an exact choice (`nodes/_otr_writer_inputs.py:85-89`). `--set` splits on first `=`; `json.loads("always include")` fails and stays a string (`scripts/otr_canonical_api_run.py:46-51, 78-82`).
- Canonical creative widget is `google/gemma-4-12b-it (11.9 GB)` (`otr_canonical.json:510-511`). P5.3.3 is correct not to hard-code E2B.
- Domain: no new node, no `NODE_CLASS_MAPPINGS` work, no tensor/VRAM/IS_CHANGED issue. Callers already lazy-import the helper inside the function body.

[ASSUMPTION] Ledger row order equals outline beat order for P5.3.6; if a published ledger reorders lines, reconstruct from outline beats plus `dialogue_slot_id` / `speaker` / `beat_id`, not from a sorted `beat_ids` list.
