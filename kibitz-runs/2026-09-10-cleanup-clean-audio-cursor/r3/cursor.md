VERDICT: yes-with-fixes — node3->4->7, music 282/283, and the DSP no-op guards match the tree; the silence/tape proof as specified will crash or overclaim unless the existing check_case control flow is reordered and a few contracts are tightened.

MUST-FIX BEFORE BUILD:
1. [P1 check_case] Silence must branch BEFORE `scripts/otr_canonical_audio_check.py:213-220`. The chirp template window is `template[round(0.65*rate):round(1.25*rate)]` (31200:60000 at 48k). `_trim_trailing_silence` (`nodes/scene_sequencer.py:289-304`) keeps `min_samples=100` on all-zero clips; two voices -> ~200-sample scene (~4 ms). That slice is empty. `correlate` on an empty template errors or yields a garbage lag; `positions[line] - measured_offset` at 221-226 then TypeErrors if lag is null. Gate `silence=True` before template/correlate/offset arithmetic. Do not compute a lag and then skip the assert.
2. [P1 check_case + Canonical branches] Same silence gate must skip ALL `measured_offset` uses, including `clips[0]` at 229-230. Keep ledger-exists, repeat-assembly tuple equality (235-239), paths/hashes/rate, and zero-sample counts. Repeat-assembly does not need lag. Opening silence still prepends a 2 s zero cue (`(1,1,96000)`) under node 7 `opening_duration_sec=10`, `crossfade_ms=500` (`workflows/otr_canonical.json:912-916`); xf = min(24000, 200) = 200, so a 1.50 s golden is the wrong coordinate for this case.
3. [P1 tape loop] Call `AudioEnhance.enhance` directly, never `CanonicalAudioRoute.run(ROUTE[1])`. Route always injects frozen `widgets_values` (`scripts/otr_canonical_audio_check.py:117-127`); after C4 those are `"off"`, so the route cannot exercise subtle/medium/heavy. Order: env (`OTR_TEST_MODE=1`) -> `load_package` -> `torch.set_num_threads(1)` -> tape loop -> only then `check_case`/`pl.new_ledger`. `new_ledger` replaces `_CURRENT` (`nodes/production_ledger.py:508-512`); AudioEnhance writes `post_audio_enhance` when `in_flight_ledger_path()` is set (`nodes/audio_enhance.py:487-512`). Import package-qualified `_otr_ledger` (`package.__name__ + ".nodes._otr_ledger"`), same module `enhance` uses via `from . import _otr_ledger`. Assert `in_flight_ledger_path() is None` first. Pass explicit `target_sample_rate=48000, spatial_width=0, haas_delay_ms=0, bass_warmth=0, lpf_cutoff_hz=0`. Put `public_tape_checks` on the top-level receipt, not stdout.
4. [P1 launcher pairing] `tests/test_canonical_audio_check.py:30-33` still requires `len(cases)==2` and `rng_seed==20260910`. Script and launcher are one contract in this chunk. After the change: three labels `canonical_audio_opening|canonical_audio_no_opening|canonical_audio_silence_opening`; one null lag plus `{scene:0,enhanced:0,master:0}`; no `rng_seed`; tape results present. Independent-process master hashes stay.
5. [C4 JSON] Surgical text replace of node 4 `widgets_values` `workflows/otr_canonical.json:754-761` and the one `extra.info.description` phrase at 3495. Do not `json.dump` the graph. A full reserialize breaks the “only two hunks” SHA inspect and poisons the C2/C3 baseline. Same chunk as INPUT_TYPES + `enhance()` signature defaults (`nodes/audio_enhance.py:323-376`) and the new bit-equality assert; JSON still `[48000, 0.3, 0.8, 0.15, 16000, "subtle"]` would fail Haas/LPF/tape-sat/wow on chirps.
6. [C4 / test_sequencer_ledger] Delete `_fake_room_tone` and the patch at `tests/test_sequencer_ledger.py:66-85` in the same chunk as deleting `_generate_room_tone`. `patch("nodes.scene_sequencer._generate_room_tone")` AttributeErrors once the symbol is gone; every `patched_sequencer_env` test dies.
7. [P1 clone] Clone `AUDIO["waveform"]` with `.clone()` after `route.run(ROUTE[0])` and before `route.run(ROUTE[1])`. Shallow-copying the dict aliases the tensor. Compare values via `torch.equal` (not identity) to `torch.cat([scene_copy, scene_copy], dim=1)`. If either rate != 48000, fail the declared assumption; do not run equality.

SHOULD-FIX:
1. [P1 no-op proof] Chirp equality cannot see `spatial_width`. Sequencer emits `(1,1,N)` (`scene_sequencer.py:1204`). `_mono_to_stereo` duplicates (`audio_enhance.py:91-95`). `_stereo_decorrelate` with L==R has side=0 (`:129-136`), identity at any amount. Haas/LPF/bass/tape-on-chirp still bite. Either add a direct L!=R stereo call in the pre-case loop, or stop claiming width is covered.
2. [C4 / C1 prose] `nodes/scene_sequencer.py:8-10` still advertises breath/BEAT/PAUSE pacing and roomtone. C1 deletes the only `breath_ms`/`beat_pause_ms`/`pause_ms`/`scene_transition_ms`/`act_break_ms` stores (`:928-932`; no reads). Fix that heading with the env-span heading at 1126.
3. [P1] Do not “fix” `_trim_trailing_silence` to drop all-zero clips. Empty clips -> bus shortfall at `sequence:1113-1122`. Silence depends on min_samples=100.
4. [P1] Resident ComfyUI keeps old `scene_sequencer` until restart. This chunk does not prove a live listen. Next production pass needs a fresh process; replay still copies frozen noisy WAVs (`assemble:1526-1528` / `_assemble_replay:1325`).

OPTIONAL / NICE-TO-HAVE:
- `__init__.py:173` display name still `" Spatial Audio Enhance"` (plan forbids mappings).
- Replay comment at `sequence:895-897` still says node 4 Haas+LPF run unbranched; widgets 0 skip those branches (`audio_enhance.py:434-452`). `_lowpass_16k:201` already no-ops cutoff<=0.
- `node_list.json` still “Spatial Audio Enhance”. Uncovered.

CUT THESE:
1. Re-implementing the full INPUT_TYPES/signature sweep and a loads/dumps/loads tautology inside `otr_canonical_audio_check.py`. `tests/test_input_types_signature_parity.py` already parametrizes every registered node; Assembler music bus 282/283 is the trap that test exists for. Keep a narrow check: AudioEnhance defaults are 0/"off", canonical widgets match, `len(links)==62` (confirmed; do not use `last_link_id` 290), 23 `OTR_*` nodes, node 4 values + description SHA inspect.
2. `np.random.seed` / `torch.manual_seed` / `rng_seed` receipt (`otr_canonical_audio_check.py:148-152,246`). After deleting `_generate_room_tone` and the hiss block (`audio_enhance.py:276-286`), this route has no RNG. Seeds would hide a missed hiss call.
3. Second silent assembly; migrating `scripts/_otr_canonical_api_prompt.json` (dump only; `otr_canonical_api_run.py` loads `workflows/otr_canonical.json`).

Grounded route: link 5 node3.0->node4.0 AUDIO; link 6 node4.0->node7.0 AUDIO; 282/283 node83 -> node7 slots 7/8; episode_audio fans 15->node12 and 272->node94. Music never enters node 4. `_loudness_normalize_clip:523-524` and `_master_loudness:380-381` leave digital zeros; PCM `*32767` has no dither (`:1757`). `_level_dialogue_clip` on zeros stays zeros (`test_segment_loudnorm.py:82-85`).

[ASSUMPTION] `load_package()` does not bind `_CURRENT` before the tape loop.
UNTRACED -- video/scopes/foley mux; live ComfyUI cache; whether WorkflowValidator.validate is slow/fragile inside this child.

Uncovered: `node_list.json`, API prompt dumps, `nodes/otr_master_audio_mux.py`, video engine consumers.
