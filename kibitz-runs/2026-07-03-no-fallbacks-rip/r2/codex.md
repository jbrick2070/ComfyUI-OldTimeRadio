VERDICT: no. The plan has invalid exception targets, stale file references, and several changes that will be swallowed by existing catch blocks unless the call sites are changed too.

MUST-FIX BEFORE BUILD:
1. [A.1/A.2/D R1] `EngineUnusable(MISSING_MODEL/REF)` is not a valid taxonomy. `EngineUsabilityReason` only has `MISSING_MODEL`, `MISSING_HF_TOKEN`, `MALFORMED_CONFIG`, etc.; no `REF` value exists, and `EngineUnusable.__init__` coerces the reason through that enum. Concrete fix: use `EngineUsabilityReason.MISSING_MODEL` with a detail naming the missing voice ref, or add a real enum value and update every reason consumer/test. Cite: `nodes/_otr_audio_engines/registry.py:57-73`, `nodes/_otr_audio_engines/registry.py:82-86`.

2. [A.9/A.12-A.14/D R3] Several R3 targets cite files/lines that do not contain the code. `nodes/story_orchestrator.py` is only 3040 lines, so references to `story_orchestrator.py:4100-5393` are stale. The news degrade, title regen, outro, and portrait references are in `nodes/OTR_LedgerScriptWriter.py`, `nodes/_otr_line_composer.py`, and `nodes/otr_meta_brief_image_prompt.py`. Also #9 cites `OTR_LedgerScriptWriter.py:682-759`, which is local `generate_fn`, not Bark voice health. Concrete fix: rewrite the inventory with current paths before coding. Cite: `nodes/story_orchestrator.py` count = 3040; `nodes/OTR_LedgerScriptWriter.py:2836-2959`, `nodes/OTR_LedgerScriptWriter.py:5201-5257`, `nodes/_otr_line_composer.py:3386-3662`, `nodes/otr_meta_brief_image_prompt.py:1028-1215`, `nodes/story_orchestrator.py:691-768`.

3. [A.10/D R3] Raising inside `_otr_body_score` will not fail loud as planned because its caller wraps the reroll/score block in `except Exception` and keeps the original line. Concrete fix: remove or narrow the catch around the body-gate scoring path, or catch only the LLM reroll call and let score failures propagate. Cite: `nodes/OTR_LedgerScriptWriter.py:1603-1659`, `nodes/OTR_LedgerScriptWriter.py:4635-4689`.

4. [A.11/D R3] “Raise `ValidationError`” is not build-ready. `OTR_LedgerScriptWriter.py` does not import a project-local `ValidationError`, and pydantic’s `ValidationError` is not a generic message exception for arbitrary contract/pitch/grammar failures. Concrete fix: use `ValueError` or add a named domain exception such as `WriterValidationFailure`, then raise `... from exc` at each soft-fail site. Cite: `nodes/OTR_LedgerScriptWriter.py:112-133`, `nodes/OTR_LedgerScriptWriter.py:3169-3174`, `nodes/OTR_LedgerScriptWriter.py:3209-3213`, `nodes/OTR_LedgerScriptWriter.py:3409-3415`.

5. [A.3/A.4/D R1] `_resolve_character_voices_fail_soft` combines three different behaviors: missing cast preset synthesis, mis-stamped announcer rerouting, and true orphan reassignment. “Fail loud on an unvoiceable character line” is ambiguous enough to break valid rerouting if implemented mechanically. Concrete fix: split this function; keep or move the announcer-role repair explicitly, and raise only for true character rows/lines that remain unvoiceable after CastLock. Cite: `nodes/cast_lock.py:180-192`, `nodes/cast_lock.py:387-513`.

6. [A.7/D R2] “Raise on an unresolved explicitly-named slot” is underspecified for the current data shape. `resolve_engine_for_role` receives `image_policy` and maps role to slot, but it currently treats missing key, empty dict entry, and empty string similarly before falling back. Concrete fix: define explicit as `slot in image_policy["image_models"]` and `_eid(models[slot]) == ""`; raise `ImageRenderError`/`ValueError` there, and separately define behavior when the slot key is absent. Cite: `nodes/otr_image_gen_dispatcher.py:157-173`, `nodes/otr_image_gen_dispatcher.py:475-492`.

SHOULD-FIX:
1. [A.6/C1] If stage-direction-only dialogue must “never reach the voice gate,” failing only in `_otr_voice_node_common.py` is late. The writer scrub currently keeps the original text when stripping would empty the line, which can still send parenthetical-only text downstream. Concrete fix: add a writer/pre-freeze assert for voiced lines emptied by the stage-direction scrub, then keep the TTS-side raise as a defense. Cite: `nodes/OTR_LedgerScriptWriter.py:5037-5054`, `nodes/_otr_voice_node_common.py:430-442`.

2. [A.13/D R3] Outro fallback removal needs call-site detail. There is an in-loop deterministic placeholder before the post-loop LLM outro pass; ripping it without replacing the progressive-ledger crash behavior changes partial-ledger recovery, not just model fallback behavior. Concrete fix: decide whether the placeholder becomes empty plus hard-fail at post-loop, or whether writing the placeholder is allowed only for crash recovery and never final output. Cite: `nodes/OTR_LedgerScriptWriter.py:4756-4766`, `nodes/OTR_LedgerScriptWriter.py:4880-4978`.

3. [A.14/D R3] Character portrait fallback is in `derive_image_prompts`, not `story_orchestrator`, and its contract explicitly says it “Never raises; never emits an empty prompt.” Concrete fix: update that function contract and all callers/tests expecting warnings plus template output before changing behavior. Cite: `nodes/otr_meta_brief_image_prompt.py:1028-1047`, `nodes/otr_meta_brief_image_prompt.py:1110-1156`.

4. [C2] The valid-model dropdown workstream is not implementable as written for Comfy Cloud. Current code says partner V3 combo options are excluded from the pin and defaults are “VERIFY”; no live catalog source is specified. Concrete fix: define the refresh source/script and failure mode, or keep this out of R1-R4. Cite: `nodes/_otr_shared/cloud_model_ids.py:1-15`, `nodes/_otr_shared/cloud_model_ids.py:29-45`.

OPTIONAL / NICE-TO-HAVE:
1. [A.1/D R1] Rename `_resolve_character_voices_fail_soft` after changing behavior; the current name will become false and will mislead test authors. Cite: `nodes/cast_lock.py:387`.
2. [A.1] Remove `missing_ref_fallback` from the base adapter comments/default too, not just concrete adapters, so new adapters do not copy a dead contract. Cite: `nodes/_otr_audio_engines/base.py:50-60`.

CUT THESE (over-engineering):
1. [D R4] Cut “workflow-JSON audit” from this rip unless a node input/widget/dropdown actually changes. The listed R1-R3 changes are runtime behavior changes; forcing workflow validation here adds noise unless C2/R5 dropdown work lands in the same patch.
2. [A.9] Cut or demote the Bark healthcheck rip if no in-repo caller exists. `_bark_health_check_for_cast` is defined in `story_orchestrator.py`, but current grep finds no call from `OTR_LedgerScriptWriter.py`; verify external/manual use before spending R3 time on it.