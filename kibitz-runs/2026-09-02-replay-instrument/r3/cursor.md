VERDICT: no -- r3 has not settled the node-7 cue-bus contract, the writer-entry order, MetaBrief, or freeze-identity, and coding the r2 fold as written will raise or mutate the source episode on the first replay.

MUST-FIX BEFORE BUILD:
1. [§9.1 / D3''] Node 7 `assemble()` currently raises if `music_cue_audio` is present and `music_cue_manifest_json` is blank (`nodes/scene_sequencer.py:1246-1248`; `parse_manifest("")` returns None at `nodes/_otr_cue_manifest.py:100-102`). Canonical links 282/283 always deliver an AUDIO dict from node 83, so that dict is never None. Fix: take the replay branch (byte-copy master, SHA vs manifest, then `audio_done`) BEFORE the cue pair check. Do not rely on an 83 stub to pass None. Wire the new socket as trailing optional `forceInput` STRING `replay_descriptor` at INPUT_TYPES optional end and canonical inputs index 10 (current last is `video_policy_json` at dst_slot 9, link 286). New link: `[289, 62, 6, 7, 10, "STRING"]` from `OTR_LedgerFreezeCascade.v2_ledger_json` (slot 6), bump `last_link_id` 288 -> 289. Do not source from node 1 `script_json` (pre-freeze, only fans to 62 via link 230). No new `widgets_values` slot (`forceInput`). Add `replay_descriptor=""` to `assemble()` -- `tests/test_input_types_signature_parity.py` will fail if the kwarg is missing.

2. [§9.2 / D3''] Writer `replay_from` must be the first statement of `OTR_LedgerScriptWriter.run()`, before `_ROLLS.resolve_bank_selection` / `_ROLLS.resolve_style_selection` (`nodes/OTR_LedgerScriptWriter.py:2919-2924`), `require_runnable_bank` (`:2925`), visual-style resolve (`:2930-2931`), and `_preflight_llm_selection` (`:2955-2964`). Those gates bind the live widgets, not the bundle. Append the widget after `gate_in` in optional (not mid-list). `gate_in` is forceInput and is canonical input index 32 (link 279 dst_slot 32); new widget-backed input is index 33; `widgets_values` currently ends at `"Q8_0"` (31 slots) -- append `""` as slot 32. Put `replay_from=""` on `run()` before the hidden auth kwargs. Add `replay_from` to BOTH `CREATIVE_WHITELIST` copies (`scripts/otr_api.py:831`, `nodes/_otr_workflow_apply.py:681`; equality pinned by `tests/test_workflow_apply.py`). `otr_canonical_api_run.py --replay-from` must `patch_creative` that widget; do not send the path through `_parse_value` (`scripts/otr_canonical_api_run.py:46-51`) -- a path that happens to be valid JSON would change type.

3. [§9.2] Node 89 `OTR_MetaBriefImagePromptGen` is not on the bypass list and runs after ShotLock (link 255: 90.out0 -> 89.in0; ComfyUI order follows links, the saved `order: 6` is stale). `generate()` always calls `_resolve_writer_llm` then `derive_image_prompts(..., llm_fn=...)` (`nodes/otr_meta_brief_image_prompt.py:2530-2532`). That is an LLM/VRAM hit on every A/A replay and, if node 91 still keys cache on live `prompt_hash` (`otr_image_gen_dispatcher.py:1514-1528`), a cache miss -> `gen_fn`. Fix: on `meta.replay_from`, 89 returns a typed JSON stub and does not resolve an LLM; 91 takes a first-line verify-and-restamp of imported `ledger["images"]` files and does not iterate MetaBrief objects or call `gen_fn`. D6' "no CUDA/LLM/TTS/image calls" is false without this.

4. [§9.4] `freeze_timestamp` alone is the same-run predicate in `_same_durable_run` (`nodes/production_ledger.py:444-466`) and `_same_frozen_episode` (`nodes/otr_shot_lock.py:167-186`). Episode id is consulted only when NEITHER side has a freeze receipt -- that is load-bearing for pending_* -> slug rename. Replay that keeps the source freeze_timestamp is therefore the same durable run as the source. If `in_flight_ledger_path()` is still the source file, ShotLock `overlay_audio_timing(..., strict=True)` (`otr_shot_lock.py:2928`) and later `stamp_durable` will join/write the ORIGINAL episode. Do not mint a second freeze timestamp (breaks banana variety at `otr_image_gen_dispatcher.py:1172-1176` and freeze-mismatch rejects at `:594-601` / ShotLock `:180-186`). Fix: stamp a workspace-unique `meta.replay_workspace_id` in `import_replay_bundle`; if either side has that key, both must match, in BOTH identity helpers. `import_replay_bundle` must `new_ledger(new_id)` / rebind `_CURRENT` before any downstream peek. Keep `freeze_timestamp` byte-identical for variety and freeze-receipt consumers.

5. [§9.2 / D3''] Node 3 placeholder cannot be `empty_audio_batch` (`[1,1,0]` in `nodes/_otr_resolved_request.py:183-187`). D3'' leaves node 4 unbranched; `OTR_AudioEnhance.enhance` resamples with `torchaudio.transforms.Resample` then Haas/LPF (`nodes/audio_enhance.py:390-408`). Canonical node 4 `haas_delay_ms` widget is 0.8. Fix: node 3 returns CPU float32 AUDIO `{waveform: [1, C, T], sample_rate: 48000}` with T large enough for that DSP (C=1 or 2). 81/82/83 may use `empty_audio_batch` + `done="replay:passthrough"` only because 3/7 do not consume their audio; 83 `cue_manifest_json` may be `""` only because of must-fix 1. Device: CPU tensors only (node 4 already `.cpu()` at `:388`).

6. [§7 D3'' / §9] Dual writers of `{ep}_master.wav`: `import_replay_bundle` "materializes assets" and node 7 "byte-copies the frozen master". Assembler today derives `<in_flight_audio_dir>/<stem>_master.wav` (`scene_sequencer.py:1436-1451`) and PCM-encodes (`:1462-1475`). Fix: one owner. Import rebases stills/portraits/ledger paths only. Node 7 copies the manifest's master entry (relative path) onto that canonical filename, SHA-256 vs manifest, THEN emits `audio_done`; load the copy only for the AUDIO return (links 15, 272). On SHA miss, raise -- do not emit `audio_done` (ShotLock is gated by link 253). Never re-encode the file.

7. [§8 / §9.3 / Q4] `actual_request_sha` over the full receipt including wall seconds, peak VRAM, and `render_run_id` makes two A/A nulls differ. Hash only causal sampler inputs: final pos/neg text, seed, frames, canvas, fps, denoise, adapter id + resolved strength, context/injection, still content hash, `model_artifacts` digests, `comparison_seed_hash`/`render_request_hash`, engine/recipe/implementation, `sampler_inputs` (explicit nulls on still_flat). Exclude wall_s, peak_vram, `render_run_id`, status timestamps. Stamp `meta.render_trace` once via `stamp_durable(meta_updates=...)` after every segment; do not catch `LedgerStampError` in node 92 (`nodes/otr_video_render_batch.py:307-310` pattern). verify: haunted `sampler_inputs` eight field names -- the "section 2 recipe table" is not in this r3 document and there is no `sampler_inputs` / `haunted` symbol in `nodes/_otr_video_engines/` at HEAD. Cheap family: all sampler keys null + still path hash, as specified.

8. [§D6' live proof] `video.shots` is not on disk today (ShotLock returns wire-only at `otr_shot_lock.py:3060-3076`; TOP_PRESERVE omits `video` at `production_ledger.py:1592-1595`). Freezing `signal_lost_the_tectal_echo_20260902_131902` cannot exercise planned-section reuse. Sequence: ship D1' (`stamp_durable({"video": ...})` + TOP_PRESERVE `video`), render a new canonical episode, freeze THAT, replay twice. Add `"video"` only to TOP_PRESERVE; do not add `render_trace`.

SHOULD-FIX:
1. [§9.5] `_still_index` reads `im["path"]` only (`render_driver.py:618`), newest `scene_*` row wins. Import must rebase `path` (not only `pool_path`) onto the new episode copy or node 92 will open the source episode's stills. Dispatcher prefers `pool_path` then `path` (`otr_image_gen_dispatcher.py:1528`). After both point inside the new dir, skip `_materialize_episode_copy` when src already equals the hashed dest (`:622-631`); Windows `shutil.copyfile` onto self fails.

2. [§9.2] Freeze-cascade stub must return the same JSON on slot 1 `script_json` and slot 6 `v2_ledger_json` (HEAD already does: `OTR_LedgerFreezeCascade.py:400-408`). Early-return BEFORE `has_current_ledger()` needs_full_rerun (`:213-227`) and BEFORE Phase 10 remints `meta.freeze_timestamp` (`nodes/_otr_ledger_freeze.py:1097`). CastLock replay return BEFORE `_enforce_freeze_gate` and BEFORE `cast_lock_revision += 1` (`cast_lock.py:336, 349-355`).

3. [§D4' / profile] Freeze script output must go through the same resolver as `production_ledger._default_out_dir` (`:_otr_paths.otr_audio_dir` / ComfyUI output). Live tree is `C:\Users\jeffr\Documents\ComfyUI\output\otr\...`, not a repo-relative `otr/episodes/_replay/`. Manifest: relative paths only, as r2 stated.

4. [§9.1] `build_variants.py --all` then `--check`, plus the four workflow tests, after the two canonical edits (writer widget + node 7 link). Repair new links by identity, not arithmetic.

5. [§D5'] `--title` patches `episode_title` (`otr_canonical_api_run.py:97-98`) but published title is ledger-owned (assembler tooltip `scene_sequencer.py:1157-1160`). Item 0 A/A can ignore this; if a later arm needs a distinct title card, stamp it in `import_replay_bundle`, do not expect the writer widget to survive the short-circuit.

6. [Q3] Keep writer `IS_CHANGED` as `time.time()` (`OTR_LedgerScriptWriter.py:2789-2793`). A hash-based IS_CHANGED that omits bundle mtime/digest will serve a stale short-circuit. ShotLock `IS_CHANGED` is routing fingerprint only (`otr_shot_lock.py:2902-2912`); freeze `IS_CHANGED` is also `time.time()` (`OTR_LedgerFreezeCascade.py:187-189`) so the new episode_id string still invalidates 90.

OPTIONAL / NICE-TO-HAVE:
- Tiny `{replay_from, episode_id, replay_workspace_id}` descriptor instead of fan-out of full `v2_ledger_json` onto node 7.
- Replay `episode_id` suffix with microseconds; `YYYYMMDD_HHMMSS` collides on same-second double replay (`rename_episode` hard-fails if dest exists, `production_ledger.py:788`).
- On replay, keep `video_revision` unchanged (`otr_shot_lock.py:3021-3061`) so the planned section is not revision-bumped.

CUT THESE:
1. A second freeze timestamp / `content_freeze_id` used as `_same_durable_run` identity -- it either collides with source (useless) or diverges freeze receipts (breaks ShotLock/dispatcher join). Workspace id is the smaller patch.
2. Node-4 replay branch -- safe to keep cut if node 3's placeholder is DSP-safe (must-fix 5).
3. Per-line WAV copy and top-level `render_trace` / TOP_PRESERVE entry -- already cut; node 92 slices the master (`otr_video_render_batch.py:418-426`).
4. Peer-engine still re-mint in item 0 -- 91 restamp only.
5. Passing `--replay-from` through a new patch seam besides CREATIVE_WHITELIST -- that whitelist is the only sanctioned direct-widget path (`scripts/otr_canonical_api_run.py:7-8, 88`).

[ASSUMPTION] torchaudio Resample + Haas on T=0 is unsafe; not executed here. [ASSUMPTION] "haunted engine" sampler field list is in an unreadable campaign doc (`../PROBLEM_STATEMENT.md` missing at the path in this anchor). verify: that table against the live adapter `_build_render_request` before coding receipts.
