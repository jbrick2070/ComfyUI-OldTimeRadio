VERDICT: yes-with-fixes -- A1/B/C are otherwise implementable, but the R4 count-correction still leaves interpret+treatment gates that can make a binding selected count and the listener's named/exclusive notes mutually irreparable.

MUST-FIX BEFORE BUILD:
1. [A1.1 + A1.3 + `_make_interpret_validator` / `_make_treatment_validator`] Binding-count amendment did not name the three cooperating gates that still implement the superseded exclusive/named override. Current code: interpret computes `expected = len(required) if plan.exclusive else max(len(required), requested)` and rejects `plan.planned != expected` (`nodes/_otr_my_story.py` 458-477); if `expected > voice_capacity` it `raise MyStoryCastError` (hard abort, not `PostValidationError`, so the typed ladder never runs; 463-470); treatment then requires `len(names) == interp.cast_plan.planned` AND every `required_speakers()` name in the cast (529-545, 555-557). After A1.1 switches treatment to the selected widget, requested=2 + exclusive three named people is unsatisfiable (interpret demands 3, treatment demands 2, membership still demands all three). That should have been closed when drift-acceptance was withdrawn. Concrete fix: (a) interpret stops recomputing expected from `exclusive`/`required`; `cast_plan.planned`/`exclusive` are receipts only; delete the `MyStoryCastError` predicted-pool abort; (b) treatment count authority is selected `num_characters`/`act_count` (announcer excluded, as `count_locked_characters` already does at `nodes/_otr_casting.py` 1396-1416); (c) a named required speaker who will not fit the selected count is a recorded conflict, not a validator `return` -- model reorganizes; no Python drop. Actual later voice-allocation failure stays a real failure.

SHOULD-FIX:
1. [A1.2] Count mismatch is `PostValidationError`, so `make_dispatching_repair_factory` falls through to `default_repair_prompt_factory` which embeds `failed_output[:400]` (`nodes/_otr_repair_prompts.py` 371-403; `nodes/_otr_structured_call.py` 479). Same 400-char generic path is what last-act coverage uses ("this is the last act and ..." is not `"locked cast"`). Concrete fix: wrap the dispatching factory on My Story treatment AND act `repair_prompt_factory=` kwargs; JSON/schema typed prompts stay; generic post-validation gets the complete failed artifact + original messages + selected counts. Do not un-truncate `default_repair_prompt_factory` for every bank in the A1 commit.
2. [A1.2] "Original listener input" is already inside `_pass_treatment`'s prompt (`nodes/_otr_my_story.py` 572-579). Paste the reorganize directive + full `failed_output` onto `original_prompt`; do not triplicate the idea.
3. [A2] Keep the R2/R3 split explicit in one sentence so 262144 cannot land in GGUF KV/reuse: nested AutoConfig (`text_config` before wrapper; `_read_advertised_context` today only reads top-level keys, `nodes/_otr_model_catalog.py` 689-704) is effective HF generation capacity after snapshot/download; `vram_priced_ctx` stays the current weights-only estimate; GGUF `n_ctx`/`reuse_key` unchanged; stop mutating `model_config.max_position_embeddings = _cap` (`nodes/_otr_model_loader.py` 1227-1229); remove `check_context_window` vs `HARD_VRAM_CONTEXT_LIMIT` (`nodes/_otr_loader_backends.py` 79-108). Prompt-fit uses actual/configured window, never curated 8192 as a veto (`CURATED_CONTEXT_OVERRIDES` Qwen pin at catalog 1828).
4. [A1.9 vs shared widget] Writer tooltip still says num_characters is "a request, not a cap" (`nodes/OTR_LedgerScriptWriter.py` 2071-2077). After the all-stories ruling that text is false for My Story and premature for Sci-Fi until A3. Update the tooltip in the A3 chunk (when all dispatch owners comply), not as a silent A1 UI lie.
5. [Sprint B] Journal site is `_SlotScheduler._make_generate_fn` after `base = _build_truncating_generate_fn(...); return base(...)` (`nodes/OTR_LedgerScriptWriter.py` 715-733). Inner OpenRouter closures are per-call and discarded. Copy primitives there for every provider; OpenRouter `receipt_out` is only the executed-slug sidecar on that inner `base`. Do not journal only inside `make_openrouter_generate_fn`.
6. [A3] Do not ship Sci-Fi/legacy alignment in the A1 commit. Named gaps are real: pitch/treatment `del n_max` (`nodes/_otr_scifi_news_pro.py` 984-1023, 1908-1910) and `_LEGACY_MAX_SPEAKING_CAST = 6` clamp/raise (`nodes/_otr_casting.py` 1176, 1468-1471, 1962-1969). A3 stays a separate qualified chunk after the My Story freeze proof, with its own finished-diff review as the plan already says.

OPTIONAL / NICE-TO-HAVE:
- Import `ProviderCapacityMessages` from `nodes/_otr_generation_budget.py` (Sci-Fi already does at `_otr_scifi_news_pro.py` 55/86), not from the Sci-Fi module.
- Character-level overlong-token split in C: hard glyph break, no hyphenation policy needed if every fragment is measured into column 1.
- Shared `num_characters` tooltip wording in A3: "selected speaking-character count, announcer excluded, binding."

CUT THESE:
1. Everything after the Recovery-gate / stale-registry banner in this input (from "~ THE ONE OPEN REGISTRY ITEM" through the truncated standing-rulings/shipping-JSON fragments). Pickup already parks the older queue outside this campaign; executing it would restart registry/publish/Mac work.
2. Sprint 5 App/player/publication matrix. A freeze-62 component proof is not D1 §11 publication.
3. Replay/migration, font-resolver refactor, generic schema binder, new widgets, `compute_episode_budget` for My Story (already unused in `_otr_my_story.py`), GGUF reuse rewrite, VRAM-estimator rewrite, two-line hero ellipsis, invented Original name map.
4. Speaker-placement follow-on. Explicitly not priority #1.

VERIFY-AT-BUILD checklist:
1. [R1 driver UNVERIFIABLE] Live Qwen3.5-4B 1-act/2-character repair of prompt `e11e5d86-2dda-41fa-b942-bb927a754b83` yields accepted_acts==requested_acts, durable ledger, freeze `frozen_clean` or `frozen_with_warns`. History success is not enough. 3/6-act legs are topology extras; timeout/OOM is a capacity outcome, not a count-policy regression.
2. [R1] `len(workflows/otr_canonical.json["links"])` is 63 (plan claim). Grounded: `last_link_id` 291; link `[291, 1, 1, 85, 10, "STRING"]` is writer `script_json` -> `OTR_MasterAudioMux` 85; writer `widgets_values` length 37; 23 `OTR_*` nodes; node 62 is `OTR_LedgerFreezeCascade`.
3. [R2] `partial_execution_targets: ["62"]` still accepted on this ComfyUI (prior prompt id is one data point, not a guarantee).
4. [R1/R2] Do not enable the local schema binder. verify: installed `lmformatenforcer` `JsonSchemaParser` default max array length is still 20 (`nodes/_otr_constrained_generate.py` exists; `invoke_structured_slot` does not call it). Treat 20 as measured at enable-time, not as repo source.
5. [R2] On-disk Qwen `config.json` / `text_config.max_position_embeddings` vs curated 8192 vs whatever AutoConfig returns after snapshot resolution, including first download.
6. [R1] Comfy Credits executed identity field on the real cache_entry (plan says "slug"; verify the key, do not invent).
7. [R3] OpenRouter model-gone fallback identity is read from the successful `OpenRouterBackend.generate` path (`nodes/_otr_openrouter_backend.py` ~1393/1543), after `_extract_text` and `assert_no_verbatim_cycle` in `make_openrouter_generate_fn` (1686-1707), never from `resolved_models_snapshot`.
8. [A1.5] Repair turns inherit `ProviderCapacityMessages` markers via structured_call rewrap (`nodes/_otr_structured_call.py` 546) and `max_new_tokens is None` at the actual slot_fn (base and typed repair).
9. [A1.8] Content-owned canonical text still present after shared `_otr_clean_transaction` / `ledger_clean` / `ledger_cleanup` / freeze; My Story does not skip the tail.
10. [Sprint C] Real-font PNG: short Lightning title unchanged; exact Lantern title every glyph inside column 1; one overlong token split without loss; scratch `_draw_col1` height matches paint; FRAMES/VRAM/REV abridgment allowed, SEED/COMMIT and full title remain.

[ASSUMPTION] Live failed My Story input did not also hit exclusive named-count vs slider; the gate clash is still reachable on the planned 1/3/6 and character-mismatch fixtures. [ASSUMPTION] SlotScheduler has no second success-return around the constrained-bind path that would skip the journal copy; verify `_otr_bind_schema` rebuild still hits the same `base(...)` line.
