# R4 grounded judgment - Codex

VERDICT: converged for A1, bounded A2, B and C. Both renewed R4 calls rc0.
Latest selected-count instruction replaces drift acceptance. No further review
round is justified by these localized implementation details. A3 all-bank
alignment remains its own pending reviewed chunk, explicitly not qualified here.

CONFIRMED / folded or already required:
- AgY M1/CursorM1: forward selected characters to initial treatment prompt,
  validator and repair factory; delete exclusive/max(named,requested) override,
  predicted pool abort and required-name veto that makes the selected count
  impossible. Record source conflicts; actual allocation remains honest.
- AgY M2: remove act-number postvalidation refusal; normalize slot after accepted
  response, before assembly. Keep original numbers as receipts.
- AgY M3: optional frame fields need schema defaults and removal of blank/cue
  postchecks, not only a later assembly change. Already included in A1 schemas.
- CursorS1-S2: local repair factory retains original message roles and complete
  parsed failed artifact for BOTH treatment counts and last-act coverage. Raw
  listener input is already in treatment prompt; do not duplicate it again.
  Syntax/schema cases retain existing typed prompts, no global400-char change.
- AgY S1-S3: explicit breaks flag/actual boundary cues, correct binding-count
  receipt note, measured glyph splitting in same flow. Already plan requirements.
- AgY optional: reserve ANNOUNCER name in treatment cast; existing _assemble
  removes that name from lookup, so collision is a genuine ledger identity defect
  and should enter the existing treatment repair rather than a later KeyError.
- CursorS3: known HF config vs allocated GGUF capacity split remains explicit.
- CursorS4/S6: shared character tooltip and older lane count gaps belong A3.
- CursorS5: actual journal site is the inner generate_fn within for_slot's
  _make_generate_fn. All provider and bound-schema returns share this one site.
- ComfyCredits actual cache key 'slug' verified at _otr_comfy_backend459.
- Failed live input requested1/two named people agrees; renewed run must mint
  a NEW prompt id using identical input, never reuse the old prompt id.

MISREAD / rejected:
- AgY M4 calls the existing title producer an unwanted extra call. The plan
  deliberately invokes that existing owner on missing title; 'no extra call'
  means no new stage. RawStoryFields has NO headline/title properties. Reject
  invented 'My Story Episode' fallback. Shared cleanup already has a truthful
  source-title backstop. Retain optional override and fix late title parity.
  Agent reproduced real-tail gap: ledger/wire filled, canon/file/news blank.
- AgY M5 test_original_radio_pipeline50 reads BANK DEFAULTS, not per-episode meta.
  B stamps runtime identity late; static generic default remains. Do not change
  that registry test to assert impossible runtime identities in the bank row.
- AgY visual verify x<654 is 1080 reference space, not universal canvas pixels.
  Measure scaled col1 coordinates; planned1080/720/832x480 proof is adequate.
- Do not delete old pending registry/App/speaker-placement work from main plan
  because it is outside this campaign. Keep scope fences. Fragmented completed
  history was independently audited and moved verbatim to archive, retaining
  real constraints/open work. Future review snapshots exclude that older queue.
- Canonical shape is verified unchanged by these changes, not permanently
  forbidden to change if an actual schema/wiring defect requires a same-chunk fix.

Verification at build remains real: full canonical target62, durable freeze
frozen_clean/frozen_with_warns, counts matching selected values, raw source,
byline and content ownership intact. One/three/six component legs, no publication
claim. Full suite baseline comparison + Bible and one finished-diff CLI review
per code chunk; commit/push and integrity audit. Baseline static credits PNGs
at canonical output/otr/episodes/credits_title_containment_20260910/evidence
show exact Lantern overlap at three scales; no prior published asset changed.
