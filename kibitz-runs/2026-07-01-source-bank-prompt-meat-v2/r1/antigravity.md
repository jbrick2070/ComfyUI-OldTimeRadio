VERDICT: build-ready as-is? no. High-level architecture contains contradictions between goals and implementation, leaks sci-fi terminology across several unaddressed python prompt sites, and includes premature scope creep for unfinished features.

MUST-FIX BEFORE BUILD:
1. [architecture-preference] The spec states a preference to "Avoid a fragile mega-template where every prompt is a pile of tiny variables" (line 55) but the proposed profile variables (e.g., `story_form_label`, `source_material_label`, `source_develop_verb`, `source_grounding_label` in LEDGER_PROMPT_AUDIT.md) do exactly that. A true cloneable-pack architecture must allow packs to define full system and user prompt overrides rather than granular string variables. Align the implementation method to match the stated goal.
2. [architecture-preference / final.md: Sec 2] `_otr_style_picker.py` allows overriding `inventor_system_prompt` and `chooser_system_prompt`, but fails to allow overriding `_INVENTOR_USER_TEMPLATE` (nodes/_otr_style_picker.py:L301). This template hardcodes the string "distinct radio drama style descriptors", leaking radio drama framing to non-radio/non-science lanes. Add an override parameter for the inventor user template.
3. [story-remnants / final.md] The story grader in `nodes/_otr_story_select.py:L165` hardcodes: "You are a tough story editor grading a short science-fiction audio drama...". The plan does not thread prompt profile overrides to `grade_story()`, meaning non-science runs will still be evaluated as sci-fi audio dramas. Add the story-form/identity label parameter to `grade_story()` and replace the hardcoded string.
4. [story-remnants / final.md] The dramatic state builder in `nodes/_otr_dramatic_state_llm.py:L435` hardcodes: "You are the story architect for a short audio drama whose premise comes from a real news item. Define the CENTRAL CONFLICT so it is authentically ABOUT that news...". The function `derive_news_dramatic_state` does not accept a prompt profile or custom request variables, meaning news-specific terminology will leak into every story model. Thread the prompt profile variables (`source_material_label`, `key_terms_label`, etc.) into this function and prompt.
5. [story-remnants / final.md] The per-line composer in `nodes/_otr_line_composer.py:L1642` hardcodes: "Ground this line in the news facts...". The `LineRequest` struct (nodes/_otr_line_composer.py:L745) does not carry a custom `line_grounding_instruction` from the prompt profile, making it impossible for the composer to adapt its grounding rules. Add this field to `LineRequest` and thread it through the orchestrator.
6. [story-remnants / final.md] The post-composition title regenerator in `nodes/OTR_LedgerScriptWriter.py:L936` hardcodes: "You are titling a single episode of a sci-fi radio drama." The function `_generate_title_from_script` does not receive the profile or a `title_form_label` parameter, leaking "sci-fi" to the LLM. Add the title-form label parameter to the function.
7. [final.md: Sec 1 vs Sec 2] Conflict between requirement and plan: `input.md` line 59 demands: "Expose planned source/story choices in the selector design. Do not hide public domain merely because the adapter is unfinished." However, `final.md` line 137 cuts it: "No public-domain workflow selector in first transplant." Resolve this contradiction; either expose the widgets with a loud error at run time or explicitly agree to defer the widget exposure to the transplant phase.

SHOULD-FIX:
1. [architecture-preference] The JSON schema for the files in `story_packs/` (line 26) is never defined. Without a schema contract, it is impossible to validate user-created story packs or compile them cleanly. Provide a draft JSON schema for the story packs.
2. [visual-styles-to-cover] Non-realistic visual styles like `anime`, `cartoon`, and `paper_origami` require specific checkpoints/LoRAs, but there is no concept of a model inventory check during ComfyUI startup or run time to verify these dependencies are present [ASSUMPTION]. Add an asset-inventory validation gate.
3. [visual-remnants] The LTX video engine (`nodes/_otr_video_engines/render_driver.py`) hardcodes lipsync and character fallback prompts assuming a realistic or 1940s style (e.g., "1940s radio actor speaking into a studio" at L1540). The new styles (like `paper_origami`) will trigger these fallbacks and create visual styles that mismatch the rest of the generation. Provide replacement overrides for lipsync/face fallbacks in the visual style catalog.

OPTIONAL / NICE-TO-HAVE:
1. [architecture-preference] For the "Simple 3-Prompt Experimental" path, pre-validate the output structure against a local schema before applying Python-heavy validation and repair. Local 7B-14B models frequently hallucinate key names, and early syntax-only repair will save expensive token calls [ASSUMPTION].

CUT THESE (scope / over-engineering):
1. [models-to-cover] Detailed prompt and Python change mappings for all 5 public-domain lanes (`faithful_radio_adaptation`, `chapter_digest_drama`, `comic_panel_radio_adaptation`, `stage_play_radio_adaptation`, `storybook_puppet_show`) should be cut from this phase's deliverable. Since the adapters are deferred/not built, mapping them now leads to speculative spec-writing that will drift. Focus only on `science_news_default` and `media_restoration_adventure`.
2. [visual-styles-to-cover] Detailed prompt mapping for experimental/non-realistic visual styles (`anime`, `cartoon`, `paper_origami`) should be cut. Since the rendering engine changes are deferred to V3, these styles cannot run successfully yet. Focus only on `sci_fi_radio` and `archival_documentary`.
