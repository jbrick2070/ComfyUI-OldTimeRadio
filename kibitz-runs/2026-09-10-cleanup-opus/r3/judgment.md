# R3 judgment - Astra

AgY yes; Cursor yes-with-fixes. Real Windows sources checked. No runtime cleanup applied.

| Claim | Judgment |
|---|---|
| AgY absolute child import / Cursor package-qualified loader | CONFIRMED import boundary. R2 already required package namespace, so not an actual instruction to import pytest's nodes; strengthened with the exact importlib expression and absolute script path. Stock ComfyUI nodes must not receive the sentinel. |
| Cursor inherit environment | CONFIRMED precaution; CPU/UTF-8 already specified. Explicitly inherit PATH/etc before setting output keys and importing the pack. |
| Cursor widget binding | CONFIRMED contract, already stated widget names in R2. Make filtering on the widget key explicit so five forceInput sockets cannot consume two widget values. |
| Cursor verdict/cleanup literals | CONFIRMED bank split at cascade:696/798. Add exact statuses and accepted success literals. Reject treating general phrase 'successful frozen verdict' as a literal string or requiring incidental warnings. |
| AgY JSON counters / Cursor no-ledger helper | CONFIRMED scope. Existing node-module json proxy plus forced successful current-ledger fixture already excludes helper paths; retain two-dump assertions. Never patch global json.dumps. |
| Cursor two serialization comments | CONFIRMED. First before unload, second carries receipt if successful; neither guarantees latest JSON when serialization fails. |
| Both secondary direct callback invocation | Primary proof is actual node. Use an independently rebuilt valid ledger for the secondary public callback proof; no new public node callback input. Earlier successful repeated-entry probe stands but need not burden this test. |
| AgY verdict/unload receipt fields | ADOPT explicit readable fields. Cursor package/module names useful; process-local id(loader) is not stable evidence and omitted. |
| Cursor preserve readiness tests/public positional signature, B14/v2/replay nets | CONFIRMED current code. No default or type guard added. Existing selected tests remain unchanged unless a concrete changed contract is found. |
| Cursor no scheduler/second workflow, no acquisition solely for diagnostics | AGREED. Tests read actual canonical file; no server/GPU/model/scheduler proof claimed. |
| Cursor source_bank/voice coverage assumption | CONFIRMED by driver's actual two-bank fixture probe, not merely inferred. |
| Cursor slot-sweep floor from R2 | Numeric post-removal count NOT measured. No claim it stays above 12. Recount at build; a test floor never warrants retaining dead runtime work. |

Latest seeded full suite finished: collected 14166, exit 2, exact same 54 unexpected failing node IDs as pre-retirement baseline. Focused fresh subprocess test and Bible passed. Preparation remains test-only.

Operator added clean audio after R3 dispatch. C4 was NOT reviewed by these R3 outputs. Its bounded R1-R3 addition will use the same two lanes, then a shared final R4 convergence reviews the integrated plan. No new worker/model/paid panel. This is required because C4 changes the actual canonical settings/schema; it must not inherit a fictitious full-arc approval from C1-C3.
