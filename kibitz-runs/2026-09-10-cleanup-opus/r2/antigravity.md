VERDICT: yes-with-fixes. The plan is well-grounded and checkable against the real codebase, but requires explicit test isolation guards on `production_ledger._CURRENT` and precise assertion string updates for G8 collision diagnostics before implementation.

MUST-FIX BEFORE BUILD:
1. [Section 4 P3 / Section 1 C3] State leakage across tests via `production_ledger._CURRENT`. The fresh fixture recipe in P3 (line 124) calls `production_ledger.new_ledger()`, which mutates the global module singleton `production_ledger._CURRENT` (`nodes/production_ledger.py:101`, `508-512`). If not isolated, subsequent tests in the suite expecting `_CURRENT` to be `None` or pointing to a different ledger will fail non-deterministically during full test runs.
Fix: In `tests/test_freeze_policy_readonly.py`, manage `production_ledger._CURRENT` within a pytest fixture or `try/finally` context that records `_CURRENT` at entry and unconditionally restores it on exit.

2. [Section 1 C2 / Section 4 P2] String mismatch in line-ID collision test assertion. In `tests/test_lfc_phase_0_10_gap_audit.py:247`, `test_duplicate_line_id_raises` asserts `any("duplicated" in e for e in ei.value.errors)`. Removing `_check_per_line_invariants`'s collision check (`nodes/_otr_ledger_freeze.py:310-313`) means the only surviving collision error comes from `_check_g8_line_id_uniqueness:933`, which formats messages as `"G8: {len(duplicates)} duplicate line_id(s) across ledger.lines[]: ..."` (using `"duplicate"`, not `"duplicated"`).
Fix: Explicitly update line 247 of `tests/test_lfc_phase_0_10_gap_audit.py` to assert `any("duplicate" in e or "G8" in e for e in ei.value.errors)` while keeping the distinct `"duplicated"` check for char-IDs at line 422 (`_check_per_cast_invariants:399`).

3. [Section 1 C3 / Section 4 P3] Loose mock sentinels in unload and passthrough tests. In `tests/test_lfc_b1_cascade_unload_in_finally.py:56-64`, `tests/test_cascade_freeze_unload_visible.py:60-68`, and `tests/test_lfc_c4_news_used_passthrough.py:60-68`, `request_slot` and `make_generate_fn` are patched with dummy return values. When removing model acquisition from `nodes/OTR_LedgerFreezeCascade.py:281-287`, merely removing or relaxing these patches risks allowing accidental re-introduction of model loading without failing tests.
Fix: In all three test files, patch `_otr_model_loader.request_slot` and `_otr_model_loader.make_generate_fn` with `MagicMock(side_effect=AssertionError("request_slot must not be called during freeze cascade"))` and assert `request_slot.call_count == 0` and `make_generate_fn.call_count == 0` across clean runs, cascade exceptions, and fallback paths.

SHOULD-FIX:
1. [Section 1 C1] Stale comment cleanups in `nodes/scene_sequencer.py`. Beyond lines 928-932 (`breath_ms`, `beat_pause_ms`, etc.) and lines 988/1124 (`current_character_name`), line 922 contains the comment `# pacing defaults are inlined here, voice_map was unused.` and lines 9-10 in the module docstring advertise `intelligent pacing (breath buffers, BEAT/PAUSE tags)`.
Fix: Remove the comment at line 922 and update the module docstring (lines 6-15) in `nodes/scene_sequencer.py` to reflect that `SceneSequencer` receives pre-rendered voice clips and positions them against ledger lines with roomtone/DSP without performing internal text-to-speech generation or buffer pacing.

2. [Section 4 P3 Table 134] Missing test module from C3 test matrix. Table 134 lists `test_freeze_policy_readonly.py`, `test_lfc_b1_cascade_unload_in_finally.py`, `test_cascade_freeze_unload_visible.py`, `test_lfc_b14_unload_on_exit.py`, `test_lfc_c4_news_used_passthrough.py`, `test_freeze_cascade_v2_ports.py`, and `test_canonical_replay.py`. It omits `tests/test_freeze_unload_ok_consumed.py`.
Fix: Add `tests/test_freeze_unload_ok_consumed.py` to the C3 pytest command line in Table 134 to verify AST compliance on `nodes/OTR_LedgerFreezeCascade.py` and ensure the `freeze_unload_ok` producer contract is verified alongside the consumer side.

3. [Section 4 P2] Integer line_id test case in `tests/test_g8_line_id_uniqueness.py`. Line 108 notes that non-string IDs must produce per-line invalid-ID errors and no G8 collisions. In `tests/test_g8_line_id_uniqueness.py:109-126`, `test_g8_skips_lines_without_line_id` currently checks `None`, `""`, and missing keys, but does not test non-string types like integers (e.g. `{"line_id": 1001}`).
Fix: Add a test row with integer `line_id` (`{"line_id": 1001, "char_id": "c01", "speaker_role": "character", "text": "int id"}`) to `test_g8_skips_lines_without_line_id` to confirm G8 safely skips non-string line IDs.

OPTIONAL / NICE-TO-HAVE:
1. [Section 4 P3] In `tests/test_llm_runtime_policy.py:339`, when renaming `test_downstream_llm_consumers_thread_the_ledger_policy` to `test_shot_lock_threads_the_ledger_policy`, ensure the docstring at line 340 is updated from `"FreezeCascade + shot-lock request_slot calls carry policy=policy_from_meta(...)"` to name only `OTR_ShotLock`.

CUT THESE (over-engineering):
1. [Section 2] Do not extract a common audio offset or pacing helper between `SceneSequencer` and `EpisodeAssembler`. The offset promotion logic in `EpisodeAssembler` (`nodes/scene_sequencer.py:1896-1938`) handles crossfades and legacy master-mix spaces, while `SceneSequencer` deals strictly with scene-audio sample accumulation. Keeping them separate avoids introducing unnecessary shared abstractions.
2. [Section 1 C3] Do not refactor or deduplicate the two JSON serialization blocks in `nodes/OTR_LedgerFreezeCascade.py` (lines 343-353 and 403-414). The pre-finally serialization preserves post-freeze ledger state if assembly raises, while the post-finally serialization guarantees `freeze_unload_ok` reaches downstream nodes. Keeping them distinct and fail-soft is safer than combining them.
3. [Section 1 C3] Do not alter the `technical_model` input socket signature or forceInput requirement in `nodes/OTR_LedgerFreezeCascade.py:142-152`. Retaining the socket and validating non-empty input with `_OTRMI.require_model` guarantees workflow schema stability across all canonical graphs without requiring graph rewiring.
