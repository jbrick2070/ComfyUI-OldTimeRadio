# R2 judgment - Astra

Both reviewers returned yes-with-fixes. All distinct claims adjudicated below; most AgY findings repeat explicit R1 requirements. No runtime cleanup implemented.

| Claim | Ruling |
|---|---|
| AgY must 1 / Cursor should 2: isolate _CURRENT | CONFIRMED code behavior; already required in R2 input P3. Locked permanent proof to a fresh subprocess, eliminating parent-module fixture pollution and restore-choice ambiguity. Actual fixture tested with production constructor/save/authorship. |
| AgY must 2 / Cursor should 3: G8 wording | CONFIRMED and already explicit. Reject AgY's loose 'duplicate OR G8' assertion; specify one G8-prefixed error containing the duplicated ID. Char-ID assertion unchanged. |
| AgY must 3: poisoned loader mocks | CONFIRMED, already mandatory for all three helpers. Explicit zero-call assertions retained. |
| AgY should 1: Scene header and 922 | ALREADY COVERED in R2 input C1; no new defect. |
| AgY should 2: add test_freeze_unload_ok_consumed as AST proof | MISREAD: file is source substring checks, not AST compliance/runtime recovery. It runs in the full suite; no extra gate or code retention to satisfy it. Actual consumer confirmed cast_lock:520-532 attempts defensive unload. |
| AgY should 3 / optional policy docstring | ALREADY COVERED integer ID and renamed shot-lock docstring. |
| AgY cuts: no common offset helper, retain two serializations/socket | CONFIRMED boundaries already required. Reject claim second serialization guarantees stamp delivery: its exception path keeps older JSON. |
| Cursor must 1: cannot inject callback through node | TRUE interface fact, MISREAD as a missing separation: input already said separately call real orchestrator. Final now explicitly runs real node with poisoned acquisition and separately calls the real orchestrator with a poisoned callback. No callback argument added to the node. |
| Cursor must 1: cleanup_locked blocks a second freeze | MISREAD if intended as an early-return blocker: phase_10 always calls run_gap_audit (_otr_ledger_freeze:1073), its docstring describes re-entry updating stamps. Astra repeated both real policy routes successfully with zero callback calls. Preserve repeated-call scope as deterministic re-entry, not scheduler-cache proof. |
| Cursor must 2: early-exit fixture details | CONFIRMED production_ledger:566-578 requires meta.replay_from, and B1 helper forces current ledger. Added exact JSON, separate patches, blank/whitespace errors and no-pretry-unload expectation. |
| Cursor must 3: serialization patch target | CONFIRMED need exact seam. Improve proposed patch: replace only node module's json binding with a loads/dumps proxy; do not mutate global json.dumps. Specify real serialization at call time and three failure combinations. |
| Cursor should 1: frozen_with_warns only | CONFIRMED current measured verdict/missing voice/portrait warnings; retain success-with-no-errors assertion rather than pinning incidental warning presence. Never require frozen_clean for this fixture. |
| Cursor should 2: source_bank before authorship, check durable verdict | CONFIRMED and adopted; parent state isolated by child. |
| Cursor should 3: reuse current G8 behavior tests | CONFIRMED; strengthen real calls, no new mirrored/source-string implementation. No combined mega-fixture needed. |
| Cursor should 4: node top docstring/S30 prose | CONFIRMED at node:1-5,135-140; added exact prose ownership. |
| Cursor should 5: runtime policy pin and seven failures | CONFIRMED source pin and qualification.json contains seven failure IDs in that module. Rename before running new node ID. |
| Cursor optional hashes/receipt | CONFIRMED goal, but self-hashing alone does not prove reproducibility. Astra found real NumPy noise in roomtone:692 and tape hiss:278, so initial deterministic claim was wrong. Fixed ONLY fresh harness/test with explicit per-case seed and two independent-process hash comparison. Focused passed 12.11s; Bible passed; full repeat running. Production untouched. |
| Cursor cut injecting callback into .run | AGREED, never proposed after clarification; no such API exists. |
| Cursor cut extra request_slot retention | AGREED. Old diagnostics never justify runtime work with no consumer. Slot sweep threshold is supplemental, not a reason to keep a call. |

R3 input adds an exact new fresh-process test recipe to make the canonical node proof independent of the old mock harness. This is PLANNED C3 work, not a claim that current acquiring node passed a no-acquisition test. The current direct real-orchestrator probe and repeated-entry proof passed both original/scifi_news_pro policies. Full seeded audio suite completion will be recorded in the final receipt. No new PBUG/Bible entry; no model/platform/scheduler changes.
