VERDICT: yes-with-fixes. C1/C2 production deletes and C3's `None` handoff match the live code; P3's test recipe as written cannot be coded as one node invocation and will miss the replay early-return unless the JSON shape is specified.

MUST-FIX BEFORE BUILD:
1. [P3 / C3] Callback proof and "node invocation" cannot be the same call. After C3, `OTR_LedgerFreezeCascade.run` always passes `None` into `_LFC_ORCH.run_freeze_cascade` (`nodes/OTR_LedgerFreezeCascade.py:313-318` today still passes `generate_fn`). A raising callback can only be injected via `_otr_freeze_cascade.run_freeze_cascade(cb, led, enable_phase_7_audio_readiness=True, enable_phase_8_video_readiness=True)`. `del generate_fn` in `_run_inline_safety_cleanup` (`nodes/_otr_freeze_cascade.py:692,792`) never invokes it; that is the only `generate_fn` use (no `generate_fn(` call exists). Freeze the same `led` twice and the second run hits `cleanup_locked`. Fix: two tests. (a) orchestrator + raising cb; assert counter==0, `retired_no_content_policy` on `original`, `not_applicable_content_owned` on `scifi_news_pro`. (b) separate graph asserts: canonical node 62 `widgets_values==[true,true]`, link `[115,1,4,62,4,"STRING"]` (`workflows/otr_canonical.json:1174-1177,3050-3056`), `NODE_CLASS_MAPPINGS["OTR_LedgerFreezeCascade"]`. Do not call `.run()` with that cb.

2. [P3 / C3] Replay / no-ledger / blank-model tests cannot reuse `_patch_runtime` in `tests/test_lfc_b1_cascade_unload_in_finally.py:44-66` (forces `has_current_ledger=True` and successful `request_slot`). Replay is decided only by `production_ledger.replay_descriptor` (`nodes/production_ledger.py:566-578`): nonempty `meta.replay_from`. `script_json="{}"` does not take that branch. Fix: replay call with `script_json='{"meta":{"replay_from":"bundle"}}'` (plus optional workspace id), no peek patch required, expect 7-tuple `out[4]=="replay"` as in `tests/test_canonical_replay.py:347-352`. No-ledger: `has_current_ledger=False`, do not patch peek, expect `needs_full_rerun`. Blank model: current-ledger patches only, `technical_model=""` / `"   "`, `pytest.raises` `nodes._otr_model_inputs.MissingModelInputError` (`require_model` at `_otr_model_inputs.py:72-85`, still before the try/finally at `OTR_LedgerFreezeCascade.py:265-269`). Fail-fast `request_slot`/`make_generate_fn` on all three; assert zero calls.

3. [P3 / C3] Serialization fallback needs a patch target. Both dumps are `json.dumps` in `nodes/OTR_LedgerFreezeCascade.py` (`343-352` then `403-413` after finally). Cascade exception never reaches the second dump. Fix: `patch("nodes.OTR_LedgerFreezeCascade.json.dumps", side_effect=[...])` on the success path only: `[TypeError("s1"), ok]` keeps incoming then may succeed post-finally; `[ok, TypeError("s2")]` keeps first serialization (no `freeze_unload_ok` if first ran pre-stamp); both fail keeps incoming/first. Do not patch `json.loads` (replay parse at line 216).

SHOULD-FIX:
1. [P3 fixture] Pin `disp.verdict=="frozen_with_warns"` and `not disp.gap_audit_post.errors`. Do not assert empty warnings or `frozen_clean`. `Ledger.__init__` has no `meta` (`production_ledger.py:973-992`); fixture cast omits `voice_preset` which is WARN-only (`_otr_ledger_freeze.py:560-569`); phase 8 missing portraits are warnings only (`_otr_readiness.py:460-468`). Recipe already reports `frozen_with_warns`.
2. [P3 fixture] Restore `_CURRENT` in `try/finally` around `new_ledger` (`production_ledger.py:508-512` is process-global). `led.save()` calls `refresh_text_metrics` (counts, not text) then cascade `_persist_cascade_meta` saves again (`_otr_freeze_cascade.py:598-610`). Assert freeze_verdict on the post-cascade file, not the pre-cascade save. Stamp `source_bank` before `stamp_receipt` (`_otr_content_authorship.py:88-91`). `accepted_artifacts={"probe":{"text":"The signal is clear."}}` matches `build_receipt` (`80-103`).
3. [P2] `tests/test_g8_line_id_uniqueness.py` already calls `run_gap_audit` / phase 0/10 and already pins 8 IDs -> `+2 more` (lines 84-106). Do not invent source-string tests. Add integer `line_id` to the skip case only (`_check_per_line_invariants` 308-309; G8 skips non-str at 921-922). In `test_duplicate_line_id_raises` pin `"G8:"` (G8 text is `duplicate`, not `duplicated`; char-id test at 417-422 stays).
4. [C3 comments] Module docstring `OTR_LedgerFreezeCascade.py:1-5` and S30 comment `137-150` still claim inline safety cleanup uses the model. Same chunk as the tooltip at 144-152. Keep `require_model` unbound (`strip()` return unused). Keep `_OTRML` for `unload_llm_if_local_resident` (`376`).
5. [C3 policy test] `test_llm_runtime_policy.py:339-349` is the only freeze source pin of `policy_from_meta` / `policy=lfc_policy`. Rename in the same chunk before the `::test_shot_lock_threads_the_ledger_policy` command. verify: "seven existing failures" in that module (not executed here).

OPTIONAL / NICE-TO-HAVE:
- C1 WAV hash compare: `tests/test_canonical_audio_check.py` already self-hashes `master_sha256`; C1 does not touch DSP. Capture pre-edit receipt hashes in the chunk log, do not add a name-absence test.
- Write `docs/2026-09-10-cleanup-opus/freeze_fixture_receipt.txt` from the real parametrize result.

CUT THESE:
1. A merged 8-dup + invalid-role fixture. Existing `test_g8_three_or_more_duplicates_reported_with_more_suffix` and `test_g8_runs_alongside_role_invariant_independently` already cover both. Safe: G8 still appends one summary (`_otr_ledger_freeze.py:927-938`).
2. Injecting the raising callback through `.run()` after C3. Node cannot forward it; unload/sentinel coverage already lives in the two mocked unload files plus C4 helper.
3. Extra `request_slot` retention for old diagnostics. `tests/test_workflow_json_guardrails.py` TestCascadeB3Surface only forbids `model_id` widget / requires `technical_model`; `tests/test_lfc_b14_unload_on_exit.py` only requires `_LFC_ORCH.run_freeze_cascade(` and `unload_llm_if_local_resident`. `tests/test_llm_slot_sweep.py` floor is 12; removing one tagged `request_slot` at freeze line 280-281 stays above it.

Grounded: C1 stores are write-only (`scene_sequencer.py` 928-932, 988, 1124; 7 matches, no reads). Assembler loop `1896-1921` matches. C2 G8 still runs at `run_gap_audit:728`. Writer dup check at `_otr_ledger_cleanup.py:490` stays. `__init__.py:171` is `OTR_SceneSequencer`. `require_model(model_id, *, slot=)` is real. `request_slot` / `make_generate_fn` / `unload_llm_if_local_resident` are real. `stamp_receipt(ledger_data, *, owner_bank, accepted_artifacts)` is real. Validator `validate(path, validate_anyway, strict_unknown_types, profile_id="", master_hash="", generated_by="", prompt=None, unique_id=None)` matches the positional True, True call. [ASSUMPTION] Astra's frozen_with_warns receipt is from this fixture's missing `voice_preset`, not from post-audit errors.
