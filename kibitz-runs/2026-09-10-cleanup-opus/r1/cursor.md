VERDICT: yes-with-fixes -- C1/C2 are real dead-store/duplicate-diagnostic cuts; C3 is an LLM-acquisition rip sold as cleanup, and C1's proof harness is not specified as a committed gate.

MUST-FIX BEFORE BUILD:
1. [§1 C3] Goal and method diverge. Goal is "stop acquiring a model the freeze cascade never calls." Method keeps `technical_model` + `_OTRMI.require_model` (nodes/OTR_LedgerFreezeCascade.py:267-268; nodes/_otr_model_inputs.py:72-85 nonempty-string only). The operator-visible tooltip still says the cascade "uses it only for bounded same-story safety cleanup" (OTR_LedgerFreezeCascade.py:142-150). That is already stale: `_run_inline_safety_cleanup` is `del generate_fn` plus a stamp (nodes/_otr_freeze_cascade.py:676-706); there is no `generate_fn(` in that module. Plan step 5 only corrects "adjacent comments" in `run()` and forbids schema change, so a literal implementer ships a lying socket. Fix: restated C3 contract in the same chunk -- freeze does not load or catalog-admit technical weights; the wire is a nonempty presence pin; rewrite that tooltip and the `run_freeze_cascade` docstring that still says "optionally clean narrow safety" (_otr_freeze_cascade.py:721-726). Do not keep `request_slot` "for safety."
2. [§1 C3 / CLAUDE.md ledger-rip] Removing `request_slot`/`make_generate_fn` (OTR_LedgerFreezeCascade.py:270-287) deletes freeze-time catalog, policy, provider, and VRAM failure. Shot-lock still acquires independently (`otr_shot_lock.py:1721`). A nonempty garbage id will now pass freeze and fail later, or never if shot-lock is skipped. The plan notes the removed paths but does not name the user-facing handoff. Fix: one C3 sentence -- freeze success no longer means the technical id is loadable; remaining admission owner is shot-lock (and any other later `request_slot("technical", ...)`).
3. [§0 / §4 P0 / §3] "Confirm prep has landed" is undefined, and C1's P1 gate is that harness. Session-start git shows `scripts/otr_canonical_audio_check.py` and `tests/test_canonical_audio_check.py` untracked, `tests/test_episode_assembler_offset_shift.py` deleted. Fix: verify working tree; if still uncommitted, land that replacement as its own P0.5 commit+push before any runtime edit. [ASSUMPTION] status may have moved; verify: git status of those three paths.

SHOULD-FIX:
1. [§1 C1] `scene_sequencer.py:9` still claims "intelligent pacing (breath buffers, BEAT/PAUSE tags)" after deleting the only remaining unread locals (`breath_ms`..`act_break_ms` at 928-932; `current_character_name` at 988/1124; no other hits in that file). Plan only deletes the two-line comment above the assignments. Include the module header in C1's comment correction, or the header becomes the leftover lie.
2. [§4 P3] Named test coupling is incomplete. `tests/test_lfc_c4_news_used_passthrough.py:59-68` uses the same successful `request_slot`/`make_generate_fn` stubs as `tests/test_lfc_b1_cascade_unload_in_finally.py` and `tests/test_cascade_freeze_unload_visible.py`. After C3 those patches become inert greens. Convert c4 to the same fail-fast sentinels in the C3 chunk. `tests/test_lfc_b14_unload_on_exit.py` is source-order only and should still pass if unload stays after `run_freeze_cascade`.
3. [§0 vs §1 C3] "Bounded cleanup sample" / "~30 production lines" (§2) does not describe C3. Split the wave label: C1/C2 are dead stores and diagnostic ownership; C3 is an acquisition rip with telemetry and failure-path change. Otherwise an implementer will treat C3 like C1.

OPTIONAL / NICE-TO-HAVE:
- `_otr_vram_levers.py:92-95` still says freeze's finally evicts the writer LLM. Still true after C3 (`unload_llm_if_local_resident` at OTR_LedgerFreezeCascade.py:376; writer skip via `OTR_WRITER_UNLOAD_AFTER_SCRIPT=0` in nodes/_otr_writer_vram.py:38-46), but no longer because freeze loaded anything. One comment tweak.
- Keep `generate_fn` as the first positional (agreed KEEP). Tests still pass lambdas (`tests/test_lfc_phase_7_8_readiness.py` and others). Deleting it is a later wave.

CUT THESE:
1. [§4 P3] "Bind the node invocation to canonical node 62 / assert unchanged link 115" as extra test work. P0 already requires byte-identical canonical SHA and no JSON edit. Link 115 is already `[115, 1, 4, 62, 4, "STRING"]` (workflows/otr_canonical.json:3050-3055); node 62 input 4 is `technical_model` (1080-1085); writer node 1 output slot 4 fans out 115 (494-500). Wiring assertions do not serve C3.
2. [§5] "Next bounded wave: trace one canonical artifact..." inside this implementation contract. Already deferred. Leaving it here invites scope bleed into C1-C3.

[ASSUMPTION] No in-repo exact-message consumer of `lines[{idx}] line_id=... is duplicated` (nodes/_otr_ledger_freeze.py:311). `tests/test_g8_line_id_uniqueness.py` filters on `"G8"`; eight identical ids already expect one G8 summary with `+2 more` (84-107). C2's "exactly one collision diagnostic" is the G8 line, not a new G8 shape.
[ASSUMPTION] External log scrapers of the per-line duplicate string: unverified, as the plan says.
UNTRACED -- did not read `docs/DEAD_CODE_HUNT_PROMPT_V5.md`, `docs/PRODUCTION_SPRINT_LESSONS.md`, or the body of `request_slot` beyond the def at nodes/_otr_model_loader.py:1891. `docs/DEAD_CODE_EXECUTION_PLAN.md:189` still lists the three-ffmpeg-resolver merge as a standing rejection; this wave proposes no ffmpeg work (Sep 4 closure is in docs/PROD_BUG_LOG.md PBUG-20260904-02).

Grounded keepers: `__init__.py:171` `OTR_SceneSequencer`; canonical node 3 is `OTR_SceneSequencer`; G8 second owner at `_check_g8_line_id_uniqueness` (893) still runs from `run_gap_audit` (728); writer duplicate check at nodes/_otr_ledger_cleanup.py:491 stays; return tuple still seven items with script_json == v2_ledger_json on the success path (OTR_LedgerFreezeCascade.py:421-428). C1/C2 have no design fork once C3's contract is named.
