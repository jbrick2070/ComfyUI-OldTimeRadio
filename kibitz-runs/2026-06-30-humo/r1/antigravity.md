VERDICT: no. The plan contains a major conflict between the clip-underrun fix and talking face lip-sync coherence, relies on dead routing code for host/announcer bookends, and omits VRAM safety caps on the base 14B class.

MUST-FIX BEFORE BUILD:
1. [CANDIDATE LEVERS - Lever 2] Lip-Sync Loop/Ping-Pong Coherence Defect: Looping or ping-ponging video frames of a character's speaking mouth while dialogue audio plays continuously breaks audio-visual synchrony. Fix: Exclude `audio_driven_face` engines (HuMo/1.7B) from the composite-level loop-fill logic, and elevate the "HuMo phrase-chunking" upstream root fix to a GOAL so dialogue beats are rendered in speech-aligned chunks rather than capped loops.
2. [OPERATOR ADDITIONS - RADIO, NOT A FACE] Dead Speaker-Role Routing: `is_never_humo_role` and `_NEVER_HUMO_ROLES` in [nodes/_otr_speaker_role.py:96](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_speaker_role.py#L96) are completely unused in the active custom nodes. Fix: Wire `is_never_humo_role` into [nodes/_otr_video_engines/render_driver.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/render_driver.py) to prevent announcer/music roles from ever dispatching a HuMo render, and remove announcer/music roles from `roles` in [nodes/_otr_video_engines/eng_humo.py:94](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/eng_humo.py#L94) to block them in dropdown selection.
3. [GOALS] Uncapped 14B Base Class VRAM OOM: Base class `HuMoEngine` (which corresponds to 14B portrait, [nodes/_otr_video_engines/eng_humo.py:80](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/eng_humo.py#L80)) has `safe_render_frames = None`, while the wide aspect counterpart `HuMo14BLandscapeEngine` has it capped at 49. Because both load the heavy 14B UNET, they share the same VRAM footprint and the portrait 14B will OOM on long beats [ASSUMPTION]. Fix: Add a VRAM-safe cap (e.g. 49) to the base `HuMoEngine.safe_render_frames`.
4. [CANDIDATE LEVERS - Lever 1] Non-Exposed Portrait Quality Knobs: Lever 1 lists audio-conditioning strength, LoRA weight, and sampler knobs to improve quality, but [nodes/_otr_video_engines/eng_humo.py:278](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/eng_humo.py#L278) hardcodes the LoRA model strength to 1.0, sampler to `uni_pc`, and does not pass or expose audio-conditioning strength to the `WanHuMoImageToVideo` node. Fix: Update `HuMoEngine._build_graph` to parameterized inputs for LoRA strength, sampler parameters, and check if the ComfyUI wrapper node actually supports audio-conditioning strength inputs.

SHOULD-FIX:
1. [CANDIDATE LEVERS - Lever 4] Dropdown Label VRAM Metadata: [nodes/otr_video_director.py:113](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/otr_video_director.py#L113) only appends aspect ratio suffixes. Auto-deriving VRAM/model tiers requires engine metadata that does not exist in the registry. Fix: Add a `vram_tier_label` or similar metadata property to the video engine registry classes, and update `_label_for` in `otr_video_director.py` to append it.
2. [CANDIDATE LEVERS - Lever 2] Loop vs Ping-Pong Inconsistency: Exact-fitting uses ping-pong mirror-extension via numpy (`fit_frames_to_target`), while composite-level loop-fill uses standard repetition via ffmpeg (`-stream_loop -1`). This results in jarring visual jumps for composite-filled clips. Fix: Align the composite-level loop-fill with mirror-extension or document why standard loop is preferred for non-dialogue segments.

OPTIONAL / NICE-TO-HAVE:
- Add visual validation tests to verify that `ltx_audio_in` correctly processes `radio_bookend.png` without falling back to a black canvas.

CUT THESE (scope / over-engineering):
1. [OPERATOR ADDITIONS - RADIO, NOT A FACE] "Animated Talking Radio" Engine: Scoping a candidate new small engine is unnecessary. The existing `ltx_audio_in` engine + `radio_bookend.png` still already handles the bookends without human faces.
2. [OPERATOR ADDITIONS - HuMo-ISOLATION SMOKE] Test Harness Tooling: Setting up a complex A/B test harness with baked episodes is useful but does not directly serve the quality, underrun, or labeling goals. Cut/defer this to a later sprint to maintain project focus.
