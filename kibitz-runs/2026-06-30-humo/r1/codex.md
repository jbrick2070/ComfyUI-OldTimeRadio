VERDICT: no. The plan mixes a 1.7B-only quality sprint, an already-partly-built clip-fill fix, and a new radio-host product decision without resolving which routing layer owns each behavior.

MUST-FIX BEFORE BUILD:
1. [GOALS / CANDIDATE LEVERS 2] The clip-underrun fix is framed as unbuilt, but current source already has default-on loop-fill in `C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\nodes\otr_silent_composite.py:243`, applies it in segment planning at `...\nodes\otr_silent_composite.py:347`, and reports `looped_fill` vs `held_last_frame` at `...\nodes\otr_silent_composite.py:401`. Concrete fix: rewrite this item as “verify why existing `OTR_CLIP_FILL` did not protect the eyeballed episode, then add any missing legibility/freeze gate,” not “build loop/ping-pong from scratch.”
2. [OPERATOR ADDITIONS / CANDIDATE LEVERS 3] “Decide animated radio vs face” contradicts the hard host preference and existing speaker-role contract. `_NEVER_HUMO_ROLES` already includes announcer/music/sfx at `...\nodes\_otr_speaker_role.py:96`, while the same file says only character dialogue gets HuMo at `...\nodes\_otr_speaker_role.py:85`. Concrete fix: delete the “face” branch; specify the radio-host route as a requirement and identify the enforcement point.
3. [OPERATOR ADDITIONS / CANDIDATE LEVERS 3] The routing story is split and currently unsafe: HuMo declares `announcer_visual`, `music_visual`, and `character_video` roles at `...\nodes\_otr_video_engines\eng_humo.py:89`, and role compatibility says announcer/music can supply `audio_ref` and `init_image` at `...\nodes\_otr_shared\role_compat.py:55`. ShotLock simply maps announcer/music to those roles and picks the policy engine at `...\nodes\otr_shot_lock.py:55` and `...\nodes\otr_shot_lock.py:779`. Concrete fix: either fail-closed if an `audio_driven_face` engine is selected for announcer/music, or explicitly scope this sprint to changing the saved workflow defaults only.
4. [GOALS / CANDIDATE LEVERS 1 / CANDIDATE LEVERS 4] “Stay 1.7B” conflicts with the canonical workflow currently selecting `humo_14B_169` for `character_video_model` in node 87 at `C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\workflows\otr_scifi_16gb_full.json` and with the registered 14B row at `...\nodes\_otr_video_engines\registry.py:274`. Concrete fix: decide whether this sprint demotes the workflow to `humo_1.7B` / `humo_1.7B_169`, or split “1.7B improvement” from “14B labeling/option clarity.”
5. [CANDIDATE LEVERS 1] Several proposed HuMo knobs are not actually exposed as knobs. Steps/cfg are env-backed at `...\nodes\_otr_video_engines\eng_humo.py:148`, but sampler is hard-coded to `uni_pc`, scheduler to `simple`, LoRA strength to `1.0`, shift to `8.0`, and no audio-conditioning strength is visible in the graph at `...\nodes\_otr_video_engines\eng_humo.py:263`. Concrete fix: classify levers as “already tunable,” “requires code change,” or “verify wrapper supports it” before build.

SHOULD-FIX:
1. [CANDIDATE LEVERS 4] The label VRAM numbers are not grounded to one authority. The plan says 1.7B is `~3.3 GB`, but registry capability rows say `7000` MB for `humo_1.7B` and `humo_1.7B_169` at `...\nodes\_otr_video_engines\registry.py:262`; the code comment says “3.3 GB + umt5/whisper” at `...\nodes\_otr_video_engines\eng_humo.py:361`. Fix: label with the true single-resident peak or explicitly call out “UNET only” vs “episode peak.”
2. [CANDIDATE LEVERS 3] “Small bespoke radio animator” is scope creep before proving the existing radio-motion path is insufficient. Current render health code expects LTX-family open beats for announcer/music at `...\nodes\_otr_video_engines\render_driver.py:1835`. Fix: first specify a still+motion/LTX route and acceptance bar; defer a new engine unless that fails.
3. [OPEN QUESTIONS] “Build order + content-only vs workflow-JSON” is still an open question, but this repo requires the canonical workflow JSON to change in the same chunk as node/widget/routing changes. Fix: make R1 output include a single owner for each change: code-only, workflow JSON, tests, or live-render verification.

OPTIONAL / NICE-TO-HAVE:
- Add an explicit “operator eyeball artifact” acceptance item for the radio host, separate from automated clip-fill/freeze metrics. [ASSUMPTION] The radio-host look is ultimately aesthetic, not fully testable.

CUT THESE (scope / over-engineering):
1. [CANDIDATE LEVERS 3] Cut “person face” as a bookend/host option. It contradicts the hard preference and existing never-HuMo role contract.
2. [CANDIDATE LEVERS 3] Cut “small bespoke radio animator” from the first build. Existing LTX/radio-still machinery is the smaller path to validate the concept.
3. [OPERATOR ADDITIONS] Cut “HuMo-isolation smoke later, not now” from the implementation batch, or promote it to the first prerequisite. Keeping it as “later” while also tuning subjective HuMo quality leaves the core quality goal unmeasurable.