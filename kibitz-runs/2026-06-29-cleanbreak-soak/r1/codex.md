VERDICT: no. The plan wants an image-gen-start bake/replay soak, but its bake surface, prune boundary, and matrix definition still describe incompatible video-only and full-render concepts.

MUST-FIX BEFORE BUILD:
1. [B1/B2] Bake-once conflicts with re-minting images. B1 says reuse the S-F bundle that bakes every referenced still, while B2 says clear/re-mint stills per image engine. The current S-F bundle copies `ledger['images']['images'][*]['path']` and `ledger['video']['shots'][*]['init_image']`, which will preserve pre-rendered image assets instead of proving the selected image engine ran (`scripts\otr_visual_smoke.py:68-105`, `scripts\otr_visual_smoke.py:111-183`; cache reuse path in `nodes\otr_image_gen_dispatcher.py:478-517`). Concrete fix: define a new combo-soak fixture, not just S-F reuse: capture the pre-image ledger, `image_policy_json`, `image_prompts_json`, `master_audio_path`, `episode_id`, and audio gate; explicitly empty `ledger["images"]`/cache/index and any pre-resolved still paths before node 91 runs.

2. [B2] The image-gen prune boundary is conceptually underspecified. `OTR_ImageGenDispatcher` cannot run from “story baked in ledger” alone; it requires `script_json`, `image_policy_json`, and `image_prompts_json` (`nodes\otr_image_gen_dispatcher.py:718-751`). The workflow wires those from node 90, node 88, and node 89 respectively, then feeds node 91 into node 92 (`workflows\otr_scifi_16gb_full.json`, nodes 88-92). Concrete fix: state the replay prompt shape: either keep only nodes 91 and 92 with those three node-91 inputs baked as literals, or keep nodes 88/89/90 live. Do not leave this as an open question.

3. [B3] “~15 combos” contradicts “image engines x video lanes.” The repo has five named image engines in the plan and in the current runner (`scripts\_otr_cov_runner.py:46-49`), but the video side is not a single small lane list: the director has 3 top-level video slots plus 3 per-role other-beats slots (`nodes\otr_video_director.py:167-217`, `nodes\otr_video_director.py:235-261`), while the current coverage runner uses a fill strategy, not a cross-product (`scripts\_otr_cov_runner.py:58-79`, `scripts\_otr_cov_runner.py:82-113`). Concrete fix: define the exact matrix. If the target is 15, say “5 image engines x 3 chosen video carriers/lanes” and list the three lanes. If it is a real image x video-engine cross-product, drop the “~15” claim.

4. [B3/B4] Acceptance lacks an image-specific proof. “Selected engines render or fail LOUD” is not enough because node 91 can report cache hits/reuse and can skip image generation when the selected video engine ignores stills (`nodes\otr_image_gen_dispatcher.py:431-441`, `nodes\otr_image_gen_dispatcher.py:478-517`, `nodes\otr_image_gen_dispatcher.py:667-672`). Concrete fix: require per-leg evidence that node 91 executed, `made > 0` when an image engine is under test, `reused == 0` unless the leg is explicitly cache-reuse, `meta.image_engines.by_role` names the selected image engine, still files exist, and node 92’s `engine_histogram`/recipe receipt names the selected video engine.

5. [B4] The replacement for the old A-ship gate is too weak. The old soak asserts real clips, OOM trail, deterministic two-pass trace, audio SHA, VRAM ceiling, and routing (`nodes\_otr_video_engines\render_driver.py:2147-2214`). The new gate only says registered combo renders/fails loud, audio byte-identical, recipe-stamped. Concrete fix: define the new gate as a structured matrix verdict with real on-disk clips, no silent substitutions, image/video engine receipt, master PCM hash equality, VRAM ceiling, prompt/node execution boundary, and deterministic replay for at least one representative leg.

SHOULD-FIX:
1. [A1/A3] Do not pre-delete `classify_failure` just because fallbacks are gone. The no-fallback path still uses it to produce named failure kinds in `RenderError` (`nodes\_otr_video_engines\render_driver.py:1526-1553`). Concrete fix: keep failure classification until the new “hard-fail LOUD with named reason” acceptance has an equivalent source.

2. [B1] “Cast baked in” needs a fixture invariant, not prose. CastLock is upstream of audio and ShotLock, but the bake plan does not say what proves the captured ledger is post-CastLock and audio-frozen. Concrete fix: bake manifest must assert cast rows, voice refs, `meta.cast_lock_revision` or equivalent, line audio paths or master-audio fallback, and stable master hash. verify: exact ledger fields present in the current `node_episode_input.json`.

3. [A2/B4] Removing `mode="soak"` is tied to workflow/widget compatibility but the plan does not state the UI result. Current `OTR_VideoRenderBatch` exposes `["soak", "single", "episode"]` with default `soak` (`nodes\otr_video_render_batch.py:89-93`), while the saved workflow uses node 92 value `"episode"` (`workflows\otr_scifi_16gb_full.json`, node 92). Concrete fix: state whether `soak` is removed from the combo list, repointed, or the mode widget is retired; then include workflow JSON and widget-vector validation in the same build.

OPTIONAL / NICE-TO-HAVE:
1. [B3] Add one “cache intentionally reused” leg after the fresh-render legs, so cache behavior is tested separately instead of accidentally masking the image-engine test.
2. [B4] Record the baked replay prompt’s executed node set, mirroring S-F’s pruned-prompt verification (`scripts\otr_visual_smoke.py:206-250`, `scripts\otr_visual_smoke.py:253-260`).

CUT THESE (scope / over-engineering):
1. [B3] Cut a literal image-engine x every-video-engine cross-product if “~15” is the operator target. It is safe because the current architecture can prove image engines with controlled still-consuming carriers, then prove video engines separately; a full cross-product expands cost without matching the stated scope.
2. [B1] Cut copying baked still assets for the new image-gen-start soak. It is safe because the master audio and story/cast are the reusable parts; copied stills directly undermine the goal of exercising image generation.
3. [A3] Cut deciding `ENGINE_FAMILY` removal at the concept level. Keep the conceptual requirement as “no fallback path remains”; let R2/R3 code grounding decide whether any non-fallback family lookup remains needed for single render, reports, or error naming.