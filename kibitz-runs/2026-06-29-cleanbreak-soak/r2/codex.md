VERDICT: no. The plan deletes live symbols without replacement and the new image_gen replay boundary cannot be built from the currently captured data.

MUST-FIX BEFORE BUILD:
1. [B1/B2] The proposed bake input does not exist. `state/node_episode_input.json` currently captures only node-92 `ledger` and `master_audio_path`, not `image_policy_json`, `image_prompts_json`, dispatcher `gate_in`, or dispatcher `episode_id` needed to replay node 91. See `nodes\otr_video_render_batch.py:220-223` and dispatcher inputs at `nodes\otr_image_gen_dispatcher.py:757-770`. Concrete fix: add a separate dispatcher-input capture, e.g. `state/node_image_gen_input.json`, containing `script_json`, `image_policy_json`, `image_prompts_json`, `gate_in`, `episode_id`; then have the bake command merge that with node-92’s `master_audio_path`.

2. [B2] The stated “full visual tail” is not the real workflow tail. The plan says `OTR_ImageGenDispatcher -> OTR_VideoRenderBatch -> OTR_SilentComposite -> OTR_MasterAudioMux`, but the saved workflow routes node 92 to node 84, then node 84 to node 86, node 86 to node 93, node 93 to node 85; node 93 also depends on node 12 and node 94. See `workflows\otr_scifi_16gb_full.json:1` links `261`, `247`, `266`, `250`, `265`, `273`. Concrete fix: either define the replay as ending at node 92 only, or include nodes 84/86/93/94/85 and bake literals for node 12’s procgen video, master audio, audio_done, and any scope/caption inputs.

3. [B4] The executed-node acceptance cannot verify node 91 with the current harness. `executed_node_set()` only reads `history_entry["outputs"].keys()` (`scripts\otr_visual_smoke.py:256-259`), while `OTR_ImageGenDispatcher` is not an output node (`nodes\otr_image_gen_dispatcher.py:714-716`). Concrete fix: verify node 91 through observable outputs (`image_done`, dispatcher report text, `stills_manifest.json`, `made/reused`) plus prompt dependency inspection, or add a test-only output/probe path.

4. [A1] Removing `FamilyInputGap` and `OomSignal` as described will leave live raise sites broken. `_assert_family_inputs_satisfiable()` still raises `FamilyInputGap` at `nodes\_otr_video_engines\render_driver.py:1427` and `1434`; `_render_one()` still raises `OomSignal` at `nodes\_otr_video_engines\render_driver.py:1447`. Concrete fix: replace `FamilyInputGap` with a non-fallback input-contract exception or `ValueError`, remove `force_oom`/`oom_*` plumbing with the soak removal, and update `classify_failure()` accordingly.

5. [A2] “abstract redundant with visualizer” is false for current roles. `abstract` serves and defaults `background_abstract` (`nodes\_otr_video_engines\cheap_families.py:167-170`); `visualizer` does not serve `background_abstract` (`nodes\_otr_video_engines\eng_visualizer.py:38-41`). Concrete fix: before unregistering `abstract`, assign an explicit replacement default for `background_abstract` such as `still_pan` or `still_flat`, or intentionally extend `visualizer` to that role and update role-compat tests.

6. [A3] The test migration list is incomplete enough to fail the build. Current tests still assert fallback chains and soak behavior outside the listed files, including `tests\test_video_character_3d.py:354-369`, `tests\test_video_gpu_smoke.py:71-73`, `tests\test_video_soak_fixture.py:22-92`, and `tests\test_video_render_driver_additive.py:486`. Concrete fix: add these to the delete/migrate list and replace remaining fallback assertions with hard-fail/no-fallback assertions.

SHOULD-FIX:
1. [A4] Deleting `allow_auto_fallback` needs an explicit call-site/test update list, not just “rebaseline.” It is in the node schema and method signature at `nodes\otr_video_director.py:216`, `278-286`, and serialized into policy at `342`; tests call it directly at `tests\test_route_a_14b_promotion.py:127` and `tests\test_still_aspect_and_labels.py:168`. Concrete fix: update `direct()` signature, every direct test call, workflow node-87 `widgets_values`, and `scripts\otr_api.py` widget-slot expectations in one change.

2. [B3] “Reuse `_otr_cov_runner.py`’s fill model” is under-specified for the new pruned replay. The current runner calls `_otr_soak_capstone.run_leg()` and full workflow profile machinery (`scripts\_otr_cov_runner.py:38-41`, `193-196`), not `otr_visual_smoke` or a baked image_gen replay. Concrete fix: add a new runner path that consumes the baked fixture and calls the new `--start-boundary image_gen` replay, while preserving the leg generation functions.

3. [B4] The acceptance field `meta.image_engines.by_role` exists only after dispatcher execution (`nodes\otr_image_gen_dispatcher.py:633-640`), but the plan does not say where the harness will read it. Concrete fix: read it from node 92’s input/output ledger or from the baked/replayed dispatcher output, not from the final mux-only history entry.

OPTIONAL / NICE-TO-HAVE:
1. [B4] Replace “master PCM hash EQUAL before/after” with an exact harness contract: file hash of baked master, decoded PCM hash of mux output, or existing run-report field. Current smoke only hashes the baked master file before/after (`scripts\otr_visual_smoke.py:359-378`), which proves the source file was not mutated, not that final mux audio is byte-identical.

CUT THESE (over-engineering):
1. [B3/B4] Cut dashboard plumbing from the first build. First make the baked image_gen replay produce a correct per-leg JSON verdict; merging into `_otr_coverage_matrix.json` and the `otr-coverage-soak` dashboard can follow once the boundary and capture data are real.