VERDICT: build-ready as-is? no. Removing fallback pathways, auto-fallback widgets, and retiring engines breaks multiple active test suites, scripts, and configuration profiles that reference these systems.

MUST-FIX BEFORE BUILD:
1. [A3 / A1] test_video_render_driver.py & test_video_survival_guide_vectors.py Breakage: Removing `make_fallback_of`, `SYNTH_FALLBACKS`, and the `fallback_engine` attributes will break [tests/test_video_render_driver.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/tests/test_video_render_driver.py#L23-L45) (which calls and asserts `make_fallback_of` and the fallback chain of "ltx_video"/"wan_i2v") and [tests/test_video_survival_guide_vectors.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/tests/test_video_survival_guide_vectors.py#L72-L82) (which resolves the fallback chain of "humo").
   Concrete Fix: Delete `test_fallback_chain_character3d_converges_to_floor`, `test_fallback_dangling_engine_gets_universal_floor`, and `test_fallback_floor_is_terminal` in `test_video_render_driver.py`, and delete `test_fallback_chains_resolve_to_a_registered_radio_floor` in `test_video_survival_guide_vectors.py` in the same commit.

2. [A3 / A1] GPU Smoke Test & test_video_gpu_smoke.py Breakage: Removing fallback logic and retiring `still_motion` will break [scripts/otr_video_gpu_smoke.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/scripts/otr_video_gpu_smoke.py#L96-L110) (`demonstrate_humo_fallback` which asserts humo converges to `still_motion` with decisions) and [tests/test_video_gpu_smoke.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/tests/test_video_gpu_smoke.py#L68-L78) (`test_humo_fallback_chain_and_demo`).
   Concrete Fix: Delete `demonstrate_humo_fallback()` from `scripts/otr_video_gpu_smoke.py` and remove its corresponding test `test_humo_fallback_chain_and_demo` from `tests/test_video_gpu_smoke.py`.

3. [A4] allow_auto_fallback Test and Smoke Script Breakage: Deleting `allow_auto_fallback` from `OTR_VideoDirector` and `schemas.py` will break [scripts/run_otr_30word_smoke.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/scripts/run_otr_30word_smoke.py#L193-L197) (which validates and asserts the field is present on `OTR_VideoDirector`), [tests/test_video_platform_aseam.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/tests/test_video_platform_aseam.py#L248,L305,L329), and other tests that explicitly check or pass `allow_auto_fallback`.
   Concrete Fix: Remove the `allow_auto_fallback` checks/assertions from `scripts/run_otr_30word_smoke.py` and clean up all `allow_auto_fallback` keywords/references in `tests/test_route_a_14b_promotion.py`, `tests/test_still_aspect_and_labels.py`, and `tests/test_video_platform_aseam.py`.

4. [A2] Capability Profiles references to Retired Engines: Retiring `still_motion` and `station_card` will cause the profiles [config/profiles/8gb_lite.json](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/config/profiles/8gb_lite.json#L12,L14,L23) and [config/profiles/cpu_floor.json](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/config/profiles/cpu_floor.json#L12,L14,L20) to fail validation in `cross_validate_profile` because they still reference retired/unregistered engine IDs for overrides.
   Concrete Fix: Update `config/profiles/8gb_lite.json` and `config/profiles/cpu_floor.json` to replace all overrides referencing `still_motion` and `station_card` with valid selectable alternatives (e.g., `still_pan` or `still_flat`).

5. [B1] Missing Capture of Image Policy and Prompts JSON: B1/B2 require capturing `image_policy_json` and `image_prompts_json` from a reference run, but these are produced by upstream nodes (88 and 89) and are not written to `state/node_episode_input.json` by `OTR_VideoRenderBatch` (which has no access to them).
   Concrete Fix: Modify `OTR_ImageGenDispatcher.dispatch` in [nodes/otr_image_gen_dispatcher.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/otr_image_gen_dispatcher.py) to write a capture file `state/node_image_input.json` containing `script_json`, `image_policy_json`, and `image_prompts_json` during the reference run, and update the bake script to read from it.

6. [B2] ComfyUI Replay Graph Rewiring and Validation Gaps: Under `--start-boundary image_gen`, the pruned prompt executes nodes 91, 92, 84, and 85, but node 84 (`OTR_SilentComposite`) has an input link `base_video_path` pointing to missing node 12, and node 85 (`OTR_MasterAudioMux`) has input links `silent_video_path` to missing node 93 and `audio_done` to missing node 7. Also `verify_executed` in `otr_visual_smoke.py` will reject this node set.
   Concrete Fix: Extend `build_pruned_prompt` and `verify_executed` in [scripts/otr_visual_smoke.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/scripts/otr_visual_smoke.py) to: (1) bake `base_video_path` and `audio_done` as literal values in the prompt, (2) rewire node 85's `silent_video_path` to point directly to node 84's output 0, and (3) update the verification set to expect exactly `{91, 92, 84, 85}` (and optionally 63).

7. [A2] Missing Cheap Default for scene_broll and background_abstract Roles: Deleting `still_motion` (which is default for `scene_broll`) and `abstract` (which is default for `background_abstract`) from `cheap_families.py` will leave those roles without any default cheap floor engine.
   Concrete Fix: Update `StillPanFamily` in [nodes/_otr_video_engines/cheap_families.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/cheap_families.py) to declare `default_roles = ("scene_broll", "background_abstract")`.

SHOULD-FIX:
1. [B3] Non-Existent _otr_cov_runner.py Reference: The plan directs to "Reuse `scripts/_otr_cov_runner.py`'s fill model", but [scripts/_otr_cov_runner.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/scripts/_otr_cov_runner.py) does not exist in the active repository [ASSUMPTION].
   Concrete Fix: Clarify that the matrix runner must be implemented from scratch in a new script or by extending `otr_coverage_sweep.py`, implementing the described additive fill logic.

2. [A1] Dead Fallback Logic in retry_taxonomy.py & run_episode: With fallbacks gone, [nodes/_otr_shared/retry_taxonomy.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_shared/retry_taxonomy.py) still holds `escalate_to_fallback=True`, `build_fallback_decision`, and `append_runtime_fallback_decision`. Also, `run_episode` retains dead group pruning logic when a fallback occurs.
   Concrete Fix: Clean up the dead fallback code in `retry_taxonomy.py` and remove the dead `prune_orphaned_groups` execution path in `render_driver.py`.

OPTIONAL / NICE-TO-HAVE:
- Clean up or delete [nodes/_otr_shared/fallback.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_shared/fallback.py) entirely if it is no longer referenced anywhere.

CUT THESE (over-engineering):
- None.
