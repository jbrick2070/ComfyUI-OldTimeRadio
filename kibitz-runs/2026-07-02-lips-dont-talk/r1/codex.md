VERDICT: no. The plan is useful, but it is not build-ready because it treats multiple role/routing failures as one controlled A/B and contains at least one prompt-delta premise contradicted by the production log.

MUST-FIX BEFORE BUILD:
1. [KNOWN DELTAS] The “controlled A/B” is not one A/B; it mixes radio-face bookends and character-face beats. Production b000/b001/b005 use the radio-face A/B path and motion-role prompts, while b002/b003/b004 use M4 character prompts. See `nodes/_otr_video_engines/render_driver.py:1070`, `:1110`, `:1431`, `:1456`, and `docs/2026-07-02-canonical-ia2v/proof_server.log:3552`, `:3612`, `:3672`, `:3728`, `:3784`, `:3838`. Fix: split the probe plan into two tracks: radio-face bookends and character-face beats, each with its own fixed still/audio/prompt baseline and ranked deltas.
2. [KNOWN DELTAS #3] The claim that production prompts “do NOT say the subject IS TALKING” is false for character beats: proof b002/b003/b004 prompts start with “face visible, speaking to camera.” See `docs/2026-07-02-canonical-ia2v/proof_server.log:3672`, `:3728`, `:3784`. Fix: rewrite this delta as role-specific: bookend motion prompts lack mouth/speech tokens; character prompts contain speech intent but are long identity/scene prompts.
3. [KNOWN DELTAS #1/#4] Resolution and SolidMask dimensions are coupled in the current engine, so the plan cannot rank them independently as written. `_build_graph_ia2v` derives base pass from `width // 2` and sets `SolidMask` to the same render canvas, while the canonical flat prompt uses base 640x360 and `SolidMask` 1280x720. See `nodes/_otr_video_engines/eng_ltx_av.py:763`, `:805`, `docs/2026-07-02-canonical-ia2v/ia2v_flat_api_prompt.json:292`, `:333`. Fix: define separate graph-level probes for target canvas/base latent size and mask dimensions, or explicitly state they are tested as a combined “render target scale” first.
4. [What the kibitz must deliver] The success criterion is underspecified. Existing eval artifacts show canonical eyeball-pass still has weak `r_mouth_vs_onset`, so correlation alone is not a valid gate; proof has lower mouth motion but the document relies on operator sound-on judgment. See `docs/2026-07-02-canonical-ia2v/canonical_eval.json` and `docs/2026-07-02-canonical-ia2v/proof2_eval.json`. Fix: define acceptance as blinded sound-on eyeball plus a relative mouth-motion threshold versus the canonical reference, not just “looks like talking.”

SHOULD-FIX:
1. [KNOWN DELTAS #2/#8] Clip length, fps, frame count, and audio window are bundled together. Canonical uses 24 fps, 121 frames, and `TrimAudioDuration` 5s; production logs show 25 fps-derived frames up to 305. See `docs/2026-07-02-canonical-ia2v/ia2v_flat_api_prompt.json:175`, `:330`, `:342`, and `docs/2026-07-02-canonical-ia2v/proof_server.log:3561`, `:3619`, `:3845`. Fix: probe fps alone, frames alone, and audio duration/trim alone.
2. [KNOWN DELTAS #1] The proposed likely fix implicitly assumes 640x360 base motion at production lengths fits the 16GB budget. Current production evidence only proves 512x288 render canvas with partial unet loading. See `nodes/_otr_video_engines/render_driver.py:1338`, `:1347`, and `docs/2026-07-02-canonical-ia2v/proof_server.log:3550`, `:3579`. Fix: add a VRAM-gated probe ladder before proposing a default production canvas.
3. [FAILING production path] “Topology-locked, therefore likely parametric” is too strong. Tests pin engine graph topology, not upstream routing choices such as still selection, radio-face override, prompt class, or audio slice selection. See `tests/test_ltx_av_ia2v_canonical.py:59`, `nodes/_otr_video_engines/render_driver.py:1039`, `:1079`, `:1194`. Fix: state topology is locked only inside `_build_graph_ia2v`; upstream production routing remains in scope.

OPTIONAL / NICE-TO-HAVE:
1. [WORKING reference] Keep the existing isolation harness as the probe vehicle; it already has env knobs for prompt, seconds, target size, fps, frame override, and output prefix. See `scripts/_otr_canonical_ia2v_smoke.py:56`, `:64`, `:67`, `:69`, `:70`.

CUT THESE (scope / over-engineering):
1. [KNOWN DELTAS #6] Negative prompt should not be in the first probe wave. It is cheap to test later, but the current evidence points to scale/routing/prompt-role deltas first; cutting it initially keeps the A/B matrix small. [ASSUMPTION]
2. [KNOWN DELTAS #8] “8n+1 vs audio window” should be folded into the frame-count/audio-duration probe, not tracked as a separate root-cause lane. The engine always snaps with `_AVD.next_8n1`; see `nodes/_otr_video_engines/eng_ltx_av.py:930`.
3. [KNOWN DELTAS #7] Still content should not be a late miscellaneous delta. Either promote it into the split radio-face/character-face A/B tracks or cut it from the first pass; as written it duplicates the uncontrolled-routing problem.