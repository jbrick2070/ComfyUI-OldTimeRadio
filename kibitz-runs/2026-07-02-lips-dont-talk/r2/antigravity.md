VERDICT: build-ready as-is? yes-with-fixes.
S4 contains a critical aspect-ratio mismatch defect and depends on a pending probe, but the plan is otherwise sound and mostly implemented.

MUST-FIX BEFORE BUILD:
1. [S4] **Aspect-Ratio Mismatch and Head-Cropping Defect**: S4 proposes preferring the character's minted portrait over the scene still as `init_image` for the wide `ltx_audio_in` engine. However, character portraits are minted in vertical `832x1216` aspect ratio by default (defined as `PORTRAIT_W`/`PORTRAIT_H` in [otr_meta_brief_image_prompt.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/otr_meta_brief_image_prompt.py#L307-L308)) when generated under a portrait engine like `humo_1.7B`. If a vertical portrait is routed to the wide `ltx_audio_in` engine (which renders at `512x288` default canvas size as seen in [render_driver.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/render_driver.py#L1387)), the guide-image scaling chain in [eng_ltx_av.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/eng_ltx_av.py#L803-L807) will center-crop the vertical portrait, cropping off ~30.7% from both the top and bottom of the image. This will lop off the character's head and face entirely, violating the strict system invariant in [render_driver.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/render_driver.py#L1086) that "the portrait can NEVER leak into a wide frame."
   - **Concrete Fix**: In the `render_driver.py` implementation of S4, check the dimensions or aspect ratio of the portrait. Only select the portrait if it is wide (`w > h` or `aspect == "wide"`). If the portrait is vertical, raise a `RenderError` (similar to the radio-face check at [render_driver.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/render_driver.py#L1137)) or fall back to the wide scene still `_still` (if present) to prevent head-cropping.
2. [S4] **Ambiguous Control Flow / Pending Dependency**: S4's routing is conditional: "ONLY if P5 shows wide stills kill articulation". Since P5 is listed as `(pending)` in the root cause table, it is ambiguous whether S4 should be enabled by default, gated behind an environment variable, or omitted.
   - **Concrete Fix**: Resolve the pending status of P5. [ASSUMPTION] If P5 shows that wide stills do not harm articulation, omit S4's routing changes. If they do, implement the routing gated behind a configuration toggle or active by default with the aspect-ratio safety check in MUST-FIX #1.

SHOULD-FIX:
1. [S3] **Dead/Unreachable Budget Limit Check**: In the character talking prompt construction in [render_driver.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/render_driver.py#L1438), the code checks `if len(_talk) > _LTX_MOTION_PROMPT_MAX:` and trims the fragment. However, `_frag` is already sliced to `text_prompt[:120]`, and `_IA2V_TALKING_CLAUSE_CHARACTER` has a fixed length of 114. Thus, `len(_talk)` is at most `120 + 2 + 114 = 236` characters, which is always less than `_LTX_MOTION_PROMPT_MAX` (240). This block is dead code and never executes.
   - **Concrete Fix**: Simplify the code in [render_driver.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/render_driver.py#L1431-L1447) by removing the redundant `if len(_talk) > _LTX_MOTION_PROMPT_MAX:` block.

OPTIONAL / NICE-TO-HAVE:
1. [S3] **Grammatical Discrepancy**: The spec specifies the template `"The <=120-char appearance fragment> is talking to the camera..."`, but the code in [render_driver.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/render_driver.py#L1436) implements `"<appearance fragment>. Talking to the camera..."`. While the code's version is cleaner because it avoids prepend article conflicts (e.g. "The a young astronomer"), this stylistic departure should be noted.

CUT THESE (over-engineering):
1. [S3] **Redundant Nested Character Slicing**: The secondary slicing `_frag[:max(_keep, 20)]` inside the unreachable branch is safe to remove entirely as it represents over-engineering for a condition that is mathematically impossible to reach.
