# A1 finished-diff QA

Read git diff vs HEAD703a355 on real Windows repo. Review only, no code edits,
model/GPU calls or tests. The full R1-R4 design arc is complete in
2026-09-10-my-story-next/campaign_receipt.json. This is the ONE finished-diff CLI
review required before push, not a new design arc. Root Codex remains judge.

Latest operator: selected act and speaking-character counts bind each run;
variable controls, no word/duration gates. MyStory reuses shared repairs.
A1 changed nodes/_otr_my_story.py, _otr_writer_tail.py, my_story prompt pack,
runner/tail tests and guides. Full selected-count repair preserves complete
failed parsed treatment/act at same existing factory seam; other typed errors
shared. Exact selected counts, announcer excluded. Interpretation notes no gate;
optional metadata/frame/title/gender; speakable last-act coverage; normalized
slot numbers before seal; actual cues+unused receipts; append credit before seal;
shared ProviderCapacityMessages+None. All attempts saved on success/failure.
Shared tail synchronizes cleanup title to canon/file/news and title-source stamp.
No canonical shape change, no new widget. A2 nativeHF and A3 other-bank counts
are PENDING; do not pretend A1 fixes them or recommend them inside this diff.

Focused184passed. Full suite running; baseline preexisting failures have actual
c7cbf45b/S4 comparison receipts, never quarantine new failures. Review current
implementation for real defects, especially counts/repair/context ownership,
finally-save behavior, optional frame+seals, title parity. Root will ground claims.
Return concise concrete findings with source lines and severity; if clean say so.
