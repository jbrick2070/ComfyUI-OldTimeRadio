# R2 grounded driver anchor

Written before R2 fanout. R1 completed Gemini3.8FlashHigh and CursorGrok4.6High.
Root grounds their useful owner findings and rejects deterministic natural-language
entailment, invented cap values, inferred Mac causality and unrequested scope cuts.

Actual owners checked: _otr_my_story P0/P1 source versus P2/P3 omission;
_full_artifact_repair preserves source-conflicting details unless corrected;
structured_call post-validator uses same typed repair (no new retry owner);
_otr_ledger_clean whitespace normalization, original marked neighbor on candidate
reread, progressive fallback, and summaries truncated in spoken_text_policy;
MetaBrief scene builders and separate image_prompts_json; dispatcher fresh/cache
reconstruction and later prompt normalization; model_loader MPS teardown already
flushes and still holds local/caller model aliases. Mac log absent from report.

Primary R2 risks: exact complaint localization versus model-generated offsets;
whole-row authorization false positives; verifier failure policy and hidden call
multiplication; cleanup transaction rollback granularity; visual fallback can still
contradict raw source. Require narrow executable fixes, preserve uncertain receipts
and prohibit claim of semantic PASS merely from copied strings or valid JSON.

Deterministic S2 boundary edit may proceed independently under the no-design-change
rule, with read-only independent precheck and finished-diff CLI review before push.
Architecture changes await R4. No live model/media runs while fixing.
