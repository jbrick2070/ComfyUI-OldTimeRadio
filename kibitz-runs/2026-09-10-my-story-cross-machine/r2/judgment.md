# R2 judgment

Both CLI readers completed: Gemini 3.8 Flash (High) and Cursor Grok 4.6 High.
Their reviews are of input.md, not the subsequent operator_followup.md. The
user's adaptive-first-pass request is newly submitted in R3, never attributed
to these completed reviews. Production base now59a40131: deterministic boundary
fix qualified and pushed separately; no architecture changes implemented yet.

Accepted and grounded:
- Inner generate_fn/constrained_generate_fn owns each fresh LMFE enforcer.
  The prior wording named outer factories ambiguously. Drop by_schema; installed
  builder overwrites config, so set effective parser.config.max_json_array_length
  to0 after builder. Explicit schema limits and tokenizer alphabet remain.
- None must disable only open-string tracking, not fail the entire guard open.
  Snapshot flags before tokenizer normalization. General list-subtype contract
  inheritance must also wrap string repair prompts, without bank-name branches.
- Cursor caught a real receipt consumer: My Story finally parses raw_output for
  proposed counts. A transport exception's raw_completion gets a separate field,
  never raw_output. Preserve transport artifact as evidence, not accepted data.
- Thread a review context explicitly into all pass factories. P0/P3 need full
  candidate repair; all post-validators conserve material except the named issue.
- Partial cleanup needs explicit anchored replacement schema; current text-only
  schema and whitespace normalization cannot enforce precise scope. Whole-row
  authorization is separate; best-candidate fallback cannot admit scope violations.
- CleanTransaction.restore alone erases auditability. Add public abort sharing
  guarded degradation handling, and run review before reconcile/reseal.
- Final visual normalization is later than MetaBrief acceptance; preserve both
  base/final identities and truthful scope through fresh/cache-hit branches.

Rejected/qualified:
- Do not reroute the operator's technical slot to creative merely to save swaps.
  Same configured model reuses residency; different selected models incur real
  accounted transitions. Make costs visible. No new predictive memory gate.
- No arbitrary maxItems on unrelated schemas, no whole-roster companion rule,
  no claim every speaker in one act must be visible at every instant.
- Candidate path is a list of string/int components, not a JSONPath language.
  Standard dict/list traversal needs no dependency. Reject the claim that quote
  presence alone supplies complete hallucination protection. No substring fallback
  to an unrelated field when a supplied path fails.
- .strip() still changes protected outer whitespace; exact accepted text stays
  exact. Python may splice model-authored replacements without writing dialogue.
- The54-failure baseline is exact JUnit my_story_music_final_full.xml, not an
  arbitrary older handoff count. Current result14372collected/14134pass/54fail/
  183skip/1xfail, no new or worsened failures. Boundary code is already done.
- O1 identifiers are scoped to this campaign; use descriptive names in code.
  Diagnostic repairs remain requested, following blockers rather than dropped.

New grounded finding: last_error retains unwound generation frames during
structured-call retries. For normal post-generation capacity errors these hold
token tensors, not established full KV. It is not proof of the Mac cause. Keep
this observation distinct from the required measured Mac diagnostic; do not grow
this chunk into speculative model retirement or new memory gates.

R3 asks especially about the newly authorized adaptive reading and its A2
prerequisite, and concrete integration/receipt boundaries. Remaining design
ambiguities must be resolved before implementing those architecture chunks.
