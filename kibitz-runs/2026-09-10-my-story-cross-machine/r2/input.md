# Cross-machine My Story repair: R2 implementation contract

Base2499b411; GO_FORWARD is the only queue. Read R1 judgment for rejected
suggestions. Root is sole production editor. Review actual real Windows files.
Do not run models, edit production, or infer missing evidence. R2 asks for
concrete build blockers, especially ownership, call budgets and scope.

## Non-negotiable outcome

Use the person's manual fields, selected acts and flexible cast; no RSS.
No word/duration/story-size/taste gates, silent shortening or substitute story.
Real resource/cancellation errors remain real. Only full canonical execution
through an existing otr/obs asset is episode qualification. One common repaired
commit, each machine's proven installed image/video/TTS route, vulnerability
recovery first, then varied acts/casts. No baseline media while fixing.

## Chunk1: grammar state, P1 JSON, music boundary truth

1. `_otr_constrained_generate.get_cached_transformers_schema_constraint`
   retains only tokenizer identity and tokenizer_data on resident entry. Fresh
   JsonSchemaParser and prefix function per actual generate invocation, including
   repeated use of one closure, retries and failures. Move acquisition inside
   both `_make_generate_fn` in writer and `make_constrained_generate_fn` closure.
   Drop legacy by_schema references if encountered. Do not capture enforcers or
   dynamically created schema classes in resident dictionaries.
2. Shared helper creates parser with implicit array cap disabled
   (CharacterLevelParserConfig.max_json_array_length=0). Real prefix builder
   replaces parser.config; reapply this one setting to the effective config
   after construction while preserving tokenizer alphabet and all other settings.
   Explicit schema maxItems/maxLength still apply. No global environment writes.
3. `ProviderCapacityMessages` gets an explicit unbounded JSON-field marker;
   the shared message-contract copying path must retain it through all retries.
   `_otr_decode_guard.make_degeneracy_criterion` accepts None as disabled
   open-string threshold. Both schema-bound local transports read the marker
   before normalization; otherwise default remains2048. Genuine verbatim-cycle
   detection stays enabled. No bank-name branch. Preserve actual halt reason and
   open-string count in error telemetry rather than labeling every halt cycling.
4. `_pass_treatment` calls existing creative_fn._otr_bind_schema(StoryTreatment)
   once when available. Preserve P2/P3 original callable, scheduler reacquisition,
   temperatures, structural/typed ladder and complete JSON acceptance. No generic
   recovery of partial output. Store exception.raw_completion in attempt receipt
   when transport raised; this is evidence only, never accepted/salvaged text.
5. One `_interstitial_count(acts, enabled)` used in P0/P1, frame and assembly.
   P0/P1 state exact numeric boundaries, including0. No keyword post-validator.
6. Tests: real LMFE prefix builder with21+entries, explicit maxItems control,
   tokenizer alphabet retained, scan-once/per-call histories collectible and
   dynamic schema lifecycle. Nonrepeating string >2048 tokens allowed with marker,
   default-bound control, genuine-cycle control. Binder accounting and every
   repair rung. One/two/three/six acts, breaks on/off, actual ledger cue counts.

## Chunk2: preserve valid speech during shared cleanup

Existing owner `_otr_ledger_clean._repair_row`, existing transaction reseals
authorized edits. Initial complaint scope stays immutable across attempts.

- Locate exact original complaint spans; keep original offsets plus source of
  authority. Use actual pattern match spans from `_otr_spoken_text_policy`, not
  its60-character display summaries. Judge quotes require unambiguous occurrence
  or an explicit model localization to original offsets. Ground returned quotes
  against source characters. Do not grant every repeated occurrence automatically.
- Candidate must preserve every original segment outside the union of approved
  spans, byte-for-byte in order, with no insertions outside approved replacements.
  Implement deterministic reconstruction from anchored replacements or equivalent
  exact conservation, not token-overlap scoring. No Python-written dialogue.
- Whole-line complaint: exact conservation alone is vacuous. Reuse the existing
  structured_call for a narrow authorization: spoken_dialogue => no_edit;
  genuine nonspoken_direction => whole_line_convert; uncertain => retain original
  with receipt. It is not a generic quality grade. A valid short coda survives;
  genuine full-line direction can still become speech through the existing repair.
- Preserve exact accepted repair string; `_call_repair` currently collapses all
  whitespace. Remove normalization where it would rewrite protected characters.
- Reread candidate using candidate in the marked neighbor row. Keep original
  approved scope; label remaining complaints as diagnostics of previous candidate.
  Reject out-of-scope candidates before cleanliness/progress scoring, including
  best-candidate fallback. Retain original on unresolved scope; render continues.
- Tests include partial direction, multiple/repeated/overlapping spans, whole-row
  valid coda, full direction conversion, out-of-scope clean candidate, progressive
  candidate, failed judge, ambiguity, and transaction hashes/metrics/rollback.

## Chunk3: source-preserving authoring and visual scene construction

Do not compile a fabricated fact inventory. Raw bundle.fields for idea,
characters,plot,setting remains authority, projected untruncated into P0-P3 and
their repairs. Interpretation and treatment are advisory derived artifacts.

At each My Story authoring post-validator, run structural validation first.
Then a narrowly instructed technical model review gets raw source plus COMPLETE
typed candidate and its scope (interpretation/treatment/actN/frame). Fixed schema:
verdict no_direct_contradiction|contradiction|uncertain; conflicts containing
source_field,source_quote,candidate_path,candidate_quote,reason; uncertainty.
Mechanically ground exact source quote and candidate path/quote. Invalid evidence
is review-schema failure, not proof of a bad story. Compatible elaboration,
unmentioned facts in this act, and character speculation are not contradictions.
Do not infer death/absence/relationships from nostalgia or a name.

A grounded contradiction returns the existing PostValidationError message so
the SAME authoring ladder repairs that complete draft. `_full_artifact_repair`
must permit correction of the identified source-conflicting detail, conserving
other material. P0/P3 also use full-candidate repair. Recheck repaired candidates.
No second authoring retry loop. Reviewer schema uses existing structured_call
with at most2 attempts, recorded separately; author retains existing max3 calls.
No review recursion. Genuine provider/capacity/cancel exceptions retain types.

Uncertain is not an invitation to manufacture confidence: keep candidate and
record unresolved review. Exhausted malformed verifier output also records
review_unavailable rather than inventing a source contradiction. No source PASS.
Grounded unresolved source contradiction after bounded author repair fails at
authoring before expensive media. This is a correctness defect, not prose taste.

Durable `meta.my_story.source_reviews` records source digest/exact checked source
hash, pass and scope, candidate hash/projection, verdict/evidence, attempt identity,
configured reviewer selection versus actual resolved model when known, and
accepted/rejected/unresolved status. Do not claim a hash proves semantics.
Save via runner checkpoints/finally, not only swallowed completion callbacks.
An accepted act receipt covers that act, not later cleanup or final visuals.

Shared cleanup gets raw source in judge/repair context for My Story. Its exact
scope conservation plus source check of proposed changed rows runs before commit;
on contradiction retry within existing cleaner budget, retain original on
exhaustion. Original source-informed authoring receipt remains auditable. After
both cleaners, check final spoken projection with the same scoped reviewer and
record its actual hash; if newly contradictory, rollback authorized cleanup via
existing transaction instead of a post-render failure. Do not rewrite whole story
in Python or use a late mux veto. Review this transaction integration carefully.

MetaBrief `_build_char_scene_request` gets original source, actual scene context
and available cast appearances. Explicitly allow/require companions when the
source/current scene places them together; preserve target face without mandating
solitude. Full source context also applies to deterministic scene fallback.
Headshots and announcer/music radio assets have distinct neutral scope; never
force all cast into every asset. Use existing max_reseed loop for source-conflict
feedback, not a new image-prompt retry engine; unresolved fallback remains flagged.
Keep speech and scene context scoped so unrelated acts do not impose co-presence.

The MetaBrief image_prompts_json is separate from ledger meta. Carry current
source/context receipt through BOTH dispatcher fresh/cache-hit branches and
stamp_durable. Final normalize_prompt_for_render adds style: record exact
base-prompt hash, actual final-render-prompt hash and known transformation rather
than claiming the base check reviewed unseen final bytes. Cached outputs require
current source/context identity; stale hashes cannot be presented as accepted.
Publication eligibility stays current rights/identity; source qualification is
separate scoped evidence, never an empty cast-discrepancy list or late mux veto.

Tests: direct living/dead conflict and correction through existing ladder;
shared table replaced by separation; compatible elaboration; fabricated evidence;
uncertainty not PASS/rejection; source beyond400chars retained; true capacity
failure; final cleanup receipt/rollback; relevant ensemble scene and neutral
portrait; fresh/cache prompt receipt identity and final normalized prompt scope.

## Mac/resource and remaining reported diagnostics

Current My Story cleanup does NOT use LMFE. The19,163MB OS kill is verified;
the report contains no raw final log or allocation trace. Do not claim M1 fixes
it. Current teardown already calls MPS empty_cache and synchronize. Preserve
epoch/orphan contracts; do not substitute generic model.to(meta) or delete caches.
Instrument actual generation return and retirement phases with process footprint,
MPS live/driver allocations, timestamps and phase IDs using existing telemetry
owners where possible. Bounded observation only, no per-token synchronization.
Measure Mac rerun to identify actual allocation before claiming lifecycle repair.
Correct stale comments that claim del entry releases surviving model aliases.

O1 explicit call phase for build_request_from_shot: cast-preflight still-deferred
info versus actual render missing-still warning; post-image hard checks unchanged.
O2 health compares planned radio-open engine with actual engine; intentional
still_pan succeeds, requested LTX falling back/missing fails, legacy absent intent
is reported unknown rather than fabricated. O3 successful same-file rename is
info in mux/image dispatcher/render manifest; mismatch remains loud. User asked
to fix all reported issues, so these follow blockers rather than being deleted.

## Verification and delivery

No node/socket/widget schema changes expected; canonical remains untouched unless
an actual surface changes. JSON roundtrip, link/widget audit and real validator
required. Each finished chunk: focused tests, full Windows suite compared to
recorded54-failure baseline, clean-OTR Bug Bible comparison, one finished-diff CLI
reader, commit+push v2.0-alpha, HEAD==origin. Do not call baseline globally green.
Existing A2capacity/Originalcredit/credit-title items remain in GO_FORWARD after
this closure. Raw4060/Mac logs still needed for unverified attribution/C2 details.
