VERDICT: yes-with-fixes -- Chunk1 LMFE/TokenEnforcer behavior is real and codable; Chunk2/3 omit types, owners, and control flow that will not compile as written.

MUST-FIX BEFORE BUILD:
1. [Chunk1.2] TokenEnforcer always replaces parser.config. `lmformatenforcer.tokenenforcer.TokenEnforcer.__init__` (venv `lmformatenforcer/tokenenforcer.py` lines 65-66) does `CharacterLevelParserConfig(alphabet=tokenizer_data.tokenizer_alphabet); parser.config = config`. That wipes `max_json_array_length` and every other field. Do not set the cap on the pre-build parser. After `build_transformers_prefix_allowed_tokens_fn`, mutate the live object: `parser.config.max_json_array_length = 0` (0 is correct here: `jsonschemaparser.py` ListParsingState only applies the implicit cap when `default_max > 0`; DEFAULT is 20 in `consts.py`). Do not assign a new `CharacterLevelParserConfig(...)` (that would drop `tokenizer_alphabet`). Do not write `LMFE_MAX_JSON_ARRAY_LENGTH`.
2. [Chunk1.1] `by_schema` identity is load-bearing today. `nodes/_otr_constrained_generate.py` `get_cached_transformers_schema_constraint` caches `(parser, prefix_fn)` in `cache_entry["_otr_lmfe_constraint_cache"]["by_schema"]`; `tests/test_constrained_generate.py::test_constraint_cache_reuses_tokenizer_scan_and_schema_prefix` asserts `parser_1_again is parser_1` and `len(internal["by_schema"]) == 2`. The mutable object is `TokenEnforcer.prefix_states` on the prefix closure, not JsonSchemaParser. Drop `by_schema`; keep only `tokenizer` identity + `tokenizer_data`. Rebuild `JsonSchemaParser` + `build_transformers_prefix_allowed_tokens_fn` on every `model.generate` entry, including retries of one `make_constrained_generate_fn` closure. Writer: `OTR_LedgerScriptWriter.py` `_build_truncating_generate_fn` lines 842-853 currently captures the cached pair at wrapper build; move that rebuild inside the inner `generate_fn` (the outer `_make_generate_fn` already rebuilds the wrapper per call, but a captured `prefix_fn` is still reused if `by_schema` lives). Rewrite the identity test to: tokenizer_data scanned once; parser/prefix new per call; 21+ array items legal when schema has no maxItems.
3. [Chunk1.3] `make_degeneracy_criterion(..., max_open_string_tokens=None)` cannot be a bare None compare. `nodes/_otr_decode_guard.py` lines 327-331 do `open_tokens >= max_open_string_tokens`; `int >= None` raises, and `__call__`'s `except Exception: return False` (lines 353-357) fails the whole guard open, including verbatim-cycle. Add an explicit `if max_open_string_tokens is None: skip open-string` branch; keep cycle detection. Type it `int | None`.
4. [Chunk1.3] Marker must be read before message rewrite, and must not be a bank `if`. Writer `_build_truncating_generate_fn` (`OTR_LedgerScriptWriter.py` ~872-875) runs `_OTRLB.normalize_messages_for_tokenizer`, which returns a plain list. Constrained path uses `_normalize_messages_for_cache_entry` (`_otr_constrained_generate.py` 255). Snapshot `getattr(messages, "<marker>", False)` first. `_prompt_with_schema_contract` (`_otr_structured_call.py` 593) can still return a bare list (`[{"role":"system",...}, *messages]`); keep `_copy_message_prompt` / `_inherit_generation_contract` on the retry path (`1090`, `1208`) so ProviderCapacityMessages class attrs survive. Put the flag on `ProviderCapacityMessages` (`_otr_generation_budget.py` 13-26), not `my_story`. Default remains `MAX_OPEN_STRING_TOKENS = 2048` when the flag is absent.
5. [Chunk1.3] Halt telemetry currently lies. Writer `OTR_LedgerScriptWriter.py` 1105-1135 logs "CYCLING" and passes `open_string_tokens=None` even when `_degeneracy_guard.reason == "open_string"`. `_otr_constrained_generate.py` 340-351 always raises "repeated a run of tokens verbatim". `_otr_model_loader.py` 2427 and 2612 same. Use `_guard.reason` in the message; set `open_string_tokens` from `telemetry()`.
6. [Chunk1.4] Bind P1 on a local, not on `creative_fn`. `_otr_bind_schema` is only attached when `_otr_local_schema_binding` (`OTR_LedgerScriptWriter.py` 737-742); SciFi never calls it (verify: no `_otr_bind_schema` in `_otr_scifi_news_pro.py`). In the runner (`_otr_my_story.py` ~980), `treatment_fn = getattr(creative_fn, "_otr_bind_schema", None); slot = treatment_fn(StoryTreatment) if treatment_fn else creative_fn`. Pass `slot` only to `_pass_treatment`. Leave P2/P3 on the original `creative_fn` so `_account_and_get_entry` still runs per call. Remote/GGUF: no bind; `invoke_structured_slot` still json_object. Do not invent a SciFi invoker.
7. [Chunk1.4] Do not park `exception.raw_completion` in `raw_output`. `_call` (`_otr_my_story.py` 373-379) stores `on_attempt_complete`'s `last_raw`; capacity/degeneracy leaves that `""` (`_otr_structured_call.py` 949, 993-996). The `finally` block (1127-1137) `parse_first_json_object(attempt["raw_output"])` to fill `proposed_acts`. A new `raw_completion` field is evidence only. Never copy it into `raw_output`.
8. [Chunk1.4] `_full_artifact_repair` instruction fights source repair. Current treatment factory (`_otr_my_story.py` 496-500) says reorganize into N acts and "Preserve its story events...". `default_repair_prompt_factory` echoes `failed_output[:400]` (`_otr_structured_call.py` 479). For PostValidationError, instruction must be "correct the named validation problem; conserve everything else." P0/P3 still use `make_dispatching_repair_factory()` (445, 619); switching them to `_full_artifact_repair` is required by [Chunk3] and must use that same conservation instruction, not the act-count sentence.
9. [Chunk2] `_otr_spoken_text_policy.Finding` has no spans. `Finding` is `(kind, detail)`; `_first_match` (`_otr_spoken_text_policy.py` 174-179) collapses whitespace and slices 60 chars; `f1_findings` uses `search` (first hit only). `_as_findings` copies `detail` as `quote` (`_otr_ledger_clean.py` 404-406). Add a span API (`finditer`, original `match.start()/end()` on the unmodified string). Do not treat `Finding.detail` as offsets. Judge `_NotSpeech.quote` (`1170-1172`) is still a 300-char string with no offsets; `_judge_row` 1249-1252 casefolds a whitespace-collapsed haystack. Require: unique original occurrence, or model `start/end` into the original line; otherwise drop the finding. Repeated matches are not auto-approved.
10. [Chunk2] Neighbor reread still shows the original. `_lines_around` (`_otr_ledger_clean.py` 306-308) injects `>>> {speaker}: {_text_of(here)}` from the live row. `_repair_row` builds `lines_around` once (1788) and rereads with `text=candidate` (1831-1838) while the marker stays original. Rebuild the marked slot from `candidate` before each reread. Keep repair `text=` as the original line if conservation is vs original.
11. [Chunk2] `_call_repair` rewrites protected text. Line 1477: `return " ".join(str(result.text or "").split())`. `_validate` (1423-1426) uses the same collapse. `_RepairedLine.text` `max_length=2000` (1421) will reject a conserved long line. Return the model string unchanged; validate non-empty without collapsing; raise max_length to the row's actual length or drop it.
12. [Chunk2] Best-candidate ships out-of-scope rewrites. `_repair_row` 1872-1901 keeps `best_text` by remaining-count. Reject conservation failures before scoring, including that fallback. Unresolved scope keeps `original` and continues the render (already the unclean path).
13. [Chunk2] Whole-line authorization has no schema/owner. Specify a structured_call schema, e.g. `verdict: spoken_dialogue|nonspoken_direction|uncertain`, `max_attempts=2`, fail-soft like `_call_repair`. Map: spoken_dialogue -> no_edit; nonspoken_direction -> allow whole-line convert through existing `_call_repair`; uncertain -> retain original + receipt. Do not reuse `_SpokenLineJudgement`.
14. [Chunk3] Source reviewer is not a post_validator as specified. `post_validator(instance) -> str | None` (`_otr_structured_call.py` 803-811, 667-670). It has no bundle, technical_fn, or receipts. Add one composer that closes over those, runs structural check first, then nested `structured_call` (max 2) on a named schema. Thread `technical_fn` into `_pass_treatment` / `_pass_act` / `_pass_frame` (today only `_pass_interpret` gets it; runner has `technical_fn` at `_otr_my_story.py` 926). Nested reviewer must not use the author post_validator (no recursion). Catch reviewer `StructuredCallFailedError` / ungroundable quotes as `review_unavailable` and return None (accept). Only mechanically grounded `contradiction` returns a PostValidationError string into the SAME author ladder (`max_attempts=3`). Record reviewer rows on `story["source_reviews"]`, not `story["attempts"]`.
15. [Chunk3] Clean-window rollback API does not exist for source failure. `CleanTransaction.reconcile` (`_otr_clean_transaction.py` 194-195) reseals, and `_degrade` is private. Writer tail (`_otr_writer_tail.py` 1384-1404) always `reconcile()` after `run_ledger_clean` + `run_ledger_cleanup`. Add a public abort that calls `_degrade` (Law 7: restore pre-clean, stamp `content_transition_degraded`, ship). Run the final spoken-projection review after both stages, before reseal. Do not raise out of the window.
16. [Chunk3] Companion selection is unspecified. `_build_char_scene_request` (`otr_meta_brief_image_prompt.py` 1595-1640) takes one `char` + one `line`. "available cast appearances" with no selector will paint the whole roster into every still. Define co-presence as speakers (or named addressees) in the current act/beat only; portraits, announcer, and music radio stay solo (`_mesh_fodder_subject_and_source` 1764-1774). Deterministic `compose_still_prompt` fallback must take the same scoped context. Free-text image prompts are not typed JSON: set `candidate_path="prompt"` (or equivalent) for the reviewer; do not reuse act JSON paths.
17. [Chunk3] Prompt-hash receipts are not the final bytes. `normalize_prompt_for_render` (`otr_image_gen_dispatcher.py` 974-1025) adds safety + `prefix_style_cue` + banana. `_NormalizedPrompt` has `pre_banana_hash` and `prompt_hash`, not a pre-style hash (`_pre_style` is local). Append `base_prompt_hash` (pre-style) and keep `prompt_hash` as final render hash; stamp both on fresh and `cache_hit` (`1545-1575`) before `stamp_durable`. Include current source/context identity in `request_cache_key` or refuse to label a stale hit accepted. Do not claim MetaBrief `prompt_hash` reviewed post-style bytes.

SHOULD-FIX:
1. [Chunk1.5] `_interstitial_count(act_count: int, include_act_breaks: bool)` already exists and is used in P0/P1/frame/assembly (`_otr_my_story.py` 346-348, 438, 489, 762, 1056). Do not take an `acts` list (`list - 1` TypeError). Lock tests for 1/2/3/6 acts x breaks on/off against ledger `music[]` counts. No keyword post-validator (`_make_treatment_validator` 455-468 has none).
2. [Chunk1] Per-call prefix rebuild is shared, not My Story-only. `make_constrained_generate_fn` also serves slot-drama (`_otr_slot_drama_contract.py`). Disabling the implicit array cap without schema `maxItems` unbounded those arrays; rely on cycle detection + capacity, or put explicit `maxItems` on those models.
3. [Chunk1.4] Bound local P1 makes JSONDecodeError retries rare. Keep the structural rung for `output_limit` / `decode_degeneracy` (`_otr_structured_call.py` 988+). Tests that expect syntax repair on LMFE-bound P1 will false-fail.
4. [Chunk3] Untruncated `bundle.fields` vs `bundle.normalized`. P0 uses `normalized` (420-424); P1 uses `project_payload` -> `_labelled_projection(norm)` (`_otr_story_input.py` 343-392), untruncated except headline 80 chars. Raw vs normalized offsets will fail quote grounding. Pick one surface for prompts AND grounding (raw if that is authority) and document the whitespace delta.
5. [Chunk3] Nested technical reviews during creative passes force slot unload/reload via `_account_and_get_entry`. Six-act success path is P0+P1+6xP2+P3 reviewer pairs plus cleanup/final/image checks. That is a Mac VRAM/time ceiling; it will confound the 19,163MB kill you are not allowed to attribute yet.
6. [Mac] `_teardown_gpu_for_entry` (`_otr_model_loader.py` 1550-1655) already `model.to("cpu")`, `del entry`, `gc.collect()`, `torch.mps.empty_cache()`, `synchronize`. Comment at 1557 still says "del cache_entry" releases GPU; the MPS block at 1641-1650 already contradicts it. Fix the comment. Instrument generate-return and teardown with process RSS + `torch.mps.current_allocated_memory` / `driver_allocated_memory` (see `tests/test_llm_teardown_releases_on_metal.py`). Do not `model.to(meta)`. Preserve epoch/orphan (`unload_llm` / `_CACHE_EPOCH_LOCK`).
7. [O1] Name collision: `render_driver.declared_render_canvas` already documents "THE DEFECT THIS CLOSES (O1)" as canvas (`render_driver.py` 472). This plan's O1 is pre-image MISSING-STILL vs render-time loud (`2689`). Use a different id. Cast-preflight vs render: `build_request_from_shot` (`2403`) vs MISSING-STILL log. Do not soften post-image hard checks.
8. [O2] LTX-OPEN health is `render_driver.py` ~6058-6083. Compare planned radio-open engine to actual; `still_pan` success stays info; requested LTX fallback/missing stays fail; absent intent -> unknown, not a fabricated LTX miss.
9. [O3] Same-file rename: `otr_master_audio_mux.py` 732-734 is already LOUD for same-file post-rename; downgrade that class to info; keep real mismatches loud. Same for image dispatcher / render manifest -- verify: exact remaining warning sites before editing.
10. [Verification] 54-failure baseline is `docs/HANDOFF_LOG.md` line 208 (14,646 cases / 54 failures). Compare to that recorded set; do not call the suite green.

OPTIONAL / NICE-TO-HAVE:
- Expose `constrained_generate_fn.prefix_allowed_tokens_fn` as a per-call property; tests currently read it off the factory closure (`test_constrained_generate.py` 132-135).
- `_raw_head` already falls back to `error.raw_completion` (`_otr_structured_call.py` 705-706); keep logs using that, receipts using the new field.
- Stamp reviewer `configured_model` vs `resolved model_id` from the slot cache_entry when present; if unknown, record unknown, do not invent.

CUT THESE:
1. [O1-O3 in this build] GO_FORWARD A1R item 6 + "Observability follow-up after A1R" already defer these past source-fidelity. They are log-level changes, not ledger repairs. Ship after Chunk1-3 + Mac measure; stuffing them here collides with the existing canvas "O1" and delays the blocking APIs above.
2. [Chunk3 Python fact inventory / late mux veto] Already forbidden; there is no code to add. Do not add a mux eligibility gate (`otr_master_audio_mux.py`) for source qualification.
3. [New image-prompt retry engine] `max_reseed` already loops in `_compose_char_scene_prompt` (`1644-1674`). Feed source-conflict into that loop; do not add a second retry stack.
4. [Keyword / boolean interstitial post-validator] Not present; do not add one "to be safe."
5. [Re-deriving `_interstitial_count`] Already the production helper; a second copy will drift.

[ASSUMPTION] Companion rule "speakers in the current act/beat" is inferred because the plan gives no selector; judge must lock a rule before image work.
[ASSUMPTION] Marker-on-`ProviderCapacityMessages` is the intended no-bank-name opt-in; only local schema-bound transports read it, so unbound SciFi/P2/P3 stay cycle-only.
[ASSUMPTION] `docs/HANDOFF_LOG.md` 54-failure figure is the "recorded54-failure baseline" named in Verification; verify the exact JUnit file before gating a chunk.
