# R1 judgment

Driver Codex. Independent CLI readers: Gemini 3.8 Flash (High), Cursor
Grok 4.6 High. Both completed against the real Windows tree. Neither approved
the underspecified fidelity design. Grounded subagent reviews agree that it
needs an executable contract. No architecture code has been edited.

Accepted: raw source missing from P2/P3; P0 interpretation cannot replace it;
P1 binding requires fresh grammar state and explicit capacity policy first;
one-act music is a producer defect; cleanup cleanliness does not authorize
unrelated rewriting; MetaBrief owns inverse scene staging; source qualification
must not become a late mux veto. Mac runtime/storage refusals are correct.

M1 correction: current My Story and cleanup never bind the grammar. The
activation_followup.md records the driver's correction immediately after R1
dispatch. Installed LMFE 0.11.3 tokenenforcer.py retains prefix_states and
allowed_token_cache. This establishes a shared static lifetime defect, not
the cause of the Mac incident. Reject Gemini's assertion of proven MPS/KV
growth and prescribed cache flushing: current teardown already flushes MPS,
and no raw Mac log locates the kill. GGUF is not an installed qualified escape.

Reject both reviewers' proposed deterministic semantic detector: verbatim
quotes prove identity, not whether a relationship contradicts another one.
No general natural-language fact compiler already exists in this repo. A
small source-versus-candidate model review is permitted correctness work;
it must identify its uncertainty and never claim infallible entailment.
Reject dropping all non-verbatim assumptions: compatible creative detail is
the purpose of adaptation. Reject finite list/string caps proposed by Cursor.
Installed LMFE supports implicit-array-limit=0, but its prefix builder replaces
parser.config. Tests must exercise that builder, not an isolated parser.

Reject Gemini's 80% retention heuristic and blanket full-line suppression
from Cursor. The former permits source mutation, the latter loses legitimate
direction-to-dialogue repair. Authorize exact located spans; require a separate
speech-versus-direction decision for whole-line complaints and preserve the
original on uncertainty. Pattern summaries are truncated, so they cannot serve
as coordinates. Candidate reread context must show the candidate rather than
the original marked neighbor.

Reject the statement that the 4060 folder is absent: Test-Path and its README
were read on this checkout at 2499b411. Raw row artifacts are absent; the report
is present. C2 stays unverified because aggregate counters alone cannot prove
what happened on that leg. O1-O3 remain requested maintenance after blockers;
the operator asked to fix all reported defects, so they are not cut.

GO_FORWARD has been corrected to reflect received Mac evidence, real visual
owners, review-before-code, no keyword rejection, and no late publication gate.
R2 input below is a concrete implementation contract, with remaining design
decisions explicitly exposed to the next readers.
