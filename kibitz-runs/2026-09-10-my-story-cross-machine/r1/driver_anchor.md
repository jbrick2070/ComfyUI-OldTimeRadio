# R1 driver anchor -- Codex, 2499b411

VERDICT: NEEDS DESIGN CONVERGENCE BEFORE IMPLEMENTATION.

CONFIRMED: the incoming docs are real full-canonical 4060 evidence, and Mac
reports distinguish stale deployment, OS memory termination and disk refusal.
No current proof qualifies My Story source preservation across a full episode.

CONFIRMED from real Windows source: `_otr_constrained_generate` stores parser
and prefix callback under each resident cache's by_schema. Installed LMFE
TransformersPrefixAllowedTokensFn holds TokenEnforcer, whose prefix_states and
allowed_token_cache are mutable per-generation state. `_SpokenLineJudgement`
is a function-local schema. Keep tokenizer-wide preparation, release request
history. The Mac kill's sole cause remains UNVERIFIABLE until runtime retest.

CONFIRMED: P0/P1 map include_act_breaks directly to yes for one act; actual
assembly derives no interstitials. Fix authoritative control projection first.
A word blacklist over generated assumptions is not a justified structural fix.

CONFIRMED: My Story P2/P3 prompts omit the raw input that P0/P1 receive. Current
fidelity_discrepancies only reflects missing names and gender. Shared cleanup
acceptance checks cleanliness rather than conservation outside complaint spans.
MetaBrief owns the missing visual source context. A fact-contract design must
cover those writers without requiring every portrait to show every character.

CONFIRMED: `_otr_publication_eligibility` is a rights/identity contract today;
an eligible receipt is not a source-quality assertion. New receipt semantics
need explicit ownership and hashing, not a silent new gate in read-only freeze.

MISREAD in the handoff proposal: enabling P1's binder is not automatically a
gate-neutral syntax change. It reaches the open-string stop policy and LMFE's
default array bound. Ground and resolve the actual builder/config path first.

UNVERIFIABLE: five model-dirty rows versus six repairs is an off-target edit.
Pattern-only targets can account for the difference; obtain row-level receipts.

MUST-FIX: source authority before P0; request-scoped grammar state; explicit
grammar limits; cleanup scope conservation; direct facts at downstream authoring
and scene assets; truthful source receipt consumed at the appropriate boundary;
phase/intent/identity-aware diagnostics. Preserve selected acts, flexible cast,
real errors, existing repair ownership and the one canonical workflow.

The driver will ground every external finding. Reviewers are read-only except
their own designated review file. No deployment, model downloads or GPU runs.
