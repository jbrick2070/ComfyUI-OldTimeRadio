VERDICT: no -- source-preservation is the stated goal, but the chosen method never specifies the check, contradicts GO_FORWARD/PBUG on gates and blacklists, and serializes an unproven Mac-memory leak that is not on the current My Story P0--P3 path.

MUST-FIX BEFORE BUILD:

1. [Proposed order 3 / F1 / Critical constraints 3-4,7] Goal and method diverge. Live 4060 failure is schema-valid P0 inventing death/absence, then P1 consuming that interpretation, then P2 writing from treatment only (`nodes/_otr_my_story.py` `_pass_interpret` 412-443, `_pass_treatment` 466-499 dumps `interp` into P1, `_pass_act` 544-588 never includes `bundle` source text, `_pass_frame` 595-617 gets title/logline/setting only). The proposed repair "carry original source into P2/P3" does not stop P0 from poisoning P1, and "review the smallest supported semantic-check design before implementation" is not a design. Concrete fix: freeze one check before any F1/F2 code -- deterministic, at the owning pass, before persist: (a) drop or reject `StoryInterpretation.assumptions` that are not verbatim-supported by `bundle.normalized` fields; (b) do not forward rejected assumptions into P1; (c) inject `project_payload(bundle)["full_text"]` into P2/P3 prompts; (d) post-validate accepted P0/P2/P3/visual/cleanup artifacts against those same source bytes. No second model judge. Do not start F1 until this is written as acceptance rules, not a research item.

2. [Critical constraint 7 vs GO_FORWARD A1R.4 / PBUG-20260910-03] Late-gate contradiction. `nodes/_otr_publication_eligibility.py` blocks only `REASON_RIGHTS_RESEARCH_ONLY` (BLOCKING_REASONS ~102, evaluate 279-306). Identity is informational. Mux consumes that receipt. GO_FORWARD A1R.4 and PBUG-03 say eligibility must consume a source-fidelity receipt. Constraint 7 forbids a late gate that destroys a finished render. Pick one and write it: authoring fail-closed BEFORE image/TTS; a separate non-blocking `meta.my_story` fidelity receipt with hashes/scope/lifecycle; publication_eligibility stays rights/identity. Do not add a blocking reason that can veto `otr/obs` after a successful canonical render.

3. [Critical constraint 3 vs PBUG-20260910-03 verification / GO_FORWARD A1R.3] Blacklist vs required fixtures. Constraint 3 forbids a death/fire/alone/waiting list. PBUG-03 verification and 4060 Steps 117/121 require rejecting exactly those substitutions. Concrete fix: do not scan vocab. Require surviving source spans (byte-equal substrings from the typed fields) and reject a structured field only when it asserts a relation that negates a span still present in the source (example: source contains "living mother" and P0 `assumptions[0]` asserts deceased). Unstated status stays unstated. If a candidate check cannot be expressed as span-presence plus structured-field contradiction, it is out of scope.

4. [Proposed order 1 / M1 vs Evidence Mac / LLM_PREFLIGHT_GUIDE] False premise that M1 is the first source-preservation blocker. `_otr_bind_schema` is assigned in `nodes/OTR_LedgerScriptWriter.py` 737-742 and is not called anywhere in production except tests (`docs/LLM_PREFLIGHT_GUIDE.md` 119-132: leftover from ripped scifi_codex; live consumer is SlotJobFields at writer ~4662). My Story `_call` -> `structured_call` never binds. Binding P1 would be the first My Story LMFE consumer and would INTRODUCE parser/prefix state onto that path. Mac kill was writer/cleanup on unbound Qwen (`docs/MAC_LESSONS_LEARNED.md` section 10). M1 cache-by-schema in `nodes/_otr_constrained_generate.py` 95-141 is real for the SlotJobFields path; it is not proven as the OS kill and is not on P0--P3 today. Concrete fix: align with GO_FORWARD A1R -- S2 + F1-narrow + C1 first; treat M1 as a hard precondition only of the P1 bind chunk; prove tokenizer-data reuse vs per-request parser/prefix release with unit tests, including cancellation. Do not block 4060 fidelity work on a Mac RSS hypothesis. [ASSUMPTION] verify: LMFE `TokenEnforcer.prefix_states` / `allowed_token_cache` actually accumulate across `generate()` when the cached `prefix_fn` is reused -- not evidenced in this repo.

5. [Grounded triage S1 / PBUG-20260910-05 / Constraint 8] P1 bind is under-specified relative to its own warnings. Binding `StoryTreatment` via `_make_generate_fn(schema_model)` (`OTR_LedgerScriptWriter.py` 844-853, 715-743) also attaches the 2048 open-string degeneracy guard (`_otr_constrained_generate.py` 288-293, `_otr_decode_guard.py` MAX_OPEN_STRING_TOKENS=2048). In-repo there is zero `max_json_array_length`. `StoryTreatment.acts`/`cast`/`turns` are unbounded lists (`_otr_my_story.py` 220-236). Concrete fix before bind: (a) one-time bind of a copy of the local P1 callable only, P2/P3 remain the original `creative_fn`; (b) set explicit max_length on every StoryTreatment list/string to values that cannot trip array-20 or 2048-open-string on a legal treatment; (c) canned three-attempt malformed-JSON control must still fail closed; (d) prove causal closure on the 4060 Qwen local path, not only that the binder was called. verify: installed LMFE version, default array cap, and whether the prefix builder overwrites `parser.config`.

SHOULD-FIX:

1. [C1 / Critical constraint 5] Use the already-written C1 rule in `docs/PROD_BUG_LOG.md` PBUG-20260829-14 addendum: keep the coda bytes unless the complaint quote is a strictly shorter in-line span. `_repair_row` (`nodes/_otr_ledger_clean.py` 1762-1871) currently commits any candidate with zero remaining pattern/judge hits, which is how "Until next time" became "That's a wrap" (4060 Step 120). Whole-line complaints must be dropped, not repaired (judge already documents this as a skim at 1263-1269 but `_validate` returns None at 1184-1198, so those quotes still reach repair). Do not invent a new coordinate ontology or mark every My Story row `protected_fact_component` (that flag is for Python-appended source facts, lines 101-134).

2. [F2 / Constraint 6] GO_FORWARD A1R.3 names ShotLock/ImageDirector as visual owners. Wrong layer. `nodes/otr_image_director.py` is engine/granularity/seed policy. Inverse staging is authored in `nodes/otr_meta_brief_image_prompt.py` `_build_char_scene_request` 1595-1641 (singular "this character", no companion) and `_compose_char_scene_prompt` 1644-1674 (first nonempty LLM line wins). Concrete fix: scene prompts for story beats must include the other required present names from source/treatment; headshots and announcer/music radio assets stay exempt. Reject inverse staging on the scene prompt before image gen, not at ImageDirector routing.

3. [S2] Producer fix is real: P0/P1 emit `"music between acts: yes"` from `include_act_breaks` alone (`_otr_my_story.py` 429-433, 479-484) while one-act assembly emits no interstitial (`_assemble` 783-793: only if act is not last). Replace with one integer `max(0, act_count-1) if include_act_breaks else 0`, same value as P3 `inter_wanted` (1050). Do not keyword-ban "music" in prose. Also stop persisting a P0 assumption/requirement that asserts between-act events when that integer is 0 (Step 121 still stored that claim in interpretation while assembly was empty).

4. [O1] Confirm the loud line is `nodes/_otr_video_engines/render_driver.py` 2688-2694 inside `build_request_from_shot`, which ShotLock calls at cast-time (`otr_shot_lock.py` 1902). Relabel that call CAST-TIME STILL DEFERRED; keep the same warning at actual render-time missing still. Do not weaken the post-image spine.

5. [Evidence and boundaries] `docs/2026-09-10-my-story-4060/` is cited as the six-leg receipt and is absent from this working tree. Durable evidence that is here: `docs/4060_DRILL_LOG.md` Steps 116--121, `docs/MAC_LESSONS_LEARNED.md` section 10, `docs/PROD_BUG_LOG.md` PBUG-20260910-03..05. Point the work queue at files that exist, or land the incoming docs commits before coding. verify: whether those three docs-only commits add the missing folder.

6. [Proposed order 6 vs Constraint 1 vs GO_FORWARD stress table] Recovery is one canonical one-act pair-lock publication on the repaired revision (5080, then 4060 Z-Image/still_pan, Mac runtime+model). Do not quietly inherit GO_FORWARD's 3x6-act / 3-story / 3-model stress matrix into this chunk. Writer/freeze on 5080 (`f829c920`) remains a component, not qualification -- keep that distinction.

OPTIONAL / NICE-TO-HAVE:

- O2: `check_ltx_open_health` (`render_driver.py` 6034-6086) warns on any non-LTX radio-open, including intentional `still_pan`. Compare expected engine from the frozen route; only demand LTX when selected. Nonfatal on 4060.
- O3: downgrade successful same-file rename in mux (`otr_master_audio_mux.py` ~732), image dispatcher, render_driver; keep identity/path mismatch loud.
- C2: leave unverified until a row-level cleanup receipt exists. Do not treat "five dirty / six edits" as a defect.
- Park A2/credits/App exactly as the input already does.

CUT THESE:

1. A general semantic-entailment / extracted-fact-list authority. Constraint 4 already forbids it; `_otr_source_grounding.py` is beat-window selection for long PD texts, not four short My Story fields. Safe to cut: listener fields already have `digest` + normalized bytes in `_otr_story_input.py`.
2. Binding P0/P2/P3 grammars in this campaign. S1 evidence is P1 treatment JSON; constraint 8's array-20/open-string blast radius grows if ActScript.lines is bound. Safe: P2/P3 already have typed post-validators and a three-attempt ladder.
3. Feeding source-fidelity into mux `eligible`. Safe: 4060 Steps 119-121 already published technically eligible, semantically wrong episodes; qualification is a receipt, not a second destroyer of `otr/obs`.
4. Treating cleanup's nested per-call pydantic classes (`_otr_ledger_clean.py` 1170-1174, 1420) as a current M1 leak. They are not passed to `get_cached_transformers_schema_constraint` unless someone binds them. Binding them is out of order.
5. Keyword ontology / family-status taxonomy as a standing subsystem. The live bugs are two fixtures. Span checks on those fields are enough.
6. RunPod, extra video/TTS engines, and the GO_FORWARD stress matrix as part of this repair. Input already parks them; do not let "coordinated tests on the few proven routes" expand back into that table.
7. M1 as a Mac-qualifying live render. D2 correctly forbids cache deletion and invented fallbacks. Prove the leak on the SlotJobFields/cached-prefix unit path; Mac retest is loaded-runtime + existing model after A1R, not a reason to delay F1.

[ASSUMPTION] Cross-machine: 5080 (`nodes/` owner) is the production editor; 4060/Mac retest only. M1 behavioral proof on MPS 16 GB is not available on the 5080 path -- do not claim the OS kill is closed by 5080 unit tests. [ASSUMPTION] 4060 writer was local Qwen (`HANDOFF_LOG.md`); bind/LMFE apply there. Mac Qwen3.5-4B MPS is also local transformers; GGUF would not use this cache. verify: Mac dropdown provider tag on the killed run.
