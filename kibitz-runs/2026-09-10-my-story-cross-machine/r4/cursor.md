VERDICT: yes-with-fixes — A/C and shared grammar/abort/visual-hash are implementable, but two D control-flow forks plus parser placement vs the min_p retry still allow incompatible first runs.

MUST-FIX BEFORE BUILD:
1. [preamble 5 / D] Inner-review exception map is still two-valued. `StructuredCallFailedError` is not in `_ATTEMPT_ERRORS` (`nodes/_otr_structured_call.py` 215-219). If the post-validator lets inner `structured_call` raise, outer hits `except Exception: raise` (975-977) and the author pass dies; if someone returns that error as a string, it becomes author repair. `GenerationDegeneracyError` is a `PromptContextOverflowError` (`nodes/_otr_generation_budget.py` 141, 169) so a leaked retryable capacity error WOULD be retried by the outer ladder. Concrete fix: around the inner `structured_call(max_attempts=2)` only: schema/parse/SCFE exhaustion -> uncertain receipt, no author repair string; `prompt_no_room` -> uncertain (unavailable verifier); torch OOM / cancel / other non-capacity errors re-raise unchanged; only a grounded contradiction returns the existing post-validation error string. Add the branch tests already named.
2. [D] “If the returned organization cannot finish within real output capacity, … split” does not say what “cannot finish” is. One builder will split on every `StructuredCallFailedError`; another only on `output_limit` / packet-too-large. That yields different window trees and coverage receipts. Concrete fix: split that source interval only when THAT call’s measured failure is `output_limit` or the packet cannot be fitted; schema/parse/non-progress uses the already-stated bounded organization repair; an irreducible nonempty window (including a 1-character span) that still cannot fit is the envelope/provider failure, not another split of the same interval.
3. [preamble 1 / A] “Fresh state before EVERY actual model.generate, including min_p compatibility retries” still allows “once per generate_fn.” The min_p retry is a second `model.generate` in the same wrapper (`nodes/OTR_LedgerScriptWriter.py` 1038-1059) reusing `gen_kwargs["prefix_allowed_tokens_fn"]`. Building the parser at the top of `generate_fn` leaves the retry on mutated LMFE state — the original P1 bug. Concrete fix: construct parser+prefix immediately before each `model.generate`, including that retry, and the same inside `constrained_generate_fn` (`nodes/_otr_constrained_generate.py` 242-315). Do not stash them on the closure (preamble 1).

SHOULD-FIX:
1. [preamble 4 / D] Name declared pair-coverage scope per pass. Today “whole-candidate qualification requires full pair coverage within its declared scope” plus “Scope an act to its actual plan” lets one builder cartesian all raw windows × all acts and another only delivered-to-that-call spans. Lock: P0/P1/P3/final spoken = all delivered raw windows × that pass’s candidate (or its explicit partitions); P2 = that act’s delivered spans × that act only; skipped pairs stay unresolved; no PASS from notes-as-relevance.
2. [D / preamble 3] Deterministic split fallback: prefer newline/paragraph boundaries inside the Python `[start_char:end_char]` interval; if none, split at midpoint; both children nonempty, no skipped chars, no overlap. Same for note-merge: consume the oldest two pending notes that still fit, else bounded non-progress then envelope failure.
3. [B / preamble 2] Keep the existing remote backend set when rewriting early routing: `_REMOTE_DISPATCH_BACKENDS = ("openrouter_http", "comfy_credits_http", "google_api_http")` at `nodes/_otr_model_loader.py` 1982. A new “remote/GGUF” list that drops Google sends those handles into native HF.
4. [A] In the grammar chunk, state that `max_json_array_length=0` disables LMFE’s implicit default (`default_max > 0` in `lmformatenforcer/jsonschemaparser.py` `ListParsingState.__init__` 647-649), it does not mean “arrays must be empty.” After `TokenEnforcer` replaces `parser.config` with `CharacterLevelParserConfig(alphabet=...)` (`lmformatenforcer/tokenenforcer.py` 65-66), mutate the live config’s `max_json_array_length` to 0 and keep that alphabet. Tests: 25 items via real `prefix_fn` (preamble 1), not the “21+” in A.
5. [C / preamble 6] After splice, if the spoken row is empty/whitespace-only, treat as authorization failure and keep the original bytes. Empty localized replacement may delete a direction span; it must not blank the row by partials.
6. [D] Name the raw-coordinate version string as a module constant. `SourceDocument` refuses whitespace-only bodies (`nodes/_otr_source_document.py` 233-235); treat strip-empty optional fields as “no document,” and keep offsets on the exact raw field bytes when the body is nonempty. Hashes stay UTF-8 (`canonical_body_sha256`, line 81).
7. [E] `CleanTransaction.abort` then a later `reconcile()`: return the stored abort receipt (`outcome` restored_pre_clean), do not run `_reseal`’s noop path as a success signal. `stamp_transition(None)` already pops only `CONTENT_TRANSITION_META_KEY` (`nodes/_otr_content_transition.py` 251-253), not `DEGRADATION_META_KEY` — keep that.

OPTIONAL / NICE-TO-HAVE:
- [E] Same-view hash as a skip optimization after the first review of that projection; never as the first-check gate.
- Align A’s “21+items” wording to preamble 1’s 25.
- Campaign-local diagnostic names in F; do not spend this build on O1 global renames.

CUT THESE:
1. [E] “Do not force a new model load in dispatcher solely to paint a false PASS” is already the rule — do not add a dispatcher LLM pass. Safe: `otr_image_gen_dispatcher.py` is a cache/render owner; unreviewed banana/final is a labelled receipt plus recovery inspection.
2. [B] Fit-inspection methods on every unused unbound helper closure in the first chunk. Organizer is technical/P0 (preamble 2). First production only needs fit on that slot callable; add creative-unbound fit when an authoring pass actually sizes with it.
3. [D] Any remaining 64-char / depth-8 / global work-item / compulsory-summary gates. Already rejected; they only reappear as “safety.”
4. [F] `model.to(meta)`, model delete, predictive story gates, unqualified smaller-writer claims. Already forbidden; they are not this campaign.
5. New abort enum / JSONPath / second author retry engine / extra visual retry loop — already rejected; do not revive in D/E.

VERIFY-AT-BUILD checklist:
- Installed LMFE: `CharacterLevelParserConfig` default array 20 (`lmformatenforcer/consts.py` `DEFAULT_MAX_JSON_ARRAY_LENGTH`); `TokenEnforcer` overwrites `parser.config`; root `ListParsingState` captures max at construction; nested lists after builder. Euler’s 0.11.3 claim: verify the venv copy used at build.
- Fresh parser+prefix on the min_p retry (`OTR_LedgerScriptWriter.py` 1059) and on every constrained `model.generate`.
- Drop `by_schema` (`nodes/_otr_constrained_generate.py` 123-140); update `tests/test_constrained_generate.py` (~134-150, ~160) which currently asserts cache + closure attributes.
- `make_degeneracy_criterion(..., max_open_string_tokens=None)` skips the open-string tracker but keeps cycle (`nodes/_otr_decode_guard.py` 272-335); snapshot `_otr_unbounded_json_field` before message normalization; unbound loader routes stay cycle-only.
- `_inherit_generation_contract` (`nodes/_otr_structured_call.py` 545-556): string repair coerced to one user-message list, original list subtype + markers copied; no My Story import.
- Native pin is an extra reuse/publish dimension beside `policy_key` (`nodes/_otr_model_loader.py` 214-235); do not stuff pin into `LLMRuntimePolicy.cache_key()` or `GGUFLoadConfig.reuse_key`. `load_llm` must stop mutating `max_position_embeddings` (1227-1229). Nested `text_config` before top-level keys (`_read_advertised_context` today only reads top-level, `_otr_model_catalog.py` 689-704).
- Remove `_otr_loader_backends.check_context_window` load reject (`79-108`); rewrite `tests/test_context_window_precondition.py` (currently expects raise below `HARD_VRAM_CONTEXT_LIMIT`).
- `MIN_OUTPUT_TOKENS` 64→1 is `_otr_generation_budget.py` 12 only; OpenRouter/Comfy floors stay 1024 (`_otr_openrouter_backend.py` 224, `_otr_comfy_backend.py` 139).
- Fit inspection must not call `_account_and_get_entry` (`OTR_LedgerScriptWriter.py` 640-646).
- `_NormalizedPrompt` is six named fields today (`otr_image_gen_dispatcher.py` 954-1022); append `base_prompt_hash` with default; consumers use `_norm.text` / `.prompt_hash` / `.pre_banana_hash`.
- `CleanTransaction` has `restore`/`reconcile`/`_degrade` and no `abort` (`nodes/_otr_clean_transaction.py`); writer always `reconcile()` (`_otr_writer_tail.py` 1403-1404).
- `_call_repair` still has `max_length=2000` (`_otr_ledger_clean.py` 1421); remove for conserved rows.
- `_pass_treatment` still binds `slot_fn=creative_fn` (`_otr_my_story.py` 493); bind a local `_otr_bind_schema(StoryTreatment)` copy only; P2/P3 stay original `creative_fn`.
- Attempt callback still stores transport failures in `raw_output` (`_otr_my_story.py` 373-378, 1126-1137). Split `raw_completion`.
- Canonical: 23 nodes (`tests/test_canonical_widget_input_parity.py` 21), writer `widgets_values` length 37 (`tests/test_workflow_json_guardrails.py` 658). verify: `len(workflows/otr_canonical.json["links"])` — plan’s “63link” vs `last_link_id` 291. No widget/socket edits.
- Mac OS-kill cause remains UNVERIFIED (R1). F’s generation/retirement observer + Mac-owner logs close it; 5080 unit tests cannot. C2 five-dirty/six-edits stays a receipt-accounting question (`judged_dirty` vs pattern-only), not a defect to “fix.”
- `docs/2026-09-10-my-story-4060/` and `docs/2026-09-11-my-story-cross-machine/` are cited by `docs/GO_FORWARD_PLAN.md` and are absent from this tree. Do not treat those paths as live evidence; use `docs/4060_DRILL_LOG.md` / `docs/MAC_LESSONS_LEARNED.md` / `docs/PROD_BUG_LOG.md` plus new obs receipts.
- Suite headline 14134 pass / 54 fail / 183 skip / 1 xfail: verify against current JUnit before citing as the gate.
- Base `59a40131`, S2 already pushed: verify `git log`; do not reimplement S2.
- First recovery: only `workflows/otr_canonical.json`, one-act living-mother/shared-dinner, assets in `otr/episodes/<ep>/` and `otr/obs/`. Source qualification ≠ publication.

[ASSUMPTION] A/B/C plus abort/visual-hash can ship as independent chunks once 1–3 are locked; D adaptive authoring waits on must-fix 1–2. [ASSUMPTION] Organizer-only fit is enough for the first adaptive production run because preamble 2 assigns P0 to technical.
