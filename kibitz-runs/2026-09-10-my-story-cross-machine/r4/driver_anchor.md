# R4 driver anchor, before reviewer dispatch

Root checked R3 claims against actual Windows code. Full inputs R1->R2->R3
remain immutable; this round consumes r3/final.md. GO_FORWARD remains main queue.
Current production edits are diagnostic-only and already have separate CLI QA.

Settled shared M1/P1 and A2 contracts are implementation-ready subject to final
confirmation. Newly grounded M1 detail: installed LMFE needs array config0 before
root-parser construction AND after prefix builder overwrites config/alphabet.
Fresh parser before every actual generation and no long-lived closure attributes.

Source coverage must track comparison pairs and delivered evidence, not marginal
coverage or hashes masquerading as semantic proof. User wants a model-organized
working brief over source that exceeds genuine capacity, no arbitrary size/pass
gate. Try the whole input when it fits. Preserve raw source and scoped receipts.
No ontology, external author retry engine, late mux veto or compulsory summary.

Grounding rejects R3's proposed fixed64char/depth8/work-item limits, P2/P3 tracker
regression claim (currently unbound), and automatic author reroll from reviewer
exhaustion (StructuredCallFailedError not in _ATTEMPT_ERRORS). Test ownership.
CleanTransaction abort is terminal/idempotent; writer calls abort XOR reconcile.

Review only new must-fix material. Mark which independent chunks converge, and
which specific unresolved design (if any) needs a focused amendment. Do not
repeat an implementation absence as a plan defect when the plan already names
its owner and change. Driver remains sole judge; no code edits by reviewers.
