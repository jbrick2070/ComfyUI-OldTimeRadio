VERDICT: yes-with-fixes -- A then B then C then D is the right build order, but several A/B/D/E contracts still drop markers, mix units, or retry the wrong slot.

MUST-FIX BEFORE BUILD:
1. [D + A] Nested reviewer inside the author ladder retries the wrong call. Plan D closes review context in the P0-P1-P2-P3 post-validator and runs `structured_call(max_attempts=2)` on `technical_fn`. Those helpers today call `structured_call` on `creative_fn` (`nodes/_otr_my_story.py` `_pass_treatment` ~471, `_call` ~367). `_parse_and_validate` runs `post_validator` after a successful author parse (`nodes/_otr_structured_call.py` ~956). `_ATTEMPT_ERRORS` includes `CAPACITY_ERRORS` (~215); reviewer OOM/overflow is therefore retryable on the author ladder (`is_rerollable_capacity_error`). That re-invokes creative, not the reviewer. Smallest fix: run source review AFTER the author `structured_call` returns; feed a contradiction back as a new author call. Fence nested exceptions: re-raise provider/OOM/cancel; only a grounded `contradiction` becomes the error string. Do not put a second `structured_call` inside `post_validator`.
2. [A] Class-level `_otr_unbounded_json_field` on `ProviderCapacityMessages` plus "P2/P3 original callable stays" turns off the only spiral detector on those passes. `ProviderCapacityMessages` is shared (`nodes/_otr_generation_budget.py` ~13; SciFi `_pass_treatment` ~1976; My Story `_call` ~370). Writer open-string tracking is already schema-only (`OTR_LedgerScriptWriter.py` ~984: `tokenizer if schema_model is not None`). P2/P3 stay unbound. The P2 specimen is the non-cycling open string (`nodes/_otr_decode_guard.py` ~321). Smallest fix: do not set the marker on the class. Set it only on the P1 bound callable after LMFE, or keep the tracker on unbound P2/P3.
3. [A] Repair/normalization drops the capacity contract; receipts store empty `raw_output` on transport halt. `_inherit_generation_contract` returns a non-list target unchanged (`nodes/_otr_structured_call.py` ~549). String repairs then hit `_prompt_to_messages` and become a plain list (~517), losing `ProviderCapacityMessages`. Gemma fold path in `normalize_messages_for_tokenizer` always returns a new plain list (`nodes/_otr_loader_backends.py` ~256). `_call.completed` stores `raw_output=raw` (~376); `notify_attempt` passes `last_raw`, which is cleared before `_invoke_slot` (~949). `_raw_head` already reads `error.raw_completion` (~705); the receipt hook does not. Writer degeneracy always sets `open_string_tokens=None` and always says "repeated a run of tokens verbatim" (`OTR_LedgerScriptWriter.py` ~1130); `constrained_generate_fn` does the same (`nodes/_otr_constrained_generate.py` ~347). Smallest fix: snapshot `_otr_unbounded_json_field` / `_otr_reserve_remaining_output_capacity` before any message rewrite; coerce string repairs to one user row then `copy.copy` the source subtype; `completed()` writes `raw_output` only from a returned string and `raw_completion` from `getattr(error,"raw_completion",None)`; halt text and `open_string_tokens` follow `_guard.reason`.
4. [E] `abort` then the existing `reconcile()` path reseals a restored ledger. Writer tail always does open -> clean -> cleanup -> `reconcile()` (`nodes/_otr_writer_tail.py` ~1384-1404). `CleanTransaction` has `restore` / `_degrade` / `reconcile`, no `abort` (`nodes/_otr_clean_transaction.py` ~147-334). `_degrade` takes an `Exception`. If abort restores then `reconcile()` runs, `_reseal` sees a no-op and stamps transition None (~241). Smallest fix: public `abort(reason, evidence=None)` shares `_degrade`'s guarded restore+receipt; writer tail calls abort XOR reconcile; never both.
5. [B + D] Fit probe is not a callable the organizer can call without lying. `_SlotScheduler.for_slot` returns `generate_fn`; `_otr_bind_schema` is `_make_generate_fn` and returns a new closure (`OTR_LedgerScriptWriter.py` ~715-745). Every generate goes through `_account_and_get_entry`, which always `request_slot` + increments `calls_by_slot` (~602). Remote/GGUF generate_fns have no tokenizer. D sizes windows from "the real prompt/context" before P0. Smallest fix: put `inspect_fit` on every `_make_generate_fn` result (bound and unbound); acquire without incrementing calls; prepare CPU tensors only; drop tensor refs, not `LLM_CACHE`; remote/GGUF: capability absent, use existing `estimate_prompt_tokens` and label it estimated. Name the organizer slot: technical, same as P0.
6. [B] HF-root / pin / `context_cap` wiring as written collides with remote/GGUF and with the pin-vs-estimate split. `request_slot` currently: `validate_model_id` (~1957) -> remote return (~1998) -> `resolve_context_cap` (~2018) -> GGUF -> `ensure_hf_home` (~2133) -> `load_llm(..., context_cap=ctx_verdict.value)` (~2189). Publish key is `policy_key` only (`~2213`); `_try_cache_hit_locked` compares only that (`~234`). `validate_model_id` Path 2 scans `hub_root` (~1737). `ensure_hf_home` mutates `os.environ` (`nodes/_otr_hf_env.py` ~122). Plan also says no new HF work on remote/GGUF. Smallest fix: identify remote/GGUF from curated row AFTER suffix-strip, return before `ensure_hf_home`. Transformers path: `ensure_hf_home` -> pass that hub into `validate_model_id` / scan / download. Snapshot pin once; HF lookup AND `_publish_cache_entry_if_current` use `(policy.cache_key(), normalized_pin)`; do not put pin on `gguf_load_key`. `load_llm(context_cap=)` is the pin only; never pass `ctx_verdict.value` as that pin. Native capacity is a separate entry field.
7. [D] Source span contract does not match `_otr_source_document`. `SourceSpan` is Python char offsets into one body; hash is UTF-8 bytes (`nodes/_otr_source_document.py` ~73-181). `_assert_tiles` requires one `[0, total)` tiling (~184). My Story has four raw fields (`nodes/_otr_story_input.py` `CREATIVE_FIELDS`; `fields.as_dict()`). P0/P1 today feed `bundle.normalized` / `project_payload` (~421, ~486, `project_payload` ~370), not raw bytes. Smallest fix: one transient `SourceDocument` per nonempty raw field; span ids are `(field, start_char, end_char)` in Python str coordinates; receipts store hashes+offsets+coordinate version, never "byte coverage" unless you also index bytes. Do not tile four fields as one body. Organizer loop: hard cap on split/merge work-items and refuse unchanged packets with `GenerationContextOverflowError` / cancel, never a source-conflict label.
8. [D] `source_reviews` will not survive a failed pass. `on_attempt_complete` exceptions are swallowed (`nodes/_otr_structured_call.py` ~932). My Story persists `story["attempts"]` in `finally` (`nodes/_otr_my_story.py` ~1126). Reviewer calls are a separate `structured_call` and will not hit that list unless you pass a callback. Smallest fix: append source_reviews onto `story` and save in the existing `_require_ledger_save` / `finally` path (interpret/treatment/act/finally already save ~977, ~1008, ~1041, ~1139).
9. [C] Candidate re-judge still marks the original row. Repair keeps `text=original` (`~1808`). Re-judge uses `text=candidate` but `lines_around` from `_lines_around(rows, index)` which marks `>>>` on the live ledger text (`~282-308`, ~1831). After a candidate that is not yet committed, `>>>` is still original. Plan C requires candidate in the marked neighbor and original only in repair context. Smallest fix: rebuild `lines_around` with the candidate in the `>>>` slot for reread only; keep repair prompt on original; do not feed `best_text` from a scope-invalid candidate (current `improved` path ~1882).
10. [C] Conserved-row contract still mutates and caps. `_RepairedLine.text` is `max_length=2000` (`nodes/_otr_ledger_clean.py` ~1421); `_call_repair` returns `" ".join(...split())` (~1478). Whole-row and speaker-fix schemas share the 2000 cap (~845). Smallest fix: drop output max_length on conserved rows; return model text unchanged after nonempty check; splice only authorized span replacements.
11. [E + dispatcher] Visual cache/receipt identity is still final `prompt_hash` only. `request_cache_key` is role/object/prompt_hash/seed/engine/kind/w/h/`anchor` (`nodes/otr_image_gen_dispatcher.py` ~135). `_NormalizedPrompt` has `pre_banana_hash` + `prompt_hash`, no `base_prompt_hash` (~954). MetaBrief objects stamp `prompt_hash` after the call (`otr_meta_brief_image_prompt.py` ~2070) and return `image_prompts_json`; `generate()` reads `script_json` only (~2494). Smallest fix: persist source/context receipt on each prompt object in that JSON; dispatcher fresh AND cache-hit compare it before labeling accepted; append `base_prompt_hash` at the END of `_NormalizedPrompt`; include source/context in `request_cache_key` the same optional way as `anchor`. Do not load a model just to stamp PASS.

SHOULD-FIX:
1. [A] Bind only in My Story `_pass_treatment`, not `_call` and not SciFi `_pass_treatment` (`nodes/_otr_scifi_news_pro.py` ~1952 uses `Treatment`, never `_otr_bind_schema`). `invoke_structured_slot` never calls `_otr_bind_schema` (`nodes/_otr_structured_call.py` ~596). Binding inside `_call` would bind P2/P3 and violate "P2/P3 original callable stays".
2. [A] `get_cached_transformers_schema_constraint` still fills `by_schema` (`nodes/_otr_constrained_generate.py` ~123-141); `make_constrained_generate_fn` captures parser at factory time (~237) for slot-drama (`OTR_LedgerScriptWriter.py` ~4662). Writer `_build_truncating_generate_fn` also captures at wrapper build (~842). Drop `by_schema`; build parser+prefix inside both inner `generate_fn`s; `verify: parser.config.max_json_array_length` on installed LMFE after `build_transformers_prefix_allowed_tokens_fn`. Update `tests/test_constrained_generate.py` (~160) which asserts `by_schema` length 2.
3. [B] `_read_advertised_context` reads only top-level int fields (`nodes/_otr_model_catalog.py` ~701); `bool` is an `int` subclass so `True` would parse as 1. Native parser must reject bool and require `type(val) is int` and `val > 0`, nested `text_config` first.
4. [B] `check_context_window` vs `HARD_VRAM_CONTEXT_LIMIT` (`nodes/_otr_loader_backends.py` ~79; `nodes/_otr_model_runtime.py` ~71, ~132) and `tests/test_context_window_precondition.py` currently require raise below 8192. Removing the rejection inverts those tests in the same B chunk. `MIN_OUTPUT_TOKENS=64` (`nodes/_otr_generation_budget.py` ~12) -> 1; OpenRouter/Comfy keep their own 1024 floors (`nodes/_otr_openrouter_backend.py` ~224; `nodes/_otr_comfy_backend.py` ~139). Do not change those env floors.
5. [C] Whole-row `structured_call(max_attempts=2)` is extra calls beside `_MAX_ATTEMPTS=2` (`nodes/_otr_ledger_clean.py` ~148). Count them on `receipt["model_calls"]`. Thread review context into `run_ledger_clean` (today: `led.data`, `creative_generate_fn`, `bank_id` only -- `_otr_writer_tail.py` ~1389). Writer tail has `technical_generate_fn`; MetaBrief does not -- ledger `source_meta.story_input.fields` is the hop.
6. [D] Name the organizer helper for `helper_context` or Sprint B credits will bucket it as `<unattributed>` (`OTR_LedgerScriptWriter.py` ~641).
7. [F] Recovery after Mac logs: F says start 5080 one-act then 4060. Keep Mac instrumentation in F after blockers, but do not treat S2 component green as that proof (document already says this -- keep it in the chunk gate).
8. [B] `load_llm` still mutates `model_config.max_position_embeddings` (`nodes/_otr_model_loader.py` ~1227). Native text load deep-copies `text_config` (`~653`) so parent vs copy can diverge if mutation stays. Remove mutation in the same B chunk as pin/cache.

OPTIONAL / NICE-TO-HAVE:
- Explicit `coordinate_version` string on every source receipt.
- Label banana-transformed prompts `base-only` vs `final-unreviewed` in the dispatcher report string.
- Halt log line that names `open_string` vs `cycle` without changing the exception type.

CUT THESE:
1. JSONPath / substring-fallback-to-another-field -- R2 already rejected; `candidate_path: list[str|int]` + dict/list walk is enough.
2. New node/socket/widget for fit inspection -- scheduler optional capability is the contract; a widget would desync canonical.
3. Predictive Mac memory gate / `model.to(meta)` / extra unload -- F forbids it; S2 already shipped.
4. Recursive author validator wrapping the reviewer -- D already says no; keep it cut.
5. Global story/pass limit as a substitute for split/merge progress -- replace with the work-item cap in must-fix 7, not a taste/length gate.
6. Compulsory summary of inputs that already fit -- D says P0 can organize directly; extra summary pass is a second writer with no new contract.

[ASSUMPTION] LMFE `JsonSchemaParser.config.max_json_array_length` is settable on the live parser after prefix build -- verify on the installed wheel before A lands.
[ASSUMPTION] "Native transport owner" for the CPU prepare helper is `_build_truncating_generate_fn` plus `make_constrained_generate_fn` plus `make_generate_fn`; if only one is factored, probe and generate will diverge.
UNTRACED: exact MetaBrief scene-companion helper vs shot-lock creative path -- `otr_meta_brief_image_prompt.py` generate() has no review-context argument today; persistence must be ledger JSON, not a Python closure.
