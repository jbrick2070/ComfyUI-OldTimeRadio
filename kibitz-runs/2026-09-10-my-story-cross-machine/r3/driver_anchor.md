# R3 driver anchor, before fanout

R2 both readers completed. Grounded inner-closure lifetime, marker preservation,
raw_completion versus raw_output receipt consumer, missing review-context plumbing,
explicit cleaner span schema, pre-reseal abort API and dispatcher normalization.
Root rejected slot rerouting, finite schema caps, JSONPath dependency and
quote-presence-as-proof. S2 alone is coded and qualified, pushed59a40131; Bible
77bd6012. Core architecture still waits for convergence.

New user steering is adaptive first-pass briefing. Read-only code grounding found
reusable source_document spans/coverage but no ready exact prompt-fit API. Existing
select_grounding samples passages and clean summaries truncate, so neither proves
all-source reading. A2 parser misses nestedtext_config; loader mutates config to
estimate and establishes HFroot after admission scan; normalized native pin must
join BOTH lookup/publication keys without changingGGUF. vram_priced_ctx differs
from native advertisedwindow and remains policy-owned. Exact probe must share
generation preparation and return only primitives, no model references.

R3 high-risk questions: measured source-window split/merge progress without
arbitrary story cap; candidate/source cross-window uncertainty; partial repair
splicing and whole-row authority; restored receipt lifetime after transaction;
visual actual versus base bytes and cached source identity. Do not invent a
standing entity/family ontology or silently change operator model choices.
Require executable wiring and tests, then R4 convergence. No live model/media
runs while defects remain open. Work queue is GO_FORWARD, not this review folder.
