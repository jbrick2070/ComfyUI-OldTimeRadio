# kibitz r2 judgment -- lyric-stills (panel: codex + claude)

ACCEPTED (grounded):
- REGISTRY IDENTITY: v1 rebuilt as an IMAGE engine (`cloud_ideogram_card`) + existing
  still_pan/still_flat for duration; still-minting video engine cut (claude MUST-1 + codex
  MUST-1/CUT-1 convergent; flux_still confirmed legacy alias, NO_TEXT_CLAUSE + guarded-import
  pattern verified by direct read).
- Guarded import + CAPABILITIES + adapter in the SAME change (codex MUST-2).
- New `kind=lyric_card` prompt path; lyric_text bypasses NO_TEXT_CLAUSE; title_mood keeps it
  (codex MUST-3/4). Episode-title ledger key = verify-at-build.
- canonicalize_image placement decision: inside the image adapter pre-return; session arg fit =
  verify-at-S1 (claude MUST-2, codex MUST-5).
- Checked-in speed->price map for estimated_usd; TURBO default + env override (claude SHOULD-1,
  codex MUST-7, codex SHOULD-3).
- Cache must be wired explicitly by the adapter (nothing in nodes/ calls it yet) (codex MUST-8).
- Excerpt helper = pure function + edge-case tests (codex SHOULD-1, claude r1 CUT-2).
- wan_i2v request-shape mismatch (top-level init_image vs asset_refs.init_image) + no-prompt
  forwarding (claude MUST-4, codex MUST-6): vid variant PARKED with blockers; explicit
  NEEDS-DECISION for the operator (claude CUT-1 / codex CUT-2 softened -- operator wants it,
  so parked-not-cut).
- Provenance palette/layout fields optional (codex SHOULD-2); OCR dep not added (codex OPTIONAL).
- My anchor SHOULD (long music beds / static card): noted as optional multi-variant later.

REJECTED:
- None outright; claude r2 CUT-2 (two classes vs card_mode) left as implementor's call.

Scope ruling: user asked r1-r2 only (small plan); r3/r4 skipped by explicit instruction.
Doc marked CODE-READY (v1, S1-gated) with one NEEDS-DECISION (vid variant).
