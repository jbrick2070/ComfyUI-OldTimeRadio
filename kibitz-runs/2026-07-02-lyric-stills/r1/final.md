# kibitz r1 judgment -- lyric-stills (panel: codex + claude; antigravity STALLED 12min/0 bytes, killed)

Both agents reviewed the PRE-clarification snapshot (input.md); operator rulings landed mid-round
(lyrics = char/announcer lines; cloud-only; music = wordless title-mood card from procgen title;
old-radio + Meta-brief composition). Claims graded against the CURRENT doc.

ACCEPTED (grounded):
- canonicalize_image is a stub (_not_built_yet, cloud_media_canonical.py:106-109) -- CONFIRMED by
  direct read. S1 promoted to hard-prerequisite section + stub cited in the doc (claude MUST-1).
- Still->clip lifecycle unspecified -- ACCEPTED; new "Still-to-clip lifecycle" section names the
  3-stage chain, _CheapFamilyBase shape, estimated_usd wiring, V-6 confirmation step
  (claude MUST-2, codex MUST-2, deepseek MUST-3 convergent).
- Resolution pad ownership = canonicalize_image per S1 contract; adapter only quantizes to COMBO
  preset (claude MUST-3). Preset list = verify-at-build.
- Profile tail EXTENDS the existing ideogram entry, no duplicated 8-word rule (claude SHOULD-2).
- estimated_usd from rendering_speed, not hardcoded (claude SHOULD-3).
- "strongest fragment" cut -> first-clause/8-word deterministic rule (claude CUT-2 + panel).
- Image registry has zero cloud rows (registry.py:107-139) -- CONFIRMED; S1 prerequisite
  (codex MUST-3).
- Cache claim narrowed; workflow-JSON claim softened to verify-selector (codex SHOULD-2/3).

OBE (operator rulings superseded):
- codex MUST-1 / claude SHOULD-1 (music text source open): music = procgen title, wordless
  title-mood card; zero writer changes.
- claude CUT-1 / codex CUT-1 (cut character_video): REJECTED by operator intent -- character
  beat lines ARE the lyrics. Continuity risk noted as an eyeball item instead.
- codex CUT-2 (audio-reactive pulse) -- already deferred; kenburns-only v1 stands.

VERIFY-AT-BUILD: Ideogram resolution presets; selector auto-exposure (V-6); OCR/legibility smoke;
negative-text compliance on title_mood cards.

Result folded into docs/GO_FORWARD_NEXT/2026-07-02-ideogram-lyric-stills.md (single source).
