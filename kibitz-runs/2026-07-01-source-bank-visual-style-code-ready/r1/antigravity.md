VERDICT: no. The plan has missing node registrations, widget migration inefficiencies, and potential runtime crashes in compatibility fields.

MUST-FIX BEFORE BUILD:
1. [C4/__init__.py] Node Registration Omission: The plan describes creating `OTR_VisualStyleDirector` in `nodes/otr_visual_style_director.py` (C4), but fails to add it to the `_NODE_MODULES` dictionary in the package's root [__init__.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/__init__.py). Without this registration, ComfyUI will never import or register the node class, rendering it a dead/unloaded node at startup. [ASSUMPTION: We assume the package's root `__init__.py` is the sole entry point for ComfyUI custom node loading, as shown in the codebase.]
   - Fix: Specify that `OTR_VisualStyleDirector` must be registered in the `_NODE_MODULES` table of [__init__.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/__init__.py): `"OTR_VisualStyleDirector": (".nodes.otr_visual_style_director", "OTR_VisualStyleDirector", " Visual Style Director")`.
2. [C4] OTR_VisualStyleDirector Missing Input Specifications: The plan defines the outputs (`RETURN_TYPES`, `RETURN_NAMES`, `FUNCTION`) for the new `OTR_VisualStyleDirector` node, but completely omits its inputs and widgets. ComfyUI nodes require an `INPUT_TYPES` classmethod to expose widgets in the UI.
   - Fix: Define `INPUT_TYPES` for `OTR_VisualStyleDirector` with a required combo widget listing the available visual styles (`["sci_fi_radio", "media_archive", "cinematic_35mm", "noir", "anime", "cartoon", "paper_origami"]`) defaulting to the default preservation style.
3. [C5] Incomplete meta.news Compatibility Mapping: In C5, the compatibility helper maps the `StoryInputPacket` to the legacy-shaped `meta.news`. However, `StoryInputPacket` uses `close_brief` whereas downstream nodes (like [OTR_LedgerScriptWriter.py:L4184](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/OTR_LedgerScriptWriter.py#L4184)) look up `news_close_brief`. If the helper does not explicitly map `close_brief` to `news_close_brief` and ensure all required fields (`key_terms`, `source_hash`, `script_brief`) are matched, downstream nodes will crash or fail silently.
   - Fix: Define the exact mapping rules for the compatibility helper: it must map `close_brief` to `news_close_brief` and correctly transfer `key_terms`, `source_title` to `headline` (if empty), and standard metadata fields so the resulting `meta.news` dictionary has complete parity with the legacy `NewsBriefs` schema.
4. [C1/C7] Double Widget Schema Migration: The plan introduces the `source_bank` widget in C1, but delays the `source_text_path` widget until C7. In ComfyUI, changing widget counts shifts positional binding of saved values in existing graphs. Doing this in two separate chunks forces two distinct workflow/test migrations.
   - Fix: Append both `source_bank` and `source_text_path` to `OTR_LedgerScriptWriter.INPUT_TYPES` in C1. The `source_text_path` widget can default to `""` and remain ignored until the `public_domain_story` implementation in C7 is completed.
5. [C3] External Network Dependency and Lack of Offline Fallback: In C3, `_resolve_inputs` is designed to fetch raw archive items directly from external feeds (LOC/NFPF/ACE-style). This introduces a runtime dependency on external internet connectivity during graph execution. If the user is offline or the feeds are down, the node will fail closed because the "Empty `script_brief` fails closed" rule is enforced. [ASSUMPTION: Users often run ComfyUI offline or in restricted network environments where raw external feeds are unreachable.]
   - Fix: Introduce an offline fallback option. Allow the user to specify a local JSON index or path to a directory of cached archive fixture items via a configuration file or a dedicated widget, similar to how `custom_premise` bypasses the RSS network fetch.

SHOULD-FIX:
1. [C2] Story Quality v2 Bypass for Non-Science Banks: Under C2, the plan states that non-science banks may bypass the pitch-room, story-select, and refine-grading modules for V1. Bypassing these modules completely removes the story quality guardrails, meaning that `media_archive` and `public_domain_story` will generate unsuited outlines prone to climax collapse and other structural defects.
   - Fix: Do not bypass these quality gates. Instead, parameterize/generalize the outline-critic prompts (in [_otr_outline.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_outline.py)) and pitch-room prompts so that the hardcoded "science-fiction" and "real science" requirements are replaced with the active source bank's domain vocabulary.
2. [C0] Pydantic extra="forbid" vs Durability: The plan specifies `ConfigDict(extra="forbid")` for `StoryInputPacket` and `VisualStylePolicy` under C0. For data models that represent saved states or are serialized/deserialized within the ledger, strict validation with `"forbid"` can cause fatal crashes if extra fields are added in future updates or by external nodes.
   - Fix: Change `ConfigDict(extra="forbid")` to `ConfigDict(extra="ignore")` for these models to ensure forward/backward compatibility when loading older or newer saved ledger JSONs.
3. [C5] Ambiguous base_tail_strategy = "suppress" behavior: The plan lists `base_tail_strategy: Literal["keep", "replace", "suppress"]` but does not clarify the exact behavior of `"suppress"`. Specifically, it is unclear if `"suppress"` means omitting only the base tail `STYLE_TAIL_DEFAULT` while still appending `positive_tail`, or suppressing all style tails entirely (which would make it behave similarly to `"replace"` with an empty string).
   - Fix: Clarify in the contract definition that `"suppress"` disables the addition of both the default base tail (`STYLE_TAIL_DEFAULT`) and the policy's `positive_tail` entirely, providing a completely clean prompt path.
4. [C5] Naive Forbidden Term Scrubbing: The plan specifies that "forbidden term scrub happens before final prompt output" but does not detail how the scrub is performed. A simple string replacement (`prompt.replace(term, "")`) can cause word fragment corruption (e.g. scrubbing "art" from "earth" or leaving double/dangling commas like `cinematic, , look`).
   - Fix: Specify that the scrubbing utility must use case-insensitive regular expressions with word boundaries (`\b`) and a clean-up pass to normalize double/trailing commas and whitespace after scrubbing.

OPTIONAL / NICE-TO-HAVE:
- [C3/C0] Standardized Telemetry / Trace fields: Add a field to `StoryInputPacket` to track the specific LLM model ID that performed the archive extraction, ensuring that the forensic logs stamped in `meta` can identify model-specific parse errors.

CUT THESE (scope / over-engineering):
1. [C4/C0] Avoid Inline Hardcoded Style Definitions:
   - Why: The plan lists the initial style IDs (`sci_fi_radio`, `media_archive`, `cinematic_35mm`, `noir`, `anime`, `cartoon`, `paper_origami`) but does not mention where their policy properties (tails, forbidden terms) are defined.
   - Cut: Cut any plan to hardcode these policies inline inside the `OTR_VisualStyleDirector` node. Instead, they should be defined in a new separate catalog module `nodes/_otr_visual_style_catalog.py` (analogous to the story style catalog `_otr_style_catalog.py`) to keep the node class clean and maintainable.
