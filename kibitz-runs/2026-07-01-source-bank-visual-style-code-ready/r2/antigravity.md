VERDICT: yes-with-fixes. The architecture is sound but has critical widget positioning and API propagation issues that will cause graph validation failures.

MUST-FIX BEFORE BUILD:
1. [Section 8 & 3] Positional Widget Index Shift: Section 8 states "Writer optional count changes from 16 to 17 for `source_bank`". Since the actual code at [nodes/OTR_LedgerScriptWriter.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/OTR_LedgerScriptWriter.py#L1800-L2222) has 22 optional widgets (total 25 widgets, verified by [tests/test_workflow_json_guardrails.py:L673](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/tests/test_workflow_json_guardrails.py#L673)), inserting `source_bank` at index 19 (making optional count go from 16 to 17) will shift all subsequent optional widgets (`openrouter_*`, `comfy_*`, `refine_*`, `story_scaffold`). This shifts the positional indices in the saved `widgets_values` array of existing workflows, causing a silent drift of saved values and violating the ComfyUI widget contract (BUG-LOCAL-097 / CLAUDE.md).
   Concrete Fix: Append the `source_bank` widget to the very end of the optional dictionary in `INPUT_TYPES()` (making it optional index 23, array position 25, and total widgets 26).
2. [Section 7 & 4] visual_style_policy_json Signature Conflict: Section 7 states `visual_style_policy_json` must be added as `forceInput: True` on MetaBrief and ShotLock `INPUT_TYPES` and signatures. Section 4 states `finish_visual_prompt` applies the policy. However, since MetaBrief runs BEFORE ShotLock, the ledger `meta` object passed to `finish_visual_prompt` does not yet contain the stamped policy. Under the current signatures, `finish_visual_prompt` cannot access the visual style policy during MetaBrief execution unless it is explicitly passed to it or temporarily stamped on `meta` at the node entry points.
   Concrete Fix: Change `finish_visual_prompt` in [nodes/_otr_story_brief_helpers.py:L458](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_story_brief_helpers.py#L458) to accept `visual_style_policy_json: str = "{}"` as a keyword argument (defaulting to avoid breaking other un-updated calls), and inside the function, resolve style directives by reading either from `meta["visual_style_policy"]` (stamped by ShotLock) or parsing `visual_style_policy_json` (passed by MetaBrief).
3. [Section 8] Whitelist Parity Failures: Section 8 states "`source_bank` goes into both headless creative whitelists." If `"source_bank"` is not added to both whitelists in the same commit, the parity check in `tests/test_workflow_apply.py` (specifically `test_creative_whitelist_parity_package_vs_script`) will fail.
   Concrete Fix: Add `"source_bank"` to both `CREATIVE_WHITELIST` sets in [nodes/_otr_workflow_apply.py:L491](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_workflow_apply.py#L491) and [scripts/otr_api.py:L753](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/scripts/otr_api.py#L753) in the same change chunk.

SHOULD-FIX:
1. [Section 1 & 2] Undefined LedgerWritingSpec Schema: The plan relies on `LedgerWritingSpec` as the control-plane data structure but does not define its schema, attributes, or type constraints, leaving the data shapes of the translator input ambiguous.
   Concrete Fix: Define `LedgerWritingSpec` explicitly (e.g. as a Pydantic `BaseModel` or `dataclass` in a new module `nodes/_otr_ledger_writing_spec.py` with fields: `source_intent: str`, `prompt_profile: str`, `source_packet: dict`, `adaptation_mode: str`, `visual_bible: dict`, `validation_expectations: dict`).
2. [Section 5] Undefined Visual Style Catalog Dropdown Options: Section 5 states that `OTR_VisualStyleDirector` has a style combo widget, but does not define the visual style choices or the catalog containing them, leaving the dropdown options undefined.
   Concrete Fix: Define the visual style catalog in `nodes/_otr_visual_style_catalog.py` (e.g., [ASSUMPTION] `VISUAL_STYLE_CATALOG = ["retro_sci_fi", "cyberpunk", "noir", "classic_radio_drama", "let_the_story_decide"]`), and have `OTR_VisualStyleDirector.INPUT_TYPES` combo use this list.
3. [Section 120] Under-defined base_tail_strategy strategy: Section 120 refers to `base_tail_strategy="suppress"` but does not define where this strategy is stored or how it behaves, leaving the control flow for default tail suppression versus custom directive override ambiguous.
   Concrete Fix: Define `base_tail_strategy` as a field on `LedgerWritingSpec` or the visual style policy dictionary, defaulting to `"pass_through"`, and check it inside `finish_visual_prompt` to determine whether the default `STYLE_TAIL_DEFAULT` is appended or bypassed.

OPTIONAL / NICE-TO-HAVE:
- Add a helper method `visual_style_policy_json_to_dict` to [nodes/_otr_story_brief_helpers.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_story_brief_helpers.py) to centralize the parsing of the policy JSON string with safe error fallbacks to avoid repetitive `json.loads` try-except blocks across MetaBrief and ShotLock.

CUT THESE (over-engineering):
- None. The plan is highly focused on a clean translation architecture and avoiding invasive legacy mutations.
