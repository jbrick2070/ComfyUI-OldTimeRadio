VERDICT: no. The arc says “rip ALL hard-baked VRAM tier classification,” but it does not yet define the replacement shape for profiles, env ceiling export, non-video registries, or widget migration.

MUST-FIX BEFORE BUILD:
1. [RIP / Questions 1,4] The scope is too narrow around video CAPABILITIES. The same tier fields drive audio and image profile availability: `nodes/_otr_audio_engines/registry.py:186`, `nodes/_otr_image_engines/registry.py:108`, and `nodes/_otr_shared/capability_profiles.py:242-257`. Concrete fix: make the rip explicitly cross-registry: audio, image, and video declarations all lose `vram_class` / `vram_estimate_mb`, while non-VRAM capability fields stay.
2. [RIP / KEEP / Questions 3,5] The document says the `vram_ceiling_gb` widget is removed and the ceiling falls back to env/default, but current `OTR_WorkflowValidator` exports `OTR_VRAM_CEILING_MB` from profile `vram_budget_mb` every execution (`nodes/_otr_workflow_validator.py:317-334`). That means the profile still hard-bakes the ceiling after the widget rip. Concrete fix: decide explicitly whether profile `vram_budget_mb` is RIP, KEEP-as-runtime-seatbelt, or renamed to a non-tier runtime ceiling. Do not leave this implicit.
3. [Questions 4] “Profiles still work as plain per-role engine pick maps” is not true until the schema changes. Current profile shape requires `vram_budget_mb` and `max_model_class` (`nodes/_otr_shared/capability_profiles.py:75-78`), and availability rejects by both class and estimate (`nodes/_otr_shared/capability_profiles.py:294-297`). Concrete fix: define the new profile schema before build: remove `max_model_class`, remove or rename `vram_budget_mb`, keep `toolchains`, `allow_sidecars`, `cpu_ok`, and registry existence validation.
4. [Questions 3 / Invariants] Widget removal is under-specified. Node 62 in `workflows/otr_scifi_16gb_full.json` has `vram_ceiling_gb` as the third widget-backed input and `widgets_values` contains `14` in the third slot; code defines the widget at `nodes/OTR_LedgerFreezeCascade.py:214-228` and the run arg at `nodes/OTR_LedgerFreezeCascade.py:302`. Concrete fix: state the exact migration: remove the INPUT_TYPES widget, remove the run kwarg and orchestrator kwarg, and delete only the third `widgets_values` element so `render_selection`, `render_max_n`, `protagonist_only`, and `manual_line_ids` keep their intended saved values.
5. [RIP / Questions 1,5] `_otr_model_catalog.py` is a hidden tier surface, not just “maybe classify.” It has `vram_fit_tier` in the model schema (`nodes/_otr_model_catalog.py:79`), dropdown/user messaging semantics around PASS/WARN/FAIL (`nodes/_otr_model_catalog.py:9-16`), and a separate `DEFAULT_VRAM_CEILING_GB` hit in search results at `nodes/_otr_model_catalog.py:1136`. Concrete fix: add the LLM catalog to the RIP/KEEP decision tree, or explicitly exclude it as LLM-selection policy rather than motion-engine VRAM tiering.

SHOULD-FIX:
1. [RIP / Questions 2] The plan treats static estimates as redundant because the runtime ceiling guards OOM, but current comments identify some “estimate” rows as measured or load-bearing context, e.g. Wan’s `free_after_use` mitigation around `nodes/_otr_video_engines/registry.py:316-322`. Concrete fix: split “remove policy gate/label” from “preserve measured engineering notes/tests.” Do not delete evidence needed to maintain runtime safety.
2. [KEEP / Questions 5] The seatbelt is not one subsystem. Runtime guard is `motion_common.dynamic_vram_ceiling_mb()` and `assert_vram_within_ceiling()` (`nodes/_otr_video_engines/motion_common.py:43-55`, `nodes/_otr_video_engines/motion_common.py:219-239`); reclaim is `wrapper_bridge.reclaim_idle_models()` (`nodes/_otr_video_engines/wrapper_bridge.py:248-301`); graph intermediate release is `free_after_use` (`nodes/_otr_video_engines/wrapper_bridge.py:304-322`). Concrete fix: list these as separate KEEP acceptance checks so a tier rip cannot accidentally remove reclaim or `free_after_use`.
3. [RIP / Questions 1] `_vram_log.py` is not tier classification, but it has a hardcoded 14.5 GB ceiling and warning text (`nodes/_vram_log.py:21-26`, `nodes/_vram_log.py:45`, `nodes/_vram_log.py:108-112`). Concrete fix: convert it to the runtime ceiling source or explicitly mark it KEEP-as-telemetry with no policy authority.
4. [RIP] `eng_humo.safe_render_frames` should be KEEP unless disproven. It is documented as a measured 14B safety cap and exact-fit behavior, not a profile tier filter (`nodes/_otr_video_engines/eng_humo.py:52-60`, `nodes/_otr_video_engines/eng_humo.py:351-378`, `nodes/_otr_video_engines/eng_humo.py:588`). Concrete fix: mark HuMo frame caps as measured runtime safety, not tier classification.

OPTIONAL / NICE-TO-HAVE:
- Add a short glossary: “tier classification” = static enable/dropdown policy; “seatbelt” = runtime ceiling/reclaim/live measurement. The current doc uses “VRAM” broadly enough that build readers can cut the wrong code.

CUT THESE (scope / over-engineering):
1. [Questions 1] Cut historical docs from the first rip pass. Search results show many old roundtable and plan references, but they are not executable unless tests assert them. It is safe to leave archival docs until after code/tests/workflow are green.
2. [Questions 6] Cut any attempt to preserve backwards compatibility shims for old tier fields. The repo already has the no-fallback/no-shim invariant, and the current code’s profile validator is fail-closed (`nodes/_otr_shared/capability_profiles.py:105-121`), so a clean schema break with updated checked-in profiles is safer.