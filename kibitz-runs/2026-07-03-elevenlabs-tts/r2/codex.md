VERDICT: no. The plan names the right surfaces, but several interfaces do not exist in the shape described and the cloud identity data will be dropped before render.

MUST-FIX BEFORE BUILD:
1. [C1/S1/S5] `runtime: cloud` cannot work by “engine resolver routes to invoke_partner_node” as written. `EngineProfile` currently rejects `cloud` (`nodes/_otr_engine_profiles.py:35`, `:87`, `:95`) and forbids undeclared fields (`:67`); `resolve_casting_plan()` only returns a profile (`:189`, `:213`). The actual voice/music dispatch ignores `profile.runtime` and calls `adapter.generate_voice()` / `adapter.generate_clip()` (`nodes/_otr_voice_node_common.py:354`, `:559`; `nodes/stable_audio_theme.py:203`, `:224`). Concrete fix: either add registered cloud audio adapters (`elevenlabs`, `cloud_sonilo_music`) whose `generate_voice`/`generate_clip` call `invoke_partner_node`, or add an explicit `profile.runtime == "cloud"` branch in the dispatchers. Also add `"cloud"` and the new profile fields to `EngineProfile`.

2. [C1/C5/S0] The example `partner_row: cloud_elevenlabs` is not a real row. The pinned rows are `cloud_elevenlabs_flash`, `cloud_elevenlabs_tts`, and `cloud_elevenlabs_voice_selector` (`nodes/_otr_shared/partner_nodes.yaml:8`, `:41`, `:74`). Fix the profile contract to require one of the actual row keys, or add a real alias row and test `_row_for()` resolves it. Do not ship a profile pointing at `cloud_elevenlabs`; `invoke_partner_node()` rejects unknown node keys via the pin table (`nodes/_otr_shared/cloud_media_invoke.py:603`, `:620`).

3. [C2/S2/S3] `provider_voice_id` will be silently lost. The schema allows extra properties (`config/voice_bank_entry_schema.json:7`), but `VoiceBankEntry` has no `provider_voice_id` field (`nodes/_otr_voice_bank.py:77`, `:86`, `:87`), `_entry_from_dict()` only copies known fields (`:158`, `:166`, `:167`), and CastLock `_stamp()` persists only `voice_ref_id`, `voice_engine`, and `commercial_clean` (`nodes/cast_lock.py:650`, `:652`, `:654`). Concrete fix: add `provider_voice_id` to the schema properties, dataclass, loader, CastLock stamp, durable cast stamp, and admission gate checks.

4. [C2/S0] “Relax minLength via a cloud-entry branch” is not implementable with the current validator. `_validate_entry()` only checks top-level `required`, property `type`, `minLength`, `minItems`, and array item type (`nodes/_otr_voice_bank.py:121`); it does not implement `oneOf`/`if`/`then` JSON Schema conditionals. Also `_entry_from_dict()` still indexes `d["ref_path"]` and `d["ref_sha256"]` unconditionally (`nodes/_otr_voice_bank.py:166`, `:167`), while those keys are required today (`config/voice_bank_entry_schema.json:8`, `:15`, `:16`, `:51`, `:56`). Concrete fix: either keep non-empty sentinel values and skip disk/sha validation in the cloud admission path, or extend the custom validator/loader to support an explicit `engine == "elevenlabs"` branch.

5. [C4/S3] The fail-loud cloud gate conflicts with the currently unconditional fail-soft repair. CastLock always calls `_resolve_character_voices_fail_soft()` before returning (`nodes/cast_lock.py:187`), and that routine can leave unresolved lines “for the node-81 engine fallback” (`nodes/cast_lock.py:386`, `:510`). Concrete fix: add a cloud-mode admission gate after CastLock that runs after the repair and raises if any selected cloud cast/line lacks `provider_voice_id`; or make the repair cloud-aware and raise instead of appending the fallback note when the selected profile runtime is `cloud`.

6. [C4/S1/S5] The budget/auth path is underspecified for audio. `invoke_partner_node()` defaults `estimated_usd=0.0` and reserves exactly the passed estimate (`nodes/_otr_shared/cloud_media_invoke.py:603`, `:605`, `:623`); if the new adapters do not pass per-line/per-cue estimates, the $10 cap is inert despite the backend cap existing (`nodes/_otr_shared/cloud_media_backend.py:110`, `:287`, `:292`). Also `invoke_partner_node()` creates the session with `get_or_create_session(current_prompt_id())` and no hidden credentials (`nodes/_otr_shared/cloud_media_invoke.py:620`), while hidden auth is only injected from `session.auth` later (`:363`, `:368`, `:379`). Concrete fix: define cost estimators for ElevenLabs chars and Sonilo duration and pass nonzero estimates; decide whether cloud audio requires `OTR_COMFY_API_KEY` or add a real way for these adapters to receive Comfy hidden auth.

7. [C3/S1] The `ELEVENLABS_VOICE` resolver is not build-specified. The TTS row requires `voice: ELEVENLABS_VOICE` (`nodes/_otr_shared/partner_nodes.yaml:32`, `:65`), while the selector row takes a `COMBO` named `voice` and outputs `ELEVENLABS_VOICE` (`:74`, `:85`). The plan says “preferred = construct payload directly” but gives no payload shape. Concrete fix: make S0/S1 include a live-object capture test for `ElevenLabsVoiceSelector.EXECUTE_NORMALIZED` and codify either `selector(provider_voice_id)` or a typed payload constructor. verify: whether the selector accepts immutable voice IDs or only display labels.

SHOULD-FIX:
1. [C5/S0] Existing conformance tests currently exclude exactly the audio rows this plan wants to implement. `KNOWN_UNADAPTERED` xfails `cloud_elevenlabs_tts`, `cloud_elevenlabs_flash`, and `cloud_sonilo_music` (`tests/test_cloud_partner_conformance.py:28`, `:31`, `:32`, `:33`), and `_engine_by_node_key()` only scans image/video registries (`:24`, `:25`, `:53`). Fix the test to include the audio registry/adapters and remove those xfails in the same sprint.

2. [S1/S5] `canonicalize_audio()` is still a stub and the loudness source is unresolved (`nodes/_otr_shared/cloud_media_canonical.py:68`, `:120`, `:127`, `:131`). Fix before any adapter returns audio to the assembler; otherwise the first real cloud audio render hits `NotImplementedError`.

3. [S5] `StableAudioTheme`’s local fallback tuple omits `stable_audio_3` (`nodes/stable_audio_theme.py:36`) even though the audio package imports the `stable_audio_3` engine (`nodes/_otr_audio_engines/__init__.py:35`) and registry capabilities know it (`nodes/_otr_audio_engines/registry.py:205`). `build_engine_combo()` usually pulls from `legacy_first_engines()` (`nodes/_otr_voice_node_common.py:162`, `:171`, `:173`), but the fallback is still stale for broken-profile/input-type paths. Include `stable_audio_3` and the new cloud music engine in the hard fallback once registered.

OPTIONAL / NICE-TO-HAVE:
- [C7/S7] Add a ledger-level request hash test for text/prompt hash, resolved model, provider voice id, seed, duration, and partner row. This is the practical determinism contract; do not assert provider byte identity.

CUT THESE (over-engineering):
1. [C2] Cut the “cloud-entry branch” in JSON Schema unless you also replace the custom validator. A non-empty sentinel plus explicit cloud skip logic is smaller and fits the current loader.
2. [Model decisions/C5] Cut the separate `cloud_elevenlabs_flash` engine/profile if tier really rides the `model` DYNAMICCOMBO. Keep one ElevenLabs audio adapter/profile and choose the resolved model string from pinned combo defaults.