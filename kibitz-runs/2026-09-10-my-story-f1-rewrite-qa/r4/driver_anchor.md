# Root grounded anchor

VERDICT: READY for independent QA; no known unresolved component blocker.

CONFIRMED by real Windows file reads and 233 passing focused regressions:
source correction is outside the author ladder; one shared structured_call
with max_attempts=2 owns all source retries. Reentry by pass id consumes no more
calls. Existing author structural validator checks the exact model returned to
the runner. Wrong full artifacts remain within the source budget and cannot
replace usable accepted input. Real provider/OOM/cancellation preserves type.

CONFIRMED: whole-source context uses raw fields at every author boundary. Source
rewrites are actual artifact/ledger corrections, never a check-only verdict.
Spoken edits preserve immutable original intervals and protected facts.
The existing closed ledger_clean/ledger_cleanup transaction stages own them.

Independent Einstein review found the failure-history guard originally stopped
before the second cleanup helper. Root widened it through reconciliation and
retained-hash stamping, with real-Ledger reload tests for later failure/cancel.
A rollback test exposed a projection mismatch: music-role rows were mistakenly
included as spoken text. Root aligned source/delivery projection with character
and announcer rows. All 233 tests now pass.

UNVERIFIABLE here: semantic accuracy of a real model and large-story capacity;
no publication or matrix promotion. Adaptive organization/F2 remain in
GO_FORWARD and will receive their own wiring/qualification. No hidden claim
that this component completes the entire campaign.

