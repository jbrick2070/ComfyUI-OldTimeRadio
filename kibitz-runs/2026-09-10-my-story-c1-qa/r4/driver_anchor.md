# Codex grounded C1 anchor

VERDICT: finished diff ready for independent QA; full/Bible checks pending.

CONFIRMED: active run_ledger_clean caller passes actual judge complaints separately
from display hints. Authorization independently gathers every admissible raw
pattern match. Exact intervals retain occurrence identity through sentence mode;
voting compares grounded occurrences. Collective coverage, including whitespace
gaps, invokes whole-row authorization. A whole-row accusation localized by that
model starts a new narrower scope rather than retaining its old collective scope.

CONFIRMED: _call_repair builds partial candidates by splicing validated model
replacements into original slices. _repair_row reconstructs again before reread,
clean acceptance or best-progress retention. Schema classes are module-level.
Real runtime errors propagate; malformed structured answers retain original.
Receipt version is ledger_clean_v3 with original/candidate hashes, scope and
authorization attempts. Protected deterministic fact rows still skip before
judging. The negative control uses a valid scoped replacement for its flagged fact.

CONFIRMED: internal read-only reviewer found three implementation gaps, all fixed
before this CLI call: pattern summary mistaken for judge authority, normalized
whole-row intent dead end, and collective whole-row authorization bypass.
New end-to-end regressions cover each; 177 focused tests pass.

No must-fix remains known to the driver. Scope construction is not source semantic
qualification. F1/F2 and remaining campaign code continue after this checkpoint;
all hardware testing and handoff preparation remain paused.
