# Root judgment of Sonnet C1 QA

Confirmed must-fix: _repair_row discarded the validity of a candidate reread.
Malformed structured output yielded an empty finding list and could enter clean
or best-progress acceptance. Root fixed both paths: a valid reread is required,
and unresolved rereads remain inside the same original-scope repair budget.
The first judge's failure no longer prevents a later candidate from being judged.

Accepted test request: candidate reread now has separate provider/OOM/cancellation/
terminal-capacity coverage, plus malformed-reread and initial-judge-recovery tests.
The seven-module final focused set is 183 passed. The final full rerun is pending.

Optional fidelity-lane comment is existing explicit behavior, not a C1 defect:
model judgments remain active everywhere, while language-only pattern findings
are ineligible on the fidelity banks. Do not create a new PBUG, broad quality
project or source-qualified claim from this observation. F1 source checks have
their existing My Story scope; they are not yet implemented by C1.

Build notes grounded: A2's actual full baseline is 14197/51/183/1, later than the
older architecture review's 14135/53 baseline. Compare failed IDs AND details.
Root read the actual shared writer-tail calls at1383-1404; the family fixture
does not replace that inspection. New Bible guard has run against clean baseline
and controlled current candidate. Legacy ID formatting was corrected to the
existing PBUG-20260829-14, preserving 146 baseline metadata issues.

Sonnet read current complete source, not a git diff (its tools lacked shell);
the unrelated diff.txt/diff_utf8.txt were correctly ignored. Root owns the real
git diff, immutable file hashes and final qualification. No Mac-kill cause or
C2 extra-edit-count diagnosis is inferred. One targeted follow-up is required
because this first review found a real blocker; not another architecture arc.
