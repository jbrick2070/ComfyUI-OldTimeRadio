VERDICT: yes-with-fixes -- relative-first is the right double-instance answer, but the insert still runs on the success path (the hazard the driver asked about) and the kokoro standalone-loader inventory is a copy-paste MISREAD. Neither crashes a render; both should land before a burned version string.

MUST-FIX BEFORE BUILD:
1. [MUST-FIX 1 / Q1 / catalog+voice_bank+kokoro inserts] Unconditional `_NODES_DIR` insert BEFORE the try/except. Same three-line block in `nodes/_otr_model_catalog.py:35-37`, `nodes/_otr_voice_bank.py:45-47`, `nodes/_otr_kokoro_voice_prefetch.py:43-45`. Package-relative FIRST does stop THIS module from binding the flat owner -- CONFIRMED: when `__package__` is set, `from ._otr_shared import env` succeeds and the except never runs. The insert still mutates `sys.path[0]` on that success path. That is exactly the enablement `nodes/otr_post_upscale_procgen_blend.py:49-54` warns about and that `tests/test_ffmpeg_resolution_precedence.py:224-243` already caught when blend imported owners flat-first (`pu.resolve_ffmpeg is ffm.resolve_ffmpeg`). Catalog is imported far earlier and more widely than blend, so this fix copies a known-bad placement onto a hotter import. Smallest fix: move the insert into the `except ImportError` arm (only mutate when the relative rung failed). Standalone load still works; package load no longer injects `nodes/` at path[0]. Do not "fix" this by wrapping env/proc. [ASSUMPTION] env two-instance at ComfyUI boot is already real from `prestartup_script.py:88-92` flat-loading kokoro onto `_otr_shared.env` while `__init__.py:54-57` binds `.nodes._otr_shared.env`; env.py has no module cache (`nodes/_otr_shared/env.py:51-63` reads `os.environ` live) so it is production-equivalent -- still do not widen the sys.path mutation.

SHOULD-FIX:
1. [Q2 / `tests/test_standalone_module_loads.py:40-48` + the three STANDALONE LOAD comments] Kokoro loader attribution is a MISREAD. `scripts/otr_fetch_lane_weights.py:9-11` names the prefetch to say voices are ABSENT. `scripts/otr_provision.py` and `scripts/otr_make_portable_voice_bank.py` load catalog/voice_bank only (`otr_provision.py:638-653`, `:963-979`; `otr_make_portable_voice_bank.py:106-122`). The production standalone loader is `prestartup_script.py:88-92`, which ALREADY inserts `nodes/` before `from _otr_kokoro_voice_prefetch import ...`. Pre-migration, kokoro's flat rung already resolved on that path -- the "LOADS -> ModuleNotFoundError" row for kokoro is true of the isolated probe, not of boot. Point STANDALONE_LOADED and the comments at prestartup. Leave the insert (inside except, per MUST-FIX 1); the probe is stricter than boot and that is the right tripwire.
2. [SHOULD-FIX 3 residual / `tests/test_process_single_owner.py:143-152` vs `:158-163`] Rebound coverage is asymmetric. `run = subprocess.run` is flagged; `P = otr_proc.Popen` then `P(argv)` is not -- the assign walker only looks at subprocess module names, and the Call walker only sees `owners.Popen(...)`. That is the same class Astra demonstrated, now on the re-export they correctly kept. Smallest fix: flag `ast.Assign`/`ast.AnnAssign` whose value is `Attribute(Name in owners, attr=="Popen")`. Do not flag `otr_proc.run` rebound (that still hits the wrapper). Live tree: no `otr_proc.Popen(` call under `nodes/` (encode_sink.py:155 is an annotation, :190 calls `otr_proc.popen`).
3. [Q2 inventory] Astra did not miss another provisioner break of this class. By-path `nodes/` loads: catalog, voice_bank (fixed); `nodes/_otr_shared/public_engines.py` via `scripts/otr_asset_index.py:67-77` -- no env/proc import, stdlib-only; `scripts/repro_pbug11_gguf_cache.py:52-65` loads model_loader/gguf_backend/llm_policy under a synthetic `otrpack.nodes` package so relative imports resolve. Do not add those to STANDALONE_LOADED. `_otr_gguf_backend.py:26-30` still has a relative-only `_otr_generation_budget` import -- CONFIRMED pre-existing, not this diff.
4. [`tests/test_standalone_module_loads.py:68-73`] Probe inherits ambient PYTHONPATH. If it contains `nodes/`, `test_the_probe_would_actually_fail_on_a_broken_module` can go red (fail-safe) or, worse, a broken module can print LOADED (fail-open). Pass a scrubbed env (`PYTHONPATH` empty, keep `SystemRoot`/`PATH`/`PYTHONUTF8`). cwd=REPO.parent is CONFIRMED correct.

OPTIONAL / NICE-TO-HAVE:
- After LOADED, assert `hasattr(mod, "otr_env")` and `mod.otr_env.get("PATH", "")` works -- proves the ladder bound the owner, not just that import did not raise.
- Drop duplicate `import os as _os_boot` / `import sys as _sys_boot` in voice_bank.py:36-37 and kokoro_voice_prefetch.py:34-35; both files already import `os` (and blend already imports `sys`). Catalog needs the aliases; the other two do not.
- Correct voice_bank comment "those loaders do not add" -- `_load_voice_bank_module` inserts `_REPO` (`otr_provision.py:967-968`), which does not help `from _otr_shared ...`; the module insert of `nodes/` is still required.

CUT THESE:
1. A reflection/`getattr`/`importlib` analyser for the spawn ratchet -- already declined; `KNOWN LIMITS` + `test_the_known_limits_are_recorded_next_to_the_guard` is the right stop. Reopening it is scope creep.
2. Wrapping `Popen` in a proxy so the re-export is "safe to call" -- would break identity. CONFIRMED still required: `nodes/_otr_shared/proc.py:51-60`, `test_the_re_exports_are_IDENTITY_aliases` (`tests/test_shared_env_and_proc_owners.py:267-273`), live `except otr_proc.CalledProcessError` in `otr_post_upscale_procgen_blend.py:1006`, `except otr_proc.TimeoutExpired` in `otr_silent_composite.py:1152` and `_otr_shared/ffprobe.py:232`, annotation `Optional[otr_proc.Popen]` in `encode_sink.py:155`. `test_the_re_export_is_still_legal_as_an_ANNOTATION_and_an_EXCEPT` is the right guard. Keep `otr_proc.popen()` as the spawn.
3. Refusing `executable=None` -- CONFIRMED too strict would break pass-through; `test_an_explicit_executable_None_is_still_fine` is correct. No production `executable=` under `nodes/` (only the new tests plus unrelated pipeline `executable=True` in story routing).
4. Migrating `eng_indextts2.py` / `_otr_writer_heartbeat.py` in this change -- BLOCKED for recorded reasons; out of scope.
5. Adding `public_engines.py` or gguf_backend to STANDALONE_LOADED -- different load contracts, not this defect.

VERIFY-AT-BUILD checklist:
- verify: `pytest tests/test_standalone_module_loads.py tests/test_process_single_owner.py tests/test_shared_env_and_proc_owners.py tests/test_gpu_residency_pid_liveness.py tests/test_ffmpeg_resolution_precedence.py -q` on the Windows venv (driver claims suite green; this lane did not run it).
- verify: `tests/test_standalone_module_loads.py` is committed with the node edits (session snapshot had it untracked). Tests are `.comfyignore`d; the tripwire only exists if it is in git.
- Q1 double-instance: after MUST-FIX 1, verify package import of catalog does NOT put `nodes/` on `sys.path`, and standalone probe still prints LOADED. verify: `sys.modules` keys `_otr_shared.env` vs `nodes._otr_shared.env` after a real ComfyUI boot (prestartup vs pack) -- identity, not behavior.
- Q3 executable=: grep `nodes/` for `executable=` besides proc.py; confirm none. confirm `python*` prefix still admits `sys.executable` (`test_an_explicit_executable_None_is_still_fine`).
- Original Astra UNVERIFIABLE / render-time: one live `otr_proc.run` of a versioned imageio ffmpeg basename (`ffmpeg-win-x86_64-v7.1.exe`) still allowed via PREFIX (`proc.py:105-107`). No production caller uses `executable=` as a substitute.
- SHOULD-FIX 4: CONFIRMED already done -- `tests/test_gpu_residency_pid_liveness.py:8-16,56-62,81-94` (Windows/no-psutil -> True, warn once, names "reclamation"). No further code.
- SHOULD-FIX 3: CONFIRMED `otr_proc.Popen` CALL flagged, annotation/except/PIPE/`popen()` clean. verify: no live `otr_proc.Popen(` under `nodes/` after this lands.
- Q2: verify no other `scripts/` `spec_from_file_location(..., nodes/...)` grew an env/proc ladder without a bootstrap.
