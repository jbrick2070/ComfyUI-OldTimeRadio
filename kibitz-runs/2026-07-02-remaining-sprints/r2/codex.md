VERDICT: no. The plan is directionally implementable, but several sections omit the actual repo interfaces that must change, so coding it as written will either not surface in dropdowns or will violate existing request/schema contracts.

MUST-FIX BEFORE BUILD:
1. [Sprint B] Cloud image adapters are not specified against the real ImageEngine interface. `nodes/_otr_image_engines/registry.py:79-83` requires `assert_usable/prepare/render_image/teardown`, and `nodes/otr_image_gen_dispatcher.py:715-733` calls `render_image()` and expects a PNG path or decoded pixels via `_coerce_pixels` at `nodes/otr_image_gen_dispatcher.py:191-214`. Concrete fix: add `nodes/_otr_image_engines/eng_cloud_image.py` with registered adapters whose `render_image()` calls `invoke_partner_node()`, runs `canonicalize_image()`, and returns `str(asset.path)`; import it from `nodes/_otr_image_engines/__init__.py`.

2. [Sprint B] Adding registered cloud image engines will fail the capability declaration invariant unless the plan also adds `CAPABILITIES` rows. `tests/test_capability_profiles.py:211-217` asserts every registered image engine has exactly one declaration, and `nodes/_otr_image_engines/registry.py:107-138` currently has no `cloud_*` image rows. Concrete fix: add `cloud_recraft`, `cloud_flux_pro`, `cloud_nano_banana_2`, and `cloud_ideogram_v4` capability rows in the same change as registration.

3. [Sprint B / Sprint D] Dynamic/COMBO partner inputs are underspecified. `cloud_nano_banana_2` requires `model: COMFY_DYNAMICCOMBO_V3` at `nodes/_otr_shared/partner_nodes.yaml:236`, ElevenLabs requires `model`, `output_format`, and `voice` at `nodes/_otr_shared/partner_nodes.yaml:58-65`, and the voice selector row only exposes `voice: COMBO` at `nodes/_otr_shared/partner_nodes.yaml:84-85`. `docs/2026-07-02-cloud-engines/PROMPT_PROFILES.md` says COMBO option lists are excluded and V3 rows are directional until expansion. Concrete fix: define a V3/COMBO resolution layer or explicit env/config values per adapter before invocation; include these emitted kwargs in the profile-to-schema conformance test.

4. [Sprint D] “Dropdown row(s) in char_voice/announcer voice menus” will not happen by adding an adapter alone. Voice dropdowns are built from `_LEGACY_FIRST_ENGINES` via `legacy_first_engines()` in `nodes/_otr_engine_profiles.py`, not from `audio.registry.engines_for_role()` (`nodes/_otr_voice_node_common.py:162-190`). Concrete fix: add the ElevenLabs adapter import in `nodes/_otr_audio_engines/__init__.py`, add profile rows in `config/audio_engine_profiles.yaml`, update `_LEGACY_FIRST_ENGINES`, and then update `workflows/otr_scifi_16gb_full.json` node 81/82 widgets if a cloud row should be saved.

5. [Sprint E] `audio_motion_profile` cannot be “handed to every engine via the request” without a schema change. `VideoRequest` rejects unknown keys via `_Forbid` at `nodes/_otr_video_engines/schemas.py:78-81`, and there is no `audio_motion_profile` field in `VideoRequest` at `nodes/_otr_video_engines/schemas.py:149-173`. Concrete fix: add a typed `AudioMotionProfile` model or `dict` field to `VideoRequest`, populate it in `build_request_from_shot()` (`nodes/_otr_video_engines/render_driver.py:972`), and add compatibility tests before consumers read it.

6. [Sprint A] Removing fallback scaffolding must also rewrite or remove the soak helpers, not just tests. `render_shot()` already raises loud with fallbacks disabled (`nodes/_otr_video_engines/render_driver.py:1890-1917`), but `run_gpu_soak()` still calls `make_fallback_of()` and expects OOM fallback trails (`nodes/_otr_video_engines/render_driver.py:2581-2605`), while `assert_soak_ok()` asserts `EXPECTED_OOM_TRAIL` (`nodes/_otr_video_engines/render_driver.py:2511-2544`). Concrete fix: replace this soak with a loud-failure/no-fallback soak or retire it from exports/tests in the same change.

7. [Sprint B] “Un-mintable still = LOUD ledger note + beat stays 2D” contradicts the current no-fallback render behavior. Image generation failures currently raise `ImageRenderError` and stop, with “NO FALLBACK” at `nodes/otr_image_gen_dispatcher.py:555-614`. [ASSUMPTION] If “stays 2D” means silently avoiding a selected 3D/video path, that is a fallback. Concrete fix: either make mint failure terminal, or define an explicit pre-selection validation phase that prevents selecting the 3D engine before render planning.

SHOULD-FIX:
1. [Sprint A] `allow_auto_fallback` remains in two live schemas, not just the director widget: `nodes/otr_video_director.py:241-345` and `nodes/_otr_video_engines/schemas.py:128-131`. Concrete fix: keep the positional widget but force policy output false, and update `Policy.allow_auto_fallback` default to false or mark it legacy ignored.

2. [Sprint B] `canonicalize_image()` is currently only a stub at `nodes/_otr_shared/cloud_media_canonical.py:106-109`. Concrete fix: specify request keys (`w`, `h`, `out_path`, role/kind), output format, sRGB verification, and exact named `CloudMediaError` codes before adapters call it.

3. [Sprint B] `cloud_seedream_2` exists in pins and prompt profiles but is omitted from the S1 stills lane list. See `nodes/_otr_shared/partner_nodes.yaml:310-338` and `docs/2026-07-02-cloud-engines/PROMPT_PROFILES.md`. Concrete fix: either explicitly cut it from S1 or add it to the adapter/conformance matrix.

OPTIONAL / NICE-TO-HAVE:
- Add a single offline fake partner-node test that exercises `invoke_partner_node()` -> image canonicalizer -> dispatcher `_coerce_pixels()` with no network.
- Add a workflow audit assertion that cloud rows appear in `OTR_VideoDirector` image dropdown schemas before touching the saved JSON.

CUT THESE (over-engineering):
1. [Sprint A] Removing fallback ledger fields immediately. Keeping never-stamped fields is safer than a schema churn pass, and the plan already allows “keep fields (stamped never).”
2. [Sprint E] Full brightness/speech-vs-music/dynamic-range profile in C1 if no engine consumes it yet. Ship the request schema field plus duration/rms/peak/onset first; add richer metrics with the first real consumer.