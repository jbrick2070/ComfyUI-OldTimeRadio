VERDICT: yes-with-fixes. The core cloud and fallback changes are structurally sound, but the soak-test refactoring is underspecified, dynamic cloud TTS parameters are unresolvable via the standard bridge, and audio canonicalization is omitted.

MUST-FIX BEFORE BUILD:
1. [Sprint A / E1 / Tests] Soak Test Failures: Ripping out fallback variables (`make_fallback_of`, `FLOOR_NAMES`, `UNIVERSAL_FLOOR`, `SYNTH_FALLBACKS`, `EXPECTED_OOM_TRAIL`) in [nodes/_otr_video_engines/render_driver.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/render_driver.py) will immediately break [tests/test_video_soak_fixture.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/tests/test_video_soak_fixture.py) and [scripts/otr_video_soak.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/scripts/otr_video_soak.py), which explicitly test OOM trails and decision logs. Moreover, `run_gpu_soak` in `render_driver.py` (called by the batch nodes in soak mode) will crash with a `RenderError` on the forced OOM.
   - *Fix*: Remove fallback list tests in [tests/test_video_render_driver.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/tests/test_video_render_driver.py) and [tests/test_video_soak_fixture.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/tests/test_video_soak_fixture.py). Update `run_gpu_soak` in `render_driver.py` and `run_episode_soak` in `scripts/otr_video_soak.py` to catch `RenderError` on forced OOM and assert that a LOUD fail-closed error is raised.
2. [Sprint D / Cloud TTS lane / S2] Omission of `canonicalize_audio`: The plan schedules Sprint D (ElevenLabs TTS) and cloud music, but completely omits Sprint S2 (`canonicalize_audio` implementation). Without it, invoking cloud audio will result in a runtime `NotImplementedError` inside [nodes/_otr_shared/cloud_media_canonical.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_shared/cloud_media_canonical.py#L99-L103).
   - *Fix*: Explicitly include the implementation of `canonicalize_audio` in Sprint D (or as a prerequisite step) in `cloud_media_canonical.py`. The function must load the raw provider WAV (using `soundfile`), resample it, downmix to mono (matching `mono_safe`), apply loudness matching using the existing local reference, and write the conformed WAV to disk.
3. [Sprint D / Cloud TTS lane] Undefined parameter resolution for ElevenLabs Voice: `cloud_elevenlabs_tts` requires a `voice` input parameter of type `ELEVENLABS_VOICE` (produced by `cloud_elevenlabs_voice_selector`). However, the `invoke_partner_node` bridge in [nodes/_otr_shared/cloud_media_invoke.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_shared/cloud_media_invoke.py) only handles outputs that map to files on disk and will crash with `CORRUPT_OUTPUT` if passed a raw `ELEVENLABS_VOICE` object.
   - *Fix*: [ASSUMPTION] Specify that the TTS adapter must resolve the `ElevenLabsVoiceSelector` class dynamically at runtime from ComfyUI using `_resolve_node_class(partner_rows()["cloud_elevenlabs_voice_selector"])`, instantiate it, and execute its `EXECUTE_NORMALIZED` method locally inside the adapter to obtain the raw `ELEVENLABS_VOICE` object, bypassing the file-centric `invoke_partner_node` bridge for this helper node.
4. [Sprint B / S1 / Portrait-mint gates] Undefined data shapes and logic for Portrait-mint gates: The plan introduces a "mint acceptance check" that flags a portrait still as "un-mintable" (making the beat stay 2D). The signature, data shape, and component location of this check are undefined.
   - *Fix*: Define the programmatic signature and logic for the check. For example, implement `verify_portrait_mintability(prompt_text: str) -> bool` or `verify_portrait_image(image_pixels: np.ndarray) -> bool` in [nodes/_otr_shared/cloud_media_canonical.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_shared/cloud_media_canonical.py). Specify that if this check returns `False`, a warning is logged, the `mesh_subject_id` is omitted from the ledger image entry, and the beat is flagged as 2D in the ledger.
5. [Sprint D / Cloud TTS lane] Missing dropdown registration in `_LEGACY_FIRST_ENGINES`: The new ElevenLabs cloud TTS engines (`cloud_elevenlabs_tts` and `cloud_elevenlabs_flash`) will not be selectable in ComfyUI dropdowns because the plan fails to specify that `_LEGACY_FIRST_ENGINES` in [nodes/_otr_engine_profiles.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_engine_profiles.py#L42-L51) must be updated.
   - *Fix*: Add `cloud_elevenlabs_tts` and `cloud_elevenlabs_flash` to `_LEGACY_FIRST_ENGINES["char_voice"]` and `_LEGACY_FIRST_ENGINES["announcer_voice"]` in `nodes/_otr_engine_profiles.py`.

SHOULD-FIX:
1. [Sprint E / SC-C1] Ambiguous audio features: Sprint E plans to compute `rms/peak/onset/silence/brightness/dynamic-range/speech-vs-music/duration` driver-side and pass them in the request. However, computing complex temporal features like `onset` (beat/tempo onsets), `brightness` (spectral centroid), and `speech-vs-music` (speech classification) is extremely difficult to do accurately without adding heavy external libraries like `librosa` or neural voice activity detectors, which are not listed in [requirements.txt](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/requirements.txt).
   - *Fix*: [ASSUMPTION] Specify that `speech-vs-music` is a static assignment based on the beat's role (announcer/character = speech, music = music), `onset` is a count of transient peaks above a threshold, `brightness` is a high-to-low frequency energy ratio via a simple FFT using `numpy` or `torch`, and `dynamic-range` is the difference between peak and RMS. Define their exact data shapes (floats or list of floats) in a Pydantic schema on `VideoRequest` in [nodes/_otr_video_engines/schemas.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/schemas.py).
2. [Sprint B / S1] Ambiguous calling context for `canonicalize_image`: Unlike the video rendering pipeline in `render_driver.py` which has a dedicated `eng.canonicalize(...)` hook, [nodes/otr_image_gen_dispatcher.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/otr_image_gen_dispatcher.py) does not have a canonicalize step in its lifecycle. It only calls `render_image(request)` and directly hashes and stamps the resulting pixels.
   - *Fix*: Specify that `canonicalize_image` must be called internally within the cloud image adapter (`nodes/_otr_image_engines/eng_cloud_image.py`) inside its `render_image` method before returning the conformed `.png` path to the dispatcher.
3. [Sprint C / S3] Undefined data schema for ShotLock audit stamps: The plan notes that `ShotLock` will write "audit stamps for cloud rows", but fails to specify the names and types of these fields in the ledger.
   - *Fix*: Define that `ShotLock` (in [nodes/otr_shot_lock.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/otr_shot_lock.py)) stamps `provider_id`, `estimated_usd_cap`, and `is_cloud = True` onto the video shot rows in the ledger.
4. [Sprint A / E2] Redundant `allow_auto_fallback` widget: Since fallback is completely disabled in `render_driver.py`, keeping the `allow_auto_fallback` widget on the ComfyUI node is confusing.
   - *Fix*: In [nodes/otr_video_director.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/otr_video_director.py#L241), set the widget default value to `False`, change its description/label to `"(Deprecated) allow_auto_fallback"`, and hardcode the internal output policy value to `False` regardless of the widget's input.

OPTIONAL / NICE-TO-HAVE:
- [Sprint D / Cloud TTS lane] Dynamic voice listing: To make the voice-selection presets robust, the UI selector could query the ComfyUI node class `INPUT_TYPES` dynamically to build the list of valid names instead of using a static, hardcoded list in python.

CUT THESE (over-engineering):
- None. The plan is already highly streamlined due to the directive cutting auto-defaults and fallback chains.
