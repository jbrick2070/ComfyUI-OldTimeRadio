VERDICT: build-ready-with-fixes. The core routing and routing fixes are correct, but the capability checks will break the existing test suites, and the content oracle fails on static still engines.

MUST-FIX BEFORE BUILD:
1. [F2] [D1] Capability eligibility routing breaks existing test suite (non-canonical roles + stub adapters)
   - Defect: Routing `engine_registry_base.engines_for_role` and `assert_usable` in [engine_registry_base.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_shared/engine_registry_base.py#L169-L226) unconditionally through `role_compat.engine_fits_role` will break the existing test suites (e.g., [test_video_platform_aseam.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/tests/test_video_platform_aseam.py) and [test_video_engine_registry_base_additive.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/tests/test_video_engine_registry_base_additive.py)). The test stubs (such as `_StubEngine` and `_stub_engine`) do not declare `required_inputs`, and the tests query/assert eligibility using non-canonical role strings (such as `"text_to_video"`), which will raise `RoleCompatError` since they are absent from `role_compat.ROLE_AVAILABLE_INPUTS`.
   - Concrete Fix: Wrap the capability validation inside `engine_registry_base.py`'s `engines_for_role` and `assert_usable` methods in fail-soft logic: if the engine does not declare `required_inputs`, or if the queried role is not present in `role_compat.ROLE_AVAILABLE_INPUTS` (raising `RoleCompatError`), fall back to the legacy `roles` whitelist check.
2. [F5] [D2] Static still engine (still_flat) fails frame variance gate
   - Defect: The content oracle's acceptance gate (F5) requires that the emitted clip must have "frame variance (motion, not a frozen still)". However, the `still_flat` engine (defined in [cheap_families.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/cheap_families.py#L225-L242)) is explicitly designed to output a dead-flat static still with no motion (`_still_motion = False`). This engine will produce zero frame variance, causing it to fail this gate unconditionally.
   - Concrete Fix: Restrict the frame variance (motion) requirement to engines that are expected to produce motion (e.g., video engines and panning still engines like `still_pan` / `still_motion`), and exempt static still engines (like `still_flat` and the retired `station_card`).

SHOULD-FIX:
1. [F1] Redundant capability updates for retired engines
   - Defect: F1 states that `accepts_still = True` should be added to `StillPanFamily`, `StillMotionFamily`, and `StationCardFamily` in [cheap_families.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/cheap_families.py). However, both `StillMotionFamily` (`still_motion`) and `StationCardFamily` (`station_card`) are designated for retirement in the same commit under the clean-break plan (as documented in [COMBO_SOAK_CONVERGED_PLAN.md](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/docs/2026-06-29-coverage-soak/COMBO_SOAK_CONVERGED_PLAN.md#L37-L51)). Updating them is redundant and contradicts the clean-break rip-out.
   - Concrete Fix: Apply the `accepts_still = True` flag only to `StillPanFamily` and omit modifications to the retired families (`StillMotionFamily` and `StationCardFamily`).
2. [F4] Loss of offline CPU soak verification in CI
   - Defect: The clean-break plan removes the offline mock soak harness ([otr_video_soak.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/scripts/otr_video_soak.py)) and its tests ([test_video_soak_fixture.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/tests/test_video_soak_fixture.py)). Since the new re-engineered combo-soak runs as a live ComfyUI API prompt via HTTP, there will be no automated offline verification of the soak/fallback/OOM classifications in the CI test suite.
   - Concrete Fix: Retain a mock-based unit test in `tests/` that loads the canonical JSON, mocks the node executions offline (e.g., using dry-runs or mocks for the image and video nodes), and asserts that the policy-applier, shot-lock, and dispatcher paths produce the expected outputs and classifications under simulation.

OPTIONAL / NICE-TO-HAVE:
- [F6] Parametrization of the Matrix Test: Parametrize the matrix test using `vreg.all_engine_names()` instead of hardcoded lists to automatically cover future engines added to the registry.

CUT THESE (over-engineering):
- None. (The clean-break plan already aggressively cuts dead code and fallback shims, which is optimal.)
