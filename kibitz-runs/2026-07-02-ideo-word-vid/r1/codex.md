VERDICT: no. The D hybrid arc is plausible, but the plan does not define the two load-bearing contracts: the exact dialogue text payload the renderer animates, and the wordless background image kind it composites over.

MUST-FIX BEFORE BUILD:
1. [Working preference / What it touches] D says "title_mood-style background mint" for the primary dialogue path, but the companion stills plan defines `title_mood` as the music_visual wordless old-radio card, while character/announcer gets `lyric_text` cards. Reusing that as-is either gives dialogue a generic music-radio plate or double-renders text under the procgen overlay. Concrete fix: define a separate wordless `lyric_background` / `word_video_plate` image kind for character/announcer beats, keyed by speaker, beat mood, Meta brief, and role; keep `title_mood` only for music/no-word beats. Cite: `C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/docs/GO_FORWARD_NEXT/2026-07-02-ideogram-lyric-stills.md`, sections "Architecture" and "What it touches"; `C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/otr_meta_brief_image_prompt.py:523`.
2. [What it touches / Risks] The plan never names the data contract for the exact words to animate. Current video requests carry `text_prompt`, but that is a visual/model prompt, not guaranteed exact script text; the raw line text exists in ledger lines and is linked by `source_line_ids`, but ShotLock shots do not stamp `text` into the shot row. Concrete fix: add an explicit request payload such as `dialogue_text`, `speaker_role`, `speaker_label`, and optional `line_id`, derived in `build_request_from_shot()` from the ledger line, and do not overload `text_prompt`. Cite: `nodes/otr_shot_lock.py:279`, `nodes/otr_shot_lock.py:731`, `nodes/_otr_video_engines/render_driver.py:972`, `nodes/_otr_video_engines/render_driver.py:1471`.
3. [Risks / open questions] Word timing is left as "verify-at-build", but it determines the actual product. The repo has line-level duration/frame counts and caption cue splitting, not word timestamps. Concrete fix: declare v1 timing now: phrase/word reveal is evenly distributed over `target_frame_count` or `dur_s`, with min on-screen thresholds; forced alignment is a later opt-in, not a v1 dependency. Cite: `nodes/otr_shot_lock.py:331`, `nodes/_otr_captions.py:117`, `nodes/_otr_captions.py:139`.
4. [What it touches] "NEW procgen word-overlay video engine" is too vague for the existing engine model. A selectable render path must be a registered video engine with declared roles, family, required inputs, capabilities, and request handling. Concrete fix: specify `ideo_word_vid` as a `_otr_video_engines` adapter that consumes `init_image` plus the new dialogue payload, renders a silent canonical clip, and is selectable through `OTR_VideoDirector`; update role compatibility if a new required input token is introduced. Cite: `nodes/_otr_video_engines/cheap_families.py:21`, `nodes/_otr_video_engines/registry.py`, `nodes/_otr_shared/role_compat.py:46`, `nodes/otr_video_director.py:128`.
5. [Working preference] The claim "words are ALWAYS legible" is an acceptance claim without a spec. Concrete fix: define hard v1 layout limits: max words/phrase, max lines, min font size, contrast rule, safe margins, and fail/skip behavior for overlong lines. Reuse caption limits only if explicitly chosen. Cite: `nodes/_otr_captions.py:24`, `nodes/_otr_captions.py:31`, `nodes/video_engine.py:672`.

SHOULD-FIX:
1. [Candidate approaches B] "No new model capability needed" is true, but "pure sequencing" is not zero-build in this repo: current planning is one shot per ledger line and current still target derivation creates one per-beat scene still. Multi-card intra-beat sequencing would need multiple image objects per beat plus a compositor/sub-shot timeline. Concrete fix: demote B to later, or restate it as a real sequencing feature. Cite: `nodes/otr_shot_lock.py:727`, `nodes/otr_meta_brief_image_prompt.py:515`.
2. [Candidate approaches C] The evidence "SDH captions at Node 58" is stale; the workflow has `OTR_CaptionBurn` at node id 86, and captions are static ASS subtitles, not kinetic typography. Concrete fix: say existing code provides PIL/ffmpeg text drawing primitives, not the word-animation engine. Cite: `workflows/otr_scifi_16gb_full.json`, node id 86; `nodes/otr_caption_burn.py:1`; `nodes/video_engine.py:636`.
3. [Risks / open questions] The plan ignores interaction with SDH caption burn. If `burn_captions` or `OTR_BURN_CAPTIONS=1` is enabled, dialogue word-video and captions will duplicate text onscreen. Concrete fix: define precedence: word-video disables SDH for those beats, or SDH stays bottom-safe with non-overlap margins. Cite: `nodes/otr_caption_burn.py:18`, `nodes/otr_caption_burn.py:206`, `workflows/otr_scifi_16gb_full.json`, node id 86.
4. [Rough size] The "~1-2 sessions" sizing is optimistic because the plan includes new image kind, video adapter, exact-text request payload, timing/layout engine, selector wiring, workflow validation, and visual QA. Concrete fix: split rough size into S1 background plate, S2 text payload/timing, S3 renderer, S4 workflow/tests.

OPTIONAL / NICE-TO-HAVE:
1. [Risks / open questions] Add a small deterministic style inventory: 3-5 font/layout presets keyed by speaker role and mood, before attempting palette extraction from the plate. [ASSUMPTION] Palette extraction can wait because exact text legibility is the primary acceptance bar.

CUT THESE (scope / over-engineering):
1. [Candidate approaches E] Cut direct cloud t2v with in-video text from v1. It contradicts the exact-text goal and the document already says no pinned row supports reliable in-video text.
2. [Candidate approaches A] Cut cloud i2v from the build plan until a promptable i2v row exists. The companion doc and current `cloud_wan_i2v` adapter show prompt text is not forwarded in the pinned row. Cite: `nodes/_otr_video_engines/eng_cloud_video.py:293`.
3. [Candidate approaches B] Cut multi-card pseudo-animation from the first build. It does not deliver "words MOVE"; it adds intra-beat sequencing work before the primary D engine proves the text/timing contract.