VERDICT: no. The sequencing choice is plausible, but `ideo_word` is not build-ready until the plan resolves registry identity and per-role/per-kind routing.

MUST-FIX BEFORE BUILD:
1. [`ideo_word` build contracts #1/#2] `ideo_word` is described as one chosen engine, then as two adapter classes, then the open question asks about splitting into two engine names. The image registry keys adapters by `inst.name`, so two registered classes are two public dropdown entries unless they collide and overwrite. See `C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\nodes\_otr_shared\engine_registry_base.py:141-149` and `:160-167`. Fix: choose one public registry contract: either one `ideo_word` engine with internal mode helpers, or two explicit engine IDs; update CAPABILITIES, conformance tests, and workflow verification to match.

2. [`ideo_word` build contracts #2/#5 + Workflow JSON] The plan does not explain how lyric cards are routed without hijacking normal objects. Current dispatch resolves image engine by object `role`, not `kind`: `C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\nodes\otr_image_gen_dispatcher.py:151-163`, `:453-477`. Existing prompt payloads include portraits and scene stills, not only beat cards: `C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\nodes\otr_meta_brief_image_prompt.py:921-930`, `:1296-1304`. Selecting `ideo_word` for `character_video`, `announcer_visual`, or `music_visual` would route every object in that role through it. Fix: define the object/routing model explicitly: when `kind=lyric_card` is emitted, how it is selected, and how ordinary portraits/stills stay on normal image engines.

3. [`Workflow JSON`] “No JSON change” conflicts with the production wiring story. The saved workflow still has node 87 `OTR_VideoDirector` image widgets set to `flux_gen1` for announcer/music/other-beats in `C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\workflows\otr_scifi_16gb_full.json` [ASSUMPTION: widget indices 3-5 from `OTR_VideoDirector.INPUT_TYPES`]. If the intent is selectable-only, say that explicitly; if the intent is production use, change the saved widget values in the same commit and run the workflow audits.

4. [`ideo_word` build contracts #2 title_mood] The episode-title source is left as “verify-at-build,” but title mood is one of the two core modes. The code already has a canonical primary: writer stamps `meta["episode_title"]` at `C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\nodes\OTR_LedgerScriptWriter.py:5358`, and video title resolution uses `meta.episode_title` first at `C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\nodes\video_engine.py:2110-2117`. Fix: specify the title read order and fail-loud behavior before build.

SHOULD-FIX:
1. [`Workflow JSON`] The plan says “ImageDirector combo,” but image-model dropdowns actually live in `OTR_VideoDirector`; `OTR_ImageDirector` explicitly has no image-model widgets. See `C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\nodes\otr_image_director.py:6-11`, `:241-306`, and `C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\nodes\otr_video_director.py:169-178`, `:203-229`. Fix the target of the selector verification.

2. [`ideo_word` build contracts #6] The conformance-test issue is real: `_engine_by_node_key()` overwrites on duplicate node keys. See `C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\tests\test_cloud_partner_conformance.py:50-59`. Fix exactly as planned: node_key -> list of engines and assert every adapter.

3. [`Verify-at-build checklist`] Add an explicit end-to-end assertion for the saved workflow/menu path: `OTR_VideoDirector.INPUT_TYPES()` contains the new engine ID, and the real workflow either selects it or deliberately remains selectable-only. Current selector source is `C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\nodes\otr_video_director.py:169-178`.

OPTIONAL / NICE-TO-HAVE:
- Add a small generated-prompt report field identifying `lyric_text` vs `title_mood`; it will make eyeball/debug runs less ambiguous.

CUT THESE (scope / over-engineering):
1. [`ideo_word` build contracts #4] Cut cross-episode `cloud_media_cache` wiring from v1 unless the key fit is trivial. Lyric cards are per-beat unique by contract, and the existing dispatcher already has ledger-local request hashing at `C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\nodes\otr_image_gen_dispatcher.py:507-510`. Global billing cache can follow after the routing contract is correct.

2. [`Open questions for the panel`] Cut the two-public-engine-name option unless you choose it in MUST-FIX #1. Keeping both “one `ideo_word`” and “split engine names” alive will leak into tests, CAPABILITIES, and workflow assertions.