# Root A1 flexible-cast final QA judgment

One Cursor Grok review completed; root grounds against actual Windows files.
Repair wording: no demonstrated contradiction (selected N is not proposed act
list), but simplified to preserve people/events/ending and change grouping to N.
This is clarification, not evidence that the prior words caused the live failure.
Raw request discrepancy CONFIRMED for direct API values outside1..10. Both count
receipts now use admitted bundle.request.num_characters, including0 (no truthy
fallback); only model hint uses existing normalization. Three regression cases.
Voice-capacity hint removal ACCEPTED: no predictive planning-down; actual P4
allocation remains the authority. D1 widget wording clarified, same UI meaning.
Missing-doc paths MISREAD: both exist on real Windows, ignored by default git
ignore/glob. They are intentionally staged as receipts, never delete them.
Full-writer unequal-cast fixture SHOULD, not blocker: dispatch bypasses legacy
count gate; actual canonical component with unequal requested/actual cast will
supply the integrated evidence. No new fake-tail success claimed. Empty-title
live suggestion not required on every run; real shared-cleanup unit proves parity.
No additional exclusive/fidelity gate. A2/B/C separate. Canonical unchanged.
Prior final full run: reported14667 tests,53 failures,0errors184skips502.751s,
zero new IDs vs S4; one old disk-space test varied to pass. After these small
review fixes focused/full/Bible rerun, not assumed green. See final A1 receipt.
