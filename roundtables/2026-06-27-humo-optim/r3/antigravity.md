VERDICT: no. The plan lacks an implementable solution for split-graph execution, contains aspect-ratio and VRAM ceiling mismatches, and fails to correctly configure the boot profile for the target engine.

MUST-FIX BEFORE BUILD:
1. [Q1 ISOLATION] The proposed split-graph option (a) in [roundtables/2026-06-27-humo-optim/r3/claude_anchor.md](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/roundtables/2026-06-27-humo-optim/r3/claude_anchor.md) (two `/prompt` calls) is blocked because ComfyUI lacks any native node or custom node in this repository to save and reload `conditioning` structures (text/audio embeddings) across distinct API execution sessions. Option (b) (mid-graph reclaim via a script-only node) will fail because the ComfyUI server is booted in a separate process and only loads node classes registered in the production `__init__.py` (`_NODE_MODULES` dictionary at [__init__.py:122-315](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/__init__.py#L122-L315)).
   Concrete Fix: Create a separate, temporary custom node package directory at `C:\Users\jeffr\Documents\ComfyUI\custom_nodes\otr_bakeoff_helper\` containing an `__init__.py` that registers a single diagnostic node class (e.g., `OTR_BakeoffReclaim`). Inside this node, import and call `nodes._otr_video_engines.wrapper_bridge.reclaim_idle_models(reason="humo bakeoff pre-sampler")`. Wire this node into the standalone graph variant (`scripts/otr_humo_bakeoff.json`) between the conditioning/encoding outputs (e.g., `LTXVConditioning`) and `KSampler` inputs to force topological execution and mid-graph VRAM eviction in a single `/prompt` transaction without editing production files.

2. [Q2 LEGS] The proposed `humo_1.7B control` leg has a portrait aspect ratio (9:16, 480x832), which is an interface mismatch for the target candidate `humo_14B_169` (which runs in landscape 16:9, 832x480 as defined in [nodes/_otr_video_engines/eng_humo.py:533-535](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/eng_humo.py#L533-L535) and [nodes/_otr_video_engines/eng_humo.py:557-559](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/eng_humo.py#L557-L559)). This prevents a direct quality/speed comparison under identical aspect ratio constraints.
   Concrete Fix: Make the control leg `humo_1.7B_169` (landscape 16:9 tier) to match the aspect ratio, resolution, still canvas, and audio length of the candidate.

3. [Q3 METRICS] Copying the LTX bakeoff runner configuration verbatim will cause the VRAM peak ceiling to default to 14.5 GB (14500 MB) (matching `VRAM_CEILING_MB = 14500` in [scripts/run_ltx_av_q_bakeoff.py:103](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/scripts/run_ltx_av_q_bakeoff.py#L103) and [nodes/_otr_video_engines/motion_common.py:40](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/motion_common.py#L40)). This misses the target constraint of the heavy HuMo engine, which is strictly <= 13.5 GB (13500 MB) as stated in r3 constraints.
   Concrete Fix: Hard-code `VRAM_CEILING_MB = 13500` in the new `run_humo_bakeoff.py` script to enforce the correct diagnostic gate.

4. [Q4 RESET/BOOT] If the harness boots the ComfyUI server using the `FLOOR` argument (as copied from LTX at [scripts/run_ltx_av_q_bakeoff.py:187](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/scripts/run_ltx_av_q_bakeoff.py#L187)), the launcher script will clear the `OTR_ENABLE_HUMO` environment variable ([scripts/_otr_soak_server_launch.cmd:60-62](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/scripts/_otr_soak_server_launch.cmd#L60-L62)). This causes `HuMoEngine.assert_usable()` to fail closed immediately, preventing validation of the class mappings or loading.
   Concrete Fix: Boot the ComfyUI server without the `FLOOR` argument (e.g. use default or pass a different identifier) to ensure `OTR_ENABLE_HUMO=1` is correctly set in the environment.

5. [Q4 RESET/BOOT] Clean-booting ComfyUI for each individual leg hides the cross-engine VRAM residency pressure (where prior engine models, like LTX-AV and Whisper, contaminate GPU memory). In production, no reboot occurs between shot beats, and `render_driver.py` performs inter-beat reclaim only on engine switches ([nodes/_otr_video_engines/render_driver.py:1550-1572](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/render_driver.py#L1550-L1572)).
   Concrete Fix: Add a dedicated "AV-stack-resident" sentinel leg to the sweep sequence. This leg must first execute a representative LTX-AV rendering prompt to load LTX-AV and Whisper weights, and then run the two-stage HuMo leg in the same ComfyUI session without rebooting. Measure the VRAM peak of this second render to verify production safety.

SHOULD-FIX:
1. [Q3 METRICS] Face detector and mouth-landmark libraries are not standard dependencies in the repository's requirements ([requirements.txt](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/requirements.txt) / [requirements.video.txt](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/requirements.video.txt)), meaning the proposed landmark motion and lip-area SSIM metrics will fail-loud on standard GPU hosts. However, the "blue-cast delta" can be computed without external face detector libraries.
   Concrete Fix: Compute the blue-cast delta in pure Python using `PIL.Image` and `numpy` (which are standard dependencies) by comparing the average color channels of the rendered video frames against the source still image. Implement a soft preflight check for face detector libraries and skip landmark-based quality metrics dynamically instead of failing the run.

2. [Q4 RESET/BOOT] The copied LTX reset script (`reset_box` at [scripts/run_ltx_av_q_bakeoff.py:146-165](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/scripts/run_ltx_av_q_bakeoff.py#L146-L165)) only terminates `main.py` and port 8000 listeners, leaving orphaned `cmd.exe` processes spawned by `_otr_soak_server_launch.cmd` and any previously hung harness scripts (`run_humo_bakeoff.py`) alive.
   Concrete Fix: Update `reset_box` to selectively kill prior `run_humo_bakeoff.py` process trees (matching by command line, excluding the current PID) and ensure that the parent `cmd.exe` shell spawned by `SOAK_LAUNCH_CMD` is terminated.

3. [Q3 METRICS] Copying LTX's workflow save-node output pattern verbatim might output clips containing audio or different frame encodings. In production, HuMo emits silent clips (`has_audio` is False) via `wrapper_bridge.encode_frames_to_silent_mp4` ([nodes/_otr_video_engines/eng_humo.py:24-25](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/eng_humo.py#L24-L25), [nodes/_otr_video_engines/eng_humo.py:352-354](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/eng_humo.py#L352-L354)).
   Concrete Fix: Ensure the output of the standalone graph uses the `SaveImage` node to write a lossless PNG frame batch to disk, and the harness script compiles these frames into a silent MP4 using the production `wrapper_bridge.encode_frames_to_silent_mp4` helper to ensure a byte-for-byte encoding match with the production renderer.

OPTIONAL / NICE-TO-HAVE:
- Include a `--dry-validate` mode in the harness script that maps all legs, generates workflow JSON files, and asserts the manifests without starting the ComfyUI server or triggering GPU execution (matching [scripts/run_ltx_av_q_bakeoff.py:50](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/scripts/run_ltx_av_q_bakeoff.py#L50)).

CUT THESE (over-engineering):
1. [Q2 LEGS] Cut the optional leg (iv) `humo_14B_169 no-LoRA ~25-step` from the diagnostic suite. The minimal leg set (baseline, candidate, and control) is sufficient to answer "5/21 quality AND <=13.5 GB" without wasting GPU time on an upper-bound reference that is not a promotion candidate.

[ASSUMPTION] We assume that standard ComfyUI standard nodes do not include `SaveConditioning` or `LoadConditioning` capability, which prevents split-graph executions using standard `/prompt` requests.
[ASSUMPTION] We assume the GPU host is equipped with OpenCV (`cv2`) but lacks dlib or mediapipe, requiring soft gates for face/landmark detection.
