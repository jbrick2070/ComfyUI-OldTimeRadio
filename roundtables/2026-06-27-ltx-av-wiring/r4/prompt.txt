ROUND 4 -- CONVERGENCE / RESIDUAL DEFECTS

ROLE: You are a senior principal engineer doing a FINAL adversarial review of
this plan before the build is locked. Three prior rounds of review have already
hardened this document; your job is to find anything that survived those rounds
and still needs fixing, plus catch any regressions introduced by the fixes
themselves.

You are one voice on a panel of independent reviewers. You do not see the other
reviews. A separate judge will verify every claim against the real source code,
so vague criticism is worthless -- be specific, cite the section, make it
checkable.

THIS ROUND'S FOCUS -- weight your attention here:
1. Residual must-fix defects -- Anything still broken, ambiguous, or
   under-specified that would block a successful build or first production run.
2. Fix-introduced regressions -- Did any edit introduce new contradictions,
   broken invariants, or scope creep? Compare stated goals against current text.
3. Build-blocking ambiguity -- Any section where a reasonable implementor would
   make two different valid choices that lead to incompatible outputs.
4. Verify-at-build items -- Confirm each earlier-UNVERIFIABLE item has a concrete
   verify step in the plan.
5. Final over-engineering sweep -- Anything that can still be cut.

Also check holistically: internally consistent end to end? matches the stated
goals? lean enough to hand to a builder today?

GROUNDING: Check claims against the REAL repo in your working directory. Do NOT
invent file contents, function names, or APIs you cannot see. If a claim depends
on code you were not shown, say "verify: <what>". Specifically: confirm the decode
params in nodes/_otr_video_engines/eng_ltx_av.py (~line 556-559), the scaler in
nodes/otr_silent_composite.py _seg_vf (~line 319-325), the ceiling constant in
nodes/_otr_video_engines/wrapper_bridge.py (VRAM_CEILING_MB), and whether the
decode temporal_size is a hardcoded engine constant or a widget in
workflows/otr_scifi_16gb_full.json.

OUTPUT (strict, plain text, no fluff):
- VERDICT: build-ready as-is? yes / yes-with-fixes / no. One line why.
- MUST-FIX BEFORE BUILD: numbered. Each = [section] + defect + concrete fix. Severity order. If none: "None -- plan converged."
- SHOULD-FIX: numbered, same format.
- OPTIONAL / NICE-TO-HAVE: brief.
- CUT THESE: numbered, with why it is safe to cut.
- VERIFY-AT-BUILD checklist.
- Mark [ASSUMPTION] anywhere you infer beyond the document or grounding.

Cite file paths / section identifiers throughout. Do not restate the document.
Prefer the smallest change that closes each defect.

------------------------------------------------------------------
YOU ARE RUNNING INSIDE A REAL REPOSITORY. Read whatever files in your current
working directory you need to ground this review in the REAL code.

The document to review is at: roundtables/2026-06-27-ltx-av-wiring/r4/input.md

Hard rules:
- You do NOT see the other reviewers. Do NOT assume they are correct.
- Do NOT write, edit, or apply any code. Review only.
- Cite real file paths for every concrete claim. If you cannot verify
  something against the code, write "verify: <what>" instead of asserting it.

Return ONLY your review in the structure specified above. No preamble.
