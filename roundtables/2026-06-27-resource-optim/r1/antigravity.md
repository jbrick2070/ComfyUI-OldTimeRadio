VERDICT: no. The plan's headroom targets are mathematically impossible with current defaults, and the portability story is broken because the draft tiers cannot run without crashing on the local LLM side.

MUST-FIX BEFORE BUILD:
1. [What to assess 4 / Portability] The `cpu_floor` and `8gb_lite` profiles ([config/profiles/cpu_floor.json](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/config/profiles/cpu_floor.json), [config/profiles/8gb_lite.json](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/config/profiles/8gb_lite.json)) are marked `"status": "draft"` and cannot be run on a CPU-only or low-VRAM machine without crashing. The LLM writer node `OTR_LedgerScriptWriter` is exempted from mapping ([config/profiles/widget_mapping.json](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/config/profiles/widget_mapping.json)#L72), meaning profiles cannot patch its writing model slots (`creative_writing_model` and `technical_model`). Since the transformers model loader ([nodes/_otr_model_loader.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_model_loader.py)#L156-L158, #L329-L344) forces 4-bit NF4 bitsandbytes quantization for the default model (`Mistral-Nemo-Instruct-2407`), which requires CUDA, loading the LLM on a CPU-only host will raise a fatal runtime error. [ASSUMPTION]
   Fix: Add the `creative_writing_model` and `technical_model` slots to the widget mapping registry in `widget_mapping.json` and override them to a CPU-compatible option (such as the Ollama local GGUF `google/gemma-4-12b-it` or a remote backend) in `cpu_floor.json` and `8gb_lite.json`.

2. [What to assess 1 / Headroom] The headroom target on the RTX 5080 (16 GB VRAM) is incompatible with the default heavy engine configurations. While the hard ceiling is `14500` MB (~14.16 GiB) in [nodes/_otr_video_engines/wrapper_bridge.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/wrapper_bridge.py#L37), the default `ltx_video` GGUF Q3_K_M is documented to peak at `~14.8 GB` (~14.46 GiB, [nodes/_otr_video_engines/registry.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/registry.py#L162)) and `wan_i2v` is estimated at `14500` MB ([nodes/_otr_video_engines/registry.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/registry.py#L195)). Because desktop operating systems and background processes typically occupy 1.0–2.5 GB of VRAM, running these heavy engines directly at or above the ceiling leaves zero actual headroom and will cause random CUDA OOM failures. [ASSUMPTION]
   Fix: Define clear pass/fail VRAM envelopes (e.g., green <= 13.0 GB, yellow <= 14.0 GB, red > 14.0 GB) and either lower the default ceiling parameter or enforce lighter quants (e.g., Q2_K or smaller quant mixtures) as the default for 16 GB configurations.

3. [What to assess 4 / Portability] The host-detection check is a hard abort rather than a graceful fallback. In [nodes/_otr_workflow_validator.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_workflow_validator.py#L289-L315), the validator throws a hard `ValueError` if the stamped profile does not match the host's actual hardware (e.g. aborting execution if `16gb_full` is run on a Mac or a sub-14.5 GB VRAM card), instead of automatically downgrading the execution to the suggested profile.
   Fix: Update the validator or the launch script to support automatic runtime profile fallback (e.g. automatically applying `8gb_lite` or `cpu_floor` instead of throwing a ValueError) when hardware constraints are violated.

SHOULD-FIX:
1. [What to assess 3 / VRAM Management] The memory reclaim mechanism fails to address known heavy engine edge cases. While `free_after_use` and `reclaim_idle_models` are implemented in [nodes/_otr_video_engines/wrapper_bridge.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/wrapper_bridge.py#L248-L322), `wan_i2v` still documents a `14499` MB peak without active mitigation ([nodes/_otr_video_engines/eng_wan_i2v.py](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/nodes/_otr_video_engines/eng_wan_i2v.py#L189-L195)). [ASSUMPTION]
   Fix: Add assertion tests in the test suite to verify post-reclaim VRAM returns to baseline before the next beat begins.

2. [What to assess 1] Resolve the conflicting definitions of the VRAM ceiling. Workflow files set `vram_ceiling_gb` of 14 ([workflows/otr_scifi_16gb_full.json](file:///C:/Users/jeffr/Documents/ComfyUI/custom_nodes/ComfyUI-OldTimeRadio/workflows/otr_scifi_16gb_full.json)), while `motion_common.py` and `wrapper_bridge.py` define it as 14,500 MB (~14.16 GiB).
   Fix: Standardize on a single configuration constant to govern both workflow validation and runtime execution.

OPTIONAL / NICE-TO-HAVE:
- [What to assess 4] Establish wall-clock/runtime limits or warnings for the CPU floor, as running `bark` or `musicgen` on CPU will cause massive generation stutters and slow down runs to unusable speeds. [ASSUMPTION]

CUT THESE (scope / over-engineering):
1. Cut the review/optimization of remote/provider LLM engines (OpenRouter and Comfy Credits). Since these run out-of-process and consume 0 GB of local VRAM, their optimization is irrelevant to local headroom and portability.
2. Cut the character_3d engines (`triposg_talk` and `hunyuan3d_talk`) from the optimization plan. They are flag-gated dark and require complex compiled toolchains (`cu128_toolkit`), so their optimization should be deferred until the baseline video pipeline is fully stable.
