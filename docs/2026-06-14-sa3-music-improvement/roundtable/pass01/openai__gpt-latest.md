<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

- VERDICT: no. The document is a problem statement with open questions, not a build-ready plan; it also leaves unresolved engine targeting, prompt/negative-prompt API, timing/trim behavior, and a brief-reader contradiction.

- MUST-FIX BEFORE BUILD:
1. [6] No concrete implementation decisions are specified. The deliverable asks for a “converged” plan, but sections [4.1]-[4.6] are still questions: no final prompt template, negative prompt, sampler values, duration strategy, model decision, or seed strategy. Concrete fix: replace the open questions with explicit constants/API changes, e.g. exact positive prompt template, exact negative prompt string, exact `steps/cfg/sampler/scheduler/denoise`, exact render duration vs returned cue duration, model filename default, and seed behavior.

2. [3A]/[3B]/[6] Negative-prompt ownership/API is undefined and likely to break callers. Grounding shows `compose_music_prompt(meta, cue_id) -> tuple[str, int]`, and `StableAudioTheme._render_clips()` unpacks exactly `prompt, duration_s`. Grounding also shows `StableAudio3Engine.generate_clip(prompt, duration_s, seed)` internally encodes `neg = CLIPTextEncode().encode(clip, "")[0]`. If the new negative prompt is added to `compose_music_prompt` by changing its return shape, current callers will break, including any legacy musicgen consumer mentioned in `_otr_music_prompt.py`. Concrete fix: either keep `compose_music_prompt()` signature unchanged and put the SA3 negative prompt inside `StableAudio3Engine.generate_clip()`, or add a separate SA3-only helper such as `compose_music_negative_prompt()` and update only SA3 call sites explicitly.

3. [2] The “Meta-brief protocol” constraint contradicts the grounded implementation. Section [2] says “keep reading period/setting/mood via `_otr_brief_reader._read_brief_field`; do NOT poke meta directly.” Grounding shows `_otr_music_prompt.py` only uses `_read_brief_field()` for `music_mood_terms`; it directly reads `meta["story_brief_terms"]["setting"]`, `meta["story_brief_terms"]["atmosphere"]`, `meta["news"]["script_brief"]`, and `meta["gen_params_initial"]["period_voice"]`. Concrete fix: either update `_otr_music_prompt.py` to read all those fields through `_read_brief_field()` if supported, or revise the constraint to match the actual allowed access pattern. Verify: whether `_read_brief_field()` supports nested paths such as `story_brief_terms.setting` and `gen_params_initial.period_voice.descriptor`.

4. [1.5]/[3B]/[6] The actual engine to modify is unresolved. Grounding shows `eng_stable_audio_3.py` registers engine name `"stable_audio_3"`, but `stable_audio_theme.py` has `_LEGACY_FIRST_FALLBACK = ("musicgen", "stable_audio_music")` and its docstring still says MusicGen is the default. Section [1.5] treats this as “not a blocker,” but it is a blocker for a build because changing `StableAudio3Engine.generate_clip()` will not improve output if the saved workflow actually selects `stable_audio_music` or `musicgen`. Concrete fix: verify the saved `otr_scifi_16gb_full.json` music engine value before coding, then either route the graph to `"stable_audio_3"` or apply equivalent sampler/prompt-negative changes to the actual selected SA engine. Also update `stable_audio_theme.py` comments/tooltips/fallbacks if SA3 is now the intended default.

5. [3C]/[4.4]/[6] The longer-render/trim strategy is unspecified and can change cue contract behavior. Grounding shows cue durations are fixed in `_otr_music_prompt.py` as `opening=12`, `closing=8`, `interstitial=4`, and `StableAudio3Engine.generate_clip()` passes `dur` to both `ConditioningStableAudio().append(..., seconds_total=dur)` and `EmptyLatentAudio().generate(dur, 1)`. If the plan renders 30-45 seconds for coherence, it must specify whether `seconds_total` is 30-45 or the final cue duration, where trimming occurs, and whether fades are applied. Concrete fix: define exact behavior, e.g. “for interstitial/opening/closing, latent length = 30.0s, `seconds_total = 30.0`, decode, deterministic trim first N seconds to legacy cue duration inside `generate_clip()`, optional fixed fade length X ms.” Do not leave this to downstream assembler code because [2] freezes the audio spine.

6. [2]/[4.7]/[6] The byte-identical test requirement is underspecified against an upstream music change. Section [2] says “Audio SPINE is FROZEN” and `test_audio_byte_identical` must stay green, but this change intentionally alters generated music prompts/sampler inputs, which can alter any final rendered audio bytes if that test covers full output including music. Concrete fix: state exactly what `test_audio_byte_identical` asserts. If it protects mux/master code only, ensure fixtures mock or freeze generated music. If it compares final audio bytes with generated cues, either update the golden intentionally or split the test so spine byte identity is tested independently from music-generation quality. Verify: current scope of `test_audio_byte_identical`.

7. [4.6]/[3E] “Best-of-N seeds at author time” has no persistence mechanism. The document says not per render, but does not say where selected seeds are stored or how the render reuses them. Without persistence, best-of-N either becomes nondeterministic/manual or silently changes render behavior. Concrete fix: choose strictly single-seed for this build, or define a stored per-cue seed override field in the ledger/workflow and update `_render_clips()` to prefer that field deterministically. Verify: whether the ledger schema already has a music seed override location.

8. [4.5]/[2]/[3D] Model choice is open while the box constraint names only `stable_audio_3_small_music.safetensors` on disk. A build that flips to a larger checkpoint would fail model availability on the stated target unless the file is guaranteed present. Concrete fix: keep `_CKPT` default as `"stable_audio_3_small_music.safetensors"` for this build; larger checkpoints may only be opt-in via `OTR_SA3_CKPT` after local availability and 16 GB residency are verified.

- SHOULD-FIX:
1. [1.2]/[3A] The proposed SA3 prompt grammar is too broad unless capped. “Genre/style, named instruments, BPM/tempo, key/mode, mood, production/era descriptor, cue arc” can easily produce long, contradictory prompts when brief fields are noisy. Concrete fix: cap positive prompt length by design: one genre phrase, max 5 instruments/production tags, one tempo/BPM phrase, one key/mode phrase, max 3 mood terms, one cue arc sentence/phrase.

2. [3A]/[4.1] Period-to-genre mapping needs deterministic fallback rules. The document gives one example for 1950s sci-fi radio but no map for absent, malformed, or non-1950s period descriptors. Concrete fix: define a small ordered map and fallback, e.g. if descriptor contains `1950`, `radio`, `atomic`, `pulp`, or `sci-fi`, use vintage orchestral sci-fi instrumentation; otherwise use “cinematic instrumental underscore, small orchestra, low strings, soft brass, light percussion, analog tape warmth.”

3. [3B]/[4.2] The sample negative prompt includes terms that may suppress desired old-time-radio sci-fi texture. “Dissonant” conflicts with eerie sci-fi tension; “noisy” conflicts with analog tape warmth. Concrete fix: use more targeted negatives such as “vocals, singing, speech, spoken words, lyrics, dialogue, voiceover, crowd noise, harsh clipping, digital distortion, muddy mix, out of tune, random noise, silence, low quality,” and avoid blanket “dissonant” unless tests prove it helps.

4. [3B]/[4.3] Sampler changes need validation against ComfyUI’s accepted sampler/scheduler names. Grounding only proves the current strings are `"dpmpp_3m_sde_gpu"` and `"exponential"`. Concrete fix: if changing them, add a smoke test or runtime assertion that the names exist in the installed ComfyUI sampler registry before render starts. Verify: actual ComfyUI API for sampler/scheduler enumeration.

5. [6] The test plan lacks a minimal build-time smoke test for SA3 path. Concrete fix: add a short test/mocked integration that verifies `StableAudio3Engine.generate_clip()` receives non-empty positive prompt, non-empty negative prompt, integer seed, expected duration, and returns an AUDIO dict with `waveform` and `sample_rate`. If full model tests are too heavy, gate them as local/manual.

6. [stable_audio_theme.py grounding]/[1.5] `StableAudioTheme` comments/tooltips are stale relative to the stated SA3 default. The grounded file says “MusicGen is the byte-identical default” and fallback is `("musicgen", "stable_audio_music")`. Concrete fix: after resolving the actual default engine, update docstrings/tooltips so the workflow UI does not mislead operators.

7. [1.6]/[3B] “Temperature maps to cfg + sampler/scheduler + steps” is directionally useful but not enough for release determinism. Concrete fix: log the final sampler constants and negative prompt version/hash in `render_log` so A/B listens can be traced to exact settings without changing determinism.

- OPTIONAL / NICE-TO-HAVE:
1. Add a small prompt-debug unit test with a representative Meta brief and expected substrings: genre, instruments, BPM/tempo, key/mode, mood, cue-specific arc, and instrumental-only/no-vocal constraint.
2. Add an environment-variable override for SA3 tuning constants only if the defaults remain code-pinned for normal renders.
3. Add a manual A/B listen checklist with fixed seeds and three cues, but do not gate the build on subjective scoring unless the criteria are explicit.

- CUT THESE (over-engineering):
1. [3E]/[4.6] Cut best-of-N seed selection for this build. It adds persistence/schema/UI questions and can compromise reproducibility. Single deterministic seed per cue already matches the existing contract in grounded `stable_audio_theme.py`.

2. [3D]/[4.5] Cut larger-model migration from this build. The stated target only guarantees `stable_audio_3_small_music.safetensors` on disk, and larger-model discovery introduces availability/VRAM/license risk without being necessary to fix prompt grammar and negative conditioning.

3. [4.3] Cut sampler grid-search from the build plan. Pick one conservative SA3 setting set and make it traceable. Extensive sampler/scheduler exploration is an authoring/evaluation task, not a release-engineering prerequisite.

4. [3A] Cut elaborate key derivation from arbitrary Meta brief fields. Unless the brief already carries key information, infer a simple deterministic default by cue/mood, e.g. minor mode for opening/interstitial and resolving minor-to-major language for closing. Full musical key mapping adds complexity with little guaranteed benefit.