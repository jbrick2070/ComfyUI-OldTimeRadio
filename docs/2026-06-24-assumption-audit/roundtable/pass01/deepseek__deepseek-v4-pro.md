<!-- requested_model: deepseek/deepseek-v4-pro | resolved_model: deepseek/deepseek-v4-pro-20260423 -->

VERDICT: no. The core assumptions that the style catalog diversifies stories and that deterministic intent-only crisis-noun substitution prevents console-standoff collapse are falsified by the proven-live failures. The pipeline is model-dependent, and critical style grammar is unused.

MUST-FIX BEFORE BUILD:
1. [OTR_LedgerScriptWriter.py F2 block + _otr_outline.py _build_user_prompt] The style-grammar lever injects only the ending_tag and ending_template; the style’s sound_world and story_engine are never passed to the outline or line composer. This causes style diversity to die at the writer (proven failure: memory_erasure_clinic_session produced a NASA lunar-mission story). Fix: In the writer’s F2 block, after selecting a style, render the full style grammar (via _otr_style_catalog.render_style_grammar) and inject it into the outline request’s style field (or a new field) so the outline LLM sees the concrete sound world and conflict shape. Also thread the grammar into the line composer’s static prefix (e.g., as a STYLE GRAMMAR block) so every beat is steered by the chosen engine.
2. [_otr_story_quality_l12.py ground_crisis_nouns + proven gemma failure] The L1 crisis-noun grounding mutates only beat.intent; the actual dialogue line is still generated by a weak model that can produce console-standoff language. The assumption that intent-only substitution prevents the collapse is false. Fix: Extend grounding to the line composer’s prompt by adding a hard constraint in the WRITE LINE block (e.g., “Do NOT use any of these words: console, lever, override, countdown, self-destruct, …”) built from GENERIC_CRISIS_NOUNS. Additionally, add a post-generation scrub that flags and rerolls lines containing those nouns when the conflict object is present.
3. [OTR_LedgerScriptWriter.py run() – model-agnostic assumption] The pipeline uses the same prompts and machinery for mistral and gemma, but gemma demonstrably ignores the instruction-following (proven failure). The assumption that a single prompt strategy works across models is broken. Fix: Introduce a model-capability profile (e.g., a flag on the model catalog entry) that gates model-specific prompt adjustments. For weak instruction-followers, add a simplified, more forceful anti-console directive and reduce reliance on complex dramatic-frame fields. Alternatively, hard-fail or warn when a known-weak model is selected for the creative slot.

SHOULD-FIX:
4. [_otr_outline.py _assemble_outline] The announcer open/close intents are hardcoded (e.g., “Open the episode and orient the listener.” and the style-grammar close). This forces a uniform bookend regardless of episode content. Should-Fix: Generate the announcer intents dynamically from the outline and style, or at minimum vary the open intent based on the selected style’s sound_world.
5. [_otr_story_quality_l12.py assign_beat_roles] The beat_role sequence is rigid (setup → pressure → personal_stake → climax) and forced for n≥3. This may produce unnatural beats when the story doesn’t fit that shape. Should-Fix: Allow the outline LLM to propose a role sequence (with validation) or make the sequence configurable per style.
6. [_otr_line_composer.py _build_user_prompt] The DRAMATIC FRAME renders many fields (objective, obstacle, turn, etc.) that a weak model may ignore, contributing to the single-prior trap. Should-Fix: For models with known instruction-following weakness, collapse the frame into a single concise directive rather than a multi-field block.

OPTIONAL / NICE-TO-HAVE:
- The video engine’s forced cfg/steps/negative (eng_ltx_av.py) could be made style-dependent for visual variety, but this is lower priority.
- The critic/reroll cascade could be augmented with a pre-generation “story check” that rejects outlines likely to collapse (e.g., detecting high density of crisis nouns in beat intents).

CUT THESE (scope / over-engineering):
- The _PERSONAL_COST fallback table in _otr_story_quality_l12.py has only a “general” domain entry. Either expand it to match the domain palette or remove it; a single generic fallback adds complexity without domain-specific value.
- The forced-default “SHARP” mode in eng_ltx_av.py is hardcoded; if it’s not yet validated across all content, consider making it opt-in per episode rather than always-on.

[ASSUMPTION] The pipeline assumes that the local LLM will follow the detailed dramatic-frame instructions; the gemma failure shows this is false for some models. (Verify: test with gemma-12b and the current prompt to confirm instruction-following rate.)
[ASSUMPTION] The style catalog’s story_engine and sound_world are assumed to influence generation, but they are never wired into the outline or composer prompts. (Verify: search for render_style_grammar or story_engine in OTR_LedgerScriptWriter.py – it is absent.)
[ASSUMPTION] The deterministic L1/L2 shaping is assumed sufficient to prevent console standoff; the proven failure contradicts this. (Verify: run gemma with OTR_STORY_QUALITY_L12=1 and check output for console-standoff language.)

3 HIGHEST-LEVERAGE STRUCTURAL CHANGES:
1. Wire the full style grammar (sound_world + story_engine) into the outline and line composer so the chosen radio-drama style actually shapes the story beyond the ending tag. This directly addresses the style-diversity collapse.
2. Move the anti-console-standoff enforcement from intent-only mutation to a multi-layer defense: (a) prompt-level banned-word list, (b) post-generation detection + reroll, (c) outline-level rejection of beats with high crisis-noun density. This tackles the core sameness failure.
3. Introduce a model-capability gate that adapts prompt complexity and fallback behavior based on the selected creative model, preventing silent failure on weak instruction-followers like gemma.