# R1 judgment

Spend this pass: ~$0.5947. Panel: GPT-5.5, Gemini-3.1-pro, DeepSeek-v4-pro.

CONVERGENCE: HIGH. All 3 models + the anchor independently identified the same
two root seams (style grammar never injected; body dialogue never gated). No
model dissented.

ACCEPTED (grounded CONFIRMED): K1 body-gate-missing (+ the "mission control"/
"news facts" smoking guns, composer 1162/1442), K2 render_style_grammar dead
(zero callers), K3 body beats starved (build_sq_data enrich gate ~694), K4
climax-always-last, K6 dead render_style_grammar, K7 position shadows arc_phase
(writer 3997, composer 1210-1217), K8 _PERSONAL_COST general-only, K9 select_style
is a draw not best-fit, K10 DOMAIN_PALETTE keyword collision, K11 forced LTX
recipe. SELF-CORRECTION accepted: compose_announcer_outro (writer 4230) overwrites
my C5 close gate -> gate moot; flag_thesis_close did the live work.

VERIFY-AT-BUILD: K5 (confirm style_descriptor != _style_slug in one live ledger
meta), K7 (does _position_for carry the dramatic directive or just "phase N of
M"), Beat.arc_phase default="setup" (Gemini SHOULD-FIX -- check the Beat model).

REJECTED / LOW: ltx_av_talk vs ltx_av_music "merge" -- genuinely different graphs
(I2V+audio vs audio-only), a defensible distinction, not a false one. CUT-
candidates (best-of-N / use_exchange / dual remote slots) are real bloat but
ORTHOGONAL to the story-quality repair -- noted, not killed this campaign.

INVARIANTS GUARDED: every fix keeps determinism (the body-gate is a deterministic
count+reroll, seed-stable), 100% local, byte-identical-when-killed (the new gate
ships behind the same story-quality flag), audio byte-identity (text-only
changes; golden re-baseline already flagged).
