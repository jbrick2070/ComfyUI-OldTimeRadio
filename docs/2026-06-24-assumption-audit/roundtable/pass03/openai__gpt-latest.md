<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. The plan still orders StoryContract selection before its stated inputs exist, leaves current F2 re-selection in conflict with the new contract, and misses live bypass paths (_ex_text, announcer outro) that will ship unvalidated / wrong-policy text.

MUST-FIX BEFORE BUILD:
1. [BUILD 2 / OTR_LedgerScriptWriter D.2 vs D.2.5] Defect: “Select PRE-outline ... writer section D.2 feeding script_brief or news_seed” is ordered wrong against the live sequence. In the grounding, `script_brief` is not assigned until D.2.5 `build_news_briefs`; D.2 is the existing style picker before news interpretation. Selecting a StoryContract at D.2 from `script_brief` will either use an undefined local or silently fall back to `news_seed`, contrary to the plan. Concrete fix: create the StoryContract after D.2.5 news interpretation succeeds/degrades and before D.5 `OutlineRequest(...)`; input should be `script_brief if script_brief else resolved["news_seed"]`. Keep the existing user/style picker value available for `build_news_briefs`, because news interpretation currently receives `style=resolved["style"]`.

2. [BUILD 2 / OTR_LedgerScriptWriter F2] Defect: the live F2 block still selects `_style_slug = _OTRSTYLE.select_style(_premise_str, meta, cast_seed)` from `outline.premise`, then derives `_ending_tag` and `_ending_template`. That directly violates “Reuse the SAME contract in F2 -- do not reselect from outline.premise” and can make outline grammar/body grammar/outro policy disagree. Concrete fix: replace the F2 `select_style(...)` call with the pre-outline `StoryContract` fields (`slug`, `ending_tag`, `ending_template`, `grammar`); do not call `select_style` anywhere after `generate_outline`.

3. [BUILD 2 / _otr_style_catalog.py] Defect: the requested interface does not exist. Grounding shows `_otr_style_catalog` exports dict helpers (`select_style`, `get_style`, `render_style_grammar`, `ending_template_for`) and no `StoryContract`, `build_story_contract`, or `as_meta_dict`. Concrete fix: add the frozen dataclass and builder in `_otr_style_catalog`, but implement it over the current catalog keys: `slug`, `label`, `sound_world`, `story_engine`, `ending_tag`, `ending_template=ending_template_for(slug)`, `grammar=render_style_grammar(slug)`.

4. [BUILD 2 / _otr_outline.OutlineRequest + Path C prompts] Defect: adding style fields to `OutlineRequest` is necessary but not sufficient. The live production path uses `_build_macro_user_prompt`, `_build_phase_user_prompt`, and `_build_beat_user_prompt`; only macro currently sees `req.style`, and phase/beat prompts have no style grammar input. Concrete fix: add defaulted `style_grammar`, `sound_world`, `story_engine`, `ending_tag` fields to `OutlineRequest`; render `style_grammar` in all three Path C prompt builders. Update every `OutlineRequest(...)` construction site shown in writer D.5 and verify `_otr_pitch_room.run_pitch_room` preserves these fields when it returns a modified request.

5. [BUILD 2/K5 / OTR_LedgerScriptWriter section I + K.5] Defect: “Collapse the two style systems” is under-specified and unsafe with the grounded writer. The live writer still uses `style_descriptor = resolved["style"]` for `LineRequest.style_descriptor`, and later stamps `meta["visual_plan"]["style"] = resolved["style"]` and `meta["style"] = resolved["style"]`. If `resolved["style"]` becomes `StoryContract.slug`, downstream visual/style consumers may receive a snake_case catalog id instead of the existing user/LLM style descriptor. Concrete fix: for this build, stamp `meta["story_contract"] = contract.as_meta_dict()` and pass `contract.grammar` through new prompt fields; do not overwrite `meta["style"]`, `visual_plan.style`, or existing `style_descriptor` until every downstream consumer is updated. [ASSUMPTION] verify downstream readers of `meta.style` / `visual_plan.style` before true collapse.

6. [BUILD 1 / _otr_story_quality_l12.py + _otr_line_composer.LineRequest] Defect: the plan depends on interfaces absent from the grounded code. `_otr_story_quality_l12` has `premise_noun_palette`, `count_ungrounded_crisis`, `CLIMAX_CLASS_ROLES`, etc., but no `validate_composed_grounding(...)`. `LineRequest` has no `grounded_nouns`. Concrete fix: add `validate_composed_grounding(text, sq_entry, grounded, ...)` to `_otr_story_quality_l12`; add `grounded_nouns: frozenset = field(default_factory=frozenset)` to `LineRequest`; populate it in writer section I from a palette computed once after cast/news/outline are known. Also update `_otr_reroll.build_reroll_line_request` because reroll reconstruction otherwise loses the palette. [ASSUMPTION] `_otr_reroll` exists as described but is not grounded here.

7. [BUILD 1 / OTR_LedgerScriptWriter section I exchange branch] Defect: the live `use_exchange` path bypasses `compose_line` entirely: `_ex_text` is assigned directly to `cleaned`, then appended to `last_lines` and ledger. If K1 is only placed after `line_res = compose_line(...)`, exchange-generated bad lines still seed the next prompt. Concrete fix: put the body-output gate after the common `cleaned` assignment but before `last_lines.append(...)` / `led.update_line_text(...)`, or explicitly validate `_ex_text` in the exchange branch and fall back to `compose_line(..., reroll_hint=...)` once on failure.

8. [BUILD 1 / OTR_LedgerScriptWriter section I] Defect: “on LineCompositionFailedError, keep the original” only works for a post-`line_res` reroll. It does not work if the original `compose_line` call failed before producing `line_res`. Concrete fix: distinguish the two cases: original compose failure should retain current existing behavior and raise; K1 fallback-to-original applies only after a successful original line and a failed validator-triggered reroll.

9. [BUILD 4 / _otr_line_composer.compose_announcer_outro + writer I.5] Defect: the live `compose_announcer_outro` signature has no `ending_tag`, and writer I.5 calls it without one. The live F3 logic sets `resolved = is_resolved_ending_change(ending_change)` and, if resolved, injects “State this outcome plainly,” which conflicts with unresolved/revelation/quiet endings. Concrete fix: add `ending_tag: str = ""` to `compose_announcer_outro`; before computing prompt branches, force `resolved = False` for `ending_tag in {"unresolved_final_sound", "revelation", "quiet_acceptance"}` or the exact policy set you intend; add an explicit “do not resolve/state the outcome” instruction for those tags. Pass `contract.ending_tag` at writer I.5.

10. [BUILD 3 / _otr_story_quality_l12.build_sq_data] Defect: non-`irreversible_choice` climax roles selected by the catalog will not get fallback content or `_enrich_intent`, because live code only enriches `BEAT_ROLE_PERSONAL_STAKE` and `BEAT_ROLE_IRREVERSIBLE_CHOICE`. This makes `revelation`, `reversal`, `confession`, `quiet_acceptance`, etc. structurally selected but body-starved. Concrete fix: replace the tuple gate with a role-keyed map covering every `CLIMAX_CLASS_ROLES` member that can be assigned by `assign_beat_roles`; call `_enrich_intent` with role-specific tail text, not the current personal-stake fallback for all non-irreversible roles.

11. [BUILD 3 / _otr_story_quality_l12.build_sq_data + _enrich_intent] Defect: live code appends enrichment and then truncates `new_intent.strip()[:_INTENT_MAX]`, so the appended clause can be cut off exactly where it is needed. Concrete fix: have `_enrich_intent` compute the tail first, reserve `len(sep + tail)` from `_INTENT_MAX`, truncate the original intent to that remaining budget, then append the full tail.

SHOULD-FIX:
1. [CORRECTIONS K7 / _otr_line_composer._build_user_prompt] Defect: live composer renders either `POSITION: ...` or the old `ARC PHASE` guidance, never both. Since writer always supplies `position`, `ARC_PHASE_GUIDANCE` is effectively shadowed in the line composer. Concrete fix: when `req.position` is present, append the guidance for `req.arc_phase` under the POSITION block rather than falling into the `elif req.arc_phase` branch.

2. [BUILD 1 / telemetry] Defect: K1 telemetry names are specified but no aggregation point is identified. Existing writer already mutates `meta.setdefault("story_quality", {})` in F2; late scrubs and compose flag summary also run after line generation. Concrete fix: update the same `meta["story_quality"]` dict in-place at the K1 gate, not by replacing it, and define exact increments for `body_gate_rerolls`, `body_gate_failed`, and `body_gate_ungrounded_crisis`.

3. [BUILD 1 / grounded palette] Defect: the proposed palette source `premise_noun_palette(roster, news_seed, *premise_texts(meta))` omits `outline.premise`, which is the actual dramatic premise emitted by the outline stage and may contain legitimate grounded objects not present in `news_seed` or `meta.news`. Concrete fix: include `outline.premise` in the once-per-writer palette computation unless you intentionally want to reject outline-introduced objects.

4. [BUILD 2 / LineRequest body injection] Defect: adding `style_grammar/sound_world/story_engine` to `LineRequest` will not affect output unless `_build_user_prompt` renders them. The live `LineRequest` currently only has `style_descriptor`, `ending_template`, and L12 fields. Concrete fix: add fields and render one canonical style grammar block in `_build_user_prompt`; keep `ending_template` as the final-beat-only instruction.

5. [BUILD 4 / fallback policy] Defect: deterministic fallback still resolves when `resolved` is true: writer I.5 can land `_resolved_outro_fallback(...)` via `compose_announcer_outro`. If unresolved-style override only changes the prompt but not fallback selection, a failed LLM close can still state an outcome. Concrete fix: thread `ending_tag` into fallback selection too; unresolved/revelation/quiet policy should call `fallback_announcer_outro` or a new non-resolving fallback, never `_resolved_outro_fallback`.

OPTIONAL / NICE-TO-HAVE:
- [BUILD 2] Add a single debug stamp comparing old `resolved["style"]` and new `StoryContract.slug` for one live ledger before deleting the old style path; the plan already says verify divergence, but make it a one-run telemetry field rather than log-only.
- [BUILD 1] Export the validator helper in `_otr_story_quality_l12.__all__` if tests or external reroll code import by public surface.

CUT THESE (over-engineering):
1. [BUILD 3] Cut consequence-role enrichment from this build. Grounding shows `assign_beat_roles` makes the climax-class beat the LAST voiced character beat and does not assign `BEAT_ROLE_CONSEQUENCE`; K4 explicitly defers post-climax roles. Supporting `consequence` now is unreachable under the current validator.
2. [BUILD 2/K5] Cut the full “collapse the two style systems” in this build. It is not required to get one StoryContract injected into outline/body/outro, and it risks breaking current `meta["style"]`, `visual_plan.style`, and downstream style readers. Keep a separate `meta.story_contract` until consumer migration is verified.