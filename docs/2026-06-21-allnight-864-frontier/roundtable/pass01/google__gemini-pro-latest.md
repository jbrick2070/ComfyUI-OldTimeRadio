<!-- requested_model: ~google/gemini-pro-latest | resolved_model: google/gemini-3.1-pro-preview-20260219 -->

VERDICT: no. Critical sequencing and schema errors guarantee the dramatic spine fails to attach to the script, and contradictory length constraints will break generation if modified.

MUST-FIX BEFORE BUILD:
1. [Section 1] Sequencing defect: Stage 5 (`dramatic_state`) runs *after* Stage 4 (`generate_outline`). Stage 4 generates the beat intents, but Stage 5 defines the central conflict and `costly_choice_beat`. Because Stage 5 runs too late and does not even read the outline, it cannot influence the beats. Fix: Swap the order. Stage 5 must run before Stage 4, and Stage 4 must be conditioned on Stage 5's output to actually place the costly choice.
2. [Section 3 - Stage 5] Missing JSON key: The schema requested in the Stage 5 prompt explicitly asks for only 4 keys (`character_a_wants`, `character_b_wants`, `dramatic_question`, `ending_change`). It omits `costly_choice_beat`. This is exactly why `episode_valid = False` on 76% of runs (Section 2b). Fix: Add `"costly_choice_beat": "..."` to the requested JSON schema in the Stage 5 prompt.
3. [Section 3 - Stage 7 & Logic Constants] Contradictory length constraints: The logic constants cap `words_per_beat_range` at 35. The Stage 7 prompt hardcodes the string "about 20-30 words". If the LLM panel fixes the undershoot by increasing the logic constant (as Section 4 suggests), the hardcoded prompt string will contradict the new variable and break the composer. Fix: Replace "about 20-30 words" in the Stage 7 prompt with "about {words_per_beat_range[0]}-{words_per_beat_range[1]} words".
4. [Section 6 vs Section 5] Ledger ordering violation: Section 6 proposes a unified announcer pass that runs "after the full script exists" to write act bridges. Section 5 states widget changes are "append-only and positional". If bridges are generated after Stage 7 and appended to `lines[]`, they will play at the end of the episode, not at the act breaks. Fix: Explicitly state in Section 6 that the renderer sorts `lines[]` by `dialogue_slot_id` or `start_s`, and require the announcer pass to map its output to pre-allocated slot IDs from Stage 4.

SHOULD-FIX:
1. [Section 3 - Stage 3] Gender blindness: The `lock_cast` prompt asks for a `character_description` but does not instruct the LLM to include the character's gender or pronouns in the text. Because Stage 7 only sees this description (and not the raw `{gender}` variable), the line composer hallucinates genders (e.g., the SOM CORBEN bug in 2c-5). Fix: Add "Include the character's gender and appropriate pronouns" to the CHARACTER VISUAL CONTRACT, or pass `{gender}` directly into the Stage 7 prompt.
2. [Section 4 vs 0] Impossible request: Section 4 asks the panel to fix premise repetition, but C4 explicitly forbids cross-episode memory or DBs. An LLM cannot fix cross-run repetition if it only sees one run at a time. Fix: Inform the panel they are allowed to propose Python-level randomization (e.g., injecting a `random.choice(themes)` variable into Stage 1) as a "small logic constant".
3. [Section 3 - Stage 7] Self-addressing bug: The prompt tells the LLM "You are responding to {PREV_SPEAKER}", which fails to prevent characters from using their own names (2c-5). Fix: Add a strict negative constraint to the OUTPUT FORMAT: "Never speak your own name."

OPTIONAL / NICE-TO-HAVE:
- [Section 3 - Stage 8] If you adopt the unified announcer pass, your deterministic fallbacks will break (they expect single string outputs, not a JSON array of multiple lines). You will need to redesign the fallback logic to handle array parsing failures.
- [ASSUMPTION] In Stage 7, `max_new_tokens = min(200, max(40, target_words*4))`. I am assuming `target_words` here refers to the *beat* budget (e.g., 30), resulting in ~120 tokens. If it accidentally receives the *episode* budget (864), the token math will cap at 200, heavily truncating the output. Ensure the variable passed is strictly the beat budget.

CUT THESE (over-engineering):
1. [Section 6] The unified ending-aware announcer pass. It requires restructuring beat assembly, ledger ordering, and fallback logic just to fix a hedge in the outro. It is safe to cut. Instead, keep Stage 8 as separate calls, but pass Stage 5's `ending_change` and the final spoken line of the script into the outro prompt so it knows exactly how the story resolved.