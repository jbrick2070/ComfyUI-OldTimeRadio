<!-- requested_model: deepseek/deepseek-v4-pro | resolved_model: deepseek/deepseek-v4-pro-20260423 -->

VERDICT: yes-with-fixes – The prompt is comprehensive and contains all necessary evidence, constraints, and stage details; however, a few gaps in clarity and scope could lead the panel to waste time on non-story issues, propose unimplementable reorderings, or violate the "no QA-only" constraint. These must be tightened before handing to LLMs.

MUST-FIX BEFORE BUILD:
1. [Section 2b] The `ledger_scrub_status = FAIL` data point is a known infrastructure bug (post-script scrubber), not a story‑quality issue. The document already states “The render pipeline … is out of scope”, but the panel could interpret the FAIL as a defect to fix. Add an explicit line: “ledger_scrub_status FAIL is a known non‑story issue; ignore it completely.” This prevents wasted effort.
2. [Section 6] The candidate “ending‑aware announcer pass” description says it would replace separate intro/outro calls, but the existing pipeline has a distinct Stage 8 `compose_announcer_intro` / `_outro`. Clarify whether the unified pass **replaces** Stage 8 (and must emit lines with same role) or runs **after** them as an additional pass. Without this, a panel might propose adding a new stage but leave the old one intact, creating duplicate announcer lines.
3. [Section 0, final paragraph] The “first move” request lacks a definition of format. It says “the single change you’d ship first, and why.” The panel could answer with a sentence like “increase words_per_beat_range” without tag or before/after. Make it explicit: “Provide the exact tag [PROMPT]/[LOGIC]/…, the stage, and (if prompt) the before→after text, as with the ranked fixes.” This ensures an actionable, checkable answer.
4. [Section 2c‑5] The qualitative close‑read lists bugs like stage‑directions‑as‑dialogue and self‑addressing. C3 prohibits “QA‑only rounds,” but a panel might over‑interpret that as forbidding any improvement to hygiene. Add a note: “You may propose stronger prompt instructions or tighten the existing post‑line hygiene rules (e.g., forbid self‑naming) – as long as it does not introduce a new scoring/rejection/reroll pass.” This keeps bug‑fixes in scope without violating C3.
5. [Section 4, Symptom‑Cause map] The structural sameness is linked to “one tension / two opposed wants / one test.” If a panel proposes adding multiple dramatic‑shape templates, they need to know that the macro stage (4a) must still produce a `central_tension` field (C2). Append a note: “You may propose modifying the macro prompt to generate alternative arc shapes (e.g., slow‑dread reveal, heist, investigation‑without‑answer) – but the output schema must remain `{title, premise, setting, time_of_day, central_tension}`; do not remove central_tension.” This avoids proposals that break the ledger contract.

SHOULD-FIX:
1. [Section 3, line composer params] The `max_new_tokens` is derived as `min(200, max(40, target_words*4))`. If the panel proposes raising `words_per_beat_range` (e.g., to 40‑60), the token limit may under‑allocate. Recommend adding a small note: “If you change per‑line word targets, also adjust the token cap (currently 200).” This prevents implementation surprises.
2. [Section 5, ledger contract] Although already stated, the panel could inadvertently propose renaming fields (e.g., changing `character_a_wants` to `protagonist_goal`). Add a reminder: “All existing `meta.dramatic_state` keys must keep their exact names; you may add new ones but not rename.” This safeguards C2.
3. [Section 2a] The per‑writer word averages (Grok‑4.3 → 269) might tempt a panel to propose model‑specific prompt forks (e.g., longer instructions for Grok). The prompt doesn’t forbid model‑specific tuning, but if the team wants model‑agnostic fixes, state it. Otherwise, leave as is.

OPTIONAL / NICE-TO-HAVE:
- Add a structured response skeleton (e.g., `## Verdict`, `## Ranked Fixes`, `## First Move`, `## Act‑Bridge Ruling`) to the prompt so the LLMs’ output is machine‑parseable and consistent across reviewers. Not required for quality but helpful for synthesis.

CUT THESE (over-engineering): None. All sections are necessary to ground the panel, and the level of detail in Section 2c is justified to show concrete symptoms.

[ASSUMPTION] The `costly_choice_beat` field exists in the outline/dramatic state even though the prompt snippets in Section 3 do not show it (they show `ending_change`). The panel is expected to propose binding it based on the symptom description; if the exact field name is required, the document should present a sample ledger excerpt.