# S30 hand-off — five commits in, ten to go

**Branch:** `s30-two-model-selector` (cut from `s29-clean-slate-gate` @ a63f3e7).
**Pushed:** origin/s30-two-model-selector @ `53ac152`.
**Date:** 2026-05-14.

## Commits landed (5 of 15)

| # | Hash | Subject |
|--:|---|---|
| 1 | `46edb4d` | B0: __init__.py forensic-comment scrub (S30 cleanbreak; LLM-stack deletion moved to B4b) |
| 2 | `2316760` | B1a: catalog dataclass + scan_local_llm_cache + dropdown choices + validator (offline-only) |
| 3 | `94d5d20` | B1a2: auto_download_if_missing + size estimate + disk pre-check + GatedModelError + resolve_hf_token |
| 4 | `d307348` | B1b: dynamic context-cap (catalog ContextCapVerdict + HARD_VRAM_CONTEXT_LIMIT clamp); delete MODEL_CONTEXT_CAPS / DEFAULT_CONTEXT_CAP |
| 5 | `53ac152` | B1c: loader slot primitives (unload_llm + request_slot + check_vram_fit) |

## Regression at HEAD

- Bug Bible: 23 passed / 1 skipped / 2 xfailed (held)
- Combined canonical: 197 passed / 7 skipped / 2 xfailed
- New test files: test_model_catalog_scan.py (35), test_model_catalog_download.py (12), test_loader_slot_primitives.py (13)
- Forbidden sweep: 0 runtime hits
- Audio C7 byte-identical: presumed held (default workflow uses Mistral-Nemo @ 8192 context_cap; matches prior static dict's value; real-pipeline gate deferred to B3 end-to-end + B8 close)

## Foundation in place

- `nodes/_otr_model_catalog.py` -- CuratedModel dataclass, CURATED_LLM_MODELS (6 entries), GATED_CURATED_MODELS, DEFAULT_LLM / TEST_TECHNICAL_LLM / TEST_OVERSIZED_LLM constants, scan_local_llm_cache, build_dropdown_choices, dropdown_choices, validate_model_id, estimate_model_size_gb, auto_download_if_missing, HARD_VRAM_CONTEXT_LIMIT, CURATED_CONTEXT_OVERRIDES, ContextCapVerdict, resolve_context_cap, DEFAULT_VRAM_CEILING_GB, VRAMFitVerdict, check_vram_fit.
- `nodes/_otr_model_inputs.py` -- MissingModelInputError, UnknownModelError, GatedModelError, InsufficientDiskSpaceError, VRAMFitFailedError, require_model.
- `nodes/_otr_hf_auth.py` -- resolve_hf_token (cross-platform safe).
- `nodes/_otr_model_loader.py` -- LLM_CACHE module-level dict, full-teardown unload_llm (with ipc_collect), request_slot 8-step sequence. MODEL_CONTEXT_CAPS / DEFAULT_CONTEXT_CAP deleted.

## P0 finding logged

**BUG-LOCAL-226** -- S30 plan section 2b claimed `nodes/story_orchestrator.py::_load_llm` was dead-runtime. The mandatory REVIEW-step grep at B0 kickoff caught a live caller chain:

```
OTR_LedgerScriptWriter._resolve_news_seed
  -> _fetch_rss_seed_or_die
  -> _so._fetch_science_news
  -> _llm_rank_news_candidates / _llm_rerank_with_bodies
  -> _generate_with_llm
  -> _load_llm
```

Sprint plan adjusted: new commit B4b inserted between B4 and B5 to rewire the RSS path through `_otr_model_loader.request_slot` BEFORE deleting the orchestrator's parallel LLM stack. Sprint commit count grew from 14 to 15.

## Remaining commits

| # | Status | Subject |
|--:|---|---|
| B2a  | pending | writer two-widget surface + output sockets (writer JSON wired) |
| B2b  | pending | writer internal routing + slot scheduler + new meta keys (HIGH risk) |
| B2c  | pending | delete cleanup_model_id legacy-strip loop + test residue |
| B3   | pending | cascade widget + toggle deletion + technical_model socket (first end-to-end) |
| B4   | pending | delete standalone LFC Phase 4/5/6 nodes + phase functions |
| B4b  | pending | rewire RSS news LLM path through slot scheduler + delete orchestrator LLM stack |
| B5   | pending | delete OTR_VisualLLMSelector + _POLISH_CACHE collapse (HIGH risk) |
| B6   | pending | wiring tests + structure-based guardrails |
| B7   | pending | arm forbidden-pattern sweep with S30 extinction markers |
| B8   | pending | sprint close -- final QA doc + ROADMAP refresh |

## Plan vs reality deviations recorded

1. `tests/test_dropdown_guardrails.py` listed in CLAUDE.md + plan does not exist. Substituted `tests/test_workflow_json_guardrails.py`.
2. `tests/v2/test_audio_byte_identical.py` listed in plan does not exist. Substituted `tests/test_audio_byte_identical.py`.
3. Branch cut from `s29-clean-slate-gate` tip (not `v2.0-alpha`) since S29 has not been merged to `v2.0-alpha` yet. Tree state is identical to what post-merge would be.
4. B0 narrowed from plan scope (LLM-stack deletion moved to B4b after the audit-miss finding).
5. B1c's `_estimate_resident_gb` divides by 2 (matches OTR's 8-bit quantization default) rather than treating BF16 download size as resident size. Documented in code.
6. B1c's `unload_llm` handles both the new LLM_CACHE and the legacy orchestrator's `_LLM_CACHE` directly -- the "forward-shim" plan element from B0 is no longer needed.

## How to resume

```cmd
cd /d C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio
git checkout s30-two-model-selector
git log --oneline -6
```

Next commit (B2a) target: `nodes/OTR_LedgerScriptWriter.py` lines 120, 330, 1163 + `workflows/otr_scifi_16gb_full.json`. Use plan section 2a-tris for the widget order pin and section B2a for the WIRE step.
