<!-- requested_model: deepseek/deepseek-v4-pro | resolved_model: deepseek/deepseek-v4-pro-20260423 -->

VERDICT: no. The plan lacks concrete prompt sources for scene stills, episode-folder storage mechanics, wiring for wan_i2v to consume scene stills, and sequencing guarantees. Critical gaps must be closed before build.

MUST-FIX BEFORE BUILD:
1. [S1] No prompt composition for scene stills exists. The current image-prompt node (`otr_meta_brief_image_prompt.py`) only handles character portraits; the render driver’s scene prompt composition is for video text prompts, not still-image prompts. A function must be created that generates still-image prompts for open/announcer/outro beats using a fixed macro-radio subject template (e.g., “a vintage radio set warming up…”) with optional brief palette/atmosphere terms and a trimmed era/style tail. Without this, no scene stills can be minted.
2. [S2] Episode-folder storage is not implemented. The dispatcher (`otr_image_gen_dispatcher.py`) saves to a global `output/otr/stills/` directory. It must be modified to accept an episode-specific output directory (e.g., `episodes/<ep>/stills/`) and save stills there. The episode slug must be passed through the graph (e.g., via `image_policy` or a new input). The global pool should be dropped to avoid confusion. Ledger `images[]` entries must carry the episode-local path.
3. [S3] wan_i2v init_image from scene stills is not wired. The render driver’s `build_request_from_shot` only sets `init_image` from character portraits (via `_portrait_index`). For wan_i2v shots with roles like `scene_broll` or `music_visual`, `init_image` will be empty, causing wan_i2v to fail (it requires an image). The image ledger must be extended to include scene stills keyed by `beat_id` (or `role`+`beat_id`), and the render driver must look up the still for the shot’s beat. The dispatcher must generate these stills and stamp them accordingly.
4. [S3] still_kenburns must accept an external init_image. The plan states it will “drift over the scene still”. [ASSUMPTION] The current `still_kenburns` engine may not support an `init_image` input; if not, it must be modified to accept one. This is a dependency for the fallback look.
5. [Sequencing] Still generation must complete before video rendering. The plan references `image_done` gating. The ComfyUI workflow must be wired so that the video render node (e.g., `OTR_VideoRenderBatch`) waits for the `image_done` token from `OTR_ImageGenDispatcher`. [ASSUMPTION] The video render node may lack a gate input; if so, one must be added. Otherwise, video may start before stills are ready, causing missing init_images.
6. [Acceptance] At least one video engine that can consume a still must be active and used. The acceptance requires a trace-stamped `init_image` on a beat. If `wan_i2v` is gated off (`OTR_ENABLE_WAN_I2V` not set) and `still_kenburns` cannot consume an external still, no engine will use the still. Ensure `wan_i2v` is enabled for the production render, or make `still_kenburns` consume the still as a fallback.

SHOULD-FIX:
1. [S5] Era-tail diet is not decided. The current era tail is heavy (~280 chars) and may over-paint stills. Define a trimmed tail for stills (e.g., atmosphere line + top-2 palette + top-2 lighting, or use `finish_visual_prompt` with `max_chars`). This prevents the Mars-episode “everything red” issue.
2. [S4] M4->HuMo creative seam is out of scope for the still spine. Clarify in the plan that it will be addressed separately to avoid scope creep.
3. [S1] The still prompt composition should ensure the macro-radio subject remains dominant. Place the concrete subject first and limit additional brief terms to a few palette/atmosphere words to avoid dilution.

OPTIONAL / NICE-TO-HAVE:
- [S3] LTX img2vid conditioning: if the installed wrapper exposes an image input, wire it; otherwise, LTX stays text-only as planned.
- [S3] 3D mesh-gen future-proofing: design still ledger keys to be compatible with future 3D routing (e.g., using `object_id` for characters, `beat_id` for scenes).

CUT THESE (over-engineering):
- The elaborate per-beat prompt derivation from the full brief (using `get_story_brief_ltx`, beat clauses, etc.) for still images is unnecessary. The June-5 macro radio look is a fixed subject; a simple template with the episode’s palette injected is sufficient and reduces risk. Cut the detailed brief integration for still prompts; use a fixed subject + palette + trimmed era/style tail.
- The per-surface tail discussion (stills full, video capped) can be simplified: use a single trimmed tail for stills and keep the existing video prompt composition separate. No need for a multi-surface tail system now.