<!-- requested_model: openai/gpt-5.5 | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. The document does not define buildable data contracts or wiring for scene stills, episode-local storage, or init_image selection; current grounded code would still feed portraits only, mostly from the global still path, and Wan/LTX would not receive scene stills.

MUST-FIX BEFORE BUILD:
1. [S1] Scene/open still generation is not buildable against current image nodes. Grounding: `otr_meta_brief_image_prompt.py` emits only `char_id -> {prompt,prompt_hash,source}` from `derive_image_prompts`; `otr_image_gen_dispatcher.dispatch_images()` iterates those items as character objects and hardcodes `role = "character_video"`. There is no beat/open scene object schema, no `beat_id`, no `shot_id`, no scene role, and no dimensions. Concrete fix: define and implement a unified still-object schema, e.g. `{"object_id","kind":"portrait|scene_open|scene_beat","role","beat_id","shot_id","prompt","prompt_hash","w","h","aspect_policy","source"}`. Extend/replace `OTR_MetaBriefImagePromptGen` or add a scene-still prompt node to emit open/announcer/outro beat still objects, then update `OTR_ImageGenDispatcher` to dispatch by object role/kind instead of assuming every item is a character portrait.

2. [S3] Current video request plumbing cannot feed scene stills to Wan or Ken Burns. Grounding: `render_driver.build_request_from_shot()` sets `init_image = _portrait_index(ledger).get(char_id, "")` for every shot, then `build_request()` puts that path into `asset_refs.init_image`. For `wan_i2v`, `eng_wan_i2v.required_inputs = ("init_image",)` and `_init_image_ref()` reads only `asset_refs["init_image"]`; music/scene beats without a `char_id` will get empty init images and fail closed. Concrete fix: add a still index keyed by `beat_id`/`shot_id` from `ledger["images"]["images"]`, and select init images by engine family: `audio_driven_face -> portrait`, `image_to_video/static_motion -> scene still`, optional LTX img2vid -> scene still. Log a LOUD warning and fall back to text/floor when no still exists.

3. [S2] Episode-local storage is not specified enough to implement and contradicts current dispatcher behavior. Grounding: `dispatch_images(..., output_dir=None)` calls `_pl.stamp_portrait(..., output_dir=output_dir)` and the module doc says current writes are `output/otr/stills/{portrait_content_hash}.png`. No episode directory is passed into the node. Concrete fix: pass/derive `episode_dir` into the dispatcher and write every generated still to `output/otr/episodes/<episode>/stills/`. Ledger image rows must store the episode-local path. If a global content cache is retained, separate it from the per-episode materialized copy/link and never let the ledger point only to the global cache path.

4. [S2] Cache semantics will break per-episode inspectability unless redesigned. Grounding: `request_cache_key()` is `(role, object_id, prompt_hash, seed, engine_id, engine_version)` and `cache_index[key] = image_id`; it does not include episode id, dimensions, kind, or local path. A reused portrait/scene object can resolve to an old/global artifact if cache state is carried forward. Concrete fix: keep the content cache keyed globally if desired, but always materialize/copy/link the cached content into the current episode `stills/` folder and stamp a new/current ledger row with local path. Include `kind`, `w`, `h`, and role/object identifiers in the render request/cache key where they affect pixels.

5. [S1/S3] Still dimensions/aspect are missing. The current image dispatch request contains only `request_id`, `role`, `object_id`, `engine_id`, `prompt`, `prompt_hash`, and `seed`; no width/height/aspect is passed. The problem statement says the current Flux default is portrait `832x1216`, but open/scene stills must be full-frame macro radio opens matching landscape video. Concrete fix: add per-still `w/h/aspect_policy` to the still-object schema and make each image engine honor it. Use landscape dimensions for scene/open stills, portrait dimensions for character stills.

6. [S3] LTX image conditioning is not implementable as written. Grounding: `eng_ltx_video.LtxVideoEngine.required_inputs = ("text_prompt",)` and `_build_graph()` is strictly text-to-video: checkpoint, CLIPLoader, CLIPTextEncode, EmptyLTXVLatentVideo, LTXVConditioning, KSampler, VAEDecode. There is no `init_image` read path. Concrete fix: either explicitly leave LTX text-only for this build, or implement a separate optional img2vid branch with verified node candidates, request field handling, required/optional input policy, graph tests, and fail-soft fallback to the current text graph. Do not count LTX as still-conditioned until that branch exists.

7. [Routing/owner + Constraints] Workflow wiring is underspecified. The constraint requires changes in node code plus saved `workflows/otr_scifi_16gb_full.json`, no runner patches. The document does not name the new node outputs/inputs or exact graph links from scene prompt generation -> dispatcher -> patched ledger -> video render. Concrete fix: specify and implement the saved workflow edits: ledger into still prompt node, image policy into dispatcher, dispatcher patched ledger into the video render path, and an `image_done`/opaque gate into the render node to force image generation before heavy video. [ASSUMPTION] Verify the actual video render ComfyUI node has or can accept such a gate input.

8. [Routing/owner] The ImageDirector policy currently cannot choose the correct image model per still type. Grounding: `OTR_ImageDirector` emits `announcer_image_model`, `music_image_model`, and `other_beats_image_model`, but `dispatch_images()` ignores the announcer/music slots and always uses `other_beats_image_model` plus `role = "character_video"`. Concrete fix: dispatcher must resolve the image engine from each still object’s role/slot: announcer opens from `announcer_image_model`, music opens from `music_image_model`, character/scene/background from the appropriate other slot or a new explicit scene slot.

9. [S5] Era-tail diet is only a question, not a build spec, and current helper does not match the proposed trim. Grounding: `get_era_tail()` appends `atmosphere_line`, top 3 `visual_palette`, and all v1 lighting+atmosphere terms returned by `get_story_brief_lighting()`. `finish_visual_prompt()` has no knobs for “top-2 palette + top-2 lighting.” Concrete fix: add explicit parameters or separate helpers for surface budgets, e.g. `finish_visual_prompt(meta, prompt, max_chars=0, style_tail=True, era_profile="still_full|video_short")`, and update portrait, scene-still, and video call sites consistently.

10. [Acceptance sketch] “At least one video engine consumed a still” is too weak and will allow the current failure mode to pass. Operator directive says stills must feed downstream engines that can take images. Concrete fix: acceptance must assert per-engine/family behavior: HuMo talking beats use portrait init images; Wan image_to_video beats use scene still init images when enabled; Ken Burns/floor uses the scene still; LTX uses scene still only if img2vid branch is verified, otherwise logs text-only fallback. Add trace/manifest fields proving the exact `init_image` path per rendered shot.

SHOULD-FIX:
1. [S4] The M4->HuMo seam appears stale or ambiguous. Grounding: `render_driver.build_request_from_shot()` already reads `shot["creative"]["text_prompt"]`, sets `req["text_prompt"]`, and stamps prompt metadata before LTX/Wan-specific composition. If the complaint is specifically that HuMo ignores `text_prompt`, verify HuMo’s request consumption before adding work here. Concrete fix: remove S4 from this still-spine build unless a failing test shows HuMo needs and can use that prompt. [ASSUMPTION] HuMo engine code was not provided.

2. [S1] “Moved INTO the image-prompt node, or shared helper both call?” must be decided before build. Current render_driver has private prompt composition logic for LTX opens inside `build_request_from_shot()`. Duplicating it in the image prompt node will drift immediately. Concrete fix: extract the subject/brief/beat/era/style composition into a pure helper used by both render_driver text prompts and scene-still prompts.

3. [S3] `build_clip_manifest()` currently reports `init_image` from `_portrait_index(led).get(row_char, "")`, so it cannot prove scene-still conditioning for Wan/Ken Burns. Concrete fix: add request trace fields and manifest fields for `init_image`, `init_image_kind`, `init_image_object_id`, and `init_image_path` from the actual request, not recomputed only from `char_id`.

4. [S1/S2] Define deterministic object IDs for scene stills. Without a contract, cache keys and ledger lookup will drift. Concrete fix: use stable IDs such as `still_<beat_id>_open`, `still_<shot_id>`, or `scene_<beat_id>` and store both `object_id` and `beat_id`.

5. [Constraints] Fail-soft behavior needs a specific missing-still path. Concrete fix: if still minting fails, stamp a ledger warning and let render_driver select current text-only/floor behavior. Do not leave `wan_i2v` selected with empty `init_image` unless the LOUD fallback decision is expected and tested.

6. [S3] 3D is parked, but the ledger schema chosen now should not be portrait-only. Concrete fix: image rows should include `kind`, `object_id`, `char_id` where applicable, `beat_id` where applicable, `content_hash`, `path`, and `provenance`, so future mesh routing can consume character stills without parsing scene still rows.

7. [S1] The “June-5 5-layer composition” is underspecified for open stills. The legacy file’s actual `compose_shot_prompt()` order is portrait, scene visual, shot hint, era tail, style tail; for macro radio opens there is no portrait. Concrete fix: define the exact scene/open still layer order separately: concrete macro-radio subject, setting/atmosphere terms, shot/framing hint, trimmed era tail, style tail.

8. [Constraints] Add tests that match the new contract. Minimum CPU tests: scene prompt objects emitted for open/announcer/outro; dispatcher writes mixed portrait+scene rows; request builder picks scene still for Wan and portrait for HuMo; missing still degrades loudly; episode-local path is stamped.

OPTIONAL / NICE-TO-HAVE:
- Add a human-readable `stills_manifest.json` under the episode `stills/` folder listing prompt, prompt hash, object id, role, source beat, engine, and local filename.
- Add a thumbnail/contact sheet for operator inspection.
- Add an environment override for the macro-radio open subject, but keep the deterministic default.

CUT THESE (over-engineering):
1. [S3] Cut LTX img2vid implementation from the first build unless the wrapper node is already verified. Safe because Wan i2v and still_kenburns can satisfy image-conditioned motion immediately, while LTX already has a working text path. Keep a loud “LTX img2vid unavailable; using text path” trace.

2. [S3] Cut 3D implementation work from this build. Safe because 3D is explicitly parked. Only preserve schema compatibility in ledger image rows.

3. [S1] Cut “still per every text-engine beat” as a hard first-build requirement if render time/VRAM becomes a blocker. Safe minimum is open + announcer/outro + any Wan/Ken-Burns-selected beats. Do not cut the requirement that every image-capable rendered beat must receive an init image when available.

4. [Routing/owner] Cut any new “primary model” abstraction. Current registries already support model-agnostic image/video engines; the needed work is schema and routing, not another selection layer.