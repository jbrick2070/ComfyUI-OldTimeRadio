# Pass 08 judgment -- Sol-grounded convergence before live requalification

Driver, coder, and sole judge: Sol. Reviewer: Antigravity review-only on exact
`Gemini 3.5 Flash (High)`, grounded from the clean detached worktree recorded in
`pass08_plan.md`. This judgment covers the static R2/R3 audit and implementation
gate; live requalification remains a separate terminal receipt.

## Reviewer findings accepted after Windows grounding

1. Both compact line-patch schemas used an LMFE-incompatible redundant
   regex-plus-length contract. The sibling audit found exactly those two
   production schemas.
2. Scifi Codex lacked final character-word ownership and the shared tail ignored
   a valid producer band. Character words now own the target-relative contract;
   announcer words remain separately reported.
3. A real zero onset was collapsed into the missing-timing branch.
4. The six banks did not share a durable music contract. The first timing writer
   needed manifest reconciliation, and the final post-audio wire join needed
   identity-gated music plus mirror transfer.

## Reviewer claims corrected or discarded by Sol

- `scifi_news_pro` is the Fable2 lane, not Scifi Codex.
- The four inline banks did not lose both bookends from the audible master.
  Their opening and closing WAVs were present and the master tail proved they
  were mixed; the confirmed defect was absent ledger rows/mirrors plus a dropped
  interstitial.
- P10 cannot bypass final hygiene: every accepted patch is merged through the
  complete `_validate_script_post` boundary.
- Manifest cue IDs are already unique by validation, so no UUID/composite-key
  redesign was warranted. Authored `cue_spec_sha256` is the required stale-audio
  gate; anchors may legally repeat and are therefore indexed losslessly.
- Music remains an optional creative bus. The fix makes rendered music
  ledger-complete without inventing a new OBS liveness blocker when no music bus
  is wired.

## Implemented convergence

- Compact repair schemas are LMFE-safe. Scifi Codex has bounded dynamic P10 word
  fitting, full revalidation, fresh recount, alternate-slot repair, and a
  truthful closest-valid floor.
- Producers emit canonical durable cue IDs, placements, and anchors. Synthesized
  legacy cues receive deterministic identities and sentinel anchors.
- A shared manifest reconciler materializes legacy rows before SceneSequencer
  timing and refuses authored identity mismatches. Dialogue anchors insert music
  without losing their voice clip. EpisodeAssembler alone promotes master
  timing and mints placement-derived mirrors. ShotLock forwards only
  same-episode, identity-matched music rows and mirrors.
- Zero onset is preserved as known timing with no fabricated title gap.

## Static qualification receipts

- Focused music/ledger/media set: `261 passed` in `19.95s`.
- Full project suite: `8282 passed, 33 skipped, 1 xfailed` in `240.88s`.
- Bug Bible: `17 passed, 18 skipped, 3 xfailed`.
- Workflow/link/widget family: `167 passed, 3 skipped`.
- Canonical workflow SHA-256 remains
  `222d19478a308c91171dfcbdccbec01c55dd639283e2550ebb59eb9842d0882d`.
- No workflow node, link, widget, or positional value changed.

## Judge verdict

Static R2/R3 convergence is **PASS**. Every accepted reviewer finding was
verified against the real Windows files and fixed across its confirmed sibling
surface; reviewer misreads were discarded. The checkpoint is eligible to push.
Live `scifi_news` qualification must resume only after a fresh selective GPU
reset. Any new live-discovered defect reopens a targeted exact-lane sibling
audit before qualification can continue to `scifi_news_pro`.
