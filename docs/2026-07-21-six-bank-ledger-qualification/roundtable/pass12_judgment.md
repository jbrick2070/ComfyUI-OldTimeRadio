# Pass 12 judgment -- six-bank 320-word delivery contract

## Verdict

The 190/320 result is a measurable completion failure, not subjective story
taste. Preserve dynamic LLM repair for quality and length, but require finite,
truthful progress into the declared band before any media consumer runs.

Antigravity remained review-only on exact `gemini-3.5-flash-high` in the clean
detached worktree
`otr-kibitz-livebug13-wordfit320-20260721-flash-high` at committed HEAD
`651118ef2a16d99812eaa34a6e6d14adb0a7b218`. Sol checked every review claim
against the real Windows repository and was the sole driver, coder, and judge.
No Claude model or credit was used.

## Grounded findings

Accepted:

- Codex P10 stopped after one failed creative/technical pair, patched several
  rows all-or-nothing, and returned its closest out-of-band artifact;
- Fable2 treated residual total drift as advisory and lacked a durable target
  owner before final proof/hash assembly;
- the four inline banks shared a length editor that could shorten but not expand,
  and stamped actual/reflection surfaces before later legal text mutations;
- freeze had no final requested-length backstop before media readiness;
- all downstream consumers already share the writer/freeze authority, so no
  canonical workflow edit is needed.

Corrected or discarded by Sol:

- captions correctly consume canonical display text, while `text_for_tts` is a
  source-bound pronunciation projection;
- content-owned rows must be repaired and resealed inside their producer, not
  mutated by a generic shared tail or freeze;
- Fable2 P3/P5 receipts remain truthful history and do not become word-fit
  receipts;
- custom runner codas must not also receive an inline outro;
- no new skeleton state, dramatic-question behavior, or nonexistent graph
  validator is part of this repair.

## Implemented contract

- one shared integer law yields 163..200 at 180 and 289..356 at 320;
- Codex and Fable2 use one-row, fresh-slot, strict-progress repair and typed
  pre-assembly exhaustion;
- Fable2 rebuilds its complete final draft, proof, authorship, and hash surface;
- inline banks fit after story QA/hygiene, re-scour, then stamp the actual and
  build story reflections from the same final rows;
- all repair ladders reject non-progress, fake commercials/products, filler,
  invented numeric claims, markup, and new visual/canon claims;
- freeze performs a read-only hash-bound recount after final metric refresh and
  returns `needs_full_rerun` before video readiness on drift;
- historical no-owner fixtures retain an explicit compatibility path, while new
  production ledgers declare an owner and are enforced.

## Verification state

Producer, inline, ordering, and freeze-focused tests are green. The full project
suite passed 8,339 tests with 33 skipped and one expected failure. BUG-12.70's
standard Bible gate passed 17 tests with 23 route-local skips and three expected
failures, and its direct OTR guard passed. The explicit canonical workflow gate
passed 48 tests at 23 nodes / 58 links; JSON round-trip succeeded, link/input and
widget ownership stayed valid, and SHA-256 remained
`f9d9c2c3a101ec607c9658456f6e191a164d8214be7b6d560bc68975d0511e9a`.
Commit/push and the real six-bank 320-word OBS receipts follow this judgment.
