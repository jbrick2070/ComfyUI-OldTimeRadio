# Pass 11 plan -- positioned crossfade timeline repair

## Live admission

The canonical `scifi_news` prompt
`a5e6e996-8f1e-4eb4-aff2-29486d4fd28c` completed the compact P5 path, story,
audio, fifteen shots, silent video, captions, and credits. Terminal mux then
rejected the body because its 2,889 frames / 115.56 seconds exceeded the
114.5433-second master by 1.0167 seconds before credits accounting. The two
durable 0.5-second music/dialogue crossfades explain exactly 25 duplicated
visual frames. VRAM remained low and stable.

## Scoped R2/R3 audit

Before editing or rendering, run exact `gemini-3.5-flash-high` review-only in a
clean detached worktree over the shared six-bank media tail. Inspect manifest
ownership, positioned planning, filesystem master reconciliation, QA semantics,
mux tolerance, credits duration ownership, canonical workflow wiring, and every
bank route. Sol grounds all claims against the real Windows files and remains
the sole driver, coder, and judge.

## Root-fix shape

1. Preserve the sum of full shot requests as render-work telemetry.
2. For complete positioned manifests, derive final CFR output frames from the
   durable post-audio ledger's accepted episode duration.
3. Partition positioned visible slots by stable next-start ownership; do not
   stretch a short requested clip across a real gap.
4. Let actual-master reconciliation shrink or grow only positioned manifests;
   preserve legacy sequential grow-only behavior.
5. Teach QA the difference between requested, rendered, planned-visible, and
   overlap-trimmed frames.
6. Keep the strict mux gate and credits declaration unchanged.
7. Add pure and real-ffmpeg regression coverage, promote the live rule, run all
   static gates, commit/push, reset, then qualify all six banks at 320 words.

No canonical workflow edit is expected because all six already share the same
wired render/composite/mux tail.
