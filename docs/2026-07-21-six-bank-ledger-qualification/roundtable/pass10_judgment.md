# Pass 10 judgment -- compiler-owned compact P5 transport

## Verdict

Implement the compact P5 transport, then resume live qualification only after
all static gates pass. The live failure was a transport/liveness defect, not a
quality verdict and not GPU thrashing.

Antigravity remained review-only on exact `gemini-3.5-flash-high`. Sol grounded
the review against the real Windows files and was the sole driver, coder, and
judge.

## Grounded findings

Accepted:

- P5's prompt and response duplicated score-owned schema and metadata;
- whole-artifact typed repair reintroduced both the failed artifact and broad
  request authority, recreating the output/context squeeze;
- a compact compiler-owned transport is structurally stronger than increasing
  one cap or deleting one prompt field;
- liveness must be a finite flat ladder, never recursive self-invocation;
- malformed raw drafts should not be carried into a fresh repair turn.

Corrected or expanded by Sol:

- the first live attempt also proved under-reserved whole-artifact output, not
  only repair-prompt bloat;
- the configured creative and technical widgets currently resolve to the same
  local Gemma 4 12B model, so technical is a fresh attempt, not a larger window;
- compact repair still needs a trusted graph/fact/word authority because the
  full validator must remain fail-closed;
- `scifi_news` reaches the Codex P5 circuit and is affected;
  `scifi_news_pro` reaches Fable2 and the four legacy banks use inline writers,
  so no sibling transport edit is warranted. All six share the later ledger and
  media tail, which remains unchanged.

Discarded:

- spoken-hygiene `CodexGraphError` is a code-invariant failure, not an ordinary
  taste rejection to be retried forever;
- `RadioScoreV4` and `BeatPlanV4` do not contain the proposed traits or compose
  flags, so there is no score-owned value to preserve for those neutral script
  fields;
- merely removing `original_request` would leave the whole-artifact output tax
  and duplicated authority intact.

## Implemented root fix

- strict `ScriptTextDraftV4` wire rows contain only `line_id` and authored text;
- Python requires exact unique closed-graph coverage, maps by ID, and compiles
  every score-owned `ScriptArtifactV4` field;
- the existing complete graph, roster, fact, hygiene, and craft validator runs
  on the compiled artifact;
- typed repair uses compact authority, preserves valid parsed text, and omits
  malformed prefixes and raw result schema;
- prompt-fit and full-output-budget markers apply to base, retry, and repair;
- the dynamic output budget now scales with authored words and row envelopes;
- one creative ladder may restart once on the technical slot, with truthful
  journal receipts and no recursion; existing row-local craft recovery remains.

No canonical workflow edit was needed: the already-wired
`scifi_news_circuit` owns this internal transport.

## Verification receipts

- focused sci-fi lane, bank-route, and runner-truthfulness gate: 165 passed;
- exact on-disk Gemma 4 12B tokenizer proof: a 900-word, 24-row base and
  semantic-repair envelope both retain the full 3,208-token reservation inside
  the 8,192-token context;
- full project suite: 8,325 passed, 33 skipped, 1 expected failure;
- Bug Bible standard gate: 17 passed, 21 skipped, 3 expected failures;
- BUG-12.68 executable OTR gate: 1 passed;
- comprehensive workflow test gate: 170 passed, 3 skipped;
- JSON round-trip, live INPUT_TYPES/widget, and link referential-integrity
  audit: green (23 nodes, 58 links);
- canonical workflow SHA-256 remained
  `f9d9c2c3a101ec607c9658456f6e191a164d8214be7b6d560bc68975d0511e9a`;
- Antigravity review spend: $0 local lane.

Live qualification remains the final gate. Any new live functional defect
again stops the campaign and triggers an exact-Flash-High targeted sibling
audit before qualification continues.
