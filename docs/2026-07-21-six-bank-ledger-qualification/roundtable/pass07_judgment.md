# Pass 07 judgment -- grounded convergence

Antigravity verdict: not build-ready without wiring fixes. Sol checked every
claim against the real Windows workspace and acted as sole judge.

Confirmed and incorporated:

1. Generic structured-call handling hard-coded P5/P7/P9 as full script
   artifacts. P7/P9 now use a direct schema-bound patch call; only P5 retains
   complete-artifact normalization/repair behavior.
2. Restoring `prior` after a failed quality retake caused an unchanged
   re-audit. Two failed patch slots now end that quality loop immediately at
   the best-valid floor.
3. P7/P9 lacked an explicit colder technical-slot attempt. The patch helper now
   rotates creative to technical and records both attempts.
4. Model-loader normalization could erase a message-list marker. Every local
   and remote backend captures the marker before normalization and enforces it
   against context and provider output caps before any call.
5. Provider-specific wrappers hid `GenerationContextOverflowError`. Quality
   handling now walks the cause chain and records a capacity floor; P6/P8
   provider failure cannot kill an already ledger-valid story.
6. Pipeline and pack prose still required complete P7/P9 artifacts. Runtime
   metadata and the engine-spec amendment now describe bounded line patches
   and mandatory fresh P6/P8 rejudgment.
7. Fact context needed both target mappings and fact IDs named in audit text.
   The compact prompt carries exact claims/quotes/numeric tokens for that set.

Corrected reviewer overreach:

- Google did not currently erase the original list before measuring it; early
  marker capture was nevertheless adopted as the uniform sibling contract.
- Capacity comes from each loaded backend's real `context_cap`, not a missing
  scifi-local constant.
- No arbitrary two-cycle global cap was accepted. Valid patches retain the
  existing dynamic quality-cycle budget; capacity/two-slot impossibility alone
  short-circuits.
- There is no `stage_direction` script field to patch. Only `line.text` is
  writable.
- No workflow, readiness, ledger, or OBS mutation belongs inside P7/P9.

Verification receipts:

- Focused lane/routing/cross-backend tests: `188 passed`.
- Full project suite: `8255 passed, 33 skipped, 1 xfailed`.
- Bug Bible: `17 passed, 18 skipped, 3 xfailed`; promoted as BUG-12.62.
- Canonical workflow validator: `23 nodes`, `57 links`, zero widget drift;
  JSON round-trip, link references, wired input names, and widget vectors clean.
- Canonical workflow SHA-256 unchanged:
  `222d19478a308c91171dfcbdccbec01c55dd639283e2550ebb59eb9842d0882d`.

Antigravity's local runner exposed no monetary-cost receipt. It did record the
resolved lane exactly as `Gemini 3.5 Flash (High)` for both R2 and R3.
