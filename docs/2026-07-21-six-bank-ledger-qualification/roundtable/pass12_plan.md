# Pass 12 plan -- six-bank 320-word delivery contract

## Live admission

The canonical `scifi_news` qualification prompt
`f62c1177-a40a-4f9e-a9ac-f9c3bcfad717` completed its source-grounded and dynamic
story passes, then composed only 190 character words for an explicit 320-word
request. The accepted delivery band is 289..356. The writer reported the miss as
advisory and entered reflection; Sol stopped the run before TTS or media, reset
the server, and confirmed baseline VRAM.

## Scoped R2/R3 audit

Before further editing or rendering, run exact `gemini-3.5-flash-high`
review-only in a clean detached worktree over all six bank routes:
`media_archive`, `original`, `public_domain`, `shakespeare`, `scifi_news`, and
`scifi_news_pro`. Inspect delivery completion, ledger ownership, spoken-hygiene
bypasses, stale receipts/seals/hashes, row-local failures, readiness mutations,
and downstream TTS/video/caption/credits/mux/OBS consumers. Sol grounds every
claim against the real Windows checkout and remains sole driver, coder, and
judge. No Claude lane is used.

## Root-fix shape

1. Put the target-relative integer delivery law and hash-bound receipts in one
   dependency-light shared module.
2. Give Codex and Fable2 producer-owned, one-row, creative/technical repair
   loops before their final proof/hash assemblies.
3. Give the four inline banks the same progressive repair law after story QA and
   before final reflections/readiness.
4. Continue through malformed or non-progress calls for a finite dynamic budget;
   retain every validated improvement and fail truthfully on exhaustion.
5. Reject filler, duplication, fake advertisements/products, invented claims,
   markup, stage directions, and new visual/canon surfaces.
6. Recount and bind reflections to the exact final rows, then add a read-only
   freeze backstop before media readiness.
7. Keep the canonical workflow unchanged because every consumer already follows
   the writer/freeze output; verify its hash, links, inputs, and widgets.
8. Promote the admitted rule, run focused/full/Bible/workflow gates, commit and
   push, reset, then qualify all six banks at 320 words through OBS publication.
