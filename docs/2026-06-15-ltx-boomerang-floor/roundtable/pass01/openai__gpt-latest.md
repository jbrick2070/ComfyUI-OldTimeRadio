<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. The plan is directionally plausible, but it is not build-ready because the core half-length calculation, metadata stamping, decode-floor change, and i2v class-resolution sequencing are underspecified or wrong against the provided `eng_ltx_video.py`.

MUST-FIX BEFORE BUILD:
1. [Proposed restore #2 / THE FORK] Half-render length is not specified and cannot be implemented by simply feeding the final target into `_ltx_frame_length`. Current `render_clip` computes `length = _ltx_frame_length(plan["target_frame_count"], self.target_fps)`, which floors short asks to 169 by default. To get a 193f boomerang, the rendered source must be 97f because `2*97-1=193`. Concrete fix: add an explicit helper for boomerang source length, e.g. `source_ask = (target_frame_count + 1) // 2`, then snap/floor/cap that source length to an `8n+1` value before graph build, and set final returned `frame_count` from the encoded mirrored array. Do not leave this as “render ~half-length”.

2. [Proposed restore #2 / Questions #1] The proposed mirror expression `frames[-2::-1]` is not equivalent to the stated ffmpeg filter for all source lengths if the intended dropped duplicate is the midpoint/turnaround frame. The ffmpeg filter is `[b]reverse,trim=start_frame=1`, which reverses the whole input then drops the first reversed frame, i.e. drops the original last frame. For frames `[0..L-1]`, output is `[0..L-1] + [L-2..0]`. `frames[-2::-1]` matches that, but the plan text says “minus duplicate midpoint”; the duplicate being removed is the original last/turnaround frame, not a midpoint. Concrete fix: correct the wording and add a unit test with a numeric frame index array proving `[0,1,2,3] -> [0,1,2,3,2,1,0]`.

3. [THE FORK / Working recommendation] The recommended “(A)+(B) hybrid” is not a concrete implementation and risks silently changing the decode floor for all LTX paths, despite current source comments explicitly saying “The decode floor governs BOTH paths (i2v included -- do not touch)” and “do NOT touch the 169f floor.” Concrete fix: choose one implementation before build. Smallest safe change: implement boomerang-only source-length selection inside `render_clip` and leave `_ltx_frame_length` unchanged for non-boomerang. Add a dedicated helper/env for boomerang minimum, e.g. `OTR_LTX_LOOP_MIN_DECODE_FRAMES`, defaulting to the proven 97 at 832x480 only, with LOUD fallback to non-loop or full-floor path if decode fails. If choosing canvas-aware global floor, update the contradictory comments and tests that encode the 169 invariant.

4. [THE FORK option A] “Canvas-aware floor -- lower `OTR_LTX_MIN_DECODE_FRAMES` at native 832x480” cannot be done inside current `_ltx_frame_length` as written because that helper only accepts `(target_frame_count, fallback)` and has no width/height. Concrete fix: either change `_ltx_frame_length(target_frame_count, fallback, width=None, height=None)` and update all callers/tests, or add a separate boomerang/canvas-aware helper called after `width,height = self._dims(request)` and after the multiple-of-32 normalization.

5. [Proposed restore #3 / Invariants] “stamp `ltx_loop_via_reverse` for the ledger/trace” is not connected to any returned structure in the grounded code. `render_clip` returns only `{"out_path": path, "frame_count": n}` and `canonicalize/_clip_from_raw` does not propagate arbitrary raw fields. Concrete fix: identify the actual ledger/trace plumbing and add the field there, or at minimum include it in the raw return and update `_clip_from_raw` only if downstream ledger reads canonical clip fields. verify: where ledger fields are collected from engine raw/canonical output.

6. [Proposed restore #1 / LtxOrbitEngine hidden dependency] Adding `_loop_via_reverse()` as a static/env helper on `LtxVideoEngine` will also affect `LtxOrbitEngine`, because `LtxOrbitEngine` inherits `render_clip` unchanged. The plan does not state whether `ltx_orbit` should boomerang. Concrete fix: explicitly decide. If only `ltx_video` should loop, gate on `self.name == "ltx_video"` or add a class attribute. If both should loop, add tests/ledger naming that cover inherited engines.

7. [Current render path / Proposed restore #2] The plan ignores the existing i2v class resolution path. In current `render_clip`, `load()` caches only `_node_candidates_sampling()`. When `use_i2v` is true, classes are resolved from `_node_candidates_i2v()`, which includes the active sampling set plus `loadimage/img2vid`. Any implementation that computes boomerang length before `use_i2v/classes` must preserve this order and pass the chosen source length into `_build_graph_i2v`. Concrete fix: place boomerang length calculation after `width,height` normalization but before `_build_graph/_build_graph_i2v`, and do not reuse the non-i2v cached classes for i2v.

8. [THE FORK / Questions #3] Fallback behavior is underspecified. “falling back to (C) only if a sub-97 decode probe fails” implies a runtime probe or retry path, but no retry mechanism exists in grounded `render_clip`; `run_graph` failures currently propagate. Concrete fix: either remove runtime fallback language and require a build-time GPU smoke proving the chosen minimum, or implement an explicit try/except around `run_graph` that logs LOUD, reruns with floor-safe length, and guarantees patcher cleanup/VRAM checks still happen. Do not imply automatic fallback without code.

9. [Invariants / blur fix] The plan states the blur fix renders at native 832x480 via `OTR_LTX_RENDER_CANVAS`, but the grounded `eng_ltx_video.py` only reads dimensions from `request.canvas` in `_dims`; there is no shown `OTR_LTX_RENDER_CANVAS` handling in this file. Concrete fix: verify where `OTR_LTX_RENDER_CANVAS` is applied before requests reach `render_clip`. If not already upstream, do not base canvas-aware floor logic on an env var that this engine never reads.

SHOULD-FIX:
1. [Questions #1] Add an explicit guard for degenerate frame arrays before mirroring. Current LTX min is 9, so normal graph output should have at least 9 frames, but encode/test fakes may return 0/1 frames. Concrete fix: if `len(frames) < 2`, skip mirror or raise a named LOUD error before `frames[-2::-1]`.

2. [Questions #1] Preserve the final `8n+1` contract explicitly. If the source length is `8k+1`, `2*source-1 = 16k+1`, which is also `8n+1`. But if the source helper ever returns a non-`8n+1` value because of cap/floor changes, this breaks. Concrete fix: assert/log `source_length % 8 == 1` before graph build and `len(frames) % 8 == 1` after mirroring.

3. [Questions #3] Audio/video duration interaction is not resolved. The engine emits silent video, but downstream master mux/composite may trim, hold, or align based on `frame_count`. Concrete fix: add a targeted integration test where target beat is 193f and boomerang output is 193f, and another where target is not exactly `2*(8k+1)-1` to verify composite behavior. verify: composite truncation/hold logic and whether it keys off canonical `frame_count`.

4. [THE FORK option C] The description of option C is internally ambiguous: “render the floor-safe full length (169), build the loop from the first ceil(N/2) frames” does not say whether N is target final length or rendered length. If N is rendered length 169, first ceil(N/2)=85 yields 169 final frames, not 193; if N is target 193, first 97 from a 169 render yields 193. Concrete fix: define variables precisely: `target_final`, `rendered_floor`, `source_count = (target_final + 1)//2`.

5. [Proposed restore #2] In-tensor concatenate can double peak CPU RAM for decoded frames. At 832x480x193x3 uint8 this is modest, but at larger canvases/caps it is nonzero. Concrete fix: enforce the existing cap before mirroring and avoid keeping extra temporaries longer than needed; consider `frames = np.concatenate(...)` only after uint8 conversion as proposed.

6. [Proposed restore #1] Env parsing is underspecified. “truthy on/1/true/yes” must also define false values and behavior for invalid values. Concrete fix: implement a tiny parser where default is true, false is `0/off/false/no`, true is `1/on/true/yes`, invalid logs LOUD and uses default.

7. [Proposed restore #2] The plan says “right after `images_to_uint8`” but not whether to mirror before or after any color conversion. Grounded code converts wrapper IMAGE to uint8, then encodes silent mp4. That is the correct insertion point. Concrete fix: add a unit test around a fake `images_to_uint8`/encoder to ensure the encoder receives the mirrored array, not the original.

OPTIONAL / NICE-TO-HAVE:
- Add a small pure helper `_boomerang_frames(frames)` so the mirror semantics are unit-testable without ComfyUI/GPU.
- Add log lines naming requested target, rendered source length, final mirrored length, width/height, and whether the decode-floor bypass was used.
- Add a golden-shape test for target 193 -> source 97 -> final 193 and target 169 -> source 89 -> final 177 or whatever policy is chosen, so non-exact targets are not accidental.

CUT THESE (over-engineering):
1. [THE FORK / Working recommendation] Cut “(A)+(B) hybrid” for first build. It changes both global decode policy and boomerang behavior at once, making failures hard to attribute. Safe cut: implement only boomerang-specific source-length handling with the proven 97 minimum; leave global `_ltx_frame_length` untouched.

2. [THE FORK / Working recommendation] Cut runtime “probe 73/49 to go lower” from this patch. Build-time GPU smoke can establish a lower default later. Runtime probing adds extra renders, retry complexity, and ambiguous determinism/failure behavior without being necessary to restore 193f from 97f.

3. [Proposed restore #2] Cut ffmpeg parity/re-encode concerns from the implementation path. The proposed in-tensor mirror before the existing encoder is enough for the stated goal; only keep a test proving index-order equivalence to the old ffmpeg filter.