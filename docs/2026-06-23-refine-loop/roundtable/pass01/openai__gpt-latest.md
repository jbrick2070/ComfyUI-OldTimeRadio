<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. The plan is not build-ready because the v1 arc depends on an unresolved holistic grader and unproven critique-to-outline feedback loop while proposing a major in-function recompose loop around fragile writer state.

MUST-FIX BEFORE BUILD:
1. [The loop step 4 / Open Question B / Build order 2] The core success mechanism is undefined: `grade_story(...) -> StoryGrade` is the loop’s judge, early-stop trigger, keep-best primary comparator, and next-pass critique source, but the plan leaves “reuse `_otr_story_critic` if viable; else a new lean rubric” unresolved. This is not an implementation detail; it determines whether the loop optimizes anything real. Concrete fix: choose one grader design before build, specify its input contract, output schema, parsing/fallback behavior, determinism expectations, and what makes `biggest_weakness` safe to feed into `diversity_hint`. If reusing `_otr_story_critic`, state exactly what existing numeric/verdict field is used; if adding a rubric, specify the rubric and how non-numeric/degenerate responses are handled.

2. [The v1 idea / The loop step 2 / Open Question C] The stated goal is “critique-informed regeneration,” but the only steering channel is `OutlineRequest.diversity_hint`, which v0 grounding shows was designed for short structural-variation instructions, not arbitrary post-compose criticism text. Feeding “single biggest weakness” directly into the outline prompt may be too broad, stale, or line-level to alter the outline structure. Concrete fix: insert a normalization layer: `critique_to_outline_hint(grade.biggest_weakness) -> bounded structural hint`, with length limits and allowed focus areas. Do not pass raw grader prose directly as `diversity_hint`.

3. [Lead design / Build order 1] The plan says to factor `[outline -> build_sq_data -> canon -> compose-all-lines]` into `_build_and_compose(...)`, but the “already true” section describes many run-local downstream steps: episode canon, ledger skeleton, `_OTRLC.compose_line` at several call sites, post-compose title regen, save ledger, and meta threading for freeze cascade. The plan does not define the boundary of “composed ledger” versus final save/title/meta state per pass. If the helper mutates shared `meta`, ledger skeletons, canon, title, or output paths on losing passes, the keep-best story may inherit loser state. Concrete fix: specify pass isolation at the architecture level: each pass must build into an isolated candidate object/snapshot, with only the winner’s ledger/meta/title committed to the live writer state. Add an explicit “no pass writes final artifacts or mutates winner-visible meta except candidate-local telemetry” invariant.

4. [Never-fail / The loop step 4] The error policy contradicts the grader’s role. It says compose/grade exceptions are skipped, and every failed pass triggers a fallback pass, but does not define what happens if composition succeeds and grading fails for a candidate. Skipping that pass discards a potentially shippable story despite “never HARD-FAIL on weak quality.” Concrete fix: separate compose success from grade success. If compose succeeds and grade fails, retain the candidate with a deterministic low/unknown grade and allow fallback to the first successfully composed candidate if no graded candidate exists. Only loud-fail when no composed ledger can be produced.

5. [The loop step 6] The keep-best comparator mixes `StoryGrade` and structural `StoryScore`, but the plan does not guarantee a `StoryScore` exists for every pass when Open Question A is unresolved. If a pass uses v0 best-of-N internally, there may be telemetry for multiple outlines and one winner; if it composes one outline, score must be explicitly computed. Concrete fix: define a single per-pass `structural_score` source: always compute `score_outline(winning_outline, meta, roster)` before `build_sq_data`, regardless of whether v0 best-of-N selected that outline. Store only that score in the refine comparator.

6. [Open Question A / The loop step 2] The plan has incompatible pass-generation models: “Build the outline” may itself run v0 best-of-N, or may compose one outline per pass. This changes runtime, telemetry shape, determinism, and whether `diversity_hint` is controlled by v0’s index hints or v1’s prior critique. Concrete fix: choose v1’s initial implementation: compose one outline per refine pass, with v0 best-of-N disabled inside the refine loop, or explicitly define nesting semantics. Smallest safe fix: do not nest v0 best-of-N in v1; v1 pass index owns `diversity_hint`.

7. [Invariants / Lead design] “build_sq_data + freeze + audio still run ONCE on the winner” is internally muddled. The loop step 3 says every pass runs `build_sq_data` and composes all lines; later invariant says `build_sq_data + freeze + audio still run ONCE on the winner.` Based on the v0 grounding, `build_sq_data` must run after the selected outline and before composition. For v1, it necessarily runs once per composed candidate, not once total, unless candidates are not truly composed. Concrete fix: restate the invariant accurately: `build_sq_data` may run once per refine candidate inside isolated pass state; freeze cascade and audio run once only on the selected winner; no final output artifacts are written for losers.

8. [Flag / Local-only] The remote provider gate assumes `resolved["creative_writing_model"].startswith(("openrouter:","comfy:"))` identifies all paid/remote compose calls. The provided v0 code uses the same prefixes for best-of-N, but v1 composes full dialogue, potentially through additional backends or model handles not shown. [ASSUMPTION] Concrete fix: verify all paid/full-compose creative providers use only `openrouter:` or `comfy:` prefixes, or centralize this behind the same provider classification used elsewhere. Until verified, document this as a hidden assumption and fail closed for unknown non-local handles.

SHOULD-FIX:
1. [The v1 idea / Open Question F] The plan reverses the previous v0 final plan’s condition “Built only after v0 proves the deterministic rubric discriminates” from `pass04_plan_FINAL.md`, replacing it with default-OFF code-first. That may be operator-directed, but it is a major governance change hidden in an open question. Concrete fix: make the acceptance criterion explicit: v1 can be built behind flag, but must not be advertised as improving story quality until validation soak shows grade lift and non-sameness lift.

2. [Telemetry] The telemetry shape records `winner_grade` and per-pass `biggest_weakness`, but not enough to diagnose whether the critique actually changed the next outline. Concrete fix: include the exact normalized `diversity_hint` used per pass and whether v0 best-of-N was nested, if nesting is retained. This is necessary to validate the concept, not just debugging.

3. [Early-stop step 5] `REFINE_BAR` is introduced as “default B+ ~= 80/100” but no flag/config source is specified, while `OTR_STORY_REFINE_PASSES` is specified. Concrete fix: either hardcode `REFINE_BAR = 80` for v1 and say so, or define `OTR_STORY_REFINE_BAR` with parse/clamp behavior. Do not leave the main stop condition implicit.

4. [Determinism] The plan claims deterministic for fixed seed/model, but local LLM generation and grading may not be controlled solely by `torch.manual_seed` and `random.seed`; the v0 final checklist explicitly marked RNG reseeding as verify-at-build. Concrete fix: carry that caveat forward: determinism is “best-effort until verified,” and add a build verification that per-pass reseeding actually changes/repeats the generation and grader outputs.

5. [Never-fail] “If EVERY pass raised -> ONE deterministic fallback pass” can duplicate work if pass 0 already raised under the same seed/hint for a persistent error. That is acceptable for transient model errors, but not for malformed input. Concrete fix: define fallback as “current production single path” only after clearing/refusing refine-only state; if the exception class indicates missing model/config, fail loud immediately rather than rerunning the same impossible call. [ASSUMPTION]

6. [Open Question D] The plan risks two story critics: a per-pass holistic grader and downstream freeze-cascade critic/reroll/doctor. It states downstream runs once, but not whether reused `_otr_story_critic` has side effects or heavy state. Concrete fix: require the v1 grader to be read-only and not invoke freeze-cascade edits/rerolls. If `_otr_story_critic` cannot be used without those side effects, build the lean rubric instead.

7. [Build order 5 / Open Question E] Worst-case runtime is acknowledged but no hard wall-clock budget exists. Since cap 5 full composes can be very long, default-OFF is not enough for unattended operator runs. Concrete fix: add optional wall-clock cutoff checked between passes; if exceeded, ship keep-best. This matches the “never unbounded” story better than pass count alone.

OPTIONAL / NICE-TO-HAVE:
- Add a minimal human-readable run summary log: requested/effective passes, each score, early stop reason, winner pass.
- Record grader model/provider separately from creative writer provider if they can differ. [ASSUMPTION]
- Add a “critique-only dry run” mode after one compose to collect grader telemetry without regenerating.

CUT THESE (scope / over-engineering):
1. [Open Question A] Cut nested v0 best-of-N inside each v1 pass for the first build. It multiplies complexity and creates conflicting `diversity_hint` ownership. Safe to cut because v1 can still test the holistic loop with one outline per pass.

2. [Build order 4] Cut remote opt-in for v1 entirely. The plan already says no remote in v1 due full-compose cost. Safe to defer because local-only default satisfies the operator’s v1 experiment without paid-call risk.

3. [The loop step 6] Cut structural tie-break beyond pass index for v1 unless `StoryScore` is already available at the exact pre-`build_sq_data` boundary. The holistic grade should be the primary concept under test; structural tie-break can be added after pass isolation is correct. If retained, define the score source as above.

4. [Telemetry] Cut exhaustive per-loser ledger retention, if implied. Keep only scalar telemetry and the winner artifact. Safe because the stated user-facing behavior is to ship the best story, not provide a multi-candidate review UI.