<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. The plan is not build-ready because the central loop requires factoring and committing large writer-local state that is not specified, and several described interfaces/data shapes do not match the grounded modules.

MUST-FIX BEFORE BUILD:
1. [Lead design / Build order 1] `_build_and_compose(...)` return/commit contract is underspecified and likely impossible to implement safely as written. The grounded facts say `run()` builds outline, SQ data, canon, ledger skeleton, every line, title regen, and saves ledger in-process, but the plan does not define the candidate object fields, which writer attributes are candidate-local, or how to suppress existing save/title side effects. Concrete fix: specify a dataclass, e.g. `RefineCandidate(pass_index:int, outline:Outline, raw_outline:Outline, structural_score:StoryScore, ledger:..., canon:..., meta:dict, title:str|None, composed_lines:list[str], ok:bool, error_type:str|None, grade:StoryGrade|None)`, and list every live writer attribute/global that `_build_and_compose` must not mutate. If existing compose functions only write into `self`, require a snapshot/restore wrapper or refactor those functions to accept explicit `ledger/meta/canon` parameters. [ASSUMPTION: writer state is mutable because plan says commit winner to live writer.]
2. [PASS ISOLATION / The loop step 4] “deep-copy the outline BEFORE `build_sq_data`” only protects beat-intent mutation; it does not prove pass isolation for `meta`, canon, ledger, roster, title, RNG state, or any caches. `score_outline(outline, meta, roster)` reads `meta`, and telemetry merges into `meta.story_quality`; losers must not leak there. Concrete fix: require `copy.deepcopy` or fresh construction for all mutable pass inputs (`outline`, `meta`, canon/episode_canon inputs, ledger skeleton, any per-line output containers) and defer all telemetry merge to a separate scalar `refine_telemetry` object until winner commit.
3. [Flags / Provider gate] The provider classification contradicts the grounded v0 gate. In `resolve_best_of_n`, non-`openrouter:`/`comfy:` is treated as local; remote `openrouter:`/`comfy:` clamps only unless opt-in. The plan says “reuse v0’s exact classification” but then says `openrouter:`/`comfy:` => clamp to 1 and “fail CLOSED for any unknown non-local handle.” There is no grounded API to distinguish “unknown non-local handle” from local; v0 treats unknown prefixes as local. Concrete fix: either (a) explicitly define a new local allowlist/predicate and do not claim it reuses v0 exactly, or (b) match v0 exactly: only `openrouter:`/`comfy:` are remote and all others are local.
4. [The loop step 2 / Build order 4] “best-of-N NOT nested -- `effective_n=1` inside the loop” conflicts with the existing writer path if `run()` currently calls `resolve_best_of_n` based on `OTR_STORY_BEST_OF_N`. Without a specified bypass, refine mode may still invoke `select_best_outline` inside each pass. Concrete fix: define the outline generation helper signature to accept `force_best_of_n:int=1` or `disable_best_of_n:bool`, and wire refine passes to call raw `generate_outline`/single-outline path directly, not `select_best_outline`.
5. [The loop step 6 / Build order 2] Adding `OutlineRequest.prior_critique` is not enough to influence production outline generation. The grounded `generate_outline` Path C uses `_build_macro_user_prompt`, `_build_phase_user_prompt`, and `_build_beat_user_prompt`; `_build_user_prompt` is legacy/back-compat and not the main path. The plan says render in `_build_user_prompt` only, which will not affect Path C generation. Concrete fix: specify exactly which Path C prompt(s) receive `prior_critique` (probably `_build_macro_user_prompt` and/or `_build_beat_user_prompt`) and assert empty string leaves all three prompt builders byte-identical.
6. [OutlineRequest.prior_critique / dataclass] `OutlineRequest` is a frozen dataclass with required `budget: EpisodeBudget` validation. Adding a new field is implementable only if it is appended after `diversity_hint` with a default; inserting it before defaulted fields will break dataclass field ordering or call sites. Concrete fix: add `prior_critique: str = ""` as the final field and validate/clamp in `__post_init__` only if necessary.
7. [The loop step 8] Keep-best comparator is written ambiguously and with wrong polarity notation: `(grade.score desc, structural_score: ungrounded_crisis_density asc / -distinct_conflict_nouns / -premise_grounding, pass_index asc)`. This is not a directly codable key and risks reversing metrics. Concrete fix: define exact Python key, e.g. `max(candidates, key=lambda c: (c.grade.score_0_100, -c.structural_score.ungrounded_crisis_density, c.structural_score.distinct_conflict_nouns, c.structural_score.premise_grounding, -c.pass_index))`, or use `min` with the inverse consistently.
8. [Never-fail] Failure taxonomy is not implementable as described. “missing-model / config error class fails LOUD” is not tied to actual exception classes; grounded outline generation raises `OutlineFailedError` and `ValueError`, but compose/model loading may raise many other types. Concrete fix: enumerate actual exception classes from the writer/backend to catch as `compose-fail skip`, `grade-fail low score`, and `config-fail abort`. If unknown, default unknown exceptions during compose to loud skip only after logging traceback, but do not silently continue on model/config initialization errors; verify exact classes in writer/backends.
9. [GRADE / Build order 3] `grade_story(composed_lines, premise, meta)` has no specified input shape for `composed_lines`. The existing ledger likely contains structured rows, not just strings; grader needs text extraction rules. Concrete fix: define accepted type (`Sequence[str]` or ledger object), how to extract spoken dialogue only vs music/sfx/announcer, max prompt length, and what happens on empty text.
10. [Telemetry] `meta.story_quality.refine_loop` assumes `meta` is a dict. Grounded `_merge_best_of_n_telemetry` no-ops when `meta` is not a dict. The plan does not state whether writer `meta` is dict-like or pydantic/object. Concrete fix: verify writer `meta` type; if dict, use `sq = meta.setdefault("story_quality", {})` and preserve existing keys. If object, define the setter path. Tests must cover existing `best_of_n` telemetry coexistence.
11. [Flags / resolve_refine_passes] The plan defines env parsing but not the function signature or return data needed for telemetry and control flow. Concrete fix: specify `resolve_refine_passes(resolved:dict|None, *, env=os.environ) -> RefineConfig(requested_passes:int, effective_passes:int, max_passes:int, bar:int, max_seconds:float, provider:str, clamp_reason:str)` and exact parsing/clamping for negative numbers, blank strings, non-int, and pass values >5.
12. [Determinism / The loop step 1] Seed conversion is incomplete. `h = sha256(...)` is a hash object/hex string depending on code; torch/random require bounded integers. Grounded v0 uses `int(hexdigest,16)`, `torch.manual_seed(seed_int % 2**64)`, `random.seed(seed_int % 2**32)`. Concrete fix: copy that exact `_seed_rngs` implementation with the `:refine:{i}` namespace and local torch import.
13. [Early-stop / wall-clock] “wall-clock budget is exceeded” is ambiguous and can cause zero candidates if checked before pass 0. Concrete fix: check budget only after at least one successful composed candidate, and define whether elapsed is measured before starting next pass or after completing current pass. Do not interrupt an in-flight compose.
14. [Never-fail fallback] Fallback after every pass failed may duplicate a pass that already failed with the same seed and critique, and the plan does not say whether fallback bypasses refine helpers or telemetry. Concrete fix: implement fallback as a single call to the current byte-identical single-pass path with seed `refine:0`, no telemetry loser state, and commit only on success; record `stop_reason="fallback_after_all_compose_failed"` if meta exists.

SHOULD-FIX:
1. [GRADE] “Robust parse: non-numeric/degenerate => deterministic low score” needs concrete bounds. Fix: define `StoryGrade` as `@dataclass(frozen=True)` with `score_0_100:int` clamped 0..100 and `biggest_weakness:str` trimmed, and use a fixed fallback such as `score_0_100=0`, `biggest_weakness="grader_unparseable"`.
2. [critique_to_hint] Prompt-injection handling is underdefined. A 200-char trim with prefix still permits instructions like “ignore previous rules.” Fix: normalize to one line, strip control chars/backticks/JSON fences, collapse whitespace, and optionally reject/replace strings containing “ignore previous/system/developer”.
3. [PASS ISOLATION] Deep-copying pydantic `Outline` may work, but Pydantic v2 has `model_copy(deep=True)`. Fix: use `outline.model_copy(deep=True)` for `Outline`, falling back to `copy.deepcopy` only for non-pydantic objects.
4. [The loop step 3] Structural scoring on raw intents after adding `prior_critique` should use the generated outline before any copy/build mutation. Fix: explicitly store `raw_outline_for_score = outline` and never score the post-`build_sq_data` copy.
5. [Telemetry] `grade_delta` baseline is undefined for pass 0 and failed passes. Fix: set pass 0 `grade_delta=None`; for later ok graded passes, `score - previous_ok_score`; for grade-fail low-score candidates, mark `grade_error_type` separately so soak does not treat grader failures as model regressions.
6. [Byte-identical OFF invariant] Adding new `OutlineRequest` field and prompt conditionals can break byte-identity via dataclass `repr` if requests are logged/hashed. Fix: byte-identical test should compare generated prompts and final ledger artifacts; if any request serialization includes dataclass fields, exclude/default-empty `prior_critique` or update serialization intentionally.
7. [Performance] Cap of 5 full composes can be very expensive because grounded `generate_outline` is `1 + act_count + num_voiced_beats` calls before line composition, and every line compose follows. Fix: log estimated pass call count before running and enforce `OTR_STORY_REFINE_MAX_SECONDS` before starting each additional pass.
8. [Provider gate] LOUD clamp should include model handle and requested/effective passes. Fix telemetry should include `clamp_reason`, not just `provider`.
9. [Tests] “full suite + byte-identical” is insufficient for isolation. Fix: add a test where pass 1 `build_sq_data` mutates intents, pass 2 starts from raw unmutated outline, and loser mutations do not appear in winner ledger/meta.

OPTIONAL / NICE-TO-HAVE:
- Add a small golden prompt test for `prior_critique=""` and non-empty across all Path C prompt builders.
- Add a grader prompt token cap/summarization if composed text can exceed local context.
- Emit per-pass elapsed seconds in telemetry for soak diagnostics.

CUT THESE (over-engineering):
1. [Flags] `OTR_STORY_REFINE_MAX_SECONDS` can be cut for v1 if pass cap is 5 and local-only. It adds ambiguous interruption semantics; a hard pass cap already prevents unbounded looping.
2. [Telemetry] Full `passes[].biggest_weakness` can be cut or truncated aggressively. The normalized hint is enough for soak, and storing raw critique risks prompt leakage/noise.
3. [Early-stop] “no improvement for 2 consecutive passes” can be cut initially. With max 3-5 passes and keep-best, bar + hard cap is simpler and deterministic; add no-improve only after telemetry proves wasted passes are common.