<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. The plan has several ordering/interface gaps: pass-local generation/composition cannot be isolated unless raw/working outlines and mutated ledger/meta boundaries are nailed down before the loop is wired, and the grader/revision overlay contracts do not yet match the existing Path C prompt and structured_call interfaces shown.

MUST-FIX BEFORE BUILD:
1. [Build order 0 + Revision overlay wiring] `diversity_hint` is planned only for `_build_macro_user_prompt`/`_build_beat_user_prompt`, but existing `select_best_outline()` already sets `OutlineRequest.diversity_hint` before calling `generate_outline`; if chunk 0 changes the dataclass field ordering later in chunk 1, tests/callers using `dataclasses.replace(outline_req, diversity_hint=hint)` can break if a non-final non-default field is inserted. Concrete fix: in chunk 0, only render existing `req.diversity_hint` in Path C builders; in chunk 1, append `prior_critique` and `prior_macro` after `diversity_hint` exactly as defaulted fields. Add a test that `_otr_story_select.select_best_outline()` still calls `dataclasses.replace(..., diversity_hint=...)` successfully after the new fields exist.

2. [Revision overlay wiring / Path C] Revision steering is underwired for Stage 2. The plan adds prior macro/critique to `_build_macro_user_prompt` and `_build_beat_user_prompt`, but `_build_phase_user_prompt()` is the stage that assigns speakers per phase and receives `macro` only after Stage 1. If the weakness is “who drives the turn” / “interpersonal conflict,” Stage 2 can still assign the same speaker pattern because it never sees the critique. Concrete fix: add a short optional critique line to `_build_phase_user_prompt()` as well, gated on `prior_critique` non-empty, or explicitly state that speaker-routing revisions are out of scope and accept that macro/beat overlays cannot alter speaker distribution reliably. Empty prompt must remain byte-identical.

3. [Pass isolation] The plan says `score_outline` runs on `raw_outline` before `build_sq_data`, but `RefineCandidate` contains both `outline` and `raw_outline` without specifying when `raw_outline` is captured relative to `build_sq_data` mutation. Grounding states `build_sq_data` mutates `beat.intent` in place; if `_build_and_compose(...)` passes the same object as both fields or scores after compose, the comparator sees grounded/mutated intents. Concrete fix: in `_build_and_compose`, immediately after `generate_outline` returns, set `raw_outline = outline.model_copy(deep=True)`, compute `structural_score = score_outline(raw_outline, meta_local, roster)`, then pass a separate `working_outline = outline.model_copy(deep=True)` into any `build_sq_data`/composition path. Assert object identity differs and raw intents remain unchanged after compose.

4. [Pass isolation / winner commit] “Only the WINNER commits to live writer state” is not sequenced with in-process line composition. Existing writer composes via `_OTRLC.compose_line` and likely mutates ledger/canon/meta as it goes [ASSUMPTION; exact containers not shown]. If chunk 3’s helper reuses live `self` fields, losers can leak even with copied outlines. Concrete fix: define `_build_and_compose(...)` signature to accept pass-local ledger, canon, meta, title containers and return them; do not assign `self.ledger`, `self.canon`, `self.title`, artifacts, or `meta.story_quality` inside the helper. Commit must be a single final assignment/copy step after comparator selection. Add a test that a failing/losing pass cannot change live title/ledger/meta.

5. [Grader] `grade_story(..., generate_fn, temperature=0.0)` says “Route through existing structured_call ladder” but `structured_call` usage in `_otr_outline.generate_outline` expects `base_temperature`, `structural_retry_temperature`, `repair_prompt_factory`, `max_new_tokens`, `max_attempts`, `helper_name`, and messages in `prompt`. There is no shown `temperature` parameter on `structured_call`. Concrete fix: implement grader using the actual interface: `structured_call(prompt=[...], schema=_StoryGradeSchema, slot_fn=generate_fn, base_temperature=0.0, structural_retry_temperature=0.0, repair_prompt_factory=make_dispatching_repair_factory(), max_new_tokens=<small>, max_attempts=<bounded>, helper_name="OTR_StoryGrade")`. Do not invent a different structured_call wrapper unless verified.

6. [Grader / deterministic seeding] The plan says “Seed before the call” but only specifies per-pass seed immediately before generation. If generation consumes RNG, then grading after composition will run from a state dependent on generation/composition internals, not from a stable grade seed. Concrete fix: seed again immediately before each `grade_story` call with a distinct namespace, e.g. `_seed_rngs(f"{cast_seed}:refine:{i}:grade")`, using the exact v0 torch/random hashing implementation. Keep generation seed as `_seed_rngs(f"{cast_seed}:refine:{i}")`.

7. [Failure taxonomy] The fallback “current byte-identical single path at `:refine:0`” conflicts with Pass 0 baseline already being today’s path. If pass 0 compose failed, rerunning fallback with the same seed can duplicate a known failure; if pass 0 was skipped due to a recoverable compose error, the fallback is not a different path. Concrete fix: make baseline pass mandatory first; if pass 0 compose fails, fail loud or run the existing non-refine run path without entering the loop, but do not call it “every pass failed fallback” unless it uses the exact pre-loop code path and seed. Specify whether fallback is before or after refinement loop and whether it can write telemetry.

8. [Flags + gate / interface contract] `resolve_refine_passes(resolved, *, env)` claims env overrides the node widget bar, but build order chunk 4 also says add widget in the same chunk as loop wiring. That means chunks 1-3 cannot compile/run tests that construct config unless the workflow/default config tolerates absent `refine_target_grade`. Concrete fix: either add the JSON/widget/default config in an earlier config-only chunk before `resolve_refine_passes`, or implement `resolve_refine_passes` to default missing widget to `"B"` and `"Off"` only when explicitly disabled. Tests must cover old workflow JSON missing the key.

9. [Flags + best-of-N collision] “force best-of-N `effective_n=1` for the in-loop outline call” is ambiguous because the loop calls `generate_outline` directly and not `select_best_outline`. Existing best-of-N is resolved outside by `resolve_best_of_n()` and can merge telemetry into `meta.story_quality`. Concrete fix: in `run()`, order resolution as: resolve refine config first; if `refine.effective_passes >= 2`, bypass `resolve_best_of_n`/`select_best_outline` entirely for the story path and log collision; if refine disabled, preserve current best-of-N behavior. Do not rely on setting an `effective_n` local that the old code path may ignore.

10. [External/provider gate] Provider gate says match v0 exactly: only `openrouter:`/`comfy:` remote. But the grader is also an LLM call, and the plan says “local-only” for the loop. If the creative writing model is remote, clamping passes to 1 avoids revision, but Pass 0 grading would still be an additional remote/local call depending on `generate_fn` unless disabled. Concrete fix: when provider is remote or effective_passes<2, skip grading entirely and run the byte-identical single path with no `refine_loop` key. Only call `grade_story` when `effective_passes >= 2` after provider clamp.

11. [Telemetry] Candidate field uses `grade` while telemetry records `score|grade_error_type`; plan also says parse failure returns `StoryGrade(score=0, biggest_weakness="grader_unparseable")` and records `grade_error_type` separately. If a grade parse fails, comparator cannot distinguish true grade 0 from fallback except telemetry, and `normalized_hint` for next pass would become `"grader_unparseable"`, causing a revision based on an infrastructure failure. Concrete fix: if `grade_error_type` is non-empty, retain candidate at floor for ordering but do not feed `"grader_unparseable"` into `critique_to_hint`; use `normalized_hint=""` and stop refinement or continue from previous valid critique only.

12. [Interface contract / StoryGrade naming] The build plan names `StoryGrade.score_0_100`, but the grader schema is `{"score": int, "biggest_weakness": str}`. Concrete fix: explicitly map parsed schema `.score` into frozen dataclass field `.score_0_100`; tests must verify comparator and telemetry use `score_0_100`, not `.score`.

13. [OutlineRequest.__post_init__] Plan says “__post_init__ trims” for `prior_critique` and `prior_macro`, but `OutlineRequest` is frozen. Direct assignment will raise `FrozenInstanceError`. Concrete fix: use `object.__setattr__(self, "prior_critique", trimmed)` and same for `prior_macro`, as is required for frozen dataclasses.

SHOULD-FIX:
1. [Revision overlay wiring] `prior_macro` is described as title/premise/setting digest, but no serialization contract is defined. If it includes raw beat intents too, the Stage 1 macro prompt can exceed the lean <250-token intent. Concrete fix: define `prior_macro` format and cap, e.g. `Title: ... | Premise: ... | Setting: ... | Beats: <numbered intents truncated to N chars>` or split macro vs beat history. Keep Stage 1 truly macro-only if that is the design.

2. [Grader extraction] “spoken dialogue lines pulled from ledger rows” lacks a contract for row fields and role source. Grounding only shows outline `speaker_role`, not ledger schema. Concrete fix: verify ledger row shape and implement one function `extract_spoken_text_for_grade(ledger)` with tests for character/announcer included and music/sfx excluded. verify: exact ledger row keys/classes and where `dialogue speaker_role` lives.

3. [Configuration] Env `OTR_STORY_REFINE_BAR` and widget target can conflict. The plan says env overrides but telemetry includes `target_grade`. Concrete fix: telemetry should record both `target_grade` widget value and resolved numeric `bar`, plus `clamp_reason`/`override_source`, so soak can tell “B dropdown” from env-forced 90.

4. [Queue/back-pressure equivalent] Each refine pass can be 1 + act_count + voiced_beats outline calls plus composition and grading. With cap 5 this can multiply local GPU runtime substantially. There is no max seconds by decision, but there should still be cancellation/cooperative interrupt checks if the writer has them [ASSUMPTION]. Concrete fix: verify existing run cancellation/interrupt hook and call it between passes and before grade. verify: available cancellation API.

5. [Prompt injection sanitizer] `critique_to_hint` rejects strings containing “ignore previous/system/developer”. A blanket reject can drop legitimate critique such as “the story ignores previous beat causality.” Concrete fix: match instruction-shaped phrases more narrowly, e.g. `ignore (all )?(previous|system|developer) (instructions|messages|prompt)`; otherwise strip dangerous tokens and keep useful critique.

6. [Build order] Chunk 3 single-pass path byte-identical depends on chunks 0-1 empty overlays and chunk 2 grader not being called. Concrete fix: add the byte-identical OFF/run test in chunk 3 as well as chunk 4, or chunk 3 can accidentally compose through helper differently and chunk 4 will be too late to localize.

OPTIONAL / NICE-TO-HAVE:
- Add a small `RefineCandidate.to_telemetry()` helper to prevent telemetry schema drift between ok/fail cases.
- Include per-pass seed namespace in debug logs, not telemetry, for reproducibility without bloating JSON.
- Add a prompt snapshot test for all three Path C builders with empty and non-empty `diversity_hint`/revision overlays.

CUT THESE (over-engineering):
1. [Telemetry] `elapsed_s` per pass is safe to cut for v1 if timing instrumentation risks touching global state or making tests flaky. Quality lift and pass count are enough for the initial soak.
2. [RefineCandidate] `meta_delta` is safe to cut if each pass already carries a full fresh `meta` and only the winner commits. A delta object creates merge-order ambiguity; commit the winner’s local meta plus `refine_loop`.
3. [Never-fail taxonomy] The “every pass failed then deterministic fallback” branch is safe to cut in favor of mandatory pass-0 baseline + loud failure if baseline cannot compose. It removes a confusing duplicate path and preserves the existing single-path failure semantics.