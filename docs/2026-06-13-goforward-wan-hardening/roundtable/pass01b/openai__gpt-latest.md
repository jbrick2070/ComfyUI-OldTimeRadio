<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. GATE-A can still go green on zero/misrouted/fallback legs, VRAM can pass with missing measurements, and Wan model/VAE/preflight requirements are not fail-closed enough for a build gate.

MUST-FIX BEFORE BUILD:
1. [Q1 / section 3 item 4 / otr_coverage_sweep.py main] The coverage sweep currently calls `soak.run_leg(... expect_engine="")`, and `_otr_soak_capstone.py` treats that as informational only. This directly invalidates section 3 item 4’s claim that the sweep is “NOT green until wan_i2v (and wan_ti2v) pass.” Fix: for any leg whose purpose is to exercise a selected engine, pass `expect_engine=engine` or add an equivalent strict trace assertion. Also ensure the generated episode actually plans at least one shot for that engine, because `_otr_soak_capstone.py` correctly raises “no shot was planned for %r” when `expect_engine` is set and no trace entry has `attempts[0] == expect_engine`.

2. [section 3 item 4 / otr_coverage_sweep.py main] The sweep returns success when no legs run: `return 0 if passed == len(results) else 1` means `0 == 0` passes. A mistyped `--only`, missing `wan_ti2v` registration, or excluding all Wan legs can produce a false green. Fix: fail if `results` is empty after filters; for GATE-A, also fail unless required core engines `wan_i2v` and `wan_ti2v` appear in results with `PASS`.

3. [section 3 item 4 / otr_coverage_sweep.py --exclude help / section 4] Availability does not read runtime opt-in flags: the script’s own help says `availability() is pure profile-fit and never reads OTR_ENABLE_WAN_I2V`. Combined with Q1, a Wan leg can be enumerated, gated off by `assert_usable`, fall back, and still pass as informational. Fix: before a core/blocking sweep, preflight `OTR_ENABLE_WAN_I2V=1`, the future `OTR_ENABLE_WAN_TI2V=1`, and required model files; disallow `--exclude wan` / `--exclude wan_i2v` / `--exclude wan_ti2v` for the acceptance run.

4. [section 5 CS-2 / _otr_soak_capstone.py run_leg] VRAM can pass with no valid driver measurement. `driver_peak = int(report.get("vram_peak_mb") or -1)` and the only failure condition is `driver_peak > VRAM_CEILING_MB`; missing/zero/negative `vram_peak_mb` passes. This contradicts the hard invariant “single resident heavy engine <= 14.5 GB host NVML.” Fix: fail closed when `vram_peak_mb` is absent or `<= 0`; also fail if the driver reports no render-phase attribution. Keep whole-run sampler informational if desired, but the render-phase gate must require a real measurement.

5. [section 4 Phase 2 / eng_wan_i2v.render_clip] The Wan engine’s own render-phase VRAM assertion is skipped when `OTR_TEST_MODE` is set: `if not os.environ.get("OTR_TEST_MODE"):` wraps the log and `_MC.assert_peak_within_ceiling(...)`. A real Phase-2 gate must not run under `OTR_TEST_MODE`. Fix: add a harness preflight that fails if `OTR_TEST_MODE` is set for Wan acceptance, or move the peak assertion outside the test-mode guard for live runs.

6. [section 4 Phase 2] “silent mp4 (h264/yuv420p/bt709, fps 25, has_audio False)” is not asserted by the shown full-episode soak. `_otr_soak_capstone.py` asserts the final/obs deliverables have audio and byte-identical audio, but it does not inspect the raw Wan clip metadata. `WanI2VEngine._clip_from_raw()` declares `has_audio=False`, `fps=25`, `yuv420p`, `bt709`, but the real-path gate must prove the produced clip/report contains those values. Fix: add a per-clip trace/report assertion for the Wan clip’s `has_audio`, codec, pixel format, fps, and bt709 fields, or add a targeted real-path test that calls the engine and ffprobes the emitted silent mp4 before mux.

7. [section 4 8GB tier / Q2 / eng_wan_i2v._loader_names] The current Wan implementation defaults VAE to `"wan_2.1_vae.safetensors"`. The plan says the TI2V-5B engine needs its own Wan2.2 VAE env. Fix: when implementing `wan_ti2v`, give it a distinct VAE env/name resolver and fail closed if the resolved VAE basename is empty or equals the 2.1 VAE basename. Do not inherit `WanI2VEngine._loader_names()` unchanged.

8. [section 4 8GB tier] The model installation path is underspecified and likely mismatched with Comfy loader semantics. Existing `eng_wan_i2v` passes basenames to `UNETLoader`/`UnetLoaderGGUF` and `VAELoader`, with defaults under Comfy’s `models` tree. The plan says “Fetch ... into `C:\ComfyUI-Models\`” without specifying Comfy-recognized subdirectories or basename envs. Fix: specify exact target folders and env vars for TI2V UNET/GGUF, VAE, and any text encoder, and add a preflight that resolves those basenames through the installed Comfy model search paths before rendering.

9. [section 4 / eng_wan_i2v.assert_usable / _loader_names] Existing `wan_i2v` only fail-closes on the primary checkpoint path. It does not preflight the CLIP name or VAE name from `_loader_names()`, and those are required graph loaders. Fix: extend `assert_usable()` or a Phase-2 preflight to verify UNET, CLIP, and VAE assets are locally present and match the recorded repo/sha/license manifest before any forward. This is required by the offline/no-runtime-fetch invariant and the plan’s “record HF repo + sha256 + license, fail-closed” rule.

10. [section 3 item 4 / Q8] Item 4 defines three additive leg-sets: visual-engine, writer-LLM, and voice-variation. The grounded `scripts/otr_coverage_sweep.py` only enumerates visual dropdown slots from `SLOTS`. Fix: either point the writer-LLM and voice-variation legs at their actual harnesses, or mark them TODO and remove them from the “Coverage sweep GREEN” acceptance surface until implemented. As written, a green `otr_coverage_sweep.py` cannot satisfy item 4.

11. [section 4 / section 5 CS-3 / Q5] CS-3 is framed as whether Wan and HuMo can “co-stage” or be “16gb-tier-compatible.” Under the hard invariant, `wan_i2v` around 14 GB and `humo_1.7B` around 7 GB cannot intentionally co-reside under 14.5 GB. The required proof is sequential residency with a reclaim barrier between beats. Fix: rewrite CS-3 acceptance to assert per-beat peak <= ceiling and verify Wan resources are reclaimed before a HuMo beat loads, and vice versa. [ASSUMPTION] This depends on render_driver sequencing/reclaim behavior, which is not shown here; verify the actual reclaim point and trace fields.

12. [section 4 Phase 2 / section 3 item 4] The plan names `scripts/otr_run_leg.ps1 / coverage_sweep --only ...` but the grounded sweep harness is `scripts/otr_coverage_sweep.py`; there is no grounded `coverage_sweep` command. Fix: spell the exact command(s), including required env vars and `--only` substrings that are guaranteed to match at least one Wan leg.

SHOULD-FIX:
1. [Q3 / registry.py CAPABILITIES] Q3 is partly overstated: the grounding says the 14499 MB peak was “without free_after_use,” while current `render_clip()` hardcodes `free_after_use=True`. Still, `CAPABILITIES["wan_i2v"].vram_estimate_mb = 14000` is below the recorded worst-known edge case and can make profile-fit decisions optimistic. Fix: set the estimate to the measured acceptance peak once Phase 2 runs, or conservatively set it to 14500 and document that `free_after_use=True` is load-bearing.

2. [Q6 / registry.py CAPABILITIES] `CAPABILITIES["wan_i2v"].model_requirements` is `["wan2.1-i2v"]` while the engine and plan are Wan 2.2 I2V. Fix the label to the actual required Wan 2.2 asset id when adding the `wan_ti2v` row, so downstream distribution/wizard work does not advertise the wrong model.

3. [Q4 / section 4] The plan’s eyeball gate catches weak motion, but it does not name the single-expert risk even though `eng_wan_i2v.py` says the two-expert HIGH/LOW MoE handoff is “future option, not wired here.” Fix: add an explicit Phase-2 risk/mitigation: if I2V-14B low-noise-only output fails the “real camera motion” gate, Path B two-expert handoff becomes the next step before declaring Wan 14B acceptable. [ASSUMPTION] The exact motion quality impact is model-behavior dependent.

4. [section 5 Harness polish / otr_coverage_sweep.py _resolve_server_output] The resolver chooses an explicit env or the newest hardcoded candidate. It does not query the live server’s `OTR_OUTPUT_DIR` or fail on mismatch, which section 5 already calls out. Because the soak gates depend on output/report identity, this is not just polish for acceptance runs. Fix: promote it before the GATE-A rerun: resolve the live server output tree from the server-side environment/report source if available, and fail loud if the client-selected tree disagrees.

5. [section 4 / eng_wan_i2v._materialize_init_image] `_materialize_init_image()` writes a fixed basename `otr_wan_init_<w>x<h>.png` into the Comfy input dir. If multiple Wan renders of the same dimensions are queued/concurrent, one can overwrite another’s init image. The plan says one coder window, but not necessarily one in-flight Wan clip. Fix: include request/shot/seed or a UUID in the staged filename, while preserving determinism of render inputs. [ASSUMPTION] Verify whether render_driver can queue multiple Wan clips concurrently.

6. [section 4 Phase 2] The plan says “Kill/reset the Phase-1 server first” but does not specify post-reset verification. Fix: after restart, assert server PID/start time changed, SageAttention is not active for the headless WAN lane, `OTR_TEST_MODE` is unset, and the intended env vars are visible to the server process before submitting the Phase-2 leg.

7. [Q1 / _otr_soak_capstone.py run_leg] `expect_engine` checks `attempts[0] == expect_engine`. If the trace records aliases, canonical engine ids, or reordered attempts, this can false-fail or miss the requested route. Fix: verify the trace schema and ensure the requested/planned engine is explicitly recorded as a stable field. [ASSUMPTION] Only `attempts` and `final_engine` are visible in the grounding.

8. [section 4 8GB tier] The plan says add “_node_candidates incl. the 5B latent node” but does not name the exact core Comfy node class or required topology. Fix: before coding, capture `/object_info` names for TI2V-5B and specify the exact loader/image-to-video/text-image-to-video latent node inputs. Otherwise this will fail late at graph resolution.

OPTIONAL / NICE-TO-HAVE:
1. [Q7] Use the exact script path `scripts/otr_coverage_sweep.py` everywhere instead of shorthand.
2. [section 4 Eyeball gate] Add the exact still, prompt, seed, frame count, dimensions, and comparison command for the two webms so the eyeball gate is reproducible within a render.
3. [section 2 GIT] The unpushed-commit side note is consistent with the stated policy, but it is an operator/release hygiene action, not a build-logic defect.

CUT THESE (over-engineering):
1. [section 3 item 4] Do not build a broad new “VRAM-budget-aware scheduler” to close CS-3 before Phase 2. The smallest sufficient fix is a sequential-residency/reclaim assertion in the existing real episode path. Scheduler work can wait until a measured reclaim failure exists.
2. [section 4 8GB tier] Do not implement `wan_ti2v` by subclassing/parameterizing every `WanI2VEngine` path if the TI2V graph topology differs. Safer cut: share only small pure helpers for dimensions/aspect/materialization/canonicalization; keep loader names, node candidates, and graph construction separate.
3. [section 3 item 4] Do not make the GATE-A sweep a full cross-product of visual engines x writer LLMs x voices. The plan says additive, and additive is enough for coverage. Keep it additive and make each leg-set’s harness explicit.