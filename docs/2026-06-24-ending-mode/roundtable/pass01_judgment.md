# R1 judgment

Panel: GPT-5.5, Gemini-3.1-pro, DeepSeek-v4-pro. Spend $0.184. Strong convergence
(both full reviews returned "not build-ready"; same fixes). Gemini's save
truncated — recovered fragment only (favor L1 over a negative ban; folded).

ACCEPTED (grounded CONFIRMED):
- ending_mode must be a closed enum, one tag/style + flavor text (GPT1/DS2/DS7).
- Do NOT replace beat_role — validate_beat_roles requires one irreversible_choice
  on the last beat; keep it, reframe the choice via the ending tag (GPT2/DS1).
- Deterministic keyword style-selector, NOT an LLM chooser (DS3/DS-CUT/GPT). Fixes
  my anchor's LLM-chooser suggestion + the C7 byte-identity risk.
- Drop the standalone negative anti-trope ban; rely on positive tag + L1 (DS-CUT,
  Gemini, my anchor MUST-FIX 2).
- Name the flag + prove byte-identical off (DS4). Concrete validation thresholds,
  baseline first (DS5/GPT). Define L1/L2/T4 interaction (DS6).
- Announcer outro after the climax dilutes the ending — govern the final CHARACTER
  beat, keep the outro generic (GPT3; ties to T4).

REJECTED / DOWNGRADED: none material — the panel was unusually aligned.

VERIFY-AT-BUILD (UNVERIFIABLE):
- Whether a weak local model actually shifts off the trope with a concrete
  pre-seeded ending — the core bet; settle with the §7 A/B.
- C7 byte-identity after inventor→deterministic-selector swap — wire then assert.

Convergence: R1 resolved the design forks. R2 = coding plan; R3 = wiring; R4 = final.
