<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. The document identifies the failure but does not yet define a buildable contract: `ending_mode` is free prose, the proposed “replace climax role” conflicts with the existing validator, and the selection/enforcement/validation paths are underspecified.

MUST-FIX BEFORE BUILD:
1. [1. Ending taxonomy] `ending_mode` is not actually a closed taxonomy. In `_otr_style_catalog.py`, each style’s `ending_mode` is a prose menu such as “a revelation, a reversal of who to trust, or one unresolved final sound,” not one deterministic archetype. This cannot be cleanly routed, measured, or validated. Concrete fix: add a closed enum, e.g. `ENDING_REVELATION`, `ENDING_REVERSAL`, `ENDING_UNRESOLVED_SOUND`, `ENDING_RECONCILIATION`, `ENDING_BITTERSWEET_PARTING`, `ENDING_IRONIC_TWIST`, `ENDING_QUIET_ACCEPTANCE`, `ENDING_CONFESSION`, `ENDING_AMBIGUOUS_FADE`; give each style exactly one primary enum plus optional `ending_variant_text` for flavor. Do not use the existing prose string as the machine contract.

2. [2. Where to enforce it] Option (a), “replace the climax `beat_role` itself,” contradicts the current L2 contract. `_otr_story_quality_l12.py` defines `BEAT_ROLES` without ending archetypes and `validate_beat_roles()` requires exactly one `irreversible_choice` on the last voiced character beat. Concrete fix: do not replace `beat_role` in the first build. Keep `irreversible_choice` as the structural/on-stage invariant and add a separate style-derived `ending_mode` field in the SQ/prompt path that reframes what the irreversible choice is. If you insist on replacing the role, then update `BEAT_ROLES`, `assign_beat_roles()`, `validate_beat_roles()`, all consumers, and tests in the same change.

3. [Root cause] The document blurs “last voiced beat” with “last voiced character beat.” The grounded code assigns `irreversible_choice` only across `speaker_role == "character"` in `build_sq_data()`, while `_assemble_outline()` appends an announcer close after the final character beat. That means the episode can still end after the climax with announcer material that may dilute or contradict the intended ending. Concrete fix: define enforcement