<!-- requested_model: deepseek/deepseek-v4-pro | resolved_model: deepseek/deepseek-v4-pro-20260423 -->

VERDICT: no. The document is a problem statement with unresolved alternatives, not a build-ready design. Key decisions (wiring mechanism, ending taxonomy, style selection algorithm, validation thresholds) are absent, and the grounding code shows the existing climax is hardwired without any wire for `ending_mode`. Build cannot begin until these are settled.

MUST-FIX BEFORE BUILD:
1. [Question for the panel (2) – Wiring approach] No single approach is chosen. The four options (replace beat_role, reframe irreversible_choice, inject at line composer, combination) are all hypothetical. The existing `_otr_story_quality_l12.assign_beat_roles` forces `irreversible_choice` as the last voiced character beat; the style catalog’s `ending_mode` field is completely unused. Must decide now which placement will be implemented, document the exact injection point(s), and specify how it interacts with the L1/L2 crisis-noun system (default-OFF). A plausible choice: extend `beat_role` assignments so the final voiced beat’s role is `<ending_mode_tag>` and the line composer prompt resolves that tag. That would be a concrete design.
2. [Question for the panel (1) – Ending taxonomy] No closed set of ending archetypes is defined. The catalog entries contain free-text narratives like "a revelation, a reversal of who to trust, or one unresolved final sound". To drive deterministic prompt injection, you need a small, mutually exclusive set of tokens (e.g., `revelation`, `reversal`, `unresolved_final_sound`, `reconciliation`, `bittersweet_parting`, `ironic_twist`, `quiet_acceptance`, `confession`, `ambiguous_fade`), each with a canonical description that can be rendered into instruction. Map every catalog `ending_mode` to exactly one such tag. Without this mapping, wiring is ambiguous.
3. [Question for the panel (4) – Style SELECT mechanic] No selection method is fixed. The style catalog’s tags are free-form and include an `emergency` bias; there is no algorithm to pick a best-fit style deterministically from the article’s premise/meta. Must design a deterministic, cheap selector (e.g., a domain-keyword classifier + a fallback to a small set of never-emergency defaults) and integrate it into the prompt-building path (likely in `_build_user_prompt` or an upstream step). The existing `select_domain` in `_otr_story_quality_l12` handles L1 domain, but style selection is separate; avoid code duplication. Specify that the selector runs *only* when the style-flag is on, and that it must produce byte-identical output for the same seed when off.
4. [Hard constraints – Byte-identical default-off] The roundtable states “Ships DARK / default-OFF / byte-identical when the flag is off” but does not define the flag or its insertion point. The current outline code (`generate_outline`) reads `req.style` as a raw string and does not consult the catalog. The entire ending_mode override must be gated by a flag (e.g., a new field on `OutlineRequest` or a global config) that, when absent/false, preserves the existing prompt and beat-role assignment exactly, guaranteeing the C7 audio path is unchanged. The design must name the flag, show its toggle point, and prove that with flag off, all generated prompts and beat roles are byte-identical to the current code.
5. [Question for the panel (5) – Validation] No measurable success criteria are set. A build cannot be validated without concrete acceptance thresholds. Define a soak-suite measurement: e.g., zero generic crisis nouns (`GENERIC_CRISIS_NOUNS`) in the climax beat, at least 80% of episodes using a non-doomsday ending archetype, average `arc_verdict` ≥ 75, and a small A/B harness with synthetic articles where the expected ending mode is obvious. Record baseline numbers with the current code first.
6. [Root cause + What we just built] The plan states the “T4 staging penalty (on-mic climax) + T2 critic adapter already exist (default-OFF). L1/L2 crisis-noun grounding (swaps console/lever/gauge ...) is built but default-OFF.” The ending-mode wiring must coexist with these systems and should not be turned on until they are all on, or must define ordering. The plan is silent on how these interact. Must specify that when the new ending-mode flag is on, whether L1/L2 grounding is also forced on, or if they can be independent, and document the expected combined behavior.
7. [Grounding: _otr_style_catalog.py] The `ending_mode` strings are inconsistent: some list alternatives (“a revelation, a reversal...”), some are a single clause (“the quiet click of the real answer...”), some include multiple outcomes separated by “, or ”. This makes programmatic parsing difficult. The mandated taxonomy (point 2) must also include a normalized representation for each entry; maybe add a new field `ending_mode_tag` to each catalog dict.

SHOULD-FIX:
1. [Question for the panel (3) – Anti-trope constraint] The document asks “Should we add an explicit negative instruction?”. The associated risk is noted. This should be resolved now: given the choice of a pull approach (reframing toward a concrete ending archetype), drop the explicit “no countdown” ban as a separate mechanism; the positive archetype is the lever. Minor to decide; if kept, it must be integrated with the override prompt and tested for obedience by the weak model.
2. [Hard constraints – no new heavy model] The style SELECT must be “cheap (no extra paid call)”, but no cost ceiling is given. The selector likely needs to classify the article’s domain or score styles without an LLM. Should specify that the selector is a deterministic Python function (no ML inference) that uses keyword matching against the premise + meta fields, akin to `select_domain`, and falls back to a small safe set.
3. [Question for the panel (2) d] The combination approach adds a “negative constraint banning the GENERIC_CRISIS_NOUNS vocabulary at the climax”. This overlaps with existing L1 crisis-noun grounding (which substitutes them). If L1 is turned on, that vocabulary is already handled; adding a second ban may confuse the model. Clarify whether the ending-mode wiring will rely on L1 being enabled or will independently handle trope verbs. Simplest: assume L1 is part of the story-quality lift and is turned on alongside; then the negative ban is redundant. Remove the combination unless proven necessary.

OPTIONAL / NICE-TO-HAVE:
- In the style catalog, annotate explicitly which styles are safe as default (never emergency) so the selector can restrict its everyday pool.
- Provide a score for each style’s “blow-up risk” to aid the selector in avoiding emergency-tagged styles.

CUT THESE (scope / over-engineering):
- The anti-trope negative constraint as a standalone prompt line. If the chosen wiring (e.g., reframe irreversible_choice via the ending_mode tag) already gives the model a concrete alternative, the negative is moot and likely ineffective. Cut it.
- The small‑LLM “chooser” (option 4c) for style selection. It introduces a non‑deterministic (even with low temperature) element that may fail the C7 byte‑identity requirement unless heavily constrained. Deterministic keyword + rule‑based classification is sufficient and safer.

[ASSUMPTION] The weak local model’s bias toward “doomsday button” can be overcome by changing the prompt instruction (reframing the `irreversible_choice` role with a concrete ending_mode tag). This must be verified experimentally; the design should include a pilot on a handful of seeds before full rollout.
[ASSUMPTION] The system can run a style selection stage after the beat plan is formed but before the line composer, implying that the beat planner’s output (beat roles) is mutable after style is chosen. The current code assigns beat roles (`assign_beat_roles`) during `build_sq_data`, which runs after the outline is generated. The design must specify at which pipeline stage the style is selected and when the ending_mode overrides the beat role. The document assumes this is feasible without disrupting the outline validator and budget checks.