<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. The plan is stale against the grounded code: the “primary” prompt restoration already exists, but only in a conditional path that can be bypassed; several proposed levers contradict current defaults and would not faithfully reproduce 5/30 as written.

MUST-FIX BEFORE BUILD:
1. [Proposed faithful-restoration / Primary] Defect: “restore the dynamic motion-prompt language” is already implemented in `render_driver.py` as `_LTX_MOTION_PROMPT_BY_ROLE` and the `_is_open` branch stamps `prompt_source="motion_role"`. Building this as a new restoration without first checking the live branch risks duplicating or fighting existing logic. Concrete fix: replace this task with a verification/fix task: assert in a real episode trace that announcer/music LTX rows have `prompt_source == "motion_role"` and non-calm prompt text/sha, unless `OTR_LTX_RADIO_PROMPT` is intentionally set.

2. [Proposed faithful-restoration / Primary] Defect: current code gives `creative.text_prompt` absolute precedence before the LTX motion branch. In `build_request_from_shot`, if `text_prompt` exists, it stamps `source="m4"` and the later LTX scene/motion block is skipped because it is gated by `and not text_prompt`. Therefore live per-beat LTX prompts can still be calm/static if ShotLock/M4 supplies creative prompts. Concrete fix: for `engine_id == "ltx_video"` and open roles (`announcer_visual` / `music_visual` / synthetic open), apply the motion-role prompt even when `creative.text_prompt` exists, or explicitly append/override with motion verbs under budget. Stamp subsource so this override is auditable.

3. [Candidate levers #1 / Proposed faithful-restoration] Defect: the plan says reinstate the 5/30 dynamic variety including aggressive `music_open`, but grounded code deliberately maps the synthetic opening music beat to `music_inter` by default via `_ltx_motion_role_key`: `OTR_LTX_OPEN_MOTION_KEY` default `"music_inter"`, with a comment saying `music_open` smeared. That is not faithful to the stated 5/30 recipe. Concrete fix: decide the target. If faithfulness to 5/30 is mandatory, set default/open mapping back to `"music_open"` and handle smear separately; if the calmer mapping is intentional, remove “faithfully reproduce 5/30” and “whip-pans / aggressive-vibrate” as acceptance criteria for the opening beat.

4. [Candidate levers #3 / Questions #3] Defect: source-length analysis is wrong for current loop path. Grounded `eng_ltx_video.py` uses `_ltx_loop_source_length` with `OTR_LTX_LOOP_MIN_DECODE_FRAMES` default 97, then mirrors to `2*src-1`; for a 169 target this renders 97 source frames and outputs 193 frames, not the plan’s “81 source frames” target. Also the global decode floor is 169 for non-loop landscape path. Concrete fix: if testing 81-source-frame attribution, add an explicit env/test override for the loop min/cap that can actually produce 81 at 832x480, and verify decode on GPU; otherwise revise the lever to “97 source frames current loop default” and stop claiming current code reproduces 81.

5. [What is ALREADY identical] Defect: this section claims the current recipe constants match 5/30, including `i2v strength 0.75`, “same model,” sampler, CFG, end-anchor, `frame_rate=25`, 832x480, and loop. Grounding shows current defaults are conditional/env-dependent: sampler mode is `OTR_LTX_SAMPLER` default `distilled`; sampler name is env-overridable; ckpt path/name is env/file dependent; I2V only happens if `asset_refs` includes an existing still; render canvas is set in `render_driver.py` only for `engine_id == "ltx_video"`; loop can be disabled by env. Concrete fix: replace “already identical” with a build-time config assertion/smoke that logs the actual ckpt name, sampler mode/name, CFG, strength, canvas, I2V init source, source length, seed, and prompt source for the target beat.

6. [OPTION: NEW LLM motion-prompt pass] Defect: adding an LLM pass is not build-ready because no hook contract is specified. Grounded code currently composes LTX prompts inside `build_request_from_shot`, after ledger read, before seed stamping; a new LLM call there would introduce latency, failure modes, cache identity questions, and possible GPU/VRAM residency conflicts with the pre-render residue-free step in `run_episode`. Concrete fix: do not build the LLM pass in this release unless you specify: exact hook, cache key, deterministic decoding settings, timeout, failure return, prompt sanitizer, char limit, and whether its output overrides M4/motion_role/env. Smallest safe fix: defer LLM pass and use existing `_LTX_MOTION_PROMPT_BY_ROLE`.

SHOULD-FIX:
1. [Questions #1] Defect: attribution method is under-specified. “Controlled smoke with the 5/30 dynamic prompt vs current prompt” is right direction but must freeze other levers. Concrete fix: define a matrix with same still, same seed, same frame ask/source length, same canvas, same sampler mode/name/CFG, same I2V strength, same negative prompt, and only vary `text_prompt`. Record `prompt_source`, prompt sha, frame source length, and I2V init status in the output.

2. [Candidate levers #2 / Proposed faithful-restoration / Secondary] Defect: “boomerang halves render cost” is only partially true against current code. For a 169 target, loop renders 97 frames and mirrors to 193, not half of 169 exactly; non-loop floor/cap behavior remains 169. Concrete fix: quantify current cost as 97-source-frame LTX render for looped path, not “half-render” generally, and benchmark ksampler at 97-source-frame loop before making it default.

3. [Questions #5] Defect: prompt-length budgets conflict. The plan says “LTX <=~188 chars, BUG-112”; grounded code uses `_LTX_MOTION_PROMPT_MAX = 240` for motion prompts and `finish_visual_prompt(... max_chars=188 ...)` only for non-open brief+beat prompts. Concrete fix: split acceptance criteria: motion-role prompts <=240 unless you intentionally lower them; non-open brief+beat prompts <=188. If 188 is mandatory for all LTX prompts, update `_LTX_MOTION_PROMPT_MAX` and re-trim templates.

4. [Proposed faithful-restoration / Primary] Defect: “seeded per-beat for variety + determinism” is not how grounded motion templates currently work. `_ltx_motion_role_key` is role/shot-id based, not randomized across multiple templates per role; seed only affects sampler noise via `request_seed`. Concrete fix: either remove “seeded variety” from scope or implement deterministic template selection keyed by request hash/shot id with trace stamping of selected key/template id.

5. [Candidate levers #1] Defect: the plan asks to “verify they survived into the live per-beat LTX prompt” but does not name the existing observability mechanism. Grounding has trace fields `prompt_source`, `prompt_sha8`, `prompt_chars`, `init_source`, `video_seed`. Concrete fix: make acceptance mechanical: trace rows for target LTX beats must show `prompt_source in {"motion_role","env"}` as intended, `init_source=="scene_still"`, and distinct sha where variety is expected.

6. [What is ALREADY identical / Candidate levers #4] Defect: seed comparison is not reproducible. 5/30 used OS entropy per the document; current real path uses `_seed_from_hash(render_request_hash or cache_keys.request_hash or shot_id)`. Concrete fix: for any comparison to 5/30, record the exact 5/30 seed if available; otherwise treat seed as an uncontrolled variable and run multiple deterministic current seeds to estimate variance.

OPTIONAL / NICE-TO-HAVE:
- Add a one-shot debug log from `eng_ltx_video.render_clip` with effective sampler mode, sampler name, CFG, I2V strength, canvas, source length, loop flag, positive prompt chars/sha, negative prompt chars/sha.
- Add a small unit test for `_ltx_motion_role_key` covering synthetic open, announcer, music close, inter, and sfx.
- Add a trace diversity helper specifically for `prompt_source=="motion_role"`; current `ltx_prompt_diversity_status` only evaluates `brief+beat`.

CUT THESE (over-engineering):
1. [OPTION: NEW LLM motion-prompt pass] Cut for this build. Grounded code already has role-specific 6/5 motion templates and trace stamping. An LLM pass adds nondeterminism, cache/keying, timeout, prompt sanitation, and VRAM/residency risk without first proving the existing motion branch is live.

2. [Proposed faithful-restoration / engine-local fallback vs brief-level] Cut engine-local fallback. `eng_ltx_video.py` correctly treats `text_prompt` as required input and should stay model adapter only. Prompt composition already lives in `render_driver.build_request_from_shot`; keep the fix there to avoid hidden prompt precedence split-brain.

3. [Candidate levers #2] Cut making ksampler the default in the same change as prompt repair. The measured smoke says ksampler is secondary and insufficient alone; changing sampler while changing prompts destroys attribution. Keep it env-selectable until the prompt-only controlled smoke is done.