# Close-out, 2026-09-20 -- what four lanes found and what was done with it

One prompt (`PROMPT_closeout_all_lanes.md`), four reports, every row grounded
against the files by the coding window before it was taken. Codex was out of
quota and did not run. The counts are the lanes' own.

| lane | must / should / docs | taken | refuted or declined |
|---|---|---|---|
| Composer 2.5 | 1 / 2 / 3 | `_stamp` cleared Lemmy's bark preset on a provisional stamp (#11) -- fixed at the root, `8d064235`; the replay test's positional engine (#2) and the HF stub's missing `**kwargs` (#9) -- both stale tests, corrected; README bank and graph counts; `apple/RUN.md` and `apple/MUSIC.md` no longer say the fidelity banks refuse a translation | -- |
| Grok 4.6 | 0 / 7 / 9 | `.cursor/` excluded from the registry zip (seven tracked rule files would have shipped), `7f284264`; the `lemmy_cameo` tooltip no longer says `never` takes a cast slot; the stale "four scenes" comment beside the live vendored call; README bank roll and shakespeare row; five "sixteen variants" counts across the apple docs; `apple/BANKS.md` and `apple/MULTILINGUAL.md` name the vendored corpus | listing the two run-to-run swaps (#14a/#14b) as expected failures -- variance explained in the ship note is honest, a listing would hide the day one becomes real |
| Gemini | 0 / 1 / 1 | the `custom_premise` tooltip no longer demands text the standing-premise floor made optional on 09-13; `apple/BANKS.md`'s language-compatibility sentence names the vendored corpus, `fd3e2311` | -- |
| Antigravity | 0 / 1 / 0 | `AGENTS.md` added to `.comfyignore` beside `CLAUDE.md` | the finding as stated is refuted: `AGENTS.md` is untracked (`git ls-files` is empty), and the zip is built from tracked files, so it could not ship today; the line is the same guard `CLAUDE.md` has, against the day it is added |

**Then the operator's own rule, applied on top (`7fcc3518`, `ea5eb14d`,
`db74f4f3`): no ordinal and no positional count in the shipped docs** -- "a
seventh row", "the twenty-second", "twenty-one of the twenty-two", "the
seventh, the IndexTTS2 voice cloner", "Source banks are the fifth kind",
"all eight admitted languages", "Ten visual styles", "All 25 nodes", "the
tenth one" -- each now says the thing without the number, so a new graph,
bank, style or language cannot make a sentence false. Structural cardinals
that cannot grow (three image roles, two cues) stay.

**All four lanes agreed on the day's `.py` diff:** no orphan helper, no
work marker, no debug print, no absolute path, no non-ASCII in a log or
print string; the tooltips of `source_bank`, `source_ref`, `visual_style`
match the code; `.comfyignore` ships everything that must ship.

**State for the ship:** `docs/2026-09-19-ship-regression.md` -- of the
fourteen known failures, #2, #9, #11 are closed; the eleven left are
cloud-lane, on-disk-artifact or test-hygiene drift, none on the shipping
still lane. `pyproject.toml` reads `2.1.6`; the next string is the
operator's to name.
