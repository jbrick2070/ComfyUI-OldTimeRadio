<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. The draft still has unresolved seam questions, contradicts the “no schema change” constraint, and asks for gates/rerolls without defining the composer/reroll integration points.

MUST-FIX BEFORE BUILD:
1. [Open Q1 / S1] The safest suppression point for music_inter text is still explicitly open. Grounding confirms `_assemble_outline` sets `music_inter.intent` to `f"Musical interlude bridging {phase_name} into the next phase."`, but the plan does not define whether ledger `line.text`, transcript rendering, or caption burn-in is authoritative. Concrete fix: before coding, verify: `production_ledger.init_lines_from_outline`, the line composer, transcript exporter, and caption burn path. Then implement one role-based helper such as `is_spoken_role(role) -> role in {"character", "announcer"}` and use it at the earliest shared text-materialization point so non-spoken roles get `text=""` or are skipped consistently while timing/music rows remain.
2. [C2 / WIRING] “Derive one `central_object` onto the dramatic state” contradicts the “no word/beat-count or budget/schema change” / fixed ledger-schema constraint. Grounding shows `DramaticState` has only `dramatic_question`, `character_a_wants`, `character_b_wants`, `costly_choice_beat`, and `ending_change`; there is no `central_object` field. Concrete fix: either store `central_object` in existing ledger `meta` under an agreed key, or explicitly declare and test a schema change. Do not silently add a Pydantic field to `DramaticState` while claiming no schema change.
3. [S3] “Inject opposed wants ... only when DramaticState wants are SOURCE-DERIVED / NON-DEFAULT” is not implementable from the grounded schema. `DramaticState` carries no provenance flag, and `derive_dramatic_state_from_meta` currently emits default wants using `_DEFAULT_A_WANTS` / `_DEFAULT_B_WANTS` with cast names. Concrete fix: add a deterministic helper in `_otr_dramatic_state.py` that classifies default-derived wants, or add explicit provenance where the state is created. Without that, the line prompt will inject generic default wants and worsen dialogue.
4. [S3] “Require each beat intent -> an ACTION VERB UNDER PRESSURE” is placed at the wrong seam. Grounding shows beat intents are produced in `_otr_outline.generate_outline` Stage 3 via `_build_beat_user_prompt` and `_BeatFleshout`; the line composer only consumes the already-created `Beat.intent`. Concrete fix: implement the action-verb requirement in `_build_beat_user_prompt` and/or a `post_validator` on the Stage 3 `structured_call` for `_BeatFleshout`, not only in the line-composer prompt.
5. [Open Q2 / S3 / C1-C5] The targeted reroll seam is undefined. `_otr_line_hygiene.py` is currently pure deterministic scrubbing/detection only; it has no composer or reroll mechanism. Concrete fix: verify the exact line-composer function and its existing retry/recompose path, then add one shared wrapper that accepts `(beat, current_text, reject_reason, context)` and performs bounded targeted rerolls. Hygiene should only return flags/reasons; it should not call the LLM.
6. [S2] The announcer-close reroll path is underspecified. The plan says “reroll via the DEDICATED ANNOUNCER composer,” but that function is not grounded here. Concrete fix: verify the announcer composer function name and call signature. Add the close-only banned-thesis scan there, not in the character critic path. Define the scan as concrete regexes with case-insensitive matching and apostrophe handling, e.g. straight/curly `Tonight['’]s revelation`; do not leave wildcard strings such as `proving * right` / `* is now shared` ambiguous.
7. [C1] The “all-generic line” gate lacks a deterministic scope and detector. “High-content beats” is undefined, and “no proper noun / no anchor” will false-pass on speaker names or sentence-initial capitalization unless specified. Concrete fix: define high-content beats as a concrete set, e.g. `speaker_role=="character"` and `arc_phase` not opener/closer/music and `beat.intent` contains one of the action verbs, or another explicit rule. Define anchor matching case-folded against `meta["specificity_anchors"]`; exclude cast names and first-token capitalization from proper-noun credit.
8. [C3 / WIRING] `speech_signature`s may require a cast schema change. The draft says “At CastLock, derive ... speech_signature,” but no CastLock/cast-row schema is grounded. Concrete fix: verify: cast row model/d