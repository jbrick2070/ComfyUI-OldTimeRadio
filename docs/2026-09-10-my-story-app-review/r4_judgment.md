# R4 judgment: scope convergence and operator cuts

VERDICT: converged as a scope, not an implementation-ready bank or preflight
PASS. Both external reviews completed. Codex is grounded driver and sole judge.
Their remaining bounded contract corrections are resolved below. No unresolved
scope decision warrants another panel call. D1 exact design and finished-code
review remain implementation prerequisites. No runtime edits or tests occurred.

The final operator clarified ownership (Opus cleanup, Codex My Story), requested
the Opus starter prompt, and explicitly cut ALL My Story replay-system work,
not just old-bundle migration. The driver removed source-snapshot restoration,
saved-episode re-authoring/rerendering requirements, related App controls and
qualification. Native saved-input reuse generates a fresh episode. Existing
bypass settings receive a cheap early incompatibility error for My Story so
they cannot silently replace submitted fields. Earlier review recommendations
are subordinate to this scope cut; no reviewer is claimed to have re-reviewed
the final owner/cut wording. The actual R4 input and GO_FORWARD snapshot remain
unchanged in the evidence archive.

## Cursor dispositions

| Claim | Grounding and decision |
|---|---|
| M1 player source | CONFIRMED path distinction already intended, wording sharpened: mux obs_copy/meta.obs_final_path is AAC publication; final/final_video_path/result0 remains PCM archive. Actual mux1464, terminal stamp1476, return1521 inspected. |
| M2 always-connected intent vs existing banks/old replay | CONFIRMED new-run compatibility seam. Stamp typed envelope on every NEW bank run, true only for My Story. Reject the proposed old-replay synthesis/migration under explicit operator cut. No new saved-episode support requirement. |
| M3 snapshot short-circuit | CONFIRMED resolver297 source-snapshot arm precedes original/custom. Operator cut removes new snapshot integration entirely: direct My Story ingress before loader, incompatible snapshot setting rejected early, existing other-bank route unchanged. No old manifest qualification or loader extension. |
| S1 two writer checks | CONFIRMED raw dedicated-field mismatch before rolls and evaluated admission after row binding. Clarified separate checks; persistence remains coordinator-owned, not resolver-owned. |
| S2 defaults paths | CONFIRMED _BANK_KEYS217 and _parse_bank237: name defaults.story_input_mode/defaults.auto_select explicitly. No top-level key extension. |
| S3 typed prompt reading | CONFIRMED already covered. Do not apply STRING-only _literal to INT/COMBO or treat missing new strings as links. |
| S4 save blank/rejected/canceled-before-execution | REJECTED expansion. User requested durable useful inputs, not empty rejected records or a frontend interception service. Clarified persistence at admission before generation and survival of later failures/cancellation; queue cancellation before nodes execute is not promised a new file. |
| S5 raw requested count | CONFIRMED already sidecar before clamp, retained. |
| S6 tuple then UI envelope | CONFIRMED explicit chunk boundary added. Keep tuple while adding delivery enforcement; UI-dict and four direct unpacker migrations land together later. RETURN_TYPES stay STRING,STRING. |
| S7 cameo | CONFIRMED already D1 obligation and D2 matrix target. Test map396 uses observed strings, not invented enum; _otr_casting1178 owns exclusion. D1 must specify the actual user-cast/house-cameo behavior and receipt before code. |
| S8 act breaks | CONFIRMED current widget5=true; changed 'hidden' to 'existing'. D1 still declares treatment rather than assuming off. |
| S9 pipeline | CONFIRMED existing complete new-bank coordinates/dispatch/no copied original seams contract retained. |
| S10 resolver args | CONFIRMED actual signature123 has *, now explicitly optional keyword-only fields/empty defaults and unchanged seven-key envelope. |
| S11 labels | UNVERIFIABLE from in-repo source alone, but driver already inspected installed frontend maps in earlier rounds. Keep native Builder save/reload and browser proof; no renamed input key or invented option aliases. |
| S12 widgets/links | CONFIRMED existing tests/variant audit obligations; make mux input10 and preserved287/288 explicit. Allocate above current290, rebaseline actual build HEAD. |
| Optional strip/path/comment/license | Blank semantics/path containment already covered. Roster comment can be currentized with changed eligibility. Synthetic license is not a mandatory permission stamp; freeze remains authority. |
| Cuts | Agree no copied runner/seams, retired interfaces, new payload key, duplicate tail context, custom dropdown/wizard, gate reindex or top-level policy. Retain small optional speaker design note because operator supplied it; remove replay work there too. No numeric DSP approved. |
| Verify-at-build items | Browser/core context/cache/budgets/current defaults/actual model receipts retained. Old replay/snapshot qualification cut. Canonical hash must be recaptured AFTER cleanup, not required to equal this scope's hash once P1 intentionally changes defaults. |

## Gemini dispositions

| Claim | Grounding and decision |
|---|---|
| M1 core import | CONFIRMED import-isolation risk, already explicit direct-call identity. Add lazy context import. REJECT supplied helper: installed utils.py ExecutionContext is NamedTuple, not dict, and shared test_standalone/node1 fallback collides across writers. Explicit context or writer-owned identity only. |
| M2 return/callers | CONFIRMED boundary clarified as above. MISREAD AUDIO return declaration: actual mux1069 is STRING,STRING. Reject both-shape shim. Four success unpackers migrate atomically with UI-dict, raising call stays raising. |
| M3 cameo map | CONFIRMED matrix obligation already included. MISREAD registry path/data layout, enum, helper location and line numbers: real nodes/story_packs/banks.json; map is strings at test396; exclusion helper in _otr_casting. Do not invent CameoPolicy.FORBIDDEN or claim observed behavior before D1/qualification. |
| S1 relative links | Captured plan is a docs-based artifact; archive paths naturally differ. Reject nonexistent docs/planning paths. Delivered scope links resolve from docs; archive README explains original base. |
| S2 warning ambiguity | MISREAD: R4 input explicitly says fail before rolls/downloads, not warning. Keep precise two writer sites and validator check; resolver-only rejection would be late. |
| S3 persistence path/atomicity | MISREAD: input already uses digest directory and atomic/idempotent writes, not bank_prompt.json. Retain same-directory atomic replace. Reject unsolicited retention deletion/size caps on durable user drafts. |
| Optional schema/JSON UI | Versioned draft schema already required. Raw JSON UI download is not needed for this low-friction slice; optional extra omitted. |
| Cut custom hiding | CONFIRMED already out of scope. No custom frontend dependency added. |
| Verify-at-build | Media browser proof, context isolation, live slot/link audit and read-only cache validation retained. Current counts are evidence, actual build schema is authority. |

## Handoff and completion

Both internal helpers found GO_FORWARD top/pickup/attack-order and scope §9
consistent: P1 -> P2 -> P3 -> D0/D1 -> creator backend/canonical publication ->
optional App presentation and qualification. Their audit preceded the final
owner/replay cuts, which the driver synchronized across both documents.
Opus's separate cleanup review is not rerun or counted in this campaign.
The delivered prompt starts only its bounded cleanup and hands off to Codex.

Actual campaign calls: 8 successful external reviews (Gemini + Cursor in each
of R1-R4) plus 1 failed R1 Claude authentication attempt = 9 attempts. No
duplicate Codex CLI. No cost receipt available. Exact byte chain and final
canonical-unchanged hash are checked by the packaging receipt.
