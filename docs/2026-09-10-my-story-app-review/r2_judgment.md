# R2 judgment - implementability

Driver Codex; successful Gemini3.8 Flash High and Cursor Grok4.6 High reviews.
Advance the revised scope. D2 is not build-ready until independent D1 delivers
its reviewed schemas/pass graph/ownership/cast contracts. No implementation.

User steering during R2: optional character/plot/setting fields, visible act
and character counts, saved input drafts, and a published episode as the only
successful My Story deliverable. R2 final incorporates this; R3/R4 read it.
R2 input/anchor are unchanged. grounding.md is supporting source evidence,
not a panel seat. Cursor artifacts originated in the separate cursor topic.

## Cursor (review order)

| Claim | Grounding / decision |
|---|---|
| M1 unlocked D1 | CONFIRMED intentionally. Make it a hard prerequisite; scope lists integration surfaces, not a completed independent design. |
| M2 ingress swallowed | CONFIRMED resolver325-371. Policy-led branch after validated snapshot, before original and generic custom. No fake fetcher. |
| M3 roll policy | CONFIRMED. Lock optional scalar story_input_mode/auto_select, absent legacy/true; user_fields_v1 requires false. Predicate runnable AND effective auto_select. No required top-level key. |
| M4 helper/link handling | CONFIRMED _literal unsuitable for unresolved links; factor existing reachability. Reject all-blank-only skip: ANY invalid applicable reachable writer raises before downloads. Never return a successful gate merely skipping assets. |
| M5 evaluated admission | CONFIRMED exact site immediately after source_bank_row binding, before preflight/env/resolution; already intended. |
| M6 signature/tail | CONFIRMED live keyword call and own duck-typed tail attributes; document list, no imported bank-specific class or duplicate context builder. |
| M7 hidden fields/six cap | User now keeps counts visible. Hidden meant omitted from App, not absent from node. Six cap MISREAD: current widget/voice stock10; selected route/reserved stock is actual boundary. Keep requested/actual counts separately. |
| M8 cameo | CONFIRMED exhaustive integration obligation; exact cameo policy remains D1 prerequisite, no inherited random injection. |
| M9 no budget API/truncation | MISREAD existing implementation. fit_output_tokens plus backend context resolution exists; HF measures actual chat-template tokens, GGUF resolved cap/estimate. Accept per-artifact repair budgets and terminal no-room handling. |
| M10 mux/format | CONFIRMED dict result/real output-root containment/four successful unpackers. Required format MISREAD: installed isVideoBySuffix recognizes MP4; format optional. |
| S1 seams | CONFIRMED: custom declarations cannot shadow production; pass refs use union, custom pack/ref/declaration parity required. |
| S2 dispatch | CONFIRMED LANE_SPECS only, not INLINE_PIPELINES. |
| S3 rights | Truthful provenance accepted. Mandatory synthetic-original label REJECTED: it implies commercial-use allowed in _otr_provenance:84. Nonbibliographic identity N/A is valid. |
| S4 labels | CONFIRMED stable names/raw combo IDs. Native rename writes shared widget/input label; not localized_name or linearData widgetName. |
| S5 builder/defaults | CONFIRMED existing plan: real builder IDs, graph default, saved rolls unchanged. |
| S6 fixtures | CONFIRMED count/roster/roll assumptions need update; three appended fields after replay_from32. |
| S7 D0 first | CONFIRMED existing sequence; later operator content/word rulings prevail. |
| S8 payload | CONFIRMED exact seven strings and nonempty seed; versioned raw bundle/digest in sidecars. |
| Optional/cuts | Keep two slots, no duplicate TailParts/context builder, inline-original template, new executable gate, retired hooks or UI wizard. |
| Assumptions | Maps read independently by driver/helper; format claim disproved. Real browser proof remains pending. |

## Gemini (review order)

| Claim | Grounding / decision |
|---|---|
| M1 programmatic rename | MISREAD plan's label wording. Explicit stable keys added. Proposed linearData label triple is invented; actual config supports height/description, shared native label is separate. |
| M2 original hijack | CONFIRMED risk. Reject global hardcoded original restriction; insert policy-led branch before existing arms and preserve existing banks. |
| M3 runner(ctx) | MISREAD invocation: actual keyword list. Tail attributes are required and D1 must define own shapes before D2. |
| M4 direct unpackers | CONFIRMED four successful calls and one raising call. Keep second result wire as report TEXT, not proposed report_path. |
| M5 invisible report | CONFIRMED ui.text ignored, though plan did not promise that key. Real episode-specific ui.files report; reject shared reports/episode_status.txt. User steering: a report accompanies, never replaces a published My Story episode. |
| M6 replay | CONFIRMED two authority paths. Reject snapshot-exists bypass: validate versioned fields/digest, mismatch and strict/allow-partial semantics. Frozen-episode replay ignores live input. |
| M7 early check | CONFIRMED exact site, already required. |
| M8 registry keys | CONFIRMED scalar defaults, no unknown top-level fields. |
| S1 fields/sidecars | CONFIRMED plus user steering; preserve raw before normalization, append three fields and forward them fully. |
| S2 cast count | CONFIRMED request semantics; visible count, explicit names, actual stock boundary. |
| S3 title | CONFIRMED tail731-741, explicit graph title wins; canonical blank. |
| S4 final tail | CONFIRMED clean/cleanup/reconcile can alter first draft; acceptance uses final text/TTS/hash/receipt. |
| Optional progress | Only coarse static Execution Message per node verified, not dynamic internal writing-pass events. |
| Optional history tests | Browser integration already required; no invented frontend test harness for a docs task. |
| Cuts | Accept no bespoke UI, provider/credential route, word controls or external-root viewer/copy workaround. Input drafts are a user-requested separate artifact. |

## Driver additions and unresolved build dependencies

Versioned draft storage on Run, raw controls before rolls, and distinct submitted
versus restored source input are explicit. My Story must publish a real file;
unexpected blocked eligibility is failed delivery. Freeze stays sole eligibility
writer; existing other-bank withheld behavior remains. D1 owes exact delivery
intent metadata, draft schema/ownership, cast/cameo and pass contracts. This
scope review cannot claim those design or live gates pass. No static finding
was recorded as a production bug; no executable/workflow changes performed.
