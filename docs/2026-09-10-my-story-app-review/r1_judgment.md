# R1 judgment - arc and user experience

Driver: Codex. Verdict: scope advances with fixes; no implementation or Gate 1
pass claimed. Two successful external reviews: Gemini 3.8 Flash (High) and
Cursor Grok 4.6 high. Claude sonnet/high failed authentication before review.
The input is immutable; the user's mid-round clarification is recorded in
../user_steering.md and incorporated only in final.md.

## Anchor decisions

All three anchor must-fixes CONFIRMED: independent design remains a separate
deliverable; manual-only input bank needs roll policy; replay/admission order
must be explicit. Grounded further: validator downloads precede writer (both
branches), so queued literal admission belongs before ensure_prompt_visual_assets.
Use actual PROMPT and dependency scope; defer unresolved linked values to writer.
Anchor should-fixes accepted: bounded docs inventory, shared defaults, meaningful
input interpretation and context policy. Hashes do not prove semantic fidelity.

## Gemini claim dispositions (review order)

| Claim | Grounding and disposition |
|---|---|
| M1 shared defaults | CONFIRMED usability risk, already partly covered. Make explicit my_story selection first. Reject separate graph/global auto-routing or new failure for existing text-plus-roll behavior; canonical and compatibility rules win. |
| M2 split OBS roots | CONFIRMED supported external override; MISREAD as current canonical launcher defect. _otr_paths:643 permits override; launcher:81-87/190 aligns default roots. /view serves folder_paths root, not every OTR override. v1 supports aligned player, truthful unavailable preview elsewhere. No duplicate AAC subsystem required. |
| M3 forced announcer/coda | MISREAD. Dispatched return at writer:3434-3480 precedes inline intro/coda; scifi_news_pro required_seams empty; GUIDE allows no announcer. New bank may own neutral framing. |
| M4 no progress / 95 nodes | MISREAD. Native Run/Cancel/queue/errors exist; graph has 23 nodes, last ID 95. Accept browser busy/error/cancel proof; reject custom progress scope and invented render-duration promise. |
| M5 drop D0 gate | REJECTED: explicit user requested README and linked preflight currentization. Bound inventory and preserve dated evidence instead. |
| S1 linearMode locks developers | MISREAD lockout; view toggle exists. Choose graph default for v1, save App layout without changing reopen behavior. |
| S2 seed needed for freshness | MISREAD. Writer IS_CHANGED:2793 always reruns; tail cast RNG uses entropy unless overridden. Add freshness/replay checks; no seed control. |
| S3 cloud incompatibility | MISREAD policy. CLAUDE prohibits paid quality chasing/adopting a paid default; existing preflight requires configured compatibility proof. Keep local default; no new credential; missing cloud evidence is recorded pending/N/A, never blanket PASS. |
| S4 1500-character limit | UNVERIFIABLE number; reject. _otr_generation_budget:213 measures effective context. Specify full prompt/output/repair capacity and no silent truncation. |
| S5 mandatory user license/publishable stamp | MISREAD absent rights versus absent eligibility. _otr_publication_eligibility:90-112 treats originals as informational; freeze:976/1102 solely stamps eligibility. Verify same-episode receipt, invent no license or second stamp. |
| Optional hardware/title controls | No demonstrated v1 need; defer. |
| Cuts and assumptions | Duplicate D0/cloud cuts rejected above. Frontend playback and actual deployment version are verify-at-build. No proven WebSocket defect or custom progress requirement. |

## Cursor claim dispositions (review order)

| Claim | Grounding and disposition |
|---|---|
| M1 narrow product to sci-fi radio | MISREAD imposed genre; canonical title/presentation does not require new runner genre. Preserve broader user intent, disclose existing visual/audio production framing. |
| M2 shared premise copy lies | CONFIRMED need bank-honest help. MISREAD proposed per-view label isolation: AppModeWidgetList rename mutates shared widget/input label. Use shared Story input label and App-only description; no false claim other banks ignore text. |
| M3 hidden two-character default | CONFIRMED. Canonical saves2; inline lock at4039 is bypassed by dispatched return3473. New bank cast authority must override hidden default locally. Cited test390 concerns retired lane, not current measured scifi_news_pro behavior. |
| M4 independence exposure | CONFIRMED disclosed exposure cannot meet pristine retrospective chronology. D0 explicitly clarifies shared-interface study versus independent architecture; retain six dimensions, no backdating or automatic pass. |
| M5 stale content rule before D1 | CONFIRMED contradiction resolved by later CLAUDE. D0 explicitly precedes prompt/schema authoring. |
| M6 roll plus text | CONFIRMED risk; proposed inert-text explanation MISREAD existing hint/User Seed behavior. Explicit source-first instruction; preserve existing runs, no new global rejection. |
| S1 guide_ref six | CONFIRMED banks.json:177, used routing:764. Scope later registration-copy correction separately from Markdown D0. |
| S2 graph reopen default | Accepted bounded v1 choice; no independent App defaults. |
| S3 remove cloud proof | Same policy misread as Gemini S3; configured lane only, record unavailable proof honestly. |
| S4 voice identity | CONFIRMED correctness under CLAUDE. Preserve explicit attributes; infer no gender solely from names. |
| S5 external OBS preview | CONFIRMED with aligned-root limitation and truthful report. Installed version is package evidence, browser proof pending. |
| S6 one dispatched lane | CONFIRMED _otr_lane_specs:75-89. State new dispatched bank choice; do not inherit inline original. |
| S7 App applicability | CONFIRMED already scoped; keep bank-specific UI checks N/A when unrelated. |
| Optional progress / signpost distinction | Accept guidance and current raw combo IDs. BankRow.label does not alias native array options. |
| Optional one-act break | MISREAD implication: episode_budget:254 has act_count-1 intermissions, hence zero at1. Declare new bank policy without claiming current forced break. |
| Cuts1-5 | Bound D0 to named current-use sections; retain user-requested work. Accept no new defaults/retired interfaces/wizard. Historical plans not rewritten. |
| Assumptions | Package1.49.6 verified locally; canonical hash unchanged. LLM interpretation is intended functionality, not proven until independent design and qualification. |

## Additional grounded integration corrections

Mux .mux currently returns a tuple. A Comfy UI dict preserves wire outputs in
result, but direct Python unpackers in test_publication_eligibility and
test_video_render_path_cw4 must migrate explicitly. Freeze remains sole
publication receipt writer; archival PCM is not a browser-AAC substitute.

D0 inventory is fixed to four primary docs, bounded current-use lessons,
SOAK_LEG_GUIDE sections3-5, docs/README navigation, and later guide_ref copy.
User-bank inspection/activation and creative/technical cleanup/freeze ownership
are grounded against live definitions, not stale docstrings.

No production bug entry: all findings here are static scope observations.
