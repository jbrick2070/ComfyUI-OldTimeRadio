Reviewed `65e4801b..fbfbe0d65475e3479837cbcc6addde154138f664` against the real Windows files. The reviewed implementation, tests, manifest, scene texts, and cited parser/source files had no working-tree difference from `fbfbe0d6`. Counterexamples below are derived from the cited control flow; no vendor invocation or GPU work was performed.

1. **ORDER — REFUTED.** `scripts/otr_vendor_scan.py:535` builds the folded roster; `:536` computes `key`; `:540-541` consults and returns from `FOLDS`; only `:542-543` performs the exact lookup. The prefix pass is later, at `:563-571`. There is no preceding exact or prefix pass.

   With roster `{"MIRANDA", "FERDINAND", "PROSPERO"}`, the accepted declaration `--fold MIRANDA=FERDINAND` makes `resolve("MIRANDA", roster)` return `FERDINAND`, although the exact pass would return `MIRANDA`. Validation at `:1276-1280` accepts this because FERDINAND is in the roster. Likewise, `--fold PROSPER=FERDINAND` overrides the existing prefix binding to PROSPERO. The module comment at `:87-88` explicitly says the fold precedes every other rule, contradicting the commit message's claimed fallback order.

   **Empty-map subclaim — HOLDS:** with `FOLDS == {}`, `key in FOLDS` is false, so this added branch cannot change any binding. Being positioned before the prefix pass does not itself change a no-fold invocation. No empty-map regression was found.

2. **KEY PARITY — REFUTED.** Both paths use `fold()`, whose entire normalization is NFD decomposition, removal of combining characters, and uppercase conversion (`scripts/otr_vendor_scan.py:227-230`). Case and accent handling therefore agree. Their surrounding transformations do not:

   - Lookup (`:536`): `fold(_LABEL_QUALIFIER.sub("", token)).strip(" .")`.
   - Declaration (`:1272`): `fold(printed.strip())`.

   For printed form `Férnando.`, `--fold Férnando.=FERDINAND` stores `FERNANDO.`; resolving that printed form looks up `FERNANDO`. The declaration passes validation but cannot supply the intended binding. For `Fernando (aparte)`, the declaration stores `FERNANDO (APARTE)`, whereas lookup removes the qualifier using `:517` and asks for `FERNANDO`. These are concrete key mismatches, not differences in accent folding. The shipped bare `FERNANDO` declaration does have parity.

3. **LEAK — REFUTED for “cleared on every entry, before any roster lookup.”** `main()` begins at `scripts/otr_vendor_scan.py:1133`; its only `FOLDS.clear()` is at `:1269`, after `roster_for(args.stem)` at `:1260` and the empty-roster return at `:1263`. Earlier exits include missing lead (`:1186-1188`), invalid page window (`:1198-1202`), probe completion (`:1206-1219`), rejected boundary (`:1240-1248`), and missing scene (`:1255-1257`). Parsing and I/O can also exit before the clear. A successful invocation leaves its populated map in place when returning at `:1319` or `:1374`; a later early-return invocation leaves that old map intact. A valid fold followed by an invalid specification also leaves a partially populated map (`:1270-1280`).

   **Successful sequential extraction subclaim — HOLDS:** a second invocation that reaches speech extraction clears the old map at `:1269`, before `speeches_from_span` at `:1284`. Thus two successful sequential calls do not reuse the first scene's folds. The early-return case retains stale global state, but does not itself extract a second scene. A later direct `resolve()` can still use that stale state whenever its target occurs in the supplied roster (`:540`); roster membership is not a scene-identity check.

   **New-test cleanup subclaim — HOLDS:** `tests/test_vendor_scan_furniture.py:443-444` clears the map in `finally`. `_scan()` constructs a fresh module object on every call (`:21-29`). This test does not call `main()` twice: it exercises `resolve()`, then merely searches `main()` source text (`:435-448`). Its string assertion cannot prove where the clear occurs or what happens on early returns.

4. **CONSUMER — HOLDS: the added `folds` key is accepted.** `nodes/_otr_verbatim_corpus.py:577-581` enumerates required fields, not an exclusive set of allowed fields. `load_manifest()` checks missing fields (`:624-627`), blank required fields (`:631-635`), confidence (`:636-646`), and `speaker_map` (`:650`); it returns copies of the complete rows at `:666`. It never rejects unknown row keys. `speaker_bindings()` reads only `speaker_map` (`:690-704`). `select_scene()` returns a row copy (`:733`); `vendored_text()` loads/selects it (`:817-818`) and verifies the text hash (`:836-839`). The writer passes `speaker_bindings(_vendored_row)` to the planner, rather than unpacking arbitrary row keys (`nodes/OTR_LedgerScriptWriter.py:3825-3841`).

   `tests/test_verbatim_corpus.py:290-307`, `:429-439`, and `:620-648` check required fields, schema version, confidence, and map shape; none enforces an allowed-key whitelist. Its full-manifest consumer tests at `:739-784` and `:1457-1473` ran with these new rows present. No inspected consumer chokes on `folds`.

5. **TEXT — REFUTED.** `config/source_banks/shakespeare/translations/pt/tempest_1_2.txt:86` ends Prospero's speech with the literal running header **`SOENA II A TEMPESTADE 31`**. Both `SOENA` and `TEMPESTADE` are standalone uppercase tokens. This is furniture embedded in dialogue, not a separate numeric line. The manifest nevertheless marks this scene `READY` with confidence `1.0` (`config/source_banks/shakespeare/translations/manifest.json:1638-1639`).

   The exhaustive line audit found:

   | File and audited lines | Non-`LABEL: speech` lines | Labels absent from its map | Digits-only lines | Specified uppercase furniture tokens | Lowercase-to-uppercase welds |
   | --- | --- | --- | --- | --- | --- |
   | `config/source_banks/shakespeare/translations/pt/tempest_1_2.txt:1-137` | None | None | None | Line 86: `SOENA`, `TEMPESTADE` | None |
   | `config/source_banks/shakespeare/translations/pt/tempest_3_1.txt:1-25` | None | None | None | None | None |

   The five labels in 1.2 are MIRANDA, PRÓSPERO, ARIEL, CÁLIBAN, FERNANDO, all present at `manifest.json:1643-1664`; the three in 3.1 are FERNANDO, MIRANDA, PRÓSPERO, all present at `:1689-1702`. Line counts match the manifest's 137 and 25 (`:1641`, `:1687`), and both files' SHA-256 values match their manifest hashes (`:1637`, `:1683`). Correct counts and hashes therefore do not exclude this contamination.

   For completeness, a case-insensitive search also finds ordinary dialogue `tempestade` at 1.2 lines 41 and 44 and `acto` at line 47; these are not running headers. Lowercase `scena` at line 98 belongs to the leaked stage instruction discussed below. The weld audit used Unicode lowercase-to-uppercase transitions inside non-whitespace tokens, including single-capital transitions.

6. **ADDITIONAL DEFECT — REFUTED: Ariel's song is assigned to Prospero.** `config/source_banks/shakespeare/translations/pt/tempest_1_2.txt:98` begins `PRÓSPERO:` but includes `Ariel torna á scena, invisível, a tocar e a cantar; Fernando vem-no seguindo. Canto de Áriel Desembarca nesta areia dourada` and the ensuing song and chorus. The next ARIEL label does not occur until line 99. This is a wrong speaker boundary despite every existing label being mapped and every line satisfying the requested shape.

   The same line explicitly identifies the song as Ariel's. The local English source independently separates Prospero's exit cue at `config/source_banks/shakespeare/sources/tempest__act1_scene2.txt:597` from ARIEL and “Come unto these yellow sands” at `:605-606`. The runtime cannot recover that lost boundary: `_colon_prefix()` returns the leading label and the remainder of the line (`nodes/_otr_passage_selector.py:369-379`); `parse_speeches()` assigns that remainder to the leading speaker (`:459-473`); `_bind()` preserves the speech text (`:416-427`). The manifest maps PRÓSPERO to PROSPERO (`manifest.json:1648-1650`). Thus the parsed speech carries Ariel's song under PROSPERO, along with an unbracketed stage instruction. This defect is separate from the header at line 86 and is introduced by the newly vendored text.

Verification: the two authorized files passed together, **160 passed in 2.85s**, using the Windows venv with UTF-8, bytecode writes disabled, CUDA masked, pytest cache disabled, plugin autoload disabled, and a unique `%TEMP%` base directory. The single warning was an unknown `asyncio_default_fixture_loop_scope` option with plugin autoload disabled. The passing tests do not test precedence conflicts, CLI key parity, or repeated `main()` cleanup; the new fold test's actual assertions are at `tests/test_vendor_scan_furniture.py:435-448`. The text guard's forbidden strings at `tests/test_verbatim_corpus.py:1384-1390` do not include this header, and label/map coverage cannot establish who owns the words inside an otherwise valid line.

VERDICT: MUST-FIX: Remove the embedded header, restore Ariel's song boundary, and correct the fold precedence, normalization, and entry-reset discrepancies.
