<!-- requested_model: x-ai/grok-4.3 | resolved_model: x-ai/grok-4.3-20260430 -->

VERDICT: Transplant-ready after prose hardening on experimental/custom packs + minimal operator tooling (confidence 70).

NEW FINDINGS (things the local panel plausibly missed):
- [CONTENT QUALITY] archival_documentary.json motion_prompts all begin with the static framing "Continuous shot, same X throughout" followed by slow push/dolly/lateral verbs; this matches the "motion verbs, no set-dressing nouns" rule from production's render_driver table but will likely produce rejected or frozen clips on LTX-video at runtime because the prompts lack dynamic action escalation or camera energy that video models actually respond to. Evidence: the four prompts in the attached archival_documentary.json file; briefing explicitly flags these as "authored tonight" and un-reviewed against LTX best practice. Fix: rewrite each to lead with a strong verb sequence (e.g., "Slow push-in while reels steadily accelerate then settle, dust motes catching light, tape threading visibly").
- [DESIGN-LEVEL] When Jeffrey hand-copies pack #7 to create pack #13 at 1 a.m., the most likely error is leaving story_pipeline_id="legacy_many_pass" while the new pack is intended for simple_4_prompt_experimental (or vice-versa); registry._load_packs + _cross_validate will reject duplicates or missing seams but gives no actionable "you copied the wrong pipeline" message and no pack-template helper exists. Evidence: registry.py:140 (key = (bank, model, pipeline)) and the fact that custom_source_bank already defaults to simple_4 in banks.json. Fix: add a 30-line `tools/new_pack.py` that takes an existing pack path, forces the operator to choose pipeline, and emits a skeleton with correct required_seams.
- [COMPATIBILITY MIRROR] The AST extractors in compat.py are brittle to any production refactor that replaces AnnAssign or top-level dict literals (e.g., NewsBriefs becoming a @dataclass or _LTX_MOTION_PROMPT_BY_ROLE being built via dict.update); tests will then fail loudly but the re-pin instructions in CompatDriftError messages will have rotted because they assume the original syntactic forms still exist in the refreshed mirror. Evidence: extract_news_briefs_fields (AnnAssign walk), extract_motion_role_keys (ast.Assign + ast.Dict), and the driver note in the briefing. Fix: add a one-paragraph "when these extractors break you must manually diff the mirror and update both the pin tuples and the extractor" note to COMPATIBILITY_MIRROR.md.

CONTENT CRITIQUE (the story-pack prose itself):
- simple_4_prompt_experimental.json (custom_source_bank): pass_1_creative_story and pass_2_creative_ledger_fill are pure meta-instructions ("Create one complete... then fill the production ledger") with zero schema example or output contract. A real local model on 16 GB VRAM will either emit free-form prose that pass_3 cannot repair or will invent its own JSON shape. Rewrite pass_2 to contain an explicit minimal ledger skeleton plus "emit ONLY valid JSON matching this shape" plus the exact field list from LedgerWritingSpec.
- gentle_thriller.json and media_restoration_adventure.json (both media_archive): their dramatic_state_system and pitch_room_system prompts are thematically close but use different urgency vocabularies ("looming public deadline" vs "institutional pressure"); when an operator flips between the two models the resulting episodes will feel tonally inconsistent rather than deliberately varied. Add a shared "media_archive_dramatic_core" label in BankDefaults and reference it from both packs.
- faithful_radio_adaptation.json: pitch_room_system tells the model "Do not invent divergent premises" yet the seam is still required by the public_domain_story bank; this creates contradictory pressure for any model that treats "three pitches" as license to vary premise. Change the seam text to "Produce three compression variants only; premises must be identical to the source."

ABSENT THINGS:
- No operator-scale workflow or CLI for "add a new story_model to an existing bank" (including the required banks.json default update and pack validation). Smallest addition: a single documented command in GO_FORWARD_PLAN.md plus a 20-line validator script that loads the new pack, checks required_seams against the bank, and prints the exact banks.json stanza to append.
- No test exercising the simple_4 runner with a real local LLM (only FakeLLM); the pass prompts may be underspecified for schema compliance. Smallest addition: a pytest that instantiates a tiny deterministic mock LLM returning a minimal valid ledger JSON and asserts round-trip through runner.run_pipeline + LedgerWritingSpec validation.
- Second-order effect after ~50 packs: Registry.state_digest + fixture_hashes walks the entire fixtures tree on every ComfyUI node refresh; this will eventually make the validator node slow. Smallest addition: a one-line note in nodes.py:IS_CHANGED that the digest cost grows linearly and a future optimization path (cached per-bank digests).

DO-NOT-DO:
- Do not promote simple_4_prompt_experimental to default_story_pipeline for any runnable bank (already correctly marked experimental and non-fallback in runner.py and banks.json).
- Do not add any adaptive-cleanup or max-pass logic back into runner.py; the briefing and contracts already cut it to documentation-only and the "fail loud, no fallback" rule must stay absolute.
- Do not relax the per-seam template-variable allowlist or the "JSON owns all prose" rule to make pack authoring "easier"; that is the exact source of the v1 drift the architecture was built to eliminate.