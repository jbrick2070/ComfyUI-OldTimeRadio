<!-- requested_model: moonshotai/kimi-k2.6 | resolved_model: moonshotai/kimi-k2.6-20260420 -->

VERDICT: Conditionally transplant-ready—the bridge machinery and science-news lane are architecturally sound, but archive/PD story-packs, motion prompts, and simple_4 pass prose require urgent rewrite before Jeffrey runs them on the RTX 5080 (confidence: moderate).

NEW FINDINGS (things the local panel plausibly missed):
1. [CONTENT/RUNTIME] The forbidden-pattern and tone-guardrail metadata never reaches the LLM; they are post-generation lint only, guaranteeing inference waste on a 16 GB VRAM machine. Concrete evidence: preview.py renders only a "Forbidden pattern count" line and explicitly notes "Forbidden patterns stay metadata: count only, never the phrases"; media_restoration_adventure.json defines eight `forbidden_plot_patterns` and seven `forbidden_leakage_terms`, yet none appear in any `prompt_stages` seam. Fix: Add a mandatory guardrail-injection step to `profiles.resolve_profile()` (or to `runner.py` for executable pipelines) that appends a formatted "Do not use..." block derived from the pack JSON into the first creative pass prompt; update `preview.py` to scan the fully rendered prompt (including the injected block) so leakage validation proves the model actually saw the constraints.
2. [CONTENT] The archival_documentary motion prompts are authored with set-dressing nouns, directly contradicting the production LTX-video best-practice rule and likely yielding static, object-focused shots. Concrete evidence: archival_documentary.json contains "catalog table", "archive boxes", "paper labels", "reels", and "guides" in its `motion_prompts` values; the briefing states the locked rule "motion verbs, no set-dressing nouns". Fix: Rewrite every motion prompt using only camera verbs and atmospheric dynamics, e.g., replace the announcer prompt with "Slow documentary push-in across labeled surfaces. Dust drifts in window light. Labels flutter faintly."
3. [SPEC-LEVEL] The simple_4 experimental pipeline's pass_1 prompt references "tone guardrails, and forbidden patterns" that `runner.py` never supplies, inviting hallucinated compliance from a local model. Concrete evidence: simple_4_prompt_experimental.json `pass_1_creative_story` says "Create one complete... story from... tone guardrails, and forbidden patterns"; `runner.py` only forwards the raw seam text (`pack.prompt_stages.get(seam)`) and does not concatenate the sibling JSON metadata lists (`tone_guardrails`, `forbidden_plot_patterns`). Fix: Either strip the dangling reference from pass_1 so the model is not asked to use invisible data, or implement an explicit `metadata_injection` helper in `runner.py` that appends formatted guardrail/pattern blocks to executable prompts before the LLM call.
4. [DESIGN/OPERATOR SCALE] Registry and ComfyUI node caches rely on Windows file metadata (`mtime_ns`, `size`) rather than content hashes, allowing silent stale-fixture service after git checkouts or 1 AM copy-paste edits. Concrete evidence: `nodes.py` `_lab_state_digest()` and `registry.py` `state_digest()` both hash `st_mtime_ns` and `st_size`; neither hashes file contents. Fix: Add a fast content-hash component (e.g., SHA256 of each fixture JSON) to the digest computation, or expose a "Force Reload Fixtures" button on `OTR_UpstreamStoryLabValidator` that explicitly nulls `_REGISTRY_CACHE`.
5. [COMPATIBILITY MIRROR] The mirror strategy detects shape drift but cannot detect mirror-file tampering or incomplete refreshes; an operator could edit mirror files to fake compliance while production diverges. Concrete evidence: `compat.py` parses the mirror but only checks the commit hash in `PRODUCTION_MIRROR_MANIFEST.md`; there is no stored manifest of expected per-file hashes for the mirrored tree. Fix: Generate a `production_mirror.sha256manifest` at every refresh and make the drift-test suite fail if on-disk mirror files do not match the manifest before AST parsing.
6. [DESIGN/RUNTIME] `VisualStylePolicy` silently accepts missing `motion_prompts` role keys, emitting empty strings to the video engine instead of failing loud. Concrete evidence: `contracts.py` `_motion_keys_allowed` only rejects unknown keys (`set(self.motion_prompts) - ALLOWED_MOTION_ROLE_KEYS`); `_otr_visual_style_policy.py` `motion_prompt_for_role` returns `""` for missing roles. Fix: Require every style that declares `motion_prompts` to supply all keys in `ALLOWED_MOTION_ROLE_KEYS`, or have the lab validator node report empty motion prompts as a hard error.

CONTENT CRITIQUE (the story-pack prose itself):
- media_restoration_adventure `outline_system`: The prompt is a theme salad ("damaged media, missing context, preservation labor...") with no structural guardrails; a local LLM will meander across acts and scene counts. Rewrite: append "The episode must contain exactly 5 scenes, 2–3 speaking characters, and one central archive object. Resolve the conflict through human choice, not technological magic."
- gentle_thriller `outline_system`: The non-violent constraints live only in `pitch_room_system` and metadata, yet the outline pass is the first creative generation step in the descriptive pipeline. Rewrite: prepend "Without weapons, gore, horror, or disaster spectacle, build suspense from..." to the outline prompt so the first draft is already constrained.
- simple_4_prompt_experimental pass prompts: Dramatically under-specified for a 16 GB local model. Pass 1 needs length caps ("Maximum 4 acts, 8 scenes, 300 words per scene"). Pass 2 needs the production ledger schema appended or it will hallucinate field names. Pass 3 needs explicit enum values. Rewrite pass 2 to include a minimal JSON schema example block, and pass 1 to include hard word-count and act limits.
- archival_documentary motion prompts: As noted in finding 2, rewrite to pure motion verbs and camera dynamics, removing all set-dressing nouns.

ABSENT THINGS:
- A `ledger_schema_fixture.json` containing the production ledger shape that simple_4 pass 2 can reference, so the local model isn't guessing at field names. Smallest version worth adding: a single annotated example ledger (≈50 lines) loaded as a fixture and validated against the real production schema.
- A `motion_prompt_linter` in the validator that rejects motion strings containing known set-dressing nouns (table, box, label, reel, canister, etc.). Smallest version: a regex-based check in the existing validator node.
- A one-page `PACK_AUTHOR_CHECKLIST.md` for Jeffrey specifying minimum dramaturgical requirements (e.g., "outline_system must declare scene count or length limit", "coda_system must name the speaker voice"). Smallest version: a markdown file in `docs/`.

DO-NOT-DO:
- Do not remove the metadata-not-prompt rule entirely; instead, make injection explicit and auditable (finding 1).
- Do not add automatic fallback from simple_4 to legacy_many_pass on failure; the loud failure boundary is correct.
- Do not simplify the registry's hard-error behavior for duplicate or missing seams; the strictness protects the operator from silent drift.