# Gap-Hunt Anchor (Claude driver, pre-fan-out)

Blind-spot hypotheses I bring to synthesis (each labeled):

1. CONTENT [CONFIRMED risk, my own authorship]: the five media_archive packs
   share one comic/warm register; episodes may converge tonally. The
   pitch-room seams vary protagonist/object/institution but not STAKES
   TEXTURE (public vs private, loss vs recognition). A pack-prose polish
   pass with a stronger dramaturgical rubric is cheap and unblocked.
2. CONTENT [CONFIRMED risk]: simple_4 pass prompts are one sentence each -
   a real local technical model will need the ledger's actual required
   fields enumerated in pass_2/pass_3 prose or it will guess field names.
   The experiment is allowed to fail, but it should fail for interesting
   reasons, not prompt vagueness.
3. RUNTIME [UNVERIFIABLE lab-side]: LTX motion prompts I authored for
   archival_documentary carry set-dressing nouns (reels, labels, boxes) -
   production's own design rule says motion verbs, minimal nouns; mine may
   read as "render this set" and produce flat pans. Needs an A/B against
   the production table's style at the visual stage.
4. OPERATOR [HYPOTHESIS]: hand-cloning a pack invites duplicate
   (bank,model,pipeline) keys - registry catches that loudly - but SUBTLER:
   forgetting to change forbidden_leakage_terms leaves the clone scanning
   for the wrong lane's terms; nothing catches a semantically-wrong term
   list. A pack-lint (terms must include the cross-lane base set) would.
5. MIRROR STRATEGY [CONFIRMED by design]: drift tests protect four shapes
   only. Production changes OUTSIDE the pinned shapes (e.g. a new required
   meta key consumed by freeze) are invisible until transplant integration
   tests. Mitigation is the re-mirror step 0 + production-side adapter test
   with the real NewsBriefs - already planned; the residual risk is
   between-mirror windows, acceptable for a one-operator cadence.
6. ABSENT [CONFIRMED]: no operator-facing "add a new story pack" runbook
   (CUSTOM_SOURCE_BANK_GUIDE.md covers banks, not packs). Cheap doc.
