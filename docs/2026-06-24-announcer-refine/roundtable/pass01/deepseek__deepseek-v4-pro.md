<!-- requested_model: deepseek/deepseek-v4-pro | resolved_model: deepseek/deepseek-v4-pro-20260423 -->

VERDICT: yes-with-fixes. The core architecture is sound and the grounding excerpts confirm key infrastructure exists, but three critical design gaps must be closed before build: (1) the open's no-spoiler enforcement is purely prompt-based and demonstrably insufficient, (2) the news coda's teaching frame directly contradicts the existing outro system prompt, and (3) KILL 2 lacks any deterministic enforcement mechanism despite KILL 1 proving instruction-following alone fails for body beats.

MUST-FIX BEFORE BUILD:
1. [2, JOB 1] No-spoiler enforcement is prompt-only, which KILL 1 already proved fails. The open's `compose_announcer_intro` receives only `script_brief` (grounding: `_otr_line_composer.py:2709-2775`), which can contain the outcome. Even the deterministic fallback echoes `script_brief` verbatim (grounding: `_otr_line_composer.py:2614-2632`). Fix: build a structured open from withheld-outcome fields only (time, place, cast, opening_situation from outline + StoryContract), never passing `script_brief` to the open's LLM call. Add a post-gate that checks the generated open against `news_close_brief` and `ending_change` for token overlap; if overlap > threshold, reroll once; if still overlapping, use a template fallback built from the structured fields only. The fallback must NOT read `script_brief`.

2. [2, JOB 3] The news coda's teaching frame ("The real story:" / "What actually happened:") directly contradicts the existing outro system prompt which forbids news-summary framing and demands a concrete final image (grounding: `_otr_line_composer.py:2519-2555`, lines "do NOT state a moral, lesson, or news-summary"). The current prompt is actively hostile to the teaching coda. Fix: rewrite `_ANNOUNCER_OUTRO_SYSTEM` to support a two-part structure: (a) the character-close reflection (preserving the concrete-image constraint), then (b) a deliberate transition to the news coda with the teaching payload. The transition phrase must be consistent across episodes (teachability) but the prompt should vary its exact wording within a small, fixed set of 3-5 recognizable variants to avoid mechanical repetition. The news payload itself (`news_close_brief`) must be delivered as a plain fact, not as drama.

3. [3, KILL 2] KILL 2 has no deterministic enforcement. The plan states "render in `_build_macro/phase/beat_user_prompt`" and "render for EVERY character beat" but provides no gate. KILL 1 proved instruction-following alone fails for body beats with weak local writers (mistral-nemo/gemma). The plan acknowledges this risk in section 1 but defers the answer. Fix: add a deterministic post-generation check per body beat that verifies at least N style-register markers from the StoryContract appear in the generated line (e.g., era-appropriate vocabulary, sound-world references, story-engine patterns). If the check fails, reroll once with amplified style instructions; if still failing, fall back to a template line that explicitly injects style markers. The check must be cheap (keyword/pattern match, not another LLM call). Define the marker set per style in the StoryContract itself. Without this, KILL 2 is the single-prior trap again and will not move weak writers.

SHOULD-FIX:
4. [2, JOB 1] The hardcoded open beat intent at `_otr_outline.py:1591` ("Open the episode and orient the listener.") is misleading because it does not drive content (grounding note confirms this). Fix: update the intent to reflect the new structured-open approach ("Set the scene: time, place, cast, opening situation. No spoilers.") so the beat metadata is truthful, even though the composer ignores it. This prevents future confusion.

5. [4, KILL 4] The 200-char truncation order is wrong: enrichment appends to `new_intent` then truncation at `_INTENT_MAX` can chop the appended tail (grounding: `_otr_story_quality_l12.py:794-806`). Fix: reserve tail space before enrichment — truncate the original intent to `_INTENT_MAX - len(enrichment_slot)` first, then append. If the reserved space is negative, truncate the enrichment slot instead.

6. [5, KILL 3] The outro's "last char line = resolution" assumption is a hidden coupling. `compose_announcer_outro` receives `final_character_line` and uses it to detect the dramatic resolution (grounding: `_otr_line_composer.py:2778-2867`). If KILL 3 allows the climax to NOT be the last beat, `final_character_line` may not be the resolution line. Fix: pass the actual climax line (by beat role, not by position) as a separate input to `compose_announcer_outro`, or tag the climax beat explicitly so the outro can find it regardless of position. The `ending_change` field already carries the fictional outcome; ensure it is always populated from the climax beat, not the last beat.

OPTIONAL / NICE-TO-HAVE:
- The plan mentions "make conflict objects premise-specific instead of the generic domain pool" in KILL 2. This is a nice-to-have that can be deferred; the style grammar injection is the critical path. Premise-specific conflicts require a separate design pass and risk scope creep.
- The plan proposes adding `meta.story_contract` without overwriting `resolved["style"]` / `meta.style` / `visual_plan.style`. This is correct discipline but verify at build that no downstream consumer reads `meta.style` expecting it to match the StoryContract; if any do, add a migration note.

CUT THESE (scope / over-engineering):
- The "re-soak" step between KILL 2+announcer+KILL 4 and KILL 3 in the build order (section 7) is unnecessary ceremony. KILL 3 is a small validator relaxation + coupling fix; it can ship in the same chunk as the announcer redesign since both touch the outro. The build order should be: KILL 2 (StoryContract) → announcer redesign + KILL 4 + KILL 3 (together, since all three touch the body/outro pipeline) → one soak. This saves a full cycle.
- The plan's suggestion to "delete the late `select_style(outline.premise, ...)`" in KILL 2 is correct but the plan should explicitly state: verify `select_style` has no other callers before deletion (grounding: `_otr_style_catalog.py:718-733`). If it is called elsewhere, keep it but stop calling it from the outline path.

ASSUMPTIONS:
- [ASSUMPTION] The outline produces structured fields (time, place, cast, opening_situation) that can be fed to the open. Verify: `OutlineRequest` response schema has these fields or can be extended to include them.
- [ASSUMPTION] `news_close_brief` is always populated for every episode. The grounding shows it is authored in `news_interpreter.py` and threaded to the outro, but verify it is never empty/null for any code path.
- [ASSUMPTION] The style grammar markers (sound_world, story_engine) are concrete enough to pattern-match in generated text. Verify: each style's `sound_world` and `story_engine` fields contain distinctive, matchable vocabulary (not abstract descriptions). If they are abstract, the deterministic gate for KILL 2 will need a different check (e.g., embedding similarity, which is expensive) or the style definitions must be hardened first.