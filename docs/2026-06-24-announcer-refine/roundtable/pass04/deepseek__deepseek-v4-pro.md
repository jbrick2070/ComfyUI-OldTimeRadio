<!-- requested_model: deepseek/deepseek-v4-pro | resolved_model: deepseek/deepseek-v4-pro-20260423 -->

VERDICT: yes-with-fixes — one build-blocking sequence bug (STEP D capture occurs before `outline.beats` is populated) plus two must-fix regressions in the coda validator contract and the climax-line lookup that will produce silent wrong output.

MUST-FIX BEFORE BUILD:
1. [STEP D] `_open_status_quo` capture reads `outline.beats` BEFORE the outline is generated. The plan places the capture "immediately after `generate_outline` (:3158) and BEFORE build_sq_data (:3245)", but the code block shown does not actually call `generate_outline` — it iterates `outline.beats` at a point where `outline` is still the request object or None. At :3158 the real code does `outline = generate_outline(outline_req, ...)` — the capture must follow that assignment, not precede it. Fix: move the capture block to AFTER `outline = generate_outline(...)` and BEFORE `build_sq_data(...)`. Cite the exact line numbers from the real run() method (the grounding says generate_outline is at :3101/:3158; the capture must sit between :3158 and :3245, with `outline` already bound to the result).

2. [STEP F] `validate_news_coda_line` signature requires `key_terms` but the plan says "Derive key terms from `news_close_brief` inside the validator if `key_terms` not threaded." This is a contradiction: the validator either takes `key_terms` as a parameter or it derives them internally — it cannot do both without a clear default. If the call site never threads `key_terms`, the parameter is dead weight and the validator must derive them. Fix: pick one. Recommend: remove `key_terms` from the signature entirely; derive inside the validator from `news_close_brief` using a simple tokenizer (split on whitespace, drop stopwords, take nouns/proper-nouns). This eliminates the threading ambiguity.

3. [STEP F] Climax-line lookup iterates `reversed(led.data.get("lines") or [])` and matches `beat_id == _climax_beat_id` — but `_climax_beat_id` is computed at :3266-3272 as the FIRST beat whose role is in `CLIMAX_CLASS_ROLES`, and the comment says "exactly one, the last voiced character beat." If the outline has multiple climax-class beats (the grounding says CLIMAX_CLASS_ROLES is a set, not a singleton), `_climax_beat_id` will be the FIRST, but the lookup takes the LAST line matching that beat_id. This is correct only if there is exactly one climax beat. If there are two (e.g., a setup-climax and a pressure-climax), the lookup will find only lines from the first climax beat, missing the actual dramatic climax. Fix: document the invariant explicitly: "CLIMAX_CLASS_ROLES produces exactly one beat per outline; if this changes, the lookup must take the LAST beat_id from CLIMAX_CLASS_ROLES, not the first." Add an assertion at :3266 that `_climax_beat_id` is set exactly once, or change the computation to take the LAST matching beat.

SHOULD-FIX:
1. [STEP C] `contract.story_engine` is passed to `_build_phase_user_prompt` and `_build_beat_user_prompt`, but `story_engine` is a field on `render_style_grammar`'s output string, not a direct attribute of `StoryContract`. The plan says `contract.grammar` is the rendered string (from `render_style_grammar(slug)`), and `contract.story_engine` is a separate field. Confirm `StoryContract` has a `story_engine: str` attribute distinct from `grammar: str`. If not, extract it from the rendered grammar or add the field. [ASSUMPTION: StoryContract dataclass includes `story_engine` as a separate field; verify against the actual dataclass definition in pass02 §0.]

2. [STEP F] The coda lead-in is described as `<fixed lead-in>` but never specified. The plan says "coda_lead_in=<fixed lead-in> if _style_grammar_on else ''" — what is the fixed string? If it's something like "And now, the real story:" or "In reality:", it must be defined as a constant in the plan. Without it, two implementors will pick different lead-ins, producing incompatible outputs. Fix: specify the exact string.

3. [STEP G] The plan says "Under flag, set `_style_slug = contract.slug` (+ `_ending_tag = contract.ending_tag`, `_ending_template = contract.ending_template`)" — but `contract.ending_template` is not mentioned anywhere else in the plan or grounding. `StoryContract` has `ending_tag` (from pass02 §0), but `ending_template` is new. If it's a real field, add it to the StoryContract definition; if it's a mistake, remove it. [ASSUMPTION: `ending_template` is a field on StoryContract; verify.]

4. [STEP E] `compose_announcer_intro` under flag passes `safe_open_brief` but the plan doesn't specify how the new `_ANNOUNCER_INTRO_SYSTEM` prompt uses it. The prompt rewrite must be specified: it reads `safe_open_brief.setting`, `safe_open_brief.time_of_day`, `safe_open_brief.opening_status_quo`, and `safe_open_brief.cast` (rendered as a list of allowed proper names), and explicitly forbids inventing any name not in that list. The current prompt already says "Use only proper names that appear in the brief" — the new prompt must say "Use only proper names that appear in the cast list" because the brief is no longer passed. Specify the exact system prompt text or the exact rules it must follow.

5. [STEP F] The early-out fallback for `story_scaffold` when `not brief and not close` routes to `fallback_news_coda_outro` — but this function is not defined in the grounding. The plan must specify its signature and behavior: it takes `coda_lead_in` and an empty or fallback body, and returns `f"{coda_lead_in} {fallback_body}"`. Without this, the implementor will invent a fallback that may not match the coda voice. [ASSUMPTION: `fallback_news_coda_outro` exists or will be created; verify signature.]

OPTIONAL / NICE-TO-HAVE:
- The `era` field for SafeOpenBrief is marked "verify-at-build" with source `meta/period or ""`. If `meta/period` is always empty in the current code, consider cutting `era` from the first build to reduce scope — it's not used in the prompt unless the prompt explicitly references it.
- The `tone` field in SafeOpenBrief is set to `contract.label` — but `label` is a human-readable style name (e.g., "Noir Detective"). This may leak style vocabulary into the announcer prompt. Consider omitting `tone` or using a simpler value.

CUT THESE:
1. [STEP H] The "DEFER consequence" note in KILL 4 — if consequence enrichment is deferred, remove it from the role-keyed map now. Half-implementing KILL 4 (all roles except consequence) adds wiring complexity for a feature that isn't active. Either include consequence or cut it cleanly; don't leave a stub.
2. [STEP C] The `sound_world` omission from phase/beat prompts is correct (audio vocabulary stays out of dialogue intent), but the plan still includes `sound_world` in the macro prompt via `contract.grammar`. If the macro prompt is the ONLY place `sound_world` appears, document that explicitly and verify it doesn't leak into line-level prompts through any other path.

VERIFY-AT-BUILD checklist (from earlier UNVERIFIABLE flags, now with concrete steps):
- `era` source: check `meta.get("period", "")` at run() top; if empty, confirm SafeOpenBrief works with `era=""`. Test: run with a period-aware seed, verify `era` is non-empty in the safe_open_brief.
- `beat_id` on climax row: add an assertion in the outro block that `_climax_beat_id` is non-empty and that at least one ledger line matches it. Test: run with `_style_grammar_on=True`, verify `_climax_line` is non-empty.
- `news_close_brief` distinct from `ending_change`: add an assertion in the coda path that `news_close_brief != ending_change` (or that they share no more than 50% word overlap). Test: run with a seed that produces a resolved ending, verify the assertion passes.
- OutlineRequest asdict/repr fixtures: run the full test suite with `_style_grammar_on=True`, capture any test that compares OutlineRequest serialization, update the fixture to include the new `style_grammar=""` key. If no such test exists, add one.
- `build_reroll_line_request` is the only LineRequest rebuild site: grep the codebase for `LineRequest(` constructor calls; confirm only `build_reroll_line_request` builds one. If any other site exists, document it. (Moot for first build since per-line register is cut, but verify for future.)

Marked [ASSUMPTION] where noted above — all three must be verified against the real StoryContract dataclass and the real `fallback_news_coda_outro` function before build.