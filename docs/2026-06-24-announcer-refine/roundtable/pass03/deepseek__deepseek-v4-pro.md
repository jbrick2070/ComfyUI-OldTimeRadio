<!-- requested_model: deepseek/deepseek-v4-pro | resolved_model: deepseek/deepseek-v4-pro-20260423 -->

VERDICT: no. The build-spec describes a coherent design but the wiring and sequencing are broken in several places that will cause the system to fail to compile, produce incorrect output, or violate the byte-identity invariant.

MUST-FIX BEFORE BUILD:
1. [§0 / §1 / §9] `build_story_contract` is called BEFORE `OutlineRequest` but its output (`StoryContract`) is NOT passed into `OutlineRequest`. The spec says `OutlineRequest` gets `story_engine` and `ending_mode` fields, but the call site at :3032 only constructs `OutlineRequest` from `news_seed`, `style`, `target_words`, `character_cast`, `script_brief`, `key_terms`. There is no wiring to inject `c.story_engine` or `c.ending_mode` into the request. Fix: add `story_engine=c.story_engine, ending_mode=c.ending_mode` to the `OutlineRequest(...)` call at :3032.
2. [§0 / §1] `build_story_contract` returns a `StoryContract` with fields `story_engine` and `ending_mode`, but the spec says `OutlineRequest` fields are `story_engine: str = ""` and `ending_mode: str = ""`. The `build_story_contract` function constructs these from `s.get("story_engine","")` and `s.get("ending_mode","")` — the keys in the style catalog dict. The `OutlineRequest` dataclass at :283 does NOT have these fields. It has `style` (user-selected). Adding them requires modifying the dataclass definition. Fix: add `story_engine: str = ""` and `ending_mode: str = ""` to `OutlineRequest` at :283.
3. [§0 VERIFY-AT-BUILD] The spec says `_build_phase_user_prompt` and `_build_beat_user_prompt` take `macro`, not `OutlineRequest`, and to "thread the contract fields through the macro/combiner or add a param." The macro is a dict produced by `_build_macro_user_prompt` (which DOES take `OutlineRequest`). The phase/beat prompts are built from this macro dict. If `story_engine` and `ending_mode` are not in the macro dict, they cannot be rendered in phase/beat prompts. Fix: in `_build_macro_user_prompt` (:1133), when `story_scaffold` is true, add `story_engine` and `ending_mode` to the returned macro dict. Then in `_build_phase_user_prompt` (:1187) and `_build_beat_user_prompt` (:1236), read them from the macro dict and render them into the prompt.
4. [§1 / §9] The spec says "LINE level: pass ONLY a compact register tag + the existing `conflict_object`". The `conflict_object` is already threaded via KILL 1. The "compact register tag" is not defined. It must be the `ending_tag` from `StoryContract`. But the spec does NOT wire `ending_tag` into `LineRequest` or the line-level prompt. Without this, the body lines cannot receive the structural steer. Fix: add `ending_tag` to the line-level payload (either via `LineRequest` or the meta dict passed to line composition) and render it in the line prompt.
5. [§2 / §9] `opening_status_quo` is derived from "the FIRST character beat's `intent` (the setup beat)". But the OPEN is composed BEFORE the body lines are generated. The setup beat's intent does not exist yet at OPEN composition time. The pipeline order is: build_story_contract -> OutlineRequest -> generate_outline -> [line loop]. The OPEN is composed during the line loop, but the spec says it uses the setup beat's intent. If the setup beat is the first beat generated, its intent exists only AFTER the first line is generated. The OPEN must be composed AFTER the first character line is generated, or the `opening_status_quo` must be derived from the outline's premise/setup beat description, not the generated line. Fix: either move OPEN composition to after the first character line is generated, or derive `opening_status_quo` from the outline's `premise`/`setting`/`opening_status_quo` field (which requires adding a field to the outline output).
6. [§2] `safe_open_brief` is a `SafeOpenBrief` dataclass. The spec says to pass it to `compose_announcer_intro`. But the function signature at :2709 only takes `script_brief`. The new signature adds `safe_open_brief` parameter, but the call site at :4465 must be updated to pass it. The spec says "under flag pass `safe_open_brief` + `forbidden_tokens` + `story_scaffold=True`". The call site at :4465 currently passes only `script_brief` and `creative_repo_id`. Fix: update the call site at :4465 to pass `safe_open_brief=safe_open_brief, forbidden_tokens=forbidden_tokens, story_scaffold=True` when the flag is on.
7. [§3] `compose_announcer_outro` signature adds `climax_character_line` and `coda_lead_in`. The call site at :4615-4634 must be updated to pass these. The spec says to find the climax beat's line and pass it as `climax_character_line`. The call site currently passes `final_character_line` (the last character line). The spec says "find the ledger line where `_ln.get("beat_id") == _climax_beat_id`". This requires iterating the ledger lines, not just taking the last one. Fix: at the call site, iterate `led.data["lines"]` to find the line with `beat_id == _climax_beat_id`, and pass its text as `climax_character_line`.
8. [§3] The NEWS CODA path says "SUPPRESS the resolved-fiction branch". The current code at :2854 has a branch that adds "State this outcome plainly" when `resolved` is true and `ending_change` is non-empty. The spec says to suppress this under flag. But the flag-gated path must still handle the case where `resolved` is true — it just must NOT tell the LLM to restate the fictional outcome. The current code does not have a flag gate for this suppression. Fix: add a conditional that skips the "State this outcome plainly" block when `story_scaffold` is true.
9. [§3] The NEWS CODA path says "pass `ending_change` only as forbidden 'do NOT restate this fictional outcome' content". The current code at :2854 uses `ending_change` as the content to state plainly. The new path must instead use `ending_change` as a forbidden token list for the LLM prompt. But the spec also says the lead-in is a post-generation prefix, and the validator checks for no lead-in variant. The forbidden content must be injected into the system prompt or user prompt as a negative constraint. Fix: add a forbidden content clause to the system prompt or user prompt when `story_scaffold` is true, using `ending_change` as the thing NOT to say.
10. [§4] The truncation order fix replaces `new_intent[:_INTENT_MAX]` with a clamped concatenation. But the current code at :794-806 does enrichment THEN truncation. The new code must do enrichment THEN concatenation with clamping. The spec shows the fix but does not wire it into the actual code path. Fix: replace the `new_intent = new_intent.strip()[:_INTENT_MAX].strip()` line with the clamped concatenation logic, ensuring `enrichment` is defined and passed.

SHOULD-FIX:
1. [§2] The `forbidden_tokens` belt uses `_TOKEN_RE` / `_content_tokens` / `_strip_possessive` from `_otr_story_quality_l12`. These are not imported or available in `_otr_line_composer.py` where `compose_announcer_intro` lives. They must be imported or the token extraction must be duplicated. Verify: the token helpers are accessible from the line composer module.
2. [§3] The `coda_lead_in` is a fixed string "The real story:" but the spec recommends a small seed-keyed set. The current implementation uses one fixed string. This is acceptable for now but should be configurable or expanded later.
3. [§3] The `validate_news_coda_line` function checks for ">=1 key term from `news_close_brief` present (reuse the news key_terms if available)". The `key_terms` from the news are not defined or threaded here. Verify: `news_close_brief` key terms extraction exists and is available.
4. [§8] The telemetry fields `meta.story_quality` are not defined in the current `meta` structure. They must be added to the meta dict initialization and the telemetry pipeline.

OPTIONAL / NICE-TO-HAVE:
1. [§7] The byte-identity boundary for `OutlineRequest` asdict/repr is noted but no specific test fixtures are identified. A grep for `OutlineRequest` in tests would identify which tests need updating.
2. [§2] The `fallback_safe_open` function is specified but not implemented. It should be a simple deterministic template using `safe_open_brief` fields.

CUT THESE (over-engineering):
1. [§1] "sound_world -> render/mood routing is DEFERRED" — this is correctly deferred. The `sound_world` field is in `StoryContract` but has no call site. It is safe to leave it unused until the render/mood routing is designed.
2. [§5] "KILL 3 -- DEFERRED" — this is correctly deferred. The outro climax-decoupling is pulled forward in §3 and is byte-identical today.

ASSUMPTION:
- The `_climax_beat_id` is computed at :3266-3272 and is in scope for the entire `run()` function. The outro call site at :4615 can access it. If it is a local variable, it must be passed or stored in a scope accessible to the outro composition.
- The `ledger` object at :4615 has `data["lines"]` where each line has a `beat_id` field. This is documented at `_otr_ledger.py:96` but the exact structure of the line dict is not shown in the grounding. Verify: each line dict has `beat_id` key.