<!-- requested_model: deepseek/deepseek-v4-pro | resolved_model: deepseek/deepseek-v4-pro-20260423 -->

VERDICT: no. The plan has a critical gap in the Dia worker protocol that breaks the single-line contract, and the refactor in Part A is incomplete against the real source code.

MUST-FIX BEFORE BUILD:
1. [Part C – Dia worker protocol] The plan says Dia's per-line render is a single `[S1]` turn, but the Dia model API (`m.generate(text, audio_prompt=ref_clip, ...)`) is NOT shown in the grounding excerpts. The plan assumes `m.generate` takes `text` and `audio_prompt` as keyword args and returns an object with `m.save_audio` – verify: Dia API signature. If `m.generate` returns raw audio tensor (not an object with `.save_audio`), the worker's `m.save_audio(out_path, out)` line will crash. Fix: ground the worker code against the actual Dia API before writing it; if `generate` returns a tensor, use `soundfile.write(out_path, out, 44100)` (mirroring the indextts2 worker's pattern of writing WAV to disk and replying with the path).

2. [Part A – Refactor is incomplete] The plan says to replace `engine in _OTR_CLONE_ENGINES` with `getattr(adapter, "requires_voice_ref", False)`, but the grounding excerpt shows TWO uses of `_OTR_CLONE_ENGINES` in `_render_per_line`:
   - Line: `if engine in _OTR_CLONE_ENGINES and not voice_ref:` (the resolve-ref block)
   - Line: `if self.ROLE == "char_voice" and engine in _OTR_CLONE_ENGINES and not voice_ref:` (the bark-fallback block)
   The plan only mentions replacing the first pattern. The second block also gates on `self.ROLE == "char_voice"` – after the refactor, the adapter metadata must carry the role-scoping intent (clone fallback should only apply to `char_voice`, not `announcer_voice`). Fix: either add `clone_fallback_role = "char_voice"` to the adapter metadata and gate the fallback on `self.ROLE == getattr(adapter, "clone_fallback_role", None)`, or explicitly state that the `self.ROLE` guard stays and only the engine-name check changes.

3. [Part A – Adapter metadata defaults] The plan says defaults on `AudioEngineAdapter` are `requires_voice_ref = False`, `voice_ref_kind = None`, `missing_ref_fallback = None`. The grounding excerpt for `base.py` shows `AudioEngineAdapter` has NO such attributes. The plan must add them to `AudioEngineAdapter` in `base.py` as part of the refactor, or the `getattr` calls will always hit the default and new adapters that don't inherit from `AudioEngineAdapter` (duck-typed) will silently get `False`/`None`. Fix: add the three attributes to `AudioEngineAdapter` in `base.py` with the stated defaults, and document that duck-typed adapters must set them explicitly.

4. [Part B – chatterbox worker `generate` kwargs] The plan says the worker calls `m.generate(text, audio_prompt_path=ref, exaggeration=ex, cfg_weight=cw)`. The grounding excerpt for `eng_chatterbox.py` shows the in-process adapter passes `audio_prompt_path`, `exaggeration`, `cfg`, `cfg_weight`, `temperature`, `generator`. The worker's `generate` call drops `cfg`, `temperature`, and `generator`. If the real `ChatterboxTTS.generate` signature requires `temperature` or `cfg`, the worker will crash. Fix: mirror the adapter's full kwarg set in the worker, or use `supported_kwargs` in the worker too.

5. [Part D – Bank mirroring] The plan says "Mirror the 36 indextts2 `char_voice` rows to `engine="chatterbox"` and `engine="dia"`". The grounding excerpt for `voice_reference_bank.json` is NOT provided – verify: the bank schema. If the bank has engine-specific fields (e.g., indextts2 rows might have an `emo_vector` field that chatterbox/dia don't use), the mirror script must drop or ignore engine-specific fields. Fix: the mirror script should copy only the common fields (`ref_path`, `ref_sha256`, `gender`, `timbre`, `roles`, `age_band`, `commercial_clean`) and assign new `voice_ref_id` prefixes.

6. [Part C – Dia worker output sample rate] The plan says Dia output sample rate is 44100 (Descript Audio Codec). The `_render_per_line` code in `_otr_voice_node_common.py` sets `sr = int(getattr(adapter, "sample_rate", 24000) or 24000)` and uses that for `pack_audio_batch`. If Dia's adapter declares `sample_rate = 44100`, the batch will pack at 44100. But the plan also says Dia reuses the existing CC0 reference bank – those refs are 22050 Hz (indextts2). The Dia worker will resample the ref clip internally (or not). If Dia expects 44100 Hz refs and gets 22050 Hz, quality may degrade. Fix: document whether the Dia worker must resample refs, or state that Dia handles any sample rate natively.

SHOULD-FIX:
1. [Part B – chatterbox worker readiness line] The plan says the worker mirrors the indextts2 worker fd dance. The indextts2 worker emits `{"ready": True}` after model load. The chatterbox worker must do the same, but the plan does not explicitly state it. Without the readiness line, the adapter's `load()` will hang on `proc.stdout.readline()`. Fix: add explicit readiness-line requirement to the chatterbox worker spec.

2. [Part C – Dia worker readiness line] Same gap as chatterbox – not explicitly stated.

3. [Part A – `get_engine` fallback call] The plan says `get_engine("bark")` -> `get_engine(getattr(adapter, "missing_ref_fallback", None))` and "skip fallback if None". The code currently calls `_get_engine("bark")` inside the fallback block. If `missing_ref_fallback` is `None`, the code must not call `get_engine(None)` (which would raise `KeyError`). Fix: add an explicit `if fallback_engine is None: continue` guard before the `_get_engine` call.

4. [Part B – chatterbox `_project` delivery vector] The adapter's `_project` method uses `delivery_vector.get("calm", 0.5)`. The worker receives `exaggeration` as a float from the adapter. The plan says the adapter sends `exaggeration` to the worker – but the current in-process adapter computes `exaggeration` inside `generate_voice`. In the Path-B refactor, the adapter must compute `exaggeration` from the delivery vector BEFORE sending the request to the worker (same pattern as indextts2's `emo_list`). The plan's worker request schema includes `exaggeration` – verify: the adapter's `generate_voice` in the new `eng_chatterbox.py` computes `exaggeration` client-side and passes it in the JSON request.

5. [Part E – Tests] The plan says "worker protocol: unit-test the JSON request/response contract with a stub model (no GPU)". The indextts2 worker has no such tests in the grounding excerpts. If the new workers are to be tested, the test infrastructure must spawn a subprocess with a fake model. This is non-trivial. Fix: either commit to building the stub-model test harness, or defer worker protocol tests to the GPU pilot (Part F) and note the risk.

OPTIONAL / NICE-TO-HAVE:
- The plan mentions `scripts/_otr_mirror_refs.py` as idempotent. Consider making it a dry-run mode that prints what it would change, so the operator can review before mutating the bank.
- Dia2 (2025-11-19) vs Dia 1.6B-0626: the plan defers this to an open question. If Dia2 is a drop-in API replacement, targeting it now avoids a second round of worker/adapter changes. If not, the plan's caution is correct.

CUT THESE (over-engineering):
1. [Part C – Dia transcript wrinkle] The `scripts/_otr_dia_transcribe_refs.py` (faster-whisper, one-time) is over-engineering if the empirical quality of audio_prompt-only is acceptable. The plan already allows audio_prompt-only as a fallback. Unless the quality difference is proven to be large, skip the transcription script and ship audio_prompt-only. If transcripts are later needed, add them then.

2. [Part D – `scripts/_otr_mirror_refs.py`] A separate script to mirror bank rows is over-engineering for 36 rows. A one-time manual edit of the bank JSON (or extending `otr_dl_indextts2_refs.py`) is simpler and less code to maintain. The plan says "no new files" but then proposes a new script. Cut the script; hand-edit the bank or add a `--engine` flag to the existing download script.

[ASSUMPTION] The Dia API `m.generate(text, audio_prompt=ref_clip, ...)` returns an object with a `.save_audio` method. If it returns a raw tensor, the worker code must change.
[ASSUMPTION] The chatterbox `generate()` signature matches the kwargs in the in-process adapter (`audio_prompt_path`, `exaggeration`, `cfg_weight`, `temperature`, `generator`). The worker's reduced kwarg set is intentional and safe.
[ASSUMPTION] The voice_reference_bank.json schema has no engine-specific fields that would break when mirrored to a different engine.