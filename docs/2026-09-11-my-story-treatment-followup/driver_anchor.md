# Treatment failure after qualified repairs: choose the next discriminator

Root is code-aware panelist and sole judge. This is a narrow continuation of
the My Story repair campaign, prompted by a new full canonical failure. No
production changes or next hardware run have begun. Please return <=750 words:
your grounded diagnosis, any demonstrated code defect, and ONE next diagnostic
that best distinguishes prompt contract, native model/sampling and transport.
Do not propose an architecture campaign or extra verifier call.

## Binding operator contract

- Full raw user story only, no RSS, chunker, truncation or substituted source.
- Existing calls check AND return actual corrections, with fixed total retries;
  no fourth/fifth rewrite, recursive review, report-only checker or budget reset.
- Selected acts binding; cast count flexible. No story length/duration rejection.
- Real provider/OOM/cancellation outcomes stay truthful; real OOM is accepted.
- Every qualifying hardware attempt loads actual workflows/otr_canonical.json
  through shipped full API runner to obs publication, never partial/harness graph.
- Same Qwen pair/source/profile/sampling was required for the measured retry.
  That measurement has now completed. Future controls may change only explicitly
  and record the changed variable. Mac/4060 remain held; RunPod lacks auth.
- No writing-quality chase. Failure to produce a parseable ledger plan is a
  structural failure. Optional title/metadata cannot become a new content gate.

## Grounded live history

All three attempts used identical full API graph input (23 nodes,63links),
one-act Jeffrey + living mother dinner, requested2 characters, actual rawsource
~1800characters: shared present-day table/meal in LA, childhood spoken memory,
girlfriend distinct and absent. Qwen/Qwen3.5-4B creative and technical, NF4/SDPA,
balanced0.85/top_p0.95/min_p0.05/repetition1.03. P1 base0.85, retry0.5,
typed repair0.1. P0 technical0.3. All fresh server/reset, native decoder262144.

1. 2c9d47f2, attempt01: P0 source correction complete JSON plus newlines; known
   model/chat EOS mismatch. P1 repeated in unfinished strings across3attempts;
   no publication. 400.67s. Hidden sampled EOS not saved in that early attempt.
2. bd814148, attempt02 after native/chat/grammar EOS alignment: P0 source
   correction and P1 succeed on first attempts; complete fresh canonical
   publication in834s. Source metadata/voicecredits/scenepixels defects found.
3. 97f2d40f, attempt03 after those reviewed fixes: P0 succeeds802tokens EOS248046.
   Its sparse correction497tokens EOS248046 now preserves omitted accepted
   metadata, male Jeffrey/female Mother; girlfriend remains nonspeaking/absent.
   P1 attempt1 repeats source instructions INSIDE logline; guard512tokens,
   exact147tokencycle. Attempt2 uses original prompt at0.5, not prior failed
   text; inside logline it writes schema discussion, then 'The task is complete',
   then 'The [varying noun] is sent' for ~2700tokens, eventually exactcycle50.
   Attempt3 typed full repair repeats '}' INSIDE still-open logline, guard448,
   exactcycle48. All3 terminate via verbatim_cycle, never EOS or length cap.
   Full terminal error408.62s, no cancellation, no lines/media/publication.
   Server selectively stopped, port8000empty, desktopVRAM2083MiB.

Actual EOS union[248044,248046] supplied and logged allcalls; P1 last tokens
5133/68770/333, ended_with_eos=False. Therefore missing EOS is NOT this failure.
The run reached neither cleanup nor cast nor visuals; those repairs remain
offline qualified, not newly live source-qualified. Keep all3 in denominator.

## Code grounding and independent root audit

P1 already binds StoryTreatment once (_otr_my_story.py499-500). Each invocation
of scheduler's bound closure builds a fresh native base (OTR_LedgerScriptWriter
744-764). Native constructs callback immediately before EACH model.generate
(1067-1098), including min_p fallback. Cached constrained helper130-159 retains
tokenizer preprocessing/EOS only, creates new JsonSchemaParser + TokenEnforcer.
Real-LMFE test_each_actual_generation_has_fresh_collectible_history asserts
empty prefix_states percall, populates them, and checks collectibility through
writer success/error/min_p and standalone success/error. No stale cursor found.

ProviderCapacityMessages intentionally disables generic single-string length
bounds for full-source My Story; exact-cycle guard remains. This makes changing
nouns inside a single string invisible until an exact cycle develops. It is a
known signal limitation, not a disconnected guard. Do NOT reintroduce a fixed
string/word/time ceiling as if it proved semantic nontermination.

P1 validator only nonemptyunique names/no reserved ANNOUNCER and exactactcount.
No invalid-cast/short-story gate caused this failure. Schema annotation says
logline one sentence, treatment system explicitly says same. Root does not
claim adding 'be concise' guarantees model termination; success02 vs failure03
could be stochastic/modelconditioning sensitivity. No demonstrated new routing
bug is established. Candidate discriminator: a narrowly specified prompt-only
contract change vs unchanged-code different installed model family; another
identical luck-seeking rerun is low information. Judge from supplied files.

Production snapshot was independently reviewed Opus5/Sonnet5; Cursor timed out
twice without a review. Final466focused;14404pass/51sameinheritedfailures;
Bible36/10same;canonicalvalidated. Do not spend this pass repeating that QA.
Full receipts and failure rawtext accompany this anchor as grounding.
