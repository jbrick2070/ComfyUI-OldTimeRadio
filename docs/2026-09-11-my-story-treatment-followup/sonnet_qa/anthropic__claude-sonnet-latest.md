<!-- requested_model: ~anthropic/claude-sonnet-latest | resolved_model: anthropic/claude-sonnet-5 -->

Must-fix (reachable, in the shown diff):

1. **Fabricated “interrupted” framing when `error.raw_completion` is merely stale, not evidence of truncation.** In `_full_artifact_repair`, `interrupted = bool(completion)` is set purely from `failed_output` being falsy plus a non-empty string on `error`. But `error` here is whatever the shared ladder's `last_error` is — nothing in the shown ladder code (lines 1057–1128) guarantees `raw_completion` on that exception corresponds to *this* attempt's halted generation rather than some earlier, unrelated raise carried forward as `last_error`. The docstring/comment in the ladder (line 1013) explicitly says raw_completion is "deliberately not fed into a repair prompt" for capacity raises, and calls bounding this "a separate, unratified change" — yet that is exactly what this diff does, from the local caller side. The PR claims this is scoped to "this local owner" only, but it is reaching into a documented, deliberate exclusion in the shared code without correcting or even referencing that comment. That comment is now stale/misleading and should be flagged, at minimum.

2. **Silent behavior for non-string, falsy-but-not-None `raw_completion`.** The test parametrization checks `None` and `{"not": "text"}` map to `""`, correctly guarded by `isinstance(completion, str)`. Good — no bug found here, but worth noting the test coverage is doing real work, not padding.

3. **`interrupted` flag conflates "no failed_output" with "was interrupted."** A non-generation error (e.g., a validation error on a real empty string) could also have `failed_output == ""` with a coincidentally nonempty `error.raw_completion` from a stale attribute set by unrelated code upstream (e.g. requests library reuse, or an error object reused across attempts). Nothing shown guarantees `error` is always freshly constructed per attempt. Given `repair_error` in the ladder can be `last_error` from a prior rung (JSONDecodeError rung, line 1046: `last_error = exc`), and this local error may itself carry a `raw_completion` attribute from an earlier partial state, the warning text asserting "The draft was interrupted during generation" may be asserted about a draft that is actually a syntax-rung failure output, not a halt. This is a plausible but unconfirmed risk — I can't verify from the diff alone whether `JSONDecodeError`-rung failures ever populate `raw_completion`.

4. **Test `test_halted_treatment_repair_...` hardcodes `treatment_calls == 3` assertion inside the mock but the outer test also separately checks `slots.treatment_calls == 3`** — redundant, not a defect, just noise.

No other reachable must-fix found in the diff. The `.parametrize` test for `_full_artifact_repair` directly exercises the precedence/priority logic (returned-wins, else raw_completion, else empty) and it looks correct and matches the stated fix contract.

Qualification limits: I cannot see `GenerationDegeneracyError`'s definition, `is_rerollable_capacity_error`, or how/where `last_error`/`repair_error` objects get constructed and whether `raw_completion` is guaranteed fresh per-attempt vs. potentially stale from an earlier rung — items 1 and 3 depend on code not shown and are flagged as risks, not confirmed defects. I did not run the tests. I make no claim about the unrelated shared-ladder policy beyond noting the local diff's proximity to its documented exclusion.