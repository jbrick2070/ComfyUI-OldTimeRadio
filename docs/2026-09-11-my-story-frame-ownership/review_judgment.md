# Finished Sonnet QA -- root judgment

Actual anthropic/claude-sonnet-5, one API call, USD0.117424. Immutable reviewed
packet: finished_sonnet_qa.md; response and usage: sonnet/. R1-R4 used eight
Opus/Gemini calls for USD1.2143. No Cursor consensus and no unanimous-clean claim.

Sonnet's verdict is "Not clean" because it highlights two explicitly retained
semantic limitations. Root accepts those limitations and records them; neither
is a new software blocker or grounds for disobeying the user's no-new-gates rule.

1. Existing typed rejection applies only when a reserved identity is in cast.
Free-prose frame leakage alone can pass structural validation. Its existing
source correction is the authorized model owner, and actual09 must inspect all
three fields. No claim that the deterministic validator proves frame semantics.
Both actual08 drafts had ANNOUNCER in cast; they do not isolate a cast-free
prose-only production failure. Keep the wider observation as a residual risk.
2. P0 is non-blocking planning with an existing optional correction, not a
structural source-fidelity guarantee. Its returned cast/count can remain wrong;
inspect and classify this honestly, without creating another rejection gate.
The Bible explicitly states these two residuals in a documentation-only note.
3. Reject the claimed capacity near-miss: actual fit was2178/131072 tokens,
roughly1.66%, with four natural-EOS calls. No evidence of context exhaustion.
source_rewrite_instruction is consumed by _call before structured_call; it is
not a third instruction sent on every treatment-author repair as alleged. It
does appear in its own existing source-correction system, alongside original
author context. No model/call budget or source truncation was introduced.
4. Local kwargs overwrite is deliberate phase ownership and matches P2's
existing pattern. No current caller supplies a composable orthogonal instruction.
Do not invent a future extension API to address a hypothetical caller.
5. Count-only regression coverage already exists and ran in the181 focused
passes: test_full_treatment_repair_preserves_material_and_matches_variable_controls
(1/3/6 acts), plus test_count_repair_exhaustion_remains_an_honest_failed_ledger.
There is no missing count-only check or need for another schema/message type.
6. Agree that canned results prove prompt transport, complete replacement and
ledger receipt integrity, not model fidelity. Fresh full canonical qualification
remains required and zero of the prior eight attempts is source-qualified.

No production or test changes after Sonnet QA. Documentation clarifies residual
limits only. Root judges code ready for existing full/Bible checks and push, then
one fresh controlled canonical09. Re-review only if an actual new code defect
requires revision; no automatic review loop or re-polling for assent.
