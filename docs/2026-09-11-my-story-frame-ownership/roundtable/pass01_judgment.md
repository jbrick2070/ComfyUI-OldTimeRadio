# R2 grounded judgment

Actual Opus5/Gemini3.1Pro, two calls, USD0.334. Root verified all visible claims.
Accept Gemini's conditional whole-treatment repair: a cast failure proves a
reserved identity, not corrupt prose. Say IF house-frame openings/closings were
included in acts/ending, replace those with source-grounded dramatic events and
conclusion; preserve already-correct fields. Accept moving the validation defect
after the shared preservation direction to make the requested correction clear.

Accept aggregate reserved-frame and act-count diagnostics in the existing
problems list. Same conditions; no new gate. Keep duplicate/empty-name behavior.
Add a combined wrong-count/reserved-frame fixture to prove one remaining typed
repair receives both and fixes both, rather than spending another call.

Use source_kwargs['source_rewrite_instruction']=... inside each phase, like the
existing act owner. **kwargs creates a fresh dict, so this cannot mutate the
caller's mapping or leak to another phase; no duplicate explicit keyword. Actual
runner source_kwargs() creates only receipts/scheduler/model keys per pass.
Opus's claimed current collision/leak is hypothetical, prevented by this exact
implementation. Test caller mapping unchanged and phase isolation, not long
identical literal pins.

Accept explicit complete named_cast and cast_plan in P0 correction instruction;
conservation of omitted fields remains. Require explicit house frame requests to
name frame ownership in requirement text as well as kind frame. Explain house
frame exclusion in reason only when relevant, retaining the dramatic cast-count
reason. P0 remains interpretation, not another admission gate or proven semantic
count check. Existing schema does not equate planned to speaking; no new check.

Keep the named dramatic announcer profession; do not introduce a generated alias
for a person literally named ANNOUNCER. The latter is an existing reserved-name
limitation, not evidenced by08 and not a newly introduced regression. Gemini's
suggested name invention violates source identity policy. Full raw source remains.
No hardcoded Jeffrey/Mother example needed; general house-versus-dramatic scope
handles 'apart from the announcer'. Keep instructions concise, no architecture
lecture; author pack defines output fields, correction instruction restates only
owner scope/explicit returned fields as with existing P2 correction.

No source/prose keyword gate; both reviewers now agree. Partial compliance is
observable by reading raw actual treatment/acts/final rows, not parser inference.
09 qualification requires house-role exclusion across cast, act beats and global
ending, retained two dramatic speakers/present shared dinner, spoken childhood
memory and mother's own response, girlfriend mention, present-day appreciation
ending, correct gender/actual voice/credits, published canonical assets and honest
images/audio limits. Schema-valid frame leakage is a failure, not a silent PASS.

Budget grounded: _otr_structured_call.py:1000-1018 skips structural retry for
PostValidationError (it is only for JSON decode/rerollable capacity errors);
1057 onward is one typed-repair final rung. Two actual calls on this failure,
despite max_attempts3. No semantic reroll/repair after failure. Existing count
repair test has the only matching 'exactly N acts' pin, retained by new wording.
No test pins the old concluding sentence. Full failure comparison is a future
check, not a promise that test outcomes must match.
