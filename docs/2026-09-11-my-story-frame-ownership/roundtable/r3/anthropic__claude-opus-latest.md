<!-- requested_model: ~anthropic/claude-opus-latest | resolved_model: anthropic/claude-opus-5 -->

## Concrete wiring/integration blockers

**1. The aggregate diagnostic cannot be "only wording": `_make_treatment_validator` early-returns.**
At `_otr_my_story.py:494` the reserved-name branch is a bare `return`, and so are the uniqueness/empty checks. `problems` is only reached after those returns. R2's "aggregate reserved-frame and act-count diagnostics in the existing problems list" therefore *is* a control-flow change to that validator, not a string edit. Specify the exact new order (duplicate/empty still early-return per R2 "keep duplicate/empty-name behavior"; reserved must append). Note the side effect: the reserved message stops being the whole error string, so any test or log assertion matching it *exactly* breaks. R1's "the test already asserts any error" is an unverified claim about one test; require a grep of every occurrence of "reserved for the frame" in tests before edit, not after.

**2. Combined repair prompt contains a permanently-present contradiction.**
`_pass_treatment` (510) passes a *fixed* instruction string to `_full_artifact_repair`, so "Return exactly %d acts / preserve grouping" is present even when the only defect is the frame leak, and the shared tail sentence (420) is appended *after* the error text. Order in the emitted user turn is: instruction → error → tail. The reserved whole-treatment directive is thus sandwiched between a grouping-preservation directive and a "preserve unaffected … ending" directive — the exact ambiguity R1 flagged. Either the reserved directive must be last, or the tail must be conditional. Fix the ordering explicitly in R4; "conditional repair wording" in the R3 text does not say which of the two strings is conditional or where it lands.

**3. P1 `source_rewrite_instruction` is dead on the failing path.**
In `_call` (375) `rewrite_story_source` runs only after `structured_call` returns an accepted model. Terminal08 failed author post-validation, so item 4's P1 instruction cannot influence attempt 1 or the typed repair at all. The entire P1 remedy reduces to the pack seam plus the validator error text. Say so in the plan; otherwise the P1 instruction is untestable scope creep that consumes a fixture slot.

**4. P0 correction can silently re-insert the ANNOUNCER via `_retain_omitted`.**
`preserve_omitted` includes `("named_cast",): "name"`. If the corrected model omits `named_cast` from `model_fields_set`, `values[name] = original[name]` restores the three-row cast verbatim; if it returns `cast_plan` with `planned` unset, `planned=3` survives beside a 2-person cast. `_pass_interpret` passes **no** `post_validator`, so `validate_artifact` accepts either outcome with `status="usable"`. Required: a canned fixture where the correction omits `named_cast`/`cast_plan.planned`, asserting the observed (possibly bad) behaviour, plus explicit qualification inspection of `story["cast_plan"]` vs `named_cast`. R2's "explicit complete named_cast and cast_plan in the instruction" is a prompt hope, not a wiring guarantee.

**5. Pack reload is asserted, not proven.** `resolve_story_pack` (785) goes through `_ensure_loaded()` and `_load_routed_pack`; the registry is cached per process, and `_otr_lane_specs._load` caches the module. Restart covers the live run, but the *test* process may hold a cached registry across cases. Also: since canonical JSON is unchanged, the workflow validator/widget/link audit proves nothing about the edited `my_story.json`. Add a pre-launch smoke that actually calls `resolve_story_pack("…my_story bank id…")` and asserts the new `prompt_stages` text is the one loaded; a malformed pack JSON otherwise surfaces only mid-run after model load. As written, the "full live-class validator/link audit" is ceremony for this change.

**6. Prequeue graph equality with 08 is under-specified and previously failed.** 08's first dry run failed COMBO validation on the bare Gemma ID; the pass used the decorated label `google/gemma-4-12b-it (23.9 GB, nv16 nv24)`. That label embeds VRAM/device text that is environment-derived and may differ after the fresh boot. Define: equality is against the *corrected* 08 wrapper input, and the pre-check is "observed dropdown label re-read at 09 boot", with label drift treated as an expected, non-story-attempt correction, not a graph inequality.

**7. Attempt-count assertion must be pinned, not described.** Per `_otr_structured_call.py:998`, `PostValidationError` skips the structural rung, so the stubborn-model case is exactly two slot invocations despite `max_attempts=3`. The fixtures should assert the invocation count numerically; "current two actual typed calls" in prose is not a test.

**8. `**source_kwargs` mutation claim is correct but narrow.** `_pass_act` (588) mutates its own fresh dict; adding the same key in `_pass_interpret`/`_pass_treatment` cannot collide because `_call` (375) names `source_rewrite_instruction` explicitly and it never reaches `kwargs`. Blocker only if any *other* caller forwards the same mapping to two phases — `source_kwargs()` at 1018 returns a fresh dict per call, so fine. Drop the long R2/R3 argumentation; one assertion test (caller mapping unchanged after two phases) suffices.

**9. Unbounded QA loop / no stop rule.** "If QA causes edits, recheck affected tests/full/Bible and Sonnet" has no iteration cap and no criterion for "affected". State a maximum (e.g. one QA-driven revision, else re-enter R4) or the gate before the live run is open-ended.

**10. Worktree roles ambiguous.** "Fresh detached baseline/candidate worktrees at 1d623f96" names one SHA for both; the candidate must be the edited tree. Name each worktree's content explicitly, since the regression comparison (14459/51/183/1 by IDs *and* normalized payloads) is meaningless if both sides are the baseline.