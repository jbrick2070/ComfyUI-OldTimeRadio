# r4 -- Sonnet 5, convergence read, 2026-09-02

CONVERGED: yes. Every MUST-FIX from r1 (Fable), r2 (Cursor grok-4.6-high) and r3
(Antigravity Gemini 3.7 Flash High) is traceable into driver_anchor.md sections 8-10 and
was re-grounded against the real files (eng_kokoro.py:233-235 one KPipeline call with
split_pattern; cast_lock.py:1598-1614 stamps char_voice_engine "auto" literally while the
announcer engine is resolved; _otr_engine_profiles.py:47-56 pins indextts2 at index 0;
main.py imports folder_paths at module top before apply_custom_paths (:230) and
execute_prestartup_script (:236); test_widget_vectors_exact needs the node-81 pin;
onnxruntime 1.29 has no InferenceSession.close).

Contradictions found and fixed in the anchor: section 10 cited section 9 for the
onnx-community nesting (it was section 8); section 9's per-load selection supersedes
section 8's "once per process" (already marked); section 10's digest-named npz supersedes
section 8's atomic rename (now marked).

Dropped without a reason (now dispositioned): r2's S10 test guidance (implemented,
f2312570); r1's PBUG follow-up line (with the live receipt); r1's phonemizer logger nit
(only if it fires).

Final test list and the two live proofs: as in driver_anchor.md sections 5, 6 and 11.
Could not verify: the 0.6.1 kwarg names (the driver read them from the wheel; r2 asks for
inspect.signature at coding time), models_dir on a Desktop --base-directory install, and
anything requiring execution.
