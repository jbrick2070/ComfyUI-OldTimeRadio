<!-- requested_model: ~google/gemini-pro-latest | resolved_model: google/gemini-3.1-pro-preview-20260219 -->

VERDICT: no. The provided adapter code is written for in-process execution, which the plan's own blocker table proves will brick the main venv, and the Stable Audio adapter imports the wrong library.

MUST-FIX BEFORE BUILD:
1.  [eng_stable_audio.py / The blocker] **Wrong library import for SA3.** The plan states Stable Audio 3 is ComfyUI-native (`Comfy-Org/stable-audio-3`) and has "no pip dep, no conflict". However, `eng_stable_audio.py` imports `stable_audio_tools`, which is the PyPI package for Stable Audio Open that the blocker table explicitly says "won't resolve" in the main venv. **Fix:** Rewrite `eng_stable_audio.py` to interface with the native ComfyUI SA3 nodes/models, or update the plan to reflect that SA3 cannot be used via the registry without the PyPI package.
2.  [eng_indextts2.py / eng_chatterbox.py / The blocker] **In-process imports guarantee venv destruction.** The blocker table proves IndexTTS2 and Chatterbox require downgrading torch/numpy, which breaks ComfyUI. Yet the adapters are written as in-process imports (`from indextts.infer import IndexTTS2`). This will crash at `load()`. **Fix:** You must adopt Option B (Sidecar). Rewrite these adapters as IPC clients (e.g., using `subprocess` and sockets/pipes) that communicate with an isolated venv.
3.  [EXECUTION-PLAN.md: CLEAN-BREAK vs Wave R0a / I-1] **Schizophrenic legacy requirements.** The `CLEAN-BREAK directive` explicitly says to remove legacy in lockstep and supersedes I-1, I-3, and R0a steps (d/e/f). However, the "Build orchestration" section still mandates building `legacy_invocation_manifest.json`, preserving legacy ports, and capturing legacy baselines. **Fix:** Delete I-1, I-3, and all legacy-preservation steps in R0a and Wave 2b. Do not build scaffolding for code you are deleting in the same PR.
4.  [Question 2 / eng_indextts2.py] **Passing torch.Generator across IPC.** If you move to a sidecar (Option B), you cannot pass a bound `torch.Generator` object across process boundaries as currently written in `generate_voice`. **Fix:** The IPC payload must pass the `seed` integer only. The sidecar server must instantiate the `torch.Generator` on its local device and apply it to the inference call.

SHOULD-FIX:
1.  [Question 1 / The blocker] **sm_120 (Blackwell) support in the sidecar.** Option B assumes Torch 2.8-cu128 can drive an RTX 5080. [ASSUMPTION] Running older CUDA on newer architectures often requires PTX JIT compilation (which causes massive startup hangs) or fails entirely if the architecture is unsupported. **Fix:** Verify cu128 runs on sm_120 before building the sidecar. If it fails, ONNX/torch-export (your Question 1 idea) is the *only* viable path for these voice models.
2.  [eng_stable_audio.py / I-7] **Bypassing ComfyUI VRAM management.** Wrapping ComfyUI native nodes inside a Python `generate_clip` function (if you fix Must-Fix 1) bypasses ComfyUI's `comfy.model_management`. **Fix:** Ensure the SA3 adapter uses ComfyUI's internal model management to load/unload weights to respect the 14.5 GB ceiling, rather than raw `torch.cuda.empty_cache()`.

OPTIONAL / NICE-TO-HAVE:
-   [eng_indextts2.py] `unload()` uses a bare `except Exception: pass` around `empty_cache()`. Log the exception; silent failures in memory management lead to impossible-to-debug OOMs on 16GB cards.

CUT THESE (over-engineering):
1.  [EXECUTION-PLAN.md: Wave 2a] **Re-render-twice legacy after the refactor.** If you are following the CLEAN-BREAK directive, legacy is gone. You do not need to re-verify legacy byte-identity after refactoring the writer. Cut it to save sprint time.