<!-- requested_model: ~anthropic/claude-opus-latest | resolved_model: anthropic/claude-4.8-opus-20260528 -->

VERDICT: no. The two documents disagree on the central decision (in-process vs sidecar) and the shipped IndexTTS2/SA3 adapters as written cannot satisfy the blocker's own invariants; the panel question "is the sidecar right?" is unanswered while the code already hard-commits to in-process.

MUST-FIX BEFORE BUILD:

1. [blocker doc "The blocker" vs EXECUTION-PLAN "Lean-alpha scope" + eng_indextts2.py] Direct contradiction on whether IndexTTS2 can be a main-venv in-process default. The blocker proves IndexTTS2 (indextts 2.0.0) pins torch==2.8.*/numpy==1.26.2/transformers==4.52.1, which downgrades the protected cu130/numpy-2.4/transformers-5.5 stack and bricks ComfyUI; it concludes "neither voice engine can be a main-venv default here." But the EXECUTION-PLAN ("Lean-alpha scope", "Engine matrix", C-6, F) and `eng_indextts2.py` (`from indextts.infer import IndexTTS2` inside `load()`, in ComfyUI's process) all assume in-process promotion. Fix: pick ONE. Either adopt sidecar option B for voice and rewrite `eng_indextts2.py`/`eng_chatterbox.py` to dispatch over IPC, or downgrade the voice default to bark/chatterbox-only and explicitly strike "IndexTTS2 = shipped default #1" from the plan. The plan cannot promote IndexTTS2 in-process given its own measured pins.

2. [Engine matrix vs eng_stable_audio.py "load()"] The plan repeatedly states SA3 is ComfyUI-NATIVE with "no pip dep, no conflict" and "wraps ComfyUI's native nodes, not a custom stable-audio-tools path" (blocker §SA3; D5; Verify-at-build SA3). But `eng_stable_audio.py` imports `from stable_audio_tools import get_pretrained_model` and `generate_diffusion_cond`, defaulting `OTR_STABLE_AUDIO_MODEL` to `stabilityai/stable-audio-open-1.0` (the NON-commercial research model, not SA3). This adapter is the conflict-PRONE path the plan says to avoid, and points at the wrong (non-commercial) weights. Fix: rewrite the adapter to drive ComfyUI's native SA3 nodes via the internal node API, or explicitly demote this adapter to `stable_audio_open` (non-default, commercial_clean=False) and write the separate `eng_stable_audio_3.py` the blocker names. verify: that ComfyUI exposes a callable SA3 sampler API for in-adapter use rather than only graph nodes.

3. [eng_indextts2.py "generate_voice" + G1 seeds] Determinism gate violation. G1 mandates the external `torch.Generator` be "passed as `generator=` into every forward" and that an engine whose `supports_external_generator` is false "is disqualified from bit-exact mode." But all three adapters construct `torch.Generator(...).manual_seed(seed)` and hand it to `supported_kwargs`, which silently DROPS the generator kwarg if the real `infer`/`generate` signature doesn't accept it (the docstrings admit the kwarg is "unverified"). Result: an engine can render via global-RNG-only and still be selected, defeating the byte-identical gate without ever tripping `supports_external_generator`. Fix: make `supported_kwargs` (or the dispatch in D) hard-fail in bit_exact mode when `generator` is not accepted, rather than dropping it. verify: `base.py supported_kwargs` actually filters by signature (not shown).

4. [eng_indextts2.py / eng_chatterbox.py "load()"] None of the three adapters bind weights to a local-only / offline path, yet `from_pretrained` (Chatterbox) and `get_pretrained_model` (Stable Audio) are network-fetch entry points by default. C-7 forbids any fetch/network during execute and `load()` is called from inside `generate_voice`/`generate_clip` at execute time. If weights aren't pre-cached, these hit the network during execute. Fix: set the offline env (e.g. `HF_HUB_OFFLINE=1`) and a local model dir in `load()`, and validate presence in `VALIDATE_INPUTS` so the C-7 named error fires at queue time, not a silent download at forward time.

5. [eng_chatterbox.py "load()" device + I-7] `ChatterboxTTS.from_pretrained(device="cuda")` is unconditional, but every other path guards on `torch.cuda.is_available()`, and R0b runs stub engines with no GPU. A box without CUDA (CI, R0b smoke) crashes in `load()`. Also `unload()` for Chatterbox/IndexTTS2 only sets `self._model=None`+`empty_cache()` with no `gc.collect()`, while I-7 explicitly requires `del`+`gc`+`empty_cache`; HF model objects with circular refs won't free before the VRAM ceiling check. Fix: guard device selection; add `gc.collect()` to every `unload()`.

SHOULD-FIX:

1. [registry.py "assert_usable" vs C-6/CLEAN-BREAK] `assert_usable` treats only `default_roles` engines as always-usable and gates everything else behind `requires_flag`. After promotion (I) "retires `OTR_ENABLE_*` flags" and flips defaults to indextts2/sa3, but those adapters ship `default_roles=()` and `requires_flag` set. The registry has no mechanism to flip an engine into `default_roles` at promotion — it's hardcoded in each adapter. Fix: state where promotion edits `default_roles`/clears `requires_flag` (adapter source? profile YAML override?), since `assert_usable` reads the adapter attributes directly.

2. [I-8 / Engine matrix] Self-contradiction on IndexTTS2 commercial state vs default status. I-8 and the matrix classify indextts2 `commercial_clean=false` (known-gated → non-blocking warning) yet also make it the shipped out-of-box default #1. Shipping a non-commercial-default with a "warn not block" posture is a product/licensing decision, not a build detail — confirm the operator accepts a default-