# PROBLEM STATEMENT -- is NO MIRROR / NO PING-PONG actually in place, and the matrix that would have shown it

**Date:** 2026-08-06
**Driver:** Claude (Cowork), CODER window. HEAD `e499b7fc` on `v2.0-alpha`.
**Operator ruling being enforced (2026-08-06):** *"there is no mirror or ping
pong unless for credits -- I thought that was already stated."*
**Status:** STATIC (file read + one execution). Nothing here may enter
`docs/PROD_BUG_LOG.md` without a retained artifact.
**Gate:** full `kibitz-plugin:kibitz` four-round arc before code.

---

## 0. WHAT IS ALREADY SETTLED, AND IS NOT BEING REDONE

The operator asked whether multi-clip was already settled in a matrix. **It is,
and this proposes no change to it.** Recorded up front so no round re-opens it:

* `docs/ENGINE_MATRIX.md` is GENERATED by `tools/engine_matrix.py` from the LIVE
  registry and has a **drift gate** -- `--check`, wired as a suite test
  (`tests/test_engine_matrix_doc.py`). It cannot drift from the adapters.
* `docs/retired/2026-08-02-all-local-engines-multiclip-maths-RETIRED.md` records the coverage
  arithmetic for all ten local engines, and every one totals exactly 17.68 s on
  a 442-frame beat.
* The commit that precedes this statement (`e499b7fc`) changed **zero lines** of
  `coverage_plan.py`, `frame_contract.py`, `beat_session.py` and
  `scripts/bench_graphs/`. It repaired the GRADER that reads the mechanism's
  output, not the mechanism.

## 1. THE ENFORCEMENT AUDIT -- where the rule IS in place

Every frame-manufacturing surface, grounded:

| surface | state | evidence |
|---|---|---|
| `wrapper_bridge.extend_frames_to_target` (the mirror extender) | **DELETED** | tombstone comment, `wrapper_bridge.py:499` |
| `eng_wan_ti2v` ping-pong | **REFUSES**, both single-clip and coverage-planned | `:1094-1100`, `:1105-1113` |
| `eng_ltx_8gb` mirror-extend | **REMOVED**; trims real frames only | `:1407-1443` |
| `eng_ltx_video` boomerang | **RETIRED** | `:559` |
| composite `_should_loop_fill` | **RETIRED 2026-08-02**, a named no-op returning False | `otr_silent_composite.py:273-287` |
| composite clip underrun | **TERMINAL** -- raises `ClipUnderrunsItsBeat`; any shortfall, no fractional threshold | `:290-320`, `:244-250` |
| credits floor-extend | **RIPPED** 2026-07-03 | `otr_silent_composite.py:783` |
| credits music loop | **RIPPED** 2026-07-03 | `video_engine.py:2263` |

**The rule is in place on every drama path, and it fails LOUD rather than
quietly filling.** That is the headline and it is good news.

## 2. THE ONE REMAINING LOOP -- and it is exactly the operator's exception

```python
# nodes/otr_silent_composite.py:452-455
if _last_clip is not None:
    emit("clip", _last_clip.get("path"), tail_n, 0,
         _last_clip.get("shot_id"), _last_clip.get("engine_id"), loop=True,
         bg_still_path=_last_clip.get("bg_still_path"))
```

The only `loop=True` in the assembly path (`-stream_loop -1`, `:585-592`). It
fills the CLOSING-THEME tail to the master length by holding the last drama clip
on screen -- the operator's 2026-06-17 "credits over the scene" look.

**This is the credits exception, and it should stay.** It is also structurally
different from a padded beat: it is a TIMELINE SEGMENT, not a manifest clip row,
so it never touches any beat's `extension_mode` and the acceptance grader cannot
see it. Nothing proposed here disturbs it.

**Open for the panel:** the rule now has exactly one exception and it is
implicit -- a bare `loop=True` with no named constant, no receipt, and nothing
asserting it is the ONLY one. Should it be named (e.g. a
`CREDITS_TAIL_BACKDROP` constant) and pinned by a test that fails if a second
`loop=True` ever appears in the assembly path? That is the difference between a
rule that holds and a rule that happens to hold.

## 3. DEFECT A -- a comment defends a live rule by citing a DELETED function

```python
# nodes/_otr_video_engines/frame_contract.py:280-286
#: ``max_render_frames`` is NOT a general planning cap ... WAN reads 17 from
#: ``config/profiles/otr_8gb_wan.json``, renders a short native clip, and
#: PING-PONGS it up to the beat's full length (``eng_wan_ti2v._floor_length``
#: -> ``wrapper_bridge.extend_frames_to_target``); its beat length is unchanged
#: by the ceiling. So narrowing WAN's contract before ``partition_beat`` would
#: turn every WAN beat into a pile of 17-frame renders...
```

Three things are wrong with this, and it is the comment that governs
`PLANNING_CAP_ENGINES` membership:

1. **`wrapper_bridge.extend_frames_to_target` DOES NOT EXIST.** Verified:
   `grep "def extend_frames_to_target"` returns nothing; `wrapper_bridge.py:499`
   is its tombstone.
2. **WAN does not ping-pong.** `eng_wan_ti2v.py:1105-1113` refuses.
3. **`wan_ti2v` is IN the allowlist**, added 2026-08-02 (`frame_contract.py:316`)
   -- so the comment argues against a decision the file itself already made
   thirty lines lower.

The mechanism the comment cites as the reason WAN is safe was deleted, which is
the same reason `wan_ti2v` had to JOIN the allowlist. The prose is a fossil of
the pre-no-mirror world sitting on top of post-no-mirror code.

## 4. DEFECT B -- the grader's single-clip carve-out is now unfounded

`acceptance.py` skips `len(planned) <= 1`, and
`tests/test_wire_w5_acceptance_grader.py:234-243` is an explicit CONTROL:

> `test_a_SINGLE_CLIP_beat_may_PAD_all_it_likes` -- *"CONTROL, and it is the
> shipped 8 GB WAN tier: a single-clip beat renders short on purpose and fills
> the beat with a mirror (PBUG-20260723-02). If this went red the grader would
> be failing production's majority path."*

The r4 round DEFERRED closing this because the files contradicted each other and
the driver could not tell which was stale. **The operator's ruling resolves it:
the tier that test defends no longer exists.** The test now protects a path the
build refuses to take.

Closing the carve-out means `extension_mode != "none"` becomes a finding on ANY
beat, not only multi-segment ones. **Grounded claim that it is safe:** no engine
can produce a non-`none` mode (`eng_wan_ti2v.py:1086` hardcodes `"none"` and
both padding paths raise; `eng_wan_i2v.py:716` and `eng_ltx_8gb` hardcode it),
and the credits tail (section 2) is not a clip row. **The panel should attack
this claim specifically** -- it is the one behaviour change proposed here.

## 5. DEFECT C -- the matrix has no column for the question the grader asks

**This is the operator's ask and it is the most useful thing in this document.**

### 5.0 WHICH MATRIX -- corrected after the operator pointed at it

The operator asked whether I was looking at the right file. I was not looking at
the primary one. **THE matrix is
`docs/2026-08-02-FINAL-all-engine-maths-and-stills.md`** -- "FINAL DECISION:
maths + still logic, every video model, local and cloud". It is the only
document covering BOTH local and cloud lanes, and it already carries the right
column shape: `dropdown name | family | contract | join | segs @442 |
consumes still | canvas`.

There are **THREE overlapping matrices**, and that is itself part of the
problem:

| doc | scope | maintained | drift-gated |
|---|---|---|---|
| `2026-08-02-FINAL-all-engine-maths-and-stills.md` | local + cloud, + still logic + a fix list | by hand | **no** |
| `retired/2026-08-02-all-local-engines-multiclip-maths-RETIRED.md` | local only, coverage arithmetic | by hand | **no** |
| `ENGINE_MATRIX.md` | per-model clip window, from the LIVE registry | generated | **yes** (`tools/engine_matrix.py --check` + suite test) |

### 5.1 THE ROT IS ALREADY MEASURABLE -- two of its own fix items have moved

The FINAL doc's fix list is four days old and has already drifted from the code:

* **F1 is DONE, and the doc still calls it blocking.** It states
  `assert_frame_affordable` (`motion_common.py`) "has ZERO call sites, while
  `PLANNING_CAP_ENGINES` contains `wan_ti2v`, `fastwan_8gb`, `ltx_8gb`. Every
  coverage-planned segment on all three renders with NO preflight VRAM check."
  **It is wired now:** `motion_common.assert_frame_affordable` is called at
  `render_driver.py:3133`, inside `_assert_beat_affordable` (`:3061`), which
  `render_beat_coverage` invokes at `:3303` -- BEFORE `BeatSession` opens, which
  is exactly where the doc asked for it.
* **F4 appears still OPEN.** The doc says `fastwan_8gb` should drop 81 -> 65 or
  be requalified. Today `max_render_frames: 81` is pinned in
  `otr_8gb_fastwan.json`, `otr_g4_fastwan.json`, `otr_w45_fastwan.json` and
  `otr_g4_wan_ti2v.json`.

**A safety item that has been FIXED still reads as blocking, and one that is
open reads the same way.** A reader cannot tell them apart, which is worse than
either state alone -- and it is the precise failure a status column prevents.

### 5.2 THE MISSING COLUMN

Post-`e499b7fc`, exactly three adapters can answer "were these frames real?":
`eng_wan_ti2v`, `eng_wan_i2v`, `eng_ltx_8gb` (plus `fastwan_8gb` by
inheritance). Cross that against the coverage matrix in
`retired/2026-08-02-all-local-engines-multiclip-maths-RETIRED.md`:

| dropdown name | multi-segment plan | emits honesty receipt |
|---|---|---|
| `wan_8gb (16:9)` | chain, 3 | **yes** |
| `fastwan_8gb (16:9)` | chain, 3 | **yes** (inherits) |
| `wan_i2v (16:9)` | chain, 3 | **yes** |
| `ltx_8gb (16:9)` | chain, 3 | **yes** (new, `e499b7fc`) |
| `ltx23_16gb_video (16:9)` | chain, 3 | **NO** |
| `humo (portrait)` | jump, 3 | **NO** |
| `humo_1.7B (portrait)` | jump, 3 | **NO** |
| `humo_1.7B_169 (16:9)` | jump, 3 | **NO** |
| `humo_14B_169 (16:9)` | jump, **10** | **NO** |
| `ltx23_16gb_audio_in` | single, 1 | n/a (never multi) |

**Five of the ten local engines are planned into real multi-clip beats and
cannot say whether their frames were rendered.** Every one of them trips
`acceptance.py`'s "declares no extension_mode" branch TODAY -- before this
change and after it. `humo_14B_169` does it across ten segments.

Had the matrix carried this column, Arm B of the previous item (`ltx_8gb`
silently receiptless while listed as a coverage planner) would have been visible
on the page instead of costing a panel round to find.

**The proposal:** add a per-path column recording (a) whether the lane emits
`native_frame_count` / `extension_mode`, and (b) its padding posture under the
no-mirror rule. Open questions for the panel:

1. Which matrix -- the GENERATED `ENGINE_MATRIX.md` (drift-gated, so the column
   would be enforced) or the hand-written multiclip-maths doc (richer, but rots)?
2. Can `tools/engine_matrix.py` derive the receipt column from the live
   registry, or does it require calling `render_clip`, which the generator must
   not do? If it cannot be derived, a drift-gated column is not possible and the
   honest answer is a hand-maintained one that says so.
3. Should the five receiptless lanes be FIXED in the same change, or recorded
   and scheduled? They are already failing; fixing five adapters is a much
   larger change than recording that they fail.

## 6. WHAT IS NOT PROPOSED

* No change to `coverage_plan.py`, the join modes, the partition arithmetic, or
  the bench graphs. The multi-clip mechanism is settled and stays settled.
* No change to the credits tail loop (section 2) beyond possibly NAMING it.
* No new `PROD_BUG_LOG.md` entry -- all of this is static.

## 7. RECEIPTS OWED

* Code-complete + full suite (baseline **8836 passed / 131 skipped / 1
  xfailed** at `e499b7fc`) + Bug Bible 17.
* If the matrix column is drift-gated, `tools/engine_matrix.py --check` must
  pass and the suite test must cover it.
* `workflows/otr_canonical.json` expected byte-unchanged -- nothing here touches
  `INPUT_TYPES`, widgets, node classes or links.
