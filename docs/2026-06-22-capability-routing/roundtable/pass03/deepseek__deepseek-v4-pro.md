<!-- requested_model: deepseek/deepseek-v4-pro | resolved_model: deepseek/deepseek-v4-pro-20260423 -->

VERDICT: yes-with-fixes. The core logic change (drop the `roles` gate) is correct and provably non-regressive, but the plan has one critical gap and two dangerous ambiguities that will cause runtime failures if not resolved before build.

MUST-FIX BEFORE BUILD:
1. [Section 2, Changes item 2] The plan says "Remove the now-dead `roles` attrs from the engine classes" but the grounding excerpt explicitly states: "OPEN: where does the director descriptor's `roles` come from? otr_video_director.py builds descriptors with 'required_inputs': tuple(getattr(eng, 'required_inputs', ())). The `roles` key source (default_roles? a separate `roles` attr? capability-derived?) is the thing to confirm." If you remove the `roles` attribute from engine classes without FIRST confirming and modifying how `otr_video_director.py` populates the descriptor's `roles` key, the `engine_fits_role` function will fail on line `roles = descriptor.get("roles")` returning None, triggering `if roles is None or required is None: return False` for EVERY engine. This is a hard break. Fix: BEFORE removing engine `roles` attrs, trace and modify `otr_video_director.py` to either (a) stop populating the `roles` key in descriptors entirely (since the gate is removed), or (b) populate it from `default_roles` or an empty tuple. Then remove the `role not in tuple(roles)` gate. Verify: check `otr_video_director.py` descriptor construction code.

2. [Section 2, Changes item 1] The plan says "remove the `role in tuple(roles)` gate" but does not address the preceding `if roles is None or required is None: return False` check. If `roles` becomes optional (because the gate is removed), this check will still fail-closed when `roles` is None, which is correct behavior. However, if the descriptor's `roles` key is removed entirely (as implied by item 2), this check MUST be updated to only fail on `required is None`. Fix: change to `if required is None: return False` (remove the `roles is None` condition). This is the minimal change that preserves fail-closed behavior for missing required_inputs while allowing the gate removal.

3. [Section 4, Wiring opens item 1] The plan acknowledges aspect ratio as a risk but defers confirmation: "CONFIRM aspect is enforced DOWNSTREAM... If a wide engine in a portrait role genuinely breaks, add an explicit supported_aspects capability." This is a gap, not a risk. The current system implicitly prevented wide engines from serving portrait roles via the `roles` whitelist (wan_i2v was not in `character_video`). With the gate removed, wan_i2v (wide) WILL become eligible for `character_video` (portrait). The plan must either (a) prove downstream enforcement exists and works BEFORE merging, or (b) add the aspect check to `engine_fits_role` as part of this change. Fix: Add `supported_aspects` to engine descriptors and an aspect compatibility check in `engine_fits_role` that gates on `role_aspect == engine_aspect` for roles with defined aspects. Verify: check `_role_aspects` in director and engine `render_aspect` attributes from grounding.

SHOULD-FIX:
1. [Section 2, Changes item 3] "CUT optional_inputs" is mentioned without context. The grounding shows no `optional_inputs` in the code excerpts. If this refers to engine attributes not shown, it's safe but unnecessary scope creep. If it refers to something in the actual codebase, it needs explicit justification. Clarify or remove from this change set.

2. [Section 4, Wiring opens item 2] "Confirm the director's auto-PICK uses default_roles (UNCHANGED)" - the plan assumes this but doesn't specify HOW to confirm. Add a specific test: for each engine with non-empty `default_roles`, assert that after the change, the auto-selected engine for that role's slot is identical to before. This requires knowing the current auto-selection logic, which isn't in the grounding. Verify: auto-selection code path in director.

3. [Section 4, Wiring opens item 3] The before/after test mechanism is described but not specified. It should be a concrete test file that: (a) imports the CURRENT `engine_fits_role` (saved as `engine_fits_role_old`), (b) imports the NEW `engine_fits_role`, (c) for all (engine, role) pairs, asserts `old_true => new_true`, (d) prints the additive deltas. This test must be committed WITH the change, not written after.

OPTIONAL / NICE-TO-HAVE:
- The plan mentions "KEEP every engine's required_inputs (wan stays ("init_image",) -- correct i2v; it still WON'T fit the still-less background_abstract)" - this is correct but could be strengthened with an explicit test: assert not engine_fits_role(wan_descriptor, "background_abstract").

CUT THESE (over-engineering):
1. [Section 2, Changes item 2] "Remove the now-dead `roles` attrs from the engine classes (or leave as comments)" - leaving as comments is unnecessary clutter. Either remove them (if the director descriptor source is fixed) or keep them as functional attributes (if the director still needs them for other purposes). The "or leave as comments" option adds no value and creates confusion. Cut it.

[ASSUMPTION] The director's auto-selection logic uses `default_roles` and not the `roles` attribute. The grounding doesn't show this code path.
[ASSUMPTION] The aspect ratio enforcement exists downstream and works correctly. The grounding mentions `_role_aspects` and `render_aspect` but doesn't show the enforcement logic.
[ASSUMPTION] The `optional_inputs` mentioned in Changes item 3 exists in the actual codebase but not in the grounding excerpts.