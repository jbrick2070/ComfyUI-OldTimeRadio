# Pairlock 04 and the next family qualification

No production change is included in this checkpoint. The last code revision is
d2837e45, already reviewed by Sonnet 5 and qualified by the full regression and
Bug Bible comparison in `../2026-09-11-my-story-treatment-followup/`.

Full canonical attempt04 on that revision failed naturally in treatment writing
after 386.22 seconds. Prompt `ff0f3b5a-595d-4732-9ddc-e85be8debde6` exhausted
exactly three treatment attempts, generating 224/1568/1568 tokens before the
verbatim-cycle guard. P0 and its correction used one call each and ended on
EOS248046 at 794/481 tokens. No assembled lines, freeze, media or publication.
The sparse P0 correction retained the accepted metadata: Jeffrey male, Mother
female, and Girlfriend nonspeaking and not required. This is actual conservation
evidence; no later casting or visual outcome is inferred.

Request, failed history, ledger and server/runner logs are the `pairlock_04_*`
files in `../2026-09-11-my-story-5080-qualification/`. The prequeue readiness
failure submitted no prompt and is preserved separately. The server was stopped
selectively only after terminal evidence was saved: port8000 empty, desktop
2055 MiB. Four recovery attempts across recorded revisions remain: three writing
failures, one publication with source defects, zero source-qualified episodes.
This is a history denominator, not a constant-build failure-rate experiment.

## Independent review and root judgment

Actual reviewers: API Opus 5 and Gemini 3.1 Pro Preview for diagnosis, followed by
one Opus 5 grounding response. Three local independent readers audited terminal
evidence, the real retry owners and installed model/profile facts. Cursor's two
earlier follow-up attempts timed out without a review; no Cursor consensus is
claimed. These calls are a scoped diagnostic continuation, not a new four-round
architecture arc or a new Sonnet code review.

| Reviewer claim | Grounded disposition |
|---|---|
| Title says no quotation marks; attempt1 copies it inside logline. | True format ambiguity and leakage; not a demonstrated cause of later broader instruction spirals. No title-only patch or speculative schema rewrite in this checkpoint. |
| Attempts2/3 are identical, proving ignored sampling or cached output. | Rejected. Captured text differs: 7399/7403 characters, common prefix3702. Equal token/cycle counts do not prove identical inputs, and even identical outputs would not establish that inference. |
| The earlier packet omitted the repair function. | True excerpt-generation mistake; original packet retained. Exact AST-selected function supplied in opus_grounding.md. |
| The existing latest-completion test is not the P1 ladder. | Rejected by tests/test_my_story_runner.py:978, which runs the actual treatment ladder and saves a valid corrected ledger. Its native-tokenization scope gap was separately probed. |
| Native retry parameters or repair messages may be lost. | CPU boundary probe uses actual ladder/scheduler/native preparation, cached tokenizer and installed LMFE. Temperatures .85/.5/.1, sampling enabled, full source, latest complete failed draft and distinct final prompt are observed. No reachable OTR routing defect found. |
| min_p failure state is process-global and contaminates other models. | Rejected. _min_p_unsupported is closure-local in _build_truncating_generate_fn; the scheduler constructs that base per call. No such TypeError was reported in live04. |
| Repairing from the entire failed draft can encourage copying. | Plausible model-conditioning risk, not a proven code defect or permission to delete required repair evidence. Full failed artifact and original source are the explicit operator contract. Three-call budget remains fixed. |
| One seeded/model-family trial settles causality. | Rejected. Neither establishes a population effect or universally validates transport. Existing family-coverage work can still collect useful compatibility evidence. |
| Mistral changes native capacity, tokenizer, grammar vocabulary and potentially inherited model defaults. | Accepted. Hold configured source/profile/sampling controls, record these model-dependent differences and actual capacity. Do not describe it as an otherwise identical native decoder or a binary causal experiment. |
| Add a fixed output cap or a predictive VRAM assertion. | Rejected as a new admission policy. Provider-capacity output policy and truthful OOM behavior are explicit operator requirements. NF4 differs from the unquantized24GB disk footprint; the existing profile supports this installed model. |

The external reviewers did not converge on the next experiment. Root resolved
the choice after grounding their claims: proceed to the already-required
different-family compatibility leg, with no production/prompt edit. The boundary
probe resolves the suspected OTR handoff loss; it does not explain the model's
initial degeneration. Qwen reliability remains open.

Reported API cost for this checkpoint: **$0.220571** ($0.130731 diagnosis plus
$0.089840 Opus grounding). Actual resolved identities and usage are in the two
manifests. No rental started. Prior review costs remain in their own receipts.

## CPU diagnostic scope

`boundary_probe_source.txt` is an archived diagnostic, not shipped executable
code. It loads only the actual local Qwen tokenizer/config, uses the saved source
and accepted interpretation, and exercises _pass_treatment, shared structured_call,
the scheduler, native prompt preparation and LMFE. A recording model boundary
captures kwargs and raises the saved failures; no weights or generation run.

Final `boundary_probe.json` shows input sizes2327/2327/4032, matching live04.
Attempts1/2 share a token hash; attempt3 differs and contains the entire second
completion and raw source. Grammar callbacks are fresh; no supplied KV cache.
The exact reconstructed prompt text and hashes are archived. They are not the
original live prompt capture, sampled token sequence, logits, or evidence of
model-internal cache behavior. Installed Transformers code was independently
checked for explicit-kwargs precedence and fresh/reset generation cache ownership.

Initial diagnostic setup mistakes are retained: missing generation_config.json
(use actual decoder EOS from config), wrong scheduler constructor arguments and
wrong pack fixture shape. A first successful probe used shortened error wording,
yielding4020 repair tokens; that receipt is explicitly superseded by the final
saved-error reconstruction yielding4032. These are diagnostic fixture failures,
not new production bugs, successful hardware runs or hidden denominator changes.

## Qualification and next step

Production files, schemas, prompt pack, sampling code and workflow are unchanged
from d2837e45. Its previous results remain applicable: 193 focused passed;
full14409 passed/51 unchanged inherited failures/183 skipped/one xfailed;
controlled Bible37 passed/10 inherited failures/11 skipped/3 xfailed. No new
full-suite run is represented here. Canonical validator, round-trip and live
widget/link audit passed again:23 nodes,63 links,37 writer widgets; canonical
SHA256 d586a286aaee4c039e410ae9a10014c5c7f4ab82d00eac0e9e1cc0564415057c.

The installed Mistral Nemo snapshot04d8a90549d23fc6bd7f642064003592df51e9b3
contains all five indexed weight shards plus tokenizer/config. Existing
otr_w45_still_pan selects it with CUDA/NF4/SDPA and14.5GB. Use the shipped full
canonical runner, same source, changing only the two submitted model selections.
Actual native context131072 and tokenization differ from Qwen; record their
runtime observations. A publication needs ledger/source/voice/credits/pixels
inspection before any source-qualified claim. GO_FORWARD owns that next leg
and later stress work. Mac/4060 remain held; RunPod remains authentication-blocked.
