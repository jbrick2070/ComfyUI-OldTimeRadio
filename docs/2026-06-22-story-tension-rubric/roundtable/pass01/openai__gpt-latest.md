<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. The plan still has unresolved architecture choices and a broken STEP 5 <-> STEP 6 data path: tension is proposed as a critic target, but no concrete persistence/rendering/reroll reconstruction path is specified.

MUST-FIX BEFORE BUILD:
1. [STEP 6 design question + Grounded fact 6 + Grounded fact 7] `beat_tension` is supposed to affect both composer and critic, but the design only says to wire it into `LineRequest`; the critic reads ledger-rendered rows, not `LineRequest`, and the line schema is frozen. Concrete fix: define one frozen-wire-safe representation, e.g. `meta.beat_tension_by_line_id = {"b004": 3, ...}`, and explicitly update the critic line renderer/user prompt to include `target_tension=N/5` by line_id. Do not add Pydantic line fields.
2. [STEP 6 design question + _otr_reroll.py build_reroll_line_request] rerolled lines will not receive the new tension signal. `build_reroll_line_request` currently reconstructs `LineRequest` with `arc_phase`, but omits `dramatic_question`, `beat_objective`, `beat_obstacle`, `beat_turn`, `beat_subtext`, `next_turn`, and `beat_tension`. If STEP 5/6 rerolls are meant to repair flatness against the same target, reroll composition must reconstruct the same dramatic frame from ledger/meta. Concrete fix: stamp the per-line dramatic frame in `meta` or otherwise reconstruct it deterministically, and pass `beat_tension` plus the existing objective/turn fields in reroll `LineRequest`.
3. [STEP 5 design question] “advances its slot line_job” is not grounded in the shown critic input. The critic prompt/rendering currently exposes `beat_intent`, not `line_job`; the grounding says the critic sees ledger rows with `arc_phase`, `beat_intent`, `mood`, word counts, text. Concrete fix: either replace `line_job` with `beat_intent` in the rubric, or explicitly expose `line_job` to the critic via meta/rendering before using it as a judgment criterion.
4. [STEP 5 design question] `failed_dimension` placement is left open: “FlatLine and/or RerollTarget (or both).” That is not build-ready because downstream behavior depends on where the value lives. Concrete fix: choose a single contract. Smallest workable option: add `failed_dimension` to `RerollTarget` only, because `_otr_reroll.py` acts only on `reroll_targets`; optionally mirror it on `FlatLine` only if UI/debug needs it. Make it backward-compatible with a default such as `"unspecified"` so older `meta.story_critic_report` dicts still validate.
5. [STEP 5 design question + _otr_reroll.py] The plan says “update the `_otr_reroll.py` hint parser/consumer,” but the shown reroll consumer has no parser; it copies `RerollTarget.hint` into `compose_line(..., reroll_hint=hint)`. Concrete fix: either remove “parser” from the design and require the critic prompt to generate final actionable hints, or add a real deterministic mapper from `failed_dimension` to a REVISE prefix before `compose_line`.
6. [STEP 6 design question] the tension curve is not chosen. The section lists A/B/C and open questions about peak placement/easing. That leaves implementers to invent story semantics. Concrete fix: pick one deterministic curve now, including edge cases. Example: character dialogue beats only; nondecreasing across character beat ordinal; `n <= 1 -> 3`; otherwise `round(1 + 4*i/(n-1))`, clamped 1..5. If you want resolution easing, rename it from “escalating tension” to “intensity” because easing contradicts the stated escalating lever.
7. [STEP 6 design question] scope of beats is ambiguous: “voiced beats,” “last line,” and “announcer close is separate” are all mentioned, but the critic judges character dialogue only and announcer lines use dedicated composer passes. Concrete fix: define tension scope as `speaker_role == "character"` only unless there is a concrete reason to score announcer lines; exclude music/SFX/announcer from the curve and from critic tension checks.
8. [STEP 5 design question] “moves toward its target tension” is not an operational rubric. A low-tension setup line should not be flagged for failing to be explosive, and a high-tension line should not be allowed to merely shout. Concrete fix: add prompt definitions for tension levels, e.g. 1=orientation/unease, 3=active pressure/choice, 5=irreversible decision/reveal/cost, and instruct the critic to judge appropriateness to the target, not raw intensity.
9. [Invariants + STEP 5 design question] adding `failed_dimension` as a required enum would break validation of existing stored `meta.story_critic_report` dicts when `_coerce_report` runs in `_otr_reroll.py`. [ASSUMPTION] old reports may exist in ledgers or tests. Concrete fix: make the field optional/defaulted and keep `StoryCriticReport.clean()` valid with no new required fields.
10. [STEP 5/6 design question] the plan’s stated goal is to reduce residual subjective quality flags, but tightening the flat rubric plus adding a tension target can easily increase flags unless thresholds are specified. Concrete fix: include a “be sparing” calibration tied to current behavior, e.g. only emit `reroll_targets` when a line fails both beat advancement and one named failed dimension, and cap reroll targets per scoped pass if needed.

SHOULD-FIX:
1. [STEP 6 design question] Candidate A “resolution -> 5” and Candidate B “peak at final beat” conflict with the already-known absence of separate climax/falling phases. A whole resolution block at maximum tension can fight actual resolution. Concrete fix: if using phase bands, make resolution range `4..5` with only the first irreversible reveal/cost beat at 5, or use ordinal ramp and accept final-beat peak explicitly.
2. [STEP 6 design question] Candidate B has an undefined `n_voiced_beats-1` denominator for one-character-beat episodes. Concrete fix: specify `n <= 1` behavior in the chosen formula.
3. [Grounded fact 2 + STEP 6 design question] “climax” language appears conceptually even though budget phases are only `setup/complication/resolution`. Concrete fix: use “peak” or “resolution-start peak,” not “climax phase,” unless a new phase is actually introduced, which the plan says not to do.
4. [STEP 5 design question] the five flatness dimensions are good as literary criteria, but they overlap with `beat_objective`, `beat_turn`, and `beat_obstacle` already in composer context. Concrete fix: map each failed dimension to existing fields so the composer and critic share the same vocabulary, e.g. `obstacle` failure references `beat_obstacle`, `decision` failure references `beat_turn`.
5. [_otr_story_critic.py post_validator] if `render_priority` must order every shown line, the current post-validator only rejects unknown ids; it does not check completeness or duplicates. Lower-weight, but if STEP 6 uses render coupling, incomplete priority lists can silently degrade behavior. Concrete fix: add a lenient warning/repair validator for render_priority completeness, or state that missing render_priority is allowed.
6. [_otr_reroll.py run_targeted_reroll docstring] the public docstring says lines are “restored to their pre-reroll state” on cap, but the module comments and code implement repair-then-ship and keep rerolled lines. Concrete fix: update the docstring; it currently contradicts the STEP 4 invariant the design relies on.
7. [Invariants] “every gate is one opus passes -> lifts the weak end, never rewrites the good” is unclear and not enforceable. Concrete fix: rewrite as a checkable invariant: “only line_ids in `reroll_targets` may be recomposed; approved lines remain byte-identical.”

OPTIONAL / NICE-TO-HAVE:
- Add a small design table showing `failed_dimension -> critic meaning -> reroll hint prefix`.
- Add ledger/meta example for one line showing `beat_tension_by_line_id` and any dramatic-frame maps.
- Add regression fixtures for short episodes: 1, 2, and 13 character beats.

CUT THESE (scope / over-engineering):
1. [STEP 5 design question] Cut “FlatLine and/or RerollTarget” dual-field uncertainty. Use one authoritative field first. Duplicating the enum in multiple schema objects invites drift and repair complexity.
2. [STEP 6 design question] Cut optional easing for this build. It conflicts with the stated “escalating beat_tension” goal and creates avoidable debate over resolution semantics. Add it later only if soaks show over-pressurized endings.
3. [STEP 6 design question] Cut critic-side “did this line hit target tension” until the target is actually visible in critic input. First build the meta/rendering path; then enable the rubric reference.
4. [STEP 5 design question] Cut any deterministic code gate for flatness. The document already says flatness remains LLM judgment; keep that boundary or this will balloon into brittle literary linting.
5. [STEP 6 design question] Cut any SceneArcContext work. This is already called out in Grounded fact 1 and confirmed by `LineRequest`; adding it would duplicate existing arc fields.