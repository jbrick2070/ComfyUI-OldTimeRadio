<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. Core stamp/profile propagation and artifact identity rules are internally inconsistent and not executable against the shown workflow/runtime surfaces.

MUST-FIX BEFORE BUILD:
1. [How a profile reaches the graph: APPLY-time, not render-time plumbing] The profile stamp is not actually reachable at execution time as written. The plan says the generator writes `profile_id + master_hash + generated_by` into “node-63 widget + node properties”, but the shown `OTR_WorkflowValidator.INPUT_TYPES` has only `workflow_json_path`, `validate_anyway`, and `strict_unknown_types`, and Comfy API prompts do not carry node `properties` into node inputs. `wrapper_bridge` also cannot “resolve the in-graph profile stamp” from node properties during render. Concrete fix: add explicit executable inputs/widgets, e.g. `profile_id`, `master_hash`, `snapshot_hash/generated_by`, to `OTR_WorkflowValidator` or a new `OTR_ProfileStamp` node, and define how that node publishes the resolved profile to runtime code used by `wrapper_bridge` before any engine loads. Do not rely on `properties`.

2. [VRAM safety becomes profile-driven] `wrapper_bridge` stamp resolution is underspecified and currently impossible from the shown code path. `workflow_to_api_prompt()` serializes node inputs, not whole UI graph metadata; `nodes/_otr_video_engines/wrapper_bridge.py` will not see node-63 properties or graph `extra`. Concrete fix: make `OTR_VRAM_CEILING_MB` the primary runtime mechanism for headless/launcher, and for UI snapshots have an executing stamp/validator node set a process/session profile context before video engines run; otherwise remove “env > stamp > 14500” and require env explicitly.

3. [Validator + node 63] Repo-relative node-63 paths are not portable with the current validator implementation. `_load_workflow()` does `p = Path(path)` for non-empty paths, so `"workflows/generated/otr_8gb_lite.gen.json"` resolves relative to the ComfyUI process CWD, not `_REPO_ROOT`. Concrete fix: in `_load_workflow`, if `path` is relative, resolve it as `_REPO_ROOT / path` before `is_file()`.

4. [Decision A / How a profile reaches the graph / Validator + node 63] The byte-identical identity check conflicts with generated artifact stamping and node-63 self-validation. `apply(master, 16gb_full) == master` byte-identical cannot also set node 63 to the generated artifact path, add generated stamp fields, or include a `master_hash` without changing bytes. The same conflict breaks the parity test: `snapshot(P)` should validate its own `.gen.json`, while `apply(master,P)` has no artifact path unless supplied. Concrete fix: define two operations: `apply_profile(master, profile)` for semantic widget values and `emit_snapshot(applied, artifact_path)` for stamp/path injection. CI identity should compare a normalized graph excluding generated-only stamp/path fields, or the master must be explicitly updated to the 16gb applied widget values without generated-only fields.

5. [How a profile reaches the graph: APPLY-time, not render-time plumbing] The “startup assertion before model loads” is not guaranteed by the shown graph. Node 63 has `OUTPUT_NODE = True`, but in the JSON its `validation_report` output has `links: null`; it is not an ancestor of heavy render nodes. [ASSUMPTION] If Comfy schedules independent output nodes without a strict “node id/order first” guarantee, heavy nodes can start before the assertion. Concrete fix: wire the validator/stamp node output as a `gate_in` dependency into the first profile-sensitive branch, or into all heavy/audio/video dispatch branches that must not load before validation.

6. [The profile system / S1 Registry capability metadata] “Hard-rejects ... two heavies co-resident” is not statically well-defined and can reject the current master-style design. The shown master has multiple heavy-looking video choices saved at once in `OTR_VideoDirector` (`ltx_video`, `ltx_video`, `humo`) plus node 92 default `"humo"`; the grounded invariant is single-heavy residency via `wrapper_bridge` lifecycle, not “only one heavy engine may appear in saved widgets.” Concrete fix: change profile validation to reject only impossible simultaneous residency as proven by execution ordering, or simply ensure every selected/fallback engine fits the profile and leave residency enforcement to `wrapper_bridge`.

7. [How a profile reaches the graph: APPLY-time, not render-time plumbing] The “profile-managed widget map covers ALL engine-bearing widgets” claim is not closed by the listed nodes. The actual workflow contains at least additional model/engine-looking saved values outside the list: node 88 `OTR_ImageDirector` has image model defaults and `request_seed`; node 1 has model/slot widgets; node 3 has `"bark"` in `widgets_values`. Some may be creative or legacy rather than profile-managed, but the plan must explicitly classify them. Concrete fix: add a schema-driven coverage test that enumerates all OTR widget-backed COMBO/string fields whose values are registered engine IDs/model selectors, then requires each to be either profile-managed or explicitly exempted with a reason.

8. [How a profile reaches the graph: Precedence] Selection precedence contradicts availability precedence. It says `OTR_FORCE_ENGINE_MAP` is the top selection override, but availability is `registry reality ∩ OTR_ENABLE_* ∩ profile-derived enable-set`; that means a forced engine outside the profile can never actually be selected. Concrete fix: specify one behavior: either force is a dev-only bypass of profile availability, or force only chooses among profile-available engines and fails loudly if outside the derived enable-set. Add tests for both parse-error and disallowed-force cases.

9. [S2 One applier, headless converted / Acceptance demo] The parity assertion is underdefined because node-63 `workflow_json_path`, stamp fields, seeds, and run IDs will legitimately differ between `snapshot(P)` and `apply(master,P)`. Concrete fix: define the exact ignored fields or pass an `artifact_path` parameter into `apply()` when generating the comparison artifact; otherwise dict equality will fail for the validator/stamp inputs.

SHOULD-FIX:
1. [How a profile reaches the graph: APPLY-time] `apply(workflow_or_api_prompt, profile)` is too vague. The current patch machinery in `scripts/otr_api.py` operates on UI workflow JSON using node ids and live schemas; API prompts are keyed by node id strings and class input names. Concrete fix: either restrict `apply()` to UI-format workflows only and always convert afterward, or implement separate typed adapters for UI workflow vs API prompt.

2. [S0/S1 sequencing] The validator needs capability metadata before it can derive enable-sets, validate role defaults, or generate model manifests. The sprint plan puts profile schema/validator in S0 and registry metadata in S1; S0 can only validate JSON shape, not “impossible profiles.” Concrete fix: split S0 into shape-only validation and defer capability/profile cross-validation gates until S1 metadata exists.

3. [The profile system] The sample schema includes `allow_sidecars`, `max_model_class`, and `vram_budget_mb`, but the enable-set rule only mentions budgets/toolchains/requirements. Concrete fix: define exactly how `allow_sidecars` and `max_model_class` affect enabled engines, or remove them from v1.

4. [Validator + node 63 / CI gates] “Build fails if any saved COMBO value is absent from the target schema” needs exceptions aligned with existing admissible dynamic values. `scripts/otr_api.py` already admits out-of-list OpenRouter and Comfy slot values via `_is_openrouter_admissible()` and `_is_comfy_admissible()`. Concrete fix: either reuse those admit paths in the new CI COMBO validator or explicitly state that generated artifacts must not contain those dynamic slot values.

5. [How a profile reaches the graph] Direct `NODE_CLASS_MAPPINGS` import for build-time schemas is plausible but risky because some node packages may import torch/sidecars at module scope. The plan notes this as verify-at-build, but generator/CI depends on it earlier. Concrete fix: make “cold import without CUDA deps” a blocking S0/S1 gate before relying on direct imports for generation.

6. [The profile system / Decision B] Generated per-profile model manifests from `model_requirements` require all enabled engines to be import-registered in the build environment. [ASSUMPTION] If adapter registration is conditional on optional deps, the manifest may omit engines. Concrete fix: require metadata to live in dep-free registry declarations, not inside modules that import heavy sidecars.

7. [Determinism] `(profile_id, seed) -> identical bytes` is stronger than the grounded invariant unless every engine, CUDA kernel, ffmpeg path, timestamp, temp filename, and metadata field is normalized. Concrete fix: define whether “identical bytes” includes container metadata and generated filenames, and pin/strip nondeterministic MP4 metadata before making this a shipped gate.

8. [v1 tier set] `cpu_floor` cannot simply be a profile if CUDA-only modules still import or probe CUDA during cold load. The plan has a verify note, but `cpu_floor` should be blocked until cold-import and queue-smoke on a no-CUDA env pass. Concrete fix: make cpu_floor artifact generation dependent on the S3 cold-import test, not only S4 smoke.

9. [How a profile reaches the graph] The profile-managed widget map uses hard-coded node ids 81/82/83/92/63. That is acceptable for the current master but brittle when the master graph is edited. Concrete fix: store map entries as `(node_type, title or unique role, widget_name)` with a check that exactly one node matches, or keep ids but add a CI failure that explains how to update the map when ids change.

OPTIONAL / NICE-TO-HAVE:
- Add a `profile.schema.json` with examples for all three profiles and committed golden derived enable-sets to make review diffs obvious.
- Include a small `profiles/README.md` explaining that profiles are capability policy, not user creative presets.
- Add a generated artifact banner in workflow `extra.info.description` saying “generated; do not edit.”

CUT THESE (over-engineering):
1. [Decision A] Cut strict byte-identical `apply(master, 16gb_full) == master`. Use normalized semantic equality over profile-managed widgets instead. It avoids unavoidable differences from stamp/path/hash fields while still preventing drift.

2. [The profile system] Cut static “two heavies co-resident” rejection from the profile validator. Residency is a runtime lifecycle invariant already grounded in `wrapper_bridge`; static graph choices and fallback chains are not equivalent to resident models.

3. [How a profile reaches the graph] Cut duplicate stamping into both node properties and widgets. Properties are not executable in API prompts; widgets/explicit node inputs are sufficient and testable.

4. [Headless = same applier] Cut grep-based soak-script enforcement if the applier API itself refuses profile-managed patches outside `apply()`. Runtime/API enforcement is stronger than grep and less brittle.

5. [Decision B] Cut launcher emission from the first build if profiles/generator/runtime assertion are not stable yet. The wizard depends on profile correctness, manifests, and VRAM ceiling behavior; it can ship after the three artifacts can run manually.