<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. The recommendation still leaves the profile source, generator contract, headless integration, Mac/MPS behavior, and validator path semantics undefined; as written it can still ship drifting or invalid JSONs.

MUST-FIX BEFORE BUILD:
1. [Reconciliation + grounded cross-platform audit / generated per-tier snapshot JSONs] Generated snapshots will not validate themselves with the current workflow validator. Grounding shows `OTR_WorkflowValidator` falls back to `workflows/otr_scifi_16gb_full.json` when its widget is empty, and the current workflow node 63 ships `widgets_values: ["", true, true]`. If you generate `otr_8gb_lite.json`, `otr_24gb_mac.json`, etc., the embedded validator will still validate `otr_scifi_16gb_full.json`, not the running snapshot. Concrete fix: make the generator set node 63 `workflow_json_path` to the generated artifact path, and update `OTR_WorkflowValidator` to resolve repo-relative paths portably; then CI must load each generated JSON and prove node 63 validates that exact file. Do not ship empty validator paths in generated artifacts unless the fallback is changed to the current workflow artifact.

2. [Working recommendation / Open question #4 / Reconciliation] The drift bug is not actually killed unless headless stops using hard-coded patch sets. Grounding says there is no second production graph; the drift is saved widget defaults vs per-script patches in `scripts/otr_api.py`, `queue_smoke.py`, and soak runners. `queue_smoke.py` still hard-codes `WORKFLOW_PATH = workflows/otr_scifi_16gb_full.json` and patches node 1 widgets by name. Concrete fix: define the committed profile source now, e.g. `profiles.json`, plus one `apply_profile(workflow, profile, schemas)` helper. The generator, UI launch path, `queue_smoke.py`, `scripts/otr_api.py` callers, and soak runners must all call that same helper before submission/export. Request-specific smoke overrides like `target_words=30` can remain separate, but production defaults/captions/credits/model switches must come only from the profile.

3. [Reconciliation + grounded cross-platform audit / No MPS path yet] The plan names Mac/MPS as a representative profile but also admits there is zero MPS routing. Grounding confirms no `torch.backends.mps` references and no centralized device-routing module. As written, a “Mac-MPS” shipped tier is false advertising and likely CPU-only. Concrete fix: either remove/rename the Mac profile to `mac_cpu_floor` for this build, or implement a centralized device selector with CUDA/MPS/CPU routing and add a Mac/MPS smoke gate. Do not list `Mac-MPS` in generated profiles until that exists.

4. [Strawman shape / Working recommendation] The profile schema is unspecified, but it is the new source of truth. Without exact keys, precedence, and validation rules, build/export/headless will diverge immediately. Concrete fix: commit a minimal schema before implementation: profile id, hardware tier, device backend, min/max VRAM, env flags (`OTR_ENABLE_*`, `OTR_FORCE_ENGINE_MAP` if used), Director widget values, non-Director node widget values, model manifest id, sidecar/venv requirement, seed policy, and fail-closed fallback policy. Add a validator that rejects unknown profile keys and rejects profiles that enable engines requiring unavailable toolchains.

5. [Strawman shape / Why one architecture with switches / current `otr_scifi_16gb_full.json`] The plan treats Director widgets as the canonical switch mechanism, but the current workflow contains other engine-looking saved selectors not covered by the plan. Concrete examples from the JSON: node 81 has `"indextts2"`, node 82 has `"kokoro"`, node 83 has `"stable_audio_3"`, and node 92 `OTR_VideoRenderBatch` has `"humo"` in its widget vector. [ASSUMPTION] The node 92 value is an engine/default selector; verify its schema name. Concrete fix: either include these nodes in the profile application map or remove/derive the duplicate selectors so Director/profile is truly canonical. Otherwise a generated “lite” profile can still save heavy audio/video defaults and drift from the Director policy.

6. [Reconciliation / generated per-tier snapshot JSONs] The generator contract is not specified tightly enough to preserve the `widgets_values` invariant. Grounding shows the converter/validator hard-fails when `widgets_values` length differs from serialized slot count, including hidden `control_after_generate` and `forceInput=True` behavior. Concrete fix: implement the generator using live `/object_info` schemas and `patch_widget_by_name`, then run `workflow_to_api_prompt` and `OTR_WorkflowValidator`/CI validation on every generated JSON. The build must fail on any COMBO value not present in the live schema unless explicitly admitted by existing code.

7. [Reconciliation / generated per-tier snapshot JSONs] The generator’s environment is a hidden dependency. `OTR_VideoDirector.INPUT_TYPES()` builds COMBO choices from the live registries; generated JSONs with saved COMBO values must be valid against the environment that loads them. Concrete fix: define whether snapshots are generated in a “max registry” build env or per-target env, and add a load/validate test for each target profile against the intended ComfyUI environment. If disabled engines remain in dropdowns but fail at execute time, document and test that exact behavior; if they disappear from choices, the generator must not save them.

8. [Hard parts #3 / Decision criteria / Working recommendation] VRAM safety is asserted but not made profile-validatable. Grounding confirms `VRAM_CEILING_MB = 14500` and single-heavy video residency in `wrapper_bridge.py`, but the plan adds profile-level model choices and sidecars without a manifest of VRAM requirements or mutual exclusions. Concrete fix: add per-engine/profile capability metadata: min VRAM, allowed device backends, sidecar requirement, and “heavy residency class.” Profile validation must reject 8GB profiles selecting HuMo 14B or multiple co-resident heavy engines before ComfyUI launch.

9. [Model-asset acquisition] The installer section says “per-profile model MANIFEST” but does not define the manifest. Grounding says only piecemeal downloaders exist today. Concrete fix: commit a manifest format with exact Hugging Face repo/path/revision, expected size, checksum or etag policy, destination resolver, license/acceptance flag if needed, and skip-existing behavior. The profile must reference manifest entries; no downloader should infer models from widget strings.

10. [Hard parts #4 / Platform invariants] Determinism is a requirement but the plan does not define how profiles set seed inputs or env overrides. Grounding says creative RNGs draw OS entropy unless `OTR_CAST_SEED` / `OTR_STYLE_SEED` are set, while current Director nodes save `request_seed=42` with `seed_mode=request_hash`. Concrete fix: add seed policy to the profile schema and test determinism per shipped profile by running the same smoke twice with fixed profile/env and comparing the expected deterministic artifacts/ledger fields.

SHOULD-FIX:
1. [Problem / Reconciliation] The document still says today there are multiple workflow JSONs causing drift, then later admits the real drift is saved production JSON defaults vs headless patch lists. Grounding confirms there is only one production workflow and other workflow files are fixtures/external examples. Concrete fix: rewrite the problem statement to match the real mechanism; otherwise implementers may solve the wrong problem by only adding generated JSONs.

2. [Reconciliation] “Runtime patching reintroduces a second source of truth” is too broad and conflicts with the required headless path. The existing robust path patches by widget name using live schemas; the defect is ad-hoc per-script patch lists, not patching itself. Concrete fix: distinguish “profile-driven patching from committed profile source” from “ad-hoc script patching.” The former is required for headless and generation.

3. [Open questions] The document still contains unresolved blocking questions after giving a “Working recommendation.” Concrete fix: close Open questions #1, #2, #4, and #5 into explicit decisions before coding. In particular, define the one allowed exception policy for `character_3d` or defer it entirely.

4. [Strawman shape / Toolchain differences] “Same graph every time; only the profile changes” depends on all custom node classes being importable on every target. [ASSUMPTION] OTR nodes are importable without the CUDA sidecars, but this must be verified for all adapters. Concrete fix: add a cold-import/load test for CPU/Mac environments with CUDA sidecars absent. If any node hard-imports CUDA-only packages at module scope, it must be lazy-imported or isolated.

5. [Model-asset acquisition] Respecting `HF_HOME` and `extra_model_paths.yaml` is necessary but not enough for shared model stores. Concrete fix: define precedence when both exist, what happens when a manifest entry already exists in one but the profile wants another path, and how failed/resumed downloads are recorded.

6. [Generated snapshots] Snapshot naming is inconsistent with the stated hardware tiers: examples include `otr_24gb_mac.json` while the problem frames 8GB vs 16GB NVIDIA, Apple/MPS, CPU. Concrete fix: define the exact shipped profile set and names once; use those names in tests, docs, installer, and generator output.

7. [ffmpeg audit] The optional `h264_videotoolbox` fast path should not be coupled to the core switchable workflow work. Concrete fix: track it as a separate performance issue; keep libx264 as the required Mac-safe baseline.

8. [Decision criteria / test matrix] “Representative profiles” is correct, but the pass/fail gates are underspecified. Concrete fix: for each shipped profile require: UI JSON validator pass, API prompt conversion pass, model manifest dry-run, cold import, one short render/queue smoke, and deterministic repeat where seeds are fixed.

OPTIONAL / NICE-TO-HAVE:
- Add a profile diff report in the generator output showing every widget/env/model changed from master.
- Add a generated markdown table from `profiles.json` for user-facing docs instead of maintaining profile docs by hand.
- Add Mac hardware encode via `h264_videotoolbox` later; libx264 already satisfies correctness.
- Add a “profile provenance” string into render reports/ledger restamps so outputs record the exact profile id and hash.

CUT THESE (over-engineering):
1. [Decision B / Strawman shape] Cut Gradio/Streamlit for this build. The required deliverable is a committed profile source plus generator/headless integration; a second UI front-end does not reduce drift and adds packaging/test surface.

2. [Strawman shape / 2D-vs-3D] Cut “2D-vs-3D becomes just another capability switch” from the first implementation unless a concrete `character_3d` node/edge difference is proven absent. 3D has sidecar/toolchain risk and can be a later profile family or explicit exception.

3. [Open questions #3] Cut any attempt at full cartesian switch testing. Keep the fixed profile gates only; the document already identifies cartesian explosion.

4. [Model-asset acquisition] Cut custom download UX beyond “detect path, show size, confirm” for the first build. The must-have is a manifest with resumable `huggingface_hub` downloads and skip-existing behavior; richer wizard flows can wait.