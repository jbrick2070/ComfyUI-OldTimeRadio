# Grounded review judgment

Root owns implementation and judgment. This is a scoped follow-up to the completed
My Story repair/EOS campaigns, not a new full four-round campaign. Independent
local agents traced P0, scope binding, voices and scene pixels before code; root
verified their claims against the real Windows files and retained live artifacts.

## Actual external reviews

- Initial Sonnet/API latest: no review returned. The harness exhausted its bounded
  retries with empty content/length. Its printed zero spend means missing usage,
  not proof the failed requests were free. Retained under sonnet_qa/.
- Sonnet5 finished-diff retry: actual complete review, no demonstrated must-fix.
  Response and usage retained under sonnet_retry/; cost $0.072408.
- Opus/API latest resolved Opus5: partial review ending mid-issue3c. Treated as
  usable findings, not completed consensus. Usage/cost in opus_consensus/manifest.
- Final Sonnet5 delta: complete review, no demonstrated must-fix. Full missing
  fallback and structured-return excerpts supplied. Cost $0.034018.
- Final Opus5 delta: complete review, approves the bounded repair. Cost $0.115020.
- Cursor first read-only local attempt: cursor-grok-4.6-high, ask mode. Timed out
  after300s without a review; its child process retained the output pipe and was
  selectively stopped after the deadline. This is a failed reviewer, not assent.
  A smaller no-profile final packet also timed out after240s without a review;
  its retained child was selectively stopped after that deadline. No further
  repeated call was made. No production files were delegated to Cursor.

The final follow-up has Opus/Sonnet agreement after grounding. Cursor has NOT
provided consensus on this follow-up. Its earlier EOS review is genuine but
does not review these new fixes; do not reuse that approval. Both failed local
attempts remain under kibitz-runs/2026-09-11-my-story-pairlock-{followup,final}/r4/.

## Findings grounded by root

| Claim | Disposition |
|---|---|
| A truthy n/a row age can hide known treatment age. | Accepted boundary case. Normalize blank/n/a before choosing; preserve a known row age and emit absence for two unknowns. Added direct controls. Actual published rows had no age_band, so this was not the measured original pixel cause. |
| A new presentation_gender or route/warning stamp is a regression. | Rejected: reporting the reference actually selected is the missing provenance fix. Source gender stays unknown. The existing fallback already uses these stamps. |
| Unspecified and unservable gender should have distinct receipts. | Accepted. gender_unspecified is separate from gender_unservable; comments/report now describe the actual route. No casting/identity change beyond the already reviewed fix. |
| Captured accepted model can be absent on structured success. | Rejected: all six actual success exits validate with the supplied post-validator, including prebuilt typed repair. No successful cache, zero-attempt or model_construct bypass exists. Tests exercise validator mutation and failed-attempt isolation. |
| Every future list must have an identity registration gate. | Rejected: current P2 dialogue requires both fields and has no stable ID; P3 lists are strings. The code deliberately avoids positional inheritance. Hypothetical schema renaming does not justify a new story rejection. |
| Aliased reconstruction fails current models. | Rejected: CastMember is the only current aliased model and explicitly supports populate_by_name; omission/replacement alias tests execute it. |
| Sonnet could not see fallback stamping; final reviewers could not see _stamp_row. | Evidence limitation resolved by root: _stamp_row calls self._stamp and tracks the same row; public tests execute actual disk/singleton/wire/reference/credits equality. No alias/signature mismatch. |

Optional speculative legacy gender sentinel/provider-voice residue observations
were not promoted to production bugs or used to widen this repair. Existing
source qualification remains open, and the review does not prove model obedience.
Final code snapshot: snapshot_final.json. Initial and final offline comparisons
and exact canonical audit are retained with the qualification receipt.
