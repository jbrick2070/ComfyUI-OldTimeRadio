# F1 source correction: author and cleanup owners

Base: 7e4a0dd6738077d2c580304cb28dae3b6af5d1a8. This component integrates source
correction. F2 visual integration remains in GO_FORWARD; the operator's later
checker+rewriter amendment supersedes the separate recursive source organizer.
No live model, GPU, media or source-fidelity qualification is claimed.

The operator amended the report-only reviewer design: checking must return
actual corrections, and retries cannot continue into a fourth/fifth rewrite
round. Each new source operation owns one structured call ladder with at most
two calls, including syntax/schema repair. It never rechecks its own rewrite
or restarts its budget on a new revision. Its pass id is episode-local and
re-entry spends no further calls. Existing author structural admission and
spoken-hygiene budgets remain separate and visible in their own receipts.

Every P0/P1/P2/P3 author prompt receives the exact four raw fields. Working
interpretation never replaces that source. Once the original author ladder
returns a usable artifact, a combined source check/rewrite uses that owner's
slot and original artifact schema. P0 stays technical; treatment, acts and
frame stay creative. The existing structural validator checks the corrected
model and that exact model is retained, including canonical speaker repairs.
Full artifacts permit missing list members to be restored. Complete failed
responses, including endings beyond 400 characters, remain in typed repair.

The new source operation has no report-only verdict. It returns usable
replacement material or keeps the accepted original after the fixed budget.
True provider/OOM/cancellation failures keep their type and attempt history.
Known actual no-fit spends no generation; unknown capacity cannot become a
measured refusal or semantic PASS. Each operation records raw field identity,
delivered scope, actual calls/fit, raw output separately from failed completion,
configured model separately from unknown executed identity, and application.

The existing row cleaner now sees raw source in its context. Its final source
correction belongs to ledger_clean, inside the existing two-stage transaction.
It returns actual replacement text bound to exact source/original quotes and
original coordinates. Overlaps, ambiguous occurrences, unknown/protected rows
and empty spoken rows are unusable; they consume the same two-call budget.
Python interleaves model-authored replacements with untouched original bytes
and uses the shared text-metrics setter. No extra source reviewer runs afterward.

The writer tail preserves attempt history if later cleanup fails or cancels.
After reconciliation/rollback it reattaches the journal to current metadata and
records actual retained speech. Its ordered projection includes speaker identity
and only character/announcer speech. Final delivery gets a separate actual hash.
Rollback never restarts correction. Each spoken operation additionally records
whether its actual editable subset was retained and the cleanup outcome, so
attempted application cannot be mistaken for retained text. No additional
semantic publication gate is introduced.

Independent Einstein review verified budget/callable/application paths and
identified the subsequent-cleanup persistence gap; the root correction covers
that boundary. A real-Ledger rollback test exposed music rows in the speech
projection; that projection now matches actual character/announcer delivery.

Final validation: 236 focused tests passed in 8.67 seconds. Full regression:
14,283 passed, 51 existing failures, 183 skipped, one xfailed. No new failed
test IDs or changed failure meaning versus the C1 baseline. One AMD dependency
assertion prints the same two dictionary differences in the opposite order.
The initial full run found one new text-metrics guard failure and an expanded
existing slot-tag failure; both were corrected and are absent from this final
comparison. Initial and final comparison JSONs are preserved beside this receipt.

Two actual Claude Code Sonnet/high finished-diff QA calls completed, including
the operator-requested repeat after fixes. Both found no blocking defect. Root
implemented the first review's two SHOULD findings and grounded the final
follow-up's nonblocking comment/dedup observations; judgments record why they
need no further edit. Final six code/test hashes match the reviewed and tested
snapshot at kibitz-runs/2026-09-11-my-story-f1-final-qa/r4/snapshot.json.
No exact resolved Sonnet version or per-call dollar spend was returned.
Independent Einstein follow-up also found no concrete defect.
Bible candidate: 31 passed/10 existing failures/11 skipped/3 xfailed. With the
same updated Bible check against the clean pre-change checkout: 30/11/11/3.
The one resolved failure requires this component's source-correction coverage;
no new or changed Bible failure remains. Bible still has 341 entries and 146
unchanged legacy metadata issues. Rule 11.39 records the existing production
My Story occurrence and its executable correction coverage.

The actual canonical validator and JSON round trip pass: 23 nodes, 63 links.
Canonical SHA256 remains
d586a286aaee4c039e410ae9a10014c5c7f4ab82d00eac0e9e1cc0564415057c.
No node interface, widget or workflow link changed. All live machine work waits
until the whole included coding campaign is finished. Then 5080 and RunPod are
authorized; Mac/4060 wait for the operator tomorrow.
