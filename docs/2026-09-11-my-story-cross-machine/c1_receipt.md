# C1 shared cleanup conservation

Base: 440573962d53521e8be9af875cb7c0ca8a772f32. No live model, GPU or media
qualification is claimed. All machine runs and hardware handoff preparation
remain paused until every included campaign coding item is complete.

The shared cleaner preserves immutable original text outside exact complaint
spans. Raw regex offsets remain separate from normalized/truncated display hints.
Model quotes require exact unique occurrence or bounded localization. Overlapping
spans merge; disjoint and adjacent spans preserve their own replacement identity.
Collective coverage of all speech requires dedicated model authorization, even
if separate accusations each looked partial. That authorization can preserve a
valid coda, narrow to a real local direction, or permit whole-row conversion.

Partial repairs return a typed list with exactly one model-written replacement
per approved ID. Python interleaves those with untouched original slices. Every
candidate is reconstructed again before either clean or progressive acceptance.
Retry complaints cannot widen original scope; reread context shows the candidate
and repair context retains the original. No blanket whitespace normalization or
2000-character limit remains. Protected Python-owned fact rows retain their
existing skip-before-judge contract.

A malformed reread cannot count as cleanliness or progress. The existing repair
budget can try again; unresolved edits retain the original with a flag. A prior
actually judged improved candidate remains eligible for flagged fallback. Real
provider, memory, cancellation and terminal-capacity failures retain their type.
Receipt version ledger_clean_v3 records original/candidate hashes, original
scope, replacement IDs, authorization attempts and rejection reasons. This is
scope conservation, not a claim of source semantic fidelity; F1/F2 remain ahead.

Review roster: internal read-only Einstein audit found three scope/localization
gaps; root fixed them and Einstein verified closure against the recorded hashes.
Requested Claude Code Sonnet/high QA read the complete current code and found
the malformed-reread acceptance gap. Root grounded and fixed it. A focused
Sonnet follow-up confirmed closure with no new must-fix; root grounded the
confirmation. Two actual CLI calls total. Exact resolved Sonnet version and dollar
cost are unavailable from this CLI; no exact version or zero-cost claim is made.
The architecture R1-R4 arc is not repeated or claimed for these scoped QA calls.

Validation complete:

- Final focused: 183 passed in seven relevant regression modules.
- Initial full: 14,241 passed / 51 baseline failed IDs / 183 skipped / 1 xfailed.
  Failure-detail comparison caught an added missing LLM-slot comment in the
  existing call-tag audit; corrected and the targeted audit returned exactly
  to the two baseline untagged calls.
- Final full: 14,247 passed / 51 unchanged baseline failures / 183 skipped /
  1 xfailed. No new failed IDs or changed failure assertions. One AMD dictionary
  comparison differs only in display order. This rerun includes the label
  correction and all six additional reread regressions.
- Final controlled-candidate Bible: 31 passed / 10 unchanged failures /
  11 skipped / 3 xfailed. New 11.64 executable guard fails on the clean pre-C1
  baseline (30 passed / 11 failures), and passes against final C1 helpers.
  Existing failure assertions match; five fixture path previews differ only
  through worktree-name truncation. 341 entries; 146 unchanged metadata issues.
- Actual canonical validator, JSON round-trip and link/widget audit pass.
  No node, widget, socket or wiring change: 23 nodes / 63 links / 37 writer
  widgets, SHA256 d586a286aaee4c039e410ae9a10014c5c7f4ab82d00eac0e9e1cc0564415057c.

Final source/test file hashes are c1_snapshot.json; the initial tested source is
retained separately as c1_initial_snapshot.json. JUnit comparisons are adjacent
c1_*_comparison.json; raw local JUnit/logs live under tmp/my_story_c1_*.
Production evidence is PBUG-20260829-14's 4060 My Story coda addendum, promoted
as Bible 11.64. Fresh canonical qualification remains open and on hold.

Bible code/entry/README are committed and pushed together as
3acc06d668b7599864834c45880b5de6cdcb73fc; HEAD equals origin/main.
