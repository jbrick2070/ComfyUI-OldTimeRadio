<!-- requested_model: deepseek/deepseek-v4-pro | resolved_model: deepseek/deepseek-v4-pro-20260423 -->

VERDICT: no. The plan is a diagnostic / options discussion, not a build-ready spec. It omits concrete code changes, how the mux receives the needed tail length, where scopes padding occurs, and how the exact equality assert handles floating-point rounding.

MUST-FIX BEFORE BUILD:
1. [Candidate Fix A / Question 2] The proposed mux gate relaxation says "assert v - a == round(hud_frames/fps) exactly" but never defines how `hud_frames` reaches the mux node. The mux (`OTRMasterAudioMux`) receives only `silent_video_path` and `master_audio_path`; there is no channel for the intended tail length. Concrete fix: either (a) add an optional `credits_tail_frames` parameter to `mux_master_audio` and wire it from the composite / floor stage, or (b) drop the exact equality in favour of a generous upper bound (e.g., `v_dur <= a_dur + CREDITS_MAX_SECONDS + tol`) and assert it, which still blocks massive drift. The equal-exact approach also fails‑closed on tiny probe rounding errors (0.001 s); must use a tolerance (≤1/fps) even if equality is desired. Without this, the mux cannot distinguish intentional post‑roll from real drift.

2. [Candidate Fix A / Question 3] The plan says "extend the composite … to the floor length (~65.7 s)" but does not give the exact change in `otr_silent_composite.py`’s `assemble_silent_timeline`. Currently that function caps `target_total` to the master mix duration (the sibling `*_master.wav` probe). To use the floor length, the logic must be changed to pull the **video** frame count from `base_video_path` (`count_video_frames`) instead of the audio probe, or to set `target_total_frames = floor_frames` unconditionally when the floor is available. Example: after the `master_dur` block, the code could read `floor_total = count_video_frames(base_video_path)` and `target_total = max(target_total or 0, floor_total)`. Provide this concrete diff; otherwise the composite will continue to cut the scroll at ~45.7 s.

3. [Candidate Fix A / Question 4] The §4D blend (`otr_post_upscale_procgen_blend.py`) uses `shortest=1` among composite, floor, and scopes. When the composite is lengthened to ~65.7 s, the scopes input will be shorter and the blend will truncate the final video. The plan says "pad the scopes input to the floor length" but gives no details. Must specify where and how the scopes video is padded (e.g., loop its last frame or pad with black) to match the floor/composite length. This change belongs in the blend node itself (or a pre‑pad step) and must be tracked in the workflow wiring.

4. [Gap / Assumption] The plan assumes the floor video’s full length (65.72 s) includes the opening, drama, and credits scroll in the correct order so that tail‑filling from the floor end yields the credits scroll. Verify that the composite’s sequential segment planner (`plan_timeline_segments`) with `target_total_frames` set to the floor length will *start* the tail gap at the correct cursor position (after the beats) so that the tail slice is taken from the very end of the floor, not from an offset that misses the credits. This may require passing an explicit `floor_frames` so that the tail start aligns. The current `_floor_aligned` logic clamps `src_start` to `ff - n` which should work if `ff` is the full floor frame count and `n` is the tail gap length; confirm with a test.

SHOULD-FIX:
1. [Candidate Fix A / Question 2] The exact equality `v - a == round(hud_frames/fps)` is fragile and over‑constrains. A simpler, equally safe guard is `v_dur ≤ a_dur + max_credits_tail_seconds` where `max_credits_tail_seconds` is the known credit scroll duration plus a small buffer (e.g., 1 s). This still catches a composite that accidentally goes 30 s over, but avoids a brittle exact match that will break on any minor duration probe variation. Consider adopting this bound.

2. [§4D blend] If the scopes video is padded by looping its last frame, the lighten‑blend of a static scopes frame over the scrolling credits may look acceptable but should be validated visually with the 6/5 reference. The plan does not mention this visual check; add a manual acceptance gate.

OPTIONAL / NICE-TO-HAVE:
- Confirm that the floor’s background loop under the credits (the 6/5 behaviour) is exactly what the floor video contains in its final 20 s; no extra work needed if it already includes the loop. A quick probe of the floor’s last 20 s would verify.
- Document the new `max_tail` / `hud_frames` parameter in the workflow JSON schema so that re‑validation covers it.

CUT THESE (over‑engineering):
- The requirement to assert `v - a == round(hud_frames/fps)` exactly and surface `hud_frames` as a new parameter can be cut if replaced with the bound approach above. The bound achieves the same drift‑detection objective with less coupling and no new inter‑node data flow, and it is safe because any video‑longer‑than‑audio tail in the final mux is now intentional by construction.

[ASSUMPTION] The blend node currently strips audio (or the composite already enforces silence). If the blend node re‑introduces audio (e.g., from the floor’s silent audio stream), the mux’s `-c:a copy` would replace it with the master audio, but the video may carry an unintended silent audio track. This is harmless but should be confirmed; if not, add `-an` to the blend node.