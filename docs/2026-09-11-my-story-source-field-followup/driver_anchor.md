# Spoken source coordinates: finish the existing contract

Root is sole coder and judge. This is a narrow conformance correction: the set
of edits accepted by the current application validator does not change. No new
architecture, node, widget, workflow wire, retry, source gate or output limit.
Finished-code Sonnet QA and independent local review precede push/live testing.

Full canonical pairlock05 on18569a3b (production d2837e45) published Dinner
Memories after10:29 with Mistral Nemo/NF4. It failed source qualification:
girlfriend mention omitted from dialogue, appreciative present-day ending left
in treatment, and one of four character images added a child. Credits now match
actual IndexTTS2 references. Complete ledger/history/logs/pixels are preserved.
The current server is stopped; no new GPU run during coding.

One directly observed avoidable repair failure: final spoken source correction
returned source_field="text" and quoted candidate text as source evidence. The
existing validator rejected it; the sole remaining repair returned edits:[],
leaving omissions. This does not prove invalid-field prevention alone restores
the girlfriend or fixes the visual model. Model judgments remain fallible.

Current source authority is _otr_story_input.CREATIVE_FIELDS, four exact strings:
idea, characters, plot, setting. _raw_values retains only these; the apply path
requires a nonempty exact quote from raw.get(source_field, ""). Therefore every
other field already fails. SpokenSourceEdit.source_field is nevertheless an
unbounded StrictStr in the bound native grammar. This admits an alias which the
same owner can never apply. source_quote and original_quote also lack clear
coordinate guidance in the actual spoken instruction.

Plan: define a Literal alias at the existing input authority and derive the same
ordered CREATIVE_FIELDS tuple with get_args (Python>=3.10 compatible). Use the
alias for SpokenSourceEdit.source_field. State in the existing spoken instruction
that source_quote comes from source[source_field], original_quote from the named
draft line, and optional offsets are zero-based/end-exclusive Python character
positions in that candidate line. Do not expand the shared schema renderer:
Field descriptions alone do not reach its compact model prompt.

Preserve exact source/quote/interval validation, speaker/id/order conservation,
native binder ownership, two total correction calls, empty edits and usable-text
retention. Do not add a semantic verifier, verdict-only call, forced obligation
checklist, mandatory girlfriend substring, or new publication refusal.

Independent read-only wiring review agrees the accepted edit set is unchanged
and the invalid alias becomes unavailable in native decoding. Verification:
actual LMFE excludes text while admitting all four source keys; the public
correction applies a source-grounded replacement, conserves unrelated bytes and
keeps malformed/repair inside two calls. Full regression and controlled Bible
comparison must retain inherited failures, not quarantine them. Canonical stays
unchanged and is revalidated. Extend existing source-grounding Bible11.39 and
PBUG20260910-03; do not create a duplicate production ID from a test fixture.
