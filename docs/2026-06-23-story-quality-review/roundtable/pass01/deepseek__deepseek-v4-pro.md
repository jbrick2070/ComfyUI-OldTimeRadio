<!-- requested_model: deepseek/deepseek-v4-pro | resolved_model: deepseek/deepseek-v4-pro-20260423 -->

VERDICT: no (not build-ready as-is). The plan correctly identifies thatsoft prompts are ignored, but the proposed deterministic levers overstate their likely effect on structural sameness. L2 claims to enforce dramatic function but only adds empty beat slots — a weak model will still fill them with generic standoff noise. L1’s denylist + palette may shift vocabulary without changing the underlying threat-loop pattern. These gaps mean the core goal (“genuinely BETTER STORIES”) is not reliably addressed by the design as written.

MUST-FIX BEFORE BUILD:
1. [L2] The plan says “phase enforces a FUNCTION, not just a budget” but then only adds two required beat SLOTS with no mechanism to guarantee their content fulfills that function. A weak model can output a “personal-stake” slot that is still a threat exchange. Fix: for each required slot, add a deterministic structural constraint in the beat skeleton (e.g., the personal-stake beat must include a character‑specific private cost or fear, injected from the brief’s character sheet; the climax beat must include a sensory consequence and a choice that alters the situation, enforced by a post‑outline regex check for absence of those markers, triggering a fallback template beat). Without this, L2 is just a budget reshuffle, not a functional upgrade.
2. [L1] The denylist + premise palette are presented as sufficient to break the standoff pattern, but the plan does not address the risk that the model will simply use other generic conflict verbs (e.g., “emergency protocol”, “override” → “manual override” is banned, but “emergency override” may not be). The palette is a list of objects, not a conflict structure. Fix: augment the palette with a required conflict type per beat derived from the premise (e.g., for a classroom brief, the beat intent must involve a student‑teacher power asymmetry, not just a “lesson plan” object). Additionally, define a fallback if the regeneration still exceeds the crisis‑noun cap (e.g., after one retry, force a deterministic beat from a small hand‑authored set keyed to arc phase). Otherwise the “regenerate once” may loop silently or produce another standoff.
3. [L1/L2 interaction] The plan assumes that fixing the beat planner (L1) and adding slots (L2) will together produce varied, rising action. But the composer prompt (L3) still receives the same weak model and may flatten any distinct beat intents into interchangeable threat dialogue. The plan does not connect L1/L2 to a composer‑side constraint that preserves the beat’s intended dramatic function. Fix: add a beat‑intent tag to each beat that the composer must respect (e.g., if the beat intent is “personal_cost”, the composer must not output a line that is purely external threat). This can be a deterministic filter: if the generated line contains no private‑stake language, regenerate that line with a stronger constraint (still within the no‑critic‑gate rule, as it’s a line‑level regeneration keyed to a beat tag, not a full QA reroll). Without this, the upstream improvements may be lost in the composer.

SHOULD-FIX:
4. [L5] The claim that gemma‑12b is the best writer rests on only 3 episodes from one soak. This is a thin evidence base for a default change that affects all future output. Should: run a small controlled comparison (e.g., 5 episodes each of gemma‑12b vs. the current default on a fixed set of briefs) and confirm the advantage before making it the default. The critic‑bug fix is independent and can proceed, but the default change should be gated on that validation.
5. [L1 regeneration] The plan says “regenerate THAT beat's intent once with the denylist reinforced” but does not specify what happens if the retry also fails (exceeds cap or is empty). Should: define a maximum retry count (2) and a deterministic fallback beat (a simple, brief‑specific action sentence) to avoid an infinite loop or a dropped beat.
6. [L4] The transcript sanitizer is described as fixing prompt‑leak tokens, but the plan notes a mojibake instance was a packet‑build artifact. Should: verify that the real ledger contains such leaks before implementing; if not, L4 can be reduced to a lightweight check rather than a full sanitizer, avoiding regex maintenance burden.

OPTIONAL / NICE-TO-HAVE:
- L6 (best‑of‑N line selection) is correctly positioned as secondary; it can be evaluated only after L1/L2 prove effective. No change needed.
- The plan could benefit from a brief mention of how the announcer outro template will extract the on‑stage choice from the climax beat (e.g., a simple slot‑filling rule). Currently it’s implied but not designed.

CUT THESE (scope / over‑engineering):
1. [L3] Action/dialogue split at the composer. While it removes narrated‑action noise, that noise is a symptom of weak model output, not a cause of poor story arcs. Fixing the beat structure (L1/L2) should reduce the need for this split. It adds a significant output‑contract change and a flag‑gated re‑baseline, which is high cost for a prose‑level polish. Safe to cut from the initial build; if needed later, it can be reintroduced.
2. [L4] Full deterministic transcript sanitizer. If the prompt‑leak problem is not confirmed in the real ledger, this is premature. A minimal check for unbalanced quotes can be part of the freeze step without a separate subsystem. Cut the dedicated sanitizer until evidence demands it.

[ASSUMPTION] The plan assumes that the critic `too_many_edits` abort is a bug that hides good gemma outputs. Verify: inspect the critic harness code to confirm that `freeze=too_many_edits` leads to ungraded arcs and that gemma outputs disproportionately trigger it.
[ASSUMPTION] The plan assumes that the denylist words are the primary cause of sameness. The soak data may show that even episodes without those words still exhibit the same standoff structure. Verify: re‑examine the soak episodes for structural repetition beyond vocabulary.
[ASSUMPTION] The plan assumes that the operator’s “no new flag‑and‑reroll gate” constraint permits a deterministic beat‑intent regeneration with a denylist check. This is a grey area; confirm with the operator that a non‑critic, outline‑time retry is acceptable.