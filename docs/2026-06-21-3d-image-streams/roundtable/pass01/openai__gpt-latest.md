<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. The document is still a problem statement with unresolved open questions, and several core assumptions conflict with the provided render path.

MUST-FIX BEFORE BUILD:
1. [The design / Open questions: Where does the fork live?] The proposed capability gate is not implementable as written from the shown engine metadata. `MeshStageEngine` exposes `family = "image_to_video"`, `required_inputs = ("init_image",)`, `uses_still = True`, roles, and `fallback_engine`, but no shown `requires_mesh_portrait`, `character_3d`, or generic mesh capability field. Gating on `family == image_to_video` would also catch `wan_i2v` and other non-mesh engines. Concrete fix: add or identify a real registry-visible capability such as `requires_mesh_fodder = True` / `mesh_source_kind = "mesh_fodder"` on mesh-capable engines, and make the image-target derivation and request builder use that field. Do not infer mesh need from family or engine name.

2. [Problem] The stated current dataflow “portrait -> mesh_stage init_image” conflicts with the provided `render_driver.py` path. `build_request_from_shot()` sets `_family = engine_family(...)`; for `_family in _SCENE_INIT_FAMILIES`, and `_SCENE_INIT_FAMILIES = {"image_to_video", "static_motion"}`, it replaces `init_image` with the beat scene still. Since `mesh_stage` is registered as `family = "image_to_video"`, real episode requests will use the scene still when present, not the portrait. Concrete fix: before the generic `_SCENE_INIT_FAMILIES` branch, handle the new mesh capability and set `init_image` to the resolved `mesh_fodder` asset; exclude mesh-capable engines from the generic scene-still branch.

3. [Open questions: Where does the fork live? / Invariants] Engine override sequencing is wrong for this design. `run_real_episode()` calls `apply_engine_override(ledger)` inside render time, after the image phase has already produced assets. If `OTR_FORCE_ENGINE_MAP` forces a shot to `mesh_stage`, the image stage will not have forked mesh fodder/background plate for that final selected engine. Concrete fix: apply any operator engine override before image target derivation, or add a pre-render validation that fails loud when a mesh-capable final engine lacks `mesh_fodder` and `background_plate`.

4. [Open questions: Subject selection] Subject selection is unresolved but required for correctness. `MeshStageEngine._request_character_id()` only knows `conditioning_refs.char_id`, top-level `char_id`, or `"uncast"`. Announcer/music roles are explicitly supported by `MeshStageEngine.roles`, but may have no character. Without a defined `mesh_subject_id` / object path, those beats will either mesh an environment still or all collapse to `"uncast"` identity. Concrete fix: define deterministic selection order, e.g. `shot.mesh_subject_id` > `shot.char_id` > line `char_id` > explicit story-object id > no-route-to-mesh. For no subject, route to a non-mesh engine before image generation/render, not generic `"uncast"`.

5. [Open questions: Ledger images taxonomy] Adding `mesh_fodder` rows without changing indexes will poison existing consumers. `_portrait_index()` indexes any image row with `object_id` or `char_id`, regardless of `kind`, and uses `setdefault`, so the first row for a character wins. A `mesh_fodder` row for the same `object_id` can be returned as the HuMo/cinematic portrait, or the cinematic portrait can hide the fodder. Concrete fix: keep existing portrait rows filtered by their existing portrait kind [ASSUMPTION: verify actual portrait kind values], add a separate `_mesh_fodder_index()` filtered on `kind == "mesh_fodder"`, and update request building to use the correct index per engine capability.

6. [Open questions: Ledger images taxonomy / The design: BACKGROUND PLATE] The current background plate lookup cannot satisfy “NO subject” plates. `build_clip_manifest()` stamps `bg_still_path` for mesh directory clips using `_still_index(led).get(bid)`, and `_still_index()` only reads rows whose `kind` starts with `scene_`. Existing `scene_character` stills may include the subject, exactly what the design forbids. Concrete fix: add a distinct `background_plate` index keyed by beat/still_pool_key and use that for mesh composites; do not reuse `scene_*` stills unless they are explicitly subject-free.

7. [Invariants: No hardcoded engine-name routing] The provided code already violates the proposed rule for the mesh background manifest: `build_clip_manifest()` uses `if exists and ctype == "directory" and str(eid) == "mesh_stage":`. If this design requires capability-based routing, this must be refactored. Concrete fix: resolve the engine from the registry and test the new mesh/background capability; only fall back to name checks in compatibility code with a loud deprecation path.

8. [The design: subject opaque / BACKGROUND PLATE] The plan does not specify the actual composite change. Grounding only shows manifest stamping of `bg_still_path`; the composite implementation is not provided. The design says “not ghosted” and mentions `_silent_procgen_blended_final`, but there is no concrete source-over alpha rule, opacity value, or compatibility behavior. Concrete fix: specify and implement normal RGBA source-over composition for mesh directory clips over `background_plate`, with opacity fixed at 1.0 and no blend/ghost mode. Verify: composite code path that consumes `bg_still_path`.

9. [Open questions: Mesh cache] The cache identity is still character/portrait-shaped. `mesh_cache_key(character_id, portrait_sha256, ...)` and `build_mesh_manifest()` fields are `character_id` and `source_portrait_sha256`. That does not model story objects, subject kind, or multiple meshable artifacts. Concrete fix: either generalize to `subject_id`, `subject_kind`, `source_image_sha256`, `source_image_kind`, or deliberately pass object ids through the existing `character_id` slot with documented prefixes like `obj:<id>` and update manifest naming. Without this, object meshes are ambiguous and observability will be misleading.

10. [Open questions: Mesh cache] “Per-character fodder so a stable cast reuses its mesh across beats/episodes” is not guaranteed. The current key hashes the actual source file bytes. If mesh-fodder images are generated per beat, even for the same character, every beat gets a new GLB. Concrete fix: define a canonical per-subject mesh-fodder asset and reuse that row across beats; only generate per-beat fodder when the subject itself changes. Cache keys must bind that canonical fodder content hash.

11. [Open questions: Aspect] The aspect contract is unresolved and will break if the same canvas/aspect is reused for both streams. `build_request_from_shot()` sets non-face engines, including `mesh_stage` as `image_to_video`, to landscape canvas `1472x832`; `MeshStageEngine` also defaults to landscape if unset. Mesh fodder wants near-square/portrait, while the plate wants 16:9. Concrete fix: image-generation targets need independent per-kind dimensions/aspect: `mesh_fodder` near-square/portrait, `background_plate` 16:9, video render canvas landscape. Do not derive fodder dimensions from `VideoRequest.canvas`.

12. [Open questions: Prompt templates] The required prompt templates and negative prompts are not defined. The document explicitly lists this as open for `flux_gen1`, `z_image_turbo`, `flux2_klein`, and `lumina_image`. Without exact templates, cache keys and deterministic outputs cannot be validated. Concrete fix: provide final template strings, negative prompt policy per engine, seed-key inputs, max lengths, and a small acceptance test that verifies “single centered subject” and “subject-free plate” rows are generated with distinct cache identities.

13. [Invariants: LOUD fallbacks] The invariant says “LOUD fallbacks,” but `render_driver.render_shot()` explicitly disables fallbacks and raises `RenderError` on any engine failure. `MeshStageEngine.fallback_engine = "still_parallax"` will not be used in this path. Concrete fix: either update the invariant/design to “fail loud, no fallback,” or re-enable fallback walking for mesh-stage failures. Do not rely on `fallback_engine` unless the driver actually honors it.

SHOULD-FIX:
1. [Open questions: Ledger images taxonomy] Define row uniqueness and newest/oldest semantics. `_portrait_index()` currently first-wins via `setdefault`; `_still_index()` newest-wins by assignment. New `mesh_fodder` and `background_plate` indexes must specify whether first or newest row wins and why. Concrete fix: use explicit keys `(kind, object_id|beat_id, engine_id, seed, aspect)` and deterministic tie-breaking.

2. [Open questions: Determinism + invariants] Cache keys for image rows are underspecified. The design says fodder must not collide with cinematic portrait, but does not say which fields enter the cache identity. Concrete fix: bind at least `kind`, subject/beat id, prompt hash, negative prompt hash, seed, image engine id/version, aspect/size, and SFW/style profile.

3. [Open questions: Subject selection] Add validation before render: if selected engine has mesh capability, request must contain an existing on-disk `asset_refs.init_image` whose observability says `init_source == "mesh_fodder"`, plus an existing `background_plate` unless explicitly allowed to composite over black. This catches dispatcher/index mistakes before Hunyuan3D burns GPU.

4. [The design: BACKGROUND PLATE] Define behavior for missing or rejected subject-free plate. Current `build_clip_manifest()` only warns and falls back to floor/black for missing scene plate. If the goal is hardening quality, black fallback may hide asset-generation failure. Concrete fix: make missing plate a loud render-blocking error for mesh-capable engines, or explicitly declare black fallback acceptable.

5. [Open questions: Subject selection] Announcer/music slots need a policy that does not accidentally create generic unrelated objects. Concrete fix: if no `mesh_subject_id` exists for those roles, do not select `mesh_stage`; route to a 2D/video engine. If an artifact is intended, it must be explicit in the ledger.

6. [Problem / The design] The document alternates between “character OR story object” and “per-character fodder.” Concrete fix: use neutral terminology throughout: “mesh subject,” with `subject_kind in {"character","object"}`.

7. [Invariants: Single resident heavy engine <= 14.5 GB] The plan adds extra image generation but does not state sequencing/reclaim. Grounding shows `run_episode()` frees still-phase residue before video, but the image fork must preserve that phase boundary. Concrete fix: state that all fodder/plate generation completes before any mesh render, then existing pre-render reclaim runs.

OPTIONAL / NICE-TO-HAVE:
- Add manifest observability fields for `mesh_source_kind`, `mesh_source_path`, `mesh_source_sha8`, `background_plate_path`, and `background_plate_sha8`.
- Add a debug contact sheet per mesh beat: fodder, generated GLB preview, transparent render, plate, final composite.
- Add a strict QA flag that rejects fodder images with multiple detected subjects or non-neutral backgrounds [ASSUMPTION: detector availability not shown].

CUT THESE (over-engineering):
1. [Open questions: Prompt templates] Do not block initial hardening on four fully tuned engine-specific prompt suites. Start with one shared mesh-fodder template and one shared plate template, with per-engine overrides only where tests prove a failure. This is safe because the core correctness issue is asset separation and routing, not final prompt polish.

2. [Open questions: Subject selection] Do not build a full “story-object register” if the ledger does not already have one [ASSUMPTION]. Use an explicit optional `mesh_subject_id`/`mesh_subject_kind` on the shot first, falling back to `char_id`; route non-explicit object beats away from mesh. This avoids inventing a registry just to prevent bad mesh inputs.

3. [Open questions: Mesh cache] Do not implement a cache migration tool. The existing content-hash key naturally prevents cinematic-portrait meshes from being reused when the source changes. Only the manifest/key naming needs generalization for subject/object clarity.