<!-- requested_model: x-ai/grok-4.3 | resolved_model: x-ai/grok-4.3-20260430 -->

VERDICT: no. The design's core gating, fork, and ledger assumptions have no grounding in the provided engine or driver code and would not integrate without new hard contradictions or silent failures.

MUST-FIX BEFORE BUILD:
1. [The design] The gating rule "gated on the engine's `requires_mesh_portrait` / `character_3d` or `mesh_stage` capability -- NEVER a hardcoded engine-name check" does not exist: eng_mesh_stage.py defines only `requires_flag`, `roles`, `family="image_to_video"`, `required_inputs=("init_image",)` and `assert_usable`; render_driver.py routes solely via `_SCENE_INIT_FAMILIES` (which already contains "image_to_video") and `engine_family()`. Concrete fix: replace the non-existent capability field with an additive `requires_mesh_portrait: bool` on the engine class (default False) and update the single call site in render_driver that already inspects family.
2. [Open questions for the panel] Fork location is unresolved and the three candidates (`OTR_MetaBriefImagePromptGen`, `OTR_ImageDirector`, `otr_image_gen_dispatcher.py`) are absent from both excerpts. render_driver.py already contains the only per-beat role+family decision point (`build_request_from_shot` + `_portrait_index`/`_still_index`). Concrete fix: declare the fork must be added inside `build_request_from_shot` (the only seam that already reads both) before any image-stage changes.
3. [Open questions for the panel + Ledger `images` taxonomy] Announcer/music slots have `char_id=""` or role in `("announcer_visual","music_visual")` and fall to `_request_character_id` returning "uncast"; no story-object register or fallback rule is present. Adding `mesh_fodder`/`background_plate` kinds would break `_portrait_index` (keys only on `object_id`/`char_id`) and `_still_index` (keys only on `kind.startswith("scene_")`). Concrete fix: specify the exact ledger key for non-character subjects and the exact `kind` values that both index functions must accept without changing their current return shapes.
4. [Mesh cache + eng_mesh_stage.py:render_clip] `mesh_cache_key` + `portrait_content_hash` are computed on whatever `still = self._still_path(request)` supplies. Switching the upstream image stage to a new fodder image changes the hash automatically, but the design claims this requires "clean cache migration" that is not implemented and would collide on "uncast" for non-character beats. Concrete fix: add an explicit `mesh_fodder_sha` field to the request so the cache key can stay stable per canonical character while still accepting the new fodder path.

SHOULD-FIX:
1. [The design + Invariants] Aspect reconciliation ("Mesh fodder wants near-square ... plate wants 16:9") contradicts eng_mesh_stage.py:DEFAULT_W/H (1472x832 landscape) and render_driver.py canvas logic that forces landscape for every non-audio_driven_face engine. Fix: state that mesh fodder inherits the engine's declared `render_aspect` (or add the field).
2. [Problem + render_driver.py:build_request_from_shot] The cinematic prompt at ~:575 is used for the single `init_image` path; the new two-stream split must not affect the existing `_SCENE_INIT_FAMILIES` branch for `wan_i2v`/`still_kenburns` or the per-beat scene-still override. Fix: add an explicit guard that the mesh-fodder fork only fires when the selected engine declares the new capability.

OPTIONAL / NICE-TO-HAVE:
- Add a one-line comment in `mesh_cache_key` documenting that the 16-hex portrait prefix is now expected to come from a mesh-fodder still rather than the cinematic portrait.

CUT THESE (over-engineering):
1. New `kind` taxonomy additions: current `_portrait_index`/`_still_index` already distinguish by `object_id` vs `kind.startswith("scene_")`; a separate `mesh_fodder` kind is unnecessary until a second consumer appears.
2. "Prompt templates for flux_gen1, z_image_turbo..." scaffolding: the design never shows where these prompts would be consumed (no image dispatcher in excerpts) and can be added later without touching the video path.