<!-- requested_model: ~anthropic/claude-opus-latest | resolved_model: anthropic/claude-4.8-opus-20260528 -->

VERDICT: no. This is a pass00 design that is mostly open questions, and its core problem statement misidentifies the mesh input against the grounding code — the design is built on a false premise.

MUST-FIX BEFORE BUILD:

1. [Problem section] False premise about the mesh source. The doc states the mesh init is the per-character PORTRAIT via `_portrait_index`. Per `render_driver.py`, `_SCENE_INIT_FAMILIES = frozenset({"image_to_video","static_motion"})` and `MeshStageEngine.family = "image_to_video"`, so `build_request_from_shot` overrides `init_image` to the per-beat SCENE STILL (`_still_index`, `kind` starts with `scene_`); the portrait is only retained for `audio_driven_face`. The observed "whole frame fused into a clay blob" matches a full scene still, not an isolated portrait. Fix: re-base the entire design on the real input (the `scene_*` still consumed via the `_SCENE_INIT_FAMILIES` branch) and target that init-selection seam, not the portrait.

2. [Open questions: capability gate] `requires_mesh_portrait` is not present on `MeshStageEngine` in the grounding, and the family is `image_to_video`, explicitly "NEVER character_3d." Gating on `character_3d` will never fire for `mesh_stage`; gating on a `requires_mesh_portrait` field that the engine does not declare also fails. Fix: add an explicit boolean capability (e.g. `requires_mesh_portrait = True`) to `MeshStageEngine` and gate on that single field. [verify: ImageDirector actually reads `requires_mesh_portrait`; it is referenced only in prose and a dispatch_images comment, not shown.]

3. [Ledger taxonomy] Proposed new kinds `mesh_fodder` / `background_plate` will be invisible to the consumers. `_still_index` only collects rows where `kind` startswith `"scene_"`, and that index is what both init-selection AND `build_clip_manifest`'s `bg_still_path` read. A `mesh_fodder`/`background_plate` kind would never be resolved. Fix: either keep the `scene_` prefix (`scene_meshfodder`, `scene_plate`) or extend `_still_index` AND add a mesh_stage-specific init branch in `build_request_from_shot`.

4. [Open questions: fork location] The fork as described cannot live in one place. The two prompts must be minted by `OTR_MetaBriefImagePromptGen` (consumed by `dispatch_images` as a pre-built `objects` payload — the dispatcher does not author prompts), but the selected-video-engine capability is only known where `_still_needed_for_role`/`engine_consumes_still` run (the dispatcher / video registry). And the actual CONSUMPTION (selecting the fodder row as `init_image`) happens in `build_request_from_shot`, which the doc never mentions. Fix: name all three seams (prompt mint, engine-capability source, init-row selection) and how the capability reaches the prompt generator.

5. [Mesh cache] The doc claims `mesh_cache_key` keys on the "CANONICAL portrait content-hash" and per-character reuse holds. In `render_clip`, the key is `mesh_cache_key(char_id, portrait_content_hash(still))` where `still` is the resolved `init_image` = the per-beat scene still. Each beat's scene still differs ⇒ different sha ⇒ cache MISS and a full mesh rebuild every beat, defeating per-character reuse. Fix: feed mesh_stage a single per-character canonical fodder file (stable b