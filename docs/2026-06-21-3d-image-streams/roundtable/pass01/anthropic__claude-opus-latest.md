<!-- requested_model: ~anthropic/claude-opus-latest | resolved_model: anthropic/claude-4.8-opus-20260528 -->

VERDICT: no -- the problem statement's central premise (mesh source = per-character portrait) is contradicted by the grounded code, and the proposed capability gate + ledger kinds don't connect to the existing video-phase consumers.

MUST-FIX BEFORE BUILD:

1. [Problem, para 1-2] False premise about the current mesh source. The doc says "the mesh source is the per-character PORTRAIT (`render_driver._portrait_index`)". In `build_request_from_shot`, `mesh_stage` resolves to family `image_to_video` (`ENGINE_FAMILY["mesh_stage"]="image_to_video"`), which is in `_SCENE_INIT_FAMILIES = {"image_to_video","static_motion"}`, so `init_image` is set from `_still_index(...)` (the per-beat `kind=scene_*` still), NOT from `_portrait_index`. The portrait is only the fallback when no scene still exists. The observed "clay head double-exposed over the scene" is exactly what feeding the *scene still* to Hunyuan3D produces. Fix: re-ground the problem on the scene still, and design the fodder to *replace the scene-still init for mesh-capable engines*, not the portrait.

2. [The design, "gated on ... requires_mesh_portrait / character_3d or mesh_stage capability"] The gate as written cannot be implemented from the grounded code. (a) `MeshStageEngine` has no `requires_mesh_portrait` attribute (not in the class body or `__all__`). (b) Its `family` is `image_to_video`, explicitly "NEVER character_3d" -- so a `character_3d` gate misses it entirely. (c) Gating on family `image_to_video` would wrongly fork `wan_i2v` too. Fix: add an explicit boolean capability (e.g. `requires_mesh_portrait = True`) to `MeshStageEngine`, verify/extend `schemas` if it's a declared field, and gate the image-phase fork on `getattr(engine, "requires_mesh_portrait", False)`. [ASSUMPTION] that `OTR_ImageDirector` "already reads requires_mesh_portrait" -- not in grounding; verify it exists before relying on it.

3. [Open questions, "Ledger images taxonomy" + "Background plate"] The proposed new `mesh_fodder` / `background_plate` kinds are invisible to the video-phase consumers and will silently break. `_still_index` only collects rows where `kind.startswith("scene_")`; `build_request_from_shot` resolves `init_image` for `image_to_video` via `_still_index`; `build_clip_manifest` resolves `bg_still_path` via `_still_index(led).get(bid,"")` and warns/falls-back-to-black when absent. A new `mesh_fodder` kind would never reach `init_image` (mesh keeps eating the scene still = blob persists), and a new `background_plate` kind would never reach `bg_still_path` (composite drops to floor/black). Fix: the plan MUST include the consumer-side changes -- teach `build_request_from_shot` to set `init_image = mesh_fodder` for mesh-capable engines, and `build_clip_manifest` (the `str(eid)=="mesh_stage