<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. The document is a problem statement, not a build-ready plan: it defines failures but not the target architecture, acceptance rubric, or convergence strategy.

MUST-FIX BEFORE BUILD:
1. [S1 vs S5.3] Goal/method conflict: S1 defines success as “first freeze” clean, but S5 asks for “an effective reroll” and convergence. Those are different product goals. Concrete fix: split metrics into primary “first-pass clean rate” and secondary “reroll recovery rate,” with separate acceptance targets and a rule for when reroll is allowed.
2. [S2, S5.1, S5.2] “Strong/escalating arc” and “flat line” are not operationally defined enough to build against. The ledger fields exist, but there is no checkable rubric for whether a line discharged `line_job`, carried `hidden_pressure`, advanced `arc_phase`, or avoided flatness. Concrete fix: add a scoring rubric with explicit pass/fail criteria per line and per episode, e.g. each line must change knowledge, pressure, relationship, decision, or obstacle; each phase must introduce a measurable state change.
3. [S5] No proposed writer structure exists. The document asks how to prompt/structure writer LLMs but does not specify the intended pipeline: outline-first, beat grid, slot-by-slot generation, self-audit, critic handoff, constrained JSON generation, or repair protocol. Concrete fix: add the intended generation architecture before build, even if minimal: “generate dramatic spine → generate slot contracts → generate lines constrained to slots → self-check → critic → targeted patch.”
4. [S5.3, S4] Reroll failure is diagnosed but not structurally addressed. “Re-composes the same 3 targets twice” implies the loop lacks stable target identity, local patch constraints, and a before/after success test. Concrete fix: define reroll as targeted patching against immutable `line_id` / `dialogue_slot_id`, preserving approved lines and requiring the critic to re-evaluate only the failed claims plus continuity impacts.
5. [S3, S4, S5.5] Cast contract fix is underspecified. The core failure is engine-vs-role conflation, but the plan does not define the canonical separation among `speaker_role`, `tts_model`, `voice_preset`, and role archetype. Concrete fix: add a cast schema invariant: `speaker_role ∈ allowed_roster`; `tts_model ∈ engine roster`; `voice_preset` required for every character/announcer; `lead/foil/support` stored in a separate field, not `speaker_role`.
6. [S1, S2, S4] Acceptance target “freeze clean the norm” is not quantified. “Norm” is not buildable. Concrete fix: define minimum pass criteria for the fix run, e.g. ≥70% `frozen_clean` across a fixed sample, 0 cast violations, no `voice_preset=None`, reduced flat-line flags by X%, and no bypass halt.
7. [S4 opening vs S4 bullets] Episode counts are internally muddy: header says “17 published episodes + ~11 failures,” while S4 says “0 / 18 episodes froze clean,” then “6 frozen_with_warns, 11 shipped via repair-then-ship,” totaling 17. Concrete fix: reconcile the denominator and classify the missing episode or remove the ambiguous “~11 failures.”
8. [S5.6] “Stop stage-direction leakage at generation” states the desired outcome but not the mechanism. Concrete fix: add generation constraints and audit behavior: separate `performance_direction` from `text`, forbid parentheses/bracketed action in spoken `text`, and make leakage a hard retry target before freeze.

SHOULD-FIX:
1. [S2] The ledger contract includes `target_words` and timing, but S1’s quality goals are dramatic. There is no stated priority when word budget conflicts with dramatic fulfillment. Concrete fix: define precedence: contract validity and dramatic state changes beat exact word count within a tolerance.
2. [S2] `arc_phase` is listed as setup → complication → resolution, but there is no episode-level phase allocation. A writer can tag phases without producing escalation. Concrete fix: require phase-level state deltas: setup poses dramatic question, complication raises cost/blocks want, resolution forces costly choice or consequence.
3. [S2, S5.4] Voice consistency depends on character voice specs, but S3’s `character_description` is portrait-heavy and may not provide usable dialogue constraints. Concrete fix: add a compact per-character voice bible: syntax, diction, taboo phrases, emotional mask, pressure behavior, sample line.
4. [S4, S5] The plan assumes the critic’s labels are reliable enough to train the repair loop, but no calibration or disagreement handling is described. [ASSUMPTION] Concrete fix: define critic evidence requirements: every flag must cite `line_id`, failed contract field, reason, and minimal repair instruction.
5. [S5] Cross-model ambition is too broad without a lowest-common-denominator contract. Local models and frontier models likely need different prompting and JSON discipline. [ASSUMPTION] Concrete fix: define a single strict intermediate representation and allow model-specific prompt wrappers only around it.
6. [S1] “Every line doing real dramatic work” risks overconstraining announcer, music, and SFX rows. Concrete fix: define different fulfillment rules by `speaker_role`; music/SFX should support transition, mood, or punctuation rather than discharge dialogue pressure identically.
7. [S3] `allowed_roster` mixes human speaker roles with non-dialogue rows: `announcer`, `character`, `music_close`, `music_inter`, `music_open`, `sfx`. Concrete fix: rename or split into `speaker_role` and `cue_type` if music/SFX are not speakers.

OPTIONAL / NICE-TO-HAVE:
- Add 2–3 gold reference ledgers: one clean miniature, one flat-line failure, one cast-role failure.
- Add a “do not generate” block with examples of forbidden stage directions and engine/role conflation.
- Add a one-page decision table for freeze outcomes: clean, warn, rerun, halt.

CUT THESE (scope / over-engineering):
1. [Opening, S5] Cut full validation across 5 writer models × 4 word tiers × 4 creativity modes for the first build. It obscures whether the method works. Start with one small model and one frontier model at one word tier, then expand.
2. [S3] Cut portrait-facing details from the R1 fulfillment build, especially “rich face/presence/voice text used for portraits.” Video/portrait concerns are out of scope, and they do not fix ledger or voice-contract compliance.
3. [S4] Cut most soak-stat volume from the build spec after preserving the few acceptance-driving facts: 0 clean freezes, reroll non-convergence, role mismatch, missing voice presets, stage-direction leakage. The rest is diagnostic background, not architecture.
