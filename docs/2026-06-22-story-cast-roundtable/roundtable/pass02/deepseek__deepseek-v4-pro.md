<!-- requested_model: deepseek/deepseek-v4-pro | resolved_model: deepseek/deepseek-v4-pro-20260423 -->

VERDICT: no. The plan instructs implementors to build features already present (prose/metadata decouple, targeted reroll, concrete critic instruction) and omits the actual gaps identified in the grounding (scene-arc isolation, critic scope, operational flat test, voice_preset fail-closed, role_mismatch source). Building as-is wastes effort and leaves the real defects unfixed.

MUST-FIX BEFORE BUILD:
1. [R1 §1, §3] Remove "prose/metadata decouple" and "targeted reroll" from the build scope. Grounding confirms `_otr_line_composer.py` already separates prose from metadata, and `_otr_reroll.py` already patches stable line_ids with a concrete `RerollTarget.hint`. Replace with the real gap: per-line generation lacks a scene-arc view. Implement a `SceneArc` object (derived from `dramatic_state` + beat outline) passed into `compose_line()` so each line is composed with awareness of escalation trajectory, not just the last N lines.
2. [R1 §2] Operational flat test. The current `FlatLine.reason` is a free LLM string; no code can verify it. Define a deterministic or small-model check: a line is flat if it fails to advance its slot’s `line_job` or escalate `hidden_pressure` relative to the prior line in the same `arc_phase`. Implement a `_is_flat(line, prev_line, slot)` function that checks for knowledge change, pressure shift, relationship move, decision force/avoid, or obstacle raise/clear. The critic must call this function and only flag lines that fail it. Per-role rules (announcer/music_*/sfx) are exempt.
3. [R1 §5] voice_preset fail-closed. Grounding shows `cast_lock.py` ~L272-291 can leave `voice_preset=None` for unmatched char_id. Modify `cast_lock` to require a valid preset for every character and announcer: if `cast_seed` is None, raise a hard error (no silent skip); if `cid` not in `voices`, either pick a default from `allowed_roster` presets or abort with a clear message. No `None` may reach the TTS engine.
4. [R1 §5] role_mismatch source. The grounding states the audit fires but the injection point is upstream. Add a tracing step: locate the code that writes `speaker_role` or `role` (likely in the orchestrator or casting pipeline) and ensure it never sets an engine name. Hardcode the mapping from `allowed_roster` roles to TTS engines in a single config, and validate at assignment time. This must be fixed before the cast-contract fix is considered complete.
5. [R1 §3] Critic scope reduction. `_otr_story_critic.py` ~L410-415 re-scores the entire character ledger, causing whack-a-mole. Modify the critic to accept a `patched_line_ids` set and only evaluate those lines plus their immediate continuity neighbors (previous and next line in the same