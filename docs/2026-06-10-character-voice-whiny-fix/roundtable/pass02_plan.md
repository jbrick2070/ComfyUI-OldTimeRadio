# Character-Voice "Whiny" Fix -- problem statement + roundtable judgment + hardened plan v3
*2026-06-10. Three review rounds: (1) self-panel (Claude, $0); (2)+(3) LIVE 4-model OpenRouter passes -- GPT-5.5, Gemini 3.1 Pro, Grok 4.3, DeepSeek V4 Pro -- open-ended prompt, every surviving claim grounded against the repo, two claims settled by MEASUREMENT on the live ref bank. Raw reviews + judgment logs: docs/2026-06-10-character-voice-whiny-fix/roundtable/. PLANNING ONLY -- coder kickoff prompt at the end.*

## 1. Problem description (unchanged)
The character dialogue generated by the IndexTTS2 TTS model has an unintended "whiny" / pleading quality. The announcer voice (Kokoro) is fine in the mix; the primary character voices (Hayes, Gulliver) lack vocal weight and sound perpetually complaining. Era accuracy is NOT the issue -- modern-sounding delivery is acceptable.

## 2. Grounded reality check (verified against the repo + live bank, 2026-06-10)
- **G1 -- The bank is 37 indextts2 char refs, not 4** (14 male + 22 female + narration; cb_*/dia_* mirrors). Female additions NOT needed.
- **G2 -- All 36 ref WAVs exist on disk**; the missing-ref bark fallback SHOULD not fire -- but its WARNING goes only to the node's render_log STRING, and `otr_runtime.log` carries ZERO voice-render lines. **Per-line engine identity is persisted nowhere; bark on past episodes cannot be excluded retroactively** (H4).
- **G3 -- Bank metadata is machine-stamped, never human-audited.** Gender by median-F0 threshold (165 Hz); generic timbre tags; `vz_donor_james` / `vz_donor_hillbilly_jim` in the FEMALE pool are probable threshold artifacts.
- **G4 -- Casting is a uniform per-episode lottery.** Canonical graph: node 81 indextts2, node 82 kokoro, node 80 CastLock auto_registry `allow_voice_reuse=true`. In-tier pick is `random.Random(seed).choice(pool)` -- uniform; scores only sort. Writer timbre vocab does not match bank tags, so male characters draw uniformly across all 14 male refs per episode_seed.
- **G5 -- scene_tension is never stamped** -> the delivery vector is keywords + punctuation only.
- **G6 -- The derived vector is biased toward whiny-adjacent emotions** (interrogatives -> surprised; help/run/danger -> afraid; sorry/lost/gone/alone -> sad; remember/once/ago -> melancholic); "?" adds 0.5 raw; 3 cues saturate a dim; NO total-mass cap; calm collapses fast; keyword+punctuation double-dips.
- **G7 -- emo_alpha pinned at 1.0** adapter-side; the worker already accepts a per-request value.
- **G8 -- True-neutral A/B free:** `OTR_DELIVERY_VECTOR=0` -> all-zero -> worker OMITS emo_vector. A default-path "neutral" line still sends calm=1.0 @ alpha 1.0 -- a different render; IndexTTS2 calm semantics UNVERIFIED.
- **G9 -- Observability gap:** resolved voice_ref_id per character is not logged or persisted on the default path; CastLock's report STRING is not persisted either.
- **G10 -- Versioning hooks exist** (DELIVERY_TABLE_VERSION; voice_bank_id+sha; PREPARE_TEXT_VERSION; profiles dormant).
- **G11 -- Same-voice collision possible** on the render-time path (allow_voice_reuse=True, no episode used-set); CastLock auto_registry HAS a used-set that the widget `allow_voice_reuse=true` defeats.
- **G12 -- The adapter skips full text normalization** (`clean_spoken_text` only): `?` `!` `...` em-dashes/colons/caps ride into the engine -- an independent punctuation-prosody channel.
- **G13 -- The delivery vector is derived from RAW pre-strip text** (runs before prep); parentheticals cue emotions then vanish from the spoken text.
- **G14 -- Ref preprocessing is peak-normalized (0.97), never loudness-normalized; no clipping/SNR screen; refs are 44100 Hz vs engine 22050** (internal resample behavior UNVERIFIED).
- **G15 -- Solo-vs-mix unsplit:** dense/compressed Kokoro vs dynamic IndexTTS2 output; nothing measures character clips against the announcer or the bed.
- **G16 -- Reuse/tier constraints verified:** CastLock catches `VoiceCastingError` per char ("NOT cast" -> render-time resolution takes over); `assign_voice_for_slot` has no tier concept (tier logic must pre-filter the pool); the render-time fallback uses the FULL bank with reuse=True, so it can bypass any CastLock-side uniqueness or tier policy.
- **G17 -- (pass 2, verified) Per-line prosody seed instability.** `engine_seed = _seed_to_int64(engine, request.stable_line_seed)` -- every line of the SAME character samples a fresh prosody draw. A character can be hesitant on one line, bright the next; zero-shot models can read this as nervous/pleading. No per-character seed mode exists.
- **G18 -- (pass 2, MEASURED on the live bank) Ref loudness + trimmer evidence.** Measured all 36 WAVs: donors sit at RMS 0.089-0.187 with 0.97 peaks; **the four curated voice-zero refs are loudness outliers** -- `stuart_bell` RMS 0.027 (peak 0.22), `peter_yearsley` 0.035 (0.31), `bill_boerst` 0.052 (0.37), `caro_davy` 0.061 (0.40) -- never normalized, 2-6x quieter than every donor. A zero-shot cloner that copies vocal effort gets a soft, low-projection prompt from exactly the "premium" male refs. Also: durations are flat 10.00s (donors) / 12.00s (narration = the first-12s fallback branch) / 5.9-10.8s (originals) -- **no ref is under 5s**, which REFUTES the pass-2 claim that the trimmer shipped single-vowel clips, while CONFIRMING the trimmer logic itself is latent-broken (the longest-voiced-run breaks at every unvoiced frame; the <2s fallback to "first 12s raw" is what actually shipped). Fix the trimmer before any future re-download; the current bank never went through a working trim.

## 3. Root-cause hypotheses (v3 -- five channels, all mechanism-grounded)
- **H1 Ref pool (timbre + read style + EFFORT):** thin/noisy/politely-read donor refs in a uniform lottery, PLUS the measured low-RMS originals (G18) -- a quiet, low-effort prompt can clone as weak/pleading regardless of pitch. F0 alone cannot rank any of this.
- **H2 Delivery (vector + alpha):** keyword-saturated afraid/sad/surprised vectors at alpha 1.0.
- **H2b Punctuation prosody:** `?`-heavy text reaches the engine un-normalized (G12) and shapes contours independently of the vector.
- **H2c Mix perception:** character clips may sit quieter/thinner than Kokoro against the bed (G15).
- **H2d Prosody-draw instability (NEW, pass 2):** per-line seeds (G17) make a character's stance wander line to line -- "whiny" may partly be "unsteady".
- **H3 Compounding:** worst case = quiet thin ref + saturated vector + rising "?" + fresh draw per line + under-weighted clip in the mix.
- **H4 Past episodes may literally be bark** (operator-ear evidence; unprovable retroactively until P3a; P0-zero proves fresh renders).
- Ruled out: era/vintage framing; announcer path; engine-widget misconfiguration; "the bank is vowel-moan clips" (G18 measurement).

## 4. Roundtable record
**Round 1 (self-panel, $0):** G1-G11, H4, P0-zero, plan v1 (commit 85ca2a4).
**Round 2 (live pass 1, $0.22, 4/4):** 31 claims -> 19 confirmed (G12-G16, H2b/H2c, audition-reel method, alpha/cap sweeps, calm-floor dropped, P3b resolve-once cache, P3a persistence design), 3 misread, 9 to P0/verify-at-build. Log: `roundtable/pass01_judgment.md`.
**Round 3 (live pass 2, ~$0.24, 4/4):** material adds -> per-line seed instability (G17, GPT; verified); P3a-MINIMAL render_log observability reordered to FIRST (GPT/Grok/DeepSeek convergent); calm property-test fixed (GPT: do not bake "calm-dominant" in before P0); P2d reuse-flip DEFERRED until P3b (3-model convergent: the render-time fallback bypasses used-set AND tier policy, G16); punctuation lever de-committed to variants (Gemini: `?`->`.` risks "depressed/robotic"; test `?!` and `...` too); emotion-soup cap candidate "primary + 1 secondary" (Gemini); real-offending-lines + short-vs-long-line + fixed-char-seed P0 cells (GPT/Gemini); lottery exposure simulation (GPT/DeepSeek); cloned-output spectral-tilt check (DeepSeek); breath-tail metric, voiced-frames-only spectral ratios (GPT/DeepSeek); trimmer latent bug + ref-loudness outliers settled by MEASUREMENT (G18 -- Gemini's catastrophe claim refuted, mechanism confirmed). Misreads discarded: "bank is vowel moans" (measured), "vector-omitted defaults to calm=1.0" (worker omits all-zero vectors), "P2a must wait for the trimmer fix" (current bank never used the trim path). Log: `roundtable/pass02_judgment.md`.

## 5. Hardened plan v3 (no architectural overhaul; all changes additive + versioned)
**P-OBS -- Minimal observability FIRST (coder, ~1 h, CPU-only, no audio-byte change).** Render_log gains per-character (and per-fallback-line) lines: `char_id -> voice_ref_id -> ref_path basename -> engine -> alpha -> delivery_version -> vector(omitted|zero|nonzero) -> seed`, plus a clip-sample-rate==pack-sr assert. Mirrored to the runtime log. This unblocks attribution for EVERY subsequent step (pass-2 convergent finding); the durable-ledger restamp stays in P3a but no longer gates diagnosis.
**P0-zero -- PROVE which engine rendered (operator, ~10 min, zero code).** Queue one 30-word smoke; read node 81 render_log + node 80 CastLock report from `/history/<prompt_id>`: `rendering N lines on 'indextts2'` vs bark WARNING. Bark present -> fix ref resolution first. Fresh-renders-only limitation stays explicit.
**P0a -- Solo-vs-mix split on EXISTING audio (operator, ~15 min, zero render).** Take known-whiny character lines from an existing episode: listen solo loudness-matched, then in the mix. Solo-OK/mix-thin tilts the whole effort toward H2c before any TTS tuning (pass-2: do this EARLY, not as a final gate). Free offline EQ/compression/loudness test on one bad WAV here too ("0/30/70/100% fixed?" question).
**P0 -- Audition matrix (operator GPU, ~1 h scripted, zero pipeline code).** Drive the Path-B worker directly with hand JSON. Lines: 3 synthetic (neutral declarative / question-heavy / fear-urgent) + **2-3 REAL offending lines from the episode** + 1 one-word interjection vs 1 long line (length cell). 3 seeds each, loudness-matched playback, spectral report (median F0, final-500ms rise, centroid, RMS, low-mid ratio) auto-computed on every output. Conditions (run punctuation/ref cells BEFORE the alpha sweep -- if they reproduce the whine alone, the sweep shrinks):
  1. `vz_bill_boerst`, vector OMITTED (quiet-original baseline)
  1b. hottest healthy donor ref, vector OMITTED (loud-donor baseline -- G18 says these two baselines may differ by themselves)
  2. derived v1 vector @ alpha 1.0 (reproduce today)
  3. punctuation variants, vector OMITTED: terminal `?` -> `.` vs `?!` vs `...` (H2b direction is unknown -- Gemini warns `.` may read depressed)
  4. worst-suspect ref (P2a composite rank), vector OMITTED; and @ alpha 0.65 (H1 + interaction)
  5. alpha sweep 0.25 / 0.45 / 0.65 on the v1 vector
  6. calm=1.0 only; and angry-floor 0.15 only (vector-shape cells)
  7. one ref pre-resampled to 22050 mono, vector OMITTED (G14)
  8. **fixed-character-seed panel:** 6 consecutive lines, per-line seeds vs one fixed seed (H2d; if it wins -> `OTR_INDEXTTS2_SEED_MODE=line|character` versioned option)
  9. neutral-rewrite of one whiny line, same ref/seed (whine-from-WRITING split -- measures the script's contribution without reopening the writer)
  Decision gate: per-channel verdicts H1/H2/H2b/H2d + alpha default + calm policy + cap strategy + punctuation variant (or none) + seed mode (or none).
**P1 -- Delivery + text calibration (coder, CPU-green; defaults from P0 data).**
  1. Delivery table v2 (version "v2"): derive from PREPARED text (accepting the loss of parenthetical affect cues -- explicit tradeoff, P0 cell 9 informs); demote stop-word interrogative cues; count punctuation once; de-bleat rule (terminal-? + afraid/sad -> demote surprised); total-mass strategy CHOSEN FROM P0 among {normalize-sum, primary-keeps-full + cap-secondaries, primary + 1 secondary max (Gemini), soft penalty}; NO unconditional calm floor. Property tests: pure-neutral text sends NO aroused dims (calm policy = whatever P0 proves; do NOT assert calm-dominant); mass cap holds on random corpora; "?"-only line never saturates surprised.
  2. `OTR_INDEXTTS2_EMO_ALPHA` env (clamp 0..1, default = P0 winner) -- cheapest dominant lever, ships even alone.
  3. Punctuation lever, experiment-gated, VARIANT from P0 cell 3 (`OTR_CHAR_DECLARATIVE_Q`, default OFF, character lines only, applied AFTER vector derivation, scoped to short non-informational questions). Fix G12: adapter adopts full shared normalization (PREPARE_TEXT version bump) + a corpus residual-character report (em-dash/colon/caps/long-parens) to catch the rest.
  4. (P0-gated) `OTR_INDEXTTS2_SEED_MODE=line|character` if cell 8 wins -- versioned, default stays `line` until listen evidence.
**P2 -- Bank audit + curation (tooling: coder; listening: operator).**
  a. `--audit` mode in `scripts/otr_dl_indextts2_refs.py`: duration, sample rate, **RMS/LUFS (G18 makes this primary, not optional)**, crest factor, clipping count, median F0 + std on VOICED frames only, voiced fraction, spectral centroid + slope (200 Hz-4 kHz), chest-weight (male 120-350 Hz / female 180-450 Hz bands, voiced frames), nasal-band ratio, end-of-line F0 rise, breath-tail ratio (last 200 ms vs body), pause-RMS SNR proxy, DC offset; composite must-listen ranking (NEVER auto-reject from metrics); flag F0 145-185 Hz as gender-ambiguous; flag the 2 suspect female tags; flag the 4 unnormalized originals.
  b. **Audition reel** (blind, loudness-matched, randomized order, hidden ids, one repeated sample for operator consistency): fixed 6-line panel per candidate ref, vector-off + calibrated, scored on body / authority / whine-risk / range / artifacts / stability. Stamp additive fields: `quality_tier` (a/b/reject), corrected `gender`, `style_tags` (`lead_safe`, `nasal_risk`, ...).
  c. Tier logic = deterministic two-stage POOL PRE-FILTER before the ladder (leads: tier-a `lead_safe` if any survive constraints, else tier-a, else configured degrade; minors: tier-b allowed; reject never) -- never reweight `_pick`. **Applies to BOTH casting routes:** `_resolve_clone_ref_path` must consume the same filtered bank, or tier-reject refs leak through the fallback (pass-2 DeepSeek).
  d. Ref hygiene (versioned asset changes, new shas): loudness-normalize the 4 quiet originals (G18); fix the trimmer (gap-tolerant ~250 ms voiced-run merge) BEFORE any future ref download; optional re-trim/re-master of existing donors stays an appendix experiment.
  e. `allow_voice_reuse=false` flip: **DEFERRED until P3b lands** (pass-2 convergent: until the render-time path shares the used-set + tier policy, the flip gives false uniqueness). Then flip it.
**P3 -- Durable observability + collision (coder, small).**
  a. Durable per-line persistence (engine + voice_ref_id + alpha + delivery version + seed) into the saved episode ledger via the video lane's restamp route -- VERIFY the route is reachable from audio nodes first; additive fields only; render_log (P-OBS) remains the guaranteed floor. After this, "was that bark?" is answerable from every episode's artifacts, forever.
  b. Per-episode used-set in `_resolve_clone_ref_path` with a per-character resolve-once cache, keyed (engine, role, char_id, episode); never override a valid stamped voice_ref_id; same tier-filtered bank as CastLock (see P2c). Prefer-unique, reuse-only-on-exhaustion.
  c. Lottery exposure simulation (CPU, zero render): 100 episode seeds -> which refs Hayes/Gulliver draw, collision rate, exposure reduction under tier filtering. Quantifies H1 before/after curation.
**P4 -- Verify (gates, in order).** Full suite + Bug Bible green; confirm `test_audio_byte_identical` is bark-path and unaffected; 30-word GPU smoke; full episode re-render; final solo-vs-mix listen (P0a repeated on the NEW render); generated-line acoustic report (final-rise + vector-mass flags) as the regression metric. DONE bar: Hayes/Gulliver no longer read as whiny on a fresh episode, operator-judged. If solo-fixed but mix-thin persists -> the H2c post/mix lane gets its own scoped decision (e.g. per-character anchor-line loudness conformance, versioned + default-off) -- NOT silently bolted onto this fix.

## 6. Invariants + scope fence (unchanged)
- Frozen-audio spine NOT reopened: by