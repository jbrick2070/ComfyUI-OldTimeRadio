# Candidate row additions (scoped 2026-07-02, no review pass needed)

Row addition is a MECHANICAL path under the converged architecture
(curate -> pin via scripts/otr_pin_partner_nodes.py -> ToS/pricing
stamp -> thin adapter on the row pattern). Panels are for design
forks; this is catalog curation. Scoped against the full 214-node
dump.

## VOICE: no addition possible on Surface A (definitive)

The complete partner audio catalog: ElevenLabs x7 (TTS/dialogue/SFX/
S2S/STT/isolation/clone), Sonilo x2 (music), Stability x3
(text-to-audio, audio-to-audio, inpaint -- not TTS). There is NO
second TTS provider. Expansion paths remain exactly as planned:
(a) additional ElevenLabs model-tier rows (same provider, marginal),
(b) Chatterbox via Surface B once its lifecycle smoke passes
(Appendix B -- the designated third-row path).

## IMAGE: pin these at the next cloud window (S1 or later)

1. `cloud_ideogram_v4` (IdeogramV4) -- PRIORITY. Best-in-catalog text
   rendering: period posters, signage, newspaper clippings, case-file
   labels and clue notes -- direct F1 Evidence Board dressing-layer
   synergy. CHEAP-tier candidate pending pricing.
2. `cloud_seedream_2` (ByteDanceSeedreamNodeV2) -- OPTIONAL second.
   Strong stylization; ByteDance provider already in the stack
   (shared provider semaphore/ToS work amortizes).

Both: add to CURATED_ROWS in the pin script, re-run pinner, drift
test, license_audit_status + approx_cost before promotion -- the
standard path, no plan change required.

## Noted for later (not curated now)

WanTextToImageApi (Wan does stills), KlingImageGenerationNode,
Recraft inpaint/upscale + Magnific upscalers (poster finishing),
Quiver SVG (vector signage), Bria video background tools.
