<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.6-sol -->

VERDICT: build-ready as-is? no. Qualification sequencing lacks the required listening gate, profile/runtime authority is ambiguous, and the audition/harness control flow can silently render contaminated or incomplete arms.

MUST-FIX BEFORE BUILD:
1. [§4, §6] The plan says neutral dialogue must be heard “before it locks in,” but §6 writes the qualification record immediately after rendering, with no listening/approval step. Insert an explicit blind-review gate after manifest generation and before qualification; record the operator’s decision using the existing qualification schema. If neutral/ordinary dialogue is rejected, do not qualify.
2. [§6] The suite runs only after qualification. A failing test or resulting code correction invalidates the already-rendered evidence and fingerprint. Run the full suite before freezing/hashing/rendering, then run route/qualification validation again after writing the record. Any subsequent adapter, profile, audition, or qualification-relevant edit must restart fingerprinting and rendering.
3. [§5.2, §5.3, §6] The manifest/qualification provenance omits the profile source hash even though `audio_engine_profiles.yaml` is text-hashed as an `engine_profile` re-baseline trigger. Record both the live adapter fingerprint and the resolved profile/source SHA, plus `engine_impl_version: "3"`. Verify: the exact qualification-record fields and helper signatures used by the existing route loader.
4. [§5.3] Clearing alpha/cap only for `shipped` is insufficient. The process renders randomized arms sequentially, and `_render_arm` currently only assigns supplied variables; a preceding control or inherited shell variable can contaminate any arm. Before every arm, `pop` all arm-controlled variables—at least `OTR_INDEXTTS2_EMO_ALPHA`, `OTR_INDEXTTS2_EMO_MASS_CAP`, and `OTR_VOICE_CHARACTER_SEED`—then apply that arm’s explicit overrides. The shipped arm should set only its intended seed override and leave alpha/cap absent.
5. [§5.3] The current audition silently accepts partial dispatch: it ignores `_done` and pairs lines with captures using `zip`, which truncates if fewer than three clips render. Require exactly one capture per expected line, reject extras, and fail before writing `MANIFEST.json` if dispatch reports failure or the count/order is wrong. Verify: the concrete success contract and shape of `BatchCharacterVoices.generate(...)[2]`.
6. [§5.4] “Fix arms track the shipped defaults instead of hardcoding” is not implementable as specified. The current PowerShell arm table requires concrete `Alpha` and `Cap` strings. Represent default arms explicitly, remove both environment variables before their server boots, and set `1.0`/`8` only for control arms. Update logging so resolved defaults are reported rather than dereferencing absent table values.
7. [§5.2] `default_params.emo_alpha` and the proposed `emo_mass_cap` are not runtime authorities in the shown adapter: `generate_voice()` calls `emotion_payload()`, which resolves only environment variables or Python constants. Either declare the YAML values metadata/cache declarations and keep the adapter constants authoritative, or wire profile values into a documented runtime interface. Add an integration test proving that render-time resolved values override profile defaults in the actual cache-key builder; a static YAML-versus-constant equality test is insufficient.
8. [§2, §3] Speaker retention is not exactly `0.440` for the measured `0.5590` line; under the cited formula it is `0.4410`. State the invariant as “at least 0.440” and calculate per-line retention as `1 - effective_mass` wherever it is recorded or asserted.

SHOULD-FIX:
1. [§5.3] Define the manifest shape precisely. Put resolved alpha and cap inside each arm because the values differ by arm; put adapter fingerprint, profile/source SHA, reference ID, reference-file SHA-256, and engine implementation version at the top level. Hash the exact final manifest bytes only after a successful complete render.
2. [§5.3] Implement output/key-directory refusal for all path states: reject a path that is a file, reject a non-empty directory, and preflight both output and key paths before rendering. Use exclusive directory creation where practical so a concurrent invocation cannot pass the checks and overwrite evidence.
3. [§5.5] Define “below-cap vector passes through unchanged” as unchanged relative to `sanitize_delivery_vector`, not unchanged relative to arbitrary raw ledger JSON; sanitization legitimately clamps, rounds, and fills keys.
4. [§5.5] Exercise cache invalidation through the real request/cache-key construction path for both environment overrides. Calling `render_time_params()` alone would not verify merge precedence against the newly added YAML defaults.
5. [§5.3] Verify the actual import path, argument contract, and return format of `live_engine_impl_version` before coding the manifest integration; none is provided in the grounding. Fail the audition if fingerprint or reference hashing cannot be completed rather than emitting a partially provenance-bearing manifest.
6. [§5.3] Update all two-arm assumptions together: `labels`, shuffle mapping, manifest title, KEY descriptions, module docstring, and the existing `"prefix"` identifier. Otherwise the third arm can be omitted or mislabeled despite the new `ARMS` entry.

OPTIONAL / NICE-TO-HAVE:
- [§5.3] Write `MANIFEST.json` through a temporary file and atomic replace so interruption cannot leave a syntactically partial manifest.
- [§6] Emit a small machine-readable prequalification report containing the resolved fingerprints, manifest hash, and test result to reduce manual transcription errors.

CUT THESE (over-engineering):
1. None. The three-arm audition, cap serialization measurement, and bounded shave each protect a stated experimental or numerical invariant and are not safe to remove from this change.