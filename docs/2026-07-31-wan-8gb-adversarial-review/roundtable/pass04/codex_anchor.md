# Round 4 Codex anchor: final convergence

Written before final local convergence review.

The report's release verdict is Wan draft/unqualified. It distinguishes the
broken guard from unproved execution feasibility, corrects the historical
177-frame/16-GB artifact, and does not create a new production bug from static
evidence.

The final plan must have no remaining claim that:

- an affine envelope inherently means co-residency;
- GGUF has no low-VRAM path;
- AIMDO caused ComfyUI's original July 2025 8 GB claim;
- the official 5B UNet is FP8;
- the canvas defect caused today's fixed-term preflight refusal;
- a reserve clamp certifies physical 8 GB;
- a prompt-only wrapper cache is safe/native-compatible;
- 14B+Lightning is proved superior; or
- SANA/CogVideoX is a qualified replacement.

Final convergence should look for unsupported numbers, an unexecutable cell,
missing canonical-workflow authority, or an accidental release recommendation.
