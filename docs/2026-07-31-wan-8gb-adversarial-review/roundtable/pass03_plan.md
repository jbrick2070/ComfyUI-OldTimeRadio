# Pass 03 converged plan

1. Carry all qualification controls through the canonical workflow, profile,
   ledger, render request, and receipt.
2. Instrument the continuous text/latent/sample/decode and transition envelope,
   including explicit unload and quiescence boundaries.
3. Split expensive lifetimes only where the executor proves they no longer
   overlap; do not infer GPU release from Python reference release.
4. Use one OTR-owned loader for explicit encoder placement and avoid parallel
   selector branches that execute both loaders.
5. Use a model-free cache-only native conditioning source with complete
   provenance, atomic writes, validation, and fail-closed misses.
6. Build admission from versioned measured envelopes and reject extrapolation.
