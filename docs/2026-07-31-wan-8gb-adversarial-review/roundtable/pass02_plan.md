# Pass 02 converged plan

1. Add a deterministic canonical replay input and a receipt contract before any
   mechanism comparison.
2. Acquire and inventory the official native FP16 UNet before the native cells.
3. Compare native Dynamic, the same native artifacts under legacy policy, GGUF
   with global Dynamic disabled, and the shipped GGUF configuration.
4. Begin encoder/cache cells only after real per-request placement and
   provenance-complete native conditioning-cache seams exist.
5. Use a receipted diagnostic-canvas override for alternate-resolution cells;
   keep 832x480 authoritative in production.
6. Finish with randomized cold repeats and a same-server production-behavior
   soak. Physical 8 GB remains the release gate.
