# pass01 judgment (ARCHITECTURE, C1+C2) -- Claude = judge

Panel: my grounded critique + GPT-5.5 + Gemini-3.1-pro + DeepSeek-v4-pro. Spend $0.07.

## UNANIMOUS (accepted)
- CUT the C1 reroll gate -> injection-only (composer lacks beat position; gate
  burns the single reroll on valid short lines; not model-agnostic). (all 4)
- CUT the act-1 outline nudge (derivation runs after the outline). (all 4)
- ZERO-RIPPLE injection: append to canon_header (C1) + news_close_brief (C2); NO
  LineRequest / compose_announcer_outro signature change, NO schema change. (all 4)
- Idempotent: do not recompute if meta already has the key (C7 on resume). (GPT/DeepSeek)
- Filter cast names + boilerplate from anchors; empty-list fallback skips injection;
  anchors = JSON list, central_object = short string. (GPT/DeepSeek)
- audio invariant = spine/no-JSON, not identical generated bytes. (GPT)

## JUDGED SPLIT (the user's exact question: deterministic vs LLM)
GPT + me + DeepSeek: DETERMINISTIC (no new LLM call). Gemini: a cheap CACHED LLM
call (regex can't judge semantic centrality). RULING: DETERMINISTIC default, but
extract from the CURATED `key_terms` (already LLM-distilled in the news brief) so
the INPUT carries the quality Gemini wanted while SELECTION stays deterministic +
C7-safe. central_object = a CONSERVATIVE object-like key_term pick, OMITTED when no
confident candidate (never invented). A cheap cached LLM extraction stays a
documented OPT-IN flag (default off). This satisfies determinism + model-agnostic +
Gemini's quality concern without a new runtime call.

## REJECTED
- A live C1 reroll gate (infeasible without beat position; dangerous to short
  dialogue). - Deterministic extraction over RAW news for centrality (Gemini right
  it's garbage) -> mitigated by extracting from curated key_terms, not raw news.

## OPEN -> pass02 (coding) / pass03 (wiring)
- Exact extractor contracts + the object-likeness heuristic + value shapes.
- The verified seams: canon_header build site, news_close_brief + outro call site,
  meta free-form round-trip, key_terms/news/cast variable names in the writer.
