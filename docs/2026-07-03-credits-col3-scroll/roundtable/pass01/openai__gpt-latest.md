<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. The draft preserves the exact overflow-only/static mechanics that the real-observation section says failed the operator goal.

MUST-FIX BEFORE BUILD:
1. [Proposed change #3] Defect: “Keep the verified scroll mechanics + duration model” directly contradicts [CRITICAL real-obs finding #1] and the operator goal of a visible roll that shows full content regardless of length. Grounding confirms current `render_credits_clip` pads short canvases to `view_h`, computes `scroll_px = max(0, scroll_img.height - view_h)`, and `compute_credits_duration_s()` uses only that overflow distance. That model can produce a static hold for short content and a barely perceptible move for near-viewport content. Concrete fix: replace the model with an explicit classic roll contract: col-3 content starts below the viewport and exits above it; scroll distance must be based on `content_h + view_h` or equivalent top/bottom padding, not merely `content_h - view_h`. Update [Proposed change #3], `compute_credits_duration_s()`, and `render_credits_clip()` to use the same roll distance.

2. [CRITICAL real-obs finding #2] Defect: the draft leaves the animation mechanism unresolved even though the grounding shows the current ffmpeg filter is `[2:v]crop=...:0:'{yexpr}'[sc]` with no `eval=frame`. The plan cannot be build-ready while it says “VERIFY which filter actually animates per-frame” and also asks to “keep verified scroll mechanics.” Concrete fix: choose one mechanism in the spec: either add the correct `crop` form with per-frame evaluation if supported by ffmpeg, or avoid animated crop and animate the scroll image with an `overlay` y-expression, which is evaluated per frame. The deliverable must include the exact intended ffmpeg filter-chain change and an acceptance test proving the scroll region changes after the lead hold.

3. [Operator change] / [Proposed change #1-2] Defect: “all the same details as before” is underspecified. The draft moves only `[ SYSTEM