# docs/_ARCHIVE -- parked / stale planning docs (DO NOT resume)

Archived 2026-06-06 so the active build (the OTR video platform) cannot drift onto a parked path. History only -- not a build path.

Moved here:
- `session_handoff.md` -- a STALE session handoff (the exact "ignore stale session_handoff.md" the anti-drift handoff warns about); no code references it.
- `story_pipeline_sprint_plan_v4_audited.md` -- the PARKED story-pipeline build plan; no code references it.

NOTE: the rest of `docs/` was intentionally LEFT in place -- those planning docs are the spec-of-record cited by live node docstrings/tests (e.g. `otr-story-spine.md`, `otr-go-forward-final.md`, `cast-system__go-forward-sprint-plan.md`). They are reference/history, NOT a build path. The ONLY active build is the OTR video platform -- see the repo-root `_START_HERE.md` and `C:\Users\jeffr\Documents\otr-video-roundtable\VIDEO_BUILD_HANDOFF.md`.
