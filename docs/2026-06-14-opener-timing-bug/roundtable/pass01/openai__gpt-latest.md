<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. The immediate symptom is plausible, but the proposed fix is not build-ready: it puts timing in the wrong place, ignores EpisodeAssembler’s existing music mirror path, and would likely create overlap/A/V-sync errors around the opening crossfade.

MUST-FIX BEFORE BUILD:
1. [Proposed fix direction / A] SceneSequencer is the wrong authoritative home for opening-music timing. Grounding: `SceneSequencer.sequence()` has no `opening_theme_audio`, no `opening_duration_sec`, no trimmed/resampled theme length, and no `crossfade_ms`; `EpisodeAssembler.assemble()` is where the opening segment is actually extracted, truncated, crossfaded, and where existing code already stamps/mirrors `ledger.music[]` into `ledger.lines[]`. Concrete fix: make the spec say the authoritative visual timing is produced in `EpisodeAssembler` after the actual opening segment is known, or in an upstream music-cue producer that `EpisodeAssembler` finalizes. Do not stamp master-mix opening timing in SceneSequencer.

2. [Confirmed evidence / Proposed fix direction] The stated duration rule is wrong/incomplete around crossfade. The document says `dur_s = opening-theme length (= first_dialogue.start_s ~= 9.5 s)`, but in `EpisodeAssembler` the scene shift is computed as `opening_segment_len - xfade_samples`; with a 10.0s opening and 500ms crossfade, first dialogue starts at 9.5s while the opening segment itself is 10.0s. Existing music stamping uses `_opening_dur_s = segments[0].shape[-1] / sample_rate`, i.e. full opening segment length, not `_shift_s`. If a `music_open` visual row is `[0,10.0)` and first dialogue is at `9.5`, `plan_timeline_segments()` cannot represent overlap; it will emit the dialogue after the music segment because it only gap-fills when `start_frame > cursor`, then always advances `cursor += n`. Concrete fix: define the opening visual beat duration as the non-overlapping lead-in window ending at scene start (`_shift_s`, i.e. first dialogue onset in master space), unless the composite planner is explicitly changed to handle overlapping visual layers.

3. [Open question 3 / 6] The title-card failure is not explained by the missing `music_open` line alone. Grounding: `_resolve_title_timing()` has a fallback that uses the first dialogue `start_s`; with a dialogue line at `9.5s`, it should set `music_open_start_f=0` and `music_open_end_f≈238` at 25fps even without a music line. Concrete fix: add a required diagnostic/test before coding the music fix: log or assert `_resolve_title_timing()` output for the failing ledger, and log the first `plan_timeline_segments()` segments. If the first segment source is `black`, fix missing/unavailable `base_video_path` or stale floor input. If the first segment is a full-frame `b000` clip, the floor title card will be hidden and needs a separate overlay/bake-in decision.

4. [Proposed fix direction / B] A timed shot alone is not sufficient unless the manifest row carries `start_s`, `target_frame_count`, and the correct visual-composition semantics. Grounding: `plan_timeline_segments()` only considers `manifest["clips"]` rows with `target_frame_count > 0`; it does not read stills on disk. A `b000_music_open` still existing in `stills_manifest.json` is irrelevant unless a clip-manifest row is generated for it. Concrete fix: specify the exact ledger/manifest contract: the opening visual row must be present in the clip manifest with `speaker_role=music_open`, `start_s=0.0`, `target_frame_count=round(_shift_s*fps)`, and a valid `shot_id`/path mapping to the b000 visual asset. verify: the VideoRenderBatch/ImageDirector code that turns ledger lines/shots into `manifest["clips"]`.

5. [Open question 4] The plan misses the sequential-mode trap. Grounding: `plan_timeline_segments()` sets `positioned = target_total_frames is not None and rows and all(r.get("start_s") is not None for r in rows)`. One untimed row in the manifest forces the whole assemble into SEQUENTIAL mode. Concrete fix: if any original untimed `music_open`/`music_visual` row can coexist with the new timed row, either update it in place, delete it, or ensure the manifest builder drops untimed/non-renderable rows. Do not append a second timed music row while leaving an untimed b000 row in the manifest. verify: whether the current manifest contains an untimed b000 row despite `ledger.lines[]` having zero music rows.

6. [Confirmed evidence / root cause] The root cause statement stops one layer too early. Grounding: `EpisodeAssembler` already has code labeled `BUG-LOCAL-107` / `BUG-LOCAL-130` that stamps `ledger.music[]` rows and mirrors them into `ledger.lines[]` with `speaker_role` values. If the final ledger has zero music lines, either `ledger.music[]` had no opening row, the assembler did not run/write before the visual branch read the ledger, or the visual branch used a stale ledger. Concrete fix: make the build task first determine which of these is true. Add a hard post-assembler check: when `opening_theme_audio is not None`, persisted ledger must contain either a valid `ledger.music` opening cue and a mirrored timed `lines[]` row, or the run fails loudly with the ledger path and counts.

7. [Open question 5 / Invariants] The A/V sync risk is under-specified. Adding an opening visual row can double-count duration if `total_target_frames` is computed as a sum of beat durations instead of the master audio frame budget. Grounding: `assemble_silent_timeline()` trusts `manifest["total_target_frames"]` first, then `plan_timeline_segments()` appends tail fill to that target. Concrete fix: require `total_target_frames == round/ceil(master_audio_duration * fps)` from the frozen master, not “sum of all rows after adding music.” Add a regression where opening visual `[0,_shift_s)` plus first dialogue at `_shift_s` yields assembled frame count equal to the existing master budget.

8. [Proposed fix direction / C] Composite-side special handling of “untimed leading music” would be a shim and would duplicate audio timing logic it cannot know. Grounding: `OTR_SilentComposite` sees only `base_video_path` and `clip_manifest_json`; it does not know the actual resampled/truncated opening audio segment or crossfade. Concrete fix: reject C as the root fix. Composite may validate/fail on untimed rows, but it should not infer the opening duration.

SHOULD-FIX:
1. [Open question 2] Decide line-vs-shot data model narrowly. For this bug, use a timed `lines[]` mirror because `video_engine._resolve_title_timing()` and `plan_timeline_segments()` already consume line/manifest timing, and `EpisodeAssembler` already mirrors `ledger.music[]` into `lines[]`. Do not redesign to “shots only” for this bug. Longer-term shot-only may be valid, but it is outside the minimal fix.

2. [Proposed fix direction] Preserve or derive the b000 visual identity explicitly. Existing mirror code copies `"shot_id": _mc.get("shot_id")`; if `ledger.music[]` has no `shot_id`, the visual branch may not find `b000_music_open`. Concrete fix: require the opening music cue to carry the b000 shot/still id, or require the manifest builder to map `music_cue_id=="opening"` to the b000 still. verify: current schema for `ledger.music[]`, `stills_manifest.json`, and manifest generation.

3. [Open question 6] Add a targeted title-card unit/integration test. Concrete fix: for a ledger with only dialogue at `start_s=9.5`, assert `_resolve_title_timing()` returns an open window starting at 0; then render/check first-frame segment source from `plan_timeline_segments()` is floor or an overlay that preserves the title. This isolates title fallback from music-line repair.

4. [Invariants] Add a regression for audio byte identity after the fix. Concrete fix: compare the saved master WAV or post-assembler audio gate before/after; the implementation must only mutate ledger/manifest/video metadata.

5. [Hidden dependencies / sequencing] Make graph ordering explicit. `EpisodeAssembler` emits `audio_done`; visual manifest generation and `OTR_SilentComposite` must depend on it or reload the ledger after it. Concrete fix: document and wire `audio_done` as the force-input gate for any node that reads music timing from the persisted ledger. [ASSUMPTION] The current workflow may already intend this, but the symptom is consistent with a stale pre-assembler read.

OPTIONAL / NICE-TO-HAVE:
- Add a debug dump of the first 5 composite segments: `order`, `source`, `shot_id`, `src_start_frame`, `n_frames`. This would immediately distinguish “black because floor missing” from “floor exists but title not rendered” from “b000 clip hides floor.”
- Add a warning in `plan_timeline_segments()` when positioned rows overlap (`start_frame < cursor`), because current behavior silently serializes them and shifts later beats.

CUT THESE (over-engineering):
1. [Proposed fix direction / A] Cut SceneSequencer stamping. It is safe to cut because SceneSequencer cannot know final opening duration/crossfade and already documents music as passthrough.

2. [Proposed fix direction / C] Cut composite inference of untimed b000 leading music. It is safe to cut because the composite should consume explicit frame-accurate manifest timing; deriving timing there risks drift and violates the “proper root fix” requirement.

3. [Open question 2] Cut a broad “music beats as shots only” schema migration for this bug. It is safe to cut because existing grounded consumers already use timed `lines[]`/manifest rows, and the minimal repair is to make the existing EpisodeAssembler music mirror produce the correct non-overlapping opening visual row.