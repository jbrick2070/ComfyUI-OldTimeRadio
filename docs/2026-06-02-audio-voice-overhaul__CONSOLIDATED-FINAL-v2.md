# OTR Audio + Voice-Casting Overhaul -- CONSOLIDATED FINAL v2 (reproducibility-hardened, code-verified)

**Date:** 2026-06-02 | **Branch:** `v2.0-alpha` | **Status:** A/B/C/C.1 SHIPPED; **R0 + D onward open**
**Supersedes** `...CONSOLIDATED-FINAL.md` (v1). Adds the 5-way reproducibility hardening + verification V23-V29.
**Headline:** V23 shows the legacy audio path is **not** byte-reproducible today -> a new **Sprint R0** prerequisite is promoted ahead of Sprint D. Without it, "legacy = byte-identical" is unenforced and every downstream baseline claim is hollow.

---

## 0. Verification corrections (ground truth)

Carried: **V1-V22** (see v1 §0; all confirmed, none overridden). New this round:

- **V23 -- [DECISIVE FAIL] legacy audio is NOT deterministic run-to-run today.** None of `BatchBarkGenerator` / `KokoroAnnouncer` / `MusicGenTheme` / `BatchAudioGenGenerator` seed python/numpy/torch before the model forward (grep: zero `manual_seed`/`Generator`/`random.seed`). `episode_seed` is a **cache-key string only** (feeds `_cache_prefix` SHA, not the RNG); Bark has no seed widget at all. With `do_sample=True` and no seed they draw from the global torch RNG -> output diverges run-to-run. The PD1 test's `FIXED_SEEDS` (`tests/test_audio_byte_identical.py:45`) is a **no-op**: `_inject_seeds_into_prompt` (`tests/_run_baseline.py:290-305`) only fires `if "seed" in inputs`, and none of the four expose an input named `seed`. Cast/style also need `OTR_CAST_SEED`/`OTR_STYLE_SEED` env (`tests/_run_baseline.py:56-64`). **=> the byte-identical baseline does not exist yet; Sprint R0 must create it (seed the legacy path + bootstrap, then capture) before any "legacy unchanged" claim is meaningful.**
- **V24 -- SDPA backend is not pinned.** No `sdpa_kernel`/`SDPBackend` anywhere; `attn_implementation` (`_otr_model_loader.py:283-301`) is LLM-only and never touches the audio models. Audio attention is dynamic dispatch -> the determinism bootstrap must ADD an explicit SDPA pin.
- **V25 -- [PASS] legacy nodes return native SR; resample is downstream, once** (Bark 24k `:391`, Kokoro 24k, MusicGen/AudioGen 32k; resample at `audio_enhance.py:369` + `scene_sequencer.py:602`). Verbatim delegation cannot double-resample.
- **V26 -- [PASS] cast traits are controlled vocab, Python-assigned.** `timbre in _TIMBRE_VOCAB{warm,sharp,deep,bright,dry,gravelly}` (`_otr_casting.py:466-473`), `role in _ROLE_VOCAB{lead,foil,support,wildcard}` (`:478-484`), round-robin assigned (`:688-689`, "LLM no longer makes these choices"); `age_band in {young_adult,middle_adult,older_adult,ageless}` (`_otr_castplanner.py:19`); `speaker_role` fixed enum. => `stable_cast_seed`'s controlled-vocab requirement is already satisfied (no free-text-LLM determinism hole).
- **V27 -- [NUANCE] MusicGen/AudioGen cache WAVs bypass EpisodeAssembler.** EpisodeAssembler returns `episode_audio` in-memory (`scene_sequencer.py:1100`); the final WAV/MP4 is muxed downstream. But MusicGen `_save_wav` (`musicgen_theme.py`) and AudioGen `_save_wav` (`batch_audiogen_generator.py:170-190`) write per-cue `.wav` directly to `otr/episodes/<ep>/audio/`, and that SHA filename is embedded in the MP4 metadata (C7-load-bearing). => the H2 commercial-clean **filename-mangling must cover the cache-write layer + the downstream mux**, not only EpisodeAssembler.
- **V28 -- [PASS] `episode_audio` fans out to BOTH video nodes as one torch tensor.** Node 7 `episode_audio` `links=[15,78]` -> node 12 `OTR_SignalLostVideo` (15) AND node 51 `OTR_BatchHumoRender` (78); `waveform` is a torch tensor (`scene_sequencer.py:846/1100`). => S0.1 must `.clone()` (torch, not numpy `.copy()`); an in-place downmix in either consumer corrupts the other's input -- the clone guards a cross-node path.
- **V29 -- [NUANCE] current flags set TF32 ON (wrong direction); no determinism flags set.** `torch.backends.cuda.matmul.allow_tf32 = True` + `cudnn.allow_tf32 = True` + `set_float32_matmul_precision('high')` (`_otr_model_loader.py:243-246`); none of `use_deterministic_algorithms`/`cudnn.deterministic`/`cudnn.benchmark`/`CUBLAS_WORKSPACE_CONFIG`/`NVIDIA_TF32_OVERRIDE` exist. => the bootstrap must FLIP the two `allow_tf32` to False AND ADD the full determinism set.

---

## 1. Goal & scope -- unchanged (registry + voice-casting; music + announcer + character; SFX out; stereo deferred). **Reproducibility is now an explicit Locked invariant via R0.**

## 2. Locked decisions (1-8 unchanged). **Add Locked-9:** the legacy/default audio path is byte-reproducible **only after R0**; until R0 lands, treat "legacy baseline" as not-yet-captured. Re-baselines keyed by `baseline_id` (below).

## 3. Sprint R0 -- legacy reproducibility prerequisite [P0 · NEW · FIRST]
The byte-identical premise must be made true before it can be preserved.
1. **Determinism bootstrap, entrypoint-level** (`oldtimeradio_deterministic_bootstrap.py`, imported BEFORE torch/Comfy/transformers): set `CUBLAS_WORKSPACE_CONFIG=:4096:8`, `PYTHONHASHSEED=0`, `NVIDIA_TF32_OVERRIDE=0`, `TOKENIZERS_PARALLELISM=false` in the **process env via the Windows launcher** (cuBLAS reads it at first handle creation; node-code is too late, V23/R3); then after `import torch`: `use_deterministic_algorithms(True, warn_only=False)`, `cudnn.deterministic=True`, `cudnn.benchmark=False`, **flip** `cuda.matmul.allow_tf32=False` + `cudnn.allow_tf32=False` (V29), pin SDPA via `torch.nn.attention.sdpa_kernel(SDPBackend.MATH)` for the audio block (V24). `assert_determinism_env_ready()` hard-crashes the UI if the env var is absent AND asserts `torch.cuda.is_initialized() is False` at assertion time (catches late-set).
2. **Seed the legacy audio nodes** (V23 fix): add explicit per-node seeding (python+numpy+torch+cuda + a `torch.Generator`) derived from a real, persisted `episode_seed`, set BEFORE each `model.generate`. Give Bark a seed input; make `episode_seed` actually seed the RNG (not just the cache key). Persist `episode_seed`/`global_rng_seed`/`audio_rng_seed`/`music_rng_seed` into the ledger root.
3. **`episode_seed` policy:** forbid implicit wall-clock/`secrets`; use content-derived (`sha256(minimized script_json)`) or a visible widget; if "random," persist for replay. Pin `OTR_CAST_SEED`/`OTR_STYLE_SEED` for capture.
4. **Capture the TRUE baselines** (operator, GPU; see Baseline scope below). This replaces the stale `baseline_v1.5.wav`.
5. **Gate:** `legacy_episode_audio_sha_stable_cross_process` -- two fresh-process renders are bit-identical. Until green, no "legacy unchanged" claim holds.

## 4. PD constraints (hardened)
- **PD1 [H1] raw delegation = HARD FORK** (CV5, 4/5): `if profile_id=="legacy_bark_manifest_v1": return _call_legacy_verbatim(script_json, manifest.widgets)` -- no `VoiceRequest`, no registry, no `json.loads->dumps` re-serialize (the legacy node gets the **exact upstream string**, node 62 slot1; V14 covers loads-only, not re-serialize), no normalize/coerce/default-inject/cache/`audio_meta`/commercial-scan/dtype-convert/`canonical_audio(48000)` (V5/V25). Manifest is type-tagged + versioned `{manifest_schema,node_type,legacy_code_sha,widgets:[{index,name,type,value}]}`; tests fail if any GUI default drifts. Golden-WAV `test_legacy_wrapper_is_raw_delegation` (direct vs wrapper SHA, under `tests/golden_audio/`).
- **PD1 baseline scope (CV7):** define `baseline_v2_audio_legacy_sha` (frozen script_json + legacy manifest; legacy VO + announcer; excludes writer/cloud/visual) and `baseline_v2_full_episode_sha` (+ all-node manifest + pinned media encoders + fixed visual seeds). `baseline_id = hash{profile_name, engine_profile_set_hash, quant_schema_hash, determinism_mode in {bit_exact, epsilon_<eps>}}`. Any change -> new `baseline_id`; old baselines are legacy-only, never silently "compatible."
- **PD2 VRAM:** `AUDIO_ENGINE_LOCK` + single-residency `AudioEngineSession`; **[V22/FM2]** adapter base-class teardown -- `load()` returns a patcher OR registers every object in `_loaded_objects`; `__exit__` `del`s each + `gc.collect()` + `empty_cache()` (mirror `_unload_bark`; mm won't free out-of-band caches) BEFORE the `OTR_UnloadAll`-gated video load (V21); `defrag` before-load AND after-unload; **[FM3]** wrap the entire engine forward in pinned `torch.autocast` + `cudnn.allow_tf32=False`; measured peak (TF32-off, SA full T5 pipeline, post-video-load).
- **PD6:** no LLM calls added.
- Guardrails: `engine` key clears b6; avoid `_s28`.

## 5. Architecture -- carried: `[H3]` stable list + `DEFAULT_ENGINE_BY_ROLE`; `[H2]` fail-closed `assert_usable`; `[H7]` typed requests, interface-blind; **engine profiles** as dropdown identity (D5).

## 6. Voice-casting subsystem (carried from v1 §5; V18 byte-safe engine-complete cast) -- hardened:
- **H5 canonical seed** (CV10): `stable_cast_seed = sha256(canonical_json({schema,episode_seed:int,casting_policy_version,char_id:canonical,gender:lower,timbre:lower_or_"",role:lower_or_"",age_band:lower_or_""}))`; null/missing -> `""`; `age_band` key kept forever as `""` if dropped (re-cast only on a `casting_policy_version` bump). Controlled vocab confirmed (V26).
- **H8 one frozen `ResolvedVoiceRequest`** -> the SOLE source of both the cache key and the engine input (CV6, 5/5): `cache_key=sha256(canonical_json(asdict(req)))`, `audio=engine.generate(req)` -- same object. **Single quantizer** (`config/audio_param_quantization.yaml`, per-param `{mode:integer_tick|hex|enum,precision,min,max,cache_key_role:IN_KEY|IGNORED}`): every path runs `widget->quantizer->canonical->(cache string AND engine input)`; the engine never sees a raw widget float. **integer-tick default** (`q=round(x*10**p)`, key on `q`, feed `float(q)/10**p`), hex reserved per-param for bit-faithful (resolves DIS1, 4-1). Every field labeled IN_KEY/IGNORED; test fails on an unlabeled new field; reject NaN/inf at assignment.
- **`stable_line_seed_v1`** = `sha256(canonical_json({schema,episode_seed,cast_lock_revision,line_id,char_id,prepared_text_sha256,engine_profile_id,voice_ref_id,delivery_profile_version}))` (V11-compatible).
- **Per-invocation engine seeding** (CV8): inside `deterministic_inference`, `engine_seed=sha256(engine_name+stable_line_seed)`; set torch/cuda/numpy/HF-Generator before the forward, restore after. (Pilot: does each engine honor an external generator? -- §9.)
- **H2/H7 commercial gate** (carried) + **V27 fix:** filename-mangling at the **lowest write layer** (`_save_wav` cache paths + `save_audio_to_workspace`), not only EpisodeAssembler; every adapter writes a boolean `commercial_clean` into `audio_meta` (validate presence); override raises at assignment; `allow_noncommercial` never bypasses `assert_release_clean`.
- **H3** re-run the char_id subset validator immediately before the first audio-gen node (not only pre-freeze), compare `cast_lock_revision`. **H4** verify `voice_ref_id` resolvable by the target engine's bank, else raise. **H6** explicit cross-character collision filter (announcer exempt). **H10** announcer pinned `bm_george`. **H11** always write a `profile_id` (even `legacy_bark_v1`) + persisted seeds + the §4 version block. **H12** split `voice_clone_allowed` from `license_allows_model_distribution`.
- **Delivery profiles:** neutral-only plumbing in v2 (identity no-op; the only selectable option), populated profiles to v2.1.

## 7. Sprints (order)
**R0** (above) -> **D** generic nodes (hard-fork delegation; verify legacy doesn't internally resample -- PASS V25; tests passthrough+no-mutation) -> **D5** engine profiles (Pydantic; hash the RESOLVED merged YAML, not raw bytes) -> **E.0-E.5** voice-casting (engine-complete H1 + H5 seeds + H6 ladder + H7 overrides + H2 gate + H3/H4 validators + H12 provenance; E.0 add `voice_ref_id`/`commercial_clean`; new caster treats a missing trait as "any") -> **S0.1** HuMo 16k-mono (node-51-internal, Whisper-only, **`.clone()` mandatory** V28, preserve duration V16) -> **F** GPU pilots (deps + flash_attn hard-fail + determinism vs real forwards + render-twice cross-process + VRAM peak + Chatterbox param pin + watermark disabled) -> **G** inference + H8 cache + line seeds -> **H** stereo only if a native-stereo voice exists -> **I** profile-named baselines + `assert_release_clean` pre-flight.

## 8. Decision calls (my recommendations -> your confirm)
- **D1** keyword v2; B.2 on `arc_phase`+`speaker_role`; LLM-emitted rejected; controlled-vocab is an H5 invariant (V26).
- **D2** profile-named sequenced re-baselines.
- **D3** defer S13 (V12).
- **D4 [tiered metric, confirm]:** Tier 0 legacy = SHA-exact, no epsilon ever. Tier 1 opt-in = SHA-exact **raw adapter PCM** (measure at the adapter, pre-resample/mix). Tier 2 reduced-determinism (only under `reduced_determinism=true`): length-equality hard precondition, then `max|delta| <= eps_LSB` AND MAE/RMS bound, dither off + rounding pinned, **and** the Whisper-encode-boundary invariance test must pass or epsilon is disallowed for any HuMo episode (a toleranced master shifts a Whisper frame -> video desync). Set epsilon **principled** (`1 LSB at baseline bit depth`), use the 20-50-episode cross-process drift campaign only as DIAGNOSIS, never to rubber-stamp the 95th percentile. Four replay modes (same-proc, fresh-proc, cache-hit, cache-disabled); if only cache-hit passes, the cache is hiding nondeterminism. An engine not bit-identical across two sequential same-process renders is **disqualified** from pinned baselines.
- **D5** engine-profile sprint between D and casting.
- **D6** delivery profiles: neutral-only in v2.
- **D7** keep age light (10 pts); legacy untouched.
- **DIS5 [confirm] PIN the exact torch nightly build** (record the build hash; "cu130 nightly" is not a pin); any torch change = hard re-baseline + Sprint-F import hard-fail; no silent auto bit-exact->epsilon fallback.

## 9. Verify-in-pilot (not repo-resolvable)
Each new engine honors an external `torch.Generator`/seed (CV8); strict-determinism survival + flash/SDPA non-determinism on sm_120 (drives D4); Chatterbox param names; Stable Audio T5 size + no enhancer. Everything else from all five reviews' verify lists is RESOLVED in §0 (V1-V29).

## 10. Re-baseline trigger union (CV7, supersedes all prior lists)
Casting/content: `casting_policy_version, delivery_profile_version, delivery_version, engine_profile_id+sha, voice_bank_id+sha, manual_override_sha, legacy_manifest_sha, param_quantization_schema_sha, cache_schema_version, SR-ladder`. Models/code: `model_sha256 (weights) + engine_impl_version (code) + custom_node_git_sha + engine_adapter_git_sha + ComfyUI version + exact torch build hash + CUDA runtime + cuDNN/cuBLAS + xformers + flash_attn (record absence) + ffmpeg + SR-converter`. Determinism: a versioned `determinism_profile` hash {CUBLAS, use_deterministic+warn_only, cudnn.benchmark/deterministic, TF32 flags, pinned SDPA, pinned autocast dtype, determinism_mode}. Environment: NVIDIA driver, GPU+compute-capability (sm_120), OS. Identity: `baseline_id` + persisted seeds. Rule: any field change -> new `baseline_id`; never silently compare against an old baseline.

## 11. Tests (additions)
`legacy_episode_audio_sha_stable_cross_process` (R0 gate); `golden_wav_legacy_direct_vs_wrapper_sha`; `cache_consistency_direct_uncached_vs_cached_hit` (+ engine sees canonical floats); `all_request_fields_in_key_or_ignored`; `clone_before_downmix_mutation_proves_test`; `eviction_completes_before_video_load`; `engine_two_sequential_renders_bit_identical_else_disqualified`; `whisper_encode_boundary_invariant_under_eps`; `commercial_mangle_covers_cache_write_paths` (V27); plus the round-3 set.

## 12. Evidence appendix (this round)
no legacy seeding `batch_bark_generator.py:418/236`, `kokoro_announcer.py:209/113/233`, `musicgen_theme.py:528/186/791`, `batch_audiogen_generator.py:248/118/475`; FIXED_SEEDS no-op `tests/test_audio_byte_identical.py:45` + `tests/_run_baseline.py:290-305/56-64`. SDPA absent (grep). native SR `batch_bark_generator.py:391`. vocab `_otr_casting.py:466-473/478-484/688-689`, `_otr_castplanner.py:19`. cache writes `musicgen_theme.py:_save_wav`, `batch_audiogen_generator.py:170-190`. fan-out node7 links 15(->12)+78(->51), torch tensor `scene_sequencer.py:846/1100`. TF32 on `_otr_model_loader.py:243-246`.

**Commits this track:** A `9b76d78`, B `1b5a39b`, C `c79cc51`, ROADMAP `2161439`, C.1 `f49d4f9`. Full `tests/` green (3440 / 12 skipped).
