<!-- requested_model: deepseek/deepseek-v4-pro | resolved_model: deepseek/deepseek-v4-pro-20260423 -->

VERDICT: build-ready? **no**. The default policy `preserve_ledger` prevents the promoted IndexTTS2 from being used on most characters, the CastLock stamp is missing the concrete `voice_ref_path` field for clone engines forcing an unreliable render‑time re‑solve, and the best‑effort resolver has a known failure mode that leads to silent bark fallout. Several gaps (no reuse warning, no commercial‑clean gate enforcement) make the system fragile for operators and future engines.

MUST-FIX BEFORE BUILD:
1. [Section 4 / `cast_lock.py`] Flip the default `cast_voice_policy` to `"auto_registry"` so that IndexTTS2, the promoted default character engine, actually gets used. The current `_CAST_POLICIES = ("preserve_ledger", "auto_registry")` makes `preserve_ledger` the first entry → default. Change `_CAST_POLICIES` to `("auto_registry", "preserve_ledger")` or explicitly set `"default": "auto_registry"` in `INPUT_TYPES` (file `cast_lock.py:86`).
2. [Section 2,5 / `cast_lock.py` `_auto_registry` + `_stamp`] CastLock must stamp `voice_ref_path` directly onto the cast row for clone engines (`indextts2`, `chatterbox`). After `self._stamp(entry, ref)` in `_auto_registry`, add `if ref.engine in ("indextts2", "chatterbox"): entry["voice_ref_path"] = ref.ref_path`. This eliminates the need for `_resolve_clone_ref_path` in the `auto_registry` path and guarantees the voice node finds the concrete path immediately (grounded: `_stamp` at `cast_lock.py:325` only sets `voice_ref_id`, `voice_engine`, `commercial_clean`; the voice node dispatch at `_otr_voice_node_common.py:219` tries `cast.get("voice_ref_path")` which is missing).
3. [Section 5 / `_otr_voice_node_common.py` `_resolve_clone_ref_path`] The best‑effort resolver is observed to return `None` for rows that *should* have a WAV on disk (e.g., 3‑female cast with one female ref on disk, `reuse=True`). This is a latent bug – likely gender missing from the cast row or the ladder logic failing to permit reuse. Must debug and harden: add a clear `log.warning` when gender is missing (so bark fallback is intentional), and verify that `assign_voice_for_slot` with `allow_voice_reuse=True` actually returns a valid entry when the female ref is present and gender is `"female"`. If it still fails, fix the ladder/reuse check. (Observed behavior: “the resolver returned None for some rows despite reuse=True” – risk of silent degradation even after MUST-FIX #2 for `preserve_ledger` users.)

SHOULD-FIX:
1. [Section 8 / `_otr_voice_bank.py` + `cast_lock.py`] Emit a loud structured WARNING when a voice is reused. In `assign_voice_for_slot`, after choosing a candidate when `allow_voice_reuse` is true, log `log.warning("reusing voice %s for char %s", ref.voice_ref_id, char_id)`. In `_auto_registry`, before stamping, if `ref.voice_ref_id in used` (detected before adding to the set), log a similar warning in the report and to `log.warning`. This makes operator‑visible when distinctness degrades due to bank scarcity.
2. [Section 6 / `cast_lock.py`] Enforce the commercial‑cleanness gate for builds. Add an optional input `require_commercial_clean` (default `False`) to `CastLock`. In `_auto_registry`, pass `require_commercial_clean=require_commercial_clean` to `assign_voice_for_slot` and/or after the loop, if `gated > 0` and `require_commercial_clean`, raise `ValueError` instead of a non‑blocking warning. This prevents accidental shipping of non‑clean voices while keeping drafts usable by default.
3. [Section 2 / `_otr_voice_node_common.py`] Remove the fallback `or cast.get("ref_path")` in the `voice_ref_path` branch (line 219). The dispatch should rely solely on `cast.get("voice_ref_path")`; CastLock is now the only writer of that field. The `ref_path` fallback masks a missing stamp and can cause inconsistent behaviour. Clean it up to reinforce the single‑source‑of‑truth contract.
4. [Section 1 / `eng_kokoro.py` + announcer path] Clarify that `auto_registry` will override the per‑episode seeded announcer voice pick (Kokoro’s `begin_episode`) with the bank‑provided `voice_ref_id`. The announcer voice then becomes deterministic (lowest `voice_ref_id` in bank) rather than random per episode. If this behaviour change is unintended, adjust the announcer resolution logic to prefer `voice_ref_id` from cast only when it is explicitly set by the user, otherwise fall back to the seeded pick.
5. [Section 5 / documentation] Document that the `preserve_ledger` path (now opt‑in) **requires** every cast row to have a `gender` field populated, otherwise the clone‑engine fallback resolver will deliberately fall back to bark (with a log message). This is not a bug but a contract that must be met by the writer ledger.

OPTIONAL / NICE-TO-HAVE:
- Create a formal “add a voice engine” checklist covering: define `voice_ref_field`, ensure CastLock stamps the concrete field (path if clone engine), provide any per‑engine hook (`begin_episode`, `prepare_text`), set `commercial_clean`, and decide whether the engine needs an explicit bank entry or an internal preset.
- Future harden the gender model to support non‑binary / unspecified without breaking determinism – the seeded encoding already includes gender as a nullable string, so an absent gender would just change the seed, which is acceptable; the hard floor could be removed for engines that do not require gender (e.g., voice conversion).

CUT THESE (over-engineering):  
None. The `_resolve_clone_ref_path` is currently needed for `preserve_ledger` and as a safety net, but could be removed if `preserve_ledger` is eventually deprecated; no immediate over‑engineering.

[ASSUMPTION] that `legacy_first_engines("char_voice")` returns `indextts2` as the very first element when `_otr_engine_profiles` is updated after the promotion. Verify with the engine‑profile configuration; otherwise the `auto_registry` default would not pick IndexTTS2 as the target engine.  
[ASSUMPTION] that the `announcer_voice_ref` pinning the single bank voice is an accepted behavioural change (announcer voice no longer random per episode). If this is a regression, the code in `eng_kokoro.py` `generate_voice` needs to give higher priority to the per‑episode seeded pick when the bank assigns a voice.