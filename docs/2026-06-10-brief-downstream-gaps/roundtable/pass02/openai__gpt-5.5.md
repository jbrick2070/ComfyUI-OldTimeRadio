<!-- requested_model: openai/gpt-5.5 | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. Grounding shows at least one explicit F2 requirement is contradicted in code, and F1 precedence semantics do not match the stated tests.

MUST-FIX BEFORE BUILD:
1. [F2] `_provide_lipsync_base` does not implement the specified prompt precedence. F2 says lipsync base should prefer the request's existing finished `text_prompt` when non-default, with `OTR_LSYNC_BASE_PROMPT` verbatim as the override. Grounding `render_driver._provide_lipsync_base` explicitly does the opposite: it always uses `OTR_LSYNC_BASE_PROMPT` or `_LSYNC_BASE_PROMPT`, with a comment rejecting the F2 decision. Concrete fix: implement `base_req["text_prompt"] = os.environ["OTR_LSYNC_BASE_PROMPT"]` when set; else use `request["text_prompt"]` if present and not equal to the generic `build_request` default; else use `_LSYNC_BASE_PROMPT`. Add the missing CPU test in F5 that captures the base request prompt for env-set, request-non-default, and default cases.
2. [F1/F5.1] `get_era_tail` does not honor the stated precedence. F1 says `atmosphere_line -> visual_palette -> v1 lighting -> default`, and F5 says “v2 precedence (atmosphere_line wins).” Grounding `get_era_tail` appends all non-empty sources: atmosphere_line, palette, and v1 tail. That means atmosphere_line does not “win”; it is concatenated with lower-precedence fields. Concrete fix: make this an exclusive fallback chain: return stripped `atmosphere_line` if non-empty; else return joined top-3 palette if non-empty; else return v1 lighting; else default. If concatenation is actually intended, rewrite F1 and F5 to stop saying “precedence” and “wins.”
3. [F1] `finish_visual_prompt(max_chars=...)` does not guarantee the max length for all valid inputs. Grounding reserves clause budget, then uses `max(budget, 20)` and re-appends `", no on-screen text"`, so small `max_chars` values can return a string longer than `max_chars`. Concrete fix: after re-appending the preserved clause, assert/enforce `len(out) <= max_chars` whenever `max_chars` is truthy, or document that `max_chars` must be large enough to hold the preserved clause plus a minimum subject. The current F1 contract has no such limitation.

SHOULD-FIX:
1. [F1] The no-text preservation logic is broader than the spec. F1 says a trailing `"no on-screen text"` clause in `prompt` is preserved. Grounding uses `NO_TEXT_CLAUSE in base` and `base.replace(...)`, so any occurrence anywhere is removed and moved to the end, including non-trailing text. Concrete fix: only preserve when the normalized prompt ends with the clause, and remove only that trailing clause.
2. [F4] Doc rot remains outside the two locations named in F4. Grounding `_otr_story_brief_helpers.py` module docstring still says `nodes/musicgen_theme.py` is wired to import `get_story_brief_music_mood`, while the function docstring says that consumer no longer exists. Grounding `log_story_brief_disposition` docstring still lists canonical consumer IDs `flux_env`, `ltx`, `humo`, `musicgen`, but F1/F5 now require `ltx_scene_open`, `shotlock_m4`, and `flux_portrait`. Concrete fix: update the module-level docstring and `log_story_brief_disposition` docstring to match the new live lanes and IDs, not only the `get_story_brief_music_mood` docstring.
3. [Acceptance/F5.5] The acceptance log for `[story_brief:ltx_scene_open]` does not prove the scene prompt branch ran. Grounding `run_real_episode` logs `ltx_scene_open` once before `apply_engine_override` and before any shot is inspected, so the line appears even if there are no `ltx_video` / `wan_i2v` scene prompts. Concrete fix: acceptance should also assert the render-driver “LTX SCENE ... composed from the episode brief” log or inspect at least one generated request prompt from a text-engine shot.
4. [F4] `OTR_LedgerScriptWriter.py ~3988` is not grounded here. verify: the stale comment about `compose_shot_prompt` appending era/style tails was actually replaced with the `finish_visual_prompt` seam description.
5. [F5.4] The render-driver tests listed do not cover the F2 lipsync-base requirement. This gap allowed the grounded `_provide_lipsync_base` contradiction to survive. Concrete fix: add an explicit unit test for `_provide_lipsync_base` prompt selection as described in MUST-FIX #1.

OPTIONAL / NICE-TO-HAVE:
- [F1] Add a test where `prompt` is empty and confirm `finish_visual_prompt` returns `""`, because the grounded implementation intentionally “never invents a subject” but F1 does not state that behavior.
- [F2] Define the generic default prompt as a named constant so “non-default” lipsync prompt detection is not string-literal fragile.

CUT THESE (over-engineering):
None recommended. The expensive acceptance render is heavy, but it is the only item here that validates actual prompt appearance through the live rendering path; cutting it would remove coverage of the stated visual goal.