VERDICT: GO-WITH-FIXES

MUST-FIX (blocks convergence):
1. [ComfyUI-OTR-UpstreamStoryLab/src/upstream_story_lab/contracts.py:25-42 vs r1_input.md:172-186] The Phase A "12 seams" list in ANCHOR 1 (interpret, outline_system, pitch_room_system, story_select_system, dramatic_state_system, line_grounding, casting_brief_seam, coda, title_system, style_pick_inventor, style_pick_chooser, labels) does NOT match the actual `TEMPLATE_SEAMS` tuple at contracts.py:25-42, which has 14 entries and different names (`coda_system` not `coda`; `style_pick_chooser_user_template` split out; no `interpret` or `labels` or `casting_brief_seam` entries -- those are handled as separate mechanisms: `interpret` is an interpreter binding not a template seam, `labels` is `LABEL_TEMPLATE_VARIABLES`/`BankDefaults` fields not a seam, `casting_brief_seam` is a `StoryInputPacket.casting_brief` content field). Plus 4 pipeline-only seams (`pass_1..pass_4`) that are explicitly out of Phase A scope (experimental, cut to docs-only per ANCHOR 1 section 9). Before r2 elaborates chunks, the plan doc must replace the loose "12 seams" prose list with the real, disjoint vocabulary: 10 template seams (TEMPLATE_SEAMS minus the 4 experimental passes) + labels-as-variables + interpret/casting_brief as non-template content fields. Otherwise a chunk plan built against the wrong list will build the wrong loader keys.

2. [ComfyUI-OTR-UpstreamStoryLab/src/upstream_story_lab/registry.py:245-307 vs r1_input.md (g)] The real architecture is NOT the minimal `get_prompt(profile_id, site_key)` function floated in question (g). It's already `Registry.resolve()` (4-axis resolution with `Resolution` audit trail) + `profiles.resolve_profile()` (bank-default merge with fail-loud missing-label/coda checks) + per-seam `string.Formatter` template-variable validation at load (registry.py:47-63). A Phase A that ships only a flat `get_prompt()` function would have to be rebuilt at Phase B; the plan should state explicitly that Phase A adopts (a subset of) the already-built lab shape rather than a new minimal function, to avoid throwaway work.

SHOULD-FIX (before r2 elaborates chunks):
1. [ComfyUI-OldTimeRadio/nodes/_otr_style_picker.py:296,317ish (INVENTOR/CHOOSER) vs contracts.py:60-69] `_INVENTOR_SYSTEM` in production is a bare constant string with NO runtime variable substitution today (confirmed: `_INVENTOR_SYSTEM = ("You are a sci-fi radio drama showrunner.")` -- zero `{}` placeholders), while `_INVENTOR_USER_TEMPLATE` is the one with `{n_required}` etc. The lab's `SEAM_RUNTIME_VARIABLES["style_pick_inventor"]` declares `n_required/seed_sample_block/article_excerpt` as if they belong to the system-prompt seam. Flag for byte-identical extraction: confirm which seam (system vs user-template) each variable set actually binds to before Phase A locks the seam-to-variable map, or the loader's per-seam allowlist will reject a legitimate template at load time.
2. [r1_input.md:58-60 (c) vs registry.py, compat.py] The compat mirrors (`NEWS_BRIEFS_FIELDS`, `NEWS_SEED_KEYS`, `MOTION_ROLE_KEYS`, `PRODUCTION_VISUAL_TAILS`) are bridge/ledger-stamping concerns (used only in bridge.py, registry cross-validation of visual styles) -- Phase A (byte-identical prompt-string extraction only) does not need them. Confirm this explicitly in the Phase A chunk list so no one imports compat.py drift tests into a Phase A PR by habit.

GROUNDING TABLE:
| claim | source file:line I checked | CONFIRMED / MISREAD / UNVERIFIABLE |
|---|---|---|
| `_otr_outline.py:532 _SYSTEM_PROMPT` prompt site is real | ComfyUI-OldTimeRadio/nodes/_otr_outline.py:528 (`_SYSTEM_PROMPT = """\`) | CONFIRMED |
| `_otr_line_composer.py:3275 _NEWS_CODA_SYSTEM` is real | ComfyUI-OldTimeRadio/nodes/_otr_line_composer.py:3274 | CONFIRMED |
| `_otr_style_picker.py:296 _INVENTOR_SYSTEM` is real | ComfyUI-OldTimeRadio/nodes/_otr_style_picker.py:295 | CONFIRMED |
| 12 story packs exist (science_news=1, media_archive=5, public_domain=5, experimental=1) | ComfyUI-OTR-UpstreamStoryLab/fixtures/story_packs/** (dir listing) | CONFIRMED |
| 5 visual styles match named ids | ComfyUI-OTR-UpstreamStoryLab/fixtures/visual_styles/*.json | CONFIRMED |
| Fable must-fix 1/2 (catalogs.py content duplication) resolved | glob for catalogs.py under UpstreamStoryLab returned zero files; profiles.py:31-96 builds StoryPromptProfile entirely from `pack.prompt_stages`/bank defaults, no literals | CONFIRMED |
| Fable must-fix 3 (meta.news mirror shape wrong) resolved | bridge.py:120-166 builds news dict with exact NEWS_BRIEFS_FIELDS keys + hard-errors on drift | CONFIRMED |
| Fable must-fix 4 (archival_documentary scene_broll dead role) resolved | fixtures/visual_styles/archival_documentary.json:20-24 -- keys are announcer/music_open/music_close/music_inter | CONFIRMED |
| NEWS_BRIEFS_FIELDS pin (13 fields) matches real NewsBriefs class | ComfyUI-OldTimeRadio/nodes/news_interpreter.py:152-172 | CONFIRMED |
| MOTION_ROLE_KEYS pin matches real render_driver dict | ComfyUI-OldTimeRadio/nodes/_otr_video_engines/render_driver.py:529-537 | CONFIRMED |
| 12-seam list in r1_input.md matches contracts.py TEMPLATE_SEAMS 1:1 | contracts.py:25-42 (14 entries, different names) vs r1_input.md:171-186 | MISREAD (see MUST-FIX 1) |
| `_INVENTOR_SYSTEM` in production takes runtime template variables | `_otr_style_picker.py:295-297` -- plain string, no `{}` placeholders | MISREAD-adjacent (SHOULD-FIX 1) |
| `casting_brief_seam` is a template seam like the other 11 | contracts.py TEMPLATE_SEAMS (no such key); StoryInputPacket.casting_brief (contracts.py:295) is a content field | MISREAD (wrong category -- MUST-FIX 1) |
