<!-- requested_model: deepseek/deepseek-v4-pro | resolved_model: deepseek/deepseek-v4-pro-20260423 -->

VERDICT: no -- The plan lacks a concrete rubric, spine-update mechanism, and integration with existing deterministic L1/L2 and freeze cascade; the central assumption that a weak local model can reliably grade and improve a story spine is unvalidated and contradicts the panel's prior findings.

MUST-FIX BEFORE BUILD:
1. [Grade pass] The rubric is undefined. A "10th-grade-English-class B or above" gate is subjective and untuned for the structural defects the plan aims to address. Fix: define a fixed, legible rubric (e.g., premise specificity, on-stage climax, character want clarity, non-genericness) with explicit pass/fail criteria and embed it in the grading prompt. Without this, convergence cannot be evaluated.
2. [Spine Update Mechanism] The process for applying structural feedback to rewrite the outline spine is entirely unspecified. The plan says "Apply that feedback by rewriting the OUTLINE/BEAT SPINE" but does not state how (LLM call? deterministic modification?). The mechanism must be specified, including how it preserves budget, cast, and other constraints. Without it, the loop is a black box incapable of implementation.
3. [Integration with L1/L2] The plan does not account for the existing deterministic beat‑shaping (L1/L2 `_otr_story_quality_l12.py`) that already fixes structural sameness by modifying beat intents and assigning roles/conflict objects. Must state: (a) whether this loop runs after L1/L2 is applied, (b) whether it preserves the L1/L2 modifications, and (c) if it re‑applies them after spine rewrite. Otherwise the deterministic improvements risk being undone or conflicting.
4. [Integration with freeze cascade and existing loops] The loop must be placed within the pipeline relative to the freeze cascade's story critic + targeted reroll (`_otr_reroll.py`). The plan says it is a “superset at the structural layer” but does not specify ordering. Fix: define whether it runs before the critic/reroll, after, or replaces them, and how it avoids two competing quality loops. The freeze cascade's reroll loop already iterates on lines; the new spine‑level loop must not duplicate or conflict.
5. [Grading Reliability on Weak Models] The plan assumes the local model (gemma-12b/mistral-nemo) can assess story quality and produce structural feedback that improves it. The panel's prior finding that a weak model cannot self‑correct on reroll directly challenges this. The plan does not propose validation, calibration, or a fallback. Fix: include a validation step (e.g., a small human‑annotated rubric calibration set, or a regression test showing that L1/L2‑enabled spines consistently grade higher than non‑L1/L2 spines) before committing to build the loop. Without it, the loop may degrade quality.
6. [Determinism and seed‑keyed reproducibility] The invariant requires seed‑keyed determinism, but the plan does not address how the improve pass (an LLM call) will be made reproducible. Fix: specify that the improve call uses temperature 0 and a deterministic seed derived from the run seed, and that the spine update (if LLM‑based) likewise is seeded. Otherwise multiple runs with the same seed may diverge.

SHOULD-FIX:
7. [Convergence guard] Add a NO‑REGRESSION guard that keeps the best‑graded spine across iterations and a divergence‑detection stop (like the existing reroll loop's halt on increased flag count) to prevent quality degradation.
8. [Placement before freeze] Clarify that the loop runs BEFORE the freeze cascade's critic/reroll, so the freeze cascade receives the final spine and critic can still act as a second quality gate. The spine‑rewrite should happen before the freeze's gap audits.
9. [best-of‑N vs iterate‑until‑good] Because local passes are free, consider generating N spines from one seed (via deterministic variation) and picking the best‑graded instead of iterating. This avoids iterative drift and is simpler, while still targeting structural diversity.

OPTIONAL / NICE-TO-HAVE:
- Feed the rubric scores and structural feedback back into the deterministic L1/L2’s domain palette or beat‑role rules as a batch learning signal between runs (offline) rather than online iteration.
- Extend the “improve” prompt with examples of good vs. bad spines to bias the weak model toward the desired structural pattern.

CUT THESE (scope / over-engineering):
- The entire loop may be redundant given the deterministic L1/L2 already addresses structural sameness. The panel should first run a controlled experiment (L1/L2 ON vs. OFF) to quantify the remaining sameness. If L1/L2 suffices, the loop should be cut to avoid unnecessary complexity and potential regression.
- If the loop remains, consider eliminating the re‑slug step and instead allowing the spine‑update to output modified beat intents that the line composer can re‑compose selectively, avoiding a full re‑composition of all lines when not needed.

[ASSUMPTION] The local model can evaluate a story ledger and produce a grade consistent with a human‑defined rubric; verify with a calibration experiment.
[ASSUMPTION] The structural feedback from the improve pass can be automatically applied to generate an improved spine without breaking budget or cast constraints; verify by defining the mechanism and running a sample.
[ASSUMPTION] Re‑running the line composer on an updated spine with the same seed will produce coherent dialogue and not introduce line‑level defects that outweigh the structural gains.
[ASSUMPTION] The extra LLM calls and re‑composition fit within local VRAM and time budgets; verify with profiling before commitment.