# R1 judgment (high-level arc / creative coherence)

Panel: GPT-5.5, DeepSeek-v4-pro substantive; Gemini-3.1-pro TRUNCATED (~455B, reasoning-model cutoff --
not re-run; GPT+DeepSeek covered the space). Spend $0.2179.

## ACCEPTED (grounded CONFIRMED)
- **GPT: "frozen ledger" is wrong vs the cascade.** CONFIRMED -- `run_story_critic` runs pre-Phase-10
  freeze; a true LFC freeze is later. -> the loop operates on a COMPOSED LEDGER CANDIDATE, BEFORE Phase-10
  freeze and BEFORE audio. Re-worded.
- **GPT/DeepSeek: "update the spine" undefined vs real `_otr_outline` (Path C: macro/phase/fleshout/
  combiner + Outline/Beat + budget validation + turning_point/button + dialogue_slot_id).** CONFIRMED.
  -> must name the exact mutable artifact + recompute path. Deferred to R2/R3 as the central build question.
- **DeepSeek + my anchor: LLM letter grade by the weak writer is theater.** ACCEPTED -> HYBRID: a
  DETERMINISTIC rubric (reuse L1/L2 `ungrounded_crisis` density + on-stage-climax/beat_role presence +
  distinct-conflict) decides PASS/FAIL; the LLM only PROPOSES the structural fix.
- **DeepSeek CUT (strongest point) + my anchor: measure L1/L2 ON-vs-OFF FIRST.** ACCEPTED as the
  PREREQUISITE -- if the deterministic L1/L2 already collapses the sameness, this loop is cut. This is the
  measurement soak already deferred to the operator.
- **No-regression keep-best + hard cap; best-of-N SELECTION over iterate-until-good for v0.** ACCEPTED
  (DeepSeek SHOULD + my anchor) -- selection cannot drift; iteration is the v1.
- **Determinism: seeded, temp-0 improve call; seed-derived spine variation.** ACCEPTED.
- **Build ON TOP of L1/L2, preserve its mutations, re-apply after any spine rewrite.** ACCEPTED.

## REJECTED / DOWNGRADED
- DeepSeek "human-annotated calibration set": good science but offline/out-of-scope for a local-first
  build; replaced by the deterministic-rubric approach (no human set needed if the gate is measurable).
- "Feed scores back into the L1/L2 palette as batch learning": NICE-TO-HAVE, parked.

## CONVERGENCE CALL
R1 converged on a REFRAME: this is NOT "ship the loop" -- it is "MEASURE first (L1/L2 ON/OFF), and IF
residual sameness remains, build a local-only best-of-N SELECTION over seed-varied spines gated by a
DETERMINISTIC rubric, pre-freeze/pre-audio, keep-best, building on L1/L2." Carry to R2 (coding plan):
define the mutable spine artifact + the selection mechanism + the deterministic rubric.
