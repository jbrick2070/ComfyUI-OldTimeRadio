<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. The plan still violates the grounded “gutters only on portrait beats” invariant and leaves timing/state inputs underspecified enough that the title-card build can silently no-op or misfire.

MUST-FIX BEFORE BUILD:
1. [OPEN / Landscape-beat gutters] Defect: v1 “COMMITS to dim, gutter-clamped scopes” even on landscape b-roll, but grounding HARD CONSTRAINT #2 says gutter effects are only valid on portrait beats and landscape beats have no gutters. This is not just an eyeball issue; it violates an explicit invariant. Concrete fix: either pass a portrait/landscape frame mask/timeline into `_CRTRenderer` and suppress gutter scopes on non-portrait frames, or cut #2 from v1. If that requires cross-file clip-manifest plumbing, update the plan; do not ship the current “faint edge telemetry on landscape” compromise.
2. [Concrete specs / Timing extractor] Defect: the renderer signature accepts `timing=None`, but the timing schema is not specified. The plan names `music_open`, `music_close`, `first_dialogue_f`, `dock_frames`, and “music-open role” without defining the exact dict keys, frame fields, or allowed `speaker_role` values. “Resolve it the way `otr_caption_burn` does” is not build-ready from the provided grounding. Concrete fix: define a small normalized timing dict produced in `render_video`, e.g. `{open:(start,end), close:(start,end)|None, first_dialogue_f:int|None}`, and enumerate the accepted role strings/predicates. Missing or malformed values must produce `None` windows, not partial state.
3. [Concrete specs / Timing extractor fallback] Defect: fallback says “derive the intro window from the `volume` envelope (music from frame 0 to the first dialogue onset), capped,” but if `start_s` is unavailable there is no reliable first-dialogue onset from the ledger. Concrete fix: specify an implementable fallback: either disable the title card when timed dialogue is unavailable, or use a purely audio-based capped window such as `0 .. min(total, round(N*fps))` with a defined sustained-volume/drop heuristic.
4. [Concrete specs / No `np.roll` on the frame] Defect: “Drift + tear = horizontal coordinate OFFSET applied to the gutter-scope + title DRAW coords only, clamped so each bbox stays inside its gutter” is contradictory for the title card; the title is not in a gutter and the dock target is section-1 text. Concrete fix: define separate bounds: gutter scopes clamp to their gutter-layer rects; title-card/hero/dock tear clamps to its own title/dock bbox or protected center/title band. Do not use “inside its gutter” for title coordinates.
5. [S3 / title card] Defect: “the 2-beat gap-fill smoke” is listed as an implementation item but has no trigger, geometry, draw order, lifetime, or relation to the title-card state machine. Concrete fix: either remove it from S3 or specify exact behavior. As written, it is untestable.
6. [Concrete specs / Signature + HARD CONSTRAINTS #5] Defect: grounding says procgen clock is 24fps, but the spec continues to pass through arbitrary `fps` from `render_video` and only says store `self.fps`. Concrete fix: explicitly enforce/validate that this renderer is driven at 24fps, or document why the existing node input is already fixed to 24fps. [ASSUMPTION] If the node can be configured to 25fps, this must hard-fail or coerce before analysis/rendering.
7. [#1 / Outro bookend + Concrete specs / Outro] Defect: “music_close = last music line” can resolve to the opening music if there is no distinct close role, causing an unintended second title-card state or overlapping/reused open window. Concrete fix: require a distinct close role/predicate, and disable outro unless a valid close interval is distinct from the open interval and `start < end <= total`.

SHOULD-FIX:
1. [S1 / foundation] Defect: `signal[0]=trig[0]=volume[0]` assumes `len(volume) > 0`. Current `_analyze_audio` normally produces frames from nonzero audio, but the constructor spec should still guard empty input. Concrete fix: if `total == 0`, create empty arrays and make `render(fi)` raise a clear bounds error or return a blank frame only if the caller can request one.
2. [Concrete specs / Signature] Defect: after changing `render(self, fi)`, the plan does not explicitly require bounds-checking `fi` against `self.total`. Current closure indexes `volume[fi]`; the new renderer will own those arrays. Concrete fix: in `render(fi)`, validate `0 <= fi < self.total` for CRT frames; leave HUD dispatch outside unchanged.
3. [#1 / title card active window] Defect: “Active window `[music_open_start_f, music_open_end_f + dock_frames)`” does not define `dock_frames`. Concrete fix: specify a deterministic frame count or formula, e.g. `dock_frames = round(0.5 * fps)` clamped to remaining frames.
4. [#1 / state machine] Defect: decode/reveal/pop/dock phase boundaries are described qualitatively but not assigned frame ranges. This will produce inconsistent implementations and fragile tests. Concrete fix: define percentages or frame counts inside the active window, including behavior for very short music-open intervals.
5. [Concrete specs / Text exemption] Defect: the plan requires section-1 ident/title card after vignette/choke, but current grounded code draws section 1 before sections 2-8. This is a real draw-order refactor. Concrete fix: explicitly split section-1/title-card into a post-vignette pass and ensure the title-card state is computed before normal section-1 suppression.
6. [#2 / RIGHT `_draw_scope`] Defect: waveform chunks are raw samples from `_analyze_audio`; the spec says trace them around circumference but does not define normalization/clipping before applying the `amp <= r*0.35` rule. Concrete fix: clamp each sample to `[-1, 1]` before converting to radial displacement.
7. [Concrete specs / Determinism] Defect: the seed formula uses only `{title}|{fi}|{salt}`. If the intended invariant is “deterministic per generation seed,” the generation seed is not included or passed into `_CRTRenderer`. Concrete fix: either change the wording to “deterministic per title/frame/effect and audio arrays” or pass the actual generation seed in `timing`/constructor. [ASSUMPTION] The real generation seed is not visible in the provided excerpt.
8. [#2 / graticules + center-band clip] Defect: “alpha_composited” is stated, but the spec does not say whether graticules are full-frame or gutter-local. Full-frame precomputed graticules would violate the center-band clip rule. Concrete fix: precompute gutter-local RGBA graticules sized to each gutter rect, then composite only at `(0,0)` and `(right_x,0)`.
9. [Concrete specs / Timing extractor] Defect: “Missing fields DISABLE that effect” needs precedence rules. If open timing is missing but close timing exists, or vice versa, the title/outro behavior is undefined. Concrete fix: open title requires valid `music_open`; outro requires valid distinct `music_close`; missing `first_dialogue_f` only disables fallback/dock constraints, not the whole renderer.
10. [S5 / regression] Defect: “audio-byte-identical” is underspecified for a visual renderer change because muxing/ffmpeg metadata can differ even if audio samples do not. Concrete fix: compare extracted audio stream bytes or decoded PCM hash, not whole mp4 bytes.

OPTIONAL / NICE-TO-HAVE:
- [#2] Define exact gutter rects from current `W,H` rather than hard-coded portrait examples; keep the 480x832-derived numbers as comments/tests only.
- [#1] Add one deterministic long-title fixture with punctuation and very long words, because monospace `textbbox` wrapping can fail on unbreakable tokens.
- [S1] Convert `volume`, `freqs`, and `waves` to known internal forms in `__init__` to avoid list/ndarray truth-value surprises.

CUT THESE (over-engineering):
1. [#1 / Outro bookend, S5] Cut the conditional outro from v1. It is extra timing/state complexity and depends on poorly specified `music_close`; the core goal is the intro title card plus gutter scopes.
2. [S3 / 2-beat gap-fill smoke] Cut unless fully specified. It is not part of the two core ideas, is currently untestable, and adds another draw-order/state interaction.
3. [#2 / `_precompute_graticules()`] Safe to cut initially. Static tick/crosshair drawing is cheap compared with per-frame image allocation/compositing, and precompute can be added later after the gutter-layer geometry is proven correct.