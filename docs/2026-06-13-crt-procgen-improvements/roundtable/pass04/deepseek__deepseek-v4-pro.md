<!-- requested_model: deepseek/deepseek-v4-pro | resolved_model: deepseek/deepseek-v4-pro-20260423 -->

VERDICT: yes-with-fixes — the plan has the right shape but contains multiple concrete correctness gaps (vignette ordering, determinism, coordinate clamping, docking ambiguity) that will silently break the intended visual result without explicit fixes.

MUST-FIX BEFORE BUILD:
1. [Section #1 / S4] Text dimming from vignette will happen because current `render()` applies vignette *after* all drawing, including ident and any title card. The plan mandates "Text exemption: section-1 ident + the title card draw AFTER the section-8 vignette/choke multiply" — but the code must be restructured to apply vignette to the non-text base, then draw text on top. Without this, the title card will be unreadably dim during signal-loss gaps (the exact bug cited from v1.5.1). Fix: split the render pass: draw all primitives except text onto `img`, convert to numpy, apply vignette, convert back to PIL, then draw text with `ImageDraw` on the resulting image.
2. [Determinism] The plan requires deterministic noise via `rng.integers` seeded per frame, but the current section-8 noise uses the global `np.random.randint` with no seeding. That call must be replaced by a local `rng` seeded with the hashlib-derived seed. Without it, determinism is broken; even though other random calls aren’t used, this will cause frame-by-frame randomness and break the “deterministic per seed” guarantee. Fix: in `render()`, replace `np.random.randint(...)` with `rng.integers(...)` where `rng = np.random.default_rng(seed)`.
3. [Title card dock coordinate ambiguity] The doc says “interpolate the hero bbox down into the section-1 ident + subtitle coords” and later “the docked state IS the normal section-1 draw.” This is ambiguous about whether the episode title persists in the ident area after docking. If it is intended to become the permanent ident text, the normal section-1 must be modified to draw the episode title instead of the hardcoded “=== SIGNAL LOST ===" during and after the dock window. If it is not, the dock is just a shrink-out transition and the card disappears – then the phrase “IS the normal section-1 draw” is misleading. Must resolve: either (a) specify that after dock the card is gone and normal draw resumes, or (b) define that the ident text is replaced by the episode title for the rest of the episode, and modify section-1 accordingly.
4. [Gutter scope amplitude overflow] The plan says “Clamp the circular‑scope amplitude `amp <= r*0.35` so `r+amp <= gutter_half_width (~323)`.” This must be enforced in the drawing code for both left and right scopes. The left FFT spokes’ bar_len calculation and the right scope waveform radial amplitude must be capped, otherwise the scopes will bleed into the center band or beyond the frame edge, violating the centre‑band protection invariant. Fix: compute max allowed radial extent (`gutter_half_width - cx_offset` or similar) and clamp `amp` before drawing.
5. [Layer‑clipping of scopes] The plan requires scopes drawn on a transparent layer sized to the gutter rect and then `alpha_composite`‑ed. If not done, drawing primitives directly on the base image with coordinates can overflow. The implementation must create an RGBA layer of exact gutter width and full height, translate draw coordinates to be relative to that layer, and composite it at the correct x offset. This is the only way to guarantee no centre incursion or edge overflow. Must be explicitly coded; the current code draws everything on the main image, so this is a change.
6. [Drift coordinate offset clamping] The “coordinate‑offset sync‑drift” must clamp the horizontal offset such that the resulting element bbox stays strictly inside its gutter (no black edge at the border, no incursion into centre band). The doc says “clamped so each bbox stays inside its gutter” but gives no bound. The implementation must compute the bbox of the drawn element (e.g., title card or scope) with the offset applied, and if it extends outside [0, gutter_right_edge] or [gutter_left_edge, frame_width], reduce the offset accordingly. Without this, a large offset could push the title card out of the gutter and onto the centre subject, breaking the green‑only blend constraint for portrait beats.

SHOULD-FIX:
7. [S4 Envelope behaviours] The “vignette choke (bounded/floored)” is not defined — a formula like `choke = max(0.4, signal)` or similar is needed to avoid arbitrary implementation. Without it, the intensity of the choke may be too weak or too strong.
8. [Per‑element brightness hierarchy] The plan says “grid scales