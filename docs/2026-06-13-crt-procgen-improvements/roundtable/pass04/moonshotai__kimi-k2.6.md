<!-- requested_model: moonshotai/kimi-k2.6 | resolved_model: moonshotai/kimi-k2.6-20260420 -->

VERDICT: yes-with-fixes. Title card placement contradicts gutter-clamp constraints and the center-band protection rule; timing extractor rests on an unverified ledger schema and an unreliable volume fallback; sprint sequencing puts S3 title-card draw before S4’s post-vignette text-exemption layer exists.

MUST-FIX BEFORE BUILD:
1. [Concept + Concrete specs / Geometry] The plan demands a “big-bold” HERO title card at 2-3× `f_title` and simultaneously demands that title DRAW coords be “clamped so each bbox stays inside its gutter.” A 72–108 px overstrike title cannot be both hero-scale and confined to a 647 px gutter; if it is centered in the frame it violates the center-band protection rule (Hard Constraint 2). Specify the intro-phase title card anchor point (e.g., temporarily centered in the evacuated center area during the music-open window, exempt from gutter-clamp) and state that the clamp applies only to the docked section-1 ident. [ASSUMPTION: the hero title is intended to be centered during the intro; if it is instead gutter-localized, provide the max wrap bbox for a 647 px gutter.]
2. [Concrete specs / Timing extractor] The extractor assumes `led["lines"]` entries carry `speaker_role` + `start_s` + `dur_s`, but the grounding only shows `led` being passed to `_parse_hud_data`; it does not show the line-level schema or the music-open role strings. Verify against the real disk-ledger format: exact field names, whether `start_s` is absolute seconds or 25fps timeline-relative frames, and the string/enum that denotes a music-open role. If `start_s` is in timeline frames, `round(start_s*fps)` with the procgen 24fps clock will drift against the 25fps timeline mentioned in the grounding facts.
3. [Concrete specs / Timing extractor] The fallback “derive the intro window from the `volume` envelope (music from frame 0 to the first dialogue onset)” is undefined. RMS volume cannot discriminate music from dialogue or silence. Replace with a concrete deterministic heuristic (e.g., sustain `vol > 0.1` for ≥N frames to declare music, first sub-threshold frame to declare onset) or remove the fallback and require valid ledger fields, because a bad window will suppress the section-1 ident at the wrong time.
4. [Sprint plan / S3 + S4] Text exemption (“section-1 ident + the title card draw AFTER the section-8 vignette/choke multiply”) is an architectural draw-order refactor. S3 (title card) cannot be correctly implemented without the base-layer → post-process → text-overlay pass. Move the frame-buffer layering refactor (draw base image, apply vignette/choke in numpy, then draw text/card elements on top) into S1 foundation, before S3.
5. [Concrete specs / #1 / Intervals] The active window is `[music_open_start_f, music_open_end_f + dock_frames)`. Without a bound, `dock_frames` can overrun `first_dialogue_f`, suppressing the episode ident/subtitle during early dialogue, or exceed `total_frames`. Specify `dock_frames` as a capped constant (e.g., `min(int(fps*0.5), first_dialogue_f - music_open_end_f)`) and clamp the interval to `[0, total)`.

SHOULD-FIX:
1. [S1 / foundation] Remove the dormant scalar `self._brightness_ema` and `self._brightness_alpha` from `__init__` to avoid shadowing/confusion with the new precomputed `signal`/`loss`/`trig` arrays.
2. [Concrete specs / Determinism] In the section-8 `np.random.randint` → `rng.integers` replacement, the old call is half-open `[-intensity, intensity+1)`. The direct translation is `rng.integers(-intensity, intensity + 1, size=arr.shape, dtype=np.int16, endpoint=False)`. State this explicitly so the builder does not use `endpoint=True` and widen the noise range.
3. [S4 / envelope behaviors] Define the vignette choke formula. The current `_vignette` floor is 0.45. If `loss` scales the whole array, the frame can black out. Specify a bounded multiplier, e.g., `np.clip(1.0 - loss * 0.6, 0.35, 1.0)`, applied to `_vignette`, preserving a readable floor.
4. [S5 / outro] Define `music_close` precisely: “last music line” may select a short sting or bumper immediately before HUD post-roll, leaving no room for a bookend animation. Use the final contiguous music segment whose duration exceeds a threshold (e.g., >1 s) or bound the outro window to end at least N frames before `total_frames`.
5. [S2 / gutter scopes] `_precompute_graticules` is mentioned but not assigned to a sprint. Assign it to S2 and specify whether graticules are alpha-composited before or after the scope trails on the gutter layer.

OPTIONAL / NICE-TO-HAVE:
- Precompute the per-frame deterministic `seed` array in `__init__` so `render(fi)` avoids a `hashlib` call on the hot path.
- Add a try/except around `hashlib.blake2s` falling back to `hashlib.sha256` for Python builds that lack blake2s.

CUT THESE (over-engineering):
1. [S3 / “2-beat gap-fill smoke”] The concept already drives scope dimming and edge-static creep via the `loss` envelope. A separate “gap-fill smoke” feature duplicates the existing volume-gated snow (section 8) and the `loss`-driven vignette choke. Cut the explicit gap-fill smoke effect; rely on the EMA envelope alone. Safe because the existing snow gate (`vol > 0.3`) already suppresses noise in low-signal gaps.
2. [Concrete specs / Determinism] Per-frame `hashlib.blake2s` is cryptographic overkill for visual noise generation. Cut to a lighter hash (e.g., `hash((salt, title, fi)) & 0xFFFFFFFFFFFFFFFF`) or the precomputed seed array above. Safe: the requirement is determinism, not collision resistance.