<!-- requested_model: deepseek/deepseek-v4-pro | resolved_model: deepseek/deepseek-v4-pro-20260423 -->

VERDICT: build-ready as-is? no. The plan contains a critical geometry bug, a missing manifest field, an ambiguous tail-detection rule, and relies on an unverified assumption about clip paths.

MUST-FIX BEFORE BUILD:
1. [Part 3] The 3-input filtergraph references `[0:v]` for the source, but the existing 2-input filtergraph in the grounding code uses `[0:v]` as the main input label. The new graph must use `[0:v]` for the source, `[1:v]` for the procgen, and `[2:v]` for the scopes. The plan's filtergraph text is correct in its labeling, but the plan states "do NOT append to the 2-input graph -- it converts to yuv420p before the 1st blend". The existing 2-input graph's final step is `format=yuv420p`. The new 3-input graph must NOT include this premature conversion. The plan's filtergraph correctly delays `format=yuv420p` to the end. VERIFY: the implementation must branch *before* the existing 2-input graph construction, not append to it.
2. [Part 2] `OTR_SceneAwareScopes` requires `clip_manifest_json` as a STRING input. The grounding code for `OTR_SilentComposite` shows `clip_manifest_json` is an optional input with `forceInput: True`. The plan states the manifest comes from `OTR_VideoRenderBatch`. VERIFY: `OTR_VideoRenderBatch` must output `clip_manifest_json` as a STRING. If it does not, the wiring is broken. The plan must specify this output or the node must accept the manifest from the same source as `OTR_SilentComposite`.
3. [Part 4] `draw_scopes` flag is added to `OTR_SignalLostVideo`'s OPTIONAL INPUT_TYPES. The grounding code shows the node's `INPUT_TYPES` has `optional` fields for `fps`, `resolution`, `episode_title`, `closing_audio`. The plan must specify that `draw_scopes` is added to this existing `optional` dict. The plan's wording "Add `"draw_scopes": ("BOOLEAN", {"default": True})` to the OPTIONAL INPUT_TYPES" is clear enough, but VERIFY: the implementation adds it to the correct classmethod.
4. [Beat map] Tail gap detection: "the TAIL gap (rolling-credits post-roll) -> SUPPRESS (or gutter-only) -- do NOT cover the credits. Distinguish the tail = the final segment(s) past the last clip." The grounding code for `plan_timeline_segments` shows the tail is a single segment emitted at the end with `source="floor"` or `"black"`. The plan's rule "final segment(s) past the last clip" is ambiguous if there are multiple trailing floor/black segments. The implementation must identify the *last* segment in the `segments` list where `source != "clip"` and suppress scopes there. The plan must clarify: "suppress scopes on the final segment if its source is 'floor' or 'black'".
5. [Part 1] The plan states the new helpers `draw_fft_scope` and `draw_scope` are "green-only palette hardcoded (CRT_GREEN/CRT_DIM brightness only)". The grounding code shows `CRT_GREEN = (0, 255, 65)` and `CRT_DIM = (0, 100, 28)`. The helpers must use ONLY these two colors (and possibly `CRT_DARK = (0, 50, 14)` for very dim elements). The plan must explicitly forbid `CRT_CYAN`, `CRT_RED`, `CRT_AMBER`, `CRT_MAGENTA`, `CRT_WHITE` in these helpers. VERIFY: the implementation does not use any non-green colors.
6. [Part 3] The 3-input filtergraph uses `colorchannelmixer` to zero R and B on the scopes input `[2:v]`. The plan states "scopes are drawn on BLACK only". If the scopes video is truly black-background with green-only drawing, the `colorchannelmixer` step is redundant but harmless. However, if any non-black pixels exist (e.g., anti-aliasing edges), zeroing R and B might cause color fringing. VERIFY: the scopes are drawn with pure green on pure black, or the `colorchannelmixer` is safe to apply.

SHOULD-FIX:
1. [Part 2] `OTR_SceneAwareScopes` returns `("STRING",)` for `scopes_mp4_path`. The plan does not specify error handling if the node fails to render. It should return an empty string or raise a clear error so the downstream blend node can skip the scopes input gracefully.
2. [Beat map] "un-probeable -> SUPPRESS + log the reason". The plan does not specify what "SUPPRESS" means for an un-probeable clip. Does it treat the segment as a gap and draw scopes, or skip scopes entirely? The safest behavior is to treat it as a landscape clip (suppress scopes) and log a warning.
3. [Part 3] The 3-input filtergraph uses `blend=all_mode=lighten` for the scopes overlay. The grounding code comments for the 2-input blend note that without `format=gbrp`, blend modes run on YUV planes and produce garbage. The plan correctly includes `format=gbrp` for the scopes input. VERIFY: the `lighten` mode is tested with the CRT post-processing (scanlines, vignette) from the floor to ensure the scopes remain visible and don't cause unexpected darkening.
4. [Part 4] The plan says "When False (v2), SKIP render() sections 2 (centre ring) + 3 (orbiting particles) + 5 (mirrored waveform) + 6 (freq bars) -- and guard their dependent vars". The grounding code shows section 2 uses `freq`, section 3 uses `freq` and `vol`, section 5 uses `wave` and `vol`, section 6 uses `freq`. The guard must ensure these variables are not accessed when `draw_scopes` is False, but the plan does not specify how the guard is implemented (e.g., conditional blocks or early returns). VERIFY: the implementation does not accidentally reference undefined variables.

OPTIONAL / NICE-TO-HAVE:
- [Part 2] The plan specifies `fps` default 25, but the grounding code for `OTR_SignalLostVideo` defaults to 24. The scopes node should match the floor's fps for consistency, or derive fps from the manifest. The plan's default of 25 is acceptable if documented.
- [Beat map] The gutter geometry formula `portrait_w=round(480*out_w/1472)` assumes the portrait clip is 480px wide on the 1472px canvas. VERIFY: this is the correct pillarbox width from the silent composite. If the composite uses a different width, the scopes will be misaligned.

CUT THESE (over-engineering):
- None. The plan is minimal and directly addresses the v2 goal.

ASSUMPTION:
- The `OTR_VideoRenderBatch` node outputs a `clip_manifest_json` STRING that matches the schema expected by `plan_timeline_segments`. If it does not, the scopes node cannot plan the timeline.
- The `path` field in each clip segment points to a pre-composite asset (not the pillarboxed canvas). The plan relies on this for aspect ratio detection.
- The `lighten` blend mode in ffmpeg works correctly with `format=gbrp` and does not introduce artifacts when overlaying green-on-black scopes onto the blended main+procgen output.