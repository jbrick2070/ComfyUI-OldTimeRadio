# Exact-row repair feedback: offline qualification complete

The existing spoken-source validator now tells its remaining repair attempt
which line ID, original quote, offsets and selected draft text failed exact
interval matching. It still applies edits only to the submitted ID. No automatic
rerouting, source-specific replacement, new schema/widget/wire, model call,
retry reset or story gate. Root corrected the diagnostic gap observed in live07;
the missing girlfriend fact itself remains an open model/source qualification.

- 250 focused tests pass. All four new checks fail the untouched pre-fix code:
  exact-ID correction with actual applied text, stubborn failure with original
  retention and spent re-entry budget, and two repeated Unicode/offset cases.
- Full suite:14,459 passes,51 inherited failures,183 skips,1xfail. Every failure
  ID and normalized payload matches the prior14,455/51/183/1 baseline. No new
  failure, changed failure or quarantine. Process exit2 from the known-fail
  guard is recorded; this is not an all-green full suite.
- Controlled Bible: candidate38pass/10 inherited/11skip/3xfail; untouched code
  baseline37pass/11fail/11skip/3xfail using the same final Bible. Only the updated
  test-coverage catalog additionally fails baseline; all10 common normalized
  failure payloads match. Existing11.39 extended;344 total entries unchanged.
- Shipped canonical validator, JSON round-trip, widget/link audit pass:
  23nodes/63links. Canonical bytes unchanged, SHA256
  d586a286aaee4c039e410ae9a10014c5c7f4ab82d00eac0e9e1cc0564415057c.
- Changed Python parses as3.10; UTF8/noBOM/nonempty checks pass. Raw logs/XML,
  comparison helper and file hashes are archived. Temporary report helper first
  counted the YAML root mapping instead of its bugs list; that report-only
  assertion was corrected, with no production/test revision or rerun needed.

Terra/Luna independently grounded the live failure and final diff. Actual
Sonnet5 finished-code QA cost USD0.036902. Root dispositions are in
review_judgment.md; no post-QA production/test change and no unanimous approval
claim. Sonnet correctly limited canned tests as model-effectiveness evidence,
and suggested other-row hints; root retained strict existing-owner scope.
Cursor followups previously timed out; no Cursor consensus is claimed.

Commit AND push OTRv2.0-alpha and Biblemain, verify both origins and committed
evidence bytes before the next live run. Then ONE fresh full canonical08 with
installed Gemma4-12B in both writer slots; next_measurement.md records grounded
route evidence and limits. Same dinner source/profile/controls; compare the
actual graph allowing only the two model selections. This is source/family
qualification, not proof a feedback change alone fixes Nemo. No code edit
during live. Preserve and diagnose any new failure before more generation.

Seven prior attempts remain3writing failures/4source-defective publications/
0source-qualified. Broader stress, Jeffrey/Codex multi-act, Original credits
and actual audio listening remain open. Mac/4060 held; RunPod has no current
authenticated access and no rental. GO_FORWARD remains the sole queue.
