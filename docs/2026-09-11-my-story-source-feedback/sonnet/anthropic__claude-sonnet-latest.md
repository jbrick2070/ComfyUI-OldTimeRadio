<!-- requested_model: ~anthropic/claude-sonnet-latest | resolved_model: anthropic/claude-sonnet-5 -->

Findings:

1. Terminal07 log shows the model consistently naming line b3 while the quote actually belongs to b1, on both attempts — i.e., the exact scenario the new feedback message targets. Yet the "meaningful tests" added (`test_spoken_wrong_row_feedback_drives_bounded_exact_id_repair`, `test_spoken_interval_feedback_repairs_repeated_unicode_quote_with_exact_offsets`) use canned `Slot(respond)` stubs that are scripted to succeed once fed the diagnostic — this proves the message is wired into the second prompt and parsed correctly, but does not establish that a real model (e.g. the Mistral-Nemo instance in the log) will actually use `original_quote_matches_other_ids`-style information to correct itself. The live case in the log terminated "unresolved" after two attempts even before this diff (per the diff, this row-naming detail wasn't previously in the error string), so the plan's own evidence undercuts the claim that this feedback would have fixed pairlock_07 — it never re-ran the corrected pipeline against that real case.

2. The diagnosis blames "no actionable row/quote context" for the failure, but the terminal log already contains `original_quote_matches_selected_line: false` and `original_quote_matches_other_ids: [...]` in the *diagnostic output* — meaning that data was already computed and logged externally, yet the actual `ValueError` raised inside `_apply_spoken_edits` (per the diff) does not include `original_quote_matches_other_ids` at all. The new error message only echoes back `line_id`, `original_quote`, `start_char`, `end_char`, `draft_text` — it does not tell the model which *other* line the quote actually belongs to. This is a materially weaker signal than what the log's own diagnosis implies is available, so the repair may not actually give the model enough to succeed on the real failure mode it was built for.

3. `_json` sorts keys (`sort_keys=True` per nodes/_otr_story_source.py:28), but the test assertions check for `json.dumps(key)+":"+json.dumps(value)+... in feedback` substring matches per key independently — this doesn't verify overall JSON structure/ordering, just that fragments exist. That's a weak assertion; it wouldn't catch, e.g., duplicate or malformed JSON as long as substrings appear.

4. The plan explicitly forbids proposing a new checker/semantic gate/retry/fuzzy-repair, and the diff honors that (only the message changed, no new call, no budget change) — consistent with scope. No over-engineering there structurally.

5. `test_spoken_wrong_row_feedback_drives_bounded_exact_id_repair`'s "repair_id" negative branch just asserts the second attempt with the same wrong edit yields unresolved status and unchanged data — this is a fine regression test for "no fuzzy reassignment," but it is exercising the harness's own stub logic (`_edit()` vs `wrong`), not verifying any actual model behavior; the parametrization only tests that the harness plumbs data through, which was already true before this change for the coarse error case.

6. No test exercises the multi-candidate case actually seen in production (quote matching more than one *other* row, i.e., `original_quote_matches_other_ids` with multiple entries) — only single wrong-row and single ambiguous-Unicode-offset cases are covered. The real failure log shows exactly one other candidate id, so that's fine, but plan's confidence that "250 focused tests pass current code" doesn't extend coverage to multi-candidate ambiguity that the row-matching diagnostic implies could occur.

7. Minor: the plan text asserts "Successful/noop paths unchanged" and "no schema relaxation" — verified true in the diff since only the `ValueError` message changed inside an existing branch; no disagreement there.

No further must-fix beyond items 1–2, which materially matter: the tests validate wiring/parsing of the new message but not that it resolves the actual documented failure, and the message itself omits the cross-row match info that the log shows was the actually informative signal.