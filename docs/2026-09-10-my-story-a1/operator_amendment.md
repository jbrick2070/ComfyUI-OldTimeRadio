# Latest operator amendment

No RSS in My Story. Character count can be flexible like Public Domain and
Shakespeare; selected acts remain binding. Root removed the My Story exact-cast
validator and contradictory prompts/docs, preserving requested/actual receipts
and named people. A3 strict-character proposal withdrawn; both R1 CLI calls and
in-progress final full suite interrupted, not counted complete. Previous A1 full
suite had exactly S4's54 failures; final amended suite must run again.

This narrows the prior selected-count correction. No new cast owner, retries,
voice reuse or source fetch. Existing complete-artifact act repair unchanged.

Timing audit: My Story helper contexts only attribute calls; the scheduler
calls its generation wrapper directly. My Story, structured_call and shared
tail do not set a per-story/per-pass deadline. The two NewsCuration timeout
call sites in story_orchestrator are not on this route. Shared token-window
capacity checks are separate and A2 corrects the known HF estimate.
