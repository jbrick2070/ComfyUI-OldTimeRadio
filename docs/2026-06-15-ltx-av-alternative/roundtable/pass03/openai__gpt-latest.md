<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: yes-with-fixes. The plan is close, but it still contains contradictory frame/audio timing rules, an M0/M1 sequencing conflict, and several ungrounded wiring assumptions that can break build/tests if implemented literally.

MUST-FIX BEFORE BUILD:
1. [B Audio input] vs [C VAE decode floor + 8n+1] vs grounded [LTX_AV_SPRINT_PLAN.md / Dims validator + I/O contracts]: The current pass02 says the audio slice is “padded/trimmed to the 8n+1 duration BEFORE generation” and says to mirror `eng_ltx_video`’s “SAME 8n+1 snap-DOWN”. The grounded sprint plan says timing authority is `timing.target_frame_count`, render length is `next_8n1(T)` snap-UP, and canonicalize trims/pads to exactly T. Grounded `eng_ltx_video._ltx_frame_length` does snap-DOWN after floor/cap, but the AV sprint explicitly rejected copying that formula for AV. Implementing pass02 literally can shorten the audio conditioning relative to the beat and/or diverge from the already planned `av_dims.py` tests. Concrete fix: replace pass02’s “audio slice padded/trimmed to 8n+1 duration” and “SAME snap-DOWN” with: “derive T from timing.target_frame_count; render_frames = min(next_8n1(T), M0 LTX_AV_MAX_FRAMES) where next_8n1(n)=((n+6)//8)*8+1; normalize/pad/trim the model audio input to render_frames / fps only if the M0 graph requires audio duration to match latent length; canonicalize output to exactly T.” Add/keep tests for 25->25 and 26->33 from the grounded plan.

2. [TICKETS M1] conflicts with [DECISIONS 1-2] and [TICKETS M0]: M1 is described as using “the M0 GRAPH SPEC node names,” but M1 is also allowed before M0 as “CPU-only skeleton + wiring + tests.” That is not executable as written unless M1’s node-gate is deferred or supplied by a stub/spec fixture. Concrete fix: split M1 into two modes: pre-M0 skeleton must not require real A2V node names and should keep graph-dependent node-gate tests skipped/fixture-only; post-M0 fills the captured node list and enables node-gate tests. Or make M1 strictly “AFTER M0 GO.” Do not let pre-M0 code hardcode placeholder A2V class names.

3. [A assert_usable ordered gate] lacks a concrete NVML API contract while making NVML REQUIRED fail-closed. Grounded sprint references `gpu_residency.nvml_available()` and `probe_used_mb()->0` fail-open risk, but pass02 only says “NVML REQUIRED.” If implementation guesses a helper name or imports pynvml directly, cold-import and testability can break. Concrete fix: specify the exact shared module/function to call, or mark “verify: locate existing gpu_residency NVML helper and use it lazily; if none exists, add a tiny shared wrapper with mocked tests.” Keep the failure reason as an existing `EngineUsabilityReason` per plan.

4. [B schemas.py] says `FAMILIES += "audio_conditioned_video"`. In Python this is wrong if `FAMILIES` is a tuple/list/set? [ASSUMPTION] The exact type is not grounded. If tuple/list, `+= "audio_conditioned_video"` will add characters or fail semantic tests; if set, it is invalid style/possibly wrong. Concrete fix: write type-specific instruction: append/add one family atom, e.g. tuple `FAMILIES = (*FAMILIES, "audio_conditioned_video")`, list `.append(...)`, or set `.add(...)` after verifying the actual type.

5. [B Prod JSON audit FIRST] contradicts [A “default_roles ()” + dark dropdown-visible] risk: pass02 expects `@register` unconditional and flag-off dropdown-visible, but also says no JSON edit if options are dynamic. Grounding says existing `ltx_video` is visible because static dropdowns can include registered default-off engines, but it does not show the actual JSON/director implementation. Concrete fix: before coding, verify the director dropdown source. If static arrays, add both engine IDs to every relevant role dropdown in `otr_scifi_16gb_full.json` and run validator/link/widget audit in same commit. If dynamic, add a test proving registered dark engines appear while `assert_usable` fails closed. Do not rely on “registry metadata alone” without this check.

6. [A Architecture fallback chains] uses `fallback humo->humo_1.7B->latentsync->still_kenburns` and [B Fallback re-ground] says verify. This is still a build blocker because the adapters’ metadata must name real registered engine IDs and resolver-supported hops. Concrete fix: make fallback metadata implementation conditional on verified registry/resolver names; if `humo_1.7B` is not an exact engine ID, use the actual registered ID or omit that hop. Add chain membership tests after verification.

7. [DECISIONS 3] “ONE module-level lazy singleton core” + [C OOM recovery] “explicit teardown … after each clip” is underspecified and can accidentally unload/reload twice or hold stale broken state after OOM. Concrete fix: define singleton state transitions: constructed unloaded -> loaded/rendering -> idle; on successful clip run reclaim and NVML wait but keep or drop weights according to M0 policy; on OOM/cancel mark core poisoned, force reclaim, release AS-3 lease, and future render raises classified restart-required EngineUnusable/RuntimeError until process restart or explicit reset. Add a test for no double-core creation across both adapters using a mocked core factory.

8. [DECISIONS 4 / M2] “OTR’s engine return has NO audio path and encoded clip has ZERO audio streams” needs to state whether raw M0/probe outputs are allowed to contain audio outside OTR. The same section says M0 research probes LTX’s own audio happen outside OTR. Concrete fix: explicitly restrict the zero-audio invariant to OTR adapter outputs and tests, and keep probe artifacts outside runtime temp paths / not consumed by canonicalize. In OTR graph, terminate at video-only decode class from M0 and encode via silent encoder only.

SHOULD-FIX:
1. [A roles/family] `ltx_av_talk` reuses `audio_driven_face` but requires `text_prompt+audio_ref+init_image`. Existing family requirements may not include `text_prompt` or `init_image` [ASSUMPTION; schemas not grounded]. The plan says add new family inputs only for music. Concrete fix: verify `FAMILY_REQUIRED_INPUTS["audio_driven_face"]` and role compatibility for `announcer_visual`/`character_video`; if the family contract cannot express the stricter talk requirements, rely on engine `required_inputs` plus tests that schema/role validation accepts these roles.

2. [A assert_usable gate 6] “av_dims on request_template.canvas (None tolerated)” is ambiguous for missing canvas dimensions. Grounded plan default canvas is 1472x832; pass02 says `None tolerated`. Concrete fix: define: if request_template is None, skip; if canvas missing, validate default 1472x832; if partial/invalid, raise EngineUnusable with existing reason. This avoids raw `int(None)`/KeyError/ValueError paths.

3. [C VAE decode floor + 8n+1] says “M0 probes actual A2V VAE floor” but [A assert_usable] only validates dims, not frame floor/max. Concrete fix: after M0, add constants for `LTX_AV_MIN_FRAMES`, `LTX_AV_MAX_FRAMES`, and VAE floor per resolution if needed, with a sheet-drift test as in the grounded sprint plan.

4. [B Audio input] “stage under a shot/seed name” is not enough to prevent temp collision across concurrent renders if same shot/seed forced twice. Concrete fix: include request_id/shot_id, seed, role, and a short hash of source path+start+duration in the staged filename or use a unique temp dir per render.

5. [A heavy forward in EXECUTOR THREAD] depends on the render driver preserving executor/thread boundaries. Grounding for render_driver is absent. Concrete fix: verify where `render_clip` is invoked; if current adapters run in executor already, do not add another executor. If not, route only the heavy forward through existing executor utilities, not a bespoke thread pool.

6. [A ISOLATION_IN_PROCESS + STOP rule] says declare sidecar if deps shift, but [TICKETS] has no sidecar design. Concrete fix: make sidecar a hard park condition for Lane B v1, not an implementation branch. Wording: “if pip freeze differs, do not build/run in-process adapter beyond skeleton; park lane.”

7. [D Graduation] says “N short clips” without a value. Concrete fix: set N before M4, e.g. N=3 for 30-word smoke or N=5 if intended as soak. Tests/greps cannot evaluate “N.”

8. [INVARIANTS] “single heavy resident proven on 16 GB with margin” conflicts with grounded hardware section saying Q3_K_M is borderline and total NVML decides. Concrete fix: phrase invariant as “single heavy resident only if M0 proves <=14500 MB peak/sustained; otherwise park.”

OPTIONAL / NICE-TO-HAVE:
- [M0 GRAPH SPIKE] Capture both Desktop and headless `/object_info` diffs in the GRAPH SPEC; node packs often differ between Desktop Manager and server envs.
- [A] Add explicit env names for AV weights after M0 (`OTR_LTX_AV_TRANSFORMER`, encoder, VAE) rather than generic `CKPT` if the graph needs separate artifacts.
- [M4] Record wall-clock and peak NVML per role separately; talk and music may use different graph branches.

CUT THESE (over-engineering):
1. [A assert_usable gate 5] Per-artifact size floors before M0 are too specific and likely stale for GGUF/offload variants. Safe cut until after M0: only require realpath existence in pre-M0 skeleton; add M0-measured floors for the selected artifact only.
2. [B Registry CAPABILITIES row vram_estimate from M0] in M1 pre-M0 is impossible. Safe cut from pre-M0: use a placeholder/dark heavy class or omit estimate until M0; fill exact estimate post-M0.
3. [C OOM recovery docstring/error restart rule] Multiple places for the same operator instruction invite drift. Safe cut: keep one adapter docstring sentence and one runtime error message; put detailed restart procedure in M0/ship notes.
4. [D README “what each engine gives” after M4] Not needed for build readiness. Safe to defer entirely until promotion; dark parked lane should not update user-facing claims.