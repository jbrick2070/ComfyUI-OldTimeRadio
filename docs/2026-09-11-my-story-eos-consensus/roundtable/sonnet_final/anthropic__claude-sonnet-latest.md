<!-- requested_model: ~anthropic/claude-sonnet-latest | resolved_model: anthropic/claude-sonnet-5 -->

VERDICT: yes-with-fixes — one real correctness gap (empty-EOS inconsistency) plus one unverified code region must be confirmed before lock.

MUST-FIX BEFORE BUILD:
1. [nodes/_otr_model_loader.py:2325-2332, OTR_LedgerScriptWriter.py gen_kwargs, _otr_constrained_generate.py:~340] — Inconsistent behavior when `prepared["eos_token_ids"]` is empty. The generate() call passes `eos_token_id=prepared["eos_token_ids"] or None`, so an empty list silently falls back to the model's *internal* generation_config default EOS (transformers utils.py:1747, kwargs override only apply when non-None/non-empty per `update()` semantics — actually passing `None` explicitly means the kwarg is dropped/ignored since `_tensor_or_none` returns None only if value is None, but here the dict key still exists with value None, so `generation_config.update(eos_token_id=None, ...)` — with `defaults_only=False` default for `**kwargs` path — will NOT overwrite generation_config.eos_token_id since the passed value is None and code at utils.py:1289 always sets attribute when `defaults_only` is False, i.e. `setattr(self, key, None)` — this actually WIPES the model's own default EOS to None**). Meanwhile, the completion-classification code (`_otr_model_loader.py` fail_on_output_limit checks, writer `ended_with_eos`, constrained `ended_with_eos`) all test `int(generated_ids[-1]) not in prepared["eos_token_ids"]` against the same empty list, so nothing can ever satisfy it — a model that even has stoppable behavior via its own default EOS will now (a) not stop via that default (because kwarg wiped it to None) and (b) always be classified as "exhausted capacity, not eligible for reroll" even mid-generation. Fix: when `native_eos_token_ids()` returns `[]`, either (a) omit the `eos_token_id` key entirely from `gen_kwargs` rather than setting `None` (so `generation_config.update` does not overwrite with `defaults_only=False`), or (b) raise a clear `ModelLoaderError` at prepare time — do not silently continue with a possibly-EOS-erasing None. Concrete check: verify `GenerationConfig.update()`'s `defaults_only=False` path (configuration_utils.py:1288-1291) really overwrites with `None` in this transformers version, and adjust the four call sites (`OTR_LedgerScriptWriter.py`, `_otr_constrained_generate.py`, `_otr_model_loader.py` x2 for base/polish) accordingly.

2. [OTR_LedgerScriptWriter.py:~1184-1220, truncated in excerpt] — The capacity-vs-EOS classification branch after `if generated_tokens == effective_max_new_tokens:` is not fully shown past line ~1200 in the grounding excerpt. The claim "ordinary base/polish calls only inspect final token if actually at a positive failure boundary" cannot be confirmed for the writer's own capacity branch from the excerpt given. Must show the remainder of that block (through where `ended_with_eos` is actually consulted inside the `generated_tokens == effective_max_new_tokens` branch) before lock — verify it correctly treats an EOS-terminated exact-capacity completion as success, matching the constrained/base/polish routes' `and int(generated_ids[-1]) not in prepared["eos_token_ids"]` pattern.

SHOULD-FIX:
1. [nodes/_otr_constrained_generate.py:137, 147-150] — When `eos_ids` is empty, `tokenizer_data.eos_token_id = list(eos_ids)` installs an empty EOS list into LMFE, meaning the JSON parser/enforcer can never legally terminate at all (LMFE only permits EOS at parser-complete states). Combined with MUST-FIX #1, an empty EOS set is a silent total-failure mode with no explicit error. Add a guard: if `native_eos_token_ids()` returns `[]`, raise `ModelLoaderError` at `prepare_native_prompt`/`get_cached_transformers_schema_constraint` rather than propagating an empty list through four call sites.
2. [tests/test_generation_budget.py new test `test_all_native_routes_stop_on_configured_or_chat_eos_at_capacity`] — Good coverage of the four-factory ID consistency, but does not include a case with empty/`None` EOS on all sources (model config, text_config, tokenizer all None) to pin down MUST-FIX #1's behavior. Add that case.

OPTIONAL / NICE-TO-HAVE:
1. `_native_token_ids` recomputes `_native_token_ids(tokenizer.eos_token_id)` twice per `prepare_native_prompt` call (once inside `native_eos_token_ids` for chat ids, once directly for `pad_token_id`) — negligible cost, not worth a fix given "bounded QA, no redesign" scope.

CUT THESE:
None — the diff is already minimal and scoped to the stated EOS-consistency goal; no scope creep detected versus the stated "no model/sampling/context/retry-policy change" constraint.

VERIFY-AT-BUILD checklist:
1. Confirm `GenerationConfig.update(eos_token_id=None, defaults_only=False, allow_custom_entries=True)` behavior in installed transformers 5.10.4 (configuration_utils.py:1283-1291) — does passing `eos_token_id=None` in `**kwargs` actually overwrite (erase) the model's own generation_config default, per MUST-FIX #1's hypothesis? This determines severity.
2. Confirm the untruncated body of `OTR_LedgerScriptWriter.py`'s capacity-classification branch (lines ~1184 onward, past what's excerpted) correctly gates on `ended_with_eos` before raising the "exhausted... not eligible for reroll" error, matching base/polish/constrained routes.
3. Confirm `native_eos_token_ids()` cannot return `[]` in any real production model (Qwen3.5-4B) — i.e. that MUST-FIX #1/#2's empty-list path is theoretical only for the target model, though the code should still not silently mishandle it for robustness/future models.
4. Confirm live baseline mismatch (tokenizer EOS 248046 vs native config 248044) is now resolved end-to-end by `native_eos_token_ids`'s union behavior on the next fresh full canonical retry, per stated NEXTACTION.