<!-- requested_model: google/gemini-3.1-pro-preview | resolved_model: google/gemini-3.1-pro-preview-20260219 -->

VERDICT: yes-with-fixes. The plan correctly identifies the missing frame cap and legacy prompt layers, but relies on scene mapping data that does not exist in the new pipeline's beat structure.

MUST-FIX BEFORE BUILD:
1. [R5-2] Defect: The plan states "Map beats->scenes via the visual_plan's scene index", but the `shot` and `line` dictionaries generated by `otr_shot_lock.py` do not contain a `scene_id` or `scene_index`. There is no way to map a beat to a scene in `render_driver.py`.
   Fix: In `render_driver.py` `build_request_from_shot`, extract `scenes = _meta.get("visual_plan", {}).get("scenes", [])`. Since beats lack scene mapping, fall back to `scenes[0].get("visual_prompt")` for the synthetic open (`b000_music_open`), and use `get_story_brief_ltx` for the rest, or update `otr_shot_lock.py` to stamp a `scene_index` onto each shot based on proportional progression.
2. [R5-1] Defect: The frame cap is proposed but not explicitly placed in the adapter code.
   Fix: In `eng_ltx_video.py` `render_clip`, add `length = min(length, int(os.environ.get("OTR_LTX_MAX_FRAMES", 121)))` before the `length = ((length - 1) // 8) * 8 + 1` snap.
3. [R5-6] Defect: Normalizing the announcer to `c01` hardcodes an assumption that the announcer is always assigned `c01`, which is brittle and will break if the cast list orders differently.
   Fix: In `otr_shot_lock.py` `extract_beats`, dynamically find the announcer's `char_id` by inspecting `ledger["cast"]` for `role == "announcer"` or `name == "announcer"`, rather than hardcoding `c01`.
4. [R5-4] Defect: The post-guard for person tokens lacks a concrete implementation in the provided code.
   Fix: In `otr_shot_lock.py` `_prompt_is_consistent`, add a check requiring `text_prompt.lower()` to contain at least one person-anchoring token (e.g., "person", "man", "woman", "face", "portrait") if the beat's role is in `CHARACTER_BEARING_ROLES`.

SHOULD-FIX:
1. [R5-5] Defect: The self-vocative scrub is deferred to the writer, but the silent failure can be caught here.
   Fix: In `otr_shot_lock.py` `extract_beats`, look up the character's `name` from the cast list and add a `log.warning` if `text.lower().startswith(name.lower() + ",")`.
2. [R5-6] Defect: The latent join hazard warning is proposed but not implemented.
   Fix: In `render_driver.py` `build_request_from_shot`, add a `_LOG.warning` if `char_id` is truthy but `char_id not in _portrait_index(ledger)`.

OPTIONAL / NICE-TO-HAVE:
- In `otr_shot_lock.py` `_build_batch_prompt`, explicitly append the instruction: "The prompt MUST describe the named character as the visible subject (face-forward, mid-shot or closer)."

CUT THESE (over-engineering):
1. [R5-1] Alternative considered: "split long synthetic beats at ShotLock into N chained shots". Safe to cut because the compositor's existing Ken Burns hold-last-frame fallback is cheaper and sufficient for the opening gap.

[ASSUMPTION] Assuming `OTR_SilentComposite` already handles `frame_count < target_frame_count` by holding the last frame with a slow zoom, as implied by "with the existing slow-zoom (Ken Burns) treatment" in R5-1.