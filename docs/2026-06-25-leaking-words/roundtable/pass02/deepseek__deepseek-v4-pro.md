<!-- requested_model: deepseek/deepseek-v4-pro | resolved_model: deepseek/deepseek-v4-pro-20260423 -->

VERDICT: no. The plan describes a multi-layer architecture that cannot be built as specified because critical APIs are missing, the data model for the core "news-bleed" feature is undefined, and the integration between the new verifier and the existing spine is ambiguous.

MUST-FIX BEFORE BUILD:
1. [Layer 2 -- News-bleed] The plan requires a "news-abstraction step that turns raw news into fictional conflict objects and emits the banned set." No such function exists in the grounding. The `_otr_line_composer.build_allowed_roster` function accepts `key_terms` but the plan explicitly states the composer "must NOT already whitelist news/key terms" for this detector to work. This is a direct contradiction. A new function `build_banned_source_proper_nouns` with a defined input (raw news brief) and output (`frozenset[str]`) must be specified. Without it, the "news-bleed" detector cannot be implemented.
2. [Layer 2 -- Caps-name vocative] The plan states to "verify-at-build: whether `scrub_self_vocative` already covers this and whether `scrub_ledger` even calls it -- GPT flagged it may be unwired." The grounding confirms `scrub_self_vocative` only strips the speaker's OWN name. The plan's proposed rule for an ALL-CAPS token matching a roster full name is a different function. A new function, e.g., `scrub_roster_vocative`, must be defined. Its call site in the writer spine (likely `_otr_ledger_scrub.py` or the writer orchestrator) must be explicitly stated.
3. [Layer 2 -- Malformed quotes] The plan proposes to "fail-CLOSED to recompose on any INTERNAL odd quote." The grounding shows `sanitize_transcript_text` only balances a single edge wrapper. A new detector, e.g., `has_internal_odd_quote`, must be defined. The recompose path (calling `compose_line` with a `reroll_hint`) must be integrated into the writer's line-processing loop, which is not described.
4. [Layer 2 -- Integration with Audio Spine] The plan states the verifier "runs in the WRITER, BEFORE TTS/freeze." The grounding shows the writer (`_otr_line_composer`) returns a `LineResult`. The plan's Layer 2 is a "per-line gate" that can trigger a "one-repair/recompose budget." The `compose_line` function already has a retry ladder. The plan does not specify if Layer 2 is a new post-processing step after `compose_line` returns, a new validation step inside the retry ladder, or a separate pass. The control flow is ambiguous and cannot be coded.
5. [Layer 3 -- Typed Repair] The plan specifies an output JSON schema `{clean_text, removed_spans, reason_codes, confidence}`. The grounding for `_otr_repair_prompts.py` shows factories for structured calls but no schema for this specific repair. A pydantic model for this JSON output must be defined. The plan also states it "Reuses the EXISTING writer LLM plumbing" but the existing `compose_line` takes a `LineRequest` and returns a `LineResult` (a string), not a JSON object. The interface for this typed repair call is undefined.

SHOULD-FIX:
1. [Layer 2 -- Stage-direction leak] The proposed regex `^["“]?[A-Z][a-z]+(ing|ed),\s*["“].+` is a destructive regex for extraction. The grounding shows a sophisticated, guarded `_leading_stage_strip` function. The plan should specify whether this new regex is an additional guard in that function, a new standalone function, or a replacement. Implementing it as a standalone regex risks false positives on legitimate dialogue like `"Running," she said, "is my only option."`
2. [Layer 2 -- Operator Switch] The plan mentions an operator switch `strict_local_clean` for fail-closed behavior. The grounding shows config flags in `_otr_config.py`. The plan must specify the exact config key and its default value to be added to `_otr_config.py`.
3. [Acceptance Gate] The acceptance gate requires "0 shipped instances of the four classes across BOTH lanes." The plan does not define "both lanes." It likely refers to the "frontier" and "local" model paths, but this is an assumption. The test harness location and execution step are not specified.

OPTIONAL / NICE-TO-HAVE:
- The plan could provide a concrete example of the `allowed_proper_nouns` vs `banned_source_proper_nouns` data structure for the news-bleed policy to remove ambiguity.

CUT THESE (over-engineering):
- None. The architecture addresses a real leak problem.

[ASSUMPTION] "BOTH lanes" in the acceptance gate refers to the local model and frontier model writer paths.