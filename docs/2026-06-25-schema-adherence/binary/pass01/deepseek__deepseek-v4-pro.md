<!-- requested_model: deepseek/deepseek-v4-pro | resolved_model: deepseek/deepseek-v4-pro-20260423 -->

VERDICT: yes-with-fixes -- the core idea (binary escalation for dialogue vs. stage-direction) is sound, but the plan omits critical integration details, over-scopes into unproven applications, and rests on unqualified claims that would misguide implementation.

MUST-FIX BEFORE BUILD:
1. [THE PROPOSAL / "A shared, ultra-tolerant BINARY-DECISION primitive"] The `binary_decide` function contract is undefined. No input schema, output format, parsing algorithm, or definition of "unparseable" (-> None). Specify: input (slot_fn? question? text?), output (single token? how extracted?), and the exact fallback behavior when parsing fails. Without this the primitive cannot be built.
2. [THE PROPOSAL / "route to the existing RECOMPOSE seam"] The plan does not state how a binary "stage direction" verdict triggers action in the existing pipeline. Must define: where in the spine is the binary call placed (after `split_stage_business` punts? before the recompose gate?), and what recompose/reroll directive is passed (e.g., reuse `_BARE_STAGE_HINT`). The integration point with `_otr_line_hygiene` must be explicit.
3. [CANDIDATE LEDGER-INTEGRITY APPLICATIONS] Scope creep: the plan lists four candidate applications, but only (1) dialogue vs stage-direction is directly motivated by the stated problem (leaked directions breaking the ledger). Applications 2 (edit no-op), 3 (speaker membership), and 4 (beat over-length) are unrelated or already handled by existing repair factories / tolerance. Cut them from this addendum now to avoid architectural bloat. They can be proposed separately after the first proves value.
4. [OPEN QUESTIONS #5] The composition with the pass04 tolerance ladder is unresolved. The plan asks whether `binary_decide` is a sibling of `structured_call` or a wrapper. This design decision must be made before building: will binary calls live inside the tolerance ladder (with its own retry/fallback), or sit as a separate pre‑filter? Without an answer the interaction is unsafe. Decide and document.
5. [THE PROPOSAL / "deterministic fallback if the binary parse is junk" / "deterministic given (model, prompt, seed)"] (a) The deterministic fallback is not specified -- what action does the pipeline take when `binary_decide` returns None? Must define (e.g., treat as dialogue, log, manual flag). (b) The claim that the binary call is deterministic with a seed is misleading and likely false across remote APIs. Clarify: the default path (no binary call) must stay deterministic; the binary call itself need not be deterministic, only safe.
6. [THE OPERATOR'S PRINCIPLE] The sweeping claim "LLMs across families ... are RELIABLE at binary classification" is unsubstantiated and contradicted by the later acknowledgement that they can return prose / "it depends." Reword to reflect that binary classification has higher *success rate* than structured emission, but still requires the tolerant parse and fallback described. The design must not assume infallibility.

SHOULD-FIX:
7. [THE PROPOSAL vs. existing code] The plan mentions escalating cases where `split_stage_business` punts, but it does not address the existing `detect_stage_business_for_reroll` Tier‑2 detector (which already covers undelimited and embedded action clauses and triggers a full recompose). Analyze whether those cases are already handled deterministically; if so, the binary escalation may be redundant. Document the overlap and justify the added call.
8. [OPEN QUESTIONS #3] Cost/latency impact is unknown. Provide a rough estimate: what fraction of lines trigger the regex‑abstain path in the current corpus? Without that, the cost‑accuracy tradeoff cannot be evaluated. A small on‑paper analysis is needed before committing to the binary escalation ordering.
9. [INTEGRATION] The plan uses "RECOMPOSE seam" without specifying what that seam expects from the binary verdict (e.g., does a "stage direction" outcome require the line to be dropped, or a reroll with a specific prompt?). Clarify the downstream actions so the binary decision maps unambiguously to the existing spine recompose logic.

OPTIONAL / NICE-TO-HAVE:
- Add an offline‑verifiable metric (counter of binary‑escalation hits and their outcomes) to track the primitive’s effectiveness and detect regressions.

CUT THESE (scope / over‑engineering):
1. [CANDIDATE LEDGER-INTEGRITY APPLICATIONS] Remove applications 2, 3, 4 entirely. The payload‑null case is already targeted by `payload_null_repair`; speaker membership has a Levenshtein resolver; beat over‑length is a separate tolerance problem. No evidence they benefit from a binary call, and they inflate the primitive's interface.
2. [THE PROPOSAL / shared `binary_decide`] Defer the idea of a shared, parameterised binary‑decision primitive. Build a single, dedicated binary classifier for dialogue vs. stage‑direction first. The other applications are cut, so the shared abstraction is premature.