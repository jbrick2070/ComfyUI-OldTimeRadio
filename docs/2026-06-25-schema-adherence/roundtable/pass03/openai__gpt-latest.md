<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. C4 has an impossible schema-injection path, and C2’s “one re-validate” sequence misses errors introduced by key normalization.

MUST-FIX BEFORE BUILD:
1. [C4 / _otr_repair_prompts.py make_dispatching_repair_factory / _otr_structured_call.py structured_call] Defect: the plan says `structured_call` passes `schema=schema` into the dispatcher builder, but grounded code shows `structured_call` never calls `make_dispatching_repair_factory`; callers pass an already-built `repair_prompt_factory`. Adding schema at `structured_call` cannot work without importing `_otr_repair_prompts` and changing ownership of factory construction. Concrete fix: change the plan so the call site constructs the dispatcher as `make_dispatching_repair_factory(schema=schema, deterministic_repair=...)`, and `structured_call` remains unaware. If backward compatibility matters, make `schema` optional and only append schema text when provided; otherwise update every call site that currently calls `make_dispatching_repair_factory(...)`.

2. [C2 / _parse_and_validate except ValidationError] Defect: `_clamp_overlong_strings(work, ve)` uses the original `ValidationError`. After `_normalize_field_keys` moves a synonym onto a canonical field, new validation errors can appear on the moved canonical field, especially `string_too_long`; the original `ve` only knew the canonical field was missing, so the clamp will not see that overlong value. The plan’s claim that “string_too_long locs are unaffected by key moves” is only true for unrelated fields. Concrete fix: after key normalization, either re-validate `work` to get a fresh `ValidationError` before clamping, or extend clamping to inspect `schema.model_fields` constraints for moved fields. Do not rely solely on the pre-normalization `ve`.

3. [C3 / structured_call retry sequencing] Defect: guarding Attempt 2 on `json.JSONDecodeError` while leaving hard-coded “Attempt 3” blocks creates inconsistent attempt numbering and `max_attempts` behavior. Example: with `max_attempts=2` and Attempt 1 raising `ValidationError`, structural retry is skipped and typed repair becomes the second actual call, but the current block/log shape is still “attempt 3/%d”. Concrete fix: restructure the ladder so attempt numbers reflect actual calls, e.g. after Attempt 1: if `last_error` is `JSONDecodeError`, optionally run structural retry; if the current last error is `ValidationError` or `PostValidationError` and attempts remain, run typed repair as the next actual attempt. Add tests for `max_attempts=2` with JSON syntax failure vs schema failure.

4. [C3 / structured_call exception contracts] Defect: the grounded code catches `(json.JSONDecodeError, ValidationError, ValueError)` on all attempts. The plan says to narrow to `(json.JSONDecodeError, ValidationError, PostValidationError)`, but this must be applied consistently to Attempt 1, structural retry, and typed repair. If any `ValueError` catch remains, programming errors from slot functions, prompt factories, or post validators will still be swallowed into the retry ladder. Concrete fix: replace all three catch tuples and add a test where `post_validator` raises plain `ValueError`; it must propagate, not become `StructuredCallFailedError`.

5. [C4 / deterministic_repair return path] Defect: grounded `DeterministicRepair = Callable[[str, BaseException], Optional[BaseModel]]`, but `structured_call` only accepts the return if `isinstance(repair_prompt, schema)`. If a deterministic callback returns the wrong `BaseModel` type, grounded code will treat that model instance as a repair prompt, pass it through `_prompt_to_messages`, and send a Pydantic object as `messages` to `slot_fn`. Concrete fix: once `schema` is closed over in `make_dispatching_repair_factory`, validate `resolved` before returning it: if `resolved is not None and not isinstance(resolved, schema)`, raise `TypeError` as a programming error or return `None` to fall back to LLM repair. Prefer `TypeError` fail-loud.

6. [C0 / schema taxonomy class attribute] Defect: `__otr_field_aliases__: dict[str, tuple[str, ...]]` on Pydantic models is under-specified. In Pydantic v2, model class attributes should be marked `ClassVar` when they are metadata and not fields/private attrs. [ASSUMPTION] Without this, some schema classes may reject the attribute or expose it unexpectedly in model metadata. Concrete fix: require `from typing import ClassVar` and define `__otr_field_aliases__: ClassVar[dict[str, tuple[str, ...]]] = {...}` on each model.

7. [C1 / _normalize_field_keys] Defect: the helper contract does not explicitly require copying before “move it.” If implemented by mutating `data`, downstream logging/repair may see modified raw parsed data instead of the actual model output. Concrete fix: first line after `isinstance(data, dict)` should be `out = dict(data)`; all moves happen on `out`; return `out`.

SHOULD-FIX:
1. [C1 / _normalize_field_keys] Defect: top-level-only is stated, but the algorithm says `target field = loc[0]` for every `missing` error. For nested missing errors, `loc[0]` could be the parent field, causing an accidental top-level alias move for a nested failure. Concrete fix: only process errors where `len(loc) == 1`, `isinstance(loc[0], str)`, and `loc[0] in schema.model_fields`.

2. [C1 / alias collision semantics] Defect: “2+ synonyms present” depends on exact key matching, but the output dict may contain non-string keys from parsed JSON? JSON object keys should be strings, but the helper accepts `object`. Concrete fix: after `isinstance(data, dict)`, only consider synonym keys where the actual key is a `str`; otherwise fail loud.

3. [C4 / schema text injection] Defect: filtering `schema.model_json_schema()` to top-level `("properties","required","type")` does not remove nested `description`, `examples`, `title`, or `$ref` bloat inside `properties`. The plan also says “bound length” but gives no bound. Concrete fix: recursively prune property metadata to only type/enum/const/items/properties/required/min/max/maxLength-style validation keys, then cap the serialized schema string to a fixed character limit with a clear fallback if exceeded.

4. [C4 / schema_field_repair protocol preservation] Defect: `schema_field_repair` currently has the `RepairPromptFactory` signature and accepts no schema text. The plan says the branch “appends” schema text but does not specify where. Concrete fix: keep `schema_field_repair` unchanged; in the dispatcher’s `ValidationError` branch, call `schema_field_repair(...)`, then append the schema block to `messages[0]["content"]` before returning.

5. [C5 / hand-rolled migration interface] Defect: `parse_validate_tolerant(raw: str, schema: type[T], ...)` only accepts raw text. C0 will find call sites by grepping both `parse_first_json_object` and `model_validate`; some may already have parsed `dict` data before validation. [ASSUMPTION] Those sites cannot call the proposed function without re-serializing or changing their parsing path. Concrete fix: either add an internal `validate_tolerant_data(data: object, schema: type[T], ...)` used by `parse_validate_tolerant`, or explicitly migrate each site to pass raw model output before parsing.

6. [C5 / structured_call wrapper] Defect: `_parse_and_validate` currently owns JSON parsing, validation, clamping, and post-validation. If it becomes a thin caller of `parse_validate_tolerant`, the new public function must preserve `PostValidationError` behavior exactly. Concrete fix: put `post_validator` execution in the shared core and test that a schema-valid object rejected by `post_validator` still raises `PostValidationError`.

7. [C6 / conformance harness] Defect: the listed corpus covers parse/validate shapes but not the C3 sequencing change. Concrete fix: add no-network tests with fake `slot_fn` for: JSONDecodeError -> structural retry runs; ValidationError -> structural retry skipped and typed repair runs once; plain ValueError from slot/post-validator propagates.

8. [C6 / telemetry] Defect: “structured log payloads or optional `on_event` callback” is not wired through function signatures. Adding `on_event` to `parse_validate_tolerant` but not `_parse_and_validate`/`structured_call`, or vice versa, will fragment telemetry. Concrete fix: choose one for v1. Smallest: structured logs only, with stable keys for `helper_name`, `attempt`, `failure_class`, `key_normalized`, `clamped`, and `llm_repair`.

OPTIONAL / NICE-TO-HAVE:
- [C4] Include rejected-key reporting later, but only after schema closure wiring is correct and the prompt-size cap is enforced.
- [C5] Add `__all__` only if this module already uses explicit public exports elsewhere; grounded `_otr_structured_call.py` does not currently define one.
- [C6] Add fixtures for alias collision plus simultaneous alias normalization and overlong string on the moved field.

CUT THESE (over-engineering):
1. [VERIFY-AT-BUILD] “Add stable error codes for the schema-repair path” is unnecessary for v1. Schema repair is already routed by `isinstance(error, ValidationError)`; adding a new error-code layer risks scope creep and does not fix the current wiring problem.

2. [C6] Optional `on_event` callback can be cut for v1 if structured logs are sufficient. It adds signature propagation across `parse_validate_tolerant`, `_parse_and_validate`, and `structured_call` without being required for correctness.

3. [C4] Native schema injection into every typed factory should stay cut. Only the dispatcher’s `ValidationError` branch needs schema text; changing all six factory interfaces would break the grounded `RepairPromptFactory` protocol.