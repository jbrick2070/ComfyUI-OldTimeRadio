CLAUDE ANCHOR -- R4 (convergence / residual defects).

VERDICT: yes -- build-ready. The plan converged across R1->R3 with no architecture left open: strict-first invariant (byte-identity), failure-arm deterministic key-normalizer mirroring `_clamp_overlong_strings`, skip-structural-on-ValidationError (the token fix), schema-into-repair AT THE CALL SITE (avoids the circular import + matches the pre-built-factory contract), one shared `validate_tolerant_data` core for the hand-rolled passes, and an INCREMENTAL classvar opt-in rollout so un-annotated schemas stay byte-identical. The wiring bugs all three panels caught are folded (C2 re-validate-between-coercions; C4 ownership/circular-import; attempt-accounting; loc/copy guards; schema-string cap; deterministic-repair type-check).

RESIDUAL (build-time, not plan defects):
1. [C2] The pasted control-flow is deliberately sketchy and the `if "instance" not in dir()` line is a code smell. REQUIREMENT is precise (re-validate to a fresh ValidationError between each coercion; fail loud if nothing coerced). At the keyboard, implement it with an explicit `coerced_any: bool` + a single final `schema.model_validate(work)`; do not ship the `dir()` hack. Not a design change.
2. [C6 ordering] Land the conformance harness BEFORE the C5 schema opt-ins so every `__otr_field_aliases__` annotation is regression-guarded as added. The combined "alias-key + overlong-on-the-moved-field" fixture is the one that proves the C2 loop; it is already listed -- keep it mandatory.
3. [verify-at-build, correctly deferred] read the real StorySpine `normalize_length` schema (set its aliases + ship the real Opus object as fixture #1); enumerate `make_dispatching_repair_factory(` callers (each forwards `schema=`); confirm the byte-identity corpus covers the migrated passes.

NO MUST-FIX remains. Convergence reached; this is the last round. Expect the panel to surface only nits -- if any names a NEW architectural defect, re-check it against the code before folding; otherwise deliver pass03_plan as the final hardened plan with these residuals as build-time checks.
