<!-- requested_model: deepseek/deepseek-v4-pro | resolved_model: deepseek/deepseek-v4-pro-20260423 -->

VERDICT: yes-with-fixes — two build-blocking wiring bugs remain (C4 schema string injection is underspecified, C2 re-validate loop silently swallows a final failure), plus one verify-at-build item from earlier rounds is still unverifiable.

MUST-FIX BEFORE BUILD:
1. [C4] The schema-string injection into `messages[0]["content"]` is described in prose but has NO concrete signature, no placement in the call graph, and no example. A builder reading `make_dispatching_repair_factory` will see no schema parameter, no injection step, and no truncation logic. Fix: add a `schema: type[BaseModel]` parameter to `make_dispatching_repair_factory`, define a private `_build_schema_snippet(schema) -> str` that extracts validation keys, caps at 2000 chars, and returns a `\nSchema:\n...` string; append it to the returned prompt’s `messages[0]["content"]` inside the `schema_field_repair` branch (and ONLY that branch — JSON syntax and PostValidationError branches must not get it). Document the snippet format with one concrete example.
2. [C2] The `else:` branch of the for/else loop calls `schema.model_validate(work)` and discards the result — if that final validate raises, the exception propagates unlogged and without the `touched` guard. Fix: wrap the final `model_validate` in a try/except that re-raises the original `ve` (or a new `StructuredCallFailedError`) so the caller sees a clear “coercion loop exhausted” failure, not a raw ValidationError with no context. Also add the `if not touched: raise` guard BEFORE the final validate so a no-op coercion doesn’t silently re-raise.

SHOULD-FIX:
1. [C3] The plan says “Fix the stale ‘4-attempt retry ladder’ comment” but the grounding shows the comment is already “4-attempt retry ladder” in the docstring and “3 rungs” in the prose — the code actually has 3 attempts. The plan text itself still says “4-attempt” in one place (the C3 section header context). Fix: grep the whole plan and codebase for “4-attempt” and replace with “3-attempt” everywhere.
2. [C5] `validate_tolerant_data` and `parse_validate_tolerant` are specified in prose but have no concrete signatures, no module placement, and no import path. A builder will guess. Fix: add a one-line signature for each, state they live in `_otr_structured_call.py` (or a new `_otr_tolerant_validate.py` if circular-import risk exists), and show the import path the migrated call sites will use.
3. [C4] The type-check “if the callback returns a non-None object that is `not isinstance(resolved, schema)`, raise `TypeError`” is described but not placed in the code. The grounding shows `make_dispatching_repair_factory` already returns `resolved` from `deterministic_repair` with no isinstance guard. Fix: add the guard immediately after `resolved = deterministic_repair(...)` inside the factory closure.

OPTIONAL / NICE-TO-HAVE:
- [C6] The conformance harness fixture list is good but doesn’t specify the test framework (pytest? unittest?). Add a one-line note: “uses pytest, no conftest dependencies.”
- [C1] The `_normalize_field_keys` docstring says “deterministic key fix” but doesn’t state the tie-breaking rule for the 2-synonym collision case (it already returns None, which is correct — just document it).

CUT THESE:
1. [C6] “Telemetry: structured LOGS only for v1 (stable keys…)” — the plan already says no `on_event` callback, which is correct, but the telemetry prose implies a logging schema that isn’t defined. Cut the telemetry spec from v1 entirely; the existing `log.warning` calls in C2 and C3 are sufficient. The structured-log schema can be a v2 follow-up.
2. [C0] The “217-site structured-pass inventory” is referenced but not shipped as a file or appendix. Cut the reference from the build plan — it’s research artifact, not build instruction. The builder only needs the list of high-value schemas to annotate first (C5 already names them).

VERIFY-AT-BUILD checklist:
- [ ] Confirm the byte-identity regression corpus covers the migrated passes (flagged by DS in R3). The plan says “Confirm the byte-identity regression corpus covers the migrated passes” but gives no corpus path, no test runner, and no pass/fail criterion. UNVERIFIABLE as written. Fix: add a concrete verify step — “Run `tests/test_schema_adherence_conformance.py` with the `--byte-identity` flag against the six migrated schemas listed in C5; all must pass with zero diffs against the baseline stored in `tests/fixtures/byte_identity_baseline/`.”
- [ ] Enumerate `make_dispatching_repair_factory(` callers; each must forward `schema=` (from R3). The plan says to enumerate them but doesn’t say where the list lives or how to check it. Fix: add “grep the codebase for `make_dispatching_repair_factory(` and confirm every call site passes `schema=` as a keyword argument; document the grep command and expected count in the build checklist.”
- [ ] The exact StorySpine `normalize_length` schema fixture (from R3). The plan says “ship that object as conformance fixture #1” but doesn’t give the fixture file path or the expected alias mapping. Fix: add “Fixture lives in `tests/fixtures/conformance/story_spine_normalize_length.json`; the alias mapping is `{'length': ('normalize_length',)}` per the real Opus shape captured on 2026-06-18.”

[ASSUMPTION] The grounding excerpts show `_otr_structured_call.py` and `_otr_repair_prompts.py` as they exist BEFORE the C1–C6 edits. The plan’s C2 re-validate loop, C4 schema injection, and C5 tolerant-validate core are NOT yet in the grounding — I am reviewing the plan text against the stated requirements, not against implemented code.