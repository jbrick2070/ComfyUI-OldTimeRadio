<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: yes-with-fixes — functional direction is converged, but C4 schema-aware repair and the “no aliases = byte-identical no-op” invariant still contain build-blocking ambiguity/contradiction.

MUST-FIX BEFORE BUILD:
1. [INVARIANT / C2 / C5] Defect: “A schema with NO `__otr_field_aliases__` is a byte-identical no-op” conflicts with C2/C5 because `_clamp_overlong_strings` still runs for schemas with no aliases. Grounding shows existing `_parse_and_validate` already clamps top-level `string_too_long` regardless of aliases. A builder could either preserve clamp behavior or gate all tolerance on aliases; those produce incompatible invalid-output behavior. Concrete fix: rewrite invariant to: “Canonical-valid output is returned unchanged. Absence of `__otr_field_aliases__` disables key normalization only; existing overlong-string clamp behavior remains where the tolerant core is used.” If true byte-identical strict behavior is required for no-alias schemas, then explicitly gate clamping too, but that would be a behavior regression from grounded current `_parse_and_validate`.

2. [C4] Defect: schema-aware typed repair says “APPENDS a capped, filtered schema string” but does not specify the schema source or exact attachment contract. Implementors could use `schema.schema()`, `schema.model_json_schema()`, raw `model_fields`, or attach to a different message, producing different prompts/tests. Concrete fix: specify exact implementation:
   - Change signature to `make_dispatching_repair_factory(*, schema: type[BaseModel], deterministic_repair: Optional[DeterministicRepair] = None)`.
   - In the `ValidationError` non-payload-null branch, call unchanged `schema_field_repair(...)`.
   - Build schema text from `schema.model_json_schema()`.
   - Recursively filter to the approved validation keys.
   - Append to `messages[0]["content"]` as a fixed suffix, e.g. `"\n\nSchema constraints:\n" + filtered_json`.
   - If `messages` is not a non-empty list whose first element has string `"content"`, raise `TypeError`.
   - Cap serialized schema text at the stated limit with a deterministic truncation note.

3. [C0 / C5 / C6] Defect: v1 opt-in schemas are named by pass area, but only StorySpine `normalize_length` has a concrete verify item for real alias shape. The plan does not require concrete alias maps/fixtures for outline macro/phase/beat, casting, news_interpreter, or story_critic before migration. Reasonable builders could invent different synonym sets. Concrete fix: add a v1 alias table generated by C0 before implementation: `file / schema class / canonical field / accepted synonyms / source fixture`. Require at least one conformance fixture per migrated schema or explicitly state that only StorySpine `normalize_length` is annotated in v1 and the rest are deferred.

4. [C3] Defect: C3 narrows caught exceptions away from plain `ValueError`, but the grounded `PostValidationError` docstring still says it subclasses `ValueError` so existing `except (..., ValueError)` arms catch it. The plan only says to fix the stale “4-attempt retry ladder” comment, not this now-wrong exception-contract documentation. Concrete fix: update `PostValidationError` docstring and `structured_call` docstring/comments to say only `json.JSONDecodeError`, `pydantic.ValidationError`, and `PostValidationError` are recoverable ladder failures; plain `ValueError` from slot functions, factories, or raising post validators propagates.

SHOULD-FIX:
1. [C2] Defect: pseudo-code uses `if "instance" not in dir():`, which is fragile and should not be copied into production. Concrete fix: replace with a sentinel-based implementation in the plan:
   `instance: Optional[T] = None`; set on successful validation; after loop, if `instance is None`, run final `schema.model_validate(work)`.

2. [C6] Defect: telemetry requires “structured LOGS” with stable keys, but C2’s sample log is free-form and does not include all stable keys. Concrete fix: define one logging shape, either `log.warning("...", extra={...})` or deterministic `key=value` text, and require C2/C3 repair logs to populate `helper_name`, `attempt`, `failure_class`, `key_normalized`, `clamped`, and `llm_repair`.

3. [C4] Defect: the filtered schema key list omits common JSON Schema validation keys likely emitted by pydantic v2, e.g. `items`, `minimum`, `maximum`, `exclusiveMinimum`, `exclusiveMaximum`, `minLength`, `pattern`, `anyOf`, `oneOf`, `allOf`. Dropping `$defs` is deliberate, but dropping `items` can make array fields useless. Concrete fix: either expand the whitelist to pydantic v2 validation keywords or state intentionally that only shallow/top-level scalar constraints are included.

4. [C3] Defect: attempt labels become dynamic, but the public ladder prose still describes fixed Attempt 1/2/3 ordering. After structural-skip, typed repair may be logged as attempt 2/3. Concrete fix: update docs to describe logical rungs separately from numeric attempts actually run.

OPTIONAL / NICE-TO-HAVE:
- [C1] Validate alias declarations during tests: each synonym must be a string, canonical field must exist in `schema.model_fields`, and no synonym may equal another canonical field unless explicitly intended.
- [C6] Add one regression asserting alias normalization does not mutate the original input dict.

CUT THESE:
1. [C0] Cut “Inventory the structured surface 217 sites / 21 files gates everything” as a v1 build gate. Safe cut: the plan already defers `_otr_radio_editor.py` and the long tail. Gate only the v1 opt-in schemas plus call sites that invoke `make_dispatching_repair_factory`; keep the full 217-site inventory as follow-up documentation.

2. [C6] Cut full telemetry key coverage from v1 if schedule is tight. Safe cut: conformance tests and fail-loud behavior protect correctness; minimal warning logs for coercions are enough for first run.

VERIFY-AT-BUILD checklist:
- [VERIFY-AT-BUILD] Read the exact StorySpine `normalize_length` schema in `_otr_story_spine.py`; set `__otr_field_aliases__` from the real Opus object shape; ship that object as conformance fixture #1.
- [VERIFY-AT-BUILD] Enumerate every `make_dispatching_repair_factory(` caller and confirm each forwards `schema=...`.
- [VERIFY-AT-BUILD] Confirm the byte-identity regression corpus covers every migrated pass.
- [C3] Verify `_otr_json.parse_first_json_object` raises `json.JSONDecodeError` for unparseable output; if it can raise plain `ValueError`, either convert it in `parse_validate_tolerant` / `_parse_and_validate` or add that exact exception to the recoverable set intentionally.
- [C3] Verify no slot function, repair factory, or post validator relies on plain `ValueError` as a recoverable content/schema failure. Convert recoverable content rejection to returned error string -> `PostValidationError`.
- [C4] Verify `_otr_structured_call.py` still does not import `_otr_repair_prompts.py`; schema-aware dispatch must remain call-site wired to avoid the grounded circular import.
- [C4] Verify deterministic repair wrong-type behavior: non-`None` result that is not `isinstance(resolved, schema)` raises `TypeError` and is not swallowed by the ladder.
- [C5] Verify each v1 opt-in schema imports `ClassVar` where needed and declares only top-level alias mappings for real canonical fields.
- [C6] Verify conformance includes alias+overlong-on-moved-field so C2’s fresh-`ValidationError` loop is exercised.