<!-- requested_model: deepseek/deepseek-v4-pro | resolved_model: deepseek/deepseek-v4-pro-20260423 -->

VERDICT: no. The plan leaves critical implementation gaps — the whitelist mapping source for `_normalize_field_keys` is undefined, the schema injection path for typed repair is ambiguous between competing approaches, and the ladder restructuring to skip the structural retry is underspecified to the point where off‑by‑one or skipped‑repair bugs are likely. Without resolving these, a developer cannot proceed from this document alone.

MUST-FIX BEFORE BUILD:
1. [Plan 2b / Plan 1] The `_normalize_field_keys` helper cannot be implemented because the plan does not specify where the whitelist of per‑schema synonym→canonical field mappings comes from. It says “WHITELISTED synonym keys” but gives no interface for passing that data into the helper. Concrete fix: choose one mechanism (e.g., a class attribute `_field_synonyms` on the pydantic model, or a separate `FIELD_TAXONOMY` dict keyed by schema) and define exactly how `_normalize_field_keys` accesses it. Without this, the function signature and its consumption of taxonomy are unknowable.
2. [Plan 4] The provision to “give the typed repair the schema” is split between “extend the protocol” and “a wrapper builder that closes over the schema” with no decision. The current `RepairPromptFactory` protocol only receives `(original_prompt, failed_output, error)`; the typed repair factories cannot access the schema. Concrete fix: either (a) add `*, schema: type[BaseModel]` to the `RepairPromptFactory` protocol, update `structured_call` to pass it, and adapt every existing factory signature (even if they ignore it), or (b) modify `make_dispatching_repair_factory` to accept the schema and inject the JSON schema string into the repair prompt via a closure, leaving the protocol untouched. The plan must pick one and describe the exact changes.
3. [Plan 3] The ladder restructuring to skip the structural retry on `ValidationError`/`PostValidationError` while still running the typed repair is described only in prose: “Route … STRAIGHT to typed repair (Attempt 3) … keep the structural rung only for `JSONDecodeError`.” The existing `structured_call` has sequential if‑blocks gated by `attempts_run < max_attempts`; simply skipping the second block could cause the typed repair attempt to be never executed or to run with the wrong `last_raw`. Concrete fix: supply a pseudocode‑level restructure that shows how the ladder conditionally omits Attempt 2 without mis‑counting attempts. For example: after Attempt 1, if `last_error` is `json.JSONDecodeError` go to Attempt 2, else skip directly to the typed‑repair block; ensure `attempts_run` stays correct and the repair block is entered exactly once.
4. [Plan 5] The hand‑rolled pass coverage is left to “factor strict‑first + `_normalize_field_keys` into a shared `parse_validate_tolerant` both paths call.” No signature, location, or interface is given. The implementor cannot build this shared helper without knowing what it exports and how existing passes are expected to call it. Concrete fix: specify the exact function signature (e.g., `def parse_validate_tolerant(raw: str, schema: type[T], *, field_synonyms: dict[str, str] | None = None, post_validator=...) -> T`) and decide whether it lives in `_otr_structured_call.py` as a module‑level function. Provide at least one example of replacing a hand‑rolled parse‑validate block.
5. [Plan 1] The per‑pass field taxonomy is required as a gate for steps 2 and 5, but the plan never defines a consumable data shape. “One table per structured schema” is not a software artefact. Concrete fix: define a Python representation (e.g., `Taxonomy = dict[str, list[str]]` for synonyms, plus a mapping from schema to taxonomy) and state how the normalizer helper retrieves it at runtime. Without this, the “gate” is not executable.

SHOULD-FIX:
- [Plan 2a] The plan recommends native `validation_alias=AliasChoices(...)` and `populate_by_name=True`. While correct in principle, adding aliases can cause shadowing bugs if a synonym clashes with another real field name. Add a warning that schema authors must verify no alias collides with an existing field or another alias within the same model.
- [Plan 3] The existing comment “4‑attempt retry ladder” contradicts the actual max attempts (3) and the plan’s own count. Correct that comment as part of this work.
- [Plan 6] The offline conformance harness is not specified but is vital for regression safety. At minimum, provide an outline of the harness structure and which schema‑model pairs are covered, so the test build can be planned and its scope agreed.

OPTIONAL / NICE-TO-HAVE:
- [Plan 4, optional part] Surfacing dropped extra keys in the repair error can be deferred; it requires extra processing and does not block the core tolerance work.
- [Plan 6] Telemetry counters (per‑helper attempts, failure class, key‑normalize fired, etc.) are nice after the core is stable but not required for build.

CUT THESE (over‑engineering):
- [Plan CUT section] The plan already explicitly cuts Lever B (required→optional), Lever C (schema‑in‑base‑prompt), Lever E (broad/fuzzy coercion), and extra repair‑prompt factories. These cuts are sound and not needed.

[ASSUMPTION] We assume the `_normalize_field_keys` helper will be called inside the `except ValidationError` arm of `_parse_and_validate`, following the same pattern as `_clamp_overlong_strings`. The order of applying fixes (clamp first, then normalize) is not specified but can be resolved by testing the errors’ types within a loop. This must be verified during implementation.