VERDICT: no. The plan misstates the current failure path and leaves resource ownership, admission, and recovery behavior undecided.

MUST-FIX BEFORE BUILD:
1. [§1.1–1.4, §3.1] Rebase the mechanism on current code. The two production callers load the model BEFORE entering _run_with_timeout (nodes/story_orchestrator.py:765–798, :905–930). Decoding already has cooperative deadline handling through _DeadlineStoppingCriteria and GGUF streaming (nodes/_otr_model_loader.py:351–380; nodes/_otr_gguf_backend.py:1460–1500). Timeout pauses are explicitly re-raised through both rankers and the writer boundary (nodes/story_orchestrator.py:832, :969; nodes/OTR_LedgerScriptWriter.py:1462). “Nothing can stop it” and “the next stage loads a second model” therefore omit existing protections. Fix: describe the residual target as an abandoned operation overlapping a subsequent prompt or another admitted consumer; preserve the existing deadline and propagation protections. Verify: the current live route that permits that overlap.

2. [§1.4, §2] The cited production incident does not establish the claimed OOM. docs/PROD_BUG_LOG.md:6636–6689 identifies an unsound process-wide VRAM-delta tripwire, labels orphan interference a plausible explanation rather than a forensic finding, and records the same-prompt propagation fix. Fix: distinguish the observed historical failure from the remaining architectural exposure. Remove categorical claims that this incident proves orphan-induced OOM or that each prior fix failed at the same defect. Verify: a current artifact demonstrating the remaining overlap and its actual failure.

3. [§1 concluding paragraph, §3.2] “Cache handle,” “active operation,” and “unreclaimed resource” need separate ownership contracts. A counter decremented when decoding exits does not prove that weights are released; make_generate_fn captures the model, and both news callers retain cache_entry and gen_fn outside the worker (nodes/_otr_model_loader.py:2409–2413; nodes/story_orchestrator.py:770–795). The teardown implementation itself warns that other references can remain (nodes/_otr_model_loader.py:1551–1573). Fix: specify one loader-owned operation record with unique identity, backend/device, abandonment state, completion signal, and retirement owner. Register before work can escape; retire only after the defined safe cleanup boundary. Do not retain completed futures or exception tracebacks indefinitely. Verify: reference lifetime and resource release after both successful and exceptional worker completion.

4. [§3.2–3.3, §4] An occupancy report is not an admission protocol. request_slot governs model acquisition, while _ghost_unload_writer independently checks residency before visual work; unload_llm_if_local_resident uses that same predicate to authorize teardown (nodes/_otr_model_loader.py:1716–1727, :1889; nodes/otr_shot_lock.py:2591–2622). Simply making has_local_resident_llm return true for an orphan would conflate “unsafe to proceed” with “safe to unload.” Fix: retain cache-residency semantics and introduce a distinct occupancy/admission operation used by competing local loads and GPU handoffs. Make admission and registration atomic relative to abandonment; a check followed by an unprotected load recreates the race. Preserve remote bypasses, which consume no local VRAM (nodes/_otr_model_loader.py:2386–2406).

5. [§3.3, §4] The never-returning worker exposes an unresolved product contradiction: bounded waiting eventually refuses, unbounded waiting can prevent publication forever, and proceeding can recreate the collision. Fix: choose explicit states and outcomes. Recommended: interruptible, bounded drain waiting; resume unchanged after safe retirement; otherwise surface a specific resource-busy/recovery-required result. Never report that result as OOM, clear occupancy because time elapsed, or silently choose a smaller model. [ASSUMPTION] This requires interpreting “OOM is the only acceptable killer” as permitting a truthful infrastructure pause; the document must settle that interpretation before implementation.

SHOULD-FIX:
1. [§3.4–3.5] Define acceptance before claiming unchanged behavior or shipping safety. Extend the existing concurrency/deadline coverage in tests/test_orphan_cache_epoch_and_deadline.py and tests/test_gguf_generation_deadline.py with abandoned-worker overlap, completion racing timeout, exceptional exit, and clean-path admission cases. Measure load/cache-hit counts, generation arguments, added wait time, peak memory, and canonical publication on both cards. Compare against baseline variability rather than demanding identical stochastic output. Documentation alone is render-inert; registry or admission code is not exempt from the required test wave (docs/PRODUCTION_SPRINT_LESSONS.md:303–312).

2. [§3.1] Audit abandonment outside the handled timeout classes. _run_with_timeout always executes shutdown(wait=False), but invalidates only in its timeout handler (nodes/story_orchestrator.py:233–308). Verify: whether cancellation or another parent-side exception can leave submitted work running without timeout recovery. Give every reachable abandonment path the same lifecycle ownership.

OPTIONAL / NICE-TO-HAVE:
[§3.2] Include operation identity, backend, phase, abandonment age, and retirement outcome in existing runtime logs to make the next incident attributable.

CUT THESE (scope / over-engineering):
1. [§3.2] Cut allocator readings as the admission authority. The cited incident already demonstrates why aggregate memory is not operation ownership (docs/PROD_BUG_LOG.md:6660–6684). Keep readings as telemetry.
2. [§3.3] Cut automatic smaller-model selection. It changes model policy without resolving orphan ownership and conflicts with §4’s rejection of silent degradation.
3. [§3.2] Cut a general GPU scheduler from this build. [ASSUMPTION] A narrowly scoped local-LLM lifecycle record and the competing handoff boundaries are sufficient; expand only if the route audit proves otherwise.
