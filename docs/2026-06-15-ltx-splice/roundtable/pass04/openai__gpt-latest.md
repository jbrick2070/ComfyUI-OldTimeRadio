<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. The plan cannot reproduce the frozen mini “graph + values EXACTLY” as written; sampler/signature/retention details are still ambiguous or contradict the grounded engine code.

MUST-FIX BEFORE BUILD:
1. [§1, §4B, §4C, §6] Missing `ManualSigmas` despite the hard invariant naming it as part of the exact mini graph. Grounded code currently uses an in-adapter `_SigmasFromValues` pseudo-node and injects `"sigmas"` after class resolution, not Comfy’s `ManualSigmas`. Concrete fix: either add `"sigmas": ("ManualSigmas",)` to the active candidate set and wire the actual mini node with the exact widget values, or explicitly amend §1 to say `_SigmasFromValues` is an allowed non-mini substitution and adjust the “graph EXACTLY” claim. Do not leave both claims.

2. [§1, §4C, §4E] Distilled “exact values” are still env-mutable in the grounded code. `_sampler_name()` reads `OTR_LTX_SAMPLER_NAME`; `_build_graph()` reads `OTR_LTX_CFG`; LoRA strength reads `OTR_LTX_DISTILLED_LORA_STRENGTH`; i2v strength reads `OTR_LTX_I2V_STRENGTH`. That violates the mini invariant: `euler_cfg_pp`, cfg `1.0`, LoRA `0.70`, i2v strength `0.75`. Concrete fix: in `distilled` production mode hardcode these four values to the mini values, or make the smoke gate clear/assert those env vars are unset and fail if they differ. If A/B knobs remain, scope them to `ksampler` mode only.

3. [§4A, §4C] `_distilled_lora_file()` return type is inconsistent with the planned wiring. Grounded code returns `(name, abs_path)`, but §4C wires `lora_name: _distilled_lora_file()`, which would pass a tuple to `LoraLoaderModelOnly`. Concrete fix: define separate helpers, e.g. `_distilled_lora_name()` for the Comfy `lora_name` input and `_distilled_lora_path()` for install/floor checks, or change `_distilled_lora_file()` to return only the Comfy-relative name and update all current tuple call sites.

4. [§4D] VRAM retention is underspecified and likely over-retains. The plan says keep `{"unet","lora",self._TERMINAL}` but also says the resident patcher is the LoRA-wrapped unet. Keeping both `unet` and `lora` can defeat `free_after_use` and hold the pre-LoRA and post-LoRA model outputs. Concrete fix: prove which output is the actual patcher and retain only that key plus terminal. Given §4B says LoRA is always present, the intended key should probably be `"lora"`: `keep={"lora", self._TERMINAL}` and `model = results.get("lora", (None,))[0]`. Add the unit test §4D requires against that exact key.

5. [§1, §4E, §6] Sampler-mode caching can still mismatch the graph. Grounded `load()` resolves `_node_candidates_sampling()` once, but `render_clip()` builds the graph later using live `_sampler_mode()`. If `OTR_LTX_SAMPLER` changes after `load()`, cached classes can be for one sampler while the graph asks for the other. Concrete fix: cache the resolved sampler mode in `load()` and have graph building use that cached mode, or make `render_clip()` detect mode drift and re-resolve classes. Document that manual A/B requires reload/restart if that is the intended contract.

6. [§4E, §7] The planned node-class gate is incomplete for “declaring usable.” §4E lists only `UnetLoaderGGUF/VAELoader/LTXAVTextEncoderLoader/LoraLoaderModelOnly/VAEDecodeTiled`. The invariant also requires the distilled sampler chain and i2v nodes: `KSamplerSelect`, `ManualSigmas` or the approved replacement, `RandomNoise`, `CFGGuider`, `SamplerCustomAdvanced`, `EmptyLTXVLatentVideo`, `LTXVConditioning`, `CLIPTextEncode` if still used, `LoadImage`, and `LTXVImgToVideoConditionOnly`. Concrete fix: `assert_usable()`/the node-class gate must resolve the full active t2v and i2v candidate sets before returning usable, not only the five loader/decode classes.

7. [§4A, §4E] Artifact path resolution is not specified enough to implement safely. Existing grounded code searches checkpoints and HF_HOME-derived dirs for the old 2B/T5 pair. The plan names five