<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. The plan still leaves invariant-breaking env paths, ambiguous load/usable failure semantics, and incomplete test specifications that can pass while the live graph is not the frozen mini recipe.

MUST-FIX BEFORE BUILD:
1. [#1, §11.2] The “hardcode OR smoke asserts env vars are unset/at-mini-values” option violates the hard invariant. A later production run with `OTR_LTX_SAMPLER_NAME`, `OTR_LTX_CFG`, `OTR_LTX_DISTILLED_LORA_STRENGTH`, or `OTR_LTX_I2V_STRENGTH` set can silently diverge from the mini after the smoke passes. Concrete fix: in `distilled` mode, ignore those env vars and hardcode `sampler_name="euler_cfg_pp"`, `cfg=1.0`, LoRA strength `0.70`, and i2v strength `0.75`. Limit those env knobs to `ksampler` mode only, or fail closed if they are set while `distilled` is active.

2. [§5, §11.6] §11.6 says `assert_usable()` checks “only Sage + the 5 weight files + floors”, which conflicts with §5’s opt-out kill switch (`OTR_ENABLE_LTX_VIDEO` defaults ON but `=0` disables). Concrete fix: keep the flag gate as the first `assert_usable()` check with default `"1"`, then Sage, then the 5 weight/floor checks. Node-class resolution belongs in `load()` as §11.6 says.

3. [§4E, §11.6] Current grounded `load()` raises a `RuntimeError` via old `_installed()` / `_ckpt_path()` semantics. That contradicts §4E’s “missing model → `EngineUnusable` LOUD → fallback” and will also keep reporting the old 2B checkpoint path unless fully rewritten. Concrete fix: replace `_installed()` / `_ckpt_path()` / `_text_encoder_name()` usage in `load()` and `_assert_stack_ready()` with the new 5-artifact resolver/floor path, and ensure missing weights produce the same `EngineUnusable(..., MISSING_MODEL, ...)` path used by `assert_usable()` or are never checked in `load()` after `assert_usable()` has passed. [ASSUMPTION] This matters if the engine manager catches `EngineUnusable` for fallback but not arbitrary `RuntimeError`.

4. [§4A, §4E] The plan names new resolver methods but does not explicitly require deleting all old 2B/T5 call sites. Grounded code still uses `_ckpt_name()`, `_ckpt_path()`, `_text_encoder_name()`, `_use_distilled_lora()`, and `_distilled_lora_file()` in graph build, usability, and load paths. Concrete fix: make Phase 1 include a mechanical replacement checklist/test: no live production path references `OTR_LTX_VIDEO_CKPT`, `OTR_LTX_VIDEO_CKPT_NAME`, `OTR_LTX_T5_ENCODER`, `_ckpt_path`, `_ckpt_name`, `_text_encoder_name`, or `_use_distilled_lora`; LoRA name/path are split per §11.3.

5. [§4A, §4E] Model path resolution is underspecified for the new five artifacts. The AV grounding uses `folder_paths.get_full_path(folder, name)` with fallback folders; the LTX video plan only says “mirror” and gives env names. Concrete fix: define the exact folder mapping in the plan/code: unet → `models/unet`, projection ckpt → `models/checkpoints`, video VAE → `models/vae`, text encoder → `models/text_encoders`, LoRA → `models/loras`, all via Comfy `folder_paths` with a deterministic headless fallback. Use those same paths for `_weight_paths()` floors and error messages.

6. [§6] The structured graph test is internally incomplete for the required “both sampler modes” coverage. It says assert `unet→lora→guider.model` and `"lora"` present in both modes, but `guider` exists only in distilled mode; ksampler mode uses `ksampler.model` in the grounded code. Concrete fix: parameterize the graph-shape test by sampler mode: distilled asserts `guider.model == W("lora",0)` and `sampleradv.latent_image`; ksampler asserts `ksampler.model == W("lora",0)` and `ksampler.latent_image`.

7. [§6] “MATCHES the mini’s known-good output” is not a buildable acceptance criterion as written. It gives no baseline artifact path, seed, prompt, length, tolerance, or hash/metric. Concrete fix: name the baseline clip/artifact, pin the request seed/canvas/length/init image/prompt used for the mini reproduction, and define the comparator: byte hash if deterministic, otherwise frame-level metric with numeric tolerance.

8. [§4G, §5, §6] Canvas and init-image routing are left as “confirm” / “ensure” but are part of the invariant. Grounded `_dims()` uses request canvas if present, so `_LTX_DEFAULT_W/H=832/480` will not matter if `render_driver` supplies a larger canvas. Concrete fix: add an automated test that `build_request_from_shot` produces `canvas={"w":832,"h":480}` for `ltx_video`, and with both portrait and scene refs present puts the full-frame scene still in `asset_refs["still"]` before LTX resolution.

SHOULD-FIX:
1. [§4D, §11.7] The VRAM keep-set requirement changed from `{"unet","lora",terminal}` to exactly `{"lora",terminal}`. Concrete fix: make the unit test assert both the `keep` set passed to `run_graph` and the retained patcher source `results.get("lora",(None,))[0]`; do not rely on post-render patcher bucket inspection alone.

2. [§6] The banned-class test should check both candidate dictionaries and resolved graph class names/aliases. In grounded `graph["vaedecode"]["class"]` is a logical alias, so only checking graph class strings can miss a bad candidate mapping.