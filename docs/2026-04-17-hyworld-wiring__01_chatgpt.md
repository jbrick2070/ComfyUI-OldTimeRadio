# Round A -- ChatGPT (gpt-5.4) elapsed=23.9s

Q1. **Pick Option A for v2.0-alpha. No router node needed.**  
Reasoning: this is the smallest change with the largest payoff, and it preserves the proven Node 12 path untouched. Given your own facts, HyWorld is still stub-mode and can legally fail/fallback; adding a router now creates a new single point of failure for no real user benefit. Two MP4s on disk is operationally ugly but architecturally safe, and safety matters more here because C7 says audio is sacred and Node 12 is the known-good baseline. So yes: **under Option A, you do not need a router node for v2.0-alpha.**

Q2. **Yes, all three dangling-socket wirings are correct, with one caveat on execution timing.**  
Reasoning: `Node 3 scene_manifest_json -> Bridge scene_manifest_json` is semantically correct; that output exists specifically to carry scene timing/offset metadata, which is exactly what Bridge says it can consume. `Node 7 output_path -> Renderer final_audio_path` is also the right wire *if* `OTR_EpisodeAssembler` writes the final WAV before emitting `output_path`; based on normal ComfyUI node behavior that is the expected contract, but I have not inspected the implementation in this session, so I’d treat that as a quick code check before merging. `Bridge shotlist_json -> Renderer shotlist_json` is the obvious and correct connection. The only real foot-gun is if Renderer starts on an `output_path` string before the file is fully flushed, but in a sane node implementation the node should only return after the file is closed; verify that in the EpisodeAssembler source rather than assuming.

Q3. **Choose (a) for v2.0-alpha: wait for `scene_manifest_json`, no head-start yet.**  
Reasoning: today the worker is CPU-only and finishes in seconds, so the async pre-bake buys you essentially nothing right now. By contrast, wiring Bridge early without manifest introduces complexity around partial inputs, later amendment, or worker-side re-read semantics that you do not need in alpha. The simplest, safest graph is: let audio complete its normal path, let SceneSequencer emit the manifest, then run Bridge/Poll/Renderer as a post-audio side branch. Revisit head-start only when the worker actually becomes GPU- or wall-clock-significant.

Q4. **Leave VRAM coordination out of the workflow JSON for v2.0-alpha; handle it inside the worker/subprocess layer.**  
Reasoning: since the current worker is CPU-only, a coordinator node in the graph would be pure future-proofing overhead and unnecessary JSON churn. Even once real models land, a file lock / mutex / coordinator internal to `worker.py` or the HyWorld subprocess launcher is the right first containment boundary because it avoids coupling the main ComfyUI graph to low-level scheduling policy. Only promote VRAM coordination into an explicit node if you later need graph-visible scheduling across multiple independent GPU consumers; you are not there yet. This matches your stated preference to avoid low-level VRAM optimization work and keep changes minimal.

Q5. **Do not remove or reduce Node 12 in this commit. Keep it exactly as the baseline renderer.**  
Reasoning: I agree with your lean: Node 12 is the proven path and should remain untouched for alpha. Under Option A it continues to produce the canonical, known-good MP4 while HyWorld produces an experimental second artifact. That gives you immediate integration coverage for the trio without risking the shipping path, and it preserves an easy rollback posture if HyWorld misbehaves. Given C7 and the “don’t break what ships” rule, removing or demoting Node 12 now would be the wrong trade.

COMMIT PLAN
  Files to modify:   `workflows/otr_scifi_16gb_full.json`; optionally a quick verification-only read of the node source that emits/consumes paths: likely `otr_v2/.../episode_assembler*.py` and `otr_v2/hyworld/{bridge.py,poll.py,renderer.py}` to confirm output contracts before wiring
  New nodes in JSON: `OTR_HyworldBridge` fed by Node 1 (`script_json`), Node 2 (`production_plan_json`), and Node 3 (`scene_manifest_json`); `OTR_HyworldPoll` fed by Bridge `hyworld_job_id`; `OTR_HyworldRenderer` fed by Poll `hyworld_assets_path`, Node 7 (`output_path`), and Bridge (`shotlist_json`)
  New links:         `1.script_json -> HyworldBridge.script_json`; `2.production_plan_json -> HyworldBridge.production_plan_json`; `3.scene_manifest_json -> HyworldBridge.scene_manifest_json`; `HyworldBridge.hyworld_job_id -> HyworldPoll.hyworld_job_id`; `HyworldPoll.hyworld_assets_path -> HyworldRenderer.hyworld_assets_path`; `7.output_path -> HyworldRenderer.final_audio_path`; `HyworldBridge.shotlist_json -> HyworldRenderer.shotlist_json`
  Existing links:    unchanged; no removals; Node 12 remains terminal on the existing path
  Test after:        validate JSON loads in ComfyUI with zero orphan/muted regressions; run one full episode smoke test on Windows; confirm final WAV hash matches pre-change baseline exactly; confirm Node 12 MP4 still renders; confirm HyWorld branch produces either a second MP4 path or clean fallback/empty-string behavior; inspect peak VRAM during Bark to confirm no change
  Rollback:          revert the workflow JSON commit SHA; no data migration needed; optionally delete any `io/hyworld_in/*` and `io/hyworld_out/*` artifacts from the test run

FIRST-MOVE: open `workflows/otr_scifi_16gb_full.json` and add the three HyWorld nodes plus the seven links above, leaving every existing link untouched.
