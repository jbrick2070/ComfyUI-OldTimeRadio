<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. The document is an evaluation prompt, not a build-ready spec; it has unresolved engine choice, unproven wheel/install paths, missing registry/profile changes, and no concrete adapter/fallback contract.

MUST-FIX BEFORE BUILD:
1. [Candidate B] The spec asks “What is the REAL no-compile path…” and “export path to frames?” instead of defining one. This is not buildable. Concrete fix: either demote native Hunyuan3D mesh/orbit to NO-GO for this sprint, or provide a verified ComfyUI 0.24.1 workflow/node path that takes `init_image -> mesh/scene -> frame directory/mp4`, with exact node names, model files, Python packages, and proof that all installs are wheel-only on Windows/Python 3.12/torch 2.10/cu130/sm_120. Do not ship Candidate B on the current wording.

2. [Hard constraints] The wheel-only constraint is not operationalized. “wheel-only” is stated, but no candidate is required to provide a reproducible install proof. Concrete fix: add an acceptance gate per candidate: exact `pip install --only-binary=:all:` command or ComfyUI-manager install manifest, resolved package versions, and explicit “does not install/downgrade torch/numpy/transformers” result. Any candidate without that proof is NO-GO.

3. [Candidate D] Gaussian/splat lanes are incompatible with the stated no-compiler environment unless a specific Windows/Python 3.12/torch 2.10/cu130/sm_120 wheel is identified. The document currently leaves this as a question. Concrete fix: mark LGM/TRELLIS/splat-renderer lanes NO-GO for the first test unless the spec names the exact rasterizer package and wheel artifact. Grounding supports blocking existing `trellis_talk`: `registry.py CAPABILITIES` marks it `required_toolchain: "cu128_toolkit"`.

4. [Candidate E] World-model lanes are left as “assumed NO-GO” but not converted into a decision. Concrete fix: mark HyWorld/WorldMirror-style world models NO-GO for this sprint unless the spec provides the same wheel-clean install proof and a bounded VRAM/render contract. Otherwise they should not be considered dropdown candidates.

5. [Candidate A] “Pure wheels, tiny VRAM, deterministic” is asserted without naming the actual implementation. Concrete fix: specify whether `still_parallax` uses DepthAnythingV2 via an existing Comfy/core node, a custom node, or direct Python/diffusers/transformers code. Include exact model asset, package dependencies, dtype/device behavior, and deterministic seed handling. If using an existing still generated by `flux_still`, do not call the whole path commercial-clean: grounding shows `FluxStillFamily.commercial_clean = False` and `CAPABILITIES["flux_still"]["model_requirements"] = ["flux.1-dev"]`.

6. [Context / Candidate A / registry.py CAPABILITIES] Adding an adapter alone is insufficient. Grounding says new engines need capability declaration rows consumed by capability profiles. Concrete fix: add a new engine id such as `still_parallax` to `CAPABILITIES` with `vram_class`, `vram_estimate_mb`, `required_toolchain: None`, `requires_sidecar`, `cpu_ok`, and `model_requirements`; then ensure the capability profile derives it. Do not reuse `still_kenburns` silently if the operator needs a distinct dropdown option.

7. [Context / Candidate B / Candidate C / registry.py VideoEngine] The spec does not define the new engine’s `family`, `required_inputs`, `roles`, `default_roles`, or `commercial_clean`. Grounding’s `VideoEngine` protocol and cheap adapters require these fields. Concrete fix: for every proposed first/runner-up engine, define the exact adapter metadata. For `still_parallax`, likely use `family = "static_motion"` or add/validate a new family token; set `required_inputs` to `("init_image",)` if it requires a still/portrait, or explicitly implement fallback when absent. Verify actual family validation in `schemas.FAMILIES` before inventing a new `3d`/`2.5d` family. [ASSUMPTION]

8. [Context / 16gb_full.json / cheap_families.py] Slot naming and role compatibility are underspecified. The app slots are `announcer_visual`, `music_visual`, `other_beats_visual`, but grounded cheap engine roles include `scene_broll`, `background_abstract`, `character_video`, `announcer_visual`, and `music_visual`; there is no grounded `other_beats_visual` role on the cheap adapters. Concrete fix: define exactly which role tokens the new 3D-ish engine supports and how `other_beats_visual` maps through `role_compat`. Verify that `OTR_ShotLock.assert_usable` accepts the new dropdown value for each intended slot.

9. [Hard constraints / cheap_families.py] The output canvas requirement says 1472x832 landscape, but grounded cheap families default to 832x480 when request canvas is absent. Concrete fix: the new adapter must explicitly require or set `canvas.w = 1472`, `canvas.h = 832`, `fps`, and `timing.target_frame_count`; do not rely on `_canvas_dims` defaults if cloning the cheap-family base.

10. [Hard constraints] The exact-frame-count contract is not translated into acceptance criteria. Concrete fix: require an automated smoke test that renders a clip and verifies: `frame_count == timing.target_frame_count`, silent/no audio stream, yuv420p/bt709 or documented canonicalization, padded/cropped to 1472x832, and ledger contains the requested seed and engine id.

11. [Hard constraints / Context] VRAM sequencing is not specified. Existing profile `16gb_full` already defaults heavy video slots to `ltx_video`/`humo`, and the new candidate may run after a heavy still/video engine. Concrete fix: define render ordering and unload points: previous heavy engine unloaded before depth/3D/orbit load; NVML preflight checks against `vram_budget_mb: 14500`; teardown must call adapter `unload()`/`teardown()` and reclaim CUDA memory where applicable. [ASSUMPTION]

12. [What the panel must deliver / Context] Fallback-chain behavior is required by the app but not defined for any new engine. Concrete fix: add explicit fallback chains, e.g. `still_parallax -> still_kenburns -> station_card/abstract`, with LOUD ledger-stamped reasons for install failure, model missing, VRAM refusal, render exception, and frame-count mismatch.

SHOULD-FIX:
1. [Candidate F] “Camera-control video LoRAs on engines we ALREADY run” is the most dependency-light 3D-feel runner-up, but it is not specified. Concrete fix: if kept, define it as a preset/LoRA variant of `ltx_video` or `wan_i2v`, not a new dependency lane; include exact LoRA/model asset, license, VRAM delta, and fallback to the base engine or `still_kenburns`.

2. [Candidate C] SV3D/Zero123-style orbit-video models are under-specified. Concrete fix: require exact model name, license, current Comfy/native workflow, resolution/frame-count behavior, and dependency proof. Without that, mark Candidate C “research only, not first test.”

3. [Hard constraints] Determinism is stated but not testable. Concrete fix: define what “reproduce” means: same seed produces byte-identical frames, perceptually identical frames, or same model latent path but encoder bytes may differ. Add seed propagation from request seed into Comfy/Python/random/torch/numpy and record nondeterministic backend caveats.

4. [Hard constraints] Licensing needs a per-engine decision, not “commercial-clean strongly preferred.” Concrete fix: add a required field for model/code/output license status and whether the engine is allowed in commercial profiles. Block or label NC/research models in the capability registry/profile.

5. [Context] “ComfyUI-native or near-native” is ambiguous for adapter implementation. Concrete fix: define allowed invocation modes: in-process node execution, headless Comfy workflow API, subprocess sidecar, or direct Python. For each, specify timeout, log capture, model-path resolution, and cleanup.

6. [Candidate A] Depth-parallax needs a degraded mode. Concrete fix: if depth inference fails or no valid still exists, fall back to ordinary `still_kenburns`; if depth map is low confidence/blank, use flat pan rather than producing warped artifacts.

7. [Candidate B] Mesh/orbit output needs canonicalization details if ever unblocked. Concrete fix: define camera path, orbit duration, transparent/background handling, texture/vertex-color fallback, frame renderer, and mp4 encoding path. Do not leave “Load 3D node camera?” as an implementation question.

8. [16gb_full.json] Profile defaults are not updated. Concrete fix: decide whether the new engine is selectable-only or a default override for one slot. If selectable-only, no role override change; if first test should appear by default, update `role_overrides` intentionally and provide rollback.

OPTIONAL / NICE-TO-HAVE:
- Add a small operator-facing label: “2.5D parallax” vs “true 3D mesh” so the dropdown does not overpromise.
- Add visual QA thumbnails/contact sheet generation for the first 8 frames, midpoint, and last frame.
- Add a per-engine “offline verified” badge in the registry metadata if cloud/API nodes are excluded.

CUT THESE (over-engineering):
1. [Candidate B] Cut Hunyuan3D texture/paint work from the first build. It adds model/dependency/license uncertainty and is unnecessary if the goal is a first dropdown-visible 3D-flavored clip in days.

2. [Candidate D] Cut Gaussian/TRELLIS/LGM investigation from the first build unless a wheel artifact is already known. Grounding already shows `trellis_talk` blocked by `cu128_toolkit`; keeping this in the sprint invites compiler-toolchain failure.

3. [Candidate E] Cut world-model evaluation from the first build. It does not fit the “days, wheel-only, <=14.5 GB, exact-frame-count clip” goal without substantial verification.

4. [Hard constraints] Cut cloud-lane footnotes from the build spec. The render path is explicitly 100% local/offline; API providers can live in a separate research note, not the implementation plan.

5. [What the panel must deliver] Cut the broad 2025-26 model survey requirement for the first test case. Replace it with one selected implementation path plus one runner-up. The smallest viable first test is `still_parallax` over an existing still, with fallback to `still_kenburns`; runner-up is an already-installed `ltx_video`/`wan_i2v` camera-orbit preset if its LoRA/assets are already wheel-clean.