<!-- requested_model: deepseek/deepseek-v4-pro | resolved_model: deepseek/deepseek-v4-pro-20260423 -->

VERDICT: no. The plan contains a critical contradiction about the lease-skip marker and an ungrounded assumption about the video dispatcher lease site that will cause a runtime error.

MUST-FIX BEFORE BUILD:
1. [§4.1 vs §4.2] The plan says "All three adapters set a class attribute `is_network = True`" and "The lease sites read `getattr(engine, "is_network", False)`". The image dispatcher (`otr_image_gen_dispatcher.py`) calls `_lease.acquire()` BEFORE resolving the engine object — it acquires the lease unconditionally, then calls `gen_fn(request)`, which eventually calls `_inprocess_gen_fn` which resolves the engine. The plan's proposed skip ("Resolve the engine before the `try`, compute `skip = is_network(engine)`, and skip `acquire`") requires restructuring the dispatcher's lease bracket. The current code acquires the lease, then calls `gen_fn`, then releases in finally. If the engine is a cloud engine, the lease is held for the entire cloud API call duration (potentially minutes), blocking all local GPU work. Fix: restructure `dispatch_images` to resolve the engine before the lease block, check `is_network`, and only acquire/release the lease for non-network engines. The plan acknowledges this but understates the code change — this is not a one-line skip; it's a control-flow restructure of the main dispatch loop.

2. [§4.2] The plan says "Apply the same branch at the video render lease site." The grounding excerpts do NOT include the video dispatcher / ShotLock code. The video lease is managed by `MotionEngineBase.prepare()` which unconditionally calls `_GR.acquire()`. For cloud video engines, `prepare()` would acquire the GPU lease before a cloud API call, blocking local GPU work for the entire cloud render duration. Fix: cloud video engines must NOT inherit from `MotionEngineBase` (which hardcodes the lease in `prepare`), OR `MotionEngineBase.prepare()` must be modified to skip the lease when `is_network` is True. The plan does not specify which. Verify: the video dispatch path (ShotLock / render node) to confirm the lease site.

3. [§2a] The plan says `render_image(self, request, prepared=None)` must be two-arg to match `_inprocess_gen_fn`. The grounding shows `_inprocess_gen_fn` calls `eng.render_image(request, prepared)`. This is correct. However, the plan also says the method should return "an in-memory uint8 numpy array" OR "a `.png` path". `_coerce_pixels` in the dispatcher handles both. But the plan does not specify how the cloud adapter will convert a Comfy API node's IMAGE tensor to uint8 — the grounding shows `flux_gen1` uses `images_to_uint8(...)[0]` from `wrapper_bridge`. The cloud adapter will NOT have access to `wrapper_bridge` (it's calling a remote API, not in-process Comfy nodes). Fix: specify the exact return format for the cloud adapter. If the Partner node returns a file path, return the path. If it returns bytes, decode to uint8 array. Do not assume `images_to_uint8` is available.

4. [§5] The invocation-seam spike is gating the entire build but is listed as a pre-build spike, not resolved in the plan. The plan says "Before coding the three adapters, settle ONE question with a throwaway probe." This means the plan is incomplete — the actual API call mechanism is unknown. Option 1 (import the partner node class and call it in-process) assumes the Partner node is a Python class that can be imported and called directly, which is ungrounded. Option 2 (POST to ComfyUI server) requires a running ComfyUI instance, which may not be available in all deployment contexts. Option 3 (direct HTTP call) bypasses ComfyUI entirely. Fix: the spike must be completed and its results folded into the plan before coding begins. The plan cannot be "build-ready" with an unresolved gating question.

5. [§4.3] The cost guard `reserve_cloud_cost` is described as "AFTER the request is assembled and BEFORE `render_*`" and "Idempotent by `request_id`". The plan does not specify where this function lives (which module), its signature, or how it tracks the running total in `ledger["billing"]`. The image dispatcher already has a `ledger` dict it mutates; the video path has its own ledger. The plan says "the image phase and the later video phase share ONE episode ceiling" but does not explain how the video phase reads the running total from the image phase. Fix: specify the shared state mechanism (ledger key? external file? in-memory singleton?) and the exact function signature.

6. [§2c] `cloud_kling_avatar` requires `audio_ref` and `init_image`. The plan says "duration = the `audio_ref` duration (probe the WAV header)". The grounding shows `eng_humo._build_render_request` extracts `audio_path` from the request's `audio_ref` via `_ref_path()`, which handles both string paths and dicts with a `path` key. The cloud adapter must do the same WAV probing. Fix: specify the WAV duration probing method (stdlib `wave` module? `ffprobe` subprocess?) and ensure it matches the `audio_ref` format from the request schema.

7. [§6] The plan says "No new node, no new link for v1" but then says "If the cost guard adds a Director widget (e.g. a per-episode credit ceiling), that is ONE new optional widget APPENDED at the end of the Director's widget list." The plan's §4.3 says the ceiling is `OTR_CLOUD_CREDIT_CEILING` env/config — no Director widget. This is a contradiction. Fix: pick one. If env-only, remove the widget mention. If widget, add it to the build plan and update the workflow JSON.

SHOULD-FIX:
1. [§4.2] The plan says to skip "the unconditional post-gen `wait_until_below_mb` NVML probe" for network engines. The grounding shows the image dispatcher calls `_lease.wait_until_below_mb(15000, attempts=3, sleep_s=0.0)` in the dispatch loop AFTER the finally block, outside the lease scope. This call is NOT conditional on the lease being held — it runs even if `lease is None`. For a cloud engine that never acquired the lease, this NVML probe is harmless but wasteful (it probes the local GPU after a cloud call). Fix: guard the post-render NVML probe with the same `is_network` check.

2. [§3] The plan says to add `CAPABILITIES` rows with `vram_class="cpu"`. The grounding does not show the `CAPABILITIES` schema or where it's consumed. The plan says "no `capability_profiles.py` validator change" is needed because `cpu` ranks 0. Verify: the capability validator's `max_model_class` check logic to confirm `cpu` is always <= any profile's max.

3. [§2b] `cloud_ltx2` has `fallback_engine = "still_kenburns"`. The plan says "cloud has no local sibling" — but the local `eng_ltx_video` exists. If the cloud call fails (timeout, auth, credit), the fallback chain should probably go through the local LTX engine before hitting still_kenburns, unless the operator explicitly chose cloud. The plan's fallback is correct for "cloud is the only LTX option" but the operator might expect local fallback. Fix: document that cloud engine failure skips local LTX entirely and goes straight to still_kenburns.

4. [§7] The test list says "`assert_usable` fails closed when flag off / no key / over budget". But §4.3 moved the cost guard OUT of `assert_usable`. The test description is stale. Fix: update the test spec to reflect that `assert_usable` only checks flag + key, and cost is checked separately.

5. [§8] Build order S1 says "dispatcher lease-skip + `declared_isolation="network"` marker". The plan §4.1 settled on `is_network = True` class attribute, NOT `declared_isolation="network"`. The sprint description is stale. Fix: update S1 to match §4.1.

OPTIONAL / NICE-TO-HAVE:
- The plan mentions `commercial_clean = False` with a "verify BFL/Comfy ToS" note. Add a dated URL field to the engine metadata for the ToS source.
- The `other_beats_video_model = cloud_kling_avatar` partial-failure behavior (character beats run, scene/background fail closed) should be documented in the Director's tooltip or a README.

CUT THESE (over-engineering):
- None identified. The plan is appropriately scoped for three new engines.

[ASSUMPTION] §5 invocation-seam: the plan assumes one of three options will work. Until the spike is done, this is ungrounded.
[ASSUMPTION] §4.2 video lease site: the plan assumes the video dispatcher has a lease site analogous to the image dispatcher. The grounding does not include the video dispatch code, so the exact change cannot be verified.