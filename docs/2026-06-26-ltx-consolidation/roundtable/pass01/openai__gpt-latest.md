<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. The plan still has unresolved routing semantics (portrait vs wide vs required still), an over-broad capability gate, and stale registry/dropdown/fallback assumptions that can make the “one engine + robust still routing” story false.

MUST-FIX BEFORE BUILD:
1. [Goal / end state + Invariant 5 + Part A + Part B] The plan contradicts itself on what still `ltx_audio_in` should consume for voice/character use. The Goal says “talk is just I2V on a face still,” Part A gives `ltx_audio_in` `character_video` and `_is_talk=True`, but Invariant 5 and Part B classify `ltx_audio_in` as a wide scene engine that must use the 16:9 scene still and never the vertical portrait. Concrete fix: decide one contract and encode it. Smallest safe fix: declare `ltx_audio_in` a wide audio-conditioned engine only; for `character_video` it must consume a 16:9 `scene_character`/scene still, not the portrait. Reserve vertical portrait routing for `family=="audio_driven_face"` only. Update Part A wording so it no longer says face/portrait talk still for `ltx_audio_in`.

2. [Part B + F2] `accepts_still` is too coarse to be the sole routing capability. Current grounded code has separate semantics: scene still for `_SCENE_INIT_FAMILIES`, scene still for `flux_still`/`flat_still`, optional I2V for `ltx_video`, portrait for `audio_driven_face`, fodder for `requires_mesh_fodder`. A single `accepts_still` boolean cannot distinguish “scene plate,” “portrait,” “mesh fodder,” “optional still,” and “required still.” Concrete fix: introduce or derive an explicit route enum/helper, e.g. `still_route(engine, shot) -> scene_still | portrait | mesh_fodder | none`, and use `required_inputs` only to decide whether missing still is terminal. Do not ship a boolean-only rule.

3. [Part B + Invariant 3] Missing-still behavior is undefined and internally contradictory. Part B says missing still gets `init_image=""`, stamped LOUD, and may continue; but grounded `LtxAudioInEngine.render_clip` raises if `_is_talk and not plan["init_image"]`. That is fine if intentional, but the plan calls it “degrade” rather than terminal failure. Concrete fix: specify two policies: optional-still engines like `ltx_video` may render text-only with `init_source=missing_scene_still`; required-still engines like `ltx_audio_in` fail before GPU with a clear `RenderError`/input-gap message and trace stamp. Add tests for both.

4. [Part B + F4] The `OTR_ENABLE_LTX_I2V` kill-switch is unresolved and can break the new default engine. Current grounded code gates only `ltx_video` I2V. If the unified rule applies the kill-switch to all LTX rows, `ltx_audio_in` can lose its required `init_image` and fail by design. Concrete fix: scope `OTR_ENABLE_LTX_I2V` to optional `ltx_video` I2V only, or create a separate explicit flag for optional scene-still conditioning. Do not gate `ltx_audio_in`’s required init image with the old `ltx_video` switch.

5. [Part C + registry.py grounding] The plan omits `VALIDATED_ENGINES`. Grounded `registry.py` still lists `"ltx_av_music"` and `"ltx_av_talk"` in `VALIDATED_ENGINES`; `ltx_audio_in` is not listed. Since Part C makes `16gb_full.json` default announcer/music to `ltx_audio_in`, leaving it out of the tested-only gate makes the shipped profile and UI validation story inconsistent. Concrete fix: remove both legacy IDs from `VALIDATED_ENGINES` and add `ltx_audio_in` only if the same commit includes the required GPU validation record/tests; otherwise do not make it the shipping default.

6. [Part B + render_driver.py grounding] The capability-driven rule assumes the selected engine is registered/imported when `build_request_from_shot` runs. Grounded current routing uses hardcoded maps partly to cover possibly-unimported engines (`ENGINE_FAMILY`, `SYNTH_FALLBACKS` comments). A registry-only `engine_consumes_still()` with a “safe default” can silently skip still routing for an unimported selected engine. Concrete fix: either guarantee/import all video adapters before request building, or store route metadata in a static declaration table available at cold import, or fail loud on an unknown selected engine instead of defaulting to “does not consume still.” [ASSUMPTION] This depends on the unseen adapter import bootstrap.

7. [Part B + F5] The plan has not proven the unified branch preserves the `station_card` exclusion. Grounded code explicitly includes `flux_still`/`flat_still` and explicitly excludes `station_card` even though all are static-image-ish lanes. If `station_card` inherits/defaults `accepts_still=True`, the proposed boolean route will change announcer-card behavior. Concrete fix: before build, enumerate every registered engine’s `accepts_still`, `family`, `render_aspect`, `required_inputs`, and intended route; pin `station_card` to `none` unless the operator explicitly wants it scene-still driven. [ASSUMPTION] The `station_card` adapter is not shown.

8. [Part D] Tests do not cover the actual high-risk routing matrix. The listed tests rename engines, but they do not explicitly assert: `ltx_audio_in` announcer/music gets `scene_still`; `ltx_audio_in` missing scene still fails loud because init is required; `audio_driven_face` keeps portrait; wide engines clear portrait on missing scene still; `mesh_stage` keeps mesh fodder; `station_card` remains excluded. Concrete fix: add a table-driven `build_request_from_shot` test over these engine/family/capability cases before touching live workflow JSON.

SHOULD-FIX:
1. [Goal / end state + Part C] The plan says routing should be model-agnostic, but Part C still preserves name-map patching in `ENGINE_FAMILY`, `_LTX_OPEN_ENGINES`, scene-prompt branches, and canvas clamp. Concrete fix: make `engine_family()` registry-first and replace LTX-AV name checks with declared capabilities (`requires_flag=="OTR_ENABLE_LTX_AV"`, `render_aspect`, route enum). Keep only explicit health-check sets where the product concept truly needs a named class of engines.

2. [Grounded eng_ltx_av.py comments + Part A] The grounded module header and class docstrings still describe “two adapters,” legacy back-compat, `ltx_av_music` T2V, and default-off/dark behavior, while the plan deletes those adapters and makes `ltx_audio_in` the 16gb default. Concrete fix: update the module-level narrative in the same commit so future maintainers do not rebuild the deleted split.

3. [Grounded render_driver.py comments + Invariant 3] `render_driver.py` still contains a large fallback-chain narrative, `make_fallback_of`, universal floor constants, and soak assertions for fallback decisions, while `render_shot` says fallbacks are disabled. The plan only removes two `SYNTH_FALLBACKS` entries, leaving the architectural story incoherent. Concrete fix: either formally keep fallback scaffolding as dead/test-only legacy, or remove/update the docs/tests around it. Do not leave “guaranteed floor fallback” comments beside “NO FALLBACKS” behavior.

4. [Part C.6] “Leave or regenerate” for `otr_scifi_16gb_full_api.json` is too loose if downstream tooling reads it. Concrete fix: state whether it is ignored by all validators/runtime paths. If any launcher or test consumes it, regenerate from canonical JSON; otherwise add it to generated-artifact exclusions. [ASSUMPTION] Consumer paths are not shown.

5. [Part E] “Commit AND push… reset the box… overnight 420 soak” mixes implementation, release ops, and long-running validation into a build plan. Concrete fix: split into pre-merge acceptance vs post-merge soak. Required pre-merge: unit/regression/workflow validator/smoke. Overnight soak should block release promotion, not the code refactor itself.

OPTIONAL / NICE-TO-HAVE:
- Add a small diagnostic dump in validation: engine id, family, accepts_still, required_inputs, render_aspect, still_route, default_roles.
- Add a migration note mapping `ltx_av_music`/`ltx_av_talk` to `ltx_audio_in` for old profiles/workflows, even if runtime aliases are deliberately not supported.
- Add a one-shot script to scan canonical workflow `widgets_values` for deleted engine IDs.

CUT THESE (scope / over-engineering):
1. [Part A / F3] Cut the `ltx_audio_in` vs `ltx_av` rename discussion. Keeping `ltx_audio_in` is already the least-churn path and serves the operator directive.
2. [F4] Cut “retire `OTR_ENABLE_LTX_I2V`” from this change. Scope it narrowly to `ltx_video` and revisit later; retiring it while changing routing multiplies behavioral risk.
3. [F1] Cut the “narrow safe-mirror” fork if the stated goal is robust model-agnostic still routing. Adding one more `ltx_audio_in` name branch preserves the patchwork the plan says it is deleting.
4. [Part C.6] Cut hand-editing any stale generated API JSON. Either regenerate from canonical workflow or leave it untouched with an explicit non-consumer proof.
5. [Part E] Cut the overnight 420 soak as a pre-build requirement. Keep it as post-build/release validation; it does not clarify the architecture and will slow iteration on the routing contract.