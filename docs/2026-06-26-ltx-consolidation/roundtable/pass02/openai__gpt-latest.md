<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. The plan has unresolved interface/control-flow gaps in the new role router, and several described behaviors cannot be implemented by the shown `render_driver.py` without additional signature/schema changes.

MUST-FIX BEFORE BUILD:
1. [B0/B1] `_is_character_face_beat(shot)` is not implementable as specified because it needs data not reliably present on `shot`. The plan says “char_id present,” but current `build_request_from_shot` resolves `char_id` from `shot.get("char_id") or line.get("char_id")`; a pure `shot` classifier cannot see the line fallback. It also references `family` without defining whether that comes from `shot["family"]`, `engine_family()`, or the registered engine. Concrete fix: define signature as `_is_character_face_beat(shot, *, engine=None, line=None, char_id="") -> bool`, compute family from registered engine first, and pass the already-resolved `char_id` from `build_request_from_shot`.

2. [B1] `still_route(engine, shot) -> {portrait|scene_still|mesh_fodder|none}` is insufficient to build a request. Current routing needs `ledger`, `line`, `char_id`, `_still_index`, `_portrait_index`, `_mesh_fodder_index`, `still_pool_key`, missing flags, and observability stamps. A route enum alone cannot select the actual path or produce the required “fail LOUD before GPU” message. Concrete fix: make the helper return a structured result, e.g. `StillRoute(route, path, init_source, missing_required, missing_optional, observability)`, with signature including `ledger`, resolved `char_id`, and beat id.

3. [B1/D] The “consumes a still” definition contradicts the required test matrix for `flux_still`/`flat_still`. B1 defines optional still consumption as `accepts_still and engine in {ltx_video}`. D requires `flux_still/flat_still -> scene_still wide, portrait cleared`. Grounding shows `flat_still accepts_still=True`, but it is not in the optional set. If `flat_still.required_inputs` does not include `init_image`, the helper will route `none`, failing the planned test. Concrete fix: explicitly include `flux_still` and `flat_still` in the optional still consumer set, or define a registry capability for optional scene-still consumers. Verify `flux_still.required_inputs` and `flat_still.required_inputs`.

4. [B1] The “required missing still -> FAIL LOUD before GPU + trace stamp” path is not wired into current control flow. If `build_request_from_shot` raises before returning a request, `run_episode` never creates a trace row because trace rows are built only after `render_shot` returns. Concrete fix: either remove “trace stamp” from pre-request failures, or introduce a typed `InputGap` exception carrying `shot_id/init_source`, catch it in `run_episode`, append a trace/error row, then re-raise.

5. [B1/_assert_family_inputs_satisfiable] Required `init_image` for `ltx_audio_in` will not be caught before GPU unless `_assert_family_inputs_satisfiable` is changed. Current `_assert_family_inputs_satisfiable` validates `FAMILY_REQUIRED_INPUTS` by family; `ltx_audio_in` has family `audio_conditioned_video`, while its engine-level `required_inputs` includes `init_image`. The current family-level check can allow a request missing `init_image` through until `LtxAudioInEngine.render_clip()` raises `GraphExecutionError` after engine setup. Concrete fix: after registry lookup, validate `getattr(engine, "required_inputs", ())` against `_present_request_tokens(request)` before `assert_usable/load/render_clip`.

6. [B2] The audio routing rule does not match current slicing control flow. Changing `_uses_ambient_master_audio()` to return false for character-face beats only prevents synthesized timing for untimed master slices; it does not prevent the later generic slice when `line.start_s/dur_s` exist. In current `build_request_from_shot`, any beat with no per-line wav and valid timing can still slice the frozen master. Concrete fix: restructure the master-slice block so character-face beats either require a clean per-line wav and raise loud if missing, or explicitly allow master slicing. The plan must choose one; “clean own-voice / no-audio degrade” is not compatible with `ltx_audio_in.required_inputs` containing `audio_ref` and no fallbacks.

7. [B2/Goal] “no-audio degrade” for `ltx_audio_in` character beats is internally inconsistent. `LtxAudioInEngine.render_clip()` raises if `audio_ref` is missing, and `render_shot()` has no fallback. So a missing clean voice is terminal, not a degrade. Concrete fix: define missing character audio as a pre-GPU `InputGap`/`RenderError`, or remove `audio_ref` from `ltx_audio_in.required_inputs` and implement a real no-audio path. The latter contradicts the stated engine contract, so the smaller fix is terminal input-gap.

8. [B3] Prompt fallback is underspecified and will not work by only gating the scene-prompt branch. Current character fallback prompt is under `elif _fam == "audio_driven_face"`. `ltx_audio_in` has family `audio_conditioned_video`, so an `ltx_audio_in` character beat with no M4 prompt will keep `build_request()`’s generic radio-studio prompt, violating “character prompt / char-fallback.” Concrete fix: replace the family-gated prompt fallback with `_is_character_face_beat(...)`, including the gear scrub and `_CHAR_FACE_FALLBACK_PROMPT` paths.

9. [B0] The classifier’s `char_id present AND role not in (announcer_visual,music_visual)` rule is too broad and can misroute non-face scene beats with a character id to portrait. Current code has roles like `scene_broll`/`background_abstract`, and lines may carry `char_id`. Concrete fix: use `role == "character_video"` as the primary face signal; only use `char_id` fallback when role is missing/legacy or when the selected engine family is explicitly face-oriented. Otherwise route scene roles to scene still.

10. [C3/B1] “Fail LOUD on an unknown selected engine” conflicts with current helper usage unless all callers are updated. Current `build_request_from_shot` calls `engine_family(_eng_id, "")`, which returns defaults for unknown names, and `_requires_fodder` silently treats unregistered engines as false. Concrete fix: at the start of `build_request_from_shot`, if `_eng_id` is nonempty and `_vreg.is_registered(_eng_id)` is false, raise `LookupError`/`RenderError` before any routing. If synthetic unregistered engines are still allowed elsewhere, document and exempt them explicitly.

11. [Part A/C/D] Deleting `LtxAvTalkEngine` and `LtxAvMusicEngine` requires updating all import sites, not only registry/tests. Grounding shows `eng_ltx_av.__all__ = ["LtxAvTalkEngine", "LtxAvMusicEngine", "LtxAudioInEngine"]`; tests are listed, but runtime imports are not enumerated. Concrete fix: grep for `LtxAvTalkEngine`, `LtxAvMusicEngine`, `ltx_av_talk`, and `ltx_av_music` across code, tests, workflow JSON, docs/debug fixtures, and launcher/validator paths; update or remove every runtime reference before deleting the classes.

12. [C3] The canvas clamp change must include `ltx_audio_in`, but the plan says “replace the name-set with `("ltx_audio_in",)`” without addressing `render_single()`. Grounding shows `render_single()` derives wide canvas from `render_aspect` and does not apply the LTX-AV 512x288 clamp. A smoke using `render_single("ltx_audio_in")` can render at `832x480`, not the 512x288 AV ceiling path. Concrete fix: apply the same `OTR_LTX_AV_RENDER_CANVAS` clamp in `render_single()` for `ltx_audio_in`, or state that smoke must go through `build_request_from_shot`.

SHOULD-FIX:
1. [B1] Define the exact type expected for `engine` in `still_route`. Current code commonly uses engine ids and only sometimes fetches registered engine objects. Concrete fix: use `engine_id: str` as input, fetch `_vreg.get_engine(engine_id)` inside the helper, and return loud on unknown.

2. [B1] `_present_request_tokens()` currently counts `init_image` present if the key exists in `asset_refs`, even if the value is empty. If engine-level required input validation is added, this can false-pass malformed requests. Concrete fix: change to `if (get("asset_refs") or {}).get("init_image"):`.

3. [B1/Coverage] The plan focuses on render-time consumption but not upstream still generation. `ltx_audio_in` requires `init_image`; if the image/still coverage planner does not mint scene stills for the new role routing, renders will fail. [ASSUMPTION] Verify and update the still target derivation code so `ltx_audio_in` scene/bookend beats get scene stills, character beats get portraits, mesh fodder remains separate, and `station_card` is still excluded.

4. [B3] The existing “append stable centered subject” clause applies to any engine id starting with `"ltx"` and non-announcer/music roles. That will include `ltx_audio_in` scene-like roles if classifier/role data is ambiguous. Concrete fix: gate this on `_is_character_face_beat(...)`, not `engine_id.startswith("ltx")`.

5. [C3] `ENGINE_FAMILY["ltx_audio_in"]="audio_conditioned_video"` should be added before any old entries are removed, and `engine_family()` already prefers `ENGINE_FAMILY` over registry. Concrete fix: either add the mapping or change `engine_family()` to registry-first as the plan suggests; do not leave both paths inconsistent.

6. [D] The routing-matrix test needs to assert actual request fields, not just route labels: `asset_refs.init_image`, `audio_ref.path`, `text_prompt`, `canvas`, and `observability.init_source`. Otherwise it will miss the current class of bugs where `init_source` says one thing and the request carries another.

7. [D] Add a negative test for `ltx_audio_in` character with missing clean audio. The plan’s behavior is ambiguous; the test should lock whether it raises input-gap or slices master audio.

8. [E] “full pytest + Bug Bible + validator” is not enough for the workflow change unless the positional `widgets_values` audit checks node-87 by index. The plan mentions this invariant, but D should include a test that the exact widget slot changed from `ltx_av_music` to `ltx_audio_in` without shifting adjacent values.

OPTIONAL / NICE-TO-HAVE:
- Add a small debug log row for the three independent routing decisions: `beat_kind`, `still_route`, `audio_route`, `prompt_route`. This will make future routing regressions inspectable without replaying the full request object.
- Rename `_is_talk` in `_LtxAvBase` later; after consolidation it really means “requires init_image,” not talk. Not necessary for build if comments are corrected.

CUT THESE (over-engineering):
1. [Part A] “If operator prefers strict gating, hold VALIDATED add until smoke is green and land it as a one-line follow-up in the same session.” This creates a two-step state where profile JSON can select an engine hidden from the tested-only dropdown. Cut the fork: either add `ltx_audio_in` to `VALIDATED_ENGINES` in this commit with the smoke gate, or do not change the profile default yet.

2. [Verify-at-build #1] Runtime enumeration of every registered engine’s full route behavior is excessive for implementation code. Keep it as a table-driven test fixture, not production logic. It is safe to cut from runtime because the helper should be deterministic and covered by tests.

3. [Part C3] “engine_family() registry-first where cheap” is a broad refactor unrelated to the rename. Cut or scope it to `ltx_audio_in` only. The concrete need is to avoid stale family mappings for this engine; a larger family-resolution rewrite increases blast radius.