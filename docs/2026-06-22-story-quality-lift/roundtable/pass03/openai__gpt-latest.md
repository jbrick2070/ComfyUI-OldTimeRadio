<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. The plan still wires one non-existent feedback channel, one non-existent per-line audit channel, and one stage-direction detector at a site that likely no longer has the raw draft.

MUST-FIX BEFORE BUILD:
1. [§4, §7.1] DEFECT 2 auto-repair via `needs_full_rerun` is not buildable with frozen workflow JSON. Grounding W1: `needs_full_rerun` is only a cascade output/string + `meta.freeze_verdict`; no node re-invokes the writer; the writer is upstream, does not read `needs_full_rerun`/`regeneration_hint`/`freeze_verdict`, and `_PL.new_ledger(...)` creates a blank ledger with empty meta. `meta["regeneration_hint"]` is written but read by nobody. Concrete fix: ship v1 as detection + LOUD telemetry only. Remove the `meta["coherence_hints"]` auto-repair path, max-1 stance rerun logic, and any acceptance expectation that the hint will influence a regenerated episode. Future auto-repair requires a real feedback edge or an explicit operator-supplied input, which violates the current JSON-free constraint.

2. [§3] The proposed per-row `meta["role_coercion"]` audit channel does not exist. Grounding W5: line rows have a fixed field set and no per-line `meta` dict; the only free-form per-line channel is `compose_flags`, with episode-level `meta` available only at top level. Concrete fix: write per-line breadcrumbs as `compose_flags` entries, e.g. `role_coercion:<source>:<prev>-><new>`, and optionally aggregate counts/details in top-level `ledger.meta["role_coercions"]`. Do not add a new row field unless you are intentionally changing the ledger row contract.

3. [§2 Tier 2] The detector is specified for `_otr_line_composer.compose_line` 2015-2060 “on the RAW draft BEFORE `strip_line_formatting`,” but grounding B says `compose_line_draft` 1689-1928 already performs formatting/leak/oversize repair before the `compose_line` reroll block. That means the 2015-2060 site may no longer have the raw draft needed for quote-boundary-sensitive detection. Concrete fix: either move `detect_stage_business_for_reroll(...)` into `compose_line_draft` immediately after the LLM draft is received and before `strip_line_formatting`/quote normalization, or change the internal return contract so `compose_line` receives both `raw_draft` and cleaned text. Keep the existing one-reroll guard.

4. [§2 Tier 1, §7.2] The prompt-builder target is still ambiguous and one candidate is wrong for this concern. Grounding W2: the per-line prompt is `_otr_line_composer.py::_build_user_prompt(req)` 1050-1338; it already has “spoken words only” language at 1307-1315 and still leaked, so this is the correct place to strengthen. `_otr_outline._build_beat_user_prompt` writes beat intent, not dialogue; adding “write only spoken words” there is an interface mismatch and risks corrupting outline intent generation. Concrete fix: apply DEFECT 1 Tier 1 only to `_otr_line_composer._build_user_prompt`; reserve `_otr_outline._build_beat_user_prompt` only for optional static stance/arc guidance, not spoken-line hygiene.

5. [§3, §7.3] `speaker_role` coverage must be finalized around all known writers, not just the three preferred sites. Grounding W3 lists additional writers/mutators: `cast_lock.py:473`, `_otr_speaker_role.py:218`, `scene_sequencer.py`, `_otr_radio_editor.py`, `_otr_legacy_to_stage1_adapter.py`, `_otr_story_spine.py`, and outline Beat writers. A final sweep works only if it is placed after all final ledger-role mutations and before any role-dependent routing such as spoken-role scrub/TTS/freeze. Concrete fix: implement the helper in `production_ledger.set_lines`, block the `_otr_ledger_reviewer.py:1063` bad `expected="announcer"` repair for cast IDs, and add one final consistency sweep at the last ledger-normalization point before freeze/TTS routing. Explicitly exempt the legitimate `cast_lock.py:473` case where `char_id=="announcer"` is restamped to announcer.

6. [§4 Detection, §7.4] Adding `"stance"` to `FailedDimension` is runtime-safe but incomplete unless the critic prompt and parser/test fixtures are updated. Grounding W4: `FailedDimension` is a `Literal[...]`; the only reroll consumer uses a free-form `[dim]` prefix, so adding `"stance"` is safe, but the critic system-prompt prose at `_otr_story_critic.py:310-329` must also be updated so the model emits stance issues. Concrete fix: update the Literal, critic prompt prose, report model/parser for `StanceIssue`, and tests proving `meta.story_critic_report` round-trips the new field. Verify: parser behavior for unknown/new report fields if the report model is strict.

7. [§6] The clean-fixture assertion `"coherence_hints" not in ledger.meta` conflicts with the now-invalid auto-repair path. Once auto-repair is cut, this assertion is harmless but should not be presented as validating the rerun channel. Concrete fix: keep it only as a no-op regression assertion that detection-only does not mutate top-level meta with repair hints.

SHOULD-FIX:
1. [§4 Detection] The `StanceIssue.target` contract says v1 is constrained to “episode central object + protagonist,” but the wiring does not state how those names are provided to `run_story_critic`. [ASSUMPTION] If the prompt lacks explicit target names, the model may invent targets or fail to map line IDs. Concrete fix: pass existing cast/protagonist/episode-object context into the critic prompt, or relax the target field to a free-form string with validation only in tests.

2. [§3] The “CI-only invariant assert” needs an explicit gate. The plan says asserts are CI-only but does not name how CI is detected. Concrete fix: wire to an existing CI/test flag if present; otherwise use a local env flag such as `OTR_STRICT_LEDGER_ASSERTS=1`. Verify: existing repo convention for CI-only assertions.

3. [§6] Counters for “composer reroll attempts, deterministic strips, role coercions, full reruns” need a stable observation channel. Logs alone are brittle for fixtures. Concrete fix: use deterministic in-memory counters returned by the test harness, `compose_flags` for per-line mutations, and top-level `ledger.meta` only for aggregate episode diagnostics.

4. [§2 Tier 3] The new quote-anchored floor must be tested for interaction with existing unanchored delimited scrub. Existing `_STAGE_DIRECTION_RES` cuts brackets/asterisks/parenthesized cues anywhere before the new quote-aware pass. Concrete fix: add fixture lines where quoted dialogue contains parentheticals that should remain, or explicitly accept that delimited cues are stripped anywhere before quote segmentation.

5. [§2, §6] `story_quality_scan.py` must import the same helper used by production, but the plan only says “same detector helpers.” Concrete fix: make `detect_stage_business_for_reroll` and any classifier used for scan/live CI live in `_otr_line_hygiene.py`; do not duplicate regex/classifier logic in the scan script.

OPTIONAL / NICE-TO-HAVE:
- [§2 Tier 3] Add a small debug reason code from `detect_stage_business_for_reroll`, e.g. `leading`, `trailing_after_quote`, `embedded_between_quotes`, `undelimited_action_clause`, so fixture failures identify which seam regressed.
- [§3] Add a one-line summary count of role coercions in episode-level telemetry to avoid scraping every row’s `compose_flags`.

CUT THESE (over-engineering):
1. [§4] Cut the `needs_full_rerun` / `meta["coherence_hints"]` auto-repair mechanism from this build. It is not merely over-engineered; grounding W1 shows there is no existing cross-run channel. Detection + telemetry is the safe v1.
2. [§4] Cut any attempt to preserve stance hints through the old ledger JSON on disk. Grounding W1 says regenerated runs get a new pending directory and the old ledger is never reloaded; implementing this would require a new external state channel.
3. [§3] Cut per-row `meta` audit. Grounding W5 says it is not part of the row contract. Use `compose_flags` plus top-level aggregate meta instead.