<!-- requested_model: ~google/gemini-pro-latest | resolved_model: google/gemini-3.1-pro-preview -->

VERDICT: no. The P0 merge logic violates its own schema caps, the retry loop catches the wrong exception, and unbounded windowing will cause a massive latency/cost blowout.

MUST-FIX BEFORE BUILD:
1. **[3.2] Schema Validation Crash on Merge:** The merge logic limits facts but completely ignores the `max_length=4` schema caps for `entities` and `numbers` in `FactIndexV4`. If the selected facts bring along 6 numbers, or if deduplication leaves 10 entities, `_validate_fact_index` will fatally crash. **Fix:** Add explicit reduction logic to slice the deduplicated `entities` and `numbers` lists to a maximum of 4 before validation.
2. **[4] Exception Swallowing / Broken Retry Loop:** The plan places the outer campaign around `invoke_codex_structured` and expects to catch `StructuredCallFailedError`. Grounding shows `invoke_codex_structured` catches this and raises a generic `CodexPassError` for *all* exceptions. The loop will see `CodexPassError`, treat it as "Any other exception exits immediately", and never retry. **Fix:** Catch `CodexPassError` in the outer loop and inspect `__cause__` to distinguish recoverable `StructuredCallFailedError`s from fatal exceptions, or move the retry loop inside `invoke_codex_structured`.
3. **[3.1] Latency/Cost Blowout from Unbounded Windows:** The plan allows a 2 MiB `full_text` payload and mandates "The union of windows must cover every A0 full_text character; no gap is allowed," running a P0 LLM candidate for *every* window. 2 MiB of text will generate ~100-200 sequential LLM calls per article, hitting massive latency and cost walls. **Fix:** Introduce a hard cap on the maximum number of P0 windows processed per article (e.g., first N and last M windows), discarding the middle if the cap is exceeded.
4. **[4] Contradiction on P3 Graph Retries:** The plan states "Do not retry: ambiguous or invalid accepted P3 graph/compiler state". The grounding explicitly states "authored graph... defects are recoverable candidate failures" and "P3 is replayed only if its accepted graph itself is defective." The plan will cause episodes to fatally crash on bad P3 graphs instead of recovering. **Fix:** Remove invalid P3 graphs from the "Do not retry" list and route them to a fresh P3 candidate cycle.
5. **[3.1] Chunking Infinite Loop:** The plan mandates adapting `p0_source_chunks` to overlap by `MAX_QUOTE_CHARS - 1`. Grounding shows `p0_source_chunks` snaps backward to `_SENTENCE_END`. If a sentence is longer than `allowance - overlap`, `offset` will not advance, causing an infinite loop. **Fix:** Define fallback logic in `p0_source_chunks` to force a hard cut if sentence snapping fails to advance the window offset by at least 1 character.

SHOULD-FIX:
1. **[2] Unsafe Tie-Breaking:** "Choose the longest nonempty extracted RSS alternative" relies purely on character count. If one alternative is bloated HTML and another is clean text, length is a poor heuristic. **Fix:** Strip HTML tags before comparing lengths for the tie-breaker.
2. **[3.2] Number Re-mapping Specificity:** The plan says "assign `F01..F06`... and remap every selected number's parent `fact_id`". **Fix:** Explicitly state that the `fact_id` string inside each retained `NumberV4` must be mutated to match the newly assigned `F01..F06` ID of its parent fact bundle.

OPTIONAL / NICE-TO-HAVE:
- **[2] Telemetry:** When stamping the ledger with the selected body route, also stamp the number of P0 windows generated to monitor the impact of the new windowing logic on throughput.

CUT THESE:
- **[3.2] Evenly spaced window ordinals:** "choose evenly spaced window ordinals including the first and last". This is over-engineering for a deterministic merge. **Why it's safe to cut