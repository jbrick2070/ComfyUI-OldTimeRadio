<!-- requested_model: deepseek/deepseek-v4-pro | resolved_model: deepseek/deepseek-v4-pro -->

VERDICT: build-ready as-is? **no**. Multiple implementation-critical details are missing or ambiguous: the window budget is undefined, span field identification for rebasing is unspecified, the head/middle/tail preview format is not given, the envelope size limit differentiation is not designed, and the retry control flow lacks state management. These gaps prevent coding directly from the plan.

MUST-FIX BEFORE BUILD:
1. [Section 3.1] `p0_source_char_budget` is never assigned a concrete value. The windowing function `p0_source_chunks` requires `budget_chars`, and the overlap logic depends on it. Without a number, window size and step cannot be computed. **Fix:** Define `p0_source_char_budget` as a constant (e.g., 4000) derived from the model’s reliable structured-extraction context window, and document the choice.
2. [Section 3.1] The instruction “For a `full_text` span only, add the window offset” assumes each span (fact/entity/number) carries a source field identifier. The current `FactV4`/`EntityV4`/`NumberV4` schemas (FactIndexV4) may not include such a field. If absent, the rebaser cannot distinguish `full_text` spans from headline/summary spans. **Fix:** [ASSUMPTION: spans lack a source field] Add a `source_field: str` to the span models and ensure P0 populates it. Update the plan to state that only spans with `source_field == "full_text"` are rebased.
3. [Section 2] The “bounded head/middle/tail preview” replacement for the body reranker’s first-800 view is not specified. Lengths, separators, and concatenation order are missing; the reranker may depend on a particular format. **Fix:** Define the preview explicitly (e.g., first 300 chars + " … " + middle 300 chars + " … " + last 300 chars). Verify the reranker can consume this format.
4. [Section 3.1] The plan introduces a 2 MiB + 128 KiB limit for the canonical A0 payload while keeping the 48 KB limit for `operator_pinned`. The current `validate_payload_envelope` (grounding) rejects anything >48 KB. No mechanism is described to route canonical payloads through a different size check. **Fix:** Add a `max_size` parameter to `validate_payload_envelope` or create a separate validation path. Enforce 48 KB only when `operator_pinned` is present; enforce the larger limit for the full seven-field A0 payload in the canonical lane.
5. [Section 4] The “opt-in canonical-lane outer campaign” requires preserving accepted upstream artifacts while retrying a single failed pass. The plan provides no state management design (data structures, storage of intermediate results, feedback injection). The current sequential call chain does not support partial retry. **Fix:** Design a `CampaignState` object that holds the accepted result of each pass. When a pass fails, the loop re-invokes only that pass with the same immutable inputs and the bounded findings from the failed cycle, leaving earlier results untouched. Sketch the loop structure and feedback prompt assembly.
6. [Section 3.2] “Use the first accepted nonempty local tone” – if all windows produce an empty tone, the final `FactIndexV4` will have an empty `tone` field, violating the schema. **Fix:** Define a fallback tone (e.g., `"neutral"`) or treat an all-empty-tone result as a candidate failure that triggers a retry of the window passes.
7. [Section 3.1] The plan relies on `_validate_fact_index` to validate the rebased FactIndex against the full A0 payload. The grounding does not show this function. [ASSUMPTION: it exists] If it does not exist or cannot handle global spans, the final validation step fails. **Fix:** Confirm `_validate_fact_index` is implemented and accepts a FactIndex with spans rebased to the full A0 coordinates. If not, implement it or adapt the existing validation.

SHOULD-FIX:
8. [Section 3.1] Performance ceiling – with a typical budget (e.g., 4000 chars) a 2 MiB article yields ~500 windows, each requiring an LLM call. Latency and cost will be prohibitive. **Fix:** Either set a much larger budget (e.g., 100 k chars if the model permits) or cap the number of windows (e.g., 20) with a coverage strategy (first, middle, last). Document the trade-off.
9. [Section 4] “No fatal fixed outer model-candidate count” risks an infinite loop on persistent model failures. **Fix:** Add a configurable `max_cycles_per_pass` (default 5). Exceeding it raises a fatal error, keeping the process bounded.
10. [Section 2] The body reranker currently uses the first 800 characters; replacing it with a head/middle/tail preview may break heuristics that expect contiguous text. [ASSUMPTION: reranker expects contiguous text] **Fix:** Review the reranker implementation. If it requires contiguous text, retain the first 800 for ranking but use the full body for the canonical lane, or adapt the reranker to the preview format.
11. [Section 5] The ledger must record “per-pass cycle totals and a bounded hash/error summary” but the new field names and types are not defined. The ledger assembly may reject unknown fields. [ASSUMPTION: ledger schema is strict] **Fix:** Define the new fields (e.g., `cycle_counts: dict[str, int]`, `error_summary: str`) and update the ledger model to accept them.
12. [Section 3.1] The existing `p0_source_chunks` truncates windows at sentence boundaries, altering the actual window length. The overlap formula (`MAX_QUOTE_CHARS - 1`) must account for this variability to avoid gaps and ensure full quote coverage. **Fix:** Clarify whether sentence-boundary truncation is kept. If kept, adjust the step to `max(1, len(window) - overlap)` and prove the union of windows still covers every character.

OPTIONAL / NICE-TO-HAVE:
- Provide a sequence diagram for the campaign retry loop.
- Add debug logging of cycle nonces and failure reasons.

CUT THESE (over-engineering):
- None; the plan is appropriately scoped.