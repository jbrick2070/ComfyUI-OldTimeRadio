<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.6-sol -->

VERDICT: build-ready as-is? no. Window admission, retry budgeting, span-field authority, and final validation order have unresolved integration defects.

MUST-FIX BEFORE BUILD:
1. [§6 P5 safety and final ledger] Spoken text is canonicalized after graph and safety revalidation, so the bytes assembled into the production ledger are not the bytes that passed those checks. Move canonicalization before the final graph/safety validation, then assemble only that validated form. If canonicalization can alter graph-sensitive markup, rerun both validators afterward.
2. [§2 P0 projection + §4 windowed P0] Per-window de-aliasing can authorize fields that full-A0 de-aliasing rejects. Example: `seed_text` contained somewhere in complete `full_text` is omitted by the full projection, but is retained in a window that does not contain that substring; the local model can then emit a `seed_text` span that fails full-A0 validation. Compute the authoritative allowed-field set from full A0 once, and constrain every window prompt, repair, validator, and merge to that same set.
3. [§3 overlapping windows + §5 fresh-candidate campaign] Window sizing does not reserve space for the cycle-2 `writer_retry` mapping. A cycle-1 prompt that fits can become prompt-no-room after a recoverable failure, which §5 classifies as permanent. Deduct the maximum serialized retry mapping—including the 1,200-character rejection, nonce, keys, and prompt framing—from every retry-enabled pass’s input budget before forming cycle-1 inputs.
4. [§3 overlapping windows + §4 windowed P0] The production source of `budget_chars` is not wired. Correctness requires both `frame_chars + overlap_chars < budget_chars` and enough remaining model context for the P0 prompt/schema/output budget. Derive it from the selected model/provider generation budget and prompt pack, validate it before starting any P0 window, and raise the existing permanent prompt/configuration error when the invariant cannot hold.
5. [§4 deterministic merge] Local fact IDs are reused independently in every P0 result, but number-parent resolution is not explicitly namespaced. Key parent lookup by `(window_ordinal, local_fact_id)` and map that key to the canonical fact bundle before deduplication or ID rewriting. Define “canonical fact-bundle identity” as a concrete tuple; never use bare `F1`, `F2`, etc. across windows.
6. [§1 common source acquisition] `_select_rss_content` does not define malformed top-level `entry["content"]` handling even though the proof requires malformed alternatives. Calling `len()` and iterating a null, string, or mapping would either fail or produce meaningless raw indices. Specify that only the feed parser’s accepted sequence type is enumerated; null or any other container type yields `("", None, 0)` or a clearly specified loud failure.
7. [§1 common source acquisition] `_select_news_body` returns four route values but supplies no deterministic rule distinguishing `summary_fallback` from `summary_only`. Define the mapping for no URL, failed fetch, empty article, and a successful article that loses by character count. Use that single result to populate both `full_text` and `meta.news_seed.body_source`.
8. [§5 typed fresh-candidate campaign] Cancellation identity can still be destroyed by the inherited broad `except Exception` path in `_invoke_codex_structured_once`; grounding shows the current wrapper converts all such exceptions to `CodexPassError`. Verify the Comfy interruption exception hierarchy and explicitly re-raise it before generic conversion in every wrapper layer. Catch `ModuleNotFoundError` only around the Comfy import, not around polling, or a missing transitive dependency can be mistaken for “Comfy absent.”
9. [§5 typed fresh-candidate campaign] “Exactly today’s journal behavior” conflicts with the new requirement that failed entries contain no accepted artifact and every entry contain cycle/nonce/disposition fields. Refactor journal entry creation so cycle metadata is available before the first model call and one layer owns terminal entry finalization. Verify whether current journal behavior stores raw failed output; if so, remove it rather than preserving today’s behavior.

SHOULD-FIX:
1. [§4 deterministic merge] Selected windows can have no “surviving fact/entity” after cross-window deduplication, especially with overlap. The `count > 6` policy then silently returns fewer rows and may omit the intended late-window sample. Define deterministic forward-then-backward fallback to the nearest window with an unselected surviving row, while retaining schema caps.
2. [§4 deterministic merge] Clarify whether “nonempty window count” means emitted source windows, locally accepted indices, or windows with surviving post-deduplication rows. `_evenly_spaced_indices` must receive one unambiguous count and return indices into the same collection.
3. [§4 deterministic merge] The one-window path promises the same final artifact, but unconditional merge-time ID reassignment can change otherwise accepted IDs. Either bypass merge normalization for one window or verify that the existing validator already requires the identical contiguous ordering.
4. [§5 fresh-candidate campaign] Unbounded recoverable cycles have no retry delay or rate-limit pacing. Add cancellation-aware capped backoff, and honor provider retry-after information where available, without adding a fatal cycle limit.
5. [§5 fresh-candidate campaign] Specify how `StructuredCallFailedError.last_error` is unwrapped. Verify whether JSON, Pydantic, post-validation, and rerollable capacity failures arrive directly or through exception chaining; classify the actual terminal cause consistently.
6. [§1 common source acquisition] Fetching all five linked articles serially can add roughly five timeout periods before reranking. Verify current execution mode; if parallelized, use a bounded fan-out and preserve shortlist order for deterministic ties. Do not introduce an unbounded executor.
7. [§1 common source acquisition] Ensure `_rss_content_index` and `_rss_content_count` survive candidate copying, body reranking, and final selection. Stamp public `meta.news_seed` fields from the selected candidate only, not from loop-local or headline-ranked state.
8. [§6 P5 safety] Define the adapter from a raw P5 candidate to `scan_spoken_ledger` explicitly. Verify the scanner’s expected row structure and normalization stage; scan the same canonical spoken representation that will be assembled.
9. [§6 final ledger] Disambiguate the later “run cleanup” from `_apply_script_safety_cleanup`. The spoken cleanup must occur exactly once before final validation; deterministic tail finalization must not be described as another