<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.5-20260423 -->

VERDICT: no. Wiring is not build-ready: render_driver already has the critical seams, but the plan leaves request mutation scope, assert_usable request_template, force-map validation, portrait resolution, ledger identity, and LOUD restamp wording underspecified or contradicted by grounding.

MUST-FIX BEFORE BUILD:
1. [Additive touch list / render_driver.py (c) music-beat audio_ref attach] Exact call site is `render_driver.build_request_from_shot()`, after `audio = _voice_audio_for_line(line)` and before `req = build_request(...)`. Current code slices the frozen master whenever `not audio` for any shot, then passes `audio_ref` to `build_request()` for all engines. The additive MUSIC change must be guarded on `shot["engine_id"] == "ltx_av_music"` and must not change the existing HuMo/talking behavior. Concrete fix: preserve today’s existing `audio` resolution path byte-identically for existing engines; add a separate branch that fills `audio` for `ltx_av_music` only when it is missing and `master_audio_path` + timing are available. Do not add `audio_ref` to `ltx_video`, `wan_i2v`, or other existing engines beyond what they receive today. Verify request JSON/hash golden fixtures for dark-lane OFF.

2. [render_driver.py build_request_from_shot / ShotLock hashes] Do not claim `render_request_hash` changes when `audio_ref` is attached. Grounding shows `render_request_hash` is stamped by `otr_shot_lock.py` from creative `request_hash`; `build_request_from_shot()` only reads it and derives `seed_bundle.request_seed` from it via `_seed_from_hash()`. Adding `audio_ref` to the request does not update ledger `render_request_hash` in shown code. Concrete fix: document/test this explicitly: existing-engine shot rows keep identical `render_request_hash`, `cache_keys.request_hash`, and `seed_bundle.request_seed`; `ltx_av_music` gets audio in the runtime request without retroactively changing ShotLock cache keys. If cache identity must include audio for the new engine, add a new engine-local cache key, not a mutation of existing `render_request_hash`.

3. [render_driver.py _render_one / ARCHITECTURE assert_usable order] Plan requires new engines’ `assert_usable` to validate `av_dims` against `request_template.canvas`, but `_render_one()` currently calls `eng.assert_usable(host_caps={}, profile={})` with no request. That makes the required CPU-fail-before-lease dimension gate impossible. Concrete fix: add a compatibility wrapper in `_render_one()` that passes `request_template=request` to engines that accept it, while preserving legacy engines that only accept `(host_caps, profile)`. Then add a unit proving `ltx_av_*` receives the request canvas during `assert_usable`.

4. [ENGINE_FAMILY / fallback identity] `render_driver.ENGINE_FAMILY` does not include `ltx_av_talk` or `ltx_av_music` in grounding. This breaks family restamping for forced/overridden/fallback paths before registry availability and makes `OTR_FORCE_ENGINE_MAP` parse behavior depend on whether the guarded adapter import registered. Concrete fix: add `"ltx_av_talk": "audio_driven_face"` and `"ltx_av_music": "audio_conditioned_video"` to `ENGINE_FAMILY`, and add tests for `engine_family()` before and after adapter registration.

5. [OTR_FORCE_ENGINE_MAP / role compatibility] `apply_engine_override()` rewrites `shot["engine_id"]` by role and only checks parse-time “known engine”; it does not run role compatibility. This bypasses `OTR_VideoDirector._resolve_and_validate()` role filtering. It does not bypass `_render_one().assert_usable()`, but that is render-time and currently has no role/request template. Concrete fix: in `apply_engine_override()`, validate forced `(role, engine)` with the same `_rc.engine_fits_role()` descriptor logic as `OTR_VideoDirector`, and fail-closed by ignoring/logging incompatible entries. Test: `OTR_FORCE_ENGINE_MAP=announcer_visual=ltx_av_talk` applies; `music_visual=ltx_av_talk` is rejected; flag OFF still reaches engine `assert_usable` and falls back rather than silently rendering.

6. [DIRECTOR -> SHOTLOCK / flag-off behavior] The plan says “assert_usable path do when flag is off (GATED_BY_FLAG) -- clean fail-closed to role default engine, or episode abort?” Grounding shows Director only validates registry/role compatibility; ShotLock does not call registry `assert_usable`; render-time `_render_one()` calls engine `assert_usable`; `render_shot()` catches and walks fallback. Therefore expected behavior is render-time fallback, not ShotLock default swap and not episode abort. Concrete fix: codify this in the plan and test it: with `OTR_ENABLE_LTX_AV` unset and a shot stamped `ltx_av_talk`, `_render_one()` raises the gated unusable exception, `render_shot()` restamps to `humo`, appends `runtime_fallback_decisions`, logs `_rt.format_swap_log(rec)`, and the episode completes.

7. [DIRECTOR slots / role compat] Need explicit test coverage for dropdown + execute filtering. Grounding: `OTRVideoDirector._video_model_combo()` returns `all_engine_names()`; `_resolve_and_validate()` checks each slot against `VIDEO_SLOT_ROLES`. Concrete fix: after adapter registration, assert `ltx_av_talk` is accepted for `announcer_video_model`; assert it is accepted for `other_beats_video_model` only because that slot includes `character_video`; assert `ltx_av_music` is accepted for `music_video_model`; assert incompatible slot picks raise `ValueError`. VERIFY-AT-BUILD: `role_compat.py` must allow required inputs for these roles, especially `audio_ref` for `music_visual` and `init_image/audio_ref` for `announcer_visual` + `character_video`.

8. [Fallback mechanics / resolver prune] The plan asks whether group restamp happens via `resolver.prune_orphaned_groups()`. Grounding shows `render_driver.render_shot()` restamps only the shot row via `_rt.restamp_shot_row()` and appends runtime decisions in `run_episode()`; it does not call `resolver.prune_orphaned_groups()` anywhere. Concrete fix: either remove the group-prune claim for these two lanes if no provider groups are introduced, or add a render-time section restamp that calls `validate_execution_groups(..., prune_orphans=True, ...)` when a fallback changes group topology. Do not claim group pruning is wired today.

9. [Fallback mechanics / LOUD restamp wording] Exact restamp wording for `(a)` talk aspect-change degrade, `(b)` music `ltx_video` degrade, `(c)` pad-tail >2s is not implementable from shown code. Only render exceptions produce `_rt.build_fallback_decision()` + `_rt.restamp_shot_row()`; canonicalize-time policy degradations have no path back into `shot["degradation_trail"]`. Concrete fix: define and implement a concrete propagation contract, e.g. canonical clip may return `degradation_events`, and `render_shot()` appends them to `out_shot["degradation_trail"]` and logs LOUD. Required strings should be fixed in tests, e.g.:
   - `ltx_av_talk->humo (aspect_change: landscape_to_pillarbox)`
   - `ltx_av_music->ltx_video (dependency_missing|oom|invalid_dag|crash_before_load: <detail>)`
   - `ltx_av_music pad_tail_last_frame (>2s: rendered=<n>, target=<T>)`
   Adjust exact failure kind to `retry_taxonomy` once inspected. VERIFY-AT-BUILD: `_rt.restamp_shot_row()` format.

10. [Engine identity ledger stamps] Acceptance grep cannot rely only on runtime logs. Grounding gives current identity stamps: `ledger["video"]["shots"][].engine_id` is restamped to the final engine; `degradation_trail` records fallback trail; `runtime_fallback_decisions` records swaps; `build_clip_manifest()` emits per-clip `engine_id` using `clip["engine_id"] or shot["engine_id"]`. Concrete fix: require `ltx_av_*` `canonicalize()` to return `{"engine_id": ..., "family": ...}` and add a manifest/ledger test proving every clip row has final `engine_id`, including after fallback. If a fallback occurs, final shot `engine_id` must be fallback engine and trail must retain original `ltx_av_*`.

11. [Portrait/init_image supply for announcer] `render_driver._portrait_index()` only indexes `ledger["images"]["images"]` by `object_id` or `char_id`; `build_request_from_shot()` uses `char_id = line.get("char_id")`. Announcer lines commonly may have no cast `char_id`; grounding `portrait_ledger.py` says non-cast announcer portraits are recorded via `ledger["images"]`, but no alias lookup is shown in render_driver. Concrete fix: add an announcer alias resolution path for `role == "announcer_visual"` when `char_id` is empty, matching the image ledger object id used by the shipped announcer portrait. Test with a ledger containing an announcer image but no line `char_id`: `ltx_av_talk` request gets `asset_refs.init_image`.

12. [Portrait missing behavior] Plan must specify and test fail-closed behavior when announcer `init_image` is missing. Grounding says render fallback catches `EngineUnusable`, `FileNotFoundError`, etc. Concrete fix: `ltx_av_talk` must raise a classified hard failure before GPU work when `audio_ref` or `init_image` is empty, causing `render_shot()` to restamp to `humo` then continue chain if needed. Do not let it “starve” by returning an empty clip or hanging in graph prep.

13. [Seeds] The plan’s wording “request_seed derives from render_request_hash (build_request)” is false for the grounding. `build_request()` derives seed from numeric suffix of `shot_id`; `build_request_from_shot()` derives from `render_request_hash` or shot id. Concrete fix: update wording/tests to target the real episode path: `build_request_from_shot()` gives deterministic per-shot seeds to new engines automatically; C7 audio env overrides are irrelevant unless they mutate ShotLock `render_request_hash` upstream. Add a test that adding `audio_ref` for `ltx_av_music` does not change `seed_bundle.request_seed`.

SHOULD-FIX:
1. [render_driver.py prompt/canvas tuple] Plan says add `ltx_av_talk` and `ltx_av_music` to the landscape canvas tuple at `:387`. Grounding currently only applies landscape canvas to `("ltx_video", "wan_i2v")`. For `ltx_av_talk`, full landscape may conflict with portrait talking-head/pillarbox policy. Concrete fix: split canvas policy: `ltx_av_music` uses landscape; `ltx_av_talk` uses the intended talk canvas or explicitly restamps aspect-change degradation if forced landscape.

2. [render_driver.py prompt gate] Plan says add `ltx_av_music` to the existing `("ltx_video","wan_i2v")` no-creative branch and add sibling `ltx_av_talk`. This will change only selected new-engine requests if guarded by engine id. Add golden tests proving dark-lane existing `ltx_video`/`wan_i2v` request prompts are byte-identical.

3. [OTR_FORCE_ENGINE_MAP parse] If adapter registration is flag-gated and skipped when `OTR_ENABLE_LTX_AV` is off, `parse_engine_override()` may reject `ltx_av_talk` as unknown unless `ENGINE_FAMILY` is updated. The ENGINE_FAMILY fix above should be tested with flag OFF.

4. [Fallback log proof] The current LOUD proof is `_LOG.warning(_rt.format_swap_log(rec))`; the plan should name that as the authoritative log line for render-time fallback, not invent new independent wording unless implemented in retry_taxonomy.

5. [ShotLock family stamping] `otr_shot_lock.build_execution_plan()` stamps `"family": ""` for all shots. Runtime restamp fixes family after fallback, but first-attempt identity lacks family. Consider stamping family at ShotLock when the engine is registered. Keep dark-lane hashes stable if doing so.

6. [Synthetic opening music] `derive_opening_music_beat()` stamps synthetic `start_s/dur_s` on the shot row; `build_request_from_shot()` already falls back to shot timing when no line exists. Ensure the `ltx_av_music` audio slice branch uses this path, not only `line.start_s/dur_s`.

OPEN-QUESTIONS:
1. VERIFY-AT-BUILD: actual `role_compat.py` role input allowances for `announcer_visual`, `character_video`, and `music_visual`; grounding does not include it.
2. VERIFY-AT-BUILD: actual `_rt.restamp_shot_row()` and `_rt.format_swap_log()` string formats before freezing exact acceptance greps.
3. VERIFY-AT-BUILD: adapter registration/import policy in `nodes/_otr_video_engines/__init__.py`; grounding does not show whether new engines register when flag is OFF.
4. VERIFY-AT-BUILD: actual image ledger object id for the shipped non-cast announcer portrait from 435ba0a so render_driver can map the announcer alias correctly.
5. VERIFY-AT-BUILD: whether existing engines currently ignore unexpected `audio_ref`; do not rely on that for byte-identical dark-lane behavior.