<!-- requested_model: ~openai/gpt-latest | resolved_model: openai/gpt-5.6-sol -->

VERDICT: no. The document proves a defect but leaves the authority model, repair behavior, data contract, and acceptance criteria unresolved, so implementation would still require architectural decisions during build.

MUST-FIX BEFORE BUILD:
1. [§4, §5.3] The proposed solution is still a fork, not a build plan. “B+E” does not define where pitch-name authority enters casting or what happens after detection. `_build_user_prompt` receives only `casting_brief`; `lock_cast`, `cast_one_character`, and `llm_write_description` do not receive `selected_concept.cast`, so they cannot safely distinguish pitch names from legitimate names in story prose. Concrete fix: add an explicit `non_roster_character_names` input derived from `SelectedConcept.cast`, thread it only to `llm_write_description` / `_build_user_prompt`, and sanitize the local `story_text` copy before constructing the per-character prompt. Do not mutate the shared `casting_brief`.

2. [§4 option E, §6 THE LAW] “Detect-and-reroll” is incompatible with the no-fail constraint unless a terminal fail-soft behavior is specified. `llm_write_description` delegates to `structured_call` and raises `CastingFailedError` after exhaustion; `lock_cast` can promote that to `CastValidationLLMError`. Adding a name validator to this retry ladder can therefore reduce publication. Concrete fix: prevention on every attempt, followed by deterministic output repair after the final successful schema parse. The repair must always return a non-empty description; it may remove or replace known pitch names and log the mutation, but must never raise or request an unbounded regeneration.

3. [§4 option B, §5.2] The stripping algorithm is undefined. A generic proper-name or “name-shape” remover can destroy legitimate role text, place names, source-owned characters, and possessives. Concrete fix: match only an episode-scoped denylist derived from `SelectedConcept.cast`, using documented normalization for case, punctuation, apostrophes, honorifics, nicknames, and full-name versus token matching. Replace each matched full identity with a neutral role label carried alongside that pitch entry; if no role label is available, delete only the matched span and normalize punctuation. Do not use the corpus-wide name-shape detector as the production sanitizer.

4. [§5.1, §4 option A] The continuity mapping is not grounded and is not defined as an authority contract. None of the supplied excerpts shows `meta.continuity`, its writer, or a mapping from pitch identities to roster identities. The quoted `"Lucille (Nia Philbin)"` could be reconciliation output, downstream inference, or another hallucination. Concrete fix: remove option A from the critical path. [ASSUMPTION] Verify: the symbol that writes `meta.continuity.facts`, its inputs, whether pairing is keyed by stable character IDs or inferred from prose, and whether it runs before or after `lock_cast`. Only reconsider A if that producer exposes an explicit one-to-one, ID-backed mapping with ambiguity handling.

5. [§5.2] The requested consumer analysis is absent, and the supplied grounding cannot establish it. `_build_user_prompt` proves one consumption path through `llm_write_description`, but not every repository consumer of `casting_brief`. Concrete fix: inventory reads of the `casting_brief` field and parameters named `casting_brief`, classifying each as shared-brief consumption or prompt-local consumption. [ASSUMPTION] Verify: writer/script, continuity, visual planning, metadata persistence, and any source-payload consumers. Until that inventory exists, prohibit mutation of `OriginalBriefsModel.casting_brief`; sanitize only `_build_user_prompt`’s local `story_text`.

6. [§3, §5.4] The plan combines at least two defect classes without defining their boundary: known pitch-name intrusion and arbitrary cross-row description copying. A denylist of `SelectedConcept.cast` can catch the former but cannot prove that another roster row’s complete face prose was copied. Concrete fix: split acceptance into two classifiers:
   a. source-name intrusion: a description contains a non-roster name from the episode’s selected concept;
   b. cross-row prose duplication: descriptions share a defined normalized span or digest beyond an explicit threshold.
   Fix (a) in this item. Log and separately scope (b) unless provenance proves the same input path.

7. [§3, §6 `otr/obs/`] There is no release criterion connecting correctness to publication safety. “Compute the union” is not enough, especially when only 124 of 1,710 ledgers expose pitch metadata. Concrete fix: define before/after measurements: zero known selected-concept non-roster names in newly generated descriptions and portrait prompts; unchanged episode publication count/rate; counts for prevention matches, output repairs, unresolved detections, and detector applicability. Keep pitch-detector and heuristic-detector totals separate as well as reporting their union.

8. [§1, §6] The treatment of already-published bad ledgers is unspecified even though the defect is framed as reaching published pixels and surviving freeze copies. A prospective code change will not repair existing `character_description` or `portrait_prompt` values. Concrete fix: explicitly choose and document one policy: prospective-only with affected ledger IDs recorded, or an offline idempotent metadata repair plus targeted portrait re-render. The migration must not run inside publication and must not alter dialogue or unrelated story